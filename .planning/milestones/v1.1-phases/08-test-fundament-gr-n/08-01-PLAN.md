---
phase: 08-test-fundament-gr-n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/actions/combat-actions.js
  - features/render-dashboard.js
  - assets/styles/migration.css
  - systems/migration/migration-wizard.js
  - tests/unit/action-registry-collisions.test.js
  - tests/e2e/app.spec.js
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "Typing a STR/DEX/CON/INT/WIS/CHA score in the character form updates the +N modifier badge (crud/party.spec.js Attribut-Modifikatoren test green)"
    - "Typing an ability score in the encounter/monster form updates its +N modifier badge (crud/encounters.spec.js Attribut-Modifikatoren test green)"
    - "With the migration-hint banner shown, #global-search is clickable/focusable via a natural Playwright click (no force, no banner-dismiss workaround)"
    - "undo()/redo() while the dice or timers tab is active re-renders that panel (renderAll dispatches renderRandomTables + renderTimers) — behavioral proof lands in 08-02 once the tab-navigation :552/:515 selector fix is present"
  artifacts:
    - "tests/unit/action-registry-collisions.test.js exists and fails CI on any non-allowlisted duplicate data-action key across ui/actions/*.js"
    - "assets/styles/migration.css has a body.has-migration-hint layout-offset rule"
    - "features/render-dashboard.js renderAll() dispatches renderRandomTables and renderTimers"
    - "tests/e2e/app.spec.js has a new banner-overlap regression test asserting the banner does not cover #global-search"
  key_links:
    - "combat-actions.js duplicate registration removed -> entity-actions.js:447/457 correct single-arg handlers win -> updateAttrMod(attr name) resolves the input+badge"
    - "showMigrationHintBanner() toggles body.has-migration-hint -> migration.css padding offset -> sticky header (#global-search) sits below the fixed banner"
    - "undo/redo -> renderAll() -> renderSafe(renderRandomTables/renderTimers) -> active dice/timers panel refreshed"
  prohibitions:
    - statement: "Failing tests must NOT be silenced via .skip/.only/deletion, nor made to pass by weakening or removing assertions, to reach 0 fails"
      status: flagged-unverified
    - statement: "The migration-banner overlap must NOT be masked in tests via {force:true} or extended waitForTimeout instead of the layout-offset app fix"
      status: flagged-unverified
    - statement: "App behavior must NOT change in a user-visible way beyond restoring correct behavior (milestone v1.1 is verhaltensneutral)"
      status: flagged-unverified
  flagged_assumptions:
    - requirement: TEST-01
      status: unresolved
      note: "Edge-probe left TEST-01 unclassified (unresolved) — no explicit edge acceptance criterion was derivable from source artifacts. Treated as a flagged assumption: TEST-01 completeness is asserted via full-suite-green-twice + per-fail triage classification (D-07), not via a probed edge predicate. Requires reviewer confirmation the acceptance interpretation is correct."
---

<objective>
Fix the three confirmed app bugs behind the 11 pre-existing E2E fails (per D-01: real app bugs are fixed in Phase 8, however deep), and give each a dedicated root-cause regression test (per D-02). All three are small, mechanical, source-level diffs pinpointed to exact line numbers in 08-RESEARCH.md (Pitfalls 1/2, 3, 7):
1. A duplicate `update-attr-mod` / `update-enc-attr-mod` registration in `combat-actions.js` silently overwrites the correct handler in `entity-actions.js` (last-write-wins), breaking the live attribute-modifier badge.
2. The fixed-position `#migration-hint-banner` has no layout offset, so it covers the top ~48px of the header (including `#global-search`) on every fresh session — a real user-facing interaction bug.
3. `renderAll()` (used by undo/redo/import) omits `renderRandomTables` and `renderTimers`, so those panels go stale after an undo while their tab is active.

Purpose: Turn the 4 app-bug-caused fails (party/encounters attribute-modifier, global-search, undo-re-render) green at the root, and add regression tests that catch the *class* of each bug going forward.
Output: 3 source fixes + 1 new Jest collision test + 1 new app.spec.js regression test.

FLAGGED ASSUMPTION (edge-probe unresolved): TEST-01 was left `unclassified` by the deterministic edge-probe. It is surfaced here as an explicit flagged assumption — TEST-01's "done" is interpreted as "all freshly-measured E2E fails are 0, each classified test-bug vs app-bug." A reviewer should confirm this interpretation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-test-fundament-gr-n/08-RESEARCH.md
@.planning/phases/08-test-fundament-gr-n/08-CONTEXT.md
@.planning/phases/08-test-fundament-gr-n/08-PATTERNS.md
@ui/actions/combat-actions.js
@ui/actions/entity-actions.js
@features/render-dashboard.js
@systems/migration/migration-wizard.js
@assets/styles/migration.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attribut-Modifikator-Kollision entfernen + Kollisions-Regressionstest (Pitfall 1/2, D-02)</name>
  <files>ui/actions/combat-actions.js, tests/unit/action-registry-collisions.test.js</files>
  <read_first>
    - ui/actions/combat-actions.js (the CombatActions object; the two attribute-modifier registrations sit just below the `roll-skill` entry, both calling the two-argument form `updateAttrMod(ctx.value, ctx.target.id)` / `updateEncAttrMod(ctx.value, ctx.target.id)`)
    - ui/actions/entity-actions.js (confirm it already registers the CORRECT single-argument handlers `updateAttrMod(ctx.target.dataset.value)` and `updateEncAttrMod(ctx.target.dataset.value)` — do NOT modify this file)
    - ui/event-delegation.js (`registerAction` has no collision guard; `_handleInput` sets `ctx.value = target.value` — the typed number — so a handler needing the attribute name must read `ctx.target.dataset.value`)
    - features/party/party-crud.js (`updateAttrMod(attr)` takes exactly one parameter and early-returns when `$('char-'+attr)` is null)
    - tests/unit/action-registry.test.js (analog: fs/path header, German test names, phase-reference banner comment)
    - 08-RESEARCH.md — Pitfall 1, Pitfall 2, and the "EventDelegation Last-Write-Wins Collision Detection" scan script (already verified against this codebase; found exactly these 2 broken collisions plus 1 harmless `apply-quick-action` duplicate)
  </read_first>
  <action>
    Remove the two attribute-modifier entries from the `CombatActions` object in `ui/actions/combat-actions.js` (the pair that calls the two-argument `updateAttrMod(ctx.value, ctx.target.id)` and `updateEncAttrMod(ctx.value, ctx.target.id)`). These are dead-weight duplicates that break the feature; `entity-actions.js` already registers the correct single-argument handlers, which win once these are gone. Do NOT change `entity-actions.js`, and do NOT change the `updateAttrMod` / `updateEncAttrMod` function bodies.

    Then create `tests/unit/action-registry-collisions.test.js` as a pure static-analysis Jest test (fs + path only, no `vm`, no DOM), mirroring the analog's German test names and a Phase-8 / D-02 / Pitfall-1 banner comment. It scans every `ui/actions/*.js` file with the regex `/^\s*'([a-zA-Z0-9_-]+)':/gm`, groups keys by file, and asserts no data-action key is registered in more than one distinct file. Provide a single explicit allowlist constant (e.g. `ALLOWED_DUPLICATE_ACTIONS`) containing only `apply-quick-action` (the pre-existing harmless duplicate between combat-actions.js and system-actions.js, idempotent per RESEARCH) with an inline comment explaining why it is tolerated; the test must fail for any duplicate NOT on that allowlist. Optionally also de-duplicate `apply-quick-action` at source and empty the allowlist — implementer's choice, but the allowlist must document any tolerated duplicate.
  </action>
  <verify>
    <automated>npx jest tests/unit/action-registry-collisions.test.js && npx jest && npx playwright test tests/e2e/crud/party.spec.js tests/e2e/crud/encounters.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `ui/actions/entity-actions.js` still registers both correct single-argument handlers (grep finds `updateAttrMod(ctx.target.dataset.value)` and `updateEncAttrMod(ctx.target.dataset.value)`)
    - `tests/unit/action-registry-collisions.test.js` exists; `npx jest tests/unit/action-registry-collisions.test.js` exits 0
    - The collision test asserts zero non-allowlisted duplicate data-action keys across `ui/actions/*.js`, and would fail if such a duplicate were reintroduced (the allowlist names only `apply-quick-action`)
    - `npx playwright test tests/e2e/crud/party.spec.js -g "Attribut-Modifikatoren"` exits 0
    - `npx playwright test tests/e2e/crud/encounters.spec.js -g "Attribut-Modifikatoren"` exits 0
    - `npx jest` (full unit+integration suite) exits 0 — no regression
  </acceptance_criteria>
  <done>The character and encounter attribute-modifier badges update live on input; the duplicate-key collision is gone and structurally prevented by the new Jest test.</done>
</task>

<task type="auto">
  <name>Task 2: Migration-Hint-Banner Layout-Offset + Global-Search-Regressionstest (Pitfall 3, D-02)</name>
  <files>systems/migration/migration-wizard.js, assets/styles/migration.css, tests/e2e/app.spec.js</files>
  <read_first>
    - systems/migration/migration-wizard.js (`showMigrationHintBanner()` inserts `#migration-hint-banner` as `document.body.firstChild` and sets a sessionStorage flag; the `close-migration-hint` EventDelegation handler removes the banner)
    - assets/styles/migration.css (the `.migration-hint-banner` rule is `position:fixed; top:0; z-index:990; min-height:48px`; the existing sibling-combinator precedent `.migration-hint-banner ~ .divergence-banner { top: 48px; }` compensates only the divergence banner, never the main app content)
    - assets/styles/core.css (the app header is `position:sticky; top:0; z-index:100` — lower z-index, same anchor, no padding compensation; confirm the header/app-content container selector to which the offset should apply)
    - tests/e2e/app.spec.js (the existing "Global Search ist fokussierbar" test clicks `#global-search` then asserts `#global-search-results` visible)
    - 08-RESEARCH.md — Pitfall 3 (recommends the cleaner `body.has-migration-hint` class approach over the `~` combinator) and its D-02 regression-test guidance
  </read_first>
  <action>
    App fix: in `showMigrationHintBanner()`, right after the banner is inserted, add the class `has-migration-hint` to `document.body.classList`; in the `close-migration-hint` handler (where the banner is removed), remove that same class. In `assets/styles/migration.css`, add a rule scoped to `body.has-migration-hint` that applies a top offset equal to the banner's min-height (48px) to the app-content/header container so the sticky header — which contains `#global-search` — is never covered by the fixed 48px banner. Confirm the exact container to offset by reading core.css/the header template before choosing the selector; prefer offsetting the app content wrapper (or `body` padding-top) over the `~` combinator, matching RESEARCH's recommended approach.

    Test fix: add a new `test(...)` block to `tests/e2e/app.spec.js` that leaves the migration-hint banner present (fresh context shows it) and asserts `#global-search` is not covered — either by performing a natural Playwright click and asserting focus/`#global-search-results` visibility within the default timeout, or by comparing `getBoundingClientRect()` of the banner's bottom edge against the search input's top edge. Do NOT use `{force:true}`, and do NOT extend the timeout to bypass the banner. After the app fix the existing "Global Search ist fokussierbar" test also passes with its natural click.
  </action>
  <verify>
    <automated>npm run build:dev && npx playwright test tests/e2e/app.spec.js && npx playwright test tests/e2e/app.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `assets/styles/migration.css` contains a `body.has-migration-hint` offset rule (grep finds `has-migration-hint`)
    - `systems/migration/migration-wizard.js` adds `has-migration-hint` to `document.body.classList` in `showMigrationHintBanner` and removes it in the close handler (grep finds a classList add and a classList remove referencing `has-migration-hint`)
    - `tests/e2e/app.spec.js` contains a new test asserting the banner does not cover `#global-search` (grep finds a new `test(` block referencing the banner and `#global-search`)
    - `npx playwright test tests/e2e/app.spec.js` exits 0 (0 failed) on two consecutive runs
  </acceptance_criteria>
  <done>With the onboarding banner visible, a natural click focuses `#global-search`; the layout offset keeps the sticky header below the fixed banner for real users too.</done>
</task>

<task type="auto">
  <name>Task 3: renderAll()-Dispatch-Lücke schließen — renderRandomTables + renderTimers (Pitfall 7)</name>
  <files>features/render-dashboard.js</files>
  <read_first>
    - features/render-dashboard.js (the `renderAll()` body binds 14 render functions from `window.*` then calls `renderSafe(...)` for each; note the existing `renderSafe(renderWiki, 'renderWiki')` / `renderSafe(renderFilterList, 'renderFilterList')` calls that omit the 3rd container-id argument)
    - systems/tab-registry.js (`TAB_RENDER_REGISTRY.dice.renders` includes `renderRandomTables`; `.timers.renders` includes `renderTimers` — these two are dispatched on switchView but not by renderAll)
    - features/random-tables.js (`renderRandomTables` has a container-missing guard around `#random-tables-list` — safe no-op off-tab)
    - features/timers/timers.js (`renderTimers` and its container; confirm whether a top-level container id exists for the optional 3rd renderSafe argument)
    - systems/undo.js (`undo()`/`redo()` both call `window.renderAll()` after restoring a snapshot)
    - 08-RESEARCH.md — Pitfall 7 (confirms both target functions have container guards; adding them is a safe no-op when their tab is inactive)
  </read_first>
  <action>
    In `features/render-dashboard.js`, add two entries to `renderAll()`'s dispatch list alongside the existing 14 `renderSafe(...)` calls: `renderSafe(renderRandomTables, 'renderRandomTables', 'random-tables-list')` and `renderSafe(renderTimers, 'renderTimers')`. Add the matching local bindings `const renderRandomTables = window.renderRandomTables;` and `const renderTimers = window.renderTimers;` in the same window-binding block that already binds the other render functions. Omit the 3rd container-id argument for `renderTimers` (matching the existing `renderWiki`/`renderFilterList`/`initQuickRefCustom` calls that also omit it) unless the timers top-level container id is confirmed during the read. Do NOT modify `systems/tab-registry.js`. This is a proactive parity fix so the two panels stay fresh after undo/redo/import; the behavioral E2E proof (tab-navigation undo/redo test) runs in plan 08-02 once its co-required `.dice-details` selector fix is present.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('features/render-dashboard.js','utf8'); if(!/renderSafe\(renderRandomTables/.test(s)||!/renderSafe\(renderTimers/.test(s)){process.exit(1)}" && python build.py && npx jest</automated>
  </verify>
  <acceptance_criteria>
    - `features/render-dashboard.js` `renderAll()` contains `renderSafe(renderRandomTables` and `renderSafe(renderTimers` (grep finds each ≥1)
    - `features/render-dashboard.js` binds `renderRandomTables` and `renderTimers` from `window.*` in the render-binding block
    - `python build.py` (dev build) exits 0 with the change (no dedup/syntax break)
    - `npx jest` exits 0
    - Behavioral cross-wave note recorded: the undo/redo re-render assertion (`tab-navigation.spec.js` :552) is verified in 08-02 after the co-required `.dice-details` selector fix (:515) lands
  </acceptance_criteria>
  <done>renderAll() dispatches renderRandomTables and renderTimers; undo/redo/import no longer leaves those panels stale while their tab is active.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User keyboard input → event-delegation action dispatch | Typed attribute values cross into the `data-action` handler table (last-write-wins, no collision guard) |
| Fixed overlay (banner) → header interaction surface | A `position:fixed` overlay can intercept pointer events destined for header controls |
| Jest test → repo source files (fs read) | The new collision test reads `ui/actions/*.js` from the local repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | `ui/actions/*` data-action registry (silent duplicate-key overwrite) | medium | mitigate | New Jest collision test fails CI on any non-allowlisted duplicate data-action key — removes the class of silent-overwrite regressions |
| T-08-02 | Denial of Service (UX) | `#migration-hint-banner` fixed overlay intercepting header clicks | low | mitigate | `body.has-migration-hint` layout offset keeps interactive header controls (incl. `#global-search`) uncovered; regression test asserts no overlap |
| T-08-03 | Tampering | New Jest test reads `ui/actions/*.js` via `fs` | low | accept | Reads own-repo source only; no external/untrusted input, no write path |

No new external packages are installed this phase (no `T-08-SC` supply-chain checkpoint needed); no new user-input, persistence, or network surface is introduced.
</threat_model>

<verification>
- `npx jest` fully green (453+ tests) including the new `action-registry-collisions.test.js`
- `npm run build:dev && npx playwright test tests/e2e/app.spec.js` green twice (banner offset + regression test)
- `npx playwright test tests/e2e/crud/party.spec.js tests/e2e/crud/encounters.spec.js` green (attribute-modifier fixed at root)
- `python build.py` dev build succeeds with the renderAll change (no dedup/syntax break)
</verification>

<success_criteria>
- 4 of the 11 fails (party attr-mod, encounters attr-mod, global-search focus, undo re-render root) are resolved at the app root
- Each app bug has a dedicated D-02 regression test (collision test; banner-overlap test; the undo/redo E2E confirmed in 08-02)
- No user-visible behavior change beyond restoring correct behavior (milestone verhaltensneutral)
</success_criteria>

## Artifacts this plan produces

New symbols/behaviors created by this plan (for downstream drift-verification exclusion):
- `tests/unit/action-registry-collisions.test.js` (new Jest test file) + its `ALLOWED_DUPLICATE_ACTIONS` allowlist constant
- CSS class + behavior `body.has-migration-hint` (`assets/styles/migration.css` rule + `systems/migration/migration-wizard.js` classList toggle)
- `renderAll()` dispatch entries `renderSafe(renderRandomTables, ...)` and `renderSafe(renderTimers, ...)` in `features/render-dashboard.js`
- A new banner-overlap regression `test(...)` block in `tests/e2e/app.spec.js`

<output>
Create `.planning/phases/08-test-fundament-gr-n/08-01-SUMMARY.md` when done
</output>
