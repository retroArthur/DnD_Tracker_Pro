---
phase: 08-test-fundament-gr-n
plan: 03
type: execute
wave: 3
depends_on: [08-01, 08-02]
files_modified:
  - tests/unit/action-registry.test.js
  - tests/unit/welt-story.test.js
  - tests/unit/stability.test.js
  - tests/unit/initiative-mob.test.js
  - tests/integration/encounter-builder.test.js
  - tests/e2e/features/character-advancement.spec.js
  - tests/e2e/features/bestiary.spec.js
  - tests/e2e/features/dice-stats.spec.js
  - tests/e2e/features/initiative.spec.js
  - tests/e2e/features/welt-story.spec.js
  - tests/e2e/app.spec.js
  - tests/e2e/tab-navigation.spec.js
  - tests/e2e/crud/quests.spec.js
  - tests/e2e/crud/party.spec.js
  - tests/e2e/crud/encounters.spec.js
  - tests/e2e/crud/npcs.spec.js
autonomous: true
requirements: [TEST-02]

must_haves:
  truths:
    - "Deterministic count assertions use exact toBe(N); any remaining toBeGreaterThan(0) has an inline comment explaining why an exact count is not assertable"
    - "No test in the suite has 100% of its expect() calls wrapped inside a single isVisible() skip-guard (masking a missing element as a silent pass)"
    - "page.evaluate() is not used to replace the interaction a test is meant to verify; the two documented Bestandsausnahmen (page.evaluate(nextTurn) in initiative, page.evaluate(switchView) in welt-story) are individually evaluated against D-06 and kept-with-reason or converted"
    - "Fixed waitForTimeout(300-500) is replaced by waitForSelector/waitForFunction only within the specs already touched by the 11-fail fixes (D-05); the other ~16 spec files are untouched"
    - "The full Jest and Playwright suites remain green after hardening (toBe is stricter, never looser)"
  artifacts:
    - "A completed assertion-hardening inventory pass over the 23 toBeGreaterThan(0) sites (10 files) from RESEARCH's Suite-Wide Hardening Inventory, refreshed against current main"
    - "An isVisible()-guard audit result: every all-assertions-inside-guard test is fixed"
  key_links:
    - "known setup count (N creates/deletes) -> deterministic list length -> toBe(N)"
    - "D-06 criterion -> only interaction-replacing page.evaluate flagged -> real Playwright interaction; state-assertion evaluate() left intact"
  prohibitions:
    - statement: "Assertions must NOT be weakened; toBeGreaterThan(0) may only become toBe(N) (stricter) or stay loose WITH an inline justification comment — never become looser or be removed"
      status: flagged-unverified
    - statement: "Stable specs outside the 11-fail touch-set must NOT be swept for waitForTimeout (D-05 limits the change to already-touched specs to avoid reintroducing flake)"
      status: flagged-unverified
    - statement: "A page.evaluate()/manual dispatch that replaces the interaction under test must NOT be kept as-is; documented setup/navigation evaluate() is allowed only when the test's subject is something else (D-06)"
      status: flagged-unverified
  flagged_assumptions:
    - requirement: TEST-02
      status: unresolved
      note: "Edge-probe left TEST-02 unclassified (unresolved). Surfaced as a flagged assumption: the 'brüchige Muster gehärtet' criterion is treated as satisfied by (a) exact toBe(N) where deterministic, (b) no all-assertions-inside-isVisible-guard tests, and (c) D-06-compliant evaluate() usage — a judgement-based review, not a probed edge predicate. Reviewer should confirm the interpretation and the kept-loose justifications."
---

<objective>
Perform the suite-wide assertion hardening required by D-04/D-05/D-06 (TEST-02), consuming the exhaustive inventory RESEARCH already produced (23 `toBeGreaterThan(0)` sites across 10 files; 87 `isVisible())` guard sites; the two named `page.evaluate()` Bestandsausnahmen; `waitForTimeout` restricted to touched specs). This is a review-and-edit sweep over test files only — no production code changes — that runs after the suite is green (08-01 + 08-02) so hardening never fights a still-red baseline.

Purpose: Make the suite trustworthy as a gate — exact counts where determinable, no masking guards that turn missing elements into silent passes, and no `page.evaluate()` that hides the very interaction a test claims to verify.
Output: Hardened assertions/waits across the inventoried files, with inline justifications where a loose assertion is legitimately kept.

FLAGGED ASSUMPTION (edge-probe unresolved): TEST-02 was left `unclassified` by the deterministic edge-probe and is surfaced here as an explicit flagged assumption (see frontmatter) — not silently dropped.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-test-fundament-gr-n/08-RESEARCH.md
@.planning/phases/08-test-fundament-gr-n/08-CONTEXT.md
@tests/setup.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Exakte Zähl-Assertions — toBeGreaterThan(0) → toBe(N) (D-04)</name>
  <files>tests/unit/action-registry.test.js, tests/unit/welt-story.test.js, tests/unit/stability.test.js, tests/unit/initiative-mob.test.js, tests/integration/encounter-builder.test.js, tests/e2e/features/character-advancement.spec.js, tests/e2e/features/bestiary.spec.js, tests/e2e/features/dice-stats.spec.js, tests/e2e/features/initiative.spec.js, tests/e2e/features/welt-story.spec.js</files>
  <read_first>
    - 08-RESEARCH.md — "Suite-Wide Hardening Inventory" (the 23-site / 10-file breakdown and the guidance: CRUD-list-length after known creates/deletes → toBe(N); fuzzy-search "at least N" and pre-seeded SRD-monster counts may legitimately stay loose)
    - tests/setup.js (global factories createTestCharacter/NPC/Encounter — to know exactly how many entities a test seeds, so the expected N is derivable)
    - Each of the 10 listed files, at their `toBeGreaterThan(0)` sites, plus enough surrounding setup to know whether the count is deterministic
  </read_first>
  <action>
    First re-run the inventory against current main (`grep -rn "toBeGreaterThan(0)" tests/`) and reconcile with RESEARCH's 23 sites (the set is stable at commit b23c82b but confirm nothing shifted after 08-01/08-02). For each site, make a per-site judgement: if the asserted count is deterministic given the test's own setup data (a known number of factory-created entities, creates minus deletes, or a fixed seeded table), replace `toBeGreaterThan(0)` with the exact `toBe(N)`. If the count is legitimately non-deterministic — e.g. a fuzzy-search "at least one result" assertion, or a pre-seeded SRD/monster/default-table count whose size may change with content edits — keep it loose but add a one-line inline comment stating why an exact count is not assertable. Do NOT touch any production code. Do NOT weaken any assertion: `toBe(N)` is strictly stronger, and loose assertions only remain where justified. Keep the whole suite green.
  </action>
  <verify>
    <automated>npx jest && npm run build:dev && npx playwright test tests/e2e/features/character-advancement.spec.js tests/e2e/features/bestiary.spec.js tests/e2e/features/dice-stats.spec.js tests/e2e/features/initiative.spec.js tests/e2e/features/welt-story.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - Deterministic count sites now use `toBe(N)` — grep shows the `toBe(` count increased in the touched files relative to baseline, and the residual `toBeGreaterThan(0)` count is reduced
    - Every remaining `toBeGreaterThan(0)` in `tests/` has an adjacent inline comment justifying why exact is not assertable (diff review)
    - `npx jest` exits 0 (full unit + integration suite)
    - The 5 touched E2E feature specs exit 0 under `npx playwright test`
    - No production (`src`/`features`/`ui`/`systems`/`core`) file is in the diff for this task
  </acceptance_criteria>
  <done>Count assertions are exact wherever the count is determinable; every retained loose assertion carries a written justification.</done>
</task>

<task type="auto">
  <name>Task 2: Maskierungs-Audit (isVisible-Guards, page.evaluate) + waitForTimeout in angefassten Specs (D-05/D-06)</name>
  <files>tests/e2e/app.spec.js, tests/e2e/tab-navigation.spec.js, tests/e2e/crud/quests.spec.js, tests/e2e/crud/party.spec.js, tests/e2e/crud/encounters.spec.js, tests/e2e/crud/npcs.spec.js, tests/e2e/features/initiative.spec.js, tests/e2e/features/welt-story.spec.js</files>
  <read_first>
    - 08-RESEARCH.md — "Suite-Wide Hardening Inventory" (87 isVisible() guard sites across 12 files; 265 page.evaluate() sites of which most are legitimate state-assertions; 395 waitForTimeout sites) and the "State of the Art" note referencing the 2026-06-14 initiative audit precedent
    - 08-CONTEXT.md — D-06 masking criterion (verbatim: a page.evaluate/manual dispatch is masking when the test is meant to verify the interaction it bypasses; legitimate when it is documented setup/navigation and the subject is something else) and D-05 (waitForTimeout replacement limited to already-touched specs)
    - tests/e2e/features/initiative.spec.js (the documented `page.evaluate(nextTurn)` Bestandsausnahme — kept in Phase 4 due to pointer-interception on a data-action=call button)
    - tests/e2e/features/welt-story.spec.js (the documented `page.evaluate(switchView)` Bestandsausnahme — kept due to a fullscreen-overlay pointer interception)
  </read_first>
  <action>
    Three-part hardening audit, editing only where a real masking problem is found:
    (a) D-06 page.evaluate audit — scan `page.evaluate()` usages for ones that SIMULATE a user action (click/fill/keyboard) in place of a real Playwright interaction API, where that interaction is the test's actual subject; convert those to real interactions. Leave the dominant legitimate usage (state assertion against `D`, and documented setup/navigation) untouched. Individually evaluate the two named Bestandsausnahmen against the D-06 criterion and keep-with-reason (both bypass a navigation/turn advance whose pointer target is intercepted, while the test's subject is the resulting state) — record the keep decision as an inline comment at each site.
    (b) isVisible()-guard audit — review the guard sites; flag and fix any test where 100% of its `expect()` calls sit inside a single `if (await x.isVisible())` guard (which silently passes when the element is missing). Replace such guards with a real assertion that the element exists, so a missing element fails instead of no-op-passing.
    (c) D-05 waitForTimeout — within the specs already touched by the 11-fail fixes ONLY (the files listed for this task), replace fixed `waitForTimeout(300-500)` with `waitForSelector`/`waitForFunction` on the concrete condition being awaited. Do NOT sweep the ~16 other spec files.
    No production code changes. No assertion weakening.
  </action>
  <verify>
    <automated>npm run build:dev && npx playwright test && npx playwright test && npx jest</automated>
  </verify>
  <acceptance_criteria>
    - No suite test has all its `expect()` calls wrapped in a single `isVisible()` skip-guard (the audit lists reviewed sites; any found are converted to hard assertions)
    - The two Bestandsausnahmen (`page.evaluate(nextTurn)` / `page.evaluate(switchView)`) each carry an inline comment documenting the D-06 keep decision
    - Within the specs listed for this task, the fixed `waitForTimeout(300-500)` calls tied to the fixed races are replaced by `waitForSelector`/`waitForFunction` (grep shows a reduced `waitForTimeout` count in those files vs. baseline); the other spec files are unchanged
    - Full `npx playwright test` exits 0 (0 failed) on two consecutive runs; `npx jest` exits 0
  </acceptance_criteria>
  <done>No masking guard turns a missing element into a silent pass; interaction-replacing evaluate() is removed where it hid the subject; touched specs await concrete conditions instead of fixed sleeps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test assertion → gate trustworthiness | Loose/masked assertions let real regressions pass the CI gate undetected |
| E2E test → real Chromium DOM | Replacing real interaction with evaluate() can hide interaction-path breaks |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-06 | Tampering (gate integrity) | Loose `toBeGreaterThan(0)` / all-inside-isVisible-guard / interaction-replacing evaluate() masking real failures | medium | mitigate | Convert to exact `toBe(N)` where deterministic; fix guard-only tests; remove interaction-masking evaluate() per D-06; keep suite green twice |
| T-08-07 | Repudiation | A retained loose assertion with no rationale hides intent | low | mitigate | Every retained `toBeGreaterThan(0)` carries an inline justification comment |

No production code, no new package, no new persistence or network surface is introduced.
</threat_model>

<verification>
- `npx jest` green (full unit + integration)
- `npm run build:dev && npx playwright test` green on two consecutive runs (the phase's trustworthiness sampling rate)
- Diff contains only `tests/**` files (no production code touched)
- Inventory reconciled: residual `toBeGreaterThan(0)` count reduced, each remainder justified inline
</verification>

<success_criteria>
- D-04 satisfied: exact `toBe(N)` wherever counts are deterministic across the inventoried files
- D-06 satisfied: no interaction-masking `page.evaluate()`; Bestandsausnahmen evaluated and documented
- D-05 satisfied: `waitForTimeout` hardened only within already-touched specs
- Suite remains fully green — hardening strengthened, never loosened, coverage
</success_criteria>

## Artifacts this plan produces

New/changed test conventions created by this plan (for downstream drift-verification exclusion):
- Exact `toBe(N)` assertions replacing deterministic `toBeGreaterThan(0)` sites across the 10 inventoried files
- Inline justification comments on any retained loose count assertion
- Inline D-06 keep-decision comments at the two `page.evaluate()` Bestandsausnahmen
- `waitForSelector`/`waitForFunction` replacing fixed `waitForTimeout` in the touched specs

<output>
Create `.planning/phases/08-test-fundament-gr-n/08-03-SUMMARY.md` when done
</output>
