---
phase: 08-test-fundament-gr-n
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - tests/e2e/tab-navigation.spec.js
  - tests/e2e/crud/quests.spec.js
  - tests/e2e/crud/npcs.spec.js
  - tests/e2e/crud/party.spec.js
autonomous: true
requirements: [TEST-01, TEST-02]

must_haves:
  truths:
    - "The 7 tab-navigation fails pass: initiative panel asserts .init-entry, party panel asserts .char-card, dice panel opens the correct #random-tables-list collapsible, timers panel uses the real addTimerWithSeconds API and asserts .timer-card"
    - "The undo/redo re-render test (tab-navigation :552) passes — the 08-01 renderAll fix plus the corrected .dice-details selector (:515) are both present"
    - "The Quest-title validation test (quests:96) and the two flaky validation tests (npcs:64, party:129) pass on two consecutive runs — the onboarding toast no longer overwrites the validation error in the shared #toast node"
  artifacts:
    - "tests/e2e/tab-navigation.spec.js references real production classes .init-entry / .char-card / .timer-card and the addTimerWithSeconds API"
    - "tests/e2e/crud/{quests,npcs,party}.spec.js seed markdownOnboardingSeen before loadApp as a documented setup step"
  key_links:
    - "getElementById('random-tables-list').closest('details') -> the correct native <details> opens -> #random-tables-list becomes visible (fixes 4 of the 4 same-classed dice-details ambiguity sites)"
    - "addTimerWithSeconds('Concentration',300) -> module-local timers array populated -> renderTimers emits .timer-card (D.timers is not a real data key)"
    - "seeded D.settings.markdownOnboardingSeen -> 2000ms onboarding setTimeout never schedules -> validation error survives in #toast during the assertion window"
  prohibitions:
    - statement: "Failing tests must NOT be silenced via .skip/.only/deletion, nor made to pass by weakening or removing assertions (toHaveCount(N)/toContainText stay exact)"
      status: flagged-unverified
    - statement: "page.evaluate()/manual dispatch must NOT replace the interaction path a test is meant to verify; it is allowed only as documented setup/navigation whose subject is something else (D-06)"
      status: flagged-unverified
    - statement: "The onboarding-toast race must NOT be masked by {force:true}, longer waitForTimeout, or by relaxing the toast-text assertion"
      status: flagged-unverified
  flagged_assumptions:
    - requirement: TEST-01
      status: unresolved
      note: "Edge-probe left TEST-01 unclassified (unresolved). Surfaced as a flagged assumption: the 7 tab-navigation + toast-race fails are treated as 'covered' by real-DOM selectors + exact assertions passing twice, not by a probed edge predicate. Reviewer should confirm."
    - requirement: TEST-02
      status: unresolved
      note: "Edge-probe left TEST-02 unclassified (unresolved). This plan contributes TEST-02's 'no maskierenden Dispatches' criterion via D-06 (onboarding seeding is documented setup, not interaction-masking); full suite-wide hardening is completed in 08-03. Reviewer should confirm the D-06 interpretation."
---

<objective>
Fix the pure test-code bugs among the 11 pre-existing E2E fails (per RESEARCH: stale CSS class selectors, an ambiguous `.dice-details` selector, a non-existent `D.timers` data location, and an onboarding-toast race). These are corrections to `tests/e2e/*` only — the app is already correct here (or made correct by 08-01). This plan depends on 08-01 so that, at verification time, the renderAll fix (Pitfall 7) and the attribute-modifier fix are already present and the shared tab-navigation / crud specs verify fully green.

Purpose: Resolve the remaining 7 fails without changing app behavior, replacing aspirational/stale selectors with the real rendered DOM shape and removing a non-deterministic toast race — satisfying TEST-01 and contributing TEST-02's "no maskierenden Dispatches" (D-06).
Output: Corrected assertions/setup in `tab-navigation.spec.js` and three crud specs.

FLAGGED ASSUMPTIONS (edge-probe unresolved): TEST-01 and TEST-02 were both left `unclassified` by the deterministic edge-probe and are surfaced here as explicit flagged assumptions (see frontmatter). They are not silently dropped.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-test-fundament-gr-n/08-RESEARCH.md
@.planning/phases/08-test-fundament-gr-n/08-CONTEXT.md
@.planning/phases/08-test-fundament-gr-n/08-PATTERNS.md
@tests/e2e/tab-navigation.spec.js
@tests/e2e/helpers/test-utils.js
@ui/editors/markdown-shortcuts.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: tab-navigation.spec.js — reale Selektoren + echte Timer-API (Pitfall 5/6/8)</name>
  <files>tests/e2e/tab-navigation.spec.js</files>
  <read_first>
    - tests/e2e/tab-navigation.spec.js (full file — the stale selectors are confirmed present: `.init-combatant` at the initiative-render tests, `.party-member` at the party re-render tests, `.timer-item` + `window.D.timers.push({...})` at the timers tests, and 5 `document.querySelector('.dice-details')` setup calls)
    - features/initiative.js (`renderInit()` emits `.init-entry` rows with `draggable="true" data-id="${cb.id}"` and a `.init-name` sub-element — the real class is `.init-entry`, never `.init-combatant`)
    - features/party/party-render.js (`renderParty()` emits `.char-card` with `id="char-${ch.id}" data-id="${ch.id}"` — the real class is `.char-card`, never `.party-member`)
    - assets/templates/view-tools.html (four separate `<details class="dice-details">` blocks; the random-tables one wraps `#random-tables-list` — `querySelector('.dice-details')` always returns the FIRST, a character-roll block, so the random-tables panel stays closed)
    - features/timers/timers.js (the real create API is `addTimerWithSeconds(name, totalSeconds)`; `renderTimers()` emits `.timer-card`; the timer object shape is `{id,name,totalSeconds,remainingSeconds,running}` — there is no `D.timers` key in core/data.js)
    - 08-RESEARCH.md — Pitfalls 5, 6, 8 and the Code Examples (exact before/after diffs)
  </read_first>
  <action>
    Apply the three RESEARCH-specified corrections to `tests/e2e/tab-navigation.spec.js`, all against the real rendered DOM (no assertion weakening, no `.skip`/`.only`):
    (a) Pitfall 5 — replace the stale initiative class `.init-combatant` with the real `.init-entry` at every occurrence, and the stale party class `.party-member` with the real `.char-card`; verify any `.first()`/`.nth(1)` text targets still point at the name/HP sub-element of the corrected row.
    (b) Pitfall 6 — replace every `document.querySelector('.dice-details')` setup call (all 5 sites) with a scoped lookup `document.getElementById('random-tables-list')?.closest('details')` so the correct random-tables collapsible is expanded; a closed native `<details>` legitimately hides its children, so this is what made `#random-tables-list` report hidden.
    (c) Pitfall 8 — rewrite the timers-test setup to call the real API via `page.evaluate(() => window.addTimerWithSeconds('Concentration', 300))` (documented setup per D-06 — the test verifies tab re-render, not the create-form), remove the `window.D.timers.push({...})` writes to the non-existent key, replace `.timer-item` with `.timer-card`, and assert the card contains the timer name.
    Keep every `toHaveCount(N)` exact. After this task the undo/redo re-render test (:552) also passes because 08-01's renderAll fix and this task's :515 `.dice-details` fix are both present.
  </action>
  <verify>
    <automated>npm run build:dev && npx playwright test tests/e2e/tab-navigation.spec.js && npx playwright test tests/e2e/tab-navigation.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/tab-navigation.spec.js` references `.init-entry`, `.char-card`, and `.timer-card` (grep finds each ≥1)
    - The timers test setup uses `window.addTimerWithSeconds(` (grep ≥1) and no longer writes to a `D.timers` array
    - The dice-panel setup scopes the collapsible via `getElementById('random-tables-list')` + `closest('details')` (grep finds `closest('details')` ≥1)
    - `npx playwright test tests/e2e/tab-navigation.spec.js` exits 0 (0 failed) on two consecutive runs, including the undo/redo re-render test
    - No `.skip(` or `.only(` was added and no `expect(` call was removed (diff review; enforced by the prohibitions block)
  </acceptance_criteria>
  <done>All 7 tab-navigation tests pass deterministically against the real rendered DOM, including the undo/redo re-render behavior enabled by 08-01.</done>
</task>

<task type="auto">
  <name>Task 2: Onboarding-Toast-Race in crud-Validierungstests entschärfen (Pitfall 4, D-06)</name>
  <files>tests/e2e/crud/quests.spec.js, tests/e2e/crud/npcs.spec.js, tests/e2e/crud/party.spec.js</files>
  <read_first>
    - tests/e2e/crud/quests.spec.js (the "Quest ohne Titel zeigt Fehlermeldung" test asserts `#toast` contains `Titel`; the beforeEach calls `loadApp(page)` then `navigateToTab`)
    - tests/e2e/crud/npcs.spec.js and tests/e2e/crud/party.spec.js (the flaky "... ohne Namen zeigt Fehlermeldung" tests share the same race; party.spec.js also has the now-passing attr-mod test from 08-01 — do NOT touch that test)
    - ui/editors/markdown-shortcuts.js (the onboarding toast is scheduled by a 2000ms `setTimeout` guarded by `if (D.settings && !D.settings.markdownOnboardingSeen)` inside `initMarkdownSettings()`; once scheduled it cannot be cancelled by id)
    - core/init.js (where `initMarkdownSettings()` is called during app init — the guard is evaluated once, at init, so the flag must be true BEFORE init runs)
    - utils/utilities.js (the legacy `#toast` element is a single shared node whose textContent is unconditionally overwritten by the last showToast call — no priority queue)
    - tests/e2e/helpers/test-utils.js (`loadApp` — do NOT modify this shared helper for a 3-spec fix)
    - 08-RESEARCH.md — Pitfall 4 (option 1: test-side seeding of `markdownOnboardingSeen`)
  </read_first>
  <action>
    In each of the three specs' `beforeEach`, BEFORE `loadApp(page)`, ensure the app's loaded settings have `markdownOnboardingSeen: true` so the 2000ms onboarding `setTimeout` never schedules and cannot overwrite the validation error in the shared legacy `#toast` node. This is a documented setup step (D-06-legitimate: these tests verify the validation-error display, not the onboarding flow). Because the guard is evaluated at init time, the seed must take effect before `initMarkdownSettings()` runs — read `core/init.js` and the settings-load path to choose the channel that reliably applies under `file://` (localStorage is restricted per the triage doc's Cluster 1; if a plain localStorage seed via `page.addInitScript()` proves unreliable, seed via the StorageAPI key the loader actually reads, or via an addInitScript that sets the app's settings default before init). Do NOT modify the shared `loadApp` helper. Do NOT mask the race with `{force:true}`, a longer `waitForTimeout`, or by relaxing the `toContainText('Titel')` / name assertions.
  </action>
  <verify>
    <automated>npm run build:dev && npx playwright test tests/e2e/crud/quests.spec.js tests/e2e/crud/npcs.spec.js tests/e2e/crud/party.spec.js && npx playwright test tests/e2e/crud/quests.spec.js tests/e2e/crud/npcs.spec.js tests/e2e/crud/party.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - Each of the three specs seeds `markdownOnboardingSeen` before `loadApp` (grep finds `markdownOnboardingSeen` in each of the three files)
    - The validation tests still assert exact toast text (grep finds `toContainText('Titel')` in quests.spec.js; the name-validation assertions remain in npcs/party)
    - `npx playwright test tests/e2e/crud/quests.spec.js tests/e2e/crud/npcs.spec.js tests/e2e/crud/party.spec.js` exits 0 on two consecutive runs (the previously-flaky npcs/party pair passes both runs)
    - The shared `tests/e2e/helpers/test-utils.js` is unchanged
  </acceptance_criteria>
  <done>The Quest-title and the two flaky name-validation tests pass deterministically; the onboarding toast can no longer stomp a validation error during the assertion window.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E test → real Chromium DOM (built bundle via file://) | Tests drive the actual app; a masking shortcut can hide a real interaction-path break |
| Test setup seed → app init-time settings | A pre-init seed influences whether the onboarding path runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04 | Spoofing (test integrity) | E2E assertions masking real failures via evaluate/force/skip | medium | mitigate | Per D-06, keep real interaction paths; use `addTimerWithSeconds` and onboarding-seed only as documented setup; assert against real classes (.init-entry/.char-card/.timer-card) with exact counts |
| T-08-05 | Tampering | Onboarding `setTimeout` overwriting a validation error in the shared `#toast` node | low | mitigate | Seed `markdownOnboardingSeen` pre-init so no lower-priority info toast can stomp the error during the assertion window |

No source/production code is modified in this plan; no new external package, persistence, or network surface is introduced.
</threat_model>

<verification>
- `npm run build:dev && npx playwright test tests/e2e/tab-navigation.spec.js` green twice (all 7 tab-navigation fails resolved, incl. undo re-render)
- `npx playwright test tests/e2e/crud/quests.spec.js tests/e2e/crud/npcs.spec.js tests/e2e/crud/party.spec.js` green twice (toast race gone, flaky pair stable)
- After this wave: the full 11-fail baseline should be 0 — confirm with a full `npx playwright test` run (formalized as the gate in 08-04)
</verification>

<success_criteria>
- All 7 remaining test-bug fails (tab-navigation ×7 incl. undo, plus the 3 validation toast-race tests) are green
- No assertion weakened, no test skipped; masking shortcuts avoided per D-06
- Combined with 08-01, the freshly-measured E2E fail count reaches 0 (TEST-01)
</success_criteria>

## Artifacts this plan produces

New/changed test symbols created by this plan (for downstream drift-verification exclusion):
- `tests/e2e/tab-navigation.spec.js` now references `.init-entry`, `.char-card`, `.timer-card`, `window.addTimerWithSeconds(...)`, and `closest('details')`
- `tests/e2e/crud/{quests,npcs,party}.spec.js` now seed `markdownOnboardingSeen` in `beforeEach`

<output>
Create `.planning/phases/08-test-fundament-gr-n/08-02-SUMMARY.md` when done
</output>
