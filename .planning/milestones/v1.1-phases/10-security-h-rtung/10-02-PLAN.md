---
phase: 10-security-h-rtung
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - systems/spellslots/import-export.js
  - tests/unit/import-sanitization.test.js
  - tests/e2e/features/import-security.spec.js
autonomous: true
requirements: [SEC-01]

must_haves:
  truths:
    - "Beide Import-Eintrittspunkte sanitisieren die HTML-tragenden Felder, bevor die Daten in D oder in den LocalStorage gelangen: executeImport() für den typspezifischen Import und importDataGlobal() für den globalen Import (D-01)"
    - "In importDataGlobal() greift die Sanitisierung in BEIDEN Zweigen — sowohl beim Anlegen einer neuen Kampagne als auch beim Überschreiben der aktuellen (RESEARCH Pitfall 2)"
    - "Sanitisiert werden ausschließlich die neun per Render-Pfad-Audit bestimmten HTML-tragenden Felder; rein textuelle und kategoriale Felder bleiben Zeichen für Zeichen unverändert, auch wenn sie spitze Klammern enthalten (D-02)"
    - "Der Überschreib-Zweig des globalen Imports legt vor dem Datenwechsel einen Undo-Punkt und eine Sicherungskopie an, nach dem Muster von executeImport() (D-07, WR-03)"
    - "Die Sanitisierung ist total definiert: ein unbekannter Entity-Typ, ein fehlendes Feld sowie ein leerer, null- oder undefined-Wert führen zu unverändertem Rückgabewert statt zu einem Fehler; eine Import-Datei mit leeren Arrays oder fehlenden Entity-Schlüsseln bricht den Import nicht ab"
    - "Der Import meldet Sanitisierungs-Eingriffe nicht — kein zusätzlicher Hinweis, kein zusätzlicher Protokolleintrag, kein zusätzlicher Dialog (D-04)"
  artifacts:
    - "HTML_FIELDS_BY_TYPE — modul-interne Konstante in systems/spellslots/import-export.js mit der Feldliste je Entity-Typ"
    - "sanitizeImportedItem(type, item) — modul-interne Hilfsfunktion in systems/spellslots/import-export.js"
    - "tests/unit/import-sanitization.test.js — Unit-Nachweis gegen den echten Quelltext via vm.runInContext"
    - "Erweiterung von tests/e2e/features/import-security.spec.js um den Nachweis, dass die GESPEICHERTEN Daten bereinigt sind"
  key_links:
    - "executeImport() → validatedItems-Abbildung → sanitizeImportedItem() → D[type] → save()"
    - "importDataGlobal() → Sanitisierungs-Schleife über HTML_FIELDS_BY_TYPE VOR der choice-Verzweigung → sowohl StorageAPI.setJSON(key, …) (neue Kampagne) als auch Object.assign(D, imp) (überschreiben)"
    - "importDataGlobal() Überschreib-Zweig → saveUndoState() + createAutoBackup() vor Object.assign(D, imp) → Rückgängig-Funktion und Dateisicherung bleiben wirksam"
  prohibitions:
    - statement: "Die Sanitisierung darf NICHT rekursiv über alle Zeichenketten-Felder laufen und darf bestehende Kampagnendaten NICHT verändern — Namen, Würfelformeln und kategoriale Felder mit spitzen Klammern gehören dem Spielleiter und dürfen weder umgeschrieben noch stillschweigend gekürzt werden (D-02, D-03)"
      status: flagged-unverified
      verification: test
---

<objective>
Mache den Import zur gleichrangigen Vertrauensgrenze wie die manuelle Eingabe: eine zentrale Feldliste plus eine Hilfsfunktion schleusen die neun HTML-tragenden Felder durch den vorhandenen Sanitizer — an beiden Eintrittspunkten und in beiden Zweigen des globalen Imports. Gleichzeitig schließt der Überschreib-Zweig die in `01-REVIEW.md` als WR-03 gemeldete Lücke: kein Datenwechsel mehr ohne Undo-Punkt und Sicherungskopie.

Purpose: Die Anzeige-Grenze aus Plan 10-01 schützt die heute bekannten Renderpfade. Verschmutzte Rohdaten überleben aber im Speicher und wandern in Exporte, Kopierfunktionen und jeden künftigen Renderpfad. Die zweite Grenze macht den gespeicherten Zustand selbst sauber.
Output: `HTML_FIELDS_BY_TYPE`, `sanitizeImportedItem()`, beide Aufrufstellen verdrahtet, Undo/Backup im Überschreib-Zweig, Unit- und E2E-Nachweis.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-security-h-rtung/10-CONTEXT.md
@.planning/phases/10-security-h-rtung/10-RESEARCH.md
@.planning/phases/10-security-h-rtung/10-PATTERNS.md
@.planning/phases/10-security-h-rtung/10-VALIDATION.md
@.planning/phases/10-security-h-rtung/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit-Nachweis für die Import-Sanitisierung anlegen (rot gegen den Ist-Zustand)</name>
  <files>tests/unit/import-sanitization.test.js</files>
  <read_first>
    - tests/unit/storage-conflict.test.js (vollständig — das Präzedenzmuster: Kontextobjekt bauen, `vm.createContext`, echten Quelltext per `fs.readFileSync` laden, `vm.runInContext`, Funktion anschließend aus dem Kontextobjekt ziehen; außerdem Test E als Muster für eine Quelltext-Strukturprüfung)
    - systems/spellslots/import-export.js (Zeilen 1-240 `IO_SCHEMA` — die gültigen Feldnamen je Typ; Zeilen 292-301 die `validatedItems`-Abbildung in `executeImport()`; Zeilen 339-376 `executeImport()` inklusive `saveUndoState()` Zeile 351 und `createAutoBackup()` Zeile 355; Zeilen 464-588 `importDataGlobal()` mit der `choice`-Verzweigung ab Zeile 530 und `Object.assign(D, imp)` Zeile 573)
    - utils/basic.js (Zeilen 58-160 `sanitizeHTML()` — die Funktion, die der Test als Kontext-Global bereitstellen muss)
    - .planning/phases/10-security-h-rtung/10-RESEARCH.md (Abschnitt „Render-Pfad-Audit — Sanitisierungs-Feldliste": die neun Felder und die ausdrücklich NICHT zu sanitisierende Liste)
    - jest.config.cjs (Testumgebung — die jsdom-Globalobjekte `document`, `DOMParser` und `Node` müssen in den vm-Kontext durchgereicht werden, sonst arbeitet der Sanitizer nicht)
  </read_first>
  <action>
    Lege `tests/unit/import-sanitization.test.js` an. Der Test lädt den ECHTEN Quelltext von `systems/spellslots/import-export.js` nach dem Muster aus `tests/unit/storage-conflict.test.js` — keine Kopie der Logik im Test.

    Kontext-Aufbau: Reiche `document`, `DOMParser`, `Node` und `console` aus der jsdom-Umgebung durch. Setze in das `window`-Objekt des Kontexts die echte `sanitizeHTML`-Funktion; lade sie dazu ebenfalls per `vm.runInContext` aus `utils/basic.js` in einen eigenen Kontext und reiche die Referenz weiter, damit der Test gegen den Produktions-Sanitizer läuft und nicht gegen den Test-Zwilling. Stubbe die übrigen Globals, die `import-export.js` in Funktionskörpern erwartet (`$`, `showToast`, `save`, `renderAll`, `saveUndoState`, `createAutoBackup`, `StorageAPI`, `getNextId`, `confirm`, `alert`) als Attrappen.

    Wichtig für den Zugriff auf die neuen Symbole: eine Funktionsdeklaration auf oberster Ebene landet als Eigenschaft am Kontextobjekt (`context.sanitizeImportedItem`), eine `const`-Deklaration jedoch NICHT — `HTML_FIELDS_BY_TYPE` musst du über einen zweiten Aufruf `vm.runInContext('HTML_FIELDS_BY_TYPE', context)` aus demselben Kontext auslesen. Schreibe das als Kommentar an die Stelle, damit spätere Leser nicht darüber stolpern.

    Testfälle (alle deutsche Namen, jede Erwartung exakt):
    - Feldliste: `HTML_FIELDS_BY_TYPE` enthält genau die neun Typen `characters`, `npcs`, `locations`, `quests`, `encounters`, `spells`, `sessionNotes`, `wiki`, `links` mit den Feldern `notes` / `description` / `description` / `description` / `traits`+`actions`+`skills` / `description` / `content` / `content` / `description`. Prüfe zusätzlich, dass jeder gelistete Feldname auch im zugehörigen `IO_SCHEMA`-Eintrag existiert — sonst zeigt die Liste auf ein Feld, das der Import gar nicht übernimmt.
    - Vektor-Katalog je Typ: für jeden der neun Typen wird ein Eintrag mit dem Review-Exploit-Vektor im HTML-tragenden Feld durch `sanitizeImportedItem()` geschickt; erwartet wird, dass das Ereignis-Attribut im Ergebnis nicht mehr vorkommt und das Element mit dem Fehler-Ereignis verschwunden ist. Ergänze je einen weiteren Vektor für `<script>`-Element, `javascript:`-Adresse und SVG-Ereignis-Attribut.
    - Leitplanke D-02 (Kern-Assertion der Prohibition): ein Eintrag, dessen NICHT gelistete Felder Zeichen mit spitzen Klammern enthalten — etwa ein `name` mit einer Würfelformel-artigen Vergleichsschreibweise und ein `title` mit einer Kleiner-als-Konstruktion — kommt in genau diesen Feldern zeichenidentisch zurück.
    - Totalität: unbekannter Typ gibt das Item unverändert zurück; ein Item ohne das gelistete Feld, mit `null`, mit `undefined` und mit leerer Zeichenkette in diesem Feld führt zu keinem Fehler und zu keiner Veränderung anderer Felder.
    - Unveränderlichkeit: der Aufruf verändert das übergebene Objekt nicht (Ausgangsobjekt bleibt gleich), sondern liefert ein neues.
    - Quelltext-Strukturprüfung (nach dem Muster von Test E in `storage-conflict.test.js`): (a) in `executeImport()` steht der Aufruf der Hilfsfunktion innerhalb der `validatedItems`-Abbildung; (b) in `importDataGlobal()` steht die Sanitisierungs-Schleife VOR der `choice`-Verzweigung — prüfe das über die Positionen der jeweiligen Zeichenketten im Quelltext (Index der Schleife kleiner als Index von `if (choice)`); (c) im Überschreib-Zweig stehen `saveUndoState` und `createAutoBackup` vor `Object.assign(D, imp)`.

    Führe die Datei aus. Sie MUSS jetzt rot sein (die Symbole existieren noch nicht). Halte die Ausgabe für die SUMMARY fest.
  </action>
  <verify>
    <automated>! npx jest tests/unit/import-sanitization.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/import-sanitization.test.js` existiert und lädt `systems/spellslots/import-export.js` sowie `utils/basic.js` über `vm.runInContext` aus dem echten Quelltext — keine im Test nachgebaute Sanitisierungslogik
    - Der Test enthält je einen Fall für alle neun Entity-Typen aus dem Render-Pfad-Audit
    - Der Test enthält mindestens einen Fall, der zeichenidentische Rückgabe für nicht gelistete Felder mit spitzen Klammern verlangt
    - Der Test enthält die drei Quelltext-Strukturprüfungen (Aufruf in `executeImport()`, Schleife vor der Verzweigung, Undo/Backup vor dem Datenwechsel)
    - Verhalten: `npx jest tests/unit/import-sanitization.test.js` schlägt gegen den aktuellen Stand fehl (das Prüfkommando dieser Aufgabe kehrt den Rückgabewert bewusst um und ist nur bei einem roten Lauf grün); die Fehlerausgabe ist in der SUMMARY zitiert
  </acceptance_criteria>
  <done>Der Nachweis steht vor der Implementierung und ist nachweislich in der Lage, ihr Fehlen zu erkennen.</done>
</task>

<task type="auto">
  <name>Task 2: Feldliste und Sanitisierungs-Hilfsfunktion einführen und an beiden Eintrittspunkten verdrahten</name>
  <files>systems/spellslots/import-export.js</files>
  <read_first>
    - systems/spellslots/import-export.js (Zeilen 1-240 `IO_SCHEMA`; Zeilen 292-301 `validatedItems`; Zeilen 339-376 `executeImport()`; Zeilen 464-588 `importDataGlobal()` — insbesondere die Meta-Feld-Bereinigung Zeile 497-500, die Charakter-Migration Zeile 502-529 und die `choice`-Verzweigung ab Zeile 530)
    - .planning/phases/10-security-h-rtung/10-PATTERNS.md (Abschnitt zu `import-export.js`: die vorgegebene Struktur für `HTML_FIELDS_BY_TYPE` und `sanitizeImportedItem()`)
    - utils/basic.js (Zeilen 58-160 `sanitizeHTML()` — Signatur und Rückgabeverhalten bei leerer Eingabe)
    - ui/editors/markdown-converter.js (Zeilen 189-192 — das im Projekt übliche Muster, `window.sanitizeHTML` in eine lokale Konstante zu holen und mit `typeof`-Guard aufzurufen)
    - CLAUDE.md (Abschnitte „Export Audit Rule" und „Build System & Deduplication Pattern": keine funktions-lokale `const`-Zuweisung eines anderswo als `const` deklarierten Namens, keine doppelten Funktionsnamen auf oberster Ebene, kein `window`-Export für modul-interne Helfer)
  </read_first>
  <action>
    Ergänze in `systems/spellslots/import-export.js` zwei neue Symbole auf Modulebene und verdrahte sie.

    (1) `HTML_FIELDS_BY_TYPE` — eine Konstante direkt nach `IO_SCHEMA`, die je Entity-Typ die HTML-tragenden Felder aufzählt: `characters` → `notes`; `npcs` → `description`; `locations` → `description`; `quests` → `description`; `encounters` → `traits`, `actions`, `skills`; `spells` → `description`; `sessionNotes` → `content`; `wiki` → `content`; `links` → `description`. Keine weiteren Typen, keine weiteren Felder. Setze einen kurzen Kommentar darüber, dass die Liste das Ergebnis des Render-Pfad-Audits ist und rein textuelle beziehungsweise kategoriale Felder bewusst außen vor bleiben, weil der Sanitizer dort legitime spitze Klammern zerstören würde.

    (2) `sanitizeImportedItem(type, item)` — eine modul-interne Funktionsdeklaration (KEIN `window`-Export, Export Audit Rule). Verhalten: ist der Typ nicht in der Feldliste, wird das Item unverändert zurückgegeben; steht `window.sanitizeHTML` nicht zur Verfügung, ebenfalls unverändert; sonst wird eine flache Kopie erzeugt und nur die gelisteten Felder werden ersetzt, und zwar nur, wenn ihr Wert eine Zeichenkette ist. Das übergebene Objekt wird nicht verändert.

    (3) `executeImport()` verdrahten: die Abbildung, die `validatedItems` erzeugt, gibt künftig das Ergebnis von `sanitizeImportedItem(dataType, validated)` zurück statt `validated`. Achtung auf den tatsächlichen Typ-Bezeichner an dieser Stelle im Quelltext — nimm den Wert, mit dem auch `IO_SCHEMA` indiziert wird.

    (4) `importDataGlobal()` verdrahten: setze eine Schleife über die Einträge von `HTML_FIELDS_BY_TYPE` unmittelbar VOR die `choice`-Verzweigung — also nach der Meta-Feld-Bereinigung und nach der Charakter-Migration, aber vor `if (choice)`. Für jeden Typ, dessen Wert in `imp` ein Feld-Array ist, wird jedes Element durch `sanitizeImportedItem()` ersetzt. Diese Position ist entscheidend: der Zweig „als neue Kampagne importieren" schreibt die Daten direkt über `StorageAPI.setJSON` in einen neuen Speicherschlüssel und ist ohne diese Vorschaltung genauso ungeschützt wie der Überschreib-Zweig. Setze einen Kommentar an die Schleife, der genau das festhält.

    Es wird KEIN Hinweis, KEIN Protokolleintrag und KEIN Dialog über durchgeführte Bereinigungen ergänzt (D-04: still säubern). Es findet KEINE Migration bestehender Daten statt (D-03).

    Führe anschließend die Unit-Datei aus Task 1 aus — sie muss jetzt vollständig grün sein — und danach die volle Jest-Suite.
  </action>
  <verify>
    <automated>npx jest tests/unit/import-sanitization.test.js && npx jest</automated>
  </verify>
  <acceptance_criteria>
    - `systems/spellslots/import-export.js` enthält die Konstante `HTML_FIELDS_BY_TYPE` mit genau neun Typen und die Funktionsdeklaration `sanitizeImportedItem`
    - Keines der beiden neuen Symbole wird an `window` exportiert
    - In `executeImport()` liefert die `validatedItems`-Abbildung das Ergebnis von `sanitizeImportedItem(...)` zurück
    - In `importDataGlobal()` steht die Sanitisierungs-Schleife im Quelltext VOR `if (choice)` — nachgewiesen durch die Strukturprüfung aus Task 1
    - `npx jest tests/unit/import-sanitization.test.js` endet mit Exit-Code 0, ebenso `npx jest`
    - Verhalten: `git diff systems/spellslots/import-export.js` zeigt keine neue Benachrichtigungs- oder Protokollausgabe im Import-Pfad (D-04) und keinen Code, der bereits gespeicherte Daten umschreibt (D-03)
  </acceptance_criteria>
  <done>Fremddaten werden an der Tür bereinigt — vor dem Speicher, in beiden Import-Wegen und in beiden Zweigen des globalen Imports.</done>
</task>

<task type="auto">
  <name>Task 3: Undo-Punkt und Sicherungskopie im Überschreib-Zweig (WR-03) plus E2E-Nachweis am gespeicherten Zustand</name>
  <files>systems/spellslots/import-export.js, tests/e2e/features/import-security.spec.js</files>
  <read_first>
    - systems/spellslots/import-export.js (Zeilen 339-376 `executeImport()` — das zu kopierende Muster: `saveUndoState('…')` Zeile 351 und der `try`-umschlossene `createAutoBackup()` mit Hinweis Zeile 353-359; Zeilen 569-581 der Überschreib-Zweig von `importDataGlobal()` mit `stopAllTracks()`, `Object.assign(D, imp)`, `renderAll()`, `save()`)
    - .planning/phases/01-stabilisierung/01-REVIEW.md (WR-03: Befund und Fixvorschlag im Wortlaut)
    - systems/backups.js (Zeilen 1-40 `createAutoBackup()` — Rückgabetyp und Fehlerverhalten; die Funktion ist asynchron, deshalb wie in `executeImport()` ohne `await` in einem `try` aufrufen)
    - tests/e2e/features/import-security.spec.js (der in Plan 10-01 angelegte Aufbau — die neue Prüfung wird als zusätzlicher Testfall angehängt, bestehende Fälle bleiben unverändert)
    - CLAUDE.md (Regel „ALWAYS call saveUndoState() before destructive operations")
  </read_first>
  <action>
    (1) Produktionsfix D-07: Ergänze im Überschreib-Zweig von `importDataGlobal()` — also im `else`-Zweig, unmittelbar vor `Object.assign(D, imp)` und vor dem Stoppen der Audiowiedergabe — einen Undo-Punkt mit sprechender deutscher Beschriftung sowie den Aufruf zur Sicherungskopie nach dem Muster aus `executeImport()`: der Sicherungsaufruf steht in einem `try`-Block mit `catch`, der lediglich eine Warnung protokolliert, damit ein fehlgeschlagenes Backup den Import nicht abbricht. Übernimm auch den kurzen Hinweis an den Nutzer, den `executeImport()` beim Anlegen der Sicherungskopie zeigt — dieser Hinweis betrifft die Sicherungskopie, nicht die Sanitisierung, und berührt D-04 daher nicht. Der Zweig „als neue Kampagne importieren" bekommt KEIN Undo und KEIN Backup: dort wird nichts überschrieben.

    (2) E2E-Nachweis am gespeicherten Zustand: hänge an `tests/e2e/features/import-security.spec.js` einen Testfall an, der dieselbe bösartige Datei über denselben Eintrittspunkt einspielt und danach den GESPEICHERTEN Zustand prüft statt der Anzeige — lies `D.wiki[0].content` aus und assertiere, dass darin weder ein Ereignis-Attribut noch ein Bild-Element mit Fehlerbehandlung vorkommt. Prüfe im selben Fall, dass nach dem Import ein Rückgängig-Schritt verfügbar ist (der Undo-Stapel hat mindestens einen Eintrag mehr als vor dem Import).

    Ergänze einen zweiten Testfall für den Zweig „als neue Kampagne importieren": derselbe Dateiinhalt, aber der Bestätigungsdialog wird angenommen. Dieser Zweig lädt die Seite anschließend neu; warte auf den Neustart der Anwendung und assertiere danach, dass der geladene Wiki-Inhalt bereinigt ist. Falls sich der Neustart im `file://`-Kontext nicht stabil abwarten lässt, ersetze diesen Fall NICHT durch eine schwächere Prüfung, sondern decke den Zweig über die bereits vorhandene Quelltext-Strukturprüfung aus Task 1 ab und halte die Begründung in der SUMMARY fest.

    Baue das Bundle und führe die Spec-Datei, danach die volle E2E-Suite und die Jest-Suite aus.
  </action>
  <verify>
    <automated>python build.py && npx playwright test tests/e2e/features/import-security.spec.js && npx jest && npx playwright test</automated>
  </verify>
  <acceptance_criteria>
    - Im `else`-Zweig von `importDataGlobal()` stehen der Undo-Aufruf und der Aufruf zur Sicherungskopie vor `Object.assign(D, imp)`
    - Der Aufruf zur Sicherungskopie ist mit `try`/`catch` umschlossen; ein Fehler dort bricht den Import nicht ab
    - Der Zweig „als neue Kampagne importieren" enthält keinen Undo-Aufruf und keinen Sicherungsaufruf
    - `tests/e2e/features/import-security.spec.js` enthält einen Testfall, der `D.wiki[0].content` nach dem Import ausliest und die Abwesenheit von Ereignis-Attributen darin belegt
    - Derselbe Testfall belegt, dass der Rückgängig-Stapel nach dem Import gewachsen ist
    - `npx playwright test`, `npx jest` und der gezielte Spec-Lauf enden mit Exit-Code 0
  </acceptance_criteria>
  <done>Ein überschreibender Import ist nicht mehr endgültig, und der gespeicherte Zustand selbst ist sauber — nicht nur seine Darstellung.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fremde JSON-Datei → `executeImport()` | Typspezifischer Import mit Schema-Prüfung, aber bisher ohne Inhaltsbereinigung |
| Fremde JSON-Datei → `importDataGlobal()` | Globaler Import ganz ohne Schema-Prüfung; zwei Zweige, beide persistieren |
| Import → bestehende Kampagne | Der Überschreib-Zweig ersetzt den kompletten Datenbestand des Spielleiters |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05 | Tampering | `executeImport()` — `validatedItems` (`import-export.js:292-301`) | high | mitigate | `sanitizeImportedItem()` in der Abbildung; Unit-Nachweis über alle neun Typen |
| T-10-06 | Tampering | `importDataGlobal()` Zweig „neue Kampagne" (`StorageAPI.setJSON`, `import-export.js:530-568`) | high | mitigate | Sanitisierungs-Schleife VOR der Verzweigung; Quelltext-Strukturprüfung erzwingt die Position dauerhaft |
| T-10-07 | Tampering | `importDataGlobal()` Zweig „überschreiben" (`Object.assign(D, imp)`, `import-export.js:573`) | high | mitigate | Dieselbe vorgeschaltete Schleife; zusätzlich E2E-Nachweis am gespeicherten Zustand |
| T-10-08 | Denial of Service / Datenverlust | Überschreiben der aktuellen Kampagne ohne Rückgängig-Punkt und ohne Sicherungskopie (WR-03) | high | mitigate | `saveUndoState()` + `createAutoBackup()` vor dem Datenwechsel, nach dem Muster von `executeImport()` |
| T-10-09 | Tampering | Übermäßige Sanitisierung nicht gelisteter Felder zerstört legitime Spielleiter-Daten | medium | mitigate | Feldliste auf die neun auditierten Felder begrenzt; Unit-Test verlangt zeichenidentische Rückgabe für nicht gelistete Felder |
| T-10-10 | Information Disclosure | Meldung über durchgeführte Bereinigungen könnte Inhalt der Fremddatei preisgeben | low | accept | D-04: es wird gar nicht gemeldet — es entsteht keine solche Ausgabe |
| T-10-SC | Tampering | npm/pip/cargo-Installationen | low | accept | Diese Phase installiert kein Paket (RESEARCH §Package Legitimacy Audit: nicht anwendbar) |
</threat_model>

## Artifacts this phase produces

Siehe die vollständige Liste in `10-01-PLAN.md`. Von DIESEM Plan neu erzeugt:

- `HTML_FIELDS_BY_TYPE` — modul-interne Konstante in `systems/spellslots/import-export.js`
- `sanitizeImportedItem(type, item)` — modul-interne Funktion in `systems/spellslots/import-export.js`
- `tests/unit/import-sanitization.test.js` — neue Testdatei
- Zusätzliche Testfälle in `tests/e2e/features/import-security.spec.js` (gespeicherter Zustand, Rückgängig-Verfügbarkeit, zweiter Import-Zweig)

<verification>
- Unit-Datei vor der Implementierung rot, danach grün — beides protokolliert
- Alle neun Entity-Typen im Vektor-Katalog abgedeckt
- Quelltext-Strukturprüfung belegt die Position der Sanitisierungs-Schleife vor der Verzweigung
- E2E belegt Bereinigung am gespeicherten Zustand und den vorhandenen Rückgängig-Schritt
- `npx jest` und `npx playwright test` grün
</verification>

<success_criteria>
- Beide Import-Eintrittspunkte und beide Zweige des globalen Imports sanitisieren die HTML-tragenden Felder (D-01, Pitfall 2)
- Nicht gelistete Felder bleiben zeichenidentisch (D-02)
- WR-03 ist geschlossen: kein überschreibender Import ohne Undo-Punkt und Sicherungskopie (D-07)
</success_criteria>

<output>
Create `.planning/phases/10-security-h-rtung/10-02-SUMMARY.md` when done
</output>
