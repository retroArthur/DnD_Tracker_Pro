---
phase: 10-security-h-rtung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/editors/markdown-converter.js
  - features/wiki/wiki.js
  - tests/e2e/features/import-security.spec.js
  - tests/e2e/features/wiki.spec.js
autonomous: true
requirements: [SEC-01]

must_haves:
  truths:
    - "Ein Wiki-Eintrag, dessen Inhalt aus einer bösartigen Import-Datei stammt, lässt sich öffnen, ohne dass ein Ereignis-Handler feuert, ein Skript-Element im DOM landet oder ein Marker-Flag im Fenster gesetzt wird (D-01, SEC-01 Kriterium 1+2)"
    - "renderMarkdownInContent() gibt am Ende sanitisiertes Markup zurück — dieselbe Schutzschicht, die markdownToHtml() in derselben Datei bereits hat (D-01)"
    - "Der Regressionstest ist vor dem Fix nachweislich rot gelaufen und danach grün — das Rot ist mit Ausgabe im SUMMARY protokolliert (D-13)"
    - "Die Sprungmarken des Wiki-Inhaltsverzeichnisses funktionieren nach der Umstellung weiterhin: ein Klick auf einen Eintrag der Inhaltsliste scrollt zur zugehörigen Überschrift (RESEARCH Pitfall 1)"
    - "Der Textinhalt eines Wiki-Eintrags bleibt bei der Anzeige vollständig erhalten — die Sanitisierung entfernt Markup, niemals Text (Leitplanke v1.1: verhaltensneutral)"
  artifacts:
    - "tests/e2e/features/import-security.spec.js — E2E-Beweis der vollständigen Exploit-Kette (Import-Datei → Import → Wiki-Eintrag öffnen → kein Skript-Execute)"
    - "Sanitisierungs-Aufruf am Ende von renderMarkdownInContent() in ui/editors/markdown-converter.js"
    - "Umgekehrte Aufrufreihenfolge in renderWikiDetail() (features/wiki/wiki.js): Markdown-Rendering inkl. Sanitisierung zuerst, Anker-Injektion danach"
    - "TOC-Anker-Regressionstest in tests/e2e/features/wiki.spec.js"
  key_links:
    - "Import-Datei → importDataGlobal() → D.wiki[] → renderWikiDetail() → renderMarkdownInContent() (sanitisiert) → parseWikiLinks() → detail.innerHTML — die Sanitisierung MUSS vor parseWikiLinks() liegen, weil parseWikiLinks() seinen Linktext unescaped einsetzt"
    - "addTOCAnchors() → id=\"toc-N\"-Attribute → sanitizeHTML()s allowedAttributes kennt kein id → Anker-Injektion muss NACH der Sanitisierung laufen, sonst verschwinden die Sprungmarken lautlos"
    - "renderMarkdownInContent() → elf weitere Entity-Renderer, die den Rückgabewert bereits in sanitizeHTML() wrappen → doppelte Sanitisierung muss idempotent bleiben (kein Markup-Unterschied in deren Ansichten)"
  prohibitions:
    - statement: "Ein Sicherheitstest dieser Phase darf NICHT als Nachweis gelten, wenn er auch ohne den Produktionscode bestehen würde — jede Assertion muss echten Produktionscode ausführen (Präzedenzfall: tests/unit/markdown-converter.test.js prüft ausschließlich seine eigenen Eingabe-Literale und beweist damit nichts, RESEARCH Pitfall 4)"
      status: flagged-unverified
      verification: test
---

<objective>
Schließe die Anzeige-Grenze der CR-01-Exploit-Kette und beweise sie end-to-end: `renderMarkdownInContent()` sanitisiert künftig wie sein Zwilling `markdownToHtml()`, die Aufrufreihenfolge in `renderWikiDetail()` wird so gedreht, dass die Inhaltsverzeichnis-Anker die Sanitisierung überleben, und ein neuer E2E-Test führt die vollständige Kette vor — von der bösartigen Import-Datei bis zum geöffneten Wiki-Eintrag.

Purpose: Der Wiki-Anzeigepfad ist laut Render-Pfad-Audit der EINZIGE heute live ausnutzbare Pfad des in `01-REVIEW.md` als Critical gemeldeten Import-XSS. Wird er geschlossen und der Beweis geführt, ist der eigentliche Angriff tot; alles Weitere in dieser Phase ist Verteidigung in der Tiefe.
Output: Gefixte Anzeige-Grenze, gedrehte Aufrufreihenfolge, neue E2E-Beweisdatei, TOC-Regressionstest.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-security-h-rtung/10-CONTEXT.md
@.planning/phases/10-security-h-rtung/10-RESEARCH.md
@.planning/phases/10-security-h-rtung/10-PATTERNS.md
@.planning/phases/10-security-h-rtung/10-VALIDATION.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Exploit-Kette „bösartige Import-Datei → Wiki-Anzeige" end-to-end schließen und beweisen</name>
  <files>tests/e2e/features/import-security.spec.js, ui/editors/markdown-converter.js, features/wiki/wiki.js</files>
  <read_first>
    - ui/editors/markdown-converter.js (Zeilen 189-247 `markdownToHtml()` — das Vorbild mit `const sanitizeHTML = window.sanitizeHTML;` am Funktionsanfang und dem `typeof sanitizeHTML === 'function'`-Guard am Ende; Zeilen 258-300 `renderMarkdownInContent()` — der zu ergänzende Zwilling)
    - features/wiki/wiki.js (Zeilen 401-460 `renderWikiDetail()`: `addTOCAnchors()` Zeile 423, `renderMarkdownInContent()` Zeile 426-428, `parseWikiLinks()` Zeile 429, `renderWikiTOC(entry.content)` Zeile 433, `detail.innerHTML` Zeile 460; Zeilen 984-1017 `extractWikiTOC()`/`renderWikiTOC()`/`addTOCAnchors()`)
    - utils/basic.js (Zeilen 58-160 `sanitizeHTML()`: die Vorbereinigungs-Regex Zeilen 61-67, `allowedTags` Zeilen 72-101 — enthält weder `img` noch `script` noch `code`, `allowedAttributes` Zeilen 102-126 — enthält KEIN `id`)
    - systems/spellslots/import-export.js (Zeilen 464-588 `importDataGlobal()`: `$('import-file')`, `confirm()`-Verzweigung Zeile 482-486, Überschreib-Zweig `Object.assign(D, imp)` Zeile 573 — der Pfad, den der E2E-Test fährt)
    - assets/templates/view-tools.html (Zeile 335: `<input type="file" id="import-file" accept=".json" data-action="import-data-global" style="display: none;">` — der Eintrittspunkt für `setInputFiles`)
    - tests/e2e/features/editor-insert.spec.js (Zeilen 16-51: `gotoBundleFresh()`, `saveAndReopenWikiEntry()` — inkl. des Aufklapp-Musters für `[data-wiki-category="locations"]`; Zeilen 223-285: T-09-01 als Vorlage für Marker-Flag- und `pageerror`-Assertions)
    - tests/e2e/helpers/test-utils.js (vorhandene Helfer, u. a. `loadApp`, `clearAppData`)
  </read_first>
  <action>
    Arbeite in drei Schritten und halte die Reihenfolge ein — der rote Lauf ist Teil des Beweises.

    SCHRITT 1 — Beweisdatei anlegen und ROT fahren.
    Lege `tests/e2e/features/import-security.spec.js` an (deutsche Testnamen, `// @ts-check`, `import { test, expect } from '@playwright/test';`, dieselbe `file://`-Ladefunktion wie in `editor-insert.spec.js`). Der Kern-Testfall stellt den wörtlichen Review-Exploit aus `01-REVIEW.md` nach:

    - Baue ein Kampagnen-JSON-Objekt mit `_campaignName`, `_exportDate` und einem `wiki`-Array mit genau einem Eintrag: `id: 1`, `title: 'Boesartiger Eintrag'`, `category: 'locations'`, `content` = der Payload `<img src=x onerror="window.__xssImport10 = true">` plus dem Klartext `HarmloserText` dahinter (der Klartext dient dem Nachweis, dass Text erhalten bleibt).
    - Lade das gebaute Bundle, registriere `page.on('pageerror', …)` und `page.on('console', …)` für Fehler, und spiele die Datei über den echten Eintrittspunkt ein: `page.setInputFiles('#import-file', { name: 'boese-kampagne.json', mimeType: 'application/json', buffer: Buffer.from(JSON.stringify(payload)) })`. Der `confirm()`-Dialog in `importDataGlobal()` wird von Playwright standardmäßig abgewiesen — das ist genau der Zweig „Aktuelle Kampagne überschreiben", der ohne `location.reload()` auskommt. Registriere den Dialog-Handler explizit (`page.on('dialog', d => d.dismiss())`), damit die Absicht im Test sichtbar ist, statt sich auf den Standard zu verlassen. Falls das `change`-Ereignis der Dateiauswahl den Import nicht selbst auslöst, rufe `importDataGlobal()` anschließend explizit über `page.evaluate` auf.
    - Wechsle zur Wiki-Ansicht, klappe bei Bedarf die Kategorie auf (Muster aus `saveAndReopenWikiEntry()`) und öffne den importierten Eintrag über `.wiki-tree-item[data-action="select-wiki-entry"]`.
    - Assertiere gegen `#wiki-detail`: (a) kein Element mit einem Attribut, dessen Name mit `on` beginnt; (b) `querySelectorAll('script').length` ist 0; (c) `querySelectorAll('img').length` ist 0; (d) `window.__xssImport10` ist `undefined`; (e) der gesammelte Fehler-Array ist leer; (f) `textContent` enthält weiterhin `HarmloserText`.
    - Ergänze einen zweiten Testfall mit weiteren Vektoren aus dem Katalog (D-15) im selben `content`-Feld: ein `<script>`-Element, ein `javascript:`-Link (`<a href="javascript:window.__xssImport10=true">Klick</a>`) und ein SVG mit `onload`. Assertiere dieselben Kriterien plus: kein `a`-Element mit einem `href`, dessen Wert mit `javascript` beginnt.

    Baue das Bundle (`python build.py`) und führe die neue Spec-Datei aus. Sie MUSS jetzt fehlschlagen (der Ereignis-Handler feuert bzw. das Attribut überlebt). Halte die Fehlerausgabe fest — sie kommt als Beleg in die SUMMARY. Ein Test, der schon vor dem Fix grün ist, prüft nicht die Kette und muss überarbeitet werden.

    SCHRITT 2 — Anzeige-Grenze schließen (`ui/editors/markdown-converter.js`).
    Ergänze `renderMarkdownInContent()` exakt nach dem Muster von `markdownToHtml()` in derselben Datei: hole `window.sanitizeHTML` in eine lokale Konstante am Funktionsanfang und wende sie am ENDE der Funktion — nach allen Markdown-Ersetzungen, unmittelbar vor `return` — auf das Ergebnis an, geschützt durch den vorhandenen `typeof … === 'function'`-Guard. Die frühe Rückgabe für leere/nicht-String-Eingaben bleibt unverändert; alle bestehenden Ersetzungsschritte bleiben unangetastet. Halte dich an die Build-Dedup-Regeln aus CLAUDE.md: keine funktions-lokale `const`-Zuweisung eines Namens, der anderswo als Top-Level-`const` existiert — `markdownToHtml()` verwendet in dieser Datei bereits genau dieses Muster, ziehe es identisch nach.

    SCHRITT 3 — Aufrufreihenfolge in `renderWikiDetail()` drehen (`features/wiki/wiki.js`).
    Heute läuft `addTOCAnchors()` VOR `renderMarkdownInContent()`. Nach Schritt 2 würde die Sanitisierung die gerade eingefügten `id`-Attribute wieder entfernen (`allowedAttributes` kennt kein `id`) und die Sprungmarken lautlos zerstören. Drehe die Reihenfolge: erst `renderMarkdownInContent(entry.content || '')` (inklusive Sanitisierung), dann `addTOCAnchors()` auf das bereits bereinigte Markup, dann unverändert `parseWikiLinks()`. Die Reihenfolge „Sanitisierung vor `parseWikiLinks()`" bleibt damit erhalten und ist sicherheitskritisch — `parseWikiLinks()` setzt seinen Linktext unescaped in einen `span` ein und ist nur deshalb ungefährlich, weil vorher global bereinigt wurde.

    `renderWikiTOC(entry.content || '')` in Zeile 433 bleibt UNVERÄNDERT auf dem Rohinhalt. Begründung, die als Code-Kommentar an die gedrehte Stelle gehört: die Inhaltsliste erscheint erst ab drei `<h2>`- bis `<h4>`-Tags, und solche Tags speichert der Editor als HTML; für solche Inhalte liefern `extractWikiTOC()` auf dem Rohinhalt und `addTOCAnchors()` auf dem gerenderten Inhalt dieselbe Trefferreihenfolge und damit dieselben `toc-N`-Kennungen. Dokumentiere im selben Kommentar die Restbedingung: enthält ein Eintrag gleichzeitig HTML-Überschriften UND Markdown-Überschriften in Rautenschreibweise, können die Indizes auseinanderlaufen — bekannte Einschränkung, in der SUMMARY festzuhalten.

    Baue erneut und führe die neue Spec-Datei aus: jetzt grün. Danach die volle E2E-Suite und die Jest-Suite, um auszuschließen, dass die doppelte Sanitisierung in den elf bereits wrappenden Entity-Renderern Markup verändert.
  </action>
  <verify>
    <automated>python build.py && npx playwright test tests/e2e/features/import-security.spec.js && npx playwright test && npx jest</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/features/import-security.spec.js` existiert und enthält mindestens zwei Testfälle; einer davon verwendet den wörtlichen Review-Exploit-Vektor mit `img`-Element und Fehler-Ereignisattribut
    - Der Test spielt die Datei über `page.setInputFiles('#import-file', …)` ein — also über den echten Eintrittspunkt, nicht durch direktes Schreiben in `D` oder in den LocalStorage
    - Verhalten (vor dem Fix): der Lauf der neuen Spec-Datei gegen das ungepatchte Bundle schlägt fehl; die Fehlerausgabe ist in der SUMMARY zitiert
    - Verhalten (nach dem Fix): `npx playwright test tests/e2e/features/import-security.spec.js` endet mit Exit-Code 0
    - `renderMarkdownInContent()` in `ui/editors/markdown-converter.js` ruft `sanitizeHTML` auf dem Ergebnis auf, bevor es zurückgegeben wird, und verwendet dabei denselben `typeof`-Guard wie `markdownToHtml()`
    - In `renderWikiDetail()` steht der Aufruf von `renderMarkdownInContent` VOR dem Aufruf von `addTOCAnchors`, und `parseWikiLinks` läuft weiterhin als letzter Schritt vor der Zuweisung an `detail.innerHTML`
    - `npx playwright test` (volle Suite) und `npx jest` enden mit Exit-Code 0 — keine bestehende Assertion in einem der elf bereits wrappenden Entity-Renderer bricht
  </acceptance_criteria>
  <done>Der in 01-REVIEW.md als Critical gemeldete Exploit ist auf seinem einzigen live ausnutzbaren Pfad tot, und ein Test beweist es von der Datei bis zum gerenderten Eintrag.</done>
</task>

<task type="auto">
  <name>Task 2: Inhaltsverzeichnis-Sprungmarken und Textererhalt gegen die Reihenfolge-Änderung absichern</name>
  <files>tests/e2e/features/wiki.spec.js</files>
  <read_first>
    - tests/e2e/features/wiki.spec.js (Aufbau, `beforeEach` mit `switchView('wiki')`, bestehende Testnamen und Selektor-Konventionen — die neuen Tests fügen sich in eine bestehende oder neue `test.describe`-Gruppe ein)
    - features/wiki/wiki.js (Zeilen 984-1017 `extractWikiTOC()`, `renderWikiTOC()` — Schwelle „mindestens drei Überschriften", Aktion `wiki-toc-jump` mit `data-target`; Zeile 1018-1024 `scrollToTOCHeading()` — setzt kurzzeitig eine Hintergrundfarbe auf der Zielüberschrift)
    - features/wiki/wiki.js (Zeile 676-705 `saveWikiEntry()` — der Speicherpfad sanitisiert bereits; deshalb enthält gespeicherter Wiki-Inhalt nur erlaubte Tags)
    - utils/basic.js (Zeilen 72-101 `allowedTags` — `code` fehlt in der Liste; das ist die Ursache der unten zu prüfenden Anzeige-Konsequenz)
    - tests/e2e/features/editor-insert.spec.js (Zeilen 30-51 `saveAndReopenWikiEntry()` — Muster für Speichern, Neuladen und Wiederöffnen eines Wiki-Eintrags)
  </read_first>
  <action>
    Ergänze in `tests/e2e/features/wiki.spec.js` zwei Regressionstests, die die Anzeige-Änderung aus Task 1 absichern.

    TEST A — Sprungmarken des Inhaltsverzeichnisses. Lege einen Wiki-Eintrag mit mindestens drei Überschriften der zweiten Ebene an (als HTML im Rich-Text-Feld, damit der Speicherpfad sie unverändert durchlässt), speichere ihn und öffne die Detailansicht. Assertiere: (a) der Block mit der Inhaltsliste ist sichtbar und enthält drei Einträge; (b) in der Detailansicht existieren drei Überschriftselemente mit den Kennungen `toc-0`, `toc-1` und `toc-2`; (c) ein Klick auf den dritten Eintrag der Inhaltsliste führt dazu, dass die zugehörige Überschrift die Sprung-Hervorhebung erhält (die Funktion setzt beim Sprung eine Hintergrundfarbe auf dem Zielelement — prüfe darauf statt auf die Scrollposition, das ist in einem kurzen Viewport stabiler). Keine feste Wartezeit verwenden; auf den Zustandswechsel warten.

    TEST B — Textererhalt bei der Anzeige-Sanitisierung. Lege einen Wiki-Eintrag an, dessen Inhalt sowohl erlaubtes Markup (fett, kursiv, Liste) als auch einen Abschnitt in Backtick-Schreibweise enthält, speichere und öffne ihn. Assertiere, dass der sichtbare Text der Detailansicht ALLE eingegebenen Textbestandteile enthält — auch den Teil, der zuvor in Backticks stand. Dieser Test hält die Leitplanke fest: die neue Sanitisierung darf Auszeichnung entfernen, aber niemals Text.

    Halte in der SUMMARY die Anzeige-Konsequenz fest, die dieser Test bewusst zulässt: die Backtick-Schreibweise wurde bisher NUR in der Wiki-Ansicht als eigenes Auszeichnungselement dargestellt, weil `wiki.js` als einziger Renderer den Rückgabewert nicht bereits sanitisiert hat; in allen anderen Entity-Ansichten fiel diese Auszeichnung schon vorher weg, weil das Element nicht in der erlaubten Tag-Liste steht. Nach dem Fix verhält sich die Wiki-Ansicht wie alle anderen — der Text bleibt vollständig erhalten, nur die Hintergrund-Auszeichnung entfällt. Das ist keine offene Aufgabe, sondern eine dokumentierte Folge des Sicherheitsfixes; sie gehört als Notiz in die SUMMARY und wird in Plan 10-05 in die Bilanz aufgenommen.

    Führe abschließend die Wiki-Spec und die volle E2E-Suite aus.
  </action>
  <verify>
    <automated>python build.py && npx playwright test tests/e2e/features/wiki.spec.js && npx playwright test</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/features/wiki.spec.js` enthält einen Test, der drei Überschriftselemente mit den Kennungen `toc-0`, `toc-1`, `toc-2` in der Wiki-Detailansicht nachweist
    - Derselbe Test klickt einen Eintrag der Inhaltsliste an und assertiert die Sprung-Hervorhebung auf der zugehörigen Überschrift
    - `tests/e2e/features/wiki.spec.js` enthält einen Test, der nachweist, dass der sichtbare Text eines gespeicherten Eintrags nach dem Öffnen alle eingegebenen Textbestandteile enthält
    - Kein neuer Test verwendet eine feste Zeitverzögerung als Wartebedingung (Phase-8-Testregel)
    - `npx playwright test tests/e2e/features/wiki.spec.js` und `npx playwright test` enden mit Exit-Code 0
    - Die SUMMARY nennt die Anzeige-Konsequenz für die Backtick-Auszeichnung in der Wiki-Ansicht als dokumentierte Folge, nicht als offenen Fehler
  </acceptance_criteria>
  <done>Die zwei Nebenwirkungen der Reihenfolge-Änderung — Sprungmarken und Textererhalt — sind automatisiert abgesichert statt nur behauptet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Import-Datei → `D` | Fremde JSON-Datei wird ungeprüfter Dateninhalt der Anwendung |
| `D` → `innerHTML` (Wiki-Detailansicht) | Gespeicherter Inhalt wird als Markup ins Dokument geschrieben — der Ausführungspunkt |
| Markdown-Konvertierung → Wiki-Link-Ersetzung | `parseWikiLinks()` setzt Linktext unescaped ein und verlässt sich darauf, dass vorher global bereinigt wurde |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering / Elevation of Privilege | `renderMarkdownInContent()` → `features/wiki/wiki.js:460` (`detail.innerHTML`) | critical | mitigate | `sanitizeHTML()` am Ende von `renderMarkdownInContent()` (D-01); E2E-Beweis über die vollständige Kette Datei → Import → Anzeige in `tests/e2e/features/import-security.spec.js` |
| T-10-02 | Tampering | `parseWikiLinks()` (`wiki.js:634-641`) — unescaped Linktext | high | mitigate | Aufrufreihenfolge festgeschrieben: Sanitisierung läuft vor `parseWikiLinks()`; Akzeptanzkriterium und Code-Kommentar halten die Reihenfolge fest |
| T-10-03 | Repudiation | Regressionstest als Beweismittel | high | mitigate | Der Test wird zuerst gegen den ungepatchten Build rot gefahren; das Rot wird mit Ausgabe in der SUMMARY protokolliert, sonst gilt der Beweis als nicht geführt |
| T-10-04 | Denial of Service | Verlust der Inhaltsverzeichnis-Sprungmarken durch entfernte `id`-Attribute | medium | mitigate | Anker-Injektion nach der Sanitisierung; die Kennungen sind codegeneriert (`toc-N`, keine Nutzereingabe); E2E-Regressionstest in `wiki.spec.js` |
| T-10-SC | Tampering | npm/pip/cargo-Installationen | low | accept | Diese Phase installiert kein Paket (RESEARCH §Package Legitimacy Audit: nicht anwendbar) — keine Lieferketten-Angriffsfläche |
</threat_model>

## Artifacts this phase produces

Vollständige Liste der von Phase 10 neu erzeugten Symbole und Dateien (gilt für alle Pläne dieser Phase; die übrigen Pläne verweisen hierauf):

- `tests/e2e/features/import-security.spec.js` (neue Datei, Plan 10-01)
- `HTML_FIELDS_BY_TYPE` (neue modul-interne Konstante in `systems/spellslots/import-export.js`, Plan 10-02)
- `sanitizeImportedItem()` (neue modul-interne Funktion in `systems/spellslots/import-export.js`, Plan 10-02)
- `tests/unit/import-sanitization.test.js` (neue Datei, Plan 10-02)
- `tests/unit/sanitizer-parity.test.js` (neue Datei, Plan 10-03)
- `'strike'` als neuer Eintrag in `allowedTags` (`utils/basic.js`, `utils/testable-utils.js`, Plan 10-03)
- `SECURITY.md` (neue Datei im Repo-Root, Plan 10-05)
- `.planning/phases/01-stabilisierung/01-SECURITY.md`, `.planning/phases/02-*/02-SECURITY.md`, `.planning/phases/09-*/09-SECURITY.md`, `.planning/phases/10-security-h-rtung/10-SECURITY.md` (neue Per-Phasen-Audit-Artefakte, Plan 10-05)
- Neue Testfälle in bestehenden Dateien: `tests/e2e/features/wiki.spec.js` (Plan 10-01), `tests/unit/security.test.js` (Plan 10-03), `tests/e2e/features/editor-formatting.spec.js` (Plan 10-03), `tests/e2e/features/editor-insert.spec.js` (Plan 10-04)

## Edge-Coverage-Probe (spec-loser Rückfall)

Diese Phase hat keine SPEC.md; die Kanten-Sonde lief ersatzweise über SEC-01 und SEC-02. Ergebnis und Verbleib:

| Zeile | Anforderung | Kategorie | Status | Verbleib |
|-------|-------------|-----------|--------|----------|
| 1 | SEC-01 | unclassified | **unresolved** | Als markierte Annahme geführt (siehe unten) — nicht stillschweigend verworfen |
| 2 | SEC-02 | empty | covered | Als Wahrheit in `10-02-PLAN.md` (`must_haves.truths`) formuliert |
| 3 | SEC-02 | encoding | covered | Als Wahrheit in `10-03-PLAN.md` (`must_haves.truths`) formuliert |

**Markierte Annahme (Zeile 1, unresolved):** Die Sonde konnte SEC-01 keiner Kanten-Kategorie zuordnen. Angenommen wird: die Anforderung ist mit den in dieser Phase geplanten Nachweisen vollständig abgedeckt — E2E-Beweis der Exploit-Kette (Plan 10-01), Feldabdeckung am Import (Plan 10-02), Vektor-Katalog gegen den echten Sanitizer (Plan 10-03), Paste-Pfad (Plan 10-04). Sollte der Abschluss-Audit (Plan 10-05) einen weiteren ungeschützten Ausführungspunkt finden, greift D-12: Critical/High wird noch in dieser Phase behoben.

<verification>
- Neue Spec-Datei vor dem Fix rot, nach dem Fix grün — beides protokolliert
- `npx playwright test` und `npx jest` grün
- Inhaltsverzeichnis-Sprungmarken automatisiert nachgewiesen
- Textererhalt bei der Anzeige-Sanitisierung automatisiert nachgewiesen
- Nur die drei vorgesehenen Dateien geändert plus die zwei Testdateien (`git diff --name-only`)
</verification>

<success_criteria>
- Der einzige live ausnutzbare Pfad des Import-XSS ist geschlossen (SEC-01 Kriterium 1)
- Ein Regressionstest belegt die vollständige Kette von der Datei bis zur Anzeige (SEC-01 Kriterium 2)
- Keine bestehende Ansicht verliert Text; die Sprungmarken funktionieren weiterhin
</success_criteria>

<output>
Create `.planning/phases/10-security-h-rtung/10-01-SUMMARY.md` when done
</output>
