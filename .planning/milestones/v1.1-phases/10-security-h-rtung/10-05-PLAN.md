---
phase: 10-security-h-rtung
plan: 05
type: execute
wave: 5
depends_on: ["10-04"]
files_modified:
  - SECURITY.md
  - .planning/phases/01-stabilisierung/01-SECURITY.md
  - .planning/phases/02-technik-fundament/02-SECURITY.md
  - .planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-SECURITY.md
  - .planning/phases/10-security-h-rtung/10-SECURITY.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements: [SEC-02]

must_haves:
  truths:
    - "Für jede der vier Angriffsflächen — Import/Export, Storage und IndexedDB, Datei-Backup, Rich-Text und innerHTML — existiert ein Audit-Ergebnis, das die konkret geprüften Dateien und Codepfade benennt (D-09, SEC-02)"
    - "Eine konsolidierte SECURITY.md im Repository-Wurzelverzeichnis führt threats_open: 0 und ist aus den Per-Phasen-Audit-Artefakten gespeist (D-10)"
    - "Jeder Threat ist entweder behoben oder mit schriftlicher Begründung als akzeptiertes Risiko eingestuft; ein offener Threat ohne Begründung blockiert den Phasenabschluss (D-12)"
    - "Die beiden bewusst akzeptierten Risiken — keine Inhaltssicherheitsrichtlinie und die Breite der Klassen- und Stil-Erlaubnis im Sanitizer — sind mit Begründung dokumentiert (D-08)"
    - "Der Audit läuft gegen den Stand NACH allen Fixes dieser Phase; die volle Test-Suite ist vor dem Audit grün (D-11)"
  artifacts:
    - "SECURITY.md im Repository-Wurzelverzeichnis mit threats_open: 0 und den vier Angriffsflächen"
    - "Per-Phasen-Audit-Artefakte für die Phasen 1, 2, 9 und 10 unterhalb von .planning/phases/"
    - "Aktualisierte Anforderungs-Rückverfolgung für SEC-01 und SEC-02 in .planning/REQUIREMENTS.md"
  key_links:
    - "Audit-Ablauf je Phase → Per-Phasen-Artefakt in .planning/phases/<phase>/ → konsolidierte SECURITY.md im Wurzelverzeichnis"
    - "Angriffsfläche Import/Export und Storage → Phase 1; Datei-Backup → Phase 2; Rich-Text und innerHTML → Phase 9 und die Fixes dieser Phase"
    - "Threat-Register der Pläne 10-01 bis 10-04 → Dispositionen in SECURITY.md → threats_open-Zähler"
  prohibitions:
    - statement: "Der Zählerstand threats_open: 0 darf NICHT durch Verengung des Prüfumfangs, durch stilles Herabstufen oder Weglassen eines Fundes oder durch Behauptung einer Prüfung erreicht werden, die nicht stattgefunden hat — jede der vier Angriffsflächen muss die konkret geprüften Dateien und Codepfade benennen, und jedes akzeptierte Risiko braucht eine geschriebene Begründung"
      status: flagged-unverified
      verification: judgment
---

<objective>
Führe den Abschluss-Audit über die vier kritischen Angriffsflächen und konsolidiere das Ergebnis in einer SECURITY.md im Repository-Wurzelverzeichnis mit `threats_open: 0`. Dies ist der letzte Schritt der Phase — er dokumentiert den Stand nach allen Fixes, nicht davor.

Purpose: Die Anforderung SEC-02 verlangt einen aktuellen Audit ohne offene Findings. Ohne eine belastbare, öffentlich sichtbare Bilanz bleibt die Sicherheitslage dieser Anwendung eine Sammlung verstreuter Planungsnotizen.
Output: Vier Per-Phasen-Audit-Artefakte, eine konsolidierte SECURITY.md, aktualisierte Rückverfolgung und Projektzustand.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-security-h-rtung/10-CONTEXT.md
@.planning/phases/10-security-h-rtung/10-RESEARCH.md
@.planning/phases/10-security-h-rtung/10-01-SUMMARY.md
@.planning/phases/10-security-h-rtung/10-02-SUMMARY.md
@.planning/phases/10-security-h-rtung/10-03-SUMMARY.md
@.planning/phases/10-security-h-rtung/10-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Volle Suite grün stellen und den Audit über die Phasen 1, 2, 9 und 10 führen</name>
  <files>.planning/phases/01-stabilisierung/01-SECURITY.md, .planning/phases/02-technik-fundament/02-SECURITY.md, .planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-SECURITY.md, .planning/phases/10-security-h-rtung/10-SECURITY.md</files>
  <read_first>
    - $HOME/.claude/gsd-core/workflows/secure-phase.md (der Ablauf: Zustandserkennung, die Regel für rückwirkende Threat-Erhebung bei Phasen ohne Register zur Planungszeit, das Abschluss-Gate und der Ablageort `<phase_dir>/<padded_phase>-SECURITY.md`)
    - $HOME/.claude/gsd-core/templates/SECURITY.md (die Vorlage, die der Ablauf für die Per-Phasen-Artefakte verwendet)
    - .planning/phases/01-stabilisierung/01-REVIEW.md (CR-01 und WR-03 im Wortlaut — der Ausgangsbefund für die Fläche Import/Export)
    - .planning/codebase/CONCERNS.md (Abschnitte zu Sicherheitsüberlegungen und Testabdeckungslücken: die innerHTML-Risikoklasse, die Breite der Klassen- und Stil-Erlaubnis, die fehlende Inhaltssicherheitsrichtlinie, die Drift der Testkopien — Stand 2026-06-11, jede Aussage vor Verwendung gegen den aktuellen Code prüfen)
    - .planning/phases/10-security-h-rtung/10-RESEARCH.md (Abschnitt „Security Domain": die Zuordnung der vier Angriffsflächen zu den Phasen und die Tabelle der anwendbaren Prüfkategorien)
    - .planning/phases/10-security-h-rtung/10-CONTEXT.md (D-09 bis D-12: Zuschnitt, Ablageort, Reihenfolge und die Lesart von threats_open)
    - .planning/WINDOWS.md (der nach Plan 10-04 geschlossene Eintrag — gehört als erledigter Befund in die Bilanz)
  </read_first>
  <action>
    (1) Vorbedingung herstellen: baue das Bundle und führe die volle Jest- sowie die volle Playwright-Suite aus. Beide müssen grün sein, bevor der Audit beginnt (D-11). Ein roter Lauf ist ein Fund, kein Formfehler — Ursache beheben, dann fortfahren.

    (2) Audit je Phase durchführen, in der Reihenfolge 1, 2, 9, 10. Nutze dafür den GSD-Ablauf für den nachgezogenen Sicherheits-Audit: rufe ihn über das Skill-Werkzeug mit dem Namen `gsd-secure-phase` und der jeweiligen Phasennummer auf, sofern dieses Werkzeug zur Verfügung steht. Steht es nicht zur Verfügung, arbeite den Ablauf aus `secure-phase.md` von Hand ab und lege das Ergebnis am selben Ort und im selben Format ab. Die Phasen 1 und 2 haben zur Planungszeit kein Threat-Register erhalten; für sie gilt die rückwirkende Erhebung: erst aus den Implementierungsdateien ein Register aufbauen, dann die Gegenmaßnahmen verifizieren.

    Prüfumfang je Angriffsfläche — jede Aussage muss die konkret gelesene Datei und den konkreten Codepfad benennen, nicht die Fläche pauschal freigeben:
    - Import/Export (Phase 1): die beiden Import-Eintrittspunkte, die Schema-Prüfung, das Größenlimit der Eingabedatei, die neue Feldbereinigung sowie die Export- und Kopierfunktionen.
    - Storage und IndexedDB (Phase 1): der Persistenzpfad, die Konfliktauflösung zwischen lokalem Speicher und Datenbank, die Speicherschlüssel je Kampagne.
    - Datei-Backup (Phase 2): das Sicherungssystem und seine Anbindung an die Dateisystem-Schnittstelle sowie der Registrierungspunkt für Nachspeicher-Rückrufe.
    - Rich-Text und innerHTML (Phase 9 und diese Phase): der Editor mit seinen Selection- und Range-Hilfsfunktionen, der Einfüge-Handler, die Speicherpfade der Entitäten, die Anzeigepfade mit Zuweisung an innerHTML.

    (3) Funde triagieren nach D-12: Threats der Stufen kritisch und hoch werden noch in dieser Phase behoben — lege dafür bei Bedarf einen zusätzlichen Plan an und melde das an den Aufrufer, statt den Fund zu vertagen. Threats der Stufen niedrig und informativ werden mit geschriebener Begründung als akzeptiertes Risiko eingestuft. Ein Threat ohne Disposition blockiert den Abschluss.

    Halte in der SUMMARY je Phase fest: Anzahl geprüfter Threats, Dispositionen und den Pfad des erzeugten Artefakts.
  </action>
  <verify>
    <automated>python build.py && npx jest && npx playwright test && node -e "const fs=require('fs');const g=require('path');const p=['.planning/phases/01-stabilisierung','.planning/phases/02-technik-fundament','.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung','.planning/phases/10-security-h-rtung'];const miss=p.filter(d=>!fs.existsSync(d)||!fs.readdirSync(d).some(f=>/-SECURITY\.md$/.test(f)));if(miss.length){console.error('Fehlende Audit-Artefakte:',miss.join(', '));process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest` und `npx playwright test` enden vor Beginn des Audits mit Exit-Code 0; das Ergebnis ist in der SUMMARY protokolliert
    - In jedem der vier Phasenverzeichnisse (1, 2, 9, 10) liegt ein Audit-Artefakt mit der Endung `-SECURITY.md`
    - Jedes Artefakt nennt für seine Angriffsfläche die konkret geprüften Dateien und Codepfade — nicht nur den Namen der Fläche
    - Jeder erfasste Threat trägt eine Schweregradangabe und eine Disposition (behoben, akzeptiert oder übertragen); akzeptierte Risiken tragen eine nicht leere Begründung
    - Verhalten: Findet der Audit einen Threat der Stufe kritisch oder hoch, wird er in dieser Phase behoben oder als zusätzlicher Plan an den Aufrufer gemeldet — er wird nicht auf einen späteren Meilenstein verschoben
  </acceptance_criteria>
  <done>Die vier Angriffsflächen sind gegen den finalen Codestand geprüft, und jedes Ergebnis liegt schriftlich vor.</done>
</task>

<task type="auto">
  <name>Task 2: Konsolidierte SECURITY.md schreiben und Rückverfolgung sowie Projektzustand aktualisieren</name>
  <files>SECURITY.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - $HOME/.claude/gsd-core/templates/SECURITY.md (Vorlage und Kopfdatenfelder — die konsolidierte Datei orientiert sich daran, ergänzt aber die Gesamtsicht über vier Flächen)
    - die vier in Task 1 erzeugten Per-Phasen-Artefakte (Quelle aller Einträge der Bilanz)
    - .planning/phases/10-security-h-rtung/10-CONTEXT.md (D-08: die beiden bewusst akzeptierten Risiken samt der dort formulierten Begründungen — Einzelnutzer-Anwendung ohne Server, Inhalte sind Daten des Spielleiters, die Richtlinie wäre architekturbedingt auf eine erlaubende Variante angewiesen und müsste zusätzlich im Dateisystem-Modus und in der installierten Variante geprüft werden; D-10: Ablageort und Zweck der konsolidierten Datei; D-12: die Lesart des Zählerstands)
    - .planning/REQUIREMENTS.md (Zeilen 21-24 die Sicherheitsanforderungen, Zeilen 55-66 die Rückverfolgungstabelle — Format der bestehenden Einträge übernehmen)
    - .planning/STATE.md (Abschnitte „Open TODOs" und „Decisions" — Stil der vorhandenen Einträge übernehmen; dort stehen zwei offene Altlasten-Posten zum Sicherheits-Audit und zu den Code-Review-Funden aus Phase 1, die mit dieser Phase erledigt sind)
    - .planning/phases/10-security-h-rtung/10-01-SUMMARY.md bis 10-04-SUMMARY.md (die dort protokollierten Fixes, die dokumentierten Folgen und die bekannten Einschränkungen — sie gehören in die Bilanz)
  </read_first>
  <action>
    (1) Schreibe `SECURITY.md` im Repository-Wurzelverzeichnis. Aufbau (Gliederung ist Ermessenssache, die folgenden Bestandteile sind Pflicht):
    - Kopfdaten mit `threats_open: 0`, Datum des Audits, geprüften Phasen und der verwendeten Prüfstufe.
    - Ein Abschnitt je Angriffsfläche — Import/Export, Storage und IndexedDB, Datei-Backup, Rich-Text und innerHTML — mit den geprüften Dateien und Codepfaden, den erfassten Threats, ihrer Schwere und ihrer Disposition.
    - Ein Abschnitt „In dieser Phase behobene Befunde" mit dem Import-Ausführungspfad über die Wiki-Anzeige, der Import-Feldbereinigung an beiden Eintrittspunkten und in beiden Zweigen, dem fehlenden Rückgängig-Punkt beim überschreibenden Import, dem Einfügepfad für Tabellen-Markup, dem Zauber-Speicherpfad und dem Datenintegritätsfehler bei durchgestrichenem Text.
    - Ein Abschnitt „Bewusst akzeptierte Risiken" mit mindestens drei Einträgen, jeweils mit Begründung: keine Inhaltssicherheitsrichtlinie; die Breite der Klassen- und Stil-Erlaubnis im Sanitizer; die verbleibende Sprödigkeit der regexbasierten Bereinigung im Einfüge-Handler. Ergänze die im Verlauf der Phase dokumentierten Folgen und Einschränkungen: den Wegfall der Hintergrund-Auszeichnung für Abschnitte in Backtick-Schreibweise in der Wiki-Ansicht und die mögliche Index-Abweichung der Inhaltsverzeichnis-Sprungmarken bei gemischten Überschriftenformaten.
    - Ein Abschnitt „Bedrohungsmodell des Nutzungsmodells": Einzelnutzer-Anwendung, kein Server, keine Anmeldung, Daten liegen unverschlüsselt lokal — das ordnet die akzeptierten Risiken ein und macht sie nachvollziehbar.
    - Ein Abschnitt „Meldeweg" für Sicherheitsmeldungen, wie es bei einer Datei dieses Namens im Wurzelverzeichnis erwartet wird.
    - Ein Verweis auf die vier Per-Phasen-Artefakte als Quelle.

    Achte auf die Zusammensetzung des Zählerstands: er ist genau dann null, wenn jeder in den Abschnitten gelistete Threat entweder als behoben oder als begründet akzeptiert geführt wird. Gezählt werden die Zeilen der Threat-Tabellen, nicht Aussagen im Fließtext. Bleibt ein Threat ohne Disposition, ist der Zählerstand nicht null und der Phasenabschluss blockiert.

    (2) Aktualisiere `.planning/REQUIREMENTS.md`: setze die Haken bei SEC-01 und SEC-02 und stelle in der Rückverfolgungstabelle beide Einträge auf abgeschlossen.

    (3) Aktualisiere `.planning/STATE.md`: ergänze unter „Open TODOs" im vorhandenen Stil einen Eintrag zum Abschluss der Phase (Datum, Umfang, Ergebnis) und hake die beiden bestehenden Altlasten-Posten zum Sicherheits-Audit und zu den Code-Review-Funden aus Phase 1 ab, soweit sie durch diese Phase erledigt sind. Ergänze unter „Decisions" die für spätere Phasen tragenden Entscheidungen dieser Phase: die Sanitisierung an beiden Grenzen, die Feldliste als Ergebnis des Render-Pfad-Audits, die Erweiterung der erlaubten Tag-Liste samt ihrer erschwerten Rücknahme und die zwei bewusst akzeptierten Risiken.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('SECURITY.md','utf8');const need=[/threats_open:\s*0/,/Import/i,/Storage/i,/Backup/i,/innerHTML/i];const bad=need.filter(r=>!r.test(s));if(bad.length){console.error('SECURITY.md unvollstaendig:',bad.map(String).join(', '));process.exit(1)}" && node -e "const s=require('fs').readFileSync('.planning/REQUIREMENTS.md','utf8');const ok=/\[x\]\s*\*\*SEC-01\*\*/.test(s)&&/\[x\]\s*\*\*SEC-02\*\*/.test(s);if(!ok){console.error('REQUIREMENTS.md: SEC-01/SEC-02 nicht abgehakt');process.exit(1)}" && npx jest && npx playwright test</automated>
  </verify>
  <acceptance_criteria>
    - `SECURITY.md` existiert im Repository-Wurzelverzeichnis und führt in den Kopfdaten `threats_open: 0`
    - Die Datei enthält je einen eigenen Abschnitt für die vier Angriffsflächen, jeweils mit den geprüften Dateien und Codepfaden
    - Die Datei enthält einen Abschnitt mit den in dieser Phase behobenen Befunden und einen Abschnitt mit mindestens drei bewusst akzeptierten Risiken, jedes mit nicht leerer Begründung
    - Jeder in einer Threat-Tabelle gelistete Eintrag trägt Schwere und Disposition; kein Eintrag ist ohne Disposition
    - `.planning/REQUIREMENTS.md` hakt SEC-01 und SEC-02 ab und führt beide in der Rückverfolgungstabelle als abgeschlossen
    - `.planning/STATE.md` enthält einen neuen Eintrag unter „Open TODOs" mit Datum sowie die tragenden Entscheidungen dieser Phase unter „Decisions"
    - `npx jest` und `npx playwright test` enden mit Exit-Code 0
  </acceptance_criteria>
  <done>Die Sicherheitslage der Anwendung steht an einer Stelle, öffentlich sichtbar, mit null offenen Threats und begründeten Restrisiken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit-Ergebnis → öffentliche Aussage | SECURITY.md im Wurzelverzeichnis ist eine nach außen sichtbare Sicherheitsbehauptung |
| Fund → Disposition | Der Übergang von „gefunden" zu „geschlossen" entscheidet, ob ein Risiko bearbeitet oder nur weggeschrieben wird |
| Planungsartefakte → Bilanz | Verstreute Per-Phasen-Ergebnisse werden zu einer Gesamtaussage verdichtet |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-19 | Repudiation | Zählerstand `threats_open: 0` in SECURITY.md | high | mitigate | Der Zähler ergibt sich aus den Zeilen der Threat-Tabellen; jede Fläche muss die geprüften Dateien und Codepfade benennen; ein Eintrag ohne Disposition blockiert den Abschluss (D-12) |
| T-10-20 | Information Disclosure | Öffentlich sichtbare Beschreibung von Angriffspfaden | low | accept | Die beschriebenen Pfade sind zum Veröffentlichungszeitpunkt behoben; die Anwendung läuft ohne Server im Einzelnutzerbetrieb — es entsteht kein fernnutzbarer Angriffsvorteil |
| T-10-21 | Tampering | Rückwirkende Threat-Erhebung für die Phasen 1 und 2 ohne Register zur Planungszeit | medium | mitigate | Für diese Phasen wird das Register zuerst aus den Implementierungsdateien aufgebaut und danach die Gegenmaßnahme verifiziert — kein Freistempel für eine ungeprüfte Fläche |
| T-10-22 | Denial of Service | Ein neuer Fund der Stufe kritisch oder hoch am Phasenende | medium | mitigate | D-12 schreibt die Behandlung noch in dieser Phase vor; das Akzeptanzkriterium verlangt Fix oder Meldung eines zusätzlichen Plans an den Aufrufer statt einer Vertagung |
| T-10-SC | Tampering | npm/pip/cargo-Installationen | low | accept | Diese Phase installiert kein Paket (RESEARCH §Package Legitimacy Audit: nicht anwendbar) |
</threat_model>

## Artifacts this phase produces

Siehe die vollständige Liste in `10-01-PLAN.md`. Von DIESEM Plan neu erzeugt:

- `SECURITY.md` im Repository-Wurzelverzeichnis
- `.planning/phases/01-stabilisierung/01-SECURITY.md`
- `.planning/phases/02-technik-fundament/02-SECURITY.md`
- `.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-SECURITY.md`
- `.planning/phases/10-security-h-rtung/10-SECURITY.md`
- Aktualisierte Einträge in `.planning/REQUIREMENTS.md` und `.planning/STATE.md`

<verification>
- Volle Jest- und Playwright-Suite grün VOR dem Audit
- Vier Per-Phasen-Audit-Artefakte vorhanden
- SECURITY.md mit `threats_open: 0`, vier Flächen, behobenen Befunden und begründeten Restrisiken
- SEC-01 und SEC-02 in REQUIREMENTS.md abgehakt und in der Rückverfolgung geführt
- STATE.md fortgeschrieben
</verification>

<success_criteria>
- SEC-02 ist erfüllt: aktueller Audit über die vier Angriffsflächen ohne offene Findings (Roadmap-Kriterien 3 und 4)
- Jeder Threat ist behoben oder begründet akzeptiert (D-12)
- Die Bilanz ist an einer Stelle im Wurzelverzeichnis auffindbar (D-10)
</success_criteria>

<output>
Create `.planning/phases/10-security-h-rtung/10-05-SUMMARY.md` when done
</output>
