---
phase: 09-editor-regressionsnetz-execcommand-abl-sung
plan: 04
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - tests/e2e/features/editor-insert.spec.js
  - tests/e2e/features/editor-smoke.spec.js
autonomous: true
requirements: [EDIT-02, EDIT-03]

must_haves:
  truths:
    - "Enter im Editor erzeugt exakt die in 09-BASELINE.md protokollierte Zeilenumbruch-Struktur; Shift+Enter erzeugt exakt die dort protokollierte Absatzstruktur"
    - "Einfügen aus der Zwischenablage erzeugt für Tabellen-HTML, für tabulatorgetrennten Text und für reinen Text jeweils exakt das in 09-BASELINE.md protokollierte Markup"
    - "Ein Einfüge-Inhalt mit Ereignis-Attributen oder Skript-Markup landet weder als ausführbares Element noch als Ereignis-Attribut im Editor-DOM und auch nicht im gespeicherten Eintrag"
    - "In allen fünf weiteren Entity-Editoren (NPCs, Orte, Quests, Sessions, Quick-Referenz) erzeugt Fett über die statische Toolbar dasselbe Markup wie im Referenz-Editor Wiki"
    - "In mindestens zwei weiteren Entity-Editoren erzeugt Fett über die floating Toolbar dasselbe Markup wie im Referenz-Editor Wiki"
    - "Die Markdown-Live-Shortcuts wandeln die Eingabemuster für Fett, Kursiv und Durchgestrichen im Wiki-Editor und in einem weiteren Entity-Editor in das protokollierte Markup um"
  artifacts:
    - "tests/e2e/features/editor-insert.spec.js existiert und deckt Tastatur- und Zwischenablage-Einfügepfade ab"
    - "tests/e2e/features/editor-smoke.spec.js existiert und deckt alle fünf weiteren Entity-Editoren sowie die Markdown-Live-Shortcuts ab"
  key_links:
    - "keydown im .rich-editor -> handleEditorKeydown (rich-text.js:564-576) -> Zeilenumbruch bzw. natives Absatzverhalten"
    - "paste im .rich-editor -> handleEditorPaste (rich-text.js:577-643) -> Attribut-Regex-Bereinigung -> Einfügen -> sanitizeHTML beim Speichern"
    - "Statische Toolbar je Editor ([data-action=format-text][data-cmd=<editor-id>]) -> dieselbe formatText-Engine -> identisches Markup über alle Entity-Editoren"
    - "input im .rich-editor -> handleMarkdownInput (150 ms) -> processMarkdownShortcuts -> MARKDOWN_PATTERNS -> Markup"
  prohibitions:
    - statement: "Ein Smoke-Test darf einen Editor NICHT als abgedeckt melden, wenn sein Formular gar nicht geöffnet wurde — jeder Editor-Smoke muss den Editor sichtbar machen und eine Markup-Assertion durchführen, sonst gilt der Editor als ungetestet"
      status: flagged-unverified
      verification: test
---

<objective>
Vervollständige das Regressionsnetz um die beiden verbleibenden Auslöserklassen: Tastatur-/Zwischenablage-Einfügungen (die fünf Insert-Call-Sites, die in Plan 09-08 migriert werden) und die Verdrahtung der geteilten Editor-Engine in allen weiteren Entity-Editoren inklusive Markdown-Live-Shortcuts (D-04-Smoke, EDIT-02).

Purpose: Ohne diese beiden Blöcke gäbe es für fünf der 21 Call-Sites keinen Vorher-Beweis, und EDIT-02 („alle Entity-Editoren funktionieren unverändert") bliebe unbelegt.
Output: `tests/e2e/features/editor-insert.spec.js`, `tests/e2e/features/editor-smoke.spec.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-CONTEXT.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-RESEARCH.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-PATTERNS.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-02-SUMMARY.md
@ui/editors/rich-text.js
@ui/editors/markdown-shortcuts.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tastatur- und Zwischenablage-Einfügepfade festnageln (inkl. bösartigem Einfüge-Inhalt)</name>
  <files>tests/e2e/features/editor-insert.spec.js</files>
  <read_first>
    - ui/editors/rich-text.js (Zeilen 491–523: `initEditorPasteHandlers` mit der Editor-ID-Liste und dem Setup-Call für den Absatztrenner; Zeilen 550–562: der delegierte `keydown`-Listener auf `.rich-editor`/`.dialog-text-area`; Zeilen 564–576: `handleEditorKeydown` — Strg+Shift+T öffnet die Tabelle, Enter ohne Shift wird abgefangen, Shift+Enter fällt durch; Zeilen 577–643: `handleEditorPaste` mit Tabellen-Erkennung, Attribut-Regex-Bereinigung, tabulatorgetrenntem Zweig und Plaintext-Zweig)
    - .planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md (Inventar-Zeilen 574, 615, 637, 642, 674 mit dem erhobenen Markup sowie Abschnitt A1 mit dem gemessenen Shift+Enter-Verhalten — diese Werte sind die Erwartungswerte)
    - utils/basic.js (Zeilen 56–125: `sanitizeHTML` — die zweite Verteidigungslinie beim Speichern; entscheidend für den bösartigen Einfüge-Testfall)
    - tests/e2e/features/editor-formatting.spec.js (Boot-, Formular- und Roundtrip-Muster)
    - 09-RESEARCH.md — Abschnitt „Security Domain" (die vorgeschaltete Attribut-Bereinigung im Einfüge-Pfad und die Whitelist beim Speichern bleiben unverändert; beide zusammen sind der Schutz)
  </read_first>
  <action>
    Lege `tests/e2e/features/editor-insert.spec.js` an (gleicher Kopf-/Boot-Stil wie die übrigen Netz-Dateien; Ziel-Editor ist `#wiki-content` im geöffneten Wiki-Formular) mit zwei `test.describe`-Blöcken:

    **Block „Tastatur":**
    - Enter ohne Shift: Text eingeben, Enter drücken, weiteren Text eingeben, exakte `innerHTML`-Assertion gegen den Baseline-Wert der Zeile 574.
    - Shift+Enter: gleicher Ablauf mit Shift+Enter, exakte Assertion gegen den in A1 gemessenen Wert. Diesen Test im Namen als A1-Referenz kennzeichnen — Plan 09-09 vergleicht gegen genau ihn, wenn der Setup-Call für den Absatztrenner entfällt.
    - Strg+Shift+T: Tastenkombination im Editor, exakte Assertion auf die eingefügte 3×3-Tabellenstruktur (Baseline-Zeile 674) inklusive exakter Zählung der Kopf- und Datenzellen.

    **Block „Zwischenablage":**
    Da eine echte Systemzwischenablage unter `file://` in Playwright nicht zuverlässig verfügbar ist, wird das Einfüge-Ereignis über `page.evaluate` mit einem selbst gebauten `DataTransfer` auf dem Editor ausgelöst — als dokumentiertes Setup-Vehikel (der geprüfte Pfad ist der Ereignis-Handler der Anwendung, nicht der Browser-Zwischenablage-Dialog). Im Kopfkommentar des Blocks begründen und auf 08-CONTEXT D-06 verweisen.
    - Tabellen-HTML: Einfügen eines HTML-Fragments mit einer Tabelle samt Fremd-Attributen (Stil-, Breiten-, Rahmen-Attribute), die die Bereinigung entfernen soll; exakte Assertion gegen Baseline-Zeile 615.
    - Tabulatorgetrennter Text: mehrzeiliger Text mit Tabulatoren; exakte Assertion gegen Baseline-Zeile 637 inklusive exakter Zeilen-/Zellenzahl.
    - Reiner Text: einzeiliger Text ohne Tabulatoren; exakte Assertion gegen Baseline-Zeile 642.
    - Sicherheitsfall: Einfügen eines HTML-Fragments mit einem Ereignis-Attribut (Fehler-Handler auf einem Bild) und einem Skript-Element. Assertionen: (a) im Editor-DOM existiert danach kein Skript-Element und kein Attribut, dessen Name mit `on` beginnt, (b) nach Speichern und Reload gilt dasselbe für den geladenen Eintrag, (c) der gesammelte `pageerror`-Array enthält keinen Hinweis auf ausgeführten Fremdcode. Diesen Test im Namen als Sicherheits-Regression kennzeichnen; er ist der Vorher-Beweis für die Bedrohung T-09-01 und muss nach der Migration in Plan 09-08 unverändert grün bleiben.

    Ergänze für den reinen-Text-Fall und den Tabellen-Fall je einen Persistenz-Roundtrip nach dem bekannten Muster.
  </action>
  <verify>
    <automated>npm run build:dev && npx playwright test tests/e2e/features/editor-insert.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/features/editor-insert.spec.js` existiert; `npx playwright test tests/e2e/features/editor-insert.spec.js` beendet mit Exit-Code 0
    - Der Spec enthält mindestens 7 Testfälle (Enter, Shift+Enter, Strg+Shift+T, 3 Einfüge-Varianten, Sicherheitsfall) plus 2 Roundtrip-Fälle
    - Verhalten: Der Shift+Enter-Test behauptet exakt den in 09-BASELINE.md/A1 gemessenen Absatz-/Umbruchwert und ist im Namen als A1-Referenz erkennbar
    - Verhalten (Sicherheit): Nach dem Einfügen des bösartigen Fragments findet `#wiki-content` weder ein Skript-Element noch ein Attribut mit `on`-Präfix; dieselbe Prüfung gilt nach Speichern und Reload
    - Verhalten: Der Tabellen-Einfügetest behauptet die exakte Zellenzahl des eingefügten Fragments
    - Der Block „Zwischenablage" trägt einen Kopfkommentar, der das Setup-Vehikel begründet und auf die Maskierungs-Regel verweist
    - Kein `waitForTimeout` im Spec: `node -e "const s=require('fs').readFileSync('tests/e2e/features/editor-insert.spec.js','utf8');process.exit(/waitForTimeout/.test(s)?1:0)"` beendet mit 0
  </acceptance_criteria>
  <done>Alle fünf Insert-Call-Sites haben einen exakten Vorher-Beweis, einschließlich eines Sicherheitsnachweises für den Einfüge-Pfad.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke-Netz für die fünf weiteren Entity-Editoren (NPCs, Orte, Quests, Sessions, Quick-Referenz)</name>
  <files>tests/e2e/features/editor-smoke.spec.js</files>
  <read_first>
    - assets/templates/view-content.html (Zeile 28: `[data-action="show-modal"][data-value="npc-modal"]`; Zeile 75: `[data-action="show-modal"][data-value="location-modal"]`; Zeile 127: `[data-action="show-modal"][data-value="quest-modal"]` — die realen Öffner-Buttons)
    - assets/templates/modals-entity.html (Zeilen 40–47: Orte-Toolbar mit `data-cmd="loc-desc"` und der Editor `#loc-desc`; Zeilen 116–123: NPC-Toolbar mit `data-cmd="npc-desc"` und `#npc-desc`; Zeilen 310–317: Quest-Toolbar mit `data-cmd="quest-desc"` und `#quest-desc`)
    - assets/templates/view-resources.html (Zeile 216: `[data-action="toggle-collapse"][data-value="session-form"]`; Zeilen 288–328: Sessions-Toolbar mit `data-cmd="session-text"` und der Editor `#session-text`)
    - assets/templates/modals-editors.html (Zeilen 538–560: `#quick-ref-entry-modal` mit Toolbar `data-cmd="quick-ref-entry-content"` und Editor `#quick-ref-entry-content`)
    - systems/spellslots/quick-reference.js (Zeilen 360–390: die beiden Funktionen, die `showModal('quick-ref-entry-modal')` aufrufen — daraus den realen Auslöser bestimmen; existiert kein `data-action`-Button, ist der direkte Aufruf per `page.evaluate` als Setup-Vehikel zulässig, der geprüfte Pfad bleibt der Toolbar-Klick)
    - tests/e2e/helpers/test-utils.js (`loadApp`, `navigateToTab` — Setup-Vehikel; `navigateToTab` öffnet bei Bedarf die Nav-Gruppe)
    - .planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md (der Fett-Erwartungswert aus dem Referenz-Editor — die Smoke-Tests behaupten denselben Wert, das ist der Beweis für „eine geteilte Engine")
  </read_first>
  <action>
    Lege `tests/e2e/features/editor-smoke.spec.js` an mit einem `test.describe('Editor-Smoke — geteilte Engine in allen Entity-Editoren')`. Definiere am Dateikopf eine Tabelle (Array von Objekten) mit je Editor: Anzeigename, Tab-Name für die Navigation, Öffner-Selektor bzw. Öffner-Schritt, Editor-ID, Toolbar-Selektor für Fett. Erzeuge daraus per Schleife je einen Testfall — so bleibt die Datei schlank und ein neuer Editor ist eine Zeile.

    Abgedeckte Editoren und Öffnungswege:
    - NPCs: Tab `npcs`, Öffner `[data-action="show-modal"][data-value="npc-modal"]`, Editor `#npc-desc`, Toolbar `[data-action="format-text"][data-cmd="npc-desc"][data-editor="bold"]`.
    - Orte: Tab `locations`, Öffner `[data-action="show-modal"][data-value="location-modal"]`, Editor `#loc-desc`.
    - Quests: Tab `quests`, Öffner `[data-action="show-modal"][data-value="quest-modal"]`, Editor `#quest-desc`.
    - Sessions: Tab `sessions`, Öffner `[data-action="toggle-collapse"][data-value="session-form"]`, Editor `#session-text`.
    - Quick-Referenz: Öffner gemäß Fund in `quick-reference.js`, Editor `#quick-ref-entry-content`.

    Pro Editor ein Testfall: Formular öffnen (vorher hart per `expect(...).toBeVisible()` auf den Editor prüfen — kein weicher Guard), Testtext eingeben, selektieren, Fett-Button der zugehörigen statischen Toolbar klicken, exakte `innerHTML`-Assertion gegen denselben Wert, den der Referenz-Editor liefert. Zusätzlich prüfen, dass `textContent` den Testtext weiterhin enthält.

    Ergänze einen zweiten `test.describe`-Block „Smoke — floating Toolbar" mit mindestens zwei Editoren (empfohlen NPCs und Sessions, weil sie unterschiedliche Container-Typen sind — Modal vs. Inline-Formular): Text selektieren, auf `#floating-toolbar.visible` warten, `[data-floating-action="bold"]` klicken, exakte Assertion gegen den Wert aus dem Referenz-Editor. Ist der Ist-Zustand eingefroren (option-b/c aus 09-01), behaupten diese beiden Tests stattdessen den protokollierten Nicht-Zustand und tragen den Vermerk „eingefrorener Defekt" im Namen.
  </action>
  <verify>
    <automated>npm run build:dev && npx playwright test tests/e2e/features/editor-smoke.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/features/editor-smoke.spec.js` existiert; `npx playwright test tests/e2e/features/editor-smoke.spec.js` beendet mit Exit-Code 0
    - Der Spec führt mindestens 5 Tests im Block der statischen Toolbar und mindestens 2 im floating-Block aus
    - Jeder Editor-Testfall enthält vor dem Klick ein `expect(...).toBeVisible()` auf den Editor bzw. den Toolbar-Button und danach eine exakte `innerHTML`-Gleichheitsassertion
    - Verhalten: Alle fünf Editoren behaupten denselben Fett-Markup-Wert wie der Referenz-Editor Wiki — weicht ein Editor ab, wird der Test rot
    - Der Spec enthält kein `isVisible()` und kein `waitForTimeout`: `node -e "const s=require('fs').readFileSync('tests/e2e/features/editor-smoke.spec.js','utf8');process.exit((/waitForTimeout/.test(s)||/isVisible\(\)/.test(s))?1:0)"` beendet mit 0
  </acceptance_criteria>
  <done>Die Verdrahtung der geteilten Editor-Engine ist in allen sechs Entity-Editoren bewiesen — nicht nur im Referenz-Editor.</done>
</task>

<task type="auto">
  <name>Task 3: Markdown-Live-Shortcuts als unveränderte Nachbarschaft absichern</name>
  <files>tests/e2e/features/editor-smoke.spec.js</files>
  <read_first>
    - ui/editors/markdown-shortcuts.js (Zeilen 17–30: `handleMarkdownInput` mit dem 150-ms-Debounce und der Bedingung `D.settings.enableMarkdownShortcuts`; Zeilen 36–80: `processMarkdownShortcuts`, das die Muster am Cursor auswertet; Zeilen 210–230: die Einstellungs-Checkbox)
    - core/constants.js (Zeilen 428–438: `MARKDOWN_PATTERNS` mit den Mustern für Fett, Kursiv, Durchgestrichen, Code, Überschrift, Link, Zitat, Listenpunkt; Zeile 586: der `window`-Export)
    - core/data.js (Zeile 32: `enableMarkdownShortcuts: true` als Voreinstellung — der Testlauf muss diese Voreinstellung nicht ändern, sie aber vor dem Tippen absichern)
    - tests/e2e/features/editor-smoke.spec.js (Stand nach Task 2)
    - .planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md (falls dort Markdown-Ergebnisse protokolliert sind, gelten diese Werte; sonst analog zum Toolbar-Fett-Wert)
  </read_first>
  <action>
    Ergänze in `tests/e2e/features/editor-smoke.spec.js` einen `test.describe`-Block „Markdown-Live-Shortcuts (Nachbarschaft, muss unverändert bleiben)".

    Je Testfall: In den Editor klicken, das Markdown-Muster per Tastatur eingeben (`pressSequentially`), dann über `waitForFunction` auf das Erscheinen des erwarteten Tags im `innerHTML` warten (nicht über `waitForTimeout` — der Umwandler arbeitet mit 150 ms Verzögerung), danach exakte `innerHTML`-Assertion.

    Abzudecken:
    - Fett-Muster im Wiki-Editor (`#wiki-content`).
    - Kursiv-Muster im Wiki-Editor.
    - Durchgestrichen-Muster im Wiki-Editor.
    - Fett-Muster in einem zweiten Entity-Editor (empfohlen `#npc-desc`), als Beweis, dass die Verdrahtung nicht nur im Referenz-Editor greift.

    Vor jedem Testfall sicherstellen, dass die Einstellung für Markdown-Shortcuts aktiv ist — per `page.evaluate` lesen und, falls nötig, setzen (dokumentiertes Setup-Vehikel; der geprüfte Pfad ist die Tastatureingabe). Im Kopfkommentar des Blocks festhalten, dass die Markdown-Kette selbst KEIN Migrationsgegenstand ist (sie nutzt die deprecated Editier-Kommando-API nicht), sondern als Nachbarschaft abgesichert wird: Success Criterion 4 der Phase verlangt, dass sie nach der Migration unverändert funktioniert.

    Ergänze einen Persistenz-Roundtrip für den Fett-Fall im Wiki-Editor.
  </action>
  <verify>
    <automated>npm run build:dev && npx playwright test tests/e2e/features/editor-smoke.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/e2e/features/editor-smoke.spec.js -g "Markdown"` beendet mit 0 und führt mindestens 4 Tests aus
    - Jeder Markdown-Testfall wartet über `waitForFunction` auf das Umwandlungsergebnis und enthält danach eine exakte `innerHTML`-Gleichheitsassertion
    - Verhalten: Das Fett-Muster im zweiten Entity-Editor liefert dasselbe Markup wie im Referenz-Editor
    - Verhalten: Der Roundtrip-Fall belegt, dass das umgewandelte Markup Speichern und Reload übersteht
    - Der Block trägt einen Kopfkommentar, der die Markdown-Kette als abzusichernde Nachbarschaft (nicht als Migrationsgegenstand) einordnet
  </acceptance_criteria>
  <done>Die Markdown-Live-Shortcuts sind als unveränderte Nachbarschaft festgenagelt — eine Regression durch die Migration würde auffallen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Zwischenablage → Editor-DOM | Fremd-HTML (potenziell mit Skript-/Ereignis-Attributen) wird eingefügt |
| Editor-DOM → Persistenz | Eingefügtes Markup überquert die Whitelist-Grenze in den gespeicherten Zustand |
| Tastatureingabe → Markdown-Umwandler → Editor-DOM | Nutzertext wird zu Markup umgewandelt |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | `handleEditorPaste` (rich-text.js:577–643) | high | mitigate | Der Sicherheits-Testfall in Task 1 belegt vor der Migration, dass weder Skript-Elemente noch Ereignis-Attribute im Editor-DOM oder im gespeicherten Eintrag landen. Die vorgeschaltete Attribut-Bereinigung und die Whitelist beim Speichern bleiben unverändert (D-02); Plan 09-08 muss denselben Test unverändert grün halten. |
| T-09-10 | Tampering | `processMarkdownShortcuts` (markdown-shortcuts.js) | medium | mitigate | Der Umwandler leitet sein Ergebnis bereits durch die Sanitisierung; der neue Roundtrip-Testfall belegt, dass das umgewandelte Markup die Whitelist passiert, ohne dass ein neuer ungeprüfter Zuweisungspfad entsteht. Kein Produktionscode wird in diesem Plan geändert. |
| T-09-11 | Denial of Service | Smoke-Schleife über 5 Editoren im CI | low | accept | Fünf kurze Testfälle gegen ein lokales Bundle; die Laufzeit des blockierenden CI-Jobs wächst um Sekunden. |
| T-09-SC | Tampering | npm/pip/cargo-Installationen | low | accept | Keine Paketinstallation in dieser Phase. |
</threat_model>

## Artifacts this phase produces

Siehe die vollständige Liste in `09-01-PLAN.md`. Von DIESEM Plan neu erzeugt:

- `tests/e2e/features/editor-insert.spec.js` — Tastatur- und Zwischenablage-Einfügepfade inklusive Sicherheits-Regressionstest
- `tests/e2e/features/editor-smoke.spec.js` — Smoke der fünf weiteren Entity-Editoren (statisch + floating) und Markdown-Live-Shortcuts

<verification>
- `npm run build:dev && npx playwright test tests/e2e/features/editor-insert.spec.js tests/e2e/features/editor-smoke.spec.js` — grün
- Kein Produktionscode geändert: `git diff --name-only` listet ausschließlich die beiden neuen Spec-Dateien
- Der Sicherheits-Testfall aus Task 1 ist im Testnamen als Sicherheits-Regression erkennbar
</verification>

<success_criteria>
- Die fünf Insert-Call-Sites haben exakte Vorher-Assertionen, inklusive Sicherheitsnachweis
- Alle sechs Entity-Editoren sind über die statische Toolbar abgedeckt, mindestens zwei zusätzlich über die floating Toolbar
- Die Markdown-Live-Shortcuts sind als unveränderte Nachbarschaft abgesichert
</success_criteria>

<output>
Create `.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-04-SUMMARY.md` when done
</output>
