---
phase: 09-editor-regressionsnetz-execcommand-abl-sung
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - core/constants.js
  - ui/editors/rich-text.js
  - tests/e2e/features/editor-formatting.spec.js
  - tests/e2e/features/wiki.spec.js
  - .planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md
autonomous: true
requirements: [EDIT-02, EDIT-03]

must_haves:
  truths:
    - "Die in 09-01/Task 3 getroffene Baseline-Entscheidung ist im Code umgesetzt und in 09-BASELINE.md als umgesetzt vermerkt — der Zustand, gegen den alle weiteren Assertions prüfen, ist damit eingefroren"
    - "Jede Formatgruppe der statischen Wiki-Toolbar (Fett, Kursiv, Unterstrichen, Durchgestrichen, Liste, Schriftart, Schriftgröße, Highlight, Vorlese-Stil, Rahmen, Tabelle, Formatierung entfernen) hat im Netz eine exakte Markup-Assertion direkt nach der Toolbar-Aktion"
    - "Jede dieser Formatgruppen hat zusätzlich eine Persistenz-Roundtrip-Assertion (formatieren, speichern, Reload, Eintrag wieder öffnen, Markup erneut exakt prüfen)"
    - "Formatiert man zwei unmittelbar aneinandergrenzende Textbereiche nacheinander mit demselben Format, entspricht das resultierende Markup exakt dem in 09-BASELINE.md protokollierten Zusammenführungs-/Trennverhalten (Adjazenz-Fall)"
    - "Eine Toolbar-Aktion ohne Textselektion (kollabierter Cursor) sowie in einem leeren Editor verändert das Markup nicht unkontrolliert und erzeugt keinen Konsolen-/Page-Error (Leer-/Einzelelement-Fall)"
    - "Markup-Gleichheit ist als exakter String-Vergleich von innerHTML definiert; ein Testtext mit Umlauten und einem Emoji überlebt jede Formatgruppe und den Persistenz-Roundtrip unverändert (Kodierungs-Fall)"
    - "Dieselbe Toolbar-Aktion zweimal hintereinander auf gleichartige Selektionen angewendet liefert byte-gleiches innerHTML — Attribut-Reihenfolge und Tag-Form sind deterministisch (Ordnungs-Fall)"
    - "Der bisherige Formatierungsblock in tests/e2e/features/wiki.spec.js beweist entweder etwas (harte Assertion) oder existiert nicht mehr — kein Test bleibt zurück, der ohne Assertion grün wird"
  artifacts:
    - "tests/e2e/features/editor-formatting.spec.js deckt alle Formatgruppen der statischen Wiki-Toolbar mit exakter Markup- und Roundtrip-Assertion ab"
    - "09-BASELINE.md trägt im Abschnitt „Offene Baseline-Entscheidung\" den Vermerk „umgesetzt\" mit Commit-Referenz"
    - "Bei Entscheidung option-a: core/constants.js definiert EDITOR_FONTS und TOOLBAR_DIMENSIONS und exportiert sie über UI_CONSTANTS sowie als window-Einzelexport"
  key_links:
    - "core/constants.js (EDITOR_FONTS/TOOLBAR_DIMENSIONS) -> window-Export -> ui/editors/rich-text.js setEditorFont/setEditorFontSize/handleSelectionChange -> Schriftart-Picker und floating Toolbar funktionsfähig"
    - "ui/actions/system-actions.js set-editor-font/set-editor-font-size -> setEditorFont/setEditorFontSize -> angewandter Wert entspricht der Auswahl im Select"
    - "Toolbar-Aktion -> #wiki-content innerHTML -> saveWikiEntry -> sanitizeHTML -> localStorage -> Reload -> edit-wiki -> identisches innerHTML"
  prohibitions:
    - statement: "Eine Formatierungsaktion darf NIEMALS Editor-Inhalt verlieren oder den Editor leeren — der Kernwert der App ist, dass am Spieltisch keine Daten verloren gehen; jede Formatgruppe wird deshalb auch auf Textverlust geprüft"
      status: flagged-unverified
      verification: test
    - statement: "Das Netz darf KEIN Text-/Formatierungs-Undo im Editor erwarten oder herbeiführen (D-01: Strg+Z ist app-weites Daten-Undo; Editor-Undo ist explizit kein Ziel dieser Phase)"
      status: flagged-unverified
      verification: judgment
---

<objective>
Setze die in 09-01 getroffene Baseline-Entscheidung im Code um und baue darauf das vollständige Regressionsnetz für die statische Toolbar des Referenz-Editors Wiki (D-04) — inklusive der vier Randfälle Adjazenz, Leer-/Einzelelement, Kodierung und Ordnung.

Purpose: Nach diesem Plan steht der größere Teil des Beweismaterials für D-02 („identisches Markup") und D-03 („Markup + Roundtrip"). Der bisherige, beweisfreie Formatierungsblock in `wiki.spec.js` wird dabei abgelöst, damit kein Test zurückbleibt, der Grün meldet, ohne etwas zu prüfen (09-RESEARCH.md Pitfall 2).
Output: erweiterte `tests/e2e/features/editor-formatting.spec.js`, umgesetzte Baseline-Entscheidung, bereinigter `wiki.spec.js`-Formatierungsblock.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-CONTEXT.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-RESEARCH.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-PATTERNS.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-01-SUMMARY.md
@ui/editors/rich-text.js
@core/constants.js
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Baseline-Entscheidung aus 09-01/Task 3 umsetzen und den Zustand einfrieren</name>
  <precondition>Der Abschnitt „Offene Baseline-Entscheidung" in `.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md` enthält eine getroffene Entscheidung (`option-a`, `option-b` oder `option-c`) inklusive der A4-Teilantwort — ohne diese Entscheidung darf dieser Task nicht laufen.</precondition>
  <reversibility rating="one-way">Alle Netz-Assertions (09-02/03/04), das Doppel-Grün-Gate (09-05) und jeder Migrations-Commit (09-06…09-09) werden gegen diesen Zustand geprüft; eine spätere Umdefinition entwertet den D-04a-Beweis und erzwingt ein Neuschreiben des kompletten Netzes.</reversibility>
  <files>core/constants.js, ui/editors/rich-text.js, .planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md</files>
  <read_first>
    - .planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md (Abschnitt A3 mit den Laufzeit-Messwerten und Abschnitt „Offene Baseline-Entscheidung" mit der getroffenen Wahl — dieser Text steuert, welcher Zweig unten gilt)
    - core/constants.js (Zeilen 420–440: `MARKDOWN_PATTERNS` als Muster für ein Konstanten-Objekt; Zeilen 545–560: der `UI_CONSTANTS`-Namespace-Export; Zeilen 562–590: die Legacy-`window.*`-Einzelexporte — beide Stellen sind das Vorbild für neue Konstanten)
    - ui/editors/rich-text.js (Zeile 351 `const EDITOR_FONTS = window.EDITOR_FONTS;` in `setEditorFont`; Zeile 371 die Verwendung; Zeilen 699–700 dieselben beiden funktions-lokalen Deklarationen in `initFloatingToolbar`; Zeile 785 die Verwendung im floating `change`-Handler; Zeile 938 `const TOOLBAR_DIMENSIONS = window.TOOLBAR_DIMENSIONS;` in `handleSelectionChange`; Zeile 970 die Destrukturierung)
    - ui/actions/system-actions.js (Zeilen 43–52: `set-editor-font` und `set-editor-font-size` übergeben den Select-WERT, nicht das Select-Element)
    - CLAUDE.md — Abschnitt „Duplicate Declaration Debugging Pattern" (Problem 1: funktions-lokale `const X = window.X` werden vom Build-Dedup-Pass entfernt und erzeugen Laufzeitfehler) und „Module-Level Constants Pattern"
    - assets/templates/view-resources.html (Zeilen 471–472: die `option`-Werte des Schriftart- und Größen-Selects — `arial|serif|mono|roboto|inter|poppins|source-sans` bzw. `11px|13px|16px|18px|20px`; diese Schlüssel muss `EDITOR_FONTS` abdecken)
    - assets/styles/editors.css (Toolbar-Maße für `.floating-toolbar` — Grundlage für realistische `TOOLBAR_DIMENSIONS`-Werte width/height/padding)
  </read_first>
  <action>
    Führe genau den Zweig aus, den 09-BASELINE.md protokolliert:

    **Bei `option-a` (Reparatur):** Drei mechanische Teilschritte.
    (1) In `core/constants.js` ein `EDITOR_FONTS`-Objekt definieren, dessen Schlüssel exakt die `option`-Werte des Schriftart-Selects sind (`arial`, `serif`, `mono`, `roboto`, `inter`, `poppins`, `source-sans`) und dessen Werte die zugehörigen CSS-Font-Family-Stacks sind (dieselben Familien, die `assets/styles/editors.css` und der Google-Fonts-Block in `build.py` bereits laden). Ebenso ein `TOOLBAR_DIMENSIONS`-Objekt mit den Schlüsseln `width`, `height`, `padding` (Zahlen in Pixeln, an `.floating-toolbar` in editors.css orientiert). Beide in den `UI_CONSTANTS`-Namespace aufnehmen UND zusätzlich als `window.EDITOR_FONTS` / `window.TOOLBAR_DIMENSIONS` im Legacy-Einzelexport-Block ergänzen, damit die bestehenden Lesestellen ohne weitere Umbauten funktionieren.
    (2) In `ui/editors/rich-text.js` die vier funktions-lokalen Zuweisungen dieser beiden Bezeichner entfernen und an den drei Verwendungsstellen direkt über `window.` zugreifen (Zeile 371 in `setEditorFont`, Zeile 785 im floating `change`-Handler, Zeile 970 in `handleSelectionChange`). Grund: Der Build-Dedup-Pass entfernt die jeweils zweite Deklaration desselben Bezeichners und hinterlässt sonst eine unaufgelöste Referenz im Bundle. An der Destrukturierungsstelle zusätzlich einen Guard ergänzen, der bei fehlendem Objekt sauber zurückkehrt statt zu werfen.
    (3) Den Argument-Mismatch auflösen: `setEditorFont` und `setEditorFontSize` müssen sowohl mit einem `<select>`-Element als auch mit einem einfachen Wert als zweitem Argument funktionieren (Typprüfung auf String vs. Element, dann entweder den String direkt oder `element.value` verwenden). Die Aufrufer in `ui/actions/system-actions.js` bleiben unverändert. Die execCommand-Aufrufe selbst bleiben in diesem Task unangetastet — sie werden erst in Plan 09-07 ersetzt.

    **Bei `option-b` (Ist-Zustand einfrieren):** Kein Codeeingriff. Stattdessen in `09-BASELINE.md` einen Abschnitt „Eingefrorene Defekte" ergänzen, der je betroffener Call-Site festhält, dass für sie kein UI-Verhaltensbeweis möglich ist und wie sie stattdessen migriert wird (direkter Funktionsaufruf-Test in Plan 09-03). `core/constants.js` und `ui/editors/rich-text.js` werden in diesem Plan dann NICHT geändert; die Frontmatter-Angabe `files_modified` bleibt gültig, es entsteht nur kein Diff.

    **Bei `option-c` (eigene Phase):** Kein Codeeingriff in Phase 9. In `09-BASELINE.md` festhalten, welche Call-Sites damit auf die neue Phase verschoben werden, und den Befund als Blocker in `.planning/STATE.md` (Abschnitt „Known Blockers / Research Flags") eintragen. Zusätzlich im Plan-Abschluss melden, dass die Pläne 09-07 und 09-09 in ihrem Umfang angepasst werden müssen.

    In allen drei Fällen: Den Abschnitt „Offene Baseline-Entscheidung" in 09-BASELINE.md auf „entschieden + umgesetzt" setzen, mit Datum und Commit-Referenz. Ab hier gilt der so protokollierte Zustand als „heute" im Sinne von D-02.
  </action>
  <verify>
    <automated>npm run build:dev && npx jest && npx playwright test tests/e2e/features/editor-formatting.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `09-BASELINE.md` enthält im Abschnitt „Offene Baseline-Entscheidung" den Status „entschieden + umgesetzt" mit Datum
    - Bei `option-a`: `node -e "const s=require('fs').readFileSync('core/constants.js','utf8');process.exit((/EDITOR_FONTS\s*=/.test(s)&&/TOOLBAR_DIMENSIONS\s*=/.test(s))?0:1)"` beendet mit 0
    - Bei `option-a`: `ui/editors/rich-text.js` enthält keine funktions-lokale Zuweisung dieser beiden Bezeichner mehr — `node -e "const s=require('fs').readFileSync('ui/editors/rich-text.js','utf8');process.exit(/const\s+(EDITOR_FONTS|TOOLBAR_DIMENSIONS)\s*=/.test(s)?1:0)"` beendet mit 0
    - Bei `option-a`, Verhalten: Nach Auswahl von „Serif" im Schriftart-Select des Wiki-Editors trägt das erzeugte Markup die Serif-Familie (nicht die Arial-Fallback-Familie) — nachgewiesen durch eine Markup-Assertion im Netz (Task 2)
    - Bei `option-a`, Verhalten: Nach einer Textselektion in `#wiki-content` erhält `#floating-toolbar` die Klasse `visible` und die Konsole meldet keinen Page-Error — nachgewiesen im Netz (Plan 09-03) und hier durch einen Smoke-Check im Build
    - `npm run build:dev` beendet mit Exit-Code 0 (kein Dedup-/Syntaxbruch)
    - `npx jest` beendet mit Exit-Code 0
    - Der Tracer aus 09-01 bleibt grün
  </acceptance_criteria>
  <done>Der Zustand, gegen den „verhaltensgleich" bewiesen wird, ist entschieden, umgesetzt und dokumentiert eingefroren.</done>
</task>

<task type="auto">
  <name>Task 2: Netz der statischen Wiki-Toolbar — alle Formatgruppen mit Markup- und Roundtrip-Assertion</name>
  <files>tests/e2e/features/editor-formatting.spec.js</files>
  <read_first>
    - tests/e2e/features/editor-formatting.spec.js (der Bold-Tracer aus 09-01 — sein Boot-, Formular-, Selektions- und Roundtrip-Muster wird für jede weitere Formatgruppe wiederverwendet, nicht neu erfunden)
    - .planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md (die Markup-Inventar-Tabelle liefert für JEDE Formatgruppe den exakten Erwartungswert; A4 sagt, was beim Durchstreichen im Roundtrip zu erwarten ist)
    - assets/templates/view-resources.html (Zeilen 458–501: alle Selektoren der statischen Wiki-Toolbar mit `data-cmd="wiki-content"` bzw. `data-editor="wiki-content"`)
    - ui/actions/system-actions.js (Zeilen 36–140: was jede Toolbar-Aktion konkret auslöst — insbesondere `set-highlight-color`, das `<mark>`-Elemente baut und den Select danach zurücksetzt, sowie `insert-link`, das `prompt()` öffnet)
    - ui/editors/rich-text.js (Zeilen 400–467: `clearEditorFormatting`, `setBorderFormat`, `setReadAloudFormat` — diese drei laufen schon heute ohne execCommand und müssen nach der Migration unverändert bleiben; Zeilen 649–677: `insertTable` mit fester 3×3-Struktur)
    - 09-RESEARCH.md — Pitfall 5 (Durchstreichen erzeugt eine andere Tag-Form als die floating Toolbar) und Pitfall 6 (Teilselektionen)
  </read_first>
  <action>
    Erweitere `tests/e2e/features/editor-formatting.spec.js` um je einen Testfall pro Formatgruppe der statischen Wiki-Toolbar, alle nach dem Tracer-Muster (echter Klick/echte Select-Auswahl, danach exakter `innerHTML`-Vergleich gegen den in 09-BASELINE.md protokollierten Wert):

    - Kursiv (`data-editor="italic"`), Unterstrichen (`underline`), Durchgestrichen (`strikethrough`), Liste (`list`) — je über `[data-action="format-text"][data-cmd="wiki-content"][data-editor="…"]`.
    - Schriftart: `selectOption` auf `[data-action="set-editor-font"][data-editor="wiki-content"]` mit dem Wert `serif` (bewusst NICHT `arial`, damit ein Fallback-auf-Arial-Fehler auffällt).
    - Schriftgröße: `selectOption` auf `[data-action="set-editor-font-size"][data-editor="wiki-content"]` mit dem Wert `20px`.
    - Highlight: `selectOption` auf `[data-action="set-highlight-color"][data-editor="wiki-content"]` mit einer der im Template angebotenen Farben; zusätzlich der Entfernen-Fall (Wert `transparent`).
    - Vorlese-Stil: `selectOption` auf `[data-action="set-read-aloud-style"][data-editor="wiki-content"]` mit `crimson` (nicht der Default `parchment`, damit die Stil-Variante mitgeprüft wird) sowie der Toggle-Fall (erneutes Anwenden entfernt den Block wieder).
    - Rahmen: Klick auf `[data-action="set-border-format"][data-editor="wiki-content"]`.
    - Tabelle: Klick auf `[data-action="insert-table"][data-editor="wiki-content"]` — Assertion auf die vollständige 3×3-Struktur (Anzahl `th` und `td` exakt, plus exakter innerHTML-Vergleich).
    - Formatierung entfernen: Klick auf `[data-action="clear-formatting"][data-value="wiki-content"]` nach vorheriger Fett-Formatierung.
    - Link über die statische Toolbar: Klick auf `[data-action="insert-link"][data-editor="wiki-content"]` mit vorher registriertem `page.on('dialog')`-Handler, der eine feste URL akzeptiert. Kennzeichne diesen Test im Namen als „unveränderter Fremdpfad" — er liegt außerhalb der 21 zu migrierenden Call-Sites (09-RESEARCH.md Pitfall 4) und muss vor UND nach der Migration unverändert grün sein.

    Für jede Formatgruppe zusätzlich einen Persistenz-Roundtrip-Testfall nach dem Tracer-Muster (speichern, `page.reload()`, Eintrag über `[data-action="select-wiki-entry"]` und `[data-action="edit-wiki"]` wieder öffnen, exakte Re-Assertion). Wo Erhebung und Roundtrip unterschiedliche Werte liefern (z. B. weil die Sanitisierungs-Whitelist eine Tag-Form entfernt — siehe A4), beide Werte getrennt pinnen und im Testnamen als vorbestehendes Verhalten kennzeichnen. Nichts angleichen, nichts reparieren.

    Zusätzlich pro Formatgruppe prüfen, dass der Editor-Text erhalten bleibt: `textContent` von `#wiki-content` muss nach der Aktion weiterhin den eingegebenen Testtext enthalten (Schutz gegen stillen Inhaltsverlust).

    Organisiere die Datei in `test.describe`-Blöcken je Prüfebene („Markup direkt nach Aktion" / „Persistenz-Roundtrip"), damit `-g`-Filterung möglich bleibt. Weiterhin gilt: kein `waitForTimeout`, keine weichen Sichtbarkeits-Guards, exakte Werte statt „größer 0".
  </action>
  <verify>
    <automated>npm run build:dev && npx playwright test tests/e2e/features/editor-formatting.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/e2e/features/editor-formatting.spec.js` beendet mit Exit-Code 0
    - Der Spec enthält mindestens 12 Testfälle für die statische Toolbar (Fett, Kursiv, Unterstrichen, Durchgestrichen, Liste, Schriftart, Schriftgröße, Highlight setzen, Highlight entfernen, Vorlese-Stil, Rahmen, Tabelle, Formatierung entfernen, Link)
    - Zu jeder Formatgruppe existiert ein Roundtrip-Testfall: `npx playwright test tests/e2e/features/editor-formatting.spec.js -g "Roundtrip"` beendet mit 0 und meldet mindestens 12 ausgeführte Tests
    - Jeder Testfall enthält mindestens eine exakte `innerHTML`-Gleichheitsassertion (kein `toContain`/Regex als einzige Markup-Prüfung)
    - Verhalten: Der Schriftart-Test wählt `serif` und behauptet die zugehörige Serif-Familie im Markup — ein Fallback auf die Arial-Familie lässt den Test rot werden
    - Verhalten: Der Tabellen-Test behauptet exakt 3 `th`- und 6 `td`-Elemente in der eingefügten Tabelle
    - Verhalten: Nach jeder Formatgruppe enthält `#wiki-content` weiterhin den eingegebenen Testtext (Inhaltsverlust-Schutz)
    - Kein `waitForTimeout` und kein `isVisible()` im Spec: `node -e "const s=require('fs').readFileSync('tests/e2e/features/editor-formatting.spec.js','utf8');process.exit((/waitForTimeout/.test(s)||/isVisible\(\)/.test(s))?1:0)"` beendet mit 0
  </acceptance_criteria>
  <done>Die statische Toolbar des Referenz-Editors ist in jeder Formatgruppe auf exaktes Markup und auf Persistenz festgenagelt.</done>
</task>

<task type="auto">
  <name>Task 3: Randfälle (Adjazenz, Leer/Einzelelement, Kodierung, Ordnung) + Ablösung des beweisfreien wiki.spec.js-Blocks</name>
  <files>tests/e2e/features/editor-formatting.spec.js, tests/e2e/features/wiki.spec.js</files>
  <read_first>
    - tests/e2e/features/wiki.spec.js (Zeilen 295–356: der bestehende `test.describe('Wiki Editor Formatierung')`-Block mit drei Tests, die per `if (await …isVisible())` klicken und danach nichts prüfen — 09-RESEARCH.md Pitfall 2)
    - tests/e2e/features/editor-formatting.spec.js (Stand nach Task 2 — die Randfälle bauen auf denselben Hilfsmustern auf)
    - .planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md (Adjazenz- und Ordnungsverhalten: welches Markup entsteht heute bei aneinandergrenzenden gleichen Formaten und bei zweifacher identischer Anwendung)
    - ui/editors/rich-text.js (Zeilen 817–861: die Toggle-Erkennung der floating Toolbar zeigt, wie „bereits formatiert" heute erkannt wird — dieselbe Semantik muss die statische Toolbar nach der Migration zeigen)
    - 09-CONTEXT.md — Abschnitt „Fortgeltende Testregeln" (Maskierungs-Kriterium, keine `waitForTimeout`, exakte Assertions)
  </read_first>
  <action>
    Ergänze in `tests/e2e/features/editor-formatting.spec.js` einen `test.describe`-Block „Randfälle" mit vier Testfällen:

    1. **Adjazenz:** Zwei unmittelbar aneinandergrenzende Wortbereiche nacheinander fett formatieren (erst das erste Wort selektieren und formatieren, dann das direkt folgende Wort). Danach exakter `innerHTML`-Vergleich gegen den in 09-BASELINE.md protokollierten Wert — dokumentiere im Testnamen, ob die Baseline die Tags zusammenführt oder getrennt lässt. Zusätzlich: erneutes Anwenden desselben Formats auf eine bereits formatierte Selektion (Toggle-Fall) mit exakter Assertion.
    2. **Leer/Einzelelement:** (a) Toolbar-Aktion bei leerem Editor ohne jede Eingabe, (b) Toolbar-Aktion mit kollabiertem Cursor ohne Selektion, (c) Toolbar-Aktion auf einer Selektion aus genau einem Zeichen. Für (a) und (b) exakte Assertion auf das in der Baseline protokollierte Ergebnis-Markup und die Zusicherung, dass weder ein Page-Error noch ein Konsolen-Error auftritt (`page.on('pageerror')` sammeln und am Ende auf leer prüfen).
    3. **Kodierung:** Testtext mit Umlauten, ß und einem Emoji (z. B. „Größenwahn ⚔️ Straße") durch Fett, Schriftgröße und den Persistenz-Roundtrip schicken; exakte `innerHTML`-Gleichheit als String-Vergleich sowie unveränderter `textContent` nach dem Reload.
    4. **Ordnung:** Dieselbe Aktion zweimal auf zwei gleichartige, frisch aufgebaute Editorinhalte anwenden und die beiden `innerHTML`-Ergebnisse gegeneinander auf exakte Gleichheit prüfen (deterministische Tag-/Attribut-Reihenfolge). Zusätzlich für die Schriftgröße prüfen, dass die Attribut-/Style-Reihenfolge des erzeugten Elements exakt dem Baseline-String entspricht.

    Danach den Block `test.describe('Wiki Editor Formatierung')` in `tests/e2e/features/wiki.spec.js` (Zeilen 295–356) auflösen: entweder ersatzlos entfernen (die Abdeckung liegt jetzt vollständig in `editor-formatting.spec.js`) oder die drei Tests auf harte Assertionen umschreiben. Bevorzugt entfernen, damit es genau eine Quelle der Wahrheit für Editor-Formatierung gibt; im Commit-Text begründen. Die übrigen Wiki-Tests (Erstellen, Hierarchie, Links, Suche, Persistenz) bleiben unangetastet.
  </action>
  <verify>
    <automated>npm run build:dev && npx playwright test tests/e2e/features/editor-formatting.spec.js tests/e2e/features/wiki.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/e2e/features/editor-formatting.spec.js -g "Randfälle"` beendet mit 0 und führt mindestens 4 Tests aus
    - Verhalten (Adjazenz): Der Adjazenz-Test schlägt fehl, wenn sich das Zusammenführungsverhalten zweier benachbarter gleicher Formate gegenüber der Baseline ändert
    - Verhalten (Leer): Toolbar-Aktion ohne Selektion und im leeren Editor erzeugt keinen Page-Error; der gesammelte Fehler-Array ist leer
    - Verhalten (Kodierung): Umlaut-/Emoji-Testtext ist nach Formatierung und Reload zeichengleich vorhanden
    - Verhalten (Ordnung): Zwei identische Aktionsläufe liefern byte-gleiches `innerHTML`
    - `tests/e2e/features/wiki.spec.js` enthält keinen assertionsfreien Formatierungstest mehr: `node -e "const s=require('fs').readFileSync('tests/e2e/features/wiki.spec.js','utf8');process.exit(/Wiki Editor Formatierung/.test(s)&&!/toHaveJSProperty|toContainText|expect\(/.test(s.split('Wiki Editor Formatierung')[1].slice(0,2000))?1:0)"` beendet mit 0
    - `npx playwright test tests/e2e/features/wiki.spec.js` beendet mit 0
  </acceptance_criteria>
  <done>Die vier Randfall-Klassen sind festgenagelt und es existiert kein Editor-Formatierungstest mehr, der grün wird, ohne etwas zu beweisen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Toolbar-Auswahlwert → Wrapper-Element (`<font>`, `<mark>`, `.read-aloud`) | Ein Auswahlwert wird zu einem CSS-/Attributwert im Editor-DOM |
| Editor-DOM → Persistenz (`sanitizeHTML` → `localStorage`) | Markup überquert die Whitelist-Grenze in den gespeicherten Zustand |
| `prompt()`-Eingabe (Link-Button) → `href`-Attribut | Nutzertext wird zur Ziel-URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04 | Tampering | `EDITOR_FONTS`-Werte in core/constants.js (nur bei option-a) | medium | mitigate | Die Font-Werte sind fest kodierte CSS-Font-Family-Stacks ohne Nutzer-Eingabe; es werden ausschließlich Eigenschaften gesetzt, die die bestehende `style`-Whitelist in `utils/basic.js` bereits erlaubt (`font-family`, `font-size`). Kein `url(...)`, kein `background-image`. Der Persistenz-Roundtrip-Test belegt, dass die Sanitisierung die Werte akzeptiert. |
| T-09-05 | Tampering | `insert-link` (`prompt()` → `href`) | medium | accept | Vorbestehender Pfad außerhalb der 21 Call-Sites (09-RESEARCH.md Pitfall 4); `sanitizeHTML()` entfernt `javascript:`-Schemata bereits vor der Persistenz. Dieser Plan ändert den Pfad nicht; die Härtung gehört zu SEC-01/SEC-02 (Phase 10). |
| T-09-06 | Denial of Service | Netz-Laufzeit im blockierenden CI-e2e-Job | low | accept | Das Netz besteht aus kurzen, deterministischen Testfällen gegen ein lokales `file://`-Bundle; die Laufzeit wächst um Sekunden, nicht um Minuten. |
| T-09-SC | Tampering | npm/pip/cargo-Installationen | low | accept | Keine Paketinstallation in dieser Phase (09-RESEARCH.md: Package-Legitimacy-Gate entfällt). |
</threat_model>

## Artifacts this phase produces

Siehe die vollständige Liste in `09-01-PLAN.md` („Artifacts this phase produces"). Von DIESEM Plan neu erzeugt bzw. erweitert:

- `tests/e2e/features/editor-formatting.spec.js` — erweitert um alle Formatgruppen der statischen Toolbar, Roundtrips und den Randfall-Block
- `EDITOR_FONTS` und `TOOLBAR_DIMENSIONS` in `core/constants.js` (nur bei Entscheidung `option-a`)
- Abschnitt „Eingefrorene Defekte" in `09-BASELINE.md` (nur bei `option-b`)

<verification>
- `npm run build:dev && npx playwright test tests/e2e/features/editor-formatting.spec.js tests/e2e/features/wiki.spec.js` — grün
- `npx jest` — grün (keine Unit-Regression durch den Konstanten-Eingriff)
- `09-BASELINE.md` weist die Baseline-Entscheidung als umgesetzt aus
- Keine Änderung an `utils/basic.js`, `utils/testable-utils.js` oder `assets/styles/editors.css` (D-02): `git diff --name-only` listet keine dieser Dateien
</verification>

<success_criteria>
- Die Baseline-Entscheidung ist im Code umgesetzt und dokumentiert eingefroren
- Jede Formatgruppe der statischen Wiki-Toolbar hat eine exakte Markup- und eine Persistenz-Roundtrip-Assertion
- Die vier Randfall-Klassen (Adjazenz, Leer/Einzelelement, Kodierung, Ordnung) sind abgedeckt
- In der Suite existiert kein beweisfreier Editor-Formatierungstest mehr
</success_criteria>

<output>
Create `.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-02-SUMMARY.md` when done
</output>
