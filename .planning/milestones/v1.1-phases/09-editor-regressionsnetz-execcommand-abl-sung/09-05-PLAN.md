---
phase: 09-editor-regressionsnetz-execcommand-abl-sung
plan: 05
type: execute
wave: 4
depends_on: ["09-03", "09-04"]
files_modified:
  - .planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md
  - .planning/STATE.md
autonomous: true
requirements: [EDIT-03]

must_haves:
  truths:
    - "Das vollständige Regressionsnetz (alle vier Spec-Dateien) ist zweimal in Folge grün gegen den unveränderten Editor-Code gelaufen — beide Läufe sind mit Zeitstempel, Commit-Kennung und Testzahl protokolliert (D-04a)"
    - "Zum Zeitpunkt des Doppel-Grün-Nachweises enthält ui/editors/rich-text.js unverändert alle 21 Aufrufe der deprecated Editier-Kommando-API — der Zählnachweis-Test aus Plan 09-03 belegt das im selben Lauf"
    - "Die volle Playwright-Suite und die Jest-Suite sind grün; das neue Netz läuft im blockierenden CI-Job ohne Konfigurationsänderung mit"
    - "Die Netz-Freeze-Regel ist schriftlich fixiert: ab hier ist jede Änderung an einer Netz-Assertion begründungspflichtig und gilt als Beweis-Leck-Verdacht"
  artifacts:
    - "09-BASELINE.md enthält den Abschnitt „D-04a Doppel-Grün-Nachweis\" mit zwei protokollierten Läufen (Zeitstempel, Commit, Testzahl, Ergebnis)"
    - "09-BASELINE.md enthält den Abschnitt „Netz-Freeze\" mit Änderungsregel, Ausnahmeregel für den Zählnachweis-Test und CI-Nachweis"
  key_links:
    - "Alle vier Netz-Spec-Dateien -> npx playwright test -> playwright.config.js (Chromium, file://dist) -> gebautes Bundle mit unverändertem Editor-Code"
    - ".github/workflows/ci.yml e2e-Job (needs: lint-and-typecheck, test) -> npx playwright test ohne Dateifilter -> neue Spec-Dateien laufen automatisch mit -> build-Job hängt davon ab"
    - "09-BASELINE.md Netz-Freeze -> Migrationspläne 09-06 bis 09-09 (dort ist jede Netzänderung außer dem Zählnachweis-Test verboten)"
  prohibitions:
    - statement: "Netz-Tests dürfen während der Migration NICHT angepasst werden, um sie grün zu bekommen — eine nachträglich geänderte Assertion entwertet den gesamten Verhaltensgleichheits-Beweis dieser Phase; einzige Ausnahme ist der ausdrücklich gekennzeichnete Zählnachweis-Test"
      status: flagged-unverified
      verification: test
    - statement: "Ein roter Netzlauf darf NICHT durch Wiederholung, erhöhte Wiederholungszahl oder selektives Ausführen einzelner Dateien in einen grünen Nachweis umgedeutet werden — der Doppel-Grün-Nachweis verlangt zwei vollständige, unmittelbar aufeinanderfolgende Läufe"
      status: flagged-unverified
      verification: judgment
---

<objective>
Führe das D-04a-Beweis-Gate durch: Das komplette Regressionsnetz muss zweimal in Folge grün gegen den unveränderten Editor-Code laufen, und dieser Nachweis muss protokolliert sein, bevor die erste Zeile Migrationscode entsteht.

Purpose: Ohne diesen Nachweis wäre jeder spätere grüne Lauf wertlos — er könnte ebenso bedeuten, dass die Tests von Anfang an nichts geprüft haben. Der Nachweis ist zugleich der Zeitpunkt, ab dem das Netz eingefroren ist.
Output: `09-BASELINE.md` mit Doppel-Grün-Protokoll, Netz-Freeze-Regel und CI-Nachweis.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-CONTEXT.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-VALIDATION.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-03-SUMMARY.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Doppel-Grün-Nachweis gegen den unveränderten Editor-Code führen und protokollieren</name>
  <precondition>`ui/editors/rich-text.js` enthält noch alle 21 Aufrufe der deprecated Editier-Kommando-API (kein Migrationsplan wurde bereits ausgeführt) — der Zählnachweis-Test aus Plan 09-03 muss im selben Lauf grün sein.</precondition>
  <files>.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md</files>
  <read_first>
    - .planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-CONTEXT.md (D-04a: das Netz muss VOR allen Migrationsplänen zweifach grün laufen; „Baseline-Beweis zweistufig" im Abschnitt Specifics)
    - .planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-VALIDATION.md (Abschnitt „Sampling Rate": Doppel-Grün ist ein expliziter, über die normale Stichprobe hinausgehender Verifikationsschritt)
    - .planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md (bestehende Struktur — der neue Abschnitt wird angehängt, nichts Bestehendes überschrieben)
    - playwright.config.js (`retries` und `workers`: im CI 2 bzw. 1, lokal abweichend — für das Protokoll ist festzuhalten, mit welchen Werten gelaufen wurde)
    - tests/e2e/features/editor-formatting.spec.js, tests/e2e/features/editor-floating.spec.js, tests/e2e/features/editor-insert.spec.js, tests/e2e/features/editor-smoke.spec.js (die vier einzufrierenden Netz-Dateien — Umfang und Testzahl fürs Protokoll)
  </read_first>
  <action>
    Baue das Bundle frisch (`npm run build:dev`) und führe danach ZWEI vollständige, unmittelbar aufeinanderfolgende Läufe des kompletten Netzes aus (alle vier Spec-Dateien in einem Aufruf). Beide Läufe müssen ohne Fehlschlag und ohne Wiederholungsversuch durchlaufen; ein Lauf, der erst nach einer Wiederholung grün wird, gilt als NICHT grün und ist als instabiler Test zu behandeln.

    Führe anschließend die volle E2E-Suite (`npx playwright test` ohne Dateifilter) und die Unit-Suite (`npx jest`) aus, um auszuschließen, dass das neue Netz bestehende Tests beeinflusst.

    Hänge an `09-BASELINE.md` einen Abschnitt „D-04a Doppel-Grün-Nachweis" an mit: Datum und Uhrzeit beider Läufe, Commit-Kennung (`git rev-parse --short HEAD`), Playwright- und Chromium-Version, verwendete `retries`/`workers`-Werte, Anzahl ausgeführter Tests je Spec-Datei sowie Gesamtergebnis je Lauf, Ergebnis der vollen Suite und der Jest-Suite, und das Ergebnis des Zählnachweis-Tests. Halte ausdrücklich fest, dass beide Läufe gegen den unveränderten Editor-Code stattfanden.

    Falls ein Test instabil ist (in einem der beiden Läufe rot): Ursache beheben — ausschließlich über eine deterministische Wartebedingung, niemals über ein Aufweichen der Assertion und niemals über eine feste Zeitverzögerung — und den Doppel-Grün-Zyklus danach vollständig von vorn beginnen. Jede solche Korrektur mit Begründung im Protokoll dokumentieren.
  </action>
  <verify>
    <automated>npm run build:dev && npx playwright test tests/e2e/features/editor-formatting.spec.js tests/e2e/features/editor-floating.spec.js tests/e2e/features/editor-insert.spec.js tests/e2e/features/editor-smoke.spec.js && npx playwright test tests/e2e/features/editor-formatting.spec.js tests/e2e/features/editor-floating.spec.js tests/e2e/features/editor-insert.spec.js tests/e2e/features/editor-smoke.spec.js && npx playwright test && npx jest</automated>
  </verify>
  <acceptance_criteria>
    - Beide Netzläufe beenden mit Exit-Code 0; `npx playwright test` (volle Suite) und `npx jest` ebenfalls
    - `09-BASELINE.md` enthält den Abschnitt „D-04a Doppel-Grün-Nachweis" mit zwei datierten Läufen, Commit-Kennung, Testzahlen je Spec-Datei und Gesamtergebnis
    - Das Protokoll nennt die zum Nachweiszeitpunkt gemessene Trefferzahl der deprecated Editier-Kommando-API in `ui/editors/rich-text.js`, und diese ist 21
    - Das Protokoll nennt die verwendeten `retries`- und `workers`-Werte sowie die Playwright-/Chromium-Version
    - Verhalten: Für den Nachweis wurde kein Testfall abgeschwächt — `git diff` der vier Spec-Dateien über diesen Plan zeigt entweder keine Änderung oder ausschließlich Änderungen an Wartebedingungen, jede davon im Protokoll begründet
  </acceptance_criteria>
  <done>Der Beweis, dass das Netz gegen den alten Code trägt, ist geführt und aktenkundig — ab hier bedeutet ein grüner Lauf etwas.</done>
</task>

<task type="auto">
  <name>Task 2: Netz-Freeze schriftlich fixieren und CI-Mitlauf nachweisen</name>
  <files>.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md, .planning/STATE.md</files>
  <read_first>
    - .github/workflows/ci.yml (der `e2e`-Job mit `needs: [lint-and-typecheck, test]` und den Schritten `npm ci`, `python build.py`, `npx playwright install --with-deps chromium`, `npx playwright test` — der Aufruf ohne Dateifilter nimmt neue Spec-Dateien automatisch auf; zusätzlich der nachgelagerte `build`-Job, dessen `needs`-Liste den e2e-Job enthält)
    - playwright.config.js (`testDir`/`testMatch` — belegt, dass die neuen Dateien vom Standardmuster erfasst werden)
    - .planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-CONTEXT.md (D-04a, letzter Satz: Netz-Tests werden während der Migration nicht angepasst; jede nötige Teständerung ist begründungspflichtig)
    - .planning/phases/08-test-fundament-gr-n/08-CONTEXT.md (D-03: der e2e-Job ist blockierend — das neue Netz erbt diese Eigenschaft ohne CI-Arbeit)
    - .planning/STATE.md (Abschnitt „Open TODOs" — Stil der vorhandenen Einträge übernehmen)
  </read_first>
  <action>
    Hänge an `09-BASELINE.md` einen Abschnitt „Netz-Freeze (gültig ab Doppel-Grün-Nachweis)" an, der Folgendes festhält:

    - Die Liste der vier eingefrorenen Spec-Dateien.
    - Die Regel: Ab jetzt bis zum Ende der Phase darf keine Assertion, kein Erwartungswert und kein Testname dieser Dateien geändert werden. Einzige Ausnahme ist der in Plan 09-03 ausdrücklich gekennzeichnete Zählnachweis-Test, der in Plan 09-09 von 21 auf 0 umgestellt wird.
    - Das Verfahren für den Fall, dass ein Netz-Test während der Migration rot wird: Zuerst gilt der Migrationscode als fehlerhaft, nicht der Test. Nur wenn nachweisbar der Test selbst falsch ist, darf er geändert werden — dann mit schriftlicher Begründung in genau diesem Abschnitt, unter Angabe von Migrationsgruppe, altem und neuem Erwartungswert und der Begründung, warum das kein Beweis-Leck ist.
    - Den CI-Nachweis: dass der blockierende e2e-Job die Specs ohne Konfigurationsänderung mitführt, mit Verweis auf die konkrete Workflow-Zeile, die den Testlauf ohne Dateifilter startet, und auf den nachgelagerten Job, der davon abhängt.
    - Die im Trockenlauf (`npx playwright test --list`) ermittelte Gesamtzahl gelisteter Tests, als Nachweis, dass alle vier Dateien vom Standard-Testmuster erfasst werden.

    Ergänze anschließend in `.planning/STATE.md` unter „Open TODOs" eine Zeile im vorhandenen Stil, die den Meilenstein festhält: Netz steht, Doppel-Grün geführt, Migration darf beginnen (mit Datum und Commit-Kennung).
  </action>
  <verify>
    <automated>npx playwright test --list > /dev/null && node -e "const s=require('fs').readFileSync('.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md','utf8');const ok=/Netz-Freeze/.test(s)&&/Doppel-Gr/.test(s)&&/editor-formatting.spec.js/.test(s)&&/editor-floating.spec.js/.test(s)&&/editor-insert.spec.js/.test(s)&&/editor-smoke.spec.js/.test(s);process.exit(ok?0:1)" && node -e "const s=require('fs').readFileSync('.github/workflows/ci.yml','utf8');process.exit(/e2e:/.test(s)&&/playwright test/.test(s)?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `09-BASELINE.md` enthält den Abschnitt „Netz-Freeze (gültig ab Doppel-Grün-Nachweis)" und nennt darin alle vier Spec-Dateien namentlich
    - Der Abschnitt formuliert die Änderungsregel, die Ausnahme für den Zählnachweis-Test und das Verfahren bei rotem Netz während der Migration
    - Der Abschnitt enthält den CI-Nachweis mit Verweis auf den blockierenden e2e-Job und die im Trockenlauf gelistete Gesamttestzahl
    - `npx playwright test --list` beendet mit Exit-Code 0 und listet die vier Netz-Dateien
    - `.planning/STATE.md` enthält unter „Open TODOs" einen neuen Eintrag zum erreichten Meilenstein mit Datum
  </acceptance_criteria>
  <done>Das Netz ist eingefroren, die Regel dafür ist schriftlich, und der CI-Mitlauf ist belegt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Testergebnis → Beweisprotokoll | Ein Laufergebnis wird zur dauerhaften Aussage über Verhaltensgleichheit |
| Netz-Spec-Dateien → Migrationsphase | Ab dem Freeze sind die Dateien Beweismittel, nicht mehr Arbeitsmaterial |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-12 | Repudiation | Doppel-Grün-Protokoll in 09-BASELINE.md | medium | mitigate | Das Protokoll führt Commit-Kennung, Zeitstempel, Testzahlen und Werkzeugversionen mit, sodass der Nachweis nachträglich reproduzierbar und nicht nachträglich umdeutbar ist. |
| T-09-13 | Tampering | Netz-Assertions während der Migration | high | mitigate | Der Netz-Freeze-Abschnitt macht jede Änderung begründungspflichtig und schreibt die Beweislast dem Migrationscode zu; die einzige zulässige Ausnahme ist namentlich benannt. Verstöße sind über `git diff` der vier Dateien in den Migrations-Commits sichtbar. |
| T-09-14 | Denial of Service | Laufzeit der vollen Suite im blockierenden CI-Job | low | accept | Die Suite bleibt im bisherigen Minutenbereich; der Zuwachs durch vier zusätzliche Spec-Dateien ist gemessen und im Protokoll dokumentiert. |
| T-09-SC | Tampering | npm/pip/cargo-Installationen | low | accept | Keine Paketinstallation in dieser Phase. |
</threat_model>

## Artifacts this phase produces

Siehe die vollständige Liste in `09-01-PLAN.md`. Von DIESEM Plan neu erzeugt:

- Abschnitt „D-04a Doppel-Grün-Nachweis" in `09-BASELINE.md`
- Abschnitt „Netz-Freeze (gültig ab Doppel-Grün-Nachweis)" in `09-BASELINE.md`
- Meilenstein-Eintrag unter „Open TODOs" in `.planning/STATE.md`

<verification>
- Zwei aufeinanderfolgende vollständige Netzläufe grün, protokolliert mit Commit-Kennung
- `npx playwright test` (volle Suite) und `npx jest` grün
- Zählnachweis-Test meldet 21 Vorkommen — der Editor-Code ist zum Nachweiszeitpunkt unverändert
- `09-BASELINE.md` enthält Doppel-Grün-Protokoll, Netz-Freeze-Regel und CI-Nachweis
- Kein Produktionscode geändert: `git diff --name-only` listet nur Planungsartefakte
</verification>

<success_criteria>
- D-04a ist erfüllt und belegt: das Netz war zweimal grün gegen den unveränderten Editor-Code
- Die Netz-Freeze-Regel steht schriftlich und benennt die einzige zulässige Ausnahme
- Der Mitlauf im blockierenden CI-Job ist nachgewiesen
</success_criteria>

<output>
Create `.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-05-SUMMARY.md` when done
</output>
