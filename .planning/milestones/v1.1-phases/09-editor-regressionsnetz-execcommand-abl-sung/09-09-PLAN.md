---
phase: 09-editor-regressionsnetz-execcommand-abl-sung
plan: 09
type: execute
wave: 8
depends_on: ["09-08"]
files_modified:
  - ui/editors/rich-text.js
  - tests/e2e/features/editor-floating.spec.js
  - CLAUDE.md
  - .planning/codebase/CONCERNS.md
  - .planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md
autonomous: false
requirements: [EDIT-01, EDIT-02, EDIT-03]

must_haves:
  truths:
    - "ui/editors/rich-text.js enthält keinen einzigen Aufruf der deprecated Editier-Kommando-API mehr — der Zählnachweis-Test steht auf 0 und ist grün (EDIT-01 vollständig erfüllt)"
    - "Das Absatzverhalten bei nicht abgefangenen Eingaben entspricht weiterhin dem in 09-BASELINE.md/A1 gemessenen Wert — der Wegfall des Setup-Aufrufs hat es nicht verändert"
    - "Das komplette Regressionsnetz ist nach der letzten Migrationsgruppe grün, ebenso die volle E2E-Suite und die Jest-Suite (EDIT-03 belegt)"
    - "Alle sechs Entity-Editoren, beide Toolbar-Varianten und die Markdown-Live-Shortcuts funktionieren unverändert — belegt durch das grüne Smoke-Netz und die menschliche Sichtprüfung (EDIT-02 belegt)"
    - "Die drei Aufrufe der deprecated API außerhalb des Editor-Moduls sind unverändert vorhanden und als bewusst außerhalb des Phasen-Scopes dokumentiert"
    - "CLAUDE.md und .planning/codebase/CONCERNS.md beschreiben den erreichten Zustand statt des alten Tech-Debt-Vermerks"
  artifacts:
    - "ui/editors/rich-text.js ohne Aufrufe der deprecated Editier-Kommando-API"
    - "Aktualisierter Abschnitt zu execCommand in CLAUDE.md (Conventions) und aktualisierter Eintrag in .planning/codebase/CONCERNS.md"
    - "09-BASELINE.md mit dem Abschluss-Protokoll (Zählstand 0, Netz grün, Abweichungsliste)"
  key_links:
    - "Wegfall des Setup-Aufrufs -> natives Absatzverhalten bei Umschalt-Eingabetaste -> A1-Referenztest im Netz"
    - "Zählnachweis-Test (Plan 09-03) -> Umstellung von 21 auf 0 -> maschineller Beleg für EDIT-01"
    - "Netz + volle Suite -> blockierender CI-Job -> Deploy-Gate"
  prohibitions:
    - statement: "Der Zählnachweis-Test ist der EINZIGE Netz-Test, der in dieser Phase geändert werden darf; jede weitere Änderung an einer Netz-Assertion in diesem Plan ist ein Beweis-Leck und muss stattdessen als Migrationsfehler behandelt werden"
      status: flagged-unverified
      verification: test
    - statement: "Der Abschluss darf NICHT gemeldet werden, solange auch nur ein Netz-Test rot ist oder durch Wiederholungsläufe grün gemacht wurde — Teilerfolge werden als Teilerfolge berichtet, nicht als Abschluss"
      status: flagged-unverified
      verification: judgment
---

<!-- planner-discipline-allow: document.execCommand -->

<objective>
Letzte Migrationsstufe und Abschluss der Phase: Der verbliebene Setup-Aufruf entfällt, der Zählnachweis wird von 21 auf 0 umgestellt, die Projektdokumentation wird auf den erreichten Stand gebracht, und der Entwickler sichtet die Editoren einmal von Hand im Browser.

Purpose: EDIT-01 gilt erst als erfüllt, wenn alle 21 Call-Sites ersetzt sind (D-06). Die menschliche Sichtprüfung deckt das ab, was Markup-Assertionen nicht können: die Optik der Toolbars, das Einfügen aus einer echten Zwischenablage und das Gefühl beim Tippen.
Output: `ui/editors/rich-text.js` frei von der deprecated API, aktualisierte Dokumentation, Abschluss-Protokoll in 09-BASELINE.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-CONTEXT.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-RESEARCH.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-08-SUMMARY.md
@ui/editors/rich-text.js
@CLAUDE.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Gruppe G): Setup-Aufruf für den Absatztrenner ablösen — die letzte Call-Site</name>
  <precondition>Plan 09-08 ist abgeschlossen und das komplette Netz war danach grün; der Zählstand im Editor-Modul beträgt 1.</precondition>
  <files>ui/editors/rich-text.js</files>
  <behavior>
    Prüfebene ist der als A1-Referenz gekennzeichnete Test aus `tests/e2e/features/editor-insert.spec.js`. Erwartetes Verhalten nach dem Eingriff:
    - Umschalt-Eingabetaste (der nicht abgefangene Zweig) erzeugt exakt die in 09-BASELINE.md/A1 gemessene Absatzstruktur
    - Die abgefangene Eingabetaste verhält sich unverändert wie nach Plan 09-08
    - Der Textinhalt bleibt in beiden Fällen vollständig erhalten
  </behavior>
  <read_first>
    - ui/editors/rich-text.js (Zeilen 505–520: `initEditorPasteHandlers` mit dem Setup-Aufruf im try/catch-Block und der anschließenden Verdrahtung der Ereignis-Zuhörer; Zeilen 564–576: `handleEditorKeydown` mit der Bedingung, dass die Umschalt-Eingabetaste NICHT abgefangen wird)
    - .planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md (Abschnitt A1 mit der gemessenen Absatzstruktur bei Umschalt-Eingabetaste — der Vergleichswert für diesen Task)
    - tests/e2e/features/editor-insert.spec.js (der A1-Referenztest — die Spezifikation dieses Tasks; er darf NICHT geändert werden)
    - 09-RESEARCH.md — Abschnitt „defaultParagraphSeparator" und Annahme A1 (die einzige robuste Alternative wäre, den bislang nicht abgefangenen Zweig ebenfalls über die Tastatur-Behandlung zu führen)
    - 09-CONTEXT.md — „Claude's Discretion": der Umgang mit dem Setup-Aufruf liegt im Ermessen, solange das Markup identisch bleibt
  </read_first>
  <action>
    Entferne zunächst den Setup-Aufruf samt seines try/catch-Blocks ersatzlos aus `initEditorPasteHandlers` und führe das komplette Netz aus.

    - Bleibt der A1-Referenztest grün, ist der Wegfall folgenlos: Ergänze an der Stelle einen kurzen Kommentar, dass der Browser den protokollierten Absatztrenner bereits als Vorgabe verwendet (mit Verweis auf 09-BASELINE.md/A1), und schließe die Gruppe ab.
    - Wird der A1-Referenztest rot, stelle das protokollierte Verhalten explizit her: Fange den bislang nicht abgefangenen Zweig in `handleEditorKeydown` zusätzlich ab und erzeuge dort über eine Range-Operation exakt die in A1 protokollierte Absatzstruktur. Nutze dafür die vorhandenen Hilfsfunktionen aus den Plänen 09-06 bis 09-08, statt neue Einfügewege zu erfinden. Halte in 09-BASELINE.md fest, dass dieser Weg gewählt wurde, welchen Messwert er reproduziert und dass die Interaktionslogik dadurch minimal verschoben wurde (Tastendruck wird nun abgefangen statt vom Browser behandelt) — bei identischem Markup.

    In beiden Fällen: keine Änderung an den Netz-Tests, kein Rückfall auf die alte API. Schreibe die Gruppe erst bei vollständig grünem Netz als einen atomaren Commit fest.
  </action>
  <verify>
    <automated>npm run build:dev && npx playwright test tests/e2e/features/editor-formatting.spec.js tests/e2e/features/editor-floating.spec.js tests/e2e/features/editor-insert.spec.js tests/e2e/features/editor-smoke.spec.js && npx jest</automated>
  </verify>
  <acceptance_criteria>
    - Das komplette Netz ist grün; `npx jest` ebenfalls
    - Im gesamten Modul verbleibt kein Aufruf der deprecated API: `node -e "const s=require('fs').readFileSync('ui/editors/rich-text.js','utf8');const code=s.split('\n').filter(l=>!/^\s*(\/\/|\*|\/\*)/.test(l)).join('\n');process.exit(/document\.execCommand/.test(code)?1:0)"` beendet mit 0
    - Verhalten: Der A1-Referenztest ist unverändert (kein Diff in `tests/e2e/features/editor-insert.spec.js`) und grün
    - Wurde der zweite Weg gewählt, dokumentiert 09-BASELINE.md die Entscheidung mit Messwert und Begründung
    - `npm run build:dev` beendet mit Exit-Code 0
    - Genau ein Commit für diese Gruppe
  </acceptance_criteria>
  <done>Die letzte der 21 Call-Sites ist ersetzt; das Absatzverhalten ist unverändert belegt.</done>
</task>

<task type="auto">
  <name>Task 2: Abschluss-Gate — Zählnachweis auf 0, volle Suiten, Produktions-Build, Dokumentation nachziehen</name>
  <files>tests/e2e/features/editor-floating.spec.js, CLAUDE.md, .planning/codebase/CONCERNS.md, .planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md</files>
  <read_first>
    - tests/e2e/features/editor-floating.spec.js (der in Plan 09-03 angelegte Zählnachweis-Test — der einzige Netz-Test, den diese Phase ändern darf; sein Kommentar verweist auf genau diesen Plan)
    - .planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md (Abschnitt „Netz-Freeze" mit der Ausnahmeregel; das Abschluss-Protokoll wird hier angehängt)
    - CLAUDE.md (Abschnitt „Conventions" mit dem Vermerk, dass die Ablösung der deprecated Editier-Kommando-API als Tech-Debt vorgemerkt ist und die Aufrufe bis dahin geduldet werden — dieser Vermerk ist nach dieser Phase überholt; außerdem die Abschnitte „Synchronized Editor Toolbars", „Read-Aloud Text Styles" und „Unified Editor Toolbars", die inhaltlich unverändert bleiben)
    - .planning/codebase/CONCERNS.md (der Eintrag zu den Fundstellen der deprecated API — Zeilen um 57 und 66–67; die drei Fundstellen außerhalb des Editor-Moduls bleiben bestehen und werden als bewusst außerhalb des Phasen-Scopes gekennzeichnet)
    - .planning/REQUIREMENTS.md (die Zeilen zu EDIT-01, EDIT-02 und EDIT-03 sowie die Traceability-Tabelle — Statusanpassungen übernimmt der Phasenabschluss, hier nur prüfen, ob die Formulierungen zum erreichten Stand passen)
  </read_first>
  <action>
    Stelle den Zählnachweis-Test in `tests/e2e/features/editor-floating.spec.js` von 21 auf 0 um: Der Test liest weiterhin `ui/editors/rich-text.js` per Dateizugriff und behauptet nun, dass dort kein Aufruf der deprecated Editier-Kommando-API mehr existiert (Kommentarzeilen beim Zählen ausfiltern, damit erklärende Kommentare den Test nicht selbst rot machen). Passe Testnamen und Kommentar an und vermerke im Kommentar, dass dies die in 09-BASELINE.md protokollierte, einzige zulässige Netzänderung dieser Phase ist.

    Führe danach die vollständigen Prüfungen aus: das komplette Netz, die volle E2E-Suite, die Jest-Suite, `npm run typecheck`, `npm run lint` und einen Produktions-Build (`npm run build`). Der Produktions-Build ist wichtig, weil er zusätzlich minifiziert und der Dedup-Pass dort dieselben Regeln anwendet.

    Hänge an `09-BASELINE.md` einen Abschnitt „Abschluss-Protokoll" an mit: Zählstand vorher/nachher je Migrationsgruppe (21 → 12 → 6 → 1 → 0), Commit-Kennungen der sieben Gruppen-Commits, Ergebnis der vollen Suiten, Liste aller Stellen, an denen die neue Implementierung bewusst vom naiven Vorbild abweicht (z. B. abweichende Tag-Form beim Durchstreichen, Wegfall des Nachbearbeitungs-Durchlaufs bei der Schriftgröße), sowie die Liste der Funde, die bewusst NICHT behoben wurden (Verweis auf die Entscheidungen aus Plan 09-01).

    Aktualisiere `CLAUDE.md`: Ersetze im Abschnitt „Conventions" den Tech-Debt-Vermerk zur deprecated Editier-Kommando-API durch eine Beschreibung des erreichten Zustands — das Editor-Modul arbeitet auf Selection/Range-DOM-Operationen, die neuen Hilfsfunktionen sind benannt, und für neue Editor-Funktionalität sind sie zu verwenden. Halte zusätzlich fest, dass außerhalb des Editor-Moduls noch drei Fundstellen existieren und wo sie liegen. Ergänze einen kurzen Hinweis auf das Regressionsnetz (vier Spec-Dateien) als Schutzschicht für künftige Editor-Änderungen.

    Aktualisiere `.planning/codebase/CONCERNS.md`: Markiere den Eintrag zu den Fundstellen im Editor-Modul als erledigt (mit Phasen- und Commit-Verweis) und formuliere die drei verbleibenden Fundstellen als eigenen, weiterhin offenen Eintrag mit dem Hinweis, dass sie in dieser Phase bewusst außerhalb des Scopes lagen.
  </action>
  <verify>
    <automated>npm run build:dev && npx playwright test && npx jest && npm run typecheck && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test` (volle Suite), `npx jest`, `npm run typecheck`, `npm run lint` und `npm run build` beenden jeweils mit Exit-Code 0
    - Der Zählnachweis-Test behauptet 0 statt 21 und filtert Kommentarzeilen aus; er ist grün
    - Verhalten: Außer dem Zählnachweis-Test wurde kein Netz-Test geändert — `git diff` der vier Spec-Dateien über diesen Plan zeigt Änderungen ausschließlich in dem Testblock des Zählnachweises
    - `09-BASELINE.md` enthält den Abschnitt „Abschluss-Protokoll" mit der Zählstands-Kette 21 → 12 → 6 → 1 → 0 und den Commit-Kennungen der Gruppen
    - `CLAUDE.md` enthält im Abschnitt „Conventions" keine Formulierung mehr, die die Aufrufe der deprecated API im Editor-Modul als geduldeten Tech-Debt beschreibt, sondern den erreichten Zustand samt Namen der neuen Hilfsfunktionen
    - `.planning/codebase/CONCERNS.md` weist den Editor-Modul-Eintrag als erledigt aus und führt die drei verbleibenden Fundstellen als eigenen offenen Eintrag
    - Die drei Fundstellen außerhalb des Editor-Moduls sind unverändert: `node -e "const fs=require('fs');const f=['ui/actions/system-actions.js','features/wiki/wiki.js','systems/entity-links.js'];const n=f.reduce((a,p)=>a+((fs.readFileSync(p,'utf8').match(/document\.execCommand/g)||[]).length),0);process.exit(n===3?0:1)"` beendet mit 0
  </acceptance_criteria>
  <done>EDIT-01 ist maschinell belegt erfüllt, alle Suiten sind grün, und die Projektdokumentation beschreibt den neuen Stand.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Handcheck im Browser — Toolbar-Optik, echte Zwischenablage, Tippgefühl</name>
  <what-built>
    Der Rich-Text-Editor arbeitet jetzt vollständig ohne die deprecated Editier-Kommando-API. Alle 21 Aufrufe wurden in sieben Gruppen durch Selection/Range-DOM-Operationen ersetzt, abgesichert durch ein Regressionsnetz aus vier Spec-Dateien, das vor der Migration zweifach grün gegen den alten Code lief und nach jeder Gruppe unverändert grün geblieben ist.

    Automatisiert bereits belegt: exaktes Markup je Formatgruppe, Persistenz über Speichern und Neuladen, beide Toolbars, alle sechs Entity-Editoren, Markdown-Live-Shortcuts, Einfüge-Pfade inklusive Sicherheitsprüfung.

    Nicht automatisiert prüfbar und deshalb dieser Handcheck: die Optik und Positionierung der Toolbars, das Einfügen aus einer echten Zwischenablage (Word/Excel/Browser) und das Tippgefühl im Editor.
  </what-built>
  <how-to-verify>
    1. `npm run build:dev` ausführen und `dist/dnd-tracker-bundled.html` im Chrome/Edge per Doppelklick öffnen (file://-Modus, wie am Spieltisch).
    2. Wiki öffnen, neuen Eintrag anlegen, Text eingeben. Jede Schaltfläche der statischen Toolbar einmal benutzen: Fett, Kursiv, Unterstrichen, Durchgestrichen, Liste, Link, Tabelle, Schriftart, Größe, Hervorheben, Vorlese-Stil, Rahmen, Formatierung entfernen. Erwartung: alles sieht aus und verhält sich wie vor der Umstellung; keine Meldung in der Browser-Konsole (F12).
    3. Text markieren und warten, bis die schwebende Toolbar erscheint. Erwartung: Sie erscheint an der richtigen Stelle über der Auswahl, ist nicht abgeschnitten, und ihre Schaltflächen wirken wie zuvor.
    4. Aus Word oder Excel eine kleine Tabelle kopieren und in den Editor einfügen. Erwartung: Die Tabelle erscheint im Editor-Stil, ohne Fremdformatierung.
    5. Eine Webseite mit formatiertem Text kopieren und einfügen. Erwartung: Text erscheint, nichts Unerwartetes passiert, keine Konsolenmeldung.
    6. Mit der Eingabetaste mehrere Zeilen tippen, dann mit Umschalt+Eingabetaste. Erwartung: Absätze und Umbrüche wie gewohnt; der Cursor bleibt dort, wo er hingehört.
    7. Markdown-Kurzschreibweisen tippen (Sternchen für Fett, Unterstrich/Sternchen für Kursiv, Tilden für Durchgestrichen). Erwartung: Umwandlung wie zuvor.
    8. Eintrag speichern, Seite neu laden, Eintrag erneut öffnen. Erwartung: Alle Formatierungen sind unverändert vorhanden.
    9. Denselben Kurz-Check in einem zweiten Editor (z. B. NPC-Beschreibung) mit Fett und Tabelle.
  </how-to-verify>
  <resume-signal>Antworte mit „freigegeben" oder beschreibe, was sich anders anfühlt/aussieht als vorher (mit Editor, Schaltfläche und beobachtetem Verhalten). Abweichungen werden als Migrationsfehler behandelt, nicht als neue Anforderungen.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nicht abgefangene Tastatureingabe → natives Absatzverhalten | Der Browser erzeugt Struktur im Editor-DOM ohne Anwendungslogik |
| Echte Zwischenablage → Editor-DOM (Handcheck) | Fremd-HTML aus realen Fremdanwendungen wird eingefügt |
| Dokumentation → künftige Entwicklung | Ein überholter Tech-Debt-Vermerk würde künftige Änderungen fehlleiten |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | Einfügen aus echter Zwischenablage (Handcheck-Schritte 4 und 5) | high | mitigate | Der automatisierte Sicherheits-Regressionstest aus Plan 09-04 deckt das synthetische Fremd-HTML ab; der Handcheck ergänzt reale Quellen (Textverarbeitung, Tabellenkalkulation, Webseite). Bereinigung vor dem Einfügen und Whitelist beim Speichern sind unverändert. |
| T-09-22 | Tampering | Wegfall des Setup-Aufrufs für den Absatztrenner | medium | mitigate | Der A1-Referenztest im eingefrorenen Netz ist der maschinelle Beleg, dass die erzeugte Absatzstruktur unverändert bleibt; weicht sie ab, wird das protokollierte Verhalten explizit hergestellt statt hingenommen. |
| T-09-23 | Repudiation | Abschluss-Protokoll und Zählnachweis-Umstellung | medium | mitigate | Das Protokoll führt die Zählstands-Kette und die Commit-Kennungen aller sieben Gruppen mit; die einzige Netzänderung ist vorab schriftlich als Ausnahme freigegeben und im Test kommentiert. |
| T-09-24 | Information Disclosure | Aktualisierte Dokumentation | low | accept | CLAUDE.md und CONCERNS.md enthalten ausschließlich Architekturhinweise, keine Geheimnisse. |
| T-09-SC | Tampering | npm/pip/cargo-Installationen | low | accept | Keine Paketinstallation in dieser Phase. |
</threat_model>

## Artifacts this phase produces

Siehe die vollständige Liste in `09-01-PLAN.md`. Von DIESEM Plan neu erzeugt:

- Abschnitt „Abschluss-Protokoll" in `09-BASELINE.md`
- Aktualisierter execCommand-Abschnitt in `CLAUDE.md` (Conventions) mit Nennung der neuen Hilfsfunktionen
- Aktualisierter/aufgeteilter Eintrag in `.planning/codebase/CONCERNS.md`
- Umgestellter Zählnachweis-Test (0 statt 21) in `tests/e2e/features/editor-floating.spec.js`

<verification>
- Zählstand im Editor-Modul: 0 (Kommentarzeilen ausgefiltert)
- Komplettes Netz, volle E2E-Suite, Jest-Suite, Typecheck, Lint und Produktions-Build grün
- Die drei Fundstellen außerhalb des Editor-Moduls unverändert (Anzahl 3)
- Außer dem Zählnachweis-Test keine Netzänderung in der gesamten Phase
- Menschliche Sichtprüfung freigegeben
</verification>

<success_criteria>
- EDIT-01: alle 21 Call-Sites ersetzt, maschinell belegt
- EDIT-02: beide Toolbars, alle sechs Entity-Editoren und die Markdown-Shortcuts funktionieren unverändert — automatisiert und von Hand bestätigt
- EDIT-03: das Regressionsnetz existiert, war vorher zweifach grün und ist nachher grün
- Dokumentation beschreibt den erreichten Zustand
</success_criteria>

<output>
Create `.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-09-SUMMARY.md` when done
</output>
