---
phase: 09-editor-regressionsnetz-execcommand-abl-sung
plan: 07
type: execute
wave: 6
depends_on: ["09-06"]
files_modified:
  - ui/editors/rich-text.js
autonomous: true
requirements: [EDIT-01, EDIT-02]

must_haves:
  truths:
    - "Schriftart und Schriftgröße über die statische Toolbar erzeugen nach der Migration byte-gleiches Markup wie in 09-BASELINE.md protokolliert; die gewählte Schriftart/Größe wird tatsächlich angewendet und nicht durch einen Ersatzwert überschrieben"
    - "Schriftart und Schriftgröße über die floating Toolbar erzeugen nach der Migration byte-gleiches Markup wie protokolliert"
    - "„Format entfernen\" der floating Toolbar liefert nach der Migration denselben Endzustand wie protokolliert — Auszeichnungen, Hervorhebungen und Rahmen der Selektion sind aufgelöst, der Textinhalt bleibt vollständig erhalten"
    - "Die gemerkte Selektion wird vor jedem Schriftart-/Größen-Eingriff wiederhergestellt, sodass ein Klick in das Auswahlfeld die Auswahl nicht verliert (bestehendes Verhalten bleibt erhalten)"
    - "Nach diesem Plan ist die Gesamtzahl der Aufrufe der deprecated Editier-Kommando-API im Modul von 12 auf 6 gesunken"
  artifacts:
    - "ui/editors/rich-text.js enthält die neuen Hilfsfunktionen applyFontFamilyToSelection und applyFontSizeToSelection als eindeutig benannte Top-Level-Funktionen"
    - "Zwei atomare Commits: einer je Migrationsgruppe (D-05)"
  key_links:
    - "Schriftart-/Größen-Auswahl (statisch) -> SystemActions set-editor-font/-size -> setEditorFont/setEditorFontSize -> gemerkte Range wiederherstellen -> applyFontFamilyToSelection/applyFontSizeToSelection"
    - "Schriftart-/Größen-Auswahl (floating) -> change-Handler (rich-text.js:770-802) -> dieselben Hilfsfunktionen -> identisches Markup wie im statischen Pfad, soweit die Baseline das ausweist"
    - "Format entfernen (floating) -> clearInlineFormattingAtSelection (aus Plan 09-06) + Auflösen von Hervorhebungen + removeSelectionBorders -> protokollierter Endzustand"
  prohibitions:
    - statement: "Der Zwischenschritt mit der numerischen Größenstufe und dem anschließenden Nachbearbeiten der erzeugten Elemente darf NICHT durch ein zusätzliches Nachbearbeiten aller Editor-Elemente ersetzt werden — die Umstellung erzeugt das Zielelement direkt an der Selektion und fasst keine Elemente außerhalb der Selektion an"
      status: flagged-unverified
      verification: test
    - statement: "Die beiden Toolbar-Varianten dürfen im Zuge der Migration NICHT auf ein gemeinsames Markup angeglichen werden, wo 09-BASELINE.md unterschiedliche Formen ausweist — bestehende Abweichungen sind Baseline, keine Bugs"
      status: flagged-unverified
      verification: judgment
---

<!-- planner-discipline-allow: document.execCommand -->

<objective>
Zweite Migrationsstufe: Schriftart und Schriftgröße in beiden Toolbars sowie „Format entfernen" der floating Toolbar werden auf Selection/Range-DOM-Operationen umgestellt — sechs Call-Sites in zwei Gruppen.

Purpose: Diese Gruppe enthält den einzigen Aufruf mit Nachbearbeitungs-Umweg (Größenstufe setzen, danach Attribute umschreiben). Sie wird direkt durch das Zielelement ersetzt, was den Code vereinfacht, ohne das persistierte Markup zu verändern.
Output: migrierte Schriftart-/Größen-/Formatentfernen-Pfade plus zwei wiederverwendbare Hilfsfunktionen, zwei atomare Commits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-CONTEXT.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-RESEARCH.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-PATTERNS.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-06-SUMMARY.md
@ui/editors/rich-text.js
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Gruppe C): Schriftart und Schriftgröße der statischen Toolbar umstellen — 2 Call-Sites</name>
  <precondition>Plan 09-06 ist abgeschlossen und das komplette Netz war danach grün — die Migration läuft strikt gruppenweise auf grünem Netz.</precondition>
  <files>ui/editors/rich-text.js</files>
  <behavior>
    Prüfebene sind die eingefrorenen Tests aus `tests/e2e/features/editor-formatting.spec.js`. Erwartetes Verhalten nach der Umstellung, byte-gleich zum Baseline-Wert:
    - Schriftart „serif" auf einer Wortselektion: das protokollierte Element mit der Serif-Familie (nicht der Ersatz-Familie)
    - Schriftgröße „20px" auf einer Wortselektion: das protokollierte Element mit genau dieser Größenangabe und ohne die numerische Größenstufe als Attribut
    - Beide Fälle überstehen den Persistenz-Roundtrip unverändert
    - Eine vor dem Aufklappen des Auswahlfelds gesetzte Selektion bleibt wirksam (die gemerkte Range wird wiederhergestellt)
    - Elemente außerhalb der Selektion bleiben unangetastet
  </behavior>
  <read_first>
    - ui/editors/rich-text.js (Zeilen 350–372: `setEditorFont` mit der Wiederherstellung der gemerkten Range und dem Zugriff auf die Schriftart-Zuordnung; Zeilen 373–399: `setEditorFontSize` mit dem Umweg über die numerische Größenstufe und dem anschließenden Umschreiben aller passenden Elemente im gesamten Editor; Zeilen 703–719: das `mousedown`-Merken der Selektion beim Anfassen eines Auswahlfelds)
    - .planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md (Inventar-Zeilen 371 und 393 mit dem exakt erhobenen Endzustand — insbesondere, ob im persistierten Markup eine Größenstufe als Attribut verbleibt)
    - tests/e2e/features/editor-formatting.spec.js (die eingefrorenen Schriftart-/Größen-Assertionen samt Roundtrip — die Spezifikation dieses Tasks)
    - 09-RESEARCH.md — Pattern 2 (Attribut-Wrapper statt Kommando-Wert; der Größen-Umweg kann entfallen, weil der Zwischenzustand ohnehin nie persistiert wurde) und der Abschnitt „fontSize-Migration"
    - ui/editors/rich-text.js (Zeilen der in Plan 09-06 angelegten Hilfsfunktion `wrapRangeWithElement` — sie wird hier wiederverwendet, nicht neu geschrieben)
    - core/constants.js (die Schriftart-Zuordnung, sofern sie in Plan 09-02 wiederhergestellt wurde — sie liefert die Familie zum ausgewählten Schlüssel)
  </read_first>
  <action>
    Lege zwei neue Top-Level-Hilfsfunktionen an (Namen exakt so, projektweit noch frei, modul-intern ohne `window`-Export):
    - `applyFontFamilyToSelection(editor, familyValue)`: aktuelle Selektion holen, ohne Range kontrolliert abbrechen, das protokollierte Zielelement mit der Schriftart-Angabe erzeugen und über `wrapRangeWithElement` um die Selektion legen.
    - `applyFontSizeToSelection(editor, sizeValue)`: analog, mit der protokollierten Größen-Angabe direkt am erzeugten Element. Der bisherige Umweg über die numerische Größenstufe mit anschließendem Umschreiben aller passenden Elemente im Editor entfällt ersatzlos — das Zielelement wird direkt erzeugt. Damit werden auch keine Elemente außerhalb der Selektion mehr angefasst; falls die Baseline für den alten Weg eine Nebenwirkung auf andere Elemente ausweist, ist das im Commit-Text zu vermerken und der Baseline-Wert bleibt maßgeblich.

    Ersetze in `setEditorFont` und `setEditorFontSize` die beiden Aufrufe durch diese Hilfsfunktionen. Die vorhandene Wiederherstellung der gemerkten Range, das Wächter-Muster und die Ermittlung von Editor-ID und Wert bleiben unverändert (sie wurden gegebenenfalls bereits in Plan 09-02 korrigiert).

    Baue das Bundle, führe das komplette Netz aus, und schreibe die Gruppe erst bei vollständig grünem Netz als einen atomaren Commit fest.
  </action>
  <verify>
    <automated>npm run build:dev && npx playwright test tests/e2e/features/editor-formatting.spec.js tests/e2e/features/editor-floating.spec.js tests/e2e/features/editor-insert.spec.js tests/e2e/features/editor-smoke.spec.js && npx jest</automated>
  </verify>
  <acceptance_criteria>
    - Das komplette Netz ist grün; `npx jest` ebenfalls
    - Verhalten: Der Schriftart-Test wählt weiterhin „serif" und die Assertion auf die Serif-Familie ist unverändert grün
    - Verhalten: Der Größen-Test behauptet weiterhin exakt „20px" im erzeugten Element und ist grün, inklusive Roundtrip
    - Im Bereich der Funktionen `setEditorFont` und `setEditorFontSize` verbleibt kein Aufruf der deprecated API: `node -e "const s=require('fs').readFileSync('ui/editors/rich-text.js','utf8');const a=s.indexOf('function setEditorFont');const b=s.indexOf('function clearEditorFormatting');const m=s.slice(a,b);process.exit(/document\.execCommand/.test(m)?1:0)"` beendet mit 0
    - `ui/editors/rich-text.js` enthält genau eine Definition von `applyFontFamilyToSelection` und genau eine von `applyFontSizeToSelection`
    - Verhalten: `setEditorFontSize` fasst keine Elemente außerhalb der Selektion mehr an — der Aufruf, der alle passenden Elemente im gesamten Editor einsammelte, existiert dort nicht mehr
    - `git diff` der vier Netz-Spec-Dateien zeigt in diesem Task keine Änderung
    - `npm run build:dev` beendet mit Exit-Code 0
    - Genau ein Commit für diese Gruppe
  </acceptance_criteria>
  <done>Schriftart und Schriftgröße der statischen Toolbar laufen ohne die deprecated API und erzeugen nachweislich identisches persistiertes Markup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Gruppe D): Schriftart, Größe und „Format entfernen" der floating Toolbar umstellen — 4 Call-Sites</name>
  <files>ui/editors/rich-text.js</files>
  <behavior>
    Prüfebene sind die eingefrorenen Tests aus `tests/e2e/features/editor-floating.spec.js`. Erwartetes Verhalten nach der Umstellung, byte-gleich zum Baseline-Wert:
    - Schriftart „serif" über das floating Auswahlfeld: das protokollierte Element mit der Serif-Familie
    - Größe „1.5em" über das floating Auswahlfeld: das protokollierte Element mit genau dieser Größenangabe
    - „Format entfernen" auf einer fett + hervorgehoben + gerahmten Selektion: der protokollierte Endzustand, Textinhalt vollständig erhalten
    - Die gemerkte Range bleibt über den Wechsel in die Toolbar hinweg wirksam
  </behavior>
  <read_first>
    - ui/editors/rich-text.js (Zeilen 770–802: der `change`-Handler der floating Toolbar mit der Wiederherstellung der gemerkten Range und den Zweigen für Schriftart, Größe und Vorlese-Stil; Zeilen 912–934: der Zweig „Format entfernen" in `applyFloatingFormat`, der zusätzlich Hervorhebungs-Elemente auflöst und `removeSelectionBorders` aufruft; Zeilen 468–490: `removeSelectionBorders`)
    - .planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md (Inventar-Zeilen 782, 788, 915, 916 mit den exakt erhobenen Endzuständen)
    - tests/e2e/features/editor-floating.spec.js (die eingefrorenen Assertionen für Selects und „Format entfernen" — die Spezifikation dieses Tasks)
    - ui/editors/rich-text.js (die in Plan 09-06 angelegte `clearInlineFormattingAtSelection` und die in Task 1 angelegten Schriftart-/Größen-Hilfsfunktionen — alle werden hier wiederverwendet, nichts wird dupliziert)
    - CLAUDE.md — „Duplicate Declaration Debugging Pattern" (keine doppelten Top-Level-Funktionsnamen; die Hilfsfunktionen existieren bereits und werden nur aufgerufen)
  </read_first>
  <action>
    Ersetze im `change`-Handler der floating Toolbar die beiden Aufrufe für Schriftart und Größe durch die in Task 1 angelegten Hilfsfunktionen. Der Größen-Zweig verliert dabei ebenfalls den Umweg über die numerische Größenstufe samt anschließendem Umschreiben der Elemente im gesamten Editor; das Zielelement wird direkt erzeugt. Die Wiederherstellung der gemerkten Range vor dem Eingriff und das Nachführen der gemerkten Range danach bleiben unverändert.

    Ersetze anschließend im Zweig „Format entfernen" von `applyFloatingFormat` die beiden Aufrufe durch `clearInlineFormattingAtSelection` (aus Plan 09-06). Die bereits vorhandene Nachbehandlung — Auflösen der in der Selektion liegenden Hervorhebungs-Elemente, `removeSelectionBorders`, Hinweismeldung — bleibt unverändert erhalten und wird nicht in die Hilfsfunktion verschoben, damit der Endzustand exakt dem protokollierten entspricht.

    Prüfe beim Umbau, dass keine der Änderungen eine neue funktions-lokale Zuweisung eines `window`-Bezeichners einführt (Build-Dedup-Regel).

    Baue das Bundle, führe das komplette Netz aus, und schreibe die Gruppe erst bei vollständig grünem Netz als zweiten atomaren Commit fest.
  </action>
  <verify>
    <automated>npm run build:dev && npx playwright test tests/e2e/features/editor-formatting.spec.js tests/e2e/features/editor-floating.spec.js tests/e2e/features/editor-insert.spec.js tests/e2e/features/editor-smoke.spec.js && npx jest</automated>
  </verify>
  <acceptance_criteria>
    - Das komplette Netz ist grün; `npx jest` ebenfalls
    - Die Gesamtzahl im Modul ist auf 6 gesunken: `node -e "const s=require('fs').readFileSync('ui/editors/rich-text.js','utf8');const n=(s.match(/document\.execCommand/g)||[]).length;process.exit(n===6?0:1)"` beendet mit 0
    - Im Bereich von `initFloatingToolbar` bis `handleSelectionChange` verbleibt kein Aufruf der deprecated API: `node -e "const s=require('fs').readFileSync('ui/editors/rich-text.js','utf8');const a=s.indexOf('function initFloatingToolbar');const b=s.indexOf('function handleSelectionChange');const m=s.slice(a,b);process.exit(/document\.execCommand/.test(m)?1:0)"` beendet mit 0
    - Verhalten: Der Test für „Format entfernen" ist unverändert grün und belegt weiterhin, dass der Textinhalt vollständig erhalten bleibt
    - Verhalten: Die floating Schriftart-/Größen-Tests sind unverändert grün, inklusive der Roundtrip-Fälle
    - Keine neue funktions-lokale `const`-Zuweisung eines `window`-Bezeichners: `node -e "const s=require('fs').readFileSync('ui/editors/rich-text.js','utf8');const bad=s.split('\n').filter(l=>/^\s+const\s+\w+\s*=\s*window\./.test(l));process.exit(bad.length>0?1:0)"` beendet mit 0
    - `git diff` der vier Netz-Spec-Dateien zeigt in diesem Task keine Änderung
    - Genau ein Commit für diese Gruppe
  </acceptance_criteria>
  <done>Beide Toolbars setzen Schriftart und Größe ohne die deprecated API; „Format entfernen" liefert unverändert den protokollierten Endzustand.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Auswahlwert (Schriftart/Größe) → Stil-Eigenschaft im Editor-DOM | Ein Auswahlfeldwert wird zur CSS-Eigenschaft im gespeicherten Markup |
| Selektion → Auflösen von Auszeichnungen | „Format entfernen" verändert bestehende Nutzerinhalte |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-02 | Tampering | `applyFontFamilyToSelection` / `applyFontSizeToSelection` | medium | mitigate | Es werden ausschließlich die Stil-Eigenschaften Schriftfamilie und Schriftgröße gesetzt, beide bereits auf der Whitelist in `utils/basic.js`. Die Werte stammen aus den festen Auswahllisten der Templates bzw. der Konstantenzuordnung, nicht aus freier Nutzereingabe. Die Roundtrip-Tests belegen die Whitelist-Passage. |
| T-09-17 | Tampering | „Format entfernen" (Auflösen von Elementen) | medium | mitigate | Der Netz-Test prüft explizit, dass der Textinhalt nach dem Auflösen vollständig erhalten bleibt; die Nachbehandlung bleibt unverändert, damit keine zusätzlichen Elemente außerhalb der Selektion entfernt werden. |
| T-09-18 | Tampering | Wegfall des Nachbearbeitungs-Durchlaufs über alle Editor-Elemente | low | mitigate | Der Wegfall verkleinert den Wirkungsbereich (nur noch die Selektion statt des gesamten Editors) — die Roundtrip-Assertionen belegen, dass das persistierte Markup identisch bleibt. |
| T-09-SC | Tampering | npm/pip/cargo-Installationen | low | accept | Keine Paketinstallation in dieser Phase. |
</threat_model>

## Artifacts this phase produces

Siehe die vollständige Liste in `09-01-PLAN.md`. Von DIESEM Plan neu erzeugt (in `ui/editors/rich-text.js`, modul-intern):

- `applyFontFamilyToSelection(editor, familyValue)`
- `applyFontSizeToSelection(editor, sizeValue)`

<verification>
- Komplettes Netz nach JEDER der beiden Gruppen grün
- Zählstand im Modul: 12 → 6
- Keine Änderung an den Netz-Spec-Dateien (`git diff --name-only` listet nur `ui/editors/rich-text.js`)
- Keine Änderung an `utils/basic.js`, `utils/testable-utils.js`, `assets/styles/editors.css` (D-02)
- Zwei atomare Commits, einer je Gruppe (D-05)
</verification>

<success_criteria>
- Sechs weitere Call-Sites sind ersetzt (Gesamtstand 15 von 21)
- Schriftart/Größe wirken in beiden Toolbars mit dem protokollierten Markup
- „Format entfernen" verhält sich unverändert und verliert keinen Textinhalt
</success_criteria>

<output>
Create `.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-07-SUMMARY.md` when done
</output>
