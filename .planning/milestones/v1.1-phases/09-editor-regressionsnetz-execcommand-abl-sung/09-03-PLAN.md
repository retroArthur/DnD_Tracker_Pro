---
phase: 09-editor-regressionsnetz-execcommand-abl-sung
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - tests/e2e/features/editor-floating.spec.js
autonomous: true
requirements: [EDIT-02, EDIT-03]

must_haves:
  truths:
    - "Eine Textselektion in einem Rich-Text-Editor bringt die floating Toolbar in den sichtbaren Zustand (Klasse visible), ohne dass ein Page-Error auftritt — bzw. das in 09-BASELINE.md eingefrorene Gegenverhalten wird explizit behauptet, falls der Entwickler den Ist-Zustand eingefroren hat"
    - "Jede Aktion der floating Toolbar (Fett, Kursiv, Unterstrichen, Durchgestrichen, Link, Liste, Rahmen, Tabelle, Vorlese-Select, Schriftart-Select, Größen-Select, Highlight-Farbfeld, Format entfernen) hat eine exakte Markup-Assertion gegen den Baseline-Wert"
    - "Die floating Toolbar erzeugt für Durchstreichen und Link nachweislich das in 09-BASELINE.md protokollierte Markup, auch wo es sich vom Ergebnis der statischen Toolbar unterscheidet — die vorbestehende Abweichung ist als solche im Testnamen benannt, nicht angeglichen"
    - "Die Kern-Formatgruppen der floating Toolbar überstehen den Persistenz-Roundtrip mit identischem Markup"
  artifacts:
    - "tests/e2e/features/editor-floating.spec.js existiert und deckt alle Bedienelemente von #floating-toolbar ab"
  key_links:
    - "Textselektion -> selectionchange (debounced 150ms) -> handleSelectionChange() -> #floating-toolbar.visible + gespeicherte Range"
    - "#floating-toolbar [data-floating-action] Klick -> applyFloatingFormat() -> Markup im aktiven .rich-editor"
    - "#floating-toolbar select change -> floating change-Handler (rich-text.js:770-802) -> Schriftart/Größe/Vorlese-Stil"
    - ".color-swatch[data-color] Klick -> applyFloatingHighlight() -> mark-Element mit Hintergrundfarbe"
  prohibitions:
    - statement: "Ein Test darf einen Formatierungspfad NICHT über page.evaluate auf die Anwendungsfunktion abkürzen, wenn ein echter Interaktionspfad existiert — page.evaluate ist ausschließlich Setup-Vehikel oder Ersatz für nachweislich UI-lose Zweige (08-CONTEXT D-06)"
      status: flagged-unverified
      verification: test
---

<objective>
Nagle die zweite Toolbar-Variante fest: Das Regressionsnetz deckt die floating Toolbar (erscheint bei Textselektion in jedem contenteditable-Editor) mit exakten Markup-Assertionen und Persistenz-Roundtrips ab.

Purpose: D-04 macht das Wiki zum Referenz-Editor über BEIDE Toolbars, D-03 legt Markup plus Persistenz-Roundtrip als Prüfebene fest, und EDIT-02 verlangt, dass beide Toolbar-Varianten nach der Ablösung unverändert funktionieren. Die floating Toolbar ist zugleich das Referenzmuster der Migration (sie arbeitet für die meisten Aktionen bereits ohne die deprecated Editier-Kommando-API) und enthält gleichzeitig vier der 21 zu migrierenden Call-Sites (Schriftart, Größe, Format entfernen ×2).
Output: `tests/e2e/features/editor-floating.spec.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-CONTEXT.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-RESEARCH.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-PATTERNS.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-02-SUMMARY.md
@ui/editors/rich-text.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Floating Toolbar sichtbar machen + Inline-Formate, Liste, Rahmen, Tabelle, Link festnageln</name>
  <files>tests/e2e/features/editor-floating.spec.js</files>
  <read_first>
    - tests/e2e/features/editor-formatting.spec.js (Boot-, Formular- und Roundtrip-Muster aus 09-01/09-02 — sie werden hier wiederverwendet, nicht neu erfunden)
    - assets/templates/modals-editors.html (Zeilen 71–115: `#floating-toolbar` mit `.toolbar-row`, den Buttons `data-floating-action="bold|italic|underline|strikethrough|link|list|border|table"`, den Selects `data-floating-action="readAloud|font|fontSize"` und der `.color-row` mit `.color-swatch[data-color]`)
    - ui/editors/rich-text.js (Zeilen 696–769: `initFloatingToolbar`, das `mousedown`/`click`/`change`-Verdrahtung und das Merken der Selektion regelt; Zeilen 817–935: `applyFloatingFormat` inkl. Toggle-Erkennung, Link-Aufbau mit target/rel und Listen-Toggle; Zeilen 937–990: `handleSelectionChange` mit dem 150-ms-Debounce und der Positionierung; Zeilen 1001+: `applyFloatingHighlight`)
    - .planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md (Abschnitt A3: ob die floating Toolbar heute erscheint; Inventar-Zeilen 782/788/915/916 mit dem erhobenen Markup; die Zeile zu Durchstreichen über die floating Toolbar im Vergleich zur statischen)
    - 09-RESEARCH.md — Pitfall 4 (Link-Markup unterscheidet sich zwischen beiden Toolbars — vorbestehend, nicht angleichen) und Pitfall 5 (Durchstreichen-Tagform)
    - tests/e2e/helpers/test-utils.js (Zeilen 11–40: `loadApp`/`navigateToTab` als Setup-Vehikel)
  </read_first>
  <action>
    Lege `tests/e2e/features/editor-floating.spec.js` an (`// @ts-check`, Playwright-Import, deutsche Testnamen, Abschnitts-Banner) mit einem gemeinsamen `beforeEach`, das die App bootet, ins Wiki wechselt, das Eintrags-Formular öffnet und Testtext in `#wiki-content` eingibt.

    Basis-Testfall „Toolbar erscheint": Text in `#wiki-content` selektieren (echte Selektion über `locator.selectText()` bzw. Maus-Drag; die Anwendung reagiert auf `selectionchange` mit 150 ms Debounce, deshalb mit `waitForSelector('#floating-toolbar.visible')` bzw. `waitForFunction` auf die Klasse warten — niemals mit `waitForTimeout`). Assertion je nach eingefrorener Baseline: Ist die Reparatur umgesetzt (option-a), muss `#floating-toolbar` die Klasse `visible` erhalten und der gesammelte `pageerror`-Array leer sein. Wurde der Ist-Zustand eingefroren (option-b/c), behauptet der Test exakt das in 09-BASELINE.md/A3 protokollierte Gegenverhalten und trägt im Namen den Vermerk „eingefrorener Defekt" plus einen Kommentar mit Verweis auf 09-BASELINE.md.

    Danach je ein Testfall pro Button mit exakter `innerHTML`-Assertion gegen den Baseline-Wert: `bold`, `italic`, `underline`, `strikethrough`, `list`, `border`, `table`, `link`. Für `link` vorab `page.on('dialog')` registrieren und eine feste URL bestätigen; die Assertion muss die von dieser Toolbar gesetzten Attribute (Ziel- und Beziehungs-Attribut) mitprüfen und im Testnamen als bewusst abweichend gegenüber der statischen Toolbar gekennzeichnet sein. Für `strikethrough` die Tag-Form aus der Baseline behaupten und im Testnamen kenntlich machen, dass sie sich von der statischen Toolbar unterscheidet (vorbestehend, nicht angleichen).

    Zusätzlich zwei Toggle-Testfälle: (a) `bold` auf bereits fett gesetzten Text erneut anwenden (Erwartung: Auszeichnung wird entfernt, exakte Assertion), (b) `list` auf eine bereits als Liste formatierte Selektion erneut anwenden (Erwartung: Liste wird aufgelöst, exakte Assertion).

    Ein Teilselektions-Testfall (09-RESEARCH.md Pitfall 6): Eine Selektion aufbauen, die mitten in einer bestehenden Auszeichnung beginnt und außerhalb endet, dann `bold` anwenden. Erwartung ist der Baseline-Wert; zusätzlich muss der gesammelte `pageerror`-Array leer bleiben (kein unbehandelter Ausnahmefehler).
  </action>
  <verify>
    <automated>npm run build:dev && npx playwright test tests/e2e/features/editor-floating.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/features/editor-floating.spec.js` existiert; `npx playwright test tests/e2e/features/editor-floating.spec.js` beendet mit Exit-Code 0
    - Der Spec enthält mindestens 11 Testfälle (Sichtbarkeit, 8 Buttons, 2 Toggle-Fälle) plus den Teilselektions-Fall
    - Jeder Formatierungs-Testfall enthält eine exakte `innerHTML`-Gleichheitsassertion
    - Verhalten: Der Sichtbarkeits-Test wartet über `waitForSelector`/`waitForFunction` auf `#floating-toolbar.visible` und sammelt `pageerror`-Ereignisse; der Fehler-Array ist am Testende leer (bzw. der eingefrorene Gegenzustand ist explizit behauptet)
    - Verhalten: Der Teilselektions-Test bleibt grün und ohne Page-Error — die Selektion überspannt nachweislich eine Elementgrenze
    - Der Spec verwendet kein `waitForTimeout` und kein `isVisible()`: `node -e "const s=require('fs').readFileSync('tests/e2e/features/editor-floating.spec.js','utf8');process.exit((/waitForTimeout/.test(s)||/isVisible\(\)/.test(s))?1:0)"` beendet mit 0
  </acceptance_criteria>
  <done>Alle Button-Aktionen der floating Toolbar sind auf exaktes Markup festgenagelt, inklusive Toggle- und Teilselektionsverhalten.</done>
</task>

<task type="auto">
  <name>Task 2: Floating-Selects (Vorlese-Stil, Schriftart, Größe), Highlight-Farbfelder und „Format entfernen" festnageln</name>
  <files>tests/e2e/features/editor-floating.spec.js</files>
  <read_first>
    - ui/editors/rich-text.js (Zeilen 770–802: der `change`-Handler der floating Toolbar — er stellt die gemerkte Range wieder her und behandelt `font`, `fontSize` und `readAloud`; Zeilen 912–934: der `removeFormat`-Zweig in `applyFloatingFormat`, der zusätzlich `mark`-Elemente auflöst und Rahmen entfernt; Zeilen 1001+: `applyFloatingHighlight` mit dem Farbwert aus `data-color`)
    - assets/templates/modals-editors.html (Zeilen 83–115: die `option`-Werte der drei Selects — Vorlese-Stile `parchment|crimson|violet|sage|sky|slate`, Schriftarten `arial|serif|mono|roboto|inter|poppins|source-sans`, Größen `0.8em|1em|1.2em|1.5em`; die `.color-swatch`-Farbwerte)
    - .planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md (Inventar-Zeilen 782, 788, 915, 916 — genau diese vier Call-Sites werden in Plan 09-07 migriert und brauchen hier ihren Vorher-Beweis)
    - tests/e2e/features/editor-floating.spec.js (Stand nach Task 1)
    - ui/actions/system-actions.js (Zeilen 92–140: der davon getrennte Highlight-Pfad der statischen Toolbar — zeigt, welche Markup-Form für Hervorhebungen erwartet wird)
  </read_first>
  <action>
    Erweitere `tests/e2e/features/editor-floating.spec.js` um einen `test.describe`-Block „Selects und Farbfelder" mit exakten Markup-Assertionen gegen die Baseline:

    - Vorlese-Stil: `selectOption` auf `#floating-toolbar [data-floating-action="readAloud"]` mit `crimson`; zusätzlich prüfen, dass der Select danach wieder auf dem ersten Eintrag steht (die Anwendung setzt ihn zurück).
    - Schriftart: `selectOption` auf `#floating-toolbar [data-floating-action="font"]` mit `serif` — der Erwartungswert stammt aus der Baseline-Zeile 782; ein Fallback auf die Arial-Familie muss den Test rot machen.
    - Schriftgröße: `selectOption` auf `#floating-toolbar [data-floating-action="fontSize"]` mit `1.5em` — Erwartungswert aus Baseline-Zeile 788.
    - Highlight: Klick auf ein `.color-swatch[data-color="…"]` mit einer konkreten Farbe aus dem Template; Assertion auf das erzeugte Hervorhebungs-Element inklusive gesetzter Stil-Eigenschaften; zusätzlich der Aufhebungsfall über das Farbfeld mit dem Transparenzwert, falls im Template vorhanden.
    - Format entfernen: Text zuerst fett + hervorgehoben + gerahmt auszeichnen, dann `[data-floating-action="removeFormat"]` klicken; exakte Assertion auf den Baseline-Endzustand (Zeilen 915/916). Zusätzlich prüfen, dass `textContent` unverändert erhalten bleibt (kein Inhaltsverlust).

    Ergänze anschließend einen `test.describe`-Block „Persistenz-Roundtrip (floating)" mit Roundtrip-Fällen für mindestens Fett, Schriftart, Schriftgröße und Highlight nach dem Muster aus 09-02 (speichern, `page.reload()`, Eintrag über `[data-action="select-wiki-entry"]` und `[data-action="edit-wiki"]` wieder öffnen, exakte Re-Assertion).

    Ist der Ist-Zustand eingefroren (option-b/c) und erscheint die floating Toolbar deshalb nicht, ersetze diese Testfälle durch je einen Test, der exakt den in 09-BASELINE.md protokollierten Nicht-Zustand behauptet, und markiere die vier betroffenen Call-Sites im Spec-Kopfkommentar als „nur per direktem Funktionsaufruf migrierbar" mit Verweis auf Plan 09-07.
  </action>
  <verify>
    <automated>npm run build:dev && npx playwright test tests/e2e/features/editor-floating.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/e2e/features/editor-floating.spec.js -g "Selects"` beendet mit 0 und führt mindestens 5 Tests aus
    - `npx playwright test tests/e2e/features/editor-floating.spec.js -g "Roundtrip"` beendet mit 0 und führt mindestens 4 Tests aus
    - Verhalten: Der Schriftart-Test wählt `serif` und behauptet die Serif-Familie im Markup
    - Verhalten: Der Größen-Test wählt `1.5em` und behauptet exakt diesen Wert im erzeugten Element
    - Verhalten: Nach „Format entfernen" ist `textContent` von `#wiki-content` unverändert (kein Inhaltsverlust), während die Auszeichnungen laut Baseline verschwunden sind
    - Jeder Testfall enthält eine exakte `innerHTML`-Gleichheitsassertion
  </acceptance_criteria>
  <done>Die vier in der floating Toolbar liegenden Migrations-Call-Sites haben einen exakten, roundtrip-geprüften Vorher-Beweis.</done>
</task>

<task type="auto">
  <name>Task 3: UI-lose Zweige von formatText (Überschrift, Highlight, Schriftart) per direktem Funktionsaufruf festnageln</name>
  <files>tests/e2e/features/editor-floating.spec.js</files>
  <read_first>
    - ui/editors/rich-text.js (Zeilen 324–349: die Zweige `heading`, `font` und `highlight` in `formatText` — die zugehörigen Call-Sites 339, 341, 344 und 346)
    - .planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md (Abschnitt A2 mit dem Grep-Ergebnis über alle Template-Dateien und die Inventar-Zeilen 339/341/344/346 mit dem per direktem Aufruf erhobenen Markup)
    - 09-RESEARCH.md — Pitfall 3 (kein Template referenziert diese Formatnamen; D-06 verlangt trotzdem, dass alle 21 Call-Sites ersetzt werden — also braucht es einen Vorher-/Nachher-Beweis ohne UI-Pfad)
    - 09-CONTEXT.md — Abschnitt „Fortgeltende Testregeln" (Maskierungs-Kriterium: `page.evaluate` ist erlaubt, wo kein Interaktionspfad existiert)
    - tests/e2e/features/editor-floating.spec.js (Stand nach Task 2)
  </read_first>
  <action>
    Ergänze in `tests/e2e/features/editor-floating.spec.js` einen `test.describe`-Block „UI-lose Zweige (kein Toolbar-Pfad vorhanden)" mit je einem Testfall für `heading`, `highlight` (Farbwert und Wert `none`) und `font`.

    Vorgehen je Testfall: Testtext in `#wiki-content` eingeben, per `page.evaluate` eine echte Selektion über den Textinhalt aufbauen (Range über `document.createRange`/`window.getSelection` — dokumentiertes Setup-Vehikel), dann `window.formatText('wiki-content', '<format>', '<wert>')` direkt aufrufen und danach exakt gegen den in 09-BASELINE.md protokollierten Wert assertieren.

    Im Kopfkommentar des Blocks festhalten, WARUM hier kein Toolbar-Klick verwendet wird: Der Grep aus 09-BASELINE.md/A2 belegt, dass kein Template diese Formatnamen auslöst. Verweise auf 09-RESEARCH.md Pitfall 3 und auf Plan 09-06, der diese vier Call-Sites migriert. Ergänze außerdem einen Kommentar, dass diese Tests bewusst KEIN Nutzerverhalten prüfen, sondern die Funktionsgleichheit toter Zweige über die Migration hinweg sichern.

    Ergänze abschließend einen Test „Inventar-Vollständigkeit", der `ui/editors/rich-text.js` über `fs` einliest und behauptet, dass genau 21 Vorkommen der deprecated Editier-Kommando-API existieren — dieser Test ist der Zählnachweis für die Baseline und wird in Plan 09-09 auf 0 umgestellt. Kennzeichne ihn im Namen eindeutig als Zählnachweis (er ist der einzige Test des Netzes, der in Plan 09-09 bewusst geändert werden darf; jede andere Netzänderung wäre ein Beweis-Leck).
  </action>
  <verify>
    <automated>npm run build:dev && npx playwright test tests/e2e/features/editor-floating.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/e2e/features/editor-floating.spec.js -g "UI-lose"` beendet mit 0 und führt mindestens 4 Tests aus (heading, highlight-Farbe, highlight-none, font)
    - Jeder dieser Testfälle enthält eine exakte `innerHTML`-Gleichheitsassertion gegen den Baseline-Wert
    - Der Zählnachweis-Test liest `ui/editors/rich-text.js` per `fs` und behauptet exakt 21 Treffer (`toBe(21)`, nicht `toBeGreaterThan`)
    - Der Zählnachweis-Test ist im Testnamen eindeutig als solcher erkennbar und trägt einen Kommentar mit Verweis auf Plan 09-09
    - Der Block trägt einen Kopfkommentar, der die Verwendung von `page.evaluate` mit dem fehlenden Interaktionspfad begründet
  </acceptance_criteria>
  <done>Auch die vier UI-losen Call-Sites haben einen exakten Vorher-Beweis, und die Zahl 21 ist als Test verankert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Selektion → floating Toolbar → Editor-DOM | Eine Nutzerselektion steuert, welcher DOM-Bereich umgebaut wird |
| Farbwert aus `data-color` → Style-Attribut | Ein Template-Wert wird zur CSS-Eigenschaft im gespeicherten Markup |
| Test-Setup (`page.evaluate` mit Range-Aufbau und direktem Funktionsaufruf) | Testkontext greift auf Anwendungs-Globals zu |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-07 | Tampering | `applyFloatingHighlight` (Farbwert → Style) | low | accept | Die Farbwerte stammen fest aus dem Template (`data-color`), nicht aus Nutzereingabe; die Style-Whitelist in `utils/basic.js` erlaubt `background-color` bereits. Dieser Plan ändert keinen Produktionscode. |
| T-09-08 | Elevation of Privilege | Direktaufruf `window.formatText` im Testkontext | low | accept | Nur im Playwright-Testkontext gegen das lokale `file://`-Bundle; die Funktion ist bereits global exportiert, es entsteht kein neuer Einstiegspunkt. |
| T-09-09 | Repudiation | Zählnachweis-Test (21 Treffer) | low | mitigate | Der Test ist im Namen als einziger bewusst änderbarer Netz-Test markiert und verweist auf den Plan, der ihn umstellt — damit ist jede spätere Änderung nachvollziehbar und nicht als stille Anpassung tarnbar. |
| T-09-SC | Tampering | npm/pip/cargo-Installationen | low | accept | Keine Paketinstallation in dieser Phase. |
</threat_model>

## Artifacts this phase produces

Siehe die vollständige Liste in `09-01-PLAN.md`. Von DIESEM Plan neu erzeugt:

- `tests/e2e/features/editor-floating.spec.js` — floating Toolbar (Buttons, Selects, Farbfelder, Format entfernen), Toggle- und Teilselektionsfälle, Roundtrips, UI-lose Zweige, Zählnachweis-Test

<verification>
- `npm run build:dev && npx playwright test tests/e2e/features/editor-floating.spec.js` — grün
- Kein Produktionscode geändert: `git diff --name-only` listet ausschließlich `tests/e2e/features/editor-floating.spec.js`
- Der Zählnachweis-Test behauptet exakt 21 Vorkommen in `ui/editors/rich-text.js`
</verification>

<success_criteria>
- Jedes Bedienelement der floating Toolbar hat eine exakte Markup-Assertion
- Die vier floating Call-Sites (Schriftart, Größe, Format entfernen ×2) und die vier UI-losen Zweige haben einen Vorher-Beweis
- Kern-Formatgruppen der floating Toolbar sind roundtrip-geprüft
</success_criteria>

<output>
Create `.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-03-SUMMARY.md` when done
</output>
