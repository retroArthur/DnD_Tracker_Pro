---
phase: 13-h-rtung-wartbarkeit
plan: 09
type: execute
wave: 4
depends_on: ["13-02", "13-08"]
files_modified:
  - features/wiki/wiki.js
  - features/wiki/wiki-crud.js
  - loader.js
autonomous: true
requirements: [MAINT-01]

estimate:
  tokens: 22500
  raw_tokens: 45000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Keine der aus `features/wiki/wiki.js` entstandenen Dateien überschreitet 800 Zeilen (D-01, hart)."
    - "Geschnitten wurde entlang der vorhandenen `// ====`-Sektionsbanner, nicht auf die Zeilenzahl hin (D-02): `wiki.js` behält STATE, CONSTANTS und RENDER; `wiki-crud.js` bekommt WIKI CRUD und WIKI UX IMPROVEMENTS."
    - "Das Verhalten ist unverändert: `tests/e2e/features/wiki.spec.js`, die volle Jest-Suite, beide `dist`-Bündel und die Build-Tests laufen grün — der volle Suiten-Lauf ist Hard-Gate VOR dem Commit (Roadmap-Auslegungshinweis)."
    - "Beide Dateien sind in `loader.js` `MODULES` registriert, `wiki.js` vor `wiki-crud.js` — die gemeinsam genutzten `const`-Deklarationen (`WikiState`, `WIKI_CATEGORIES`, `WIKI_TEMPLATES`) stehen in `wiki.js` und müssen im Loader-Modus vorher ausgewertet sein. In `build.py` wird NICHTS eingetragen (ARCH-01: `loader.js` ist die einzige Modulliste)."
    - "Keine Funktion steht nach der Aufteilung in beiden Dateien: `check_duplicate_functions()` bricht den Build sonst vor dem Bündeln ab und nennt beide Dateien — dieser Abbruch ist der gewünschte Ausgang und wird nicht umgangen (D-06)."
    - "Jede der beiden Dateien trägt ihren eigenen `// [SECTION:…]`-Kopf und einen eigenen Exportblock am Dateiende; exportiert wird nur, was andere Module oder `data-action`-Handler brauchen (CLAUDE.md Export-Audit-Regel). Die Summe der `window.`-Exporte beider Dateien ist gleich der bisherigen Summe."
    - "MAINT-01 · Randfall `encoding`: „Zeilenzahl\" bedeutet in D-01 physische, zeilenumbruchgetrennte Zeilen, wie `wc -l` sie zählt — nicht Anweisungen, nicht logische Zeilen, nicht Zeichen. Das gilt für alle vier Aufteilungen dieser Phase."
    - "MAINT-01 · Randfall `empty`: Nach der Aufteilung rendert jede betroffene Ansicht auch mit einer leeren Kampagne (keine Einträge, keine Kategorien) fehlerfrei — geprüft über die bestehende E2E-Suite, die gegen einen frischen Zustand startet."
  prohibitions:
    - "Ein Suiten-Lauf, der nicht gelaufen ist, gilt nicht als Gate. Der Roadmap-Auslegungshinweis verlangt vor JEDEM Commit einer MAINT-01-Aufteilung einen vollen Suiten-Lauf; er ist erfüllt durch einen tatsächlich ausgeführten Lauf mit vorgelegter Ausgabe, nie durch die Behauptung, er wäre grün. Dasselbe gilt für die drei folgenden Aufteilungspläne 13-10, 13-11 und 13-12."
  artifacts:
    - features/wiki/wiki.js
    - features/wiki/wiki-crud.js
    - loader.js
  key_links:
    - "`loader.js` `MODULES` → `build.py` liest die Liste zur Build-Zeit und bricht ab, wenn eine gelistete Datei fehlt (ARCH-01, Phase 11)"
    - "`renderWikiDetail()` (bleibt in `wiki.js`) ruft `parseWikiLinks()`, `addTOCAnchors()`, `findBacklinks()`, `extractWikiLinks()`, `renderWikiBreadcrumb()`, `renderWikiTOC()`, `renderWikiTemplateSelector()` — alle wandern nach `wiki-crud.js`; die Auflösung passiert zur Laufzeit, nicht zur Ladezeit"
    - "`ui/actions/wiki-actions.js` und `systems/wiki-links.js` rufen die exportierten Wiki-Funktionen über `window` — die Exportnamen dürfen sich nicht ändern"
---

<objective>
Erste der vier Aufteilungen aus `MAINT-01`, nach der Risikoreihenfolge aus D-05:
`wiki.js` → `initiative.js` → `rich-text.js` → `dmscreen-render.js`. `features/wiki/wiki.js` ist mit
1217 Zeilen die kleinste der vier, hat die klarste Schnittkante und wird in 14 Testdateien erwähnt —
der risikoärmste Einstieg, an dem das Verfahren geübt wird, bevor es auf die schwereren Fälle trifft.

Purpose: Erfolgskriterium 4 der Phase, erstes Viertel. Zugleich ist dieser Plan die Blaupause für
13-10, 13-11 und 13-12: dasselbe Verfahren (Sektionsbanner als Schnittkante, `loader.js` als einzige
Registrierung, `python build.py` nach jeder Verschiebung, voller Suiten-Lauf als Hard-Gate vor dem
Commit).
Output: `features/wiki/wiki.js` (gekürzt) und `features/wiki/wiki-crud.js` (neu), beide unter 800
Zeilen, beide in `loader.js` registriert.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-h-rtung-wartbarkeit/13-CONTEXT.md
@.planning/phases/13-h-rtung-wartbarkeit/13-RESEARCH.md
@.planning/phases/13-h-rtung-wartbarkeit/13-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Dieser Plan erzeugt:

| Symbol / Datei | Art | Ort |
|---|---|---|
| `features/wiki/wiki-crud.js` | neues Quellmodul (WIKI CRUD + WIKI UX IMPROVEMENTS) | `features/wiki/` |
| `'features/wiki/wiki-crud.js'` | neuer Eintrag im `MODULES`-Array, direkt hinter `'features/wiki/wiki.js'` | `loader.js` |

Verschobene, nicht neu erzeugte Symbole (die Namen bleiben, nur die Datei ändert sich):
`selectWikiEntry`, `toggleWikiCategory`, `expandAllWikiCategories`, `collapseAllWikiCategories`,
`showWikiForm`, `hideWikiForm`, `updateWikiParentSelect`, `initWikiCategoryListener`,
`initWikiSearchListener`, `isDescendantOf`, `parseWikiLinks`, `extractWikiLinks`, `findBacklinks`,
`toggleWikiEntry`, `filterWiki`, `sortWiki`, `saveWikiEntry`, `editWikiEntry`, `cancelWikiEdit`,
`clearWikiForm`, `deleteWikiEntry`, `toggleWikiPin`, `navigateToWikiEntry`, `createWikiFromLink`,
`searchWikiTag`, `insertWikiLink`, `addToWikiRecentlyViewed`, `getWikiBreadcrumb`,
`renderWikiBreadcrumb`, `getSearchContextPreview`, `renderWikiSearchDropdown`,
`handleWikiSearchKeydown`, `updateSearchDropdownSelection`, `extractWikiTOC`, `renderWikiTOC`,
`addTOCAnchors`, `scrollToTOCHeading`, `renderWikiTemplateSelector`, `applyWikiTemplate`,
`showWikiLinkSuggester`, `hideWikiLinkSuggester`, `insertWikiLinkSuggestion`, `handleWikiContentInput`.

**Kein Eintrag in `build.py`.** `build.py` liest `MODULES` aus `loader.js` (ARCH-01/D-01, Phase 11) und
bricht hart ab, wenn eine gelistete Datei auf der Platte fehlt.

<tasks>

<task type="tracer">
  <name>Task 1: `wiki-crud.js` anlegen, EINE Sektion verschieben, registrieren, bauen</name>
  <reversibility rating="costly">Die 800-Zeilen-Grenze aus D-01 später zu senken hieße erneut aufteilen; sie zu heben ist billig, macht aber die geleistete Aufteilung nicht rückgängig. Ein Rückbau dieser Aufteilung müsste `loader.js`, die Exportblöcke und die Testpfade erneut anfassen.</reversibility>
  <files>features/wiki/wiki-crud.js, features/wiki/wiki.js, loader.js</files>
  <read_first>
    - `features/wiki/wiki.js` vollständig — insbesondere die sechs Sektionsbanner (`STATE`, `CONSTANTS`,
      `RENDER`, `WIKI CRUD`, `WIKI UX IMPROVEMENTS`, `EXPORTS FOR GLOBAL ACCESS`) und den Exportblock
      am Dateiende. **Die Zeilennummern aus `13-RESEARCH.md` gelten nach den Plänen 13-02 und 13-08
      nicht mehr exakt — orientiere dich an den Bannertexten, nicht an Zahlen.**
    - `features/npcs/npc-render.js` und `features/npcs/npc-crud.js` (je Zeilen 1–10 und der Exportblock
      am Dateiende) — die etablierte Aufteilungskonvention dieses Projekts: eigener
      `// [SECTION:X]`-Kopf je Datei, eigener `EXPORTS FOR GLOBAL ACCESS`-Block am Ende, keine
      `import`/`export`-Anweisungen
    - `features/party/party-render.js` / `party-crud.js` (dieselbe Konvention, `-render`/`-crud`-Namensschema)
    - `loader.js` Zeilen 55–115 (`MODULES`-Array; `'features/wiki/wiki.js'` steht heute allein in der
      Feature-Gruppe zwischen `fraktionen-crud.js` und `shops-core.js`)
    - `CLAUDE.md` §Gotchas Nr. 1 (ARCH-01: Registrierung ausschließlich in `loader.js`),
      §„Build System & Deduplication" (`check_duplicate_functions()`), §„const/var Conflict"
    - `ui/actions/wiki-actions.js` und `systems/wiki-links.js` — welche Wiki-Funktionen von außen über
      `window` gerufen werden (diese Namen müssen exportiert bleiben)
  </read_first>
  <action>
    Lege `features/wiki/wiki-crud.js` an, mit einem eigenen `// [SECTION:WIKI_CRUD]`-Kopf im Stil von
    `features/npcs/npc-crud.js` und einem eigenen `EXPORTS FOR GLOBAL ACCESS`-Block am Dateiende.
    Verschiebe als Tracer GENAU EINE Sektion: `WIKI UX IMPROVEMENTS` — vom Banner bis unmittelbar vor
    den `EXPORTS FOR GLOBAL ACCESS`-Block. Das sind die Funktionen `addToWikiRecentlyViewed`,
    `getWikiBreadcrumb`, `renderWikiBreadcrumb`, `getSearchContextPreview`, `renderWikiSearchDropdown`,
    `handleWikiSearchKeydown`, `updateSearchDropdownSelection`, `extractWikiTOC`, `renderWikiTOC`,
    `addTOCAnchors`, `scrollToTOCHeading`, `renderWikiTemplateSelector`, `applyWikiTemplate`,
    `showWikiLinkSuggester`, `hideWikiLinkSuggester`, `insertWikiLinkSuggestion`,
    `handleWikiContentInput`.
    **Verschieben heißt verschieben, nicht kopieren.** Bleibt eine Funktion zusätzlich in `wiki.js`
    stehen, bricht `check_duplicate_functions()` den Build vor dem Bündeln ab und nennt beide Dateien.
    Das ist der gewünschte Ausgang (D-06) — nicht umgehen, sondern die Dublette in der Quelle beheben.
    Die zugehörigen `window.`-Exporte aus dem `wiki.js`-Exportblock in den neuen Exportblock von
    `wiki-crud.js` überführen: `renderWikiSearchDropdown`, `handleWikiSearchKeydown`,
    `scrollToTOCHeading`, `applyWikiTemplate`, `insertWikiLinkSuggestion`, `handleWikiContentInput`.
    Nur diese — die übrigen gehören noch zu Sektionen, die in `wiki.js` verbleiben.
    Keine `import`/`export`-Anweisungen (Non-ESM). Kein `var X = window.X` für `const`/`let`-deklarierte
    Globals: `WikiState`, `WIKI_CATEGORIES` und `WIKI_TEMPLATES` bleiben in `wiki.js` und sind in
    `wiki-crud.js` direkt als bare Bezeichner lesbar, weil beide Dateien sich denselben globalen
    lexikalischen Geltungsbereich teilen — im Bündel ohnehin, im Loader-Modus, weil `wiki.js` vorher
    ausgewertet wird.
    `'features/wiki/wiki-crud.js'` in `loader.js` `MODULES` eintragen, unmittelbar HINTER
    `'features/wiki/wiki.js'`. Nichts in `build.py` eintragen.
    Danach bauen und die Wiki-Spezifikation laufen lassen.
  </action>
  <verify>
    <automated>PYTHONIOENCODING=utf-8 python build.py</automated>
    <fails_when>Exit-Code ungleich 0, oder die Ausgabe enthält `[FEHLER]` (dann steht eine Funktion in
    zwei Quelldateien oder eine gelistete Datei fehlt), oder die Ausgabe enthält `[ABORTED]`</fails_when>
    <automated>node --check features/wiki/wiki.js &amp;&amp; node --check features/wiki/wiki-crud.js</automated>
    <fails_when>Exit-Code ungleich 0 oder eine SyntaxError-Meldung auf stderr</fails_when>
    <automated>npx playwright test tests/e2e/features/wiki.spec.js</automated>
    <fails_when>Exit-Code ungleich 0, oder die Zusammenfassung meldet mindestens ein `failed`, oder sie
    meldet `0 passed`</fails_when>
  </verify>
  <acceptance_criteria>
    - `features/wiki/wiki-crud.js` existiert und beginnt mit einem `// [SECTION:` -Kopf
    - `grep -c "features/wiki/wiki-crud.js" loader.js` liefert `1`
    - `grep -c "features/wiki" build.py` liefert `0` — in `build.py` wurde nichts eingetragen
    - `grep -c "^function addToWikiRecentlyViewed" features/wiki/wiki.js` liefert `0` und
      `grep -c "^function addToWikiRecentlyViewed" features/wiki/wiki-crud.js` liefert `1`
    - `grep -c "^import \|^export " features/wiki/wiki-crud.js` liefert `0`
    - `PYTHONIOENCODING=utf-8 python build.py` endet mit Exit-Code 0
    - Verhalten: `npx playwright test tests/e2e/features/wiki.spec.js` läuft grün gegen das frisch
      gebaute Bündel — Baum, Detailansicht, Suche, Vorlagen und Link-Vorschläge funktionieren
      unverändert
  </acceptance_criteria>
  <done>Eine Sektion ist verschoben, registriert, gebaut und durch die Wiki-Spezifikation abgesichert — das Verfahren steht.</done>
</task>

<task type="auto">
  <name>Task 2: Zweite Sektion verschieben und beide Dateien unter die Grenze bringen</name>
  <files>features/wiki/wiki.js, features/wiki/wiki-crud.js</files>
  <read_first>
    - `features/wiki/wiki.js` und `features/wiki/wiki-crud.js` (Stand nach Task 1)
    - Der `WIKI CRUD`-Bannerblock in `wiki.js` mit den Funktionen `selectWikiEntry`,
      `toggleWikiCategory`, `expandAllWikiCategories`, `collapseAllWikiCategories`, `showWikiForm`,
      `hideWikiForm`, `updateWikiParentSelect`, `initWikiCategoryListener`, `initWikiSearchListener`,
      `isDescendantOf`, `parseWikiLinks`, `extractWikiLinks`, `findBacklinks`, `toggleWikiEntry`,
      `filterWiki`, `sortWiki`, `saveWikiEntry`, `editWikiEntry`, `cancelWikiEdit`, `clearWikiForm`,
      `deleteWikiEntry`, `toggleWikiPin`, `navigateToWikiEntry`, `createWikiFromLink`, `searchWikiTag`,
      `insertWikiLink`
    - Der `EXPORTS FOR GLOBAL ACCESS`-Block in `wiki.js` (welche der 30 Exporte zu welcher Sektion gehören)
  </read_first>
  <action>
    Verschiebe die Sektion `WIKI CRUD` vollständig nach `features/wiki/wiki-crud.js`, oberhalb der in
    Task 1 verschobenen UX-Sektion, sodass die Reihenfolge innerhalb der neuen Datei der bisherigen
    Reihenfolge in `wiki.js` entspricht. Die zugehörigen Exporte wandern mit: `selectWikiEntry`,
    `toggleWikiCategory`, `expandAllWikiCategories`, `collapseAllWikiCategories`, `showWikiForm`,
    `hideWikiForm`, `initWikiCategoryListener`, `initWikiSearchListener`, `parseWikiLinks`,
    `toggleWikiEntry`, `filterWiki`, `sortWiki`, `saveWikiEntry`, `editWikiEntry`, `deleteWikiEntry`,
    `toggleWikiPin`, `navigateToWikiEntry`, `createWikiFromLink`, `searchWikiTag`, `insertWikiLink`.
    In `wiki.js` verbleiben STATE, CONSTANTS, RENDER und deren Exporte: `renderWiki`, `renderWikiTree`.
    Prüfe den Exportblock beider Dateien gegen den ursprünglichen: die Vereinigungsmenge muss identisch
    sein. Ein verlorener Export macht einen `data-action`-Knopf still wirkungslos — das ist die
    typische Verhaltensänderung, die eine Aufteilung einschleppt.
    Lauf nach der Verschiebung sofort `python build.py`, nicht erst am Ende: `check_duplicate_functions()`
    soll eine vergessene Dublette nach EINER Verschiebung melden, nicht nach mehreren.
    Prüfe danach die Zeilenzahl beider Dateien. Liegt eine über 800, teile die betroffene
    Verantwortlichkeit entlang einer Unter-Sektion weiter auf (D-02, keine Ausnahme von D-01) und
    registriere die dritte Datei ebenfalls in `loader.js`. Erwartung nach den Sektionsgrößen:
    `wiki.js` bei rund 570, `wiki-crud.js` bei rund 680 Zeilen — beide unter der Grenze, eine dritte
    Datei also voraussichtlich nicht nötig.
  </action>
  <verify>
    <automated>PYTHONIOENCODING=utf-8 python build.py &amp;&amp; node --check features/wiki/wiki.js &amp;&amp; node --check features/wiki/wiki-crud.js</automated>
    <fails_when>Exit-Code ungleich 0, oder die Build-Ausgabe enthält `[FEHLER]`/`[ABORTED]`, oder eine
    SyntaxError-Meldung erscheint auf stderr</fails_when>
    <automated>test "$(wc -l &lt; features/wiki/wiki.js)" -le 800 &amp;&amp; test "$(wc -l &lt; features/wiki/wiki-crud.js)" -le 800</automated>
    <fails_when>Exit-Code ungleich 0 — dann überschreitet mindestens eine der beiden Dateien die
    800-Zeilen-Grenze aus D-01</fails_when>
    <automated>npx playwright test tests/e2e/features/wiki.spec.js tests/e2e/tab-navigation.spec.js</automated>
    <fails_when>Exit-Code ungleich 0, oder die Zusammenfassung meldet mindestens ein `failed`</fails_when>
  </verify>
  <acceptance_criteria>
    - `wc -l < features/wiki/wiki.js` liefert einen Wert ≤ 800
    - `wc -l < features/wiki/wiki-crud.js` liefert einen Wert ≤ 800
    - `grep -c "^window\." features/wiki/wiki.js` plus `grep -c "^window\." features/wiki/wiki-crud.js`
      ergibt zusammen `30` — dieselbe Exportmenge wie vor der Aufteilung
    - `grep -c "^function parseWikiLinks" features/wiki/wiki.js` liefert `0`
    - `PYTHONIOENCODING=utf-8 python build.py` endet mit Exit-Code 0
    - Verhalten: Wiki-Baum, Detailansicht mit Backlinks und Inhaltsverzeichnis, Suche mit
      Vorschlagsliste und Kategoriefilter funktionieren unverändert
  </acceptance_criteria>
  <done>Beide Dateien liegen unter 800 Zeilen, die Exportmenge ist vollständig, der Build assembliert.</done>
</task>

<task type="auto">
  <name>Task 3: Hard-Gate — volle Suiten, beide Bündel, Typprüfung vor dem Commit</name>
  <files>features/wiki/wiki.js, features/wiki/wiki-crud.js, loader.js</files>
  <read_first>
    - `features/wiki/wiki.js` und `features/wiki/wiki-crud.js` (Endstand)
    - `loader.js` (`MODULES`-Array mit beiden Einträgen in der richtigen Reihenfolge)
    - `.planning/ROADMAP.md` §Phase 13 → „Auslegungshinweis zum Zuschnitt" (eigener Plan je Datei,
      voller Suiten-Lauf als Hard-Gate vor dem Commit)
    - `.planning/STATE.md` (die Baseline-Zahlen aus Phase 12: Jest 908/908 über 31 Suiten, Playwright
      321 passed / 2 skipped, `pytest tests/build` 24/24 — diese Phase erhöht die Jest-Zahlen durch
      neue Testdateien, senken darf sie keine)
  </read_first>
  <action>
    Führe den vollständigen Gate-Lauf aus, BEVOR die Aufteilung committet wird. Der Roadmap-Hinweis
    verlangt ihn ausdrücklich für jede der vier MAINT-01-Aufteilungen, weil dieser Posten der
    riskanteste des Milestones ist und kein eigenes Phasen-Gate hat.
    Der Lauf umfasst: die volle Jest-Suite, die Typprüfung, beide `dist`-Bündel (Entwicklung und
    Produktion), die Python-Build-Tests und die volle Playwright-Suite gegen das frisch gebaute
    Entwicklungs-Bündel. Playwright läuft gegen `dist/dnd-tracker-bundled.html`; `npm run build`
    schreibt nur das Produktions-Bündel, deshalb muss `python build.py` (ohne Schalter) unmittelbar
    davor laufen.
    Lege die Ausgabe jedes Laufs im SUMMARY vor — Zahl der bestandenen Tests je Suite, Größe beider
    Bündel, Zeilenzahl beider Wiki-Dateien. Eine Behauptung ohne vorgelegte Ausgabe erfüllt das Gate
    nicht.
    Prüfe zusätzlich, dass die Typprüfung keine neuen Fehler meldet: `features/wiki/` hat heute keine
    `.d.ts`-Begleitdatei, die neue Datei braucht also keine. Falls `npx tsc --noEmit` doch etwas
    meldet, ist das ein Befund dieser Aufteilung und wird behoben, nicht ignoriert.
    Erst wenn alle Läufe grün sind, wird committet.
  </action>
  <verify>
    <automated>npx jest</automated>
    <fails_when>Exit-Code ungleich 0, oder die Zusammenfassung meldet mindestens ein `failed`, oder die
    Zahl bestandener Tests liegt unter 908</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>Exit-Code ungleich 0 oder die Ausgabe enthält `error TS`</fails_when>
    <automated>PYTHONIOENCODING=utf-8 python build.py &amp;&amp; PYTHONIOENCODING=utf-8 python build.py --production &amp;&amp; python -m pytest tests/build -q</automated>
    <fails_when>Exit-Code ungleich 0, oder eine Build-Ausgabe enthält `[FEHLER]`/`[ABORTED]`, oder die
    pytest-Zusammenfassung meldet mindestens ein `failed`</fails_when>
    <automated>PYTHONIOENCODING=utf-8 python build.py &amp;&amp; npx playwright test</automated>
    <fails_when>Exit-Code ungleich 0, oder die Zusammenfassung meldet mindestens ein `failed`, oder die
    Zahl bestandener Tests liegt unter 321</fails_when>
  </verify>
  <acceptance_criteria>
    - `npx jest` grün mit mindestens 908 bestandenen Tests
    - `npx tsc --noEmit` endet mit Exit-Code 0 ohne `error TS`
    - `npx playwright test` grün mit mindestens 321 bestandenen Tests
    - `python -m pytest tests/build -q` grün mit 24 Tests
    - Beide `dist`-Dateien werden neu geschrieben; keine der beiden Build-Ausgaben enthält `[FEHLER]`
      oder `[ABORTED]`
    - Das SUMMARY nennt für jede Suite die tatsächlich gemessene Zahl, nicht die erwartete
  </acceptance_criteria>
  <done>Das Hard-Gate ist mit vorgelegter Ausgabe bestanden; die Aufteilung ist verhaltensneutral belegt und darf committet werden.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wiki-Eintragsinhalt (Nutzertext) → Render-Pipeline → DOM | Die Aufteilung darf die Reihenfolge `sanitizeHTML()` → `addTOCAnchors()` → `parseWikiLinks()` nicht verändern. `parseWikiLinks()` wandert in die neue Datei, `renderWikiDetail()` bleibt in der alten — der Aufruf läuft zur Laufzeit und ist damit unabhängig von der Dateigrenze. |
| `loader.js` `MODULES` → `build.py` → Bündel | Eine fehlende oder falsch eingeordnete Registrierung erzeugt entweder einen abgebrochenen Build oder ein Bündel, in dem eine `const`-Deklaration nach ihrer ersten Verwendung ausgewertet wird. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-34 | Tampering | Verlorene Escaping-/Sanitisierungsreihenfolge beim Verschieben | high | mitigate | `parseWikiLinks()` wird unverändert verschoben (Plan 13-02 hat es bereits gehärtet); die Aufrufreihenfolge in `renderWikiDetail()` bleibt Zeile für Zeile identisch. Abgesichert durch `tests/unit/wiki-links.test.js` und `tests/e2e/features/wiki.spec.js` im Hard-Gate. |
| T-13-35 | Denial of Service | Verlorener `window.`-Export beim Verschieben | high | mitigate | Die Vereinigungsmenge der Exporte beider Dateien wird gegen die ursprünglichen 30 abgeglichen. Ein verlorener Export macht einen `data-action`-Knopf still wirkungslos — die typische Verhaltensänderung einer Aufteilung. |
| T-13-36 | Tampering | Funktion bleibt in beiden Dateien stehen | medium | mitigate | `check_duplicate_functions()` bricht den Build vor dem Bündeln ab und nennt beide Dateien. `python build.py` läuft nach JEDER Verschiebung, nicht erst am Ende (D-06, RESEARCH Pitfall 4). |
| T-13-37 | Tampering | Zweite Modulliste in `build.py` | medium | mitigate | In `build.py` wird nichts eingetragen; die Abnahme prüft das explizit (`grep -c "features/wiki" build.py` liefert `0`). Die Doppelliste war der Defekt, den ARCH-01 in Phase 11 strukturell beseitigt hat. |
| T-13-38 | Repudiation | Behauptetes statt gelaufenes Suiten-Gate | high | mitigate | Als Verbot in `must_haves.prohibitions` festgehalten; das SUMMARY muss je Suite die gemessene Zahl nennen. |
| T-13-SC | Tampering | npm/pip/cargo installs | low | accept | Dieser Plan installiert kein Paket (`13-RESEARCH.md` → „Package Legitimacy Audit: Nicht anwendbar"). |
</threat_model>

<verification>
1. `wc -l < features/wiki/wiki.js` und `wc -l < features/wiki/wiki-crud.js` liefern beide ≤ 800
2. `grep -c "features/wiki/wiki-crud.js" loader.js` liefert `1`; `grep -c "features/wiki" build.py` liefert `0`
3. Exportsumme beider Dateien = 30 (unverändert)
4. `npx jest` grün, ≥ 908 Tests
5. `npx tsc --noEmit` ohne `error TS`
6. `PYTHONIOENCODING=utf-8 python build.py && PYTHONIOENCODING=utf-8 python build.py --production && python -m pytest tests/build -q` grün
7. `PYTHONIOENCODING=utf-8 python build.py && npx playwright test` grün, ≥ 321 Tests
</verification>

<success_criteria>
- Erfolgskriterium 4, erstes Viertel: `features/wiki/wiki.js` ist entlang seiner Sektionsbanner
  aufgeteilt, keine Nachfolgedatei überschreitet 800 Zeilen, das Verhalten ist unverändert
- Registrierung ausschließlich in `loader.js`, in Abhängigkeitsreihenfolge
- Volles Suiten-Gate mit vorgelegter Ausgabe vor dem Commit
</success_criteria>

<output>
Create `.planning/phases/13-h-rtung-wartbarkeit/13-09-SUMMARY.md` when done
</output>
