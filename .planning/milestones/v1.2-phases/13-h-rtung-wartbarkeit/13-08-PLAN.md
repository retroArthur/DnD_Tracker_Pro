---
phase: 13-h-rtung-wartbarkeit
plan: 08
type: execute
wave: 3
depends_on: ["13-01", "13-02", "13-03", "13-04", "13-06", "13-07"]
files_modified:
  - render/helpers.js
  - ui/event-delegation.js
  - ui/safe-render.js
  - ui/editors/markdown-shortcuts.js
  - utils/basic.js
  - utils/performance.js
  - utils/utilities.js
  - systems/undo.js
  - systems/backups.js
  - systems/tab-registry.js
  - systems/campaign-manager/campaign-manager.js
  - systems/spellslots/import-export.js
  - systems/spellslots/quick-roll.js
  - systems/spellslots/version-migration.js
  - systems/file-backup/file-backup-manager.js
  - features/initiative.js
  - features/wiki/wiki.js
  - features/random-tables.js
  - features/bestiary/bestiary-render.js
  - features/bestiary/bestiary-editor.js
  - features/session-prep/session-prep-render.js
  - features/timeline/timeline-render.js
  - features/reise/reise-render.js
  - features/fraktionen/fraktionen-render.js
  - features/shops/shops-core.js
  - features/shops/shop-export.js
  - features/soundboard/soundboard-player.js
  - features/soundboard/soundboard-render.js
  - features/dice-stats/dice-stats-render.js
  - core/init.js
  - tools/debug.js
  - tests/unit/console-hygiene.test.js
  - tests/unit/file-backup.test.js
autonomous: true
requirements: [MAINT-06]

estimate:
  tokens: 35000
  raw_tokens: 70000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Kein in `loader.js` `MODULES` gelistetes Modul schreibt mehr ungefiltert auf die Konsole. Es bleibt genau EIN sanktionierter Ausgang: die Ausgabe innerhalb von `ErrorHandler.log()` in `render/helpers.js`, markiert mit dem Kommentar-Kennzeichen `gsd:konsolen-senke`."
    - "Die Diagnosefähigkeit bleibt erhalten: die Aufrufe werden nicht gelöscht, sondern auf `ErrorHandler.log()` hinter `APP_CONFIG.DEBUG_MODE` umgestellt — Löschen nähme die Diagnose, die beim Debuggen am Spieltisch gebraucht wird."
    - "`build.py` strippt Konsolenaufrufe nicht; der Guard sitzt deshalb im Quellcode, nicht im Build (das wäre eine Build-System-Änderung mit eigenem TDD-Aufwand und ist in `13-CONTEXT.md` ausdrücklich zurückgestellt)."
    - "`loader.js` selbst und alles unter `tests/`, `dist/`, `node_modules/` liegen außerhalb des Geltungsbereichs: `loader.js` ist der Entwicklungs-Bootstrap und landet in keinem `dist`-Bündel (belegt: das gebündelte HTML enthält seine Boot-Meldungen nicht)."
    - "Die Kopfkommentare in `systems/file-backup/file-backup-manager.js` (Zeile 6 und Zeile 674 — RESEARCH-Korrektur gegenüber `13-CONTEXT.md`, das fälschlich 387 nennt) beschreiben `registerPostSaveHook()` statt des verbotenen Monkey-Patch-Musters."
    - "Die bereits korrekten Warnkommentare bei `file-backup-manager.js:681-682`, die das verbotene Muster ausdrücklich als verboten benennen, bleiben unangetastet — sie sind die Erklärung, nicht der Fehler."
    - "MAINT-06 · Randfall `idempotency`: Ein zweiter Durchlauf der Umstellung ändert nichts mehr — die Zählung der ungefilterten Konsolenaufrufe bleibt bei 0, und ein erneuter Lauf erzeugt keinen Diff."
    - statement: "Ein abgebrochener Teilstand dieser Umstellung lässt Build und volle Suite grün, weil jede einzelne Aufrufstelle unabhängig von den anderen umgestellt wird und keine Umstellung eine andere voraussetzt."
      verification: backstop
  prohibitions:
    - "Kein Diagnose- oder Datenverlust ohne sichtbares Signal. Diese Umstellung darf keinen Konsolenaufruf ersatzlos löschen, dessen Information am Spieltisch beim Debuggen gebraucht wird — sie wird hinter den Guard verlegt, nicht entsorgt. Wo eine Ausgabe tatsächlich wertlos ist, wird die Löschung im SUMMARY einzeln benannt und begründet, nie stillschweigend im Sammel-Diff versteckt. Dasselbe gilt sinngemäß für die anderen beiden Stellen dieser Phase, an denen dem Spielleiter etwas weggenommen werden könnte: den Würfelstatistik-Deckel (Plan 13-07) und das Undo-Byte-Budget (Plan 13-06)."
  artifacts:
    - tests/unit/console-hygiene.test.js
    - render/helpers.js
    - systems/file-backup/file-backup-manager.js
  key_links:
    - "`loader.js` `MODULES` (124 Einträge) definiert den Geltungsbereich — dieselbe Liste, die `build.py` liest (ARCH-01, Phase 11). Der Prüftest leitet seinen Dateisatz daraus ab, nicht aus einer eigenen Liste."
    - "`render/helpers.js` `ErrorHandler.log()` ist der einzige sanktionierte Ausgang; alle umgestellten Aufrufstellen laufen darüber"
    - "`systems/spellslots/persistence.js:14-18` (`registerPostSaveHook`) ist die reale Implementierung, die die korrigierten Kopfkommentare beschreiben müssen; `file-backup-manager.js:685-686` ruft sie bereits korrekt auf"
---

<objective>
Der Quellbaum enthält 81 ungefilterte Konsolenaufrufe in 31 der 124 in `loader.js` gelisteten Module —
im Widerspruch zur CLAUDE.md-Zusicherung „Zero console.log in production", die dieses Projekt seit dem
Januar-2026-Refactoring führt. Zusätzlich behaupten zwei Kopfkommentare im Datei-Backup weiterhin das
explizit verbotene `window.save`-Monkey-Patch-Muster, obwohl der Code seit Phase 12 korrekt
`registerPostSaveHook()` benutzt.

Purpose: Erfolgskriterium 8 der Phase — „Produktionspfade schreiben nichts mehr ungefiltert auf die
Konsole; die Kopfkommentare im Datei-Backup beschreiben `registerPostSaveHook()` statt des verbotenen
`window.save`-Musters." Ein Kommentar, der ein verbotenes Muster als Vorgehen beschreibt, ist eine
Anleitung zum Wiedereinbau — genau die irreführende Stelle, die diese Phase beseitigt.
Output: 31 umgestellte Module, ein markierter sanktionierter Ausgang, ein Prüftest, zwei korrigierte
Kopfkommentare.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-h-rtung-wartbarkeit/13-CONTEXT.md
@.planning/phases/13-h-rtung-wartbarkeit/13-RESEARCH.md
@.planning/phases/13-h-rtung-wartbarkeit/13-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Dieser Plan erzeugt:

| Symbol / Datei | Art | Ort |
|---|---|---|
| `gsd:konsolen-senke` | Kommentar-Kennzeichen am einzigen sanktionierten Ausgang | `render/helpers.js` |
| `tests/unit/console-hygiene.test.js` | neue Jest-Testdatei (leitet den Dateisatz aus `loader.js` `MODULES` ab) | `tests/unit/` |

Kein neues Quellmodul, kein neuer `loader.js`-`MODULES`-Eintrag. Die Datei- und Zahlangaben oben sind
der Stand zum Planungszeitpunkt (81 Aufrufstellen in 31 Modulen); Plan 13-01 senkt sie um eine,
Plan 13-04 kann weitere verschoben haben. **Leite die Liste zur Ausführungszeit frisch ab** statt sie
aus diesem Dokument zu übernehmen.

<tasks>

<task type="tracer">
  <name>Task 1: Sanktionierten Ausgang markieren, ein Modul vollständig umstellen, Prüftest bauen</name>
  <files>render/helpers.js, ui/event-delegation.js, tests/unit/console-hygiene.test.js</files>
  <read_first>
    - `render/helpers.js` Zeilen 1–100 (`ErrorHandler` mit `_errorLog`, `_maxErrors`, `_consoleLog`,
      `_showToast`, `_debugLog`; `log(fnName, error, context)` schreibt bei gesetztem `_consoleLog` in
      die Konsole und zusätzlich ins Debug-Log; darunter `showError()` mit Toast-Debouncing)
    - `render/helpers.js` Zeilen 80–95 und 230–240 und 318–330 (die drei weiteren Ausgaben dieser
      Datei: im `onError`-Rückruf, beim fehlenden globalen Datenobjekt, bei durchgeführten
      Datenreparaturen — sie sind KEIN sanktionierter Ausgang und werden umgestellt)
    - `ui/event-delegation.js` (7 Aufrufstellen — das Tracer-Modul; sieh dir an, welche davon
      Diagnose im Fehlerfall sind und welche reine Ablaufprotokollierung)
    - `systems/undo.js` Zeilen 18–22 (die produktive Vorlage für den Guard-Aufbau
      `window.APP_CONFIG?.DEBUG_MODE && window.ErrorHandler` mit `ErrorHandler.log(kontext, fehler, zusatz)`)
    - `loader.js` Zeilen 1–175 (`MODULES`-Array — der Geltungsbereich; `loader.js` selbst steht NICHT
      in seiner eigenen Liste und ist damit außerhalb)
    - `CLAUDE.md` §„Phase 1: Quick Wins" → „Problem 1: Production Console Pollution" (das
      Projektmuster, das hier vervollständigt wird)
  </read_first>
  <action>
    Schritt 1 — den einen sanktionierten Ausgang markieren: in `render/helpers.js` schreibt
    `ErrorHandler.log()` die Fehlermeldung selbst aus. Das ist der bewusste Fehlerkanal des Projekts
    und bleibt bestehen. Hänge an genau diese eine Zeile einen Zeilenendkommentar mit dem Kennzeichen
    `gsd:konsolen-senke` und einer kurzen Begründung. Das Kennzeichen ist der Anker, an dem der
    Prüftest diese eine Stelle vom Rest unterscheidet — es muss auf DERSELBEN Zeile stehen wie die
    Ausgabe.
    Schritt 2 — Prüftest bauen: `tests/unit/console-hygiene.test.js` liest `loader.js`, extrahiert
    das `MODULES`-Array und prüft für jede dieser Dateien, dass sie keine ungefilterte
    Konsolenausgabe mehr enthält. Ausgenommen ist ausschließlich, was das Kennzeichen aus Schritt 1 auf
    derselben Zeile trägt. Der Test meldet im Fehlerfall Datei und Zeilennummer jedes Treffers, damit
    die Abarbeitung in Task 2 daran entlanglaufen kann. Findet die Extraktion null Module, muss der
    Test hart fehlschlagen — ein leer-grüner Prüftest wäre schlimmer als keiner.
    Wichtig für den Test selbst: er darf die gesuchten Methodennamen nur EINMAL enthalten, nämlich im
    Suchmuster. Formuliere seine eigenen Kommentare so, dass sie die Namen nicht wiederholen — sonst
    prüft der Test irgendwann sich selbst. `tests/` liegt ohnehin außerhalb des Geltungsbereichs, aber
    die Regel hält den Test lesbar.
    Schritt 3 — `ui/event-delegation.js` vollständig umstellen (7 Stellen) als Tracer für das
    Verfahren: jede Ausgabe wird zu `window.ErrorHandler.log(kontext, fehler, zusatz)` hinter dem
    Guard `window.APP_CONFIG?.DEBUG_MODE && window.ErrorHandler`, wobei `kontext` der Modul-/
    Funktionsname ist. Ist an einer Stelle kein Fehlerobjekt vorhanden, konstruiere eines mit der
    bisherigen Meldung als Text — `ErrorHandler.log()` erwartet ein Fehlerobjekt und normalisiert sonst.
    Kein `const X = window.X` innerhalb einer Funktion (CLAUDE.md „Duplicate Declaration Debugging
    Pattern"). Keine Ausgabe ersatzlos löschen: die Information wandert hinter den Guard, sie
    verschwindet nicht.
    Schritt 4 — die drei übrigen Ausgaben in `render/helpers.js` (außerhalb von `ErrorHandler.log()`)
    ebenfalls umstellen. Die Meldung über ein fehlendes globales Datenobjekt ist ein echter Fatalfall
    und darf ohne `DEBUG_MODE`-Guard direkt über `ErrorHandler.log()` laufen — sie geht dann durch den
    einen sanktionierten Ausgang und ist damit nicht mehr „ungefiltert". Benenne diese Ausnahme im
    SUMMARY.
    Setze in KEINER geänderten Datei einen Kommentar, der die gesuchten Methodennamen des
    Konsolenobjekts enthält — der Prüftest sucht per Textmuster und würde an einem erklärenden
    Kommentar hängenbleiben.
  </action>
  <verify>
    <automated>npx jest tests/unit/console-hygiene.test.js</automated>
    <fails_when>Exit-Code ungleich 0 ist hier ERWARTET, solange Task 2 nicht gelaufen ist. Fehlerhaft
    ist stattdessen: die Ausgabe listet null geprüfte Module, ODER sie listet `ui/event-delegation.js`
    oder `render/helpers.js` noch als Treffer, ODER der Lauf endet mit „0 passed"</fails_when>
    <automated>node --check render/helpers.js &amp;&amp; node --check ui/event-delegation.js</automated>
    <fails_when>Exit-Code ungleich 0 oder eine SyntaxError-Meldung auf stderr</fails_when>
    <automated>PYTHONIOENCODING=utf-8 python build.py &amp;&amp; npx playwright test tests/e2e/smoke.spec.js</automated>
    <fails_when>Exit-Code ungleich 0, oder die Build-Ausgabe enthält `[FEHLER]`/`[ABORTED]`, oder die
    Playwright-Zusammenfassung meldet mindestens ein `failed`</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'gsd:konsolen-senke' render/helpers.js` liefert `1`
    - `tests/unit/console-hygiene.test.js` existiert, liest `loader.js` und filtert über das Kennzeichen
    - Der Prüftest meldet für `ui/event-delegation.js` und `render/helpers.js` null Treffer
    - `grep -c 'ErrorHandler' ui/event-delegation.js` liefert mindestens `7`
    - Verhalten: `PYTHONIOENCODING=utf-8 python build.py && npx playwright test tests/e2e/smoke.spec.js`
      läuft grün — die Aktionsschicht funktioniert unverändert
  </acceptance_criteria>
  <done>Der eine sanktionierte Ausgang ist markiert, der Prüftest steht, und ein vollständiges Modul beweist das Verfahren.</done>
</task>

<task type="auto">
  <name>Task 2: Die verbleibenden Module umstellen, bis der Prüftest grün ist</name>
  <files>utils/basic.js, utils/performance.js, utils/utilities.js, systems/undo.js, systems/backups.js, systems/tab-registry.js, systems/campaign-manager/campaign-manager.js, systems/spellslots/import-export.js, systems/spellslots/quick-roll.js, systems/spellslots/version-migration.js, features/initiative.js, features/wiki/wiki.js, features/random-tables.js, features/bestiary/bestiary-render.js, features/bestiary/bestiary-editor.js, features/session-prep/session-prep-render.js, features/timeline/timeline-render.js, features/reise/reise-render.js, features/fraktionen/fraktionen-render.js, features/shops/shops-core.js, features/shops/shop-export.js, features/soundboard/soundboard-player.js, features/soundboard/soundboard-render.js, features/dice-stats/dice-stats-render.js, ui/safe-render.js, ui/editors/markdown-shortcuts.js, tools/debug.js, core/init.js</files>
  <read_first>
    - Die Trefferliste aus dem Prüftest von Task 1 (Datei + Zeilennummer je Stelle) — sie ist die
      Arbeitsliste, nicht die Aufzählung in diesem Plan
    - `ui/event-delegation.js` (Stand nach Task 1) als Vorlage für die Umstellungsform
    - `systems/spellslots/import-export.js` (9 Stellen — das Modul mit den meisten Treffern nach
      `systems/tab-registry.js`)
    - `systems/tab-registry.js` (12 Stellen; **Achtung:** die Datei wurde in Plan 13-04 umgebaut — die
      Zeilennummern aus der Recherche gelten dort nicht mehr, arbeite ausschließlich mit der frischen
      Trefferliste)
    - `utils/basic.js` (8 Stellen; **Achtung:** die Datei wurde in Plan 13-06 um `utf8ByteLength()`
      erweitert)
    - `tools/debug.js` (3 Stellen — die Datei steht in `loader.js` `MODULES` und ist damit Teil des
      Bündels, also im Geltungsbereich, auch wenn sie ein Entwicklerwerkzeug ist)
    - `core/init.js` Zeilen 1–25 (die globalen Fehler-Handler `window.onerror` und
      `unhandledRejection` — sie rufen `ErrorHandler.log()` bereits korrekt auf und sind die Vorlage
      für Fatalfälle ohne Guard)
  </read_first>
  <action>
    Arbeite die Trefferliste des Prüftests ab, Modul für Modul, nach demselben Verfahren wie in Task 1:
    jede Ausgabe wird zu `ErrorHandler.log(kontext, fehler, zusatz)` hinter dem
    `APP_CONFIG.DEBUG_MODE`-Guard. Drei Ausnahmen, die ohne Guard direkt über `ErrorHandler.log()`
    laufen dürfen, weil die Ausgabe dann ohnehin durch den einen sanktionierten Ausgang geht:
    (a) Fatalfälle beim Start (fehlendes globales Datenobjekt, gescheiterte Initialisierung),
    (b) Fehler in den globalen Handlern aus `core/init.js`,
    (c) Fehlermeldungen, deren Ausbleiben einen stillen Datenverlust verdecken würde (Import/Export,
    Migration, Backup). Jede genutzte Ausnahme wird im SUMMARY einzeln benannt.
    Löschen statt umstellen ist nur zulässig, wenn eine Ausgabe nachweislich keine Information trägt
    (reine Ablaufmarkierung ohne Wert). Jede Löschung wird im SUMMARY einzeln aufgeführt und begründet
    — nicht im Sammel-Diff versteckt.
    Führe `python build.py` nach jeweils ein paar Modulen aus, nicht erst am Ende: die Umstellung
    berührt 31 Dateien, und ein Syntaxfehler soll nach fünf Dateien auffallen, nicht nach 31.
    Setze in keiner geänderten Datei einen Kommentar, der die gesuchten Methodennamen des
    Konsolenobjekts enthält.
    Halte im SUMMARY fest: Ausgangszahl der Treffer, Zahl der umgestellten Stellen, Zahl der
    gelöschten Stellen mit Begründung, Zahl der guard-freien Ausnahmen mit Begründung.
  </action>
  <verify>
    <automated>npx jest tests/unit/console-hygiene.test.js</automated>
    <fails_when>Exit-Code ungleich 0, oder die Ausgabe listet mindestens eine Datei mit einem
    verbliebenen Treffer, oder sie meldet null geprüfte Module</fails_when>
    <automated>npx jest</automated>
    <fails_when>Exit-Code ungleich 0, oder die Zusammenfassung meldet mindestens ein `failed`</fails_when>
    <automated>PYTHONIOENCODING=utf-8 python build.py &amp;&amp; PYTHONIOENCODING=utf-8 python build.py --production &amp;&amp; python -m pytest tests/build -q</automated>
    <fails_when>Exit-Code ungleich 0, oder eine der Build-Ausgaben enthält `[FEHLER]`/`[ABORTED]`, oder
    die pytest-Zusammenfassung meldet mindestens ein `failed`</fails_when>
    <automated>npx playwright test</automated>
    <fails_when>Exit-Code ungleich 0, oder die Zusammenfassung meldet mindestens ein `failed`, oder die
    Zahl bestandener Tests liegt unter 321</fails_when>
  </verify>
  <acceptance_criteria>
    - `npx jest tests/unit/console-hygiene.test.js` endet mit Exit-Code 0
    - Der Prüftest berichtet, dass er mindestens 120 Module aus `loader.js` geprüft hat
    - `grep -c 'gsd:konsolen-senke' render/helpers.js` liefert weiterhin `1` — es gibt genau einen
      sanktionierten Ausgang
    - Verhalten: die volle Playwright-Suite läuft grün (Baseline aus STATE.md: 321 passed / 2 skipped)
    - `npx jest` läuft vollständig grün
    - `python -m pytest tests/build -q` läuft grün (Baseline: 24 Tests)
    - Das SUMMARY beziffert Ausgangs-, Umstellungs-, Löschungs- und Ausnahmezahl
  </acceptance_criteria>
  <done>Kein gebündeltes Modul schreibt mehr ungefiltert auf die Konsole; ein Prüftest hält den Zustand fest.</done>
</task>

<task type="auto">
  <name>Task 3: Irreführende Kopfkommentare im Datei-Backup nachziehen</name>
  <files>systems/file-backup/file-backup-manager.js, tests/unit/file-backup.test.js</files>
  <read_first>
    - `systems/file-backup/file-backup-manager.js` Zeilen 1–15 (der Dateikopf; Zeile 6 beschreibt
      `initFileBackup()` mit einem Live-Sync-Muster, das sich in die globale Speicherfunktion einhängt)
    - `systems/file-backup/file-backup-manager.js` Zeilen 665–695 (die JSDoc über `initFileBackup()`
      mit derselben falschen Aussage auf Zeile 674 — **RESEARCH-Korrektur: 674, nicht 387 wie in
      `13-CONTEXT.md` zitiert; Zeile 387 ist eine unabhängige JSDoc für `pruneOldSnapshots()` ohne
      Bezug zu Save-Hooks**; direkt darunter auf 681–682 die bereits KORREKTE Warnung, warum das
      Monkey-Patch-Muster strukturell wirkungslos ist, und auf 685–686 der korrekte Aufruf von
      `window.registerPostSaveHook(onAfterSave)`)
    - `systems/spellslots/persistence.js` Zeilen 1–30 (`registerPostSaveHook(fn)` und
      `_notifyPostSaveHooks()` — die reale Implementierung, die die korrigierten Kommentare beschreiben
      müssen: Hooks laufen an JEDEM Persist-Erfolgspunkt, nach dem Schreibvorgang, isoliert gegen
      werfende Hooks)
    - `CLAUDE.md` §„Live-Sync Pattern (Post-Save-Hooks)" — die Projektregel, die beide Kommentare
      künftig widerspiegeln müssen
    - `tests/unit/file-backup-hook.test.js` (der bestehende Test des Hook-Verhaltens)
  </read_first>
  <action>
    Zwei Kommentarstellen in `systems/file-backup/file-backup-manager.js` richtigstellen: der Dateikopf
    (Zeile 6) und die JSDoc über `initFileBackup()` (Zeile 674) behaupten beide, das Modul hänge sich in
    die globale Speicherfunktion ein. Das ist das explizit verbotene Monkey-Patch-Muster, und der Code
    tut es nicht — er registriert sich seit Phase 12 korrekt über `registerPostSaveHook()`
    (Aufruf auf Zeile 685–686). Schreibe beide Stellen so um, dass sie die tatsächliche Registrierung
    beschreiben: Registrierung über `registerPostSaveHook()`, Ausführung an jedem Persist-Erfolgspunkt,
    Isolation gegen werfende Hooks, eigene Entprellung im Hook.
    Die bereits korrekten Zeilen 681–682, die das Monkey-Patch-Muster ausdrücklich als verboten
    benennen und begründen (bare Aufrufe binden im Bündel an die lexikalische Deklaration und umgehen
    jeden Wrapper), bleiben unverändert — sie sind die Erklärung, nicht der Fehler.
    Die Abnahmeprüfung sucht nach der Formulierung, die das Einhängen in die globale Speicherfunktion
    behauptet. Diese Formulierung darf nach dem Umschreiben an keiner der beiden Stellen mehr stehen;
    die Warnung auf 681–682 benutzt sie ohnehin nicht.
    Ergänze in `tests/unit/file-backup.test.js` einen Prüffall, der den Quelltext von
    `file-backup-manager.js` liest und sicherstellt, dass (a) die falsche Formulierung nicht vorkommt
    und (b) `registerPostSaveHook` sowohl im Dateikopf als auch in der JSDoc über `initFileBackup()`
    genannt wird. Ein Kommentar ohne Test verrutscht beim nächsten Refactoring wieder.
  </action>
  <verify>
    <automated>test "$(grep -c 'in window.save() ein' systems/file-backup/file-backup-manager.js)" -eq 0</automated>
    <fails_when>Exit-Code ungleich 0 — dann behauptet mindestens eine Kommentarstelle weiterhin das
    verbotene Muster als Vorgehen</fails_when>
    <automated>test "$(sed -n '1,15p' systems/file-backup/file-backup-manager.js | grep -c 'registerPostSaveHook')" -ge 1</automated>
    <fails_when>Exit-Code ungleich 0 — dann nennt der Dateikopf die tatsächliche Registrierung nicht</fails_when>
    <automated>npx jest tests/unit/file-backup.test.js tests/unit/file-backup-hook.test.js tests/unit/file-backup-idb.test.js</automated>
    <fails_when>Exit-Code ungleich 0, oder die Zusammenfassung meldet mindestens ein `failed`, oder der
    neue Prüffall meldet die falsche Formulierung als weiterhin vorhanden</fails_when>
    <automated>npx jest &amp;&amp; PYTHONIOENCODING=utf-8 python build.py</automated>
    <fails_when>Exit-Code ungleich 0, oder die Build-Ausgabe enthält `[FEHLER]`/`[ABORTED]`</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'in window.save() ein' systems/file-backup/file-backup-manager.js` liefert `0`
    - Der Dateikopf (Zeilen 1–15) enthält `registerPostSaveHook`
    - Die JSDoc über `initFileBackup()` enthält `registerPostSaveHook`
    - Die Zeilen mit der Begründung, warum das Monkey-Patch-Muster strukturell wirkungslos ist, sind
      unverändert vorhanden (`grep -c 'Monkey-Patch' systems/file-backup/file-backup-manager.js`
      liefert mindestens `1`)
    - `tests/unit/file-backup.test.js` enthält einen Prüffall, der den Quelltext auf beide Bedingungen
      prüft
    - `npx jest` läuft vollständig grün
  </acceptance_criteria>
  <done>Beide Kopfkommentare beschreiben den Code, der dasteht — und ein Test hält das fest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anwendungszustand (Kampagnendaten, Dateinamen, Schlüssel) → Browser-Konsole | Eine Konsolenausgabe in einem Produktionsbündel legt internen Zustand offen und läuft an jedem Diagnosekanal des Projekts vorbei. |
| Kopfkommentar → nächster Entwickler | Ein Kommentar, der ein verbotenes Muster als Vorgehen beschreibt, ist eine Anleitung zum Wiedereinbau — genau der Defekt, den UAT 02 in Phase 12 aufgedeckt hat. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-29 | Information Disclosure | 81 ungefilterte Konsolenaufrufe in 31 gebündelten Modulen | medium | mitigate | Umstellung auf `ErrorHandler.log()` hinter `APP_CONFIG.DEBUG_MODE`; genau ein sanktionierter Ausgang bleibt, markiert und per Test durchgesetzt. |
| T-13-30 | Repudiation | Irreführende Kopfkommentare (`file-backup-manager.js:6,674`) | high | mitigate | Beide Stellen beschreiben künftig `registerPostSaveHook()`. Das Muster, das sie heute behaupten, ist strukturell wirkungslos (bare Aufrufe binden an die lexikalische Deklaration) — ein Entwickler, der es kopiert, baut eine Live-Sync, die nie feuert. Genau dieser Defekt hat in Phase 12 dazu geführt, dass Datei-Backups nie geschrieben wurden. Durch einen Quelltext-Prüffall abgesichert. |
| T-13-31 | Denial of Service | Verlorene Diagnosefähigkeit durch pauschales Löschen | high | mitigate | Ausgaben werden verlegt, nicht entsorgt; jede tatsächliche Löschung wird im SUMMARY einzeln benannt und begründet. Als Verbot in `must_haves.prohibitions` festgehalten. |
| T-13-32 | Tampering | Selbst-entwertender Prüftest | medium | mitigate | Der Prüftest sucht per Textmuster; ein erklärender Kommentar mit denselben Methodennamen würde ihn entwerten. Die Aufgaben schreiben deshalb ausdrücklich vor, diese Namen in keinem Kommentar zu setzen, und der sanktionierte Ausgang wird über ein eindeutiges Kennzeichen auf derselben Zeile ausgenommen. |
| T-13-33 | Tampering | Geltungsbereich aus einer zweiten Liste statt aus `loader.js` | medium | mitigate | Der Prüftest leitet seinen Dateisatz aus dem `MODULES`-Array ab — dieselbe einzige Modulliste, die `build.py` liest (ARCH-01). Eine zweite, handgepflegte Liste wäre exakt die Drift, die Phase 11 strukturell beseitigt hat. |
| T-13-SC | Tampering | npm/pip/cargo installs | low | accept | Dieser Plan installiert kein Paket (`13-RESEARCH.md` → „Package Legitimacy Audit: Nicht anwendbar"). |
</threat_model>

<verification>
1. `npx jest tests/unit/console-hygiene.test.js` grün, mit mindestens 120 geprüften Modulen
2. `grep -c 'gsd:konsolen-senke' render/helpers.js` liefert `1`
3. `grep -c 'in window.save() ein' systems/file-backup/file-backup-manager.js` liefert `0`
4. `npx jest` vollständig grün
5. `PYTHONIOENCODING=utf-8 python build.py && PYTHONIOENCODING=utf-8 python build.py --production && python -m pytest tests/build -q` grün
6. `npx playwright test` grün, mindestens 321 bestanden
</verification>

<success_criteria>
- Erfolgskriterium 8: kein gebündeltes Modul schreibt mehr ungefiltert auf die Konsole; die
  Kopfkommentare im Datei-Backup beschreiben `registerPostSaveHook()`
- Genau ein sanktionierter Ausgang, markiert und per Test durchgesetzt
- Keine Diagnosefähigkeit verloren: jede Löschung ist im SUMMARY einzeln begründet
- Volle Jest-, Playwright- und Build-Test-Suite grün
</success_criteria>

<output>
Create `.planning/phases/13-h-rtung-wartbarkeit/13-08-SUMMARY.md` when done
</output>
