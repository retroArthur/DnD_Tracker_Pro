---
phase: 13-h-rtung-wartbarkeit
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/dmscreen-characterization.test.js
  - tests/unit/__snapshots__/dmscreen-characterization.test.js.snap
autonomous: true
requirements: [MAINT-01]

estimate:
  tokens: 22500
  raw_tokens: 45000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "`tests/unit/dmscreen-characterization.test.js` existiert, ist grün gegen das UNGETEILTE `features/dmscreen/dmscreen-render.js` und hält die HTML-Ausgabe aller 21 Widget-Typen als Snapshot fest (D-04)."
    - "Der Test lädt seine Modulliste aus dem `MODULES`-Array von `loader.js` (alle Einträge unter `features/dmscreen/`) statt einen festen Dateipfad zu nennen — dadurch läuft er nach der Aufteilung in Plan 13-12 unverändert weiter."
    - "Der Test greift ausschließlich auf die öffentliche Oberfläche zu (`getDMScreenWidgets()` als Registry, `window.renderDMScreen` als Einstieg) und niemals auf einzelne Widget-Renderer über deren internen Funktionsnamen."
    - "Der Snapshot ist deterministisch: gleiche `D`-Fixture, gleiche Stubs, gleiche Ausgabe — keine Zeitstempel, keine Zufallszahlen, keine Abhängigkeit von der Systemzeit."
    - "Die Snapshot-Datei `tests/unit/__snapshots__/dmscreen-characterization.test.js.snap` ist eingecheckt; spätere Läufe prüfen mit `--ci`, sodass ein fehlender oder abweichender Snapshot rot wird statt still neu geschrieben zu werden."
  prohibitions:
    - "Der Charakterisierungs-Snapshot darf nie an eine neue Ausgabe angepasst werden. Er wird gegen das ungeteilte Modul erzeugt und bleibt danach unverändert; eine Abweichung nach der Aufteilung ist ein Befund über die Aufteilung, kein Anlass, den Snapshot zu aktualisieren. Ein nachgezogener Snapshot macht das gesamte Netz wertlos — genau die Fehlerklasse „ein grüner Test ist kein Beweis\", die `11-LEARNINGS.md` und `08-LEARNINGS.md` für dieses Projekt zweimal dokumentiert haben."
  artifacts:
    - tests/unit/dmscreen-characterization.test.js
    - tests/unit/__snapshots__/dmscreen-characterization.test.js.snap
  key_links:
    - "`loader.js` `MODULES` (Einträge unter `features/dmscreen/`) → Sandbox-Ladereihenfolge des Tests → `getDMScreenWidgets()` → je `render()` pro Widget-Typ"
    - "Dieser Plan ist die harte Vorbedingung für Plan 13-12: ohne grünen Snapshot darf dort keine Zeile verschoben werden (D-04)"
---

<objective>
`features/dmscreen/dmscreen-render.js` ist mit 1576 Zeilen, 58 Funktionen und 21 Widget-Typen die
riskanteste der vier aufzuteilenden Dateien — und die einzige ohne eigene Testabdeckung. Sie kommt in
genau zwei Testdateien vor (`tests/unit/migration-wizard.test.js`, `tests/unit/stability.test.js`), mit
zusammen fünf beiläufigen Erwähnungen. D-04 verlangt deshalb, dass ein Charakterisierungstest gegen das
**ungeteilte** Modul steht und grün ist, **bevor die erste Zeile verschoben wird**.

Purpose: Das Vorsichtsbudget dieser Phase gehört hierher, nicht zu `rich-text.js`. Die Roadmap nimmt an,
`rich-text.js` sei wegen seines Testnetzes der begründungspflichtige Fall — tatsächlich ist es der am
besten abgesicherte der vier (79 eingefrorene E2E-Tests aus Phase 9). Der riskante ist der DM-Screen.
Dieser Plan produziert das fehlende Netz und **ändert keine einzige Zeile Produktionscode**.
Output: `tests/unit/dmscreen-characterization.test.js` plus die eingecheckte Snapshot-Datei.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-h-rtung-wartbarkeit/13-CONTEXT.md
@.planning/phases/13-h-rtung-wartbarkeit/13-RESEARCH.md
@.planning/phases/13-h-rtung-wartbarkeit/13-PATTERNS.md
@.planning/phases/13-h-rtung-wartbarkeit/13-VALIDATION.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Dieser Plan erzeugt:

| Symbol / Datei | Art | Ort |
|---|---|---|
| `loadDmScreenSandbox()` | Test-Hilfsfunktion (lädt alle `features/dmscreen/`-Module aus `loader.js` `MODULES` in eine Sandbox) | `tests/unit/dmscreen-characterization.test.js` |
| `DMS_FIXTURE` | deterministische `D`-Fixture für den Snapshot | `tests/unit/dmscreen-characterization.test.js` |
| `tests/unit/dmscreen-characterization.test.js` | neue Jest-Testdatei | `tests/unit/` |
| `tests/unit/__snapshots__/dmscreen-characterization.test.js.snap` | eingecheckter Snapshot | `tests/unit/__snapshots__/` |

**Kein Produktionscode wird geändert.** Kein neuer `loader.js`-`MODULES`-Eintrag.

<tasks>

<task type="tracer">
  <name>Task 1: Sandbox-Harnisch aus der loader.js-Modulliste, Snapshot für EIN Widget</name>
  <files>tests/unit/dmscreen-characterization.test.js, tests/unit/__snapshots__/dmscreen-characterization.test.js.snap</files>
  <read_first>
    - `loader.js` Zeilen 1–175 (Aufbau des `MODULES`-Arrays; der Eintrag
      `'features/dmscreen/dmscreen-render.js'` steht heute allein unter „DM Screen")
    - `features/dmscreen/dmscreen-render.js` Zeilen 1–100 (Kopf, `CONSTANTS`-Sektion, und
      insbesondere `const DMS_LIVE_SYNC_DELAY = UI_TIMING.DM_SCREEN_SYNC_DELAY;` auf Zeile 99 — das
      Modul liest `UI_TIMING` bereits beim Laden, die Sandbox muss es also vorher bereitstellen)
    - `features/dmscreen/dmscreen-render.js` Zeilen 639–782 (`getDMScreenWidgets()` — die Registry, die
      pro Typ `{ name, icon, render, compact }` liefert; sie ist NICHT nach `window` exportiert und muss
      deshalb als Sandbox-Global gelesen werden)
    - `features/dmscreen/dmscreen-render.js` Zeilen 1573–1576 (die einzigen beiden `window`-Exporte:
      `renderDMScreen`, `resetDMScreenLayout`)
    - `tests/unit/file-backup-idb.test.js` Zeilen 1–75 (Muster: echtes Quellmodul per `vm` in eine
      frische Sandbox laden; Mock-Bau mit `jest.fn()`)
    - `tests/unit/stability.test.js` (der im Projekt etablierte Charakterisierungs-Stil gegen
      bestehende Render-Ausgabe, den D-04 als Präzedenz nennt)
    - `core/constants.js` (welche Konstanten die Widget-Renderer erwarten: `DND_RULES`,
      `UI_CONSTANTS`, `UI_TIMING`, `CONDITIONS`, `CONDITION_COLORS`, `DAMAGE_TYPES`, `ATTRIBUTES`,
      `SKILL_INFO`, `COMBAT_CONSTANTS`)
  </read_first>
  <action>
    Lege `tests/unit/dmscreen-characterization.test.js` an. Baue zuerst `loadDmScreenSandbox()`:
    die Funktion liest `loader.js`, extrahiert aus dem `MODULES`-Array **in Array-Reihenfolge** alle
    Einträge, deren Pfad mit `features/dmscreen/` beginnt, und wertet diese Dateien nacheinander in
    EINER gemeinsamen `vm`-Sandbox aus. Genau dieser Umweg über `loader.js` ist der Punkt: nach der
    Aufteilung in Plan 13-12 liegen die Widget-Renderer in mehreren Dateien, und der Test findet sie
    dann ohne Änderung — `loader.js` ist die einzige Modulliste des Projekts (ARCH-01).
    Schlägt die Extraktion fehl oder findet sie null Einträge, muss der Test hart fehlschlagen mit
    einer Meldung, die das sagt — nicht still eine leere Liste laden.
    Die Sandbox bekommt vor dem Laden: ein deterministisches `window`/`globalThis`, eine feste
    `D`-Fixture (`DMS_FIXTURE`) mit je zwei Charakteren mit festen HP/AC-Werten, einer Initiative mit
    zwei Kämpfern und `round: 1`, zwei Zufallstabellen, festen `dmScreenLayout`/`dmScreenProfiles`,
    festen Notizen; die Konstanten-Namensräume aus `core/constants.js` (lade die Datei ebenfalls in die
    Sandbox, statt die Werte im Test nachzubauen — sonst prüft der Snapshot deine Kopie, nicht das
    Projekt); sowie Stubs für `$`, `esc`, `showToast`, `save`, `saveImmediate`, `debounce`
    (synchron durchreichend), `registerPostSaveHook`, `EntityLookup`, `getEntityForCombat`.
    Determinismus ist Abnahmekriterium: keine Systemzeit, keine Zufallszahlen. Falls ein Widget
    `Date.now()` oder `Math.random()` aufruft, in der Sandbox durch feste Werte ersetzen und die
    betroffenen Widgets im SUMMARY benennen.
    Dann als Tracer den Snapshot für GENAU EIN Widget erzeugen: hole die Registry über
    `getDMScreenWidgets()` aus der Sandbox, nimm den Typ `party`, rufe dessen `render()` auf und halte
    die zurückgegebene HTML-Zeichenkette mit `toMatchSnapshot()` fest. Prüfe zusätzlich in derselben
    Aufgabe, dass die Registry genau 21 Typen enthält und dass jeder Eintrag die Felder `name`, `icon`,
    `render` (Funktion) und `compact` (Boolean) trägt.
    Erzeuge den Snapshot mit einem Lauf OHNE `--ci` (sonst schreibt Jest keine neuen Snapshots) und
    checke die entstandene `.snap`-Datei mit ein.
  </action>
  <verify>
    <automated>npx jest tests/unit/dmscreen-characterization.test.js</automated>
    <fails_when>Exit-Code ungleich 0, oder die Zusammenfassung meldet „0 passed", oder ein Testfall
    meldet eine andere Typenzahl als 21, oder die Modulliste aus `loader.js` kam leer zurück</fails_when>
    <automated>test -s tests/unit/__snapshots__/dmscreen-characterization.test.js.snap</automated>
    <fails_when>Exit-Code ungleich 0 — dann wurde keine Snapshot-Datei geschrieben oder sie ist leer</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/unit/dmscreen-characterization.test.js` existiert und enthält `loadDmScreenSandbox`
    - Die Datei enthält `loader.js` als gelesenen Pfad und `features/dmscreen/` als Filterpräfix
    - Die Datei enthält KEINEN direkten Aufruf eines Widget-Renderers über dessen internen Namen
      (`grep -c 'renderDMS' tests/unit/dmscreen-characterization.test.js` liefert `0`)
    - `tests/unit/__snapshots__/dmscreen-characterization.test.js.snap` existiert und ist nicht leer
    - Verhalten: zwei aufeinanderfolgende Läufe von `npx jest tests/unit/dmscreen-characterization.test.js`
      liefern dasselbe Ergebnis ohne Snapshot-Aktualisierung (Determinismus)
    - `npx jest tests/unit/dmscreen-characterization.test.js` endet mit Exit-Code 0
  </acceptance_criteria>
  <done>Der Harnisch lädt die DM-Screen-Module über die loader.js-Modulliste und hält die Ausgabe eines Widgets reproduzierbar fest.</done>
</task>

<task type="auto">
  <name>Task 2: Snapshot auf alle 21 Widget-Typen und den Gesamt-Einstieg ausdehnen</name>
  <files>tests/unit/dmscreen-characterization.test.js, tests/unit/__snapshots__/dmscreen-characterization.test.js.snap</files>
  <read_first>
    - `tests/unit/dmscreen-characterization.test.js` (Stand nach Task 1)
    - `features/dmscreen/dmscreen-render.js` Zeilen 639–782 (die vollständige Typenliste der Registry:
      `party`, `initiative`, `dice`, `conditions`, `dc`, `tables`, `rules`, `notes`, `actions`,
      `attributes`, `saves`, `skills`, `economy`, `sizes`, `objects`, `improvised`, `ritual`, `damage`,
      `terrain`, `knowledge`, `travel`)
    - `features/dmscreen/dmscreen-render.js` Zeilen 314–426 (`renderDMScreen()` — der öffentliche
      Einstieg; er schreibt in einen Container, den die Sandbox bereitstellen muss)
    - `features/dmscreen/dmscreen-render.js` Zeilen 194–313 (Profil-Funktionen: `switchDMSProfile`,
      `saveDMSProfileAs`, `deleteDMSProfile`, `renderDMSProfileList` — die vier Standardprofile
      Standard/Kampf/Minimal/Referenz gehören mit in den Snapshot, weil Plan 13-12 sie in eine eigene
      Datei verschiebt)
    - `CLAUDE.md` §„DM Screen (Widget-Based Dashboard)" — die verbindliche Aufzählung der 21 Typen
  </read_first>
  <action>
    Dehne die Charakterisierung auf die volle Oberfläche aus:
    (a) Ein datengetriebener Testfall iteriert über ALLE Einträge aus `getDMScreenWidgets()` und hält je
    Typ die HTML-Ausgabe von `render()` als eigenen benannten Snapshot fest. Die Iteration läuft über
    die Registry-Schlüssel, nicht über eine im Test hartkodierte Liste — so bleibt der Fall nach der
    Aufteilung gültig. Ein zweiter Fall friert die sortierte Schlüsselliste selbst als Snapshot ein,
    damit ein verschwundener oder umbenannter Typ auffällt.
    (b) Ein Testfall ruft `window.renderDMScreen()` gegen einen Sandbox-Container auf und hält dessen
    `innerHTML` als Snapshot fest. Das ist der Einstieg, den `TAB_RENDER_REGISTRY` benutzt; er deckt die
    Zusammenstellung aus Header-Leiste (`compact: true`) und Grid (`compact: false`) ab, die kein
    Einzelwidget-Snapshot erfasst.
    (c) Ein Testfall wechselt über `switchDMSProfile()` nacheinander durch die vier Standardprofile und
    hält je Profil die resultierende `dmScreenLayout`-Struktur als Snapshot fest.
    (d) Ein Randfall-Testfall läuft dasselbe gegen eine LEERE `D`-Fixture (keine Charaktere, keine
    Initiative, keine Tabellen, keine Notizen) und hält die Ausgabe ebenfalls fest — Widgets, die bei
    leerer Kampagne anders rendern, sind genau die, bei denen eine Aufteilung am ehesten etwas
    verschiebt.
    Führe den Lauf einmal ohne `--ci` aus, um die Snapshots zu schreiben, prüfe die entstandene
    `.snap`-Datei per Sichtkontrolle auf offensichtlichen Unsinn (leere Strings, `undefined`,
    `[object Object]`) und checke sie dann ein. Danach den Lauf mit `--ci` wiederholen: ab jetzt ist
    ein abweichender Snapshot ein Fehler, kein Anlass zum Neuschreiben.
    Halte im SUMMARY fest: die Zahl der erzeugten Snapshots, die Gesamtgröße der `.snap`-Datei, und
    welche Widgets bei leerer Fixture eine abweichende Ausgabe liefern.
  </action>
  <verify>
    <automated>npx jest tests/unit/dmscreen-characterization.test.js --ci</automated>
    <fails_when>Exit-Code ungleich 0, oder die Ausgabe enthält `snapshots written`, oder die
    Zusammenfassung meldet mindestens ein `failed`, oder sie meldet `0 passed`</fails_when>
    <automated>test "$(grep -c '^exports\[' tests/unit/__snapshots__/dmscreen-characterization.test.js.snap)" -ge 25</automated>
    <fails_when>Exit-Code ungleich 0 — dann enthält die Snapshot-Datei weniger als 25 Einträge, also
    fehlen Widget-Typen oder die zusätzlichen Fälle</fails_when>
    <automated>npx jest</automated>
    <fails_when>Exit-Code ungleich 0, oder die Zusammenfassung meldet mindestens ein `failed`, oder die
    Suite-Zahl liegt unter der um diese Datei erhöhten Baseline</fails_when>
  </verify>
  <acceptance_criteria>
    - Die Snapshot-Datei enthält mindestens 25 Einträge (21 Widget-Typen + Typenliste + Gesamt-Einstieg
      + Profile + Leerfall)
    - `tests/unit/dmscreen-characterization.test.js` iteriert über die Registry-Schlüssel statt über
      eine im Test hartkodierte Typenliste
    - `grep -c 'renderDMS' tests/unit/dmscreen-characterization.test.js` liefert weiterhin `0`
    - `npx jest tests/unit/dmscreen-characterization.test.js --ci` endet mit Exit-Code 0 und schreibt
      keine Snapshots
    - Verhalten: die Snapshot-Datei enthält für jeden der 21 Typen einen Eintrag mit nicht-leerem
      HTML-Inhalt (kein `""`, kein `undefined`, kein `[object Object]`)
    - `npx jest` läuft vollständig grün
    - `git status` zeigt keine Änderung an `features/dmscreen/dmscreen-render.js` — dieser Plan fasst
      keinen Produktionscode an
  </acceptance_criteria>
  <done>Das Netz aus D-04 steht: alle 21 Widget-Typen, der Gesamt-Einstieg, die vier Profile und der Leerfall sind gegen das ungeteilte Modul festgehalten und eingecheckt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kampagnendaten (`D`) → Widget-Renderer → `innerHTML` des DM-Screen-Containers | 21 Renderer schreiben Nutzerdaten in HTML-Zeichenketten; `esc()` ist die einzige Schutzschicht. Der Snapshot friert genau diesen Ausgabepfad ein. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-16 | Tampering | Alle 21 Widget-Renderer (`features/dmscreen/dmscreen-render.js`) | high | mitigate | Der Snapshot friert die vollständige HTML-Ausgabe je Widget ein. Eine Aufteilung, die versehentlich einen `esc()`-Aufruf verliert oder eine Escaping-Reihenfolge verschiebt, ändert die Ausgabe und wird in Plan 13-12 rot — das ist der einzige automatisierte Schutz, den dieses Modul hat. |
| T-13-17 | Repudiation | Snapshot-Aktualisierung statt Befund | high | mitigate | Läufe nach der Erstellung verwenden `--ci`; ein abweichender Snapshot wird zum Fehler statt still neu geschrieben. Zusätzlich als Verbot in `must_haves.prohibitions` festgehalten. |
| T-13-18 | Tampering | Nichtdeterminismus im Snapshot (Zeit, Zufall) | medium | mitigate | Systemzeit und Zufallsquellen werden in der Sandbox durch feste Werte ersetzt; zwei aufeinanderfolgende Läufe müssen identisch sein. Ein flackernder Snapshot wäre schlimmer als keiner, weil er Rot-Meldungen entwertet. |
| T-13-19 | Denial of Service | Fest verdrahteter Dateipfad im Test | medium | mitigate | Die Modulliste kommt aus `loader.js` `MODULES`; ein fest verdrahteter Pfad würde nach Plan 13-12 ins Leere zeigen und den Test entweder rot oder — schlimmer — leer-grün machen. Die Extraktion schlägt bei null Treffern hart fehl. |
| T-13-SC | Tampering | npm/pip/cargo installs | low | accept | Dieser Plan installiert kein Paket. `fake-indexeddb` und andere Test-Hilfsbibliotheken sind laut `13-RESEARCH.md` § „Don't Hand-Roll" ausdrücklich abgelehnt — der Harnisch nutzt das etablierte handgerollte `vm`-Sandbox-Muster. |
</threat_model>

<verification>
1. `npx jest tests/unit/dmscreen-characterization.test.js --ci` grün, ohne geschriebene Snapshots
2. `tests/unit/__snapshots__/dmscreen-characterization.test.js.snap` enthält mindestens 25 Einträge
3. `grep -c 'renderDMS' tests/unit/dmscreen-characterization.test.js` liefert `0` (nur öffentliche Oberfläche)
4. `npx jest` vollständig grün
5. `git diff --name-only` listet keine Datei unter `features/` — kein Produktionscode geändert
</verification>

<success_criteria>
- D-04 ist erfüllt: das Regressionsnetz für `dmscreen-render.js` existiert und ist grün, bevor Plan 13-12 startet
- Der Test überlebt die Aufteilung unverändert, weil er über `loader.js` `MODULES` lädt und nur die öffentliche Oberfläche anspricht
- Der Snapshot ist deterministisch und eingecheckt
</success_criteria>

<output>
Create `.planning/phases/13-h-rtung-wartbarkeit/13-05-SUMMARY.md` when done
</output>
