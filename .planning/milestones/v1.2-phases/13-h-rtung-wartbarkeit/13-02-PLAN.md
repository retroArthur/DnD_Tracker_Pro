---
phase: 13-h-rtung-wartbarkeit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - systems/entity-links.js
  - features/wiki/wiki.js
  - ui/actions/system-actions.js
  - tests/unit/wiki-links.test.js
autonomous: true
requirements: [SEC-04, MAINT-04, MAINT-02]

estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Die Textsuche nach der deprecated Editier-Kommando-API liefert im gesamten Quellbaum (`core`, `features`, `systems`, `ui`, `utils`, `render`, `loader.js`) keinen Aufruf mehr — nur noch die drei Kommentar-Erwähnungen in `utils/basic.js:125,126,235`, die laut Erfolgskriterium 6 bleiben dürfen."
    - "`parseWikiLinks()` schickt sowohl den Attributwert als auch den sichtbaren Linktext durch `esc()`; ein Wiki-Link-Titel mit `<`, `>`, `\"` oder `'` erzeugt kein Markup mehr."
    - "Der Reihenfolge-Kommentar bei `features/wiki/wiki.js:432-434` beschreibt nach dem Fix den tatsächlichen Zustand: `parseWikiLinks()` ist selbst sicher und nicht mehr von der vorgelagerten Sanitisierung abhängig."
    - "Das `wiki-tree-item`-Element trägt genau ein `data-id`-Attribut (das zweite, folgenlose Duplikat bei `features/wiki/wiki.js:391-392` ist entfernt)."
    - "MAINT-04 · Randfall `adjacency`: Text, der an einem bestehenden Textknoten unmittelbar anliegt, wird von `insertTextAtSelection()` eingefügt, ohne den Nachbarknoten zu spalten oder zu verschmelzen — der resultierende `textContent` des Editors ist zeichengleich zum bisherigen Ergebnis."
    - "MAINT-04 · Randfall `empty`: Ohne aktive Selektion (`selection.rangeCount === 0`) verändert `insertTextAtSelection()` das DOM nicht und wirft nicht; die aufrufende Stelle fokussiert deshalb vorher den Editor."
    - "MAINT-04 · Randfall `ordering`: Nach dem Einfügen steht die Selektion zusammengeklappt hinter dem eingefügten Text — unmittelbar weitergetippter Text landet dahinter, nicht davor."
    - "SEC-04 — offene Kanten-Annahme (Probe-Kategorie `unclassified`, bleibt `unresolved`): Die deterministische Kantensonde konnte SEC-04 keiner Randfallklasse zuordnen. Planer-Annahme: die relevanten Kanten sind ein Linktitel mit HTML-Metazeichen und ein Linktitel, der auf einen nicht existierenden Wiki-Eintrag zeigt (`missing`-Klasse); beide sind in `tests/unit/wiki-links.test.js` abgedeckt. Nicht automatisch aufgelöst; als Annahme markiert."
  artifacts:
    - tests/unit/wiki-links.test.js
    - systems/entity-links.js
    - features/wiki/wiki.js
    - ui/actions/system-actions.js
  key_links:
    - "`insertEntityLink()` (`systems/entity-links.js`) → `editor.focus()` → `insertTextAtSelection()` (definiert in `ui/editors/rich-text.js`, global im Bündel)"
    - "`renderMarkdownInContent()` → `addTOCAnchors()` → `parseWikiLinks()` → DOM: die dokumentierte Reihenfolgeabhängigkeit, die SEC-04 auflöst"
---

<objective>
Drei Fixes an derselben Naht — Wiki-Rendering und Editor-DOM: die letzten drei Aufrufe der deprecated
Editier-Kommando-API außerhalb des Editor-Moduls werden mit den Phase-9-Hilfsfunktionen abgelöst
(MAINT-04), die Regex-Capture in `parseWikiLinks()` wird vollständig escapt (SEC-04), und das doppelte
`data-id`-Attribut im Wiki-Baum verschwindet (MAINT-02, Teil 1 von 2).

Purpose: Erfolgskriterien 1 (zweite Hälfte), 6 und 7 (Teil „doppeltes `data-id`") der Phase. Alle drei
Stellen liegen in Dateien, die später in dieser Phase aufgeteilt werden (`features/wiki/wiki.js`,
Plan 13-09) — deshalb zeilengenau JETZT, solange die Zeilennummern aus Recherche und Anforderung noch
gelten.
Output: drei migrierte Aufrufstellen, ein gehärtetes `parseWikiLinks()`, ein bereinigtes
Baum-Element, neue Unit-Testdatei `tests/unit/wiki-links.test.js`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-h-rtung-wartbarkeit/13-CONTEXT.md
@.planning/phases/13-h-rtung-wartbarkeit/13-RESEARCH.md
@.planning/phases/13-h-rtung-wartbarkeit/13-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Dieser Plan erzeugt:

| Symbol / Datei | Art | Ort |
|---|---|---|
| `tests/unit/wiki-links.test.js` | neue Jest-Testdatei | `tests/unit/` |

Keine neuen Quellmodule, kein neuer `loader.js`-`MODULES`-Eintrag — alle drei geänderten Quelldateien
sind bereits registriert. Die genutzten Hilfsfunktionen `insertTextAtSelection()` und
`wrapRangeWithElement()` existieren bereits in `ui/editors/rich-text.js` (Phase 9) und werden nur
aufgerufen, nicht neu geschrieben.

<tasks>

<task type="tracer">
  <name>Task 1: Erste Aufrufstelle end-to-end auf die Phase-9-Hilfsfunktion umstellen</name>
  <files>systems/entity-links.js</files>
  <read_first>
    - `systems/entity-links.js` Zeilen 60–100 (`insertEntityLink()`: `linkCode` wird gebaut, der Editor
      per `$(insertEntityLinkTargetEditor)` geholt, fokussiert, dann eingefügt)
    - `ui/editors/rich-text.js` Zeilen 882–946 (`insertTextAtSelection()` und
      `insertLineBreakAtSelection()` — Signatur, Selektionsverhalten und der ausführliche
      Erklärkommentar zur Cursor-Stabilität)
    - `ui/editors/rich-text.js` Zeilen 329–360 (`wrapRangeWithElement()`, `unwrapEditorElement()`,
      `closestEditorAncestor()`)
    - `CLAUDE.md` §Conventions → „execCommand-Ablösung (abgeschlossen, Phase 9)" — die Liste der
      Ersatzfunktionen und die drei bewusst nicht migrierten Aufrufstellen, die dieser Plan schließt
    - `tests/e2e/features/editor-insert.spec.js` (wie das Einfügeverhalten in der bestehenden Suite
      geprüft wird)
  </read_first>
  <action>
    In `systems/entity-links.js` (Aufruf bei Zeile 87, innerhalb `insertEntityLink()`) den Aufruf der
    deprecated Editier-Kommando-API mit dem Befehlsnamen `insertText` durch `insertTextAtSelection(linkCode)`
    ersetzen. Der vorangehende `editor.focus()`-Aufruf bleibt und ist Voraussetzung: ohne Fokus gibt es
    keine Selektion, und `insertTextAtSelection()` kehrt dann ohne DOM-Änderung zurück (Randfall
    `empty`). Rufe die Funktion über `window.insertTextAtSelection` mit `typeof`-Prüfung auf, damit ein
    Ladereihenfolge-Problem zu einem stillen No-Op statt zu einem `TypeError` führt — `entity-links.js`
    steht in `loader.js` vor `ui/editors/rich-text.js`, die Auflösung passiert aber erst zur Klickzeit.
    Kein `const X = window.X` innerhalb der Funktion (CLAUDE.md „Duplicate Declaration Debugging
    Pattern") — direkt `window.insertTextAtSelection(...)` aufrufen.
    Anschließend einen Dev-Bundle bauen und die vier eingefrorenen Editor-Specs aus Phase 9 laufen
    lassen. Sie sind das Regressionsnetz für jede Änderung am Einfügeverhalten.
  </action>
  <verify>
    <automated>node --check systems/entity-links.js &amp;&amp; PYTHONIOENCODING=utf-8 python build.py</automated>
    <fails_when>Exit-Code ungleich 0, oder die Build-Ausgabe enthält `[FEHLER]` oder `[ABORTED]`</fails_when>
    <automated>npx playwright test tests/e2e/features/editor-insert.spec.js tests/e2e/features/editor-smoke.spec.js</automated>
    <fails_when>Exit-Code ungleich 0, oder die Zusammenfassung meldet mindestens ein `failed`, oder sie
    meldet `0 passed`</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "insertTextAtSelection" systems/entity-links.js` liefert mindestens `1`
    - `grep -c "document\.execCommand" systems/entity-links.js` liefert `0`
    - `PYTHONIOENCODING=utf-8 python build.py` endet mit Exit-Code 0
    - Verhalten: `npx playwright test tests/e2e/features/editor-insert.spec.js tests/e2e/features/editor-smoke.spec.js`
      läuft grün gegen das frisch gebaute `dist/dnd-tracker-bundled.html`
  </acceptance_criteria>
  <done>Eine der drei Aufrufstellen läuft über die Phase-9-Hilfsfunktion, mit grünem Editor-Regressionsnetz als Beweis.</done>
</task>

<task type="auto">
  <name>Task 2: Die verbleibenden zwei Aufrufstellen ablösen, Textsuche auf Null bringen</name>
  <files>features/wiki/wiki.js, ui/actions/system-actions.js</files>
  <read_first>
    - `features/wiki/wiki.js` Zeilen 820–840 (`insertWikiLink()`: `prompt()` liefert den Titel, dann
      wird `[[titel]]` eingefügt; der Befehlsname ist `insertText`)
    - `ui/actions/system-actions.js` Zeilen 70–95 (Aktion `insert-link`: Editor per `$(editorId)`,
      `editor.focus()`, `prompt('Link URL eingeben:')`, dann Befehlsname `createLink`)
    - `ui/editors/rich-text.js` Zeilen 329–340 (`wrapRangeWithElement(range, wrapper)` — nimmt einen
      Range und ein fertiges Wrapper-Element, kapselt `surroundContents()` mit
      Extract/Append/Insert-Rückfall)
    - `utils/basic.js` Zeilen 120–130 und 230–240 (die drei Kommentar-Erwähnungen, die bleiben)
  </read_first>
  <action>
    Zwei weitere Aufrufstellen ablösen:
    (a) `features/wiki/wiki.js:831` in `insertWikiLink()` — Befehlsname `insertText`, Ersatz
    `window.insertTextAtSelection(...)` mit `typeof`-Prüfung, analog Task 1. Es gibt hier keinen
    vorgelagerten `focus()`-Aufruf; ergänze ihn nicht blind, sondern prüfe im Code, welches Element
    zuletzt fokussiert war — im Zweifel den Fall „keine Selektion" sauber durchfallen lassen (No-Op
    statt Ausnahme) und das im SUMMARY vermerken.
    (b) `ui/actions/system-actions.js:82` in der Aktion `insert-link` — Befehlsname `createLink`,
    Ersatz über `window.wrapRangeWithElement(range, anchor)`: aktuelle Selektion über
    `window.getSelection()` holen, bei `rangeCount === 0` ohne Änderung zurückkehren, sonst
    `getRangeAt(0)` nehmen, ein `a`-Element per `document.createElement('a')` bauen, dessen `href` auf
    den vom Nutzer eingegebenen `url` setzen und den Range damit umschließen. Der bestehende
    `showToast('🔗 Link eingefügt')`-Aufruf bleibt und läuft nur, wenn tatsächlich umschlossen wurde.
    Danach muss die Textsuche nach der deprecated Editier-Kommando-API im Quellbaum null Aufrufe
    liefern. Die drei Treffer in `utils/basic.js:125,126,235` sind Kommentare ohne Aufruf und bleiben
    unangetastet — Erfolgskriterium 6 verlangt nur Freiheit außerhalb von Kommentaren.
    Setze in den Kommentaren der drei geänderten Dateien nicht den Namen der abgelösten API in
    Verbindung mit dem `document`-Objekt — die Abnahmeprüfung sucht genau diese Zeichenfolge und würde
    an einem erklärenden Kommentar hängenbleiben.
  </action>
  <verify>
    <automated>node --check features/wiki/wiki.js &amp;&amp; node --check ui/actions/system-actions.js &amp;&amp; PYTHONIOENCODING=utf-8 python build.py</automated>
    <fails_when>Exit-Code ungleich 0, oder die Build-Ausgabe enthält `[FEHLER]` oder `[ABORTED]`</fails_when>
    <automated>test "$(grep -rn 'document\.execCommand' --include=*.js core features systems ui utils render loader.js | wc -l)" -eq 0</automated>
    <fails_when>Exit-Code ungleich 0 — dann steht im Quellbaum noch mindestens ein Aufruf der
    abgelösten API</fails_when>
    <automated>npx playwright test tests/e2e/features/editor-insert.spec.js tests/e2e/features/editor-formatting.spec.js tests/e2e/features/editor-floating.spec.js tests/e2e/features/editor-smoke.spec.js tests/e2e/features/wiki.spec.js</automated>
    <fails_when>Exit-Code ungleich 0, oder die Zusammenfassung meldet mindestens ein `failed`, oder sie
    meldet `0 passed`</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'document\.execCommand' --include=*.js core features systems ui utils render loader.js | wc -l` liefert `0`
    - `grep -c "execCommand" utils/basic.js` liefert weiterhin `3` (die Kommentare bleiben)
    - `features/wiki/wiki.js` enthält `insertTextAtSelection`
    - `ui/actions/system-actions.js` enthält `wrapRangeWithElement` und `createElement('a')`
    - Verhalten: alle vier eingefrorenen Editor-Specs aus Phase 9 plus `wiki.spec.js` laufen grün
  </acceptance_criteria>
  <done>Erfolgskriterium 6 ist erfüllt: kein Aufruf der abgelösten API mehr im Quellbaum, das Editor-Netz bleibt grün.</done>
</task>

<task type="auto">
  <name>Task 3: `parseWikiLinks()` vollständig escapen, Reihenfolge-Kommentar nachziehen, doppeltes `data-id` entfernen</name>
  <files>features/wiki/wiki.js, tests/unit/wiki-links.test.js</files>
  <read_first>
    - `features/wiki/wiki.js` Zeilen 644–660 (`parseWikiLinks()`: heute wird nur `"` durch `&quot;`
      ersetzt und der sichtbare `linkText` unescaped in den Textknoten geschrieben)
    - `features/wiki/wiki.js` Zeilen 425–440 (der Reihenfolge-Kommentar, der die Abhängigkeit von
      `sanitizeHTML()` in `renderMarkdownInContent()` dokumentiert und mit „NICHT ändern" endet)
    - `features/wiki/wiki.js` Zeilen 386–396 (das `wiki-tree-item`-Element mit `data-id` auf Zeile 391
      UND erneut auf Zeile 392)
    - `utils/basic.js` — die Definition von `esc()`, um zu sehen, welche Zeichen behandelt werden
    - `tests/unit/dice-stats.test.js` Zeilen 1–40 (Stilvorlage für eine reine Logik-Testdatei ohne DOM)
    - `tests/unit/file-backup-idb.test.js` Zeilen 1–75 (Alternative: echtes Quellmodul per `vm` laden)
  </read_first>
  <action>
    Lege `tests/unit/wiki-links.test.js` an und schreibe die Fälle ZUERST, rot gegen den heutigen Stand:
    ein Wiki-Link-Titel mit `<`, `>`, `"` und `'` darf im erzeugten HTML weder ein öffnendes Tag noch
    einen Attribut-Ausbruch erzeugen; ein Titel ohne Sonderzeichen bleibt im sichtbaren Text
    unverändert lesbar; ein Titel, der auf keinen existierenden Eintrag zeigt, bekommt weiterhin die
    Klasse `missing`, ein existierender nicht. Lade `features/wiki/wiki.js` per `vm` nach dem Muster
    aus `tests/unit/file-backup-idb.test.js` und stubbe `esc` in der Sandbox NICHT — es muss die echte
    Projektfunktion aus `utils/basic.js` sein, sonst prüft der Test das Escaping nicht wirklich.
    Dann `parseWikiLinks()` härten: sowohl der Attributwert (heute `escapedText`, gebaut über ein
    manuelles Ersetzen des Anführungszeichens) als auch der sichtbare Linktext im Textknoten laufen
    durch das projektweite `esc()`. Das manuelle Zeichen-Ersetzen entfällt ersatzlos. Der Vergleich für
    `exists` läuft weiterhin auf dem UNescapten Rohtitel — sonst findet ein Titel mit Sonderzeichen
    seinen Eintrag nicht mehr.
    Danach den Reihenfolge-Kommentar bei Zeile 432–434 nachziehen: er behauptet heute, die Stelle sei
    „nur deshalb ungefährlich, weil `sanitizeHTML()` vorher bereits global bereinigt hat — NICHT
    ändern". Nach dem Fix stimmt das nicht mehr. Formuliere ihn so um, dass er sagt: `parseWikiLinks()`
    escapt seinen Capture selbst und ist nicht mehr von der vorgelagerten Sanitisierung abhängig; die
    Aufrufreihenfolge `renderMarkdownInContent()` → `addTOCAnchors()` → `parseWikiLinks()` bleibt aus
    anderen Gründen bestehen (die Anker-Ids brauchen das gerenderte Markup).
    Zuletzt in Zeile 391/392 das zweite, folgenlose `data-id`-Attribut am `wiki-tree-item`-`div`
    entfernen. Das Element behält genau ein `data-id`. Der HTML-Parser verwarf das zweite ohnehin —
    die Änderung ist verhaltensneutral und macht die Absicht sichtbar.
  </action>
  <verify>
    <automated>npx jest tests/unit/wiki-links.test.js</automated>
    <fails_when>Exit-Code ungleich 0, oder die Zusammenfassung meldet „0 passed", oder ein Testfall
    meldet, dass ein Titel mit `&lt;` im Ergebnis ein öffnendes Tag erzeugt hat</fails_when>
    <automated>PYTHONIOENCODING=utf-8 python build.py &amp;&amp; npx playwright test tests/e2e/features/wiki.spec.js</automated>
    <fails_when>Exit-Code ungleich 0, oder die Build-Ausgabe enthält `[FEHLER]`/`[ABORTED]`, oder die
    Playwright-Zusammenfassung meldet mindestens ein `failed`</fails_when>
    <automated>npx jest</automated>
    <fails_when>Exit-Code ungleich 0, oder die Zusammenfassung meldet mindestens ein `failed`, oder die
    Suite-Zahl liegt unter 31</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'esc(' features/wiki/wiki.js` liefert einen Wert, der um mindestens 2 höher liegt als
      vor der Änderung, und `parseWikiLinks()` enthält keinen manuellen `replace(/"/g` mehr
    - `grep -c 'NICHT ändern' features/wiki/wiki.js` liefert `0`
    - Der `wiki-tree-item`-Block enthält genau ein `data-id="${entry.id}"` (heute zwei aufeinander
      folgende Zeilen mit demselben Attribut)
    - `npx jest tests/unit/wiki-links.test.js` endet mit Exit-Code 0
    - Verhalten: ein Wiki-Eintrag mit dem Titel `Der <b>Hobbit</b>` erzeugt im Baum und im
      Link-Rendering sichtbaren Text mit Entities statt fettem Markup
    - `npx jest` läuft vollständig grün (Baseline: 31 Suiten)
  </acceptance_criteria>
  <done>SEC-04 ist geschlossen, der irreführende Reihenfolge-Kommentar stimmt wieder, das doppelte Attribut ist weg.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wiki-Eintragsinhalt (Nutzertext) → Render-Pipeline → DOM | `entry.content` läuft durch `renderMarkdownInContent()` (inkl. `sanitizeHTML()`), `addTOCAnchors()` und `parseWikiLinks()`, bevor es per `innerHTML` in den DOM geht. |
| Wiki-Eintragstitel (Nutzertext) → Baum-Rendering | `entry.title` landet in Attributen und Textknoten des `wiki-tree-item`-Elements. |
| `prompt()`-Eingabe (Nutzertext) → Editor-DOM | `insertWikiLink()` und die Aktion `insert-link` schreiben eine Nutzereingabe direkt in den Editor bzw. in ein `href`-Attribut. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-05 | Tampering | `parseWikiLinks()` (`features/wiki/wiki.js`) | high | mitigate | Attributwert UND sichtbarer Textknoten laufen durch `esc()`; das punktuelle Ersetzen nur des Anführungszeichens entfällt. Abgedeckt durch `tests/unit/wiki-links.test.js`. |
| T-13-06 | Tampering | Reihenfolgeabhängigkeit von `sanitizeHTML()` | high | mitigate | Die implizite Zusicherung „nur sicher, weil vorher sanitisiert wurde" wird durch eine explizite Escaping-Zusicherung an der Stelle selbst ersetzt; der Kommentar wird nachgezogen, damit die nächste Änderung nicht auf einer falschen Annahme aufsetzt. |
| T-13-07 | Tampering | `href` aus `prompt()` in der Aktion `insert-link` | medium | accept | Einzelnutzer-Offline-App: der Nutzer gibt die URL selbst ein, es gibt keine fremde Quelle. Der bisherige Stand hat ebenfalls keine URL-Schema-Prüfung; eine solche einzuführen wäre eine Verhaltensänderung außerhalb des Scopes dieser verhaltensneutralen Phase. Als Nebenbefund im SUMMARY festhalten. |
| T-13-08 | Information Disclosure | Doppeltes `data-id`-Attribut | low | mitigate | Ersatzlos entfernt; kein Datenfluss betroffen, aber die Stelle liest sich nicht mehr, als würden zwei verschiedene Ids gemeint sein. |
| T-13-SC | Tampering | npm/pip/cargo installs | low | accept | Dieser Plan installiert kein Paket (`13-RESEARCH.md` → „Package Legitimacy Audit: Nicht anwendbar"). |
</threat_model>

<verification>
1. `grep -rn 'document\.execCommand' --include=*.js core features systems ui utils render loader.js | wc -l` liefert `0` (Erfolgskriterium 6)
2. `grep -c "execCommand" utils/basic.js` liefert weiterhin `3` — die Kommentare sind unangetastet
3. `npx jest tests/unit/wiki-links.test.js` grün
4. `npx jest` vollständig grün (31 Suiten)
5. `PYTHONIOENCODING=utf-8 python build.py && npx playwright test tests/e2e/features/editor-formatting.spec.js tests/e2e/features/editor-floating.spec.js tests/e2e/features/editor-insert.spec.js tests/e2e/features/editor-smoke.spec.js tests/e2e/features/wiki.spec.js` grün
</verification>

<success_criteria>
- Erfolgskriterium 1 (zweite Hälfte): die Regex-Capture in `parseWikiLinks()` ist escapt
- Erfolgskriterium 6: keine Aufrufe der abgelösten Editier-Kommando-API außerhalb von Kommentaren
- Erfolgskriterium 7 (Teil): das doppelte `data-id` ist weg
- Kein Testfall der bestehenden Suiten wird rot
</success_criteria>

<output>
Create `.planning/phases/13-h-rtung-wartbarkeit/13-02-SUMMARY.md` when done
</output>
