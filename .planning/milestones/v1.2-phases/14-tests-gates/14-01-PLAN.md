---
phase: 14-tests-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - systems/avatars.js
  - .planning/phases/14-tests-gates/14-GATE-BASELINE.md
autonomous: true
requirements: [TEST-05]

estimate:
  tokens: 25000
  raw_tokens: 50000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "`npx eslint .` endet mit Exit 0 (0 Fehler); die CI-Kette hinter `lint-and-typecheck` kann wieder anlaufen."
    - "Der Steuerzeichen-Strip in `validateAvatarURL()` ist unverändert wirksam: `tests/unit/avatars.test.js` bleibt mit allen 14 Tests grün."
    - "Die Ausnahme in `systems/avatars.js` gilt für genau eine Zeile (`eslint-disable-next-line`), nicht für die Datei und nicht für das Projekt."
    - "Ein eingechecktes Messprotokoll hält die Vor-Schärfungs-Zahlen fest, auf die die Ratschen der Pläne 14-06/14-07/14-08/14-09 später gepinnt werden."
  artifacts:
    - systems/avatars.js
    - .planning/phases/14-tests-gates/14-GATE-BASELINE.md
  key_links:
    - "`systems/avatars.js` → `npx eslint .` Exit-Code → CI-Job `lint-and-typecheck` → `needs:`-Kette (`e2e`, `build`, `smoke-test`, `deploy`)"
    - "`14-GATE-BASELINE.md` → die gemessenen Startwerte für D-10 (`--max-warnings`), D-11 (`include`-Zulassungsliste), D-13 (Ausnahmeliste), D-14 (`coverageThreshold`)"
  prohibitions:
    - "Der Steuerzeichen-Strip in `validateAvatarURL()` darf nicht abgeschwächt, umgeschrieben oder durch eine vermeintlich lint-freundlichere Variante ersetzt werden — D-07 schaltet ausschließlich den Linter an dieser einen Zeile stumm."
    - "Die Regel `no-control-regex` darf nicht projektweit oder dateiweit abgeschaltet werden, um den Fehler loszuwerden."
---

<objective>
Den seit dem 2026-09-05 roten Lint-Lauf grün stellen (D-07) und die Vor-Schärfungs-Messwerte
dieser Phase als eingechecktes Protokoll festhalten.

Purpose: `npm run lint` bricht seit Commit `d2a521c` (WR-01, Phase 12) mit Exit 1 ab. Der CI-Job
`lint-and-typecheck` ist damit rot, und `e2e`, `build`, `smoke-test` und `deploy` hängen per
`needs:` daran. Jede weitere Gate-Arbeit dieser Phase liefe sonst in einer Pipeline, die vorher
nicht durchläuft — deshalb schreibt D-15 diesen Fix als allererste, eigene Aufgabe fest.
Der zweite Task erhebt in einem Zug alle Zahlen, auf die die späteren Ratschen gepinnt werden;
CONTEXT.md und RESEARCH.md verlangen beide ausdrücklich frische Messung statt Übernahme
(RESEARCH.md weicht bei der `tsc --checkJs`-Zahl deutlich von CONTEXT.md ab), und das Projekt hat
mit `13-PERF-MEASUREMENT.md` bereits die Form dafür.

Output: `systems/avatars.js` mit einer begründeten Einzelzeilen-Ausnahme;
`.planning/phases/14-tests-gates/14-GATE-BASELINE.md` mit den reproduzierbaren Befehlen und ihren
Ergebnissen.

**Formhinweis zur Tracer-Ordnung:** Der Tracer dieser Phase (Ende-zu-Ende-Pfad
`loader.js MODULES` → Generator → Artefakt → `eslint.config.js` → `npm run lint` → Drift-Test)
liegt bewusst in Plan 14-02, nicht hier. D-07 ist als *eigene erste Aufgabe* gesetzt (D-15) und
hat Vorrang vor der Tracer-Positionierung; er ist die Voraussetzung dafür, dass der Tracer
überhaupt einen grünen Lint-Lauf als Nachweis benutzen kann.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-tests-gates/14-CONTEXT.md
@.planning/phases/14-tests-gates/14-RESEARCH.md
@.planning/phases/14-tests-gates/14-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Roten `no-control-regex`-Fehler gezielt und begründet stumm schalten (D-07)</name>
  <files>systems/avatars.js</files>
  <read_first>
    - `systems/avatars.js` (Zeilen 1–40) — die Funktion `validateAvatarURL()` und der Steuerzeichen-Strip, der den Fehler auslöst
    - `tests/unit/avatars.test.js` — das vorhandene Regressionsnetz für genau diesen Fix (14 Tests, WR-01)
    - `eslint.config.js` (Zeilen 89–112) — der globale Regelblock und die dort bereits vorhandene begründete Ausnahme zu `no-misleading-character-class` (Zeile 109) als Kommentar-Konvention
  </read_first>
  <action>
    Direkt über der Zeile mit dem Steuerzeichen-Strip in `validateAvatarURL()` eine
    `eslint-disable-next-line no-control-regex`-Zeile einfügen, versehen mit einer Begründung im
    Stil der bestehenden Ausnahme in `eslint.config.js:109` (Grund + Referenz in derselben Zeile).
    Die Begründung muss zwei Dinge nennen: dass die Zeichenklasse die Steuerzeichen absichtlich
    trifft, und die Herkunft WR-01 / Phase 12 / Commit `d2a521c`.

    Der reguläre Ausdruck selbst bleibt **unverändert** — Zeichenklasse, Flags und
    Ersetzungsziel werden nicht angefasst. Es wird nichts an der Datei umgestellt, kein
    `String.fromCharCode`-Umbau, keine Aufspaltung in mehrere `replace`-Aufrufe, keine
    Umstellung auf ein `u`-Flag. Der Linter wird stumm geschaltet, nicht der Schutz.

    Die Regel `no-control-regex` bleibt in `eslint.config.js` unangetastet und weiterhin für
    den gesamten Rest des Projekts aktiv; `eslint.config.js` wird in diesem Task nicht
    geändert.
  </action>
  <verify>
    <automated>npx jest tests/unit/avatars.test.js</automated>
    <fails_when>non-zero exit, oder die Zusammenfassung nennt weniger als 14 bestandene Tests</fails_when>
    <automated>node --check systems/avatars.js</automated>
    <fails_when>non-zero exit, oder stderr enthält "SyntaxError"</fails_when>
    <automated>npx eslint systems/avatars.js</automated>
    <fails_when>non-zero exit, oder die Ausgabe enthält "no-control-regex"</fails_when>
    <automated>test "$(grep -c 'eslint-disable' systems/avatars.js)" -eq 1</automated>
    <fails_when>non-zero exit — es steht keine oder mehr als eine Disable-Zeile in der Datei (dateiweite oder mehrfache Abschaltung)</fails_when>
    <automated>test "$(grep -c 'eslint-disable-next-line no-control-regex' systems/avatars.js)" -eq 1</automated>
    <fails_when>non-zero exit — die Ausnahme ist nicht auf genau eine Folgezeile begrenzt</fails_when>
  </verify>
  <acceptance_criteria>
    - `npx eslint systems/avatars.js` gibt weder Fehler noch eine `no-control-regex`-Meldung aus.
    - `systems/avatars.js` enthält genau eine `eslint-disable`-Zeile, und diese ist die
      `-next-line`-Variante für `no-control-regex`.
    - Die Begründungszeile nennt sowohl `WR-01` als auch den Commit `d2a521c`.
    - `git diff systems/avatars.js` zeigt ausschließlich hinzugefügte Kommentar-/Disable-Zeilen —
      keine geänderte Zeile innerhalb des regulären Ausdrucks.
    - `eslint.config.js` steht unverändert in `git status`.
    - `npx jest tests/unit/avatars.test.js` meldet 14 bestandene Tests.
  </acceptance_criteria>
  <done>`npx eslint .` endet mit 0 Fehlern; der Steuerzeichen-Strip funktioniert nachweislich unverändert.</done>
  <reversibility rating="reversible">Eine Disable-Zeile lässt sich jederzeit ersatzlos entfernen; der geschützte Code bleibt unberührt.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Vor-Schärfungs-Messprotokoll erheben und einchecken</name>
  <files>.planning/phases/14-tests-gates/14-GATE-BASELINE.md</files>
  <read_first>
    - `.planning/phases/13-h-rtung-wartbarkeit/13-PERF-MEASUREMENT.md` — die Form, der dieses Protokoll folgt (Befehl, Rohausgabe, Datum, Interpretation)
    - `.planning/phases/14-tests-gates/14-RESEARCH.md` § „Measured Baseline — Re-Verified" und § „Abweichung im Detail: `tsc --checkJs`-Fehlerzahl" — die beiden Zahlen, die zwischen CONTEXT.md und RESEARCH.md auseinanderlaufen
    - `jest.config.cjs` (Zeilen 6, 45–58, 65–72) — `roots`, `collectCoverageFrom`, `coverageThreshold`
    - `tsconfig.json` (Zeilen 10–11, 46–56) — `allowJs`/`checkJs`, `include`/`exclude`
    - `loader.js` — das `MODULES`-Array als einzige Modulliste (ARCH-01)
  </read_first>
  <action>
    `.planning/phases/14-tests-gates/14-GATE-BASELINE.md` anlegen. Die Datei ist ein
    Messprotokoll: je Messung der exakt ausgeführte Befehl, das Datum, die relevanten Zahlen und
    ein Satz Interpretation. Keine Zahl aus CONTEXT.md oder RESEARCH.md übernehmen — alle Werte in
    diesem Task frisch erheben. Weichen sie von den dort dokumentierten ab, beide Werte
    nebeneinander stellen und die eigene Messung als maßgeblich kennzeichnen.

    Fünf Messblöcke, in dieser Reihenfolge:

    1. **Lint-Reststand nach D-07.** `npx eslint . --format json` in eine Datei schreiben und
       auswerten: Gesamtfehler, Gesamtwarnungen, Aufschlüsselung nach `ruleId` (absteigend),
       davon `no-undef`-Vorkommen und Anzahl eindeutiger Namen. Zusätzlich: Warnungen ohne
       `no-undef` — das ist der Kandidat für die spätere `--max-warnings`-Ratsche aus D-10.
       Die JSON-Datei ist ein Zwischenergebnis und wird **nicht** eingecheckt.
    2. **`tsc --checkJs`-Erhebung.** Eine temporäre Konfiguration `tsconfig.checkjs-probe.json`
       anlegen, die `./tsconfig.json` per `extends` erbt und `checkJs: true`, `noEmit: true`,
       `emitDeclarationOnly: false` überschreibt; `npx tsc -p tsconfig.checkjs-probe.json`
       ausführen. Protokollieren: Gesamtfehlerzahl, Aufschlüsselung nach TS-Code (absteigend),
       Anzahl geprüfter Dateien, und — für D-11 entscheidend — die **vollständige alphabetische
       Liste der Dateien ohne einen einzigen Fehler**. Die Probe-Konfiguration nach der Messung
       wieder löschen, sie wird nicht eingecheckt.
    3. **Coverage vor und nach dem `roots`-Fix.** `npx jest --coverage` (heutiger Stand) und
       `npx jest --coverage --roots=.` (Ad-hoc-Override, ohne `jest.config.cjs` anzufassen).
       Je Lauf protokollieren: Anzahl Suiten und Tests, instrumentierte Module, Statements
       gedeckt/gesamt, Branches, Functions, Lines. Für `utils/testable-utils.js` zusätzlich die
       vier Einzelwerte (Statements/Branches/Functions/Lines) — das sind die Zielwerte für D-14.
    4. **Modul-zu-Test-Abdeckung.** Die Modulliste aus `loader.js MODULES` extrahieren und für
       drei Kriterien zählen, wie viele Module *keine* Erwähnung in einer Datei unter `tests/`
       finden: (a) der `loader.js`-relative Pfad kommt wörtlich vor, (b) der Basisname ohne
       `.js` kommt als Teilzeichenkette vor, (c) der Basisname mit Wortgrenzen. Die drei Zahlen
       nebeneinander protokollieren, damit die Kriteriumswahl in 14-09 belegt statt behauptet ist.
       Ausdrücklich mit protokollieren: die elf Module unter `features/session-prep/`,
       `features/npc-generator/`, `features/timeline/`, `features/reise/`, `features/fraktionen/`
       und ob sie unter Kriterium (a) bzw. (c) heute als abgedeckt gelten.
    5. **Testsummen der beiden Sammeldateien.** `npx jest tests/unit/welt-story.test.js` und
       `npx playwright test tests/e2e/features/welt-story.spec.js --list` — die beiden Summen sind
       die binären Abnahmewerte für D-05 in 14-04/14-05. Der Playwright-Aufruf braucht
       `PYTHONIOENCODING=utf-8 python build.py` nicht, weil `--list` keine Seite lädt.

    Am Ende der Datei einen Abschnitt „Offene Divergenzen" mit je einem Absatz zu den beiden
    Stellen, an denen RESEARCH.md und CONTEXT.md auseinanderlaufen (`tsc --checkJs`-Fehlerzahl,
    `vm`-Ladepfad-Zahl 28 vs. 30), und der Angabe, welche Zahl dieses Protokoll als maßgeblich
    setzt.
  </action>
  <verify>
    <automated>test -f .planning/phases/14-tests-gates/14-GATE-BASELINE.md</automated>
    <fails_when>non-zero exit — das Protokoll wurde nicht angelegt</fails_when>
    <automated>test "$(grep -c '^## ' .planning/phases/14-tests-gates/14-GATE-BASELINE.md)" -ge 6</automated>
    <fails_when>non-zero exit — weniger als sechs Abschnitte, also fehlt mindestens einer der fünf Messblöcke oder der Divergenz-Abschnitt</fails_when>
    <automated>test ! -f tsconfig.checkjs-probe.json</automated>
    <fails_when>non-zero exit — die temporäre Probe-Konfiguration wurde nicht wieder entfernt</fails_when>
    <automated>npx jest tests/unit/welt-story.test.js</automated>
    <fails_when>non-zero exit, oder die Zusammenfassung meldet eine andere Zahl als 37 bestandene Tests</fails_when>
  </verify>
  <acceptance_criteria>
    - `14-GATE-BASELINE.md` enthält für jede der fünf Messungen den wörtlich ausgeführten Befehl
      und das Ergebnis; kein Wert ist mit „laut CONTEXT.md" oder „laut RESEARCH.md" belegt.
    - Der Abschnitt zur `tsc`-Messung enthält eine namentliche Liste der fehlerfreien Dateien,
      eine pro Zeile — diese Liste ist der Eingang für 14-07.
    - Der Abschnitt zu `utils/testable-utils.js` nennt vier getrennte Prozentwerte.
    - Der Abschnitt zur Modul-zu-Test-Abdeckung nennt drei Zahlen für drei benannte Kriterien
      und beziffert die elf Welt-Module getrennt.
    - Der Abschnitt „Offene Divergenzen" nennt für beide Divergenzen die eigene Messung als
      maßgeblich und begründet das in einem Satz.
    - `git status` zeigt weder `tsconfig.checkjs-probe.json` noch eine ESLint-JSON-Datei.
  </acceptance_criteria>
  <done>Alle Zahlen, auf die 14-06 bis 14-09 ihre Ratschen pinnen, stehen mit Befehl und Datum in einer eingecheckten Datei.</done>
</task>

</tasks>

## Artifacts this phase produces

Neu entstehende Symbole, Dateien, Skripte und Konfigurationsschlüssel dieser Phase (phasenweit,
nicht nur dieser Plan) — für die Quellen-Erdung nachgelagerter Prüfungen:

- Funktionen: `seedCleanSession`, `generateGlobalsFromModules`, `extractModulesFromLoader`,
  `renderGlobalsArtifact`
- Konstanten: `MODULE_TEST_EXCEPTIONS`
- Neue Dateien: `tools/generate-eslint-globals.js`, `eslint.generated-globals.js`,
  `tsconfig.strict.json`, `tests/unit/eslint-globals-freshness.test.js`,
  `tests/unit/module-test-coverage.test.js`,
  `tests/e2e/features/{session-prep,npc-generator,timeline,reise,fraktionen}.spec.js`,
  `tests/unit/{session-prep,npc-generator,timeline,reise,fraktionen}.test.js`
- Neue npm-Skripte: `typecheck:strict`, `globals:generate`
- Geänderte npm-Skripte: `lint` (Ratsche), `check` (um den strikten Typecheck erweitert);
  entfallendes Skript: `lint:all`
- Messprotokolle: `.planning/phases/14-tests-gates/14-GATE-BASELINE.md`,
  `.planning/phases/14-tests-gates/14-TOAST-RACE-MEASUREMENT.md`
- Entfallende Dateien: `tests/e2e/features/welt-story.spec.js`, `tests/unit/welt-story.test.js`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nutzer-URL → `validateAvatarURL()` | Frei eingegebener Text aus dem Avatar-Dialog kreuzt hier die Grenze zur Protokollprüfung; der Steuerzeichen-Strip ist die Prüfung selbst. |
| Quelldatei → Linter-Konfiguration | Eine Ausnahme im Quelltext entscheidet, welche Regel für welchen Umfang nicht mehr greift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering | `systems/avatars.js` Steuerzeichen-Strip | high | mitigate | Der reguläre Ausdruck wird nicht angefasst; `tests/unit/avatars.test.js` (14 Tests, WR-01-Bypässe wie `java\tscript:`) läuft als Pflicht-Verify des Tasks und beweist die unveränderte Wirkung. |
| T-14-02 | Tampering | Reichweite der Lint-Ausnahme | high | mitigate | Ausnahme ausschließlich als `eslint-disable-next-line`; zwei Zählprüfungen (`grep -c 'eslint-disable' == 1` und `grep -c 'eslint-disable-next-line no-control-regex' == 1`) schließen eine datei- oder projektweite Abschaltung aus. `eslint.config.js` bleibt in diesem Plan unverändert. |
| T-14-03 | Repudiation | Messprotokoll | low | accept | Das Protokoll ist ein eingechecktes Artefakt mit Befehl und Datum; Manipulation wäre in der Git-Historie sichtbar. Kein weiterer Schutz nötig. |
| T-14-SC | Tampering | npm/pip/cargo-Installationen | high | mitigate | Diese Phase installiert **kein** Paket. RESEARCH.md § „Package Legitimacy Audit" hält fest, dass alle Werkzeuge bereits vorhanden sind; die einzige optionale Ergänzung (`globals`) wird bewusst nicht übernommen — die 8 Browser- und 6 Node-Namen werden in 14-02 von Hand eingetragen. Kein Installationsschritt, damit kein Legitimitäts-Checkpoint. |
</threat_model>

<verification>
- `npx eslint .` endet mit Exit 0 (0 Fehler). Die Warnungszahl darf sich gegenüber dem
  Ausgangsstand nicht erhöht haben.
- `npx jest tests/unit/avatars.test.js` grün (14 Tests).
- `npx jest` (volle Unit-Suite) unverändert grün.
- `.planning/phases/14-tests-gates/14-GATE-BASELINE.md` existiert und ist eingecheckt.
</verification>

<success_criteria>
- Der seit 2026-09-05 rote Lint-Lauf ist grün, ohne dass eine Regel projekt- oder dateiweit
  abgeschaltet wurde.
- Der WR-01-Schutz ist durch einen laufenden Test belegt, nicht durch eine Behauptung.
- Alle Ratschen-Startwerte der Pläne 14-06 bis 14-09 sind gemessen, datiert und eingecheckt.
</success_criteria>

## Planer-Annahmen und Hinweise

- **Kanon-Breadcrumb (Prohibition-Probe):** Generische OWASP-/Injection-/Prototype-Pollution-Punkte
  sind Kanon und werden von `/gsd-secure-phase` plus dem `<threat_model>` oben abgedeckt; sie
  werden hier nicht als eigene Prohibition geprägt.
- **Abweichung gegenüber CONTEXT.md `<measured_baseline>`:** CONTEXT.md nennt sechs tote
  Aktionsziele und stuft `core/init.js:106` als „harmlos, weil `typeof`-abgesichert" ein. Gegen den
  Live-Baum gemessen meldet ESLint diese Stelle sehr wohl (der *Aufruf* hinter der Prüfung ist
  ungeschützt). Es sind damit **sieben** Fundstellen, nicht sechs — behandelt in 14-06.

<output>
Create `.planning/phases/14-tests-gates/14-01-SUMMARY.md` when done
</output>
