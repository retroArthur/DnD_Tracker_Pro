---
phase: 14-tests-gates
plan: 05
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - tests/unit/session-prep.test.js
  - tests/unit/npc-generator.test.js
  - tests/unit/timeline.test.js
  - tests/unit/reise.test.js
  - tests/unit/fraktionen.test.js
  - tests/unit/welt-story.test.js
autonomous: true
requirements: [TEST-04]

estimate:
  tokens: 40000
  raw_tokens: 80000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "Session-Prep, NPC-Generator, Timeline, Reise und Fraktionen haben je eine eigene Unit-Testdatei unter `tests/unit/`, benannt nach dem Quellverzeichnis."
    - "Die Summe der Unit-Tests ueber die fuenf neuen Dateien ist exakt gleich der Testzahl der abgeloesten Sammeldatei — 37, gemessen und in `14-GATE-BASELINE.md` protokolliert."
    - "`tests/unit/welt-story.test.js` existiert nicht mehr."
    - "Das lokale Setup jedes Blocks — die jeweils eigene `makeMockD()`-Hilfsfunktion und die eigenen `global.*`-Stubs — wandert mit in die Zieldatei und wird NICHT in eine gemeinsame Setup-Datei gehoben."
    - "Kein Test wurde beim Verschieben veraendert, uebersprungen oder hinzugefuegt; deutsche Testnamen und `// ====`-Sektionsbanner sind woertlich erhalten."
  artifacts:
    - tests/unit/session-prep.test.js
    - tests/unit/npc-generator.test.js
    - tests/unit/timeline.test.js
    - tests/unit/reise.test.js
    - tests/unit/fraktionen.test.js
  key_links:
    - "`tests/unit/welt-story.test.js` (5 `describe`-Bloecke mit je eigenem `makeMockD()`) -> je eine Zieldatei mit demselben Block und demselben lokalen Setup"
  prohibitions:
    - "Beim Aufteilen darf kein Test verloren gehen, uebersprungen oder abgeschwaecht werden, um die Aufteilung gruen zu bekommen."
    - "Das lokale Setup darf nicht zusammengefuehrt oder in `tests/setup.js` verschoben werden — das waere eine Strukturaenderung ueber den Schnitt hinaus."
---

<objective>
Die Unit-Sammeldatei `tests/unit/welt-story.test.js` (593 Zeilen, 37 Tests, fuenf
`describe`-Bloecke) in fuenf dedizierte Dateien aufteilen — mechanisch und verhaltensneutral,
als zweite Haelfte von `DEBT-28`.

Purpose: `DEBT-28` nennt ausdruecklich beide Sammeldateien. Nur die E2E-Haelfte zu teilen (Plan
14-04) liesse mit dieser Datei — 593 Zeilen, fuenf Domaenen — genau das stehen, worueber das
Requirement klagt (D-04). Beide Dateien sind aus Phase-5-Wave-0-Stubs gewachsen; jeder Block
traegt sein eigenes lokales Setup und ist in sich geschlossen, das Verschieben braucht keine
Zusammenfuehrung (D-05).

Output: fuenf neue Unit-Testdateien, benannt nach dem Quellverzeichnis; die Sammeldatei entfaellt.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-tests-gates/14-CONTEXT.md
@.planning/phases/14-tests-gates/14-PATTERNS.md
@.planning/phases/14-tests-gates/14-GATE-BASELINE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Die fuenf `describe`-Bloecke samt lokalem Setup in fuenf Zieldateien uebernehmen</name>
  <files>tests/unit/session-prep.test.js, tests/unit/npc-generator.test.js, tests/unit/timeline.test.js, tests/unit/reise.test.js, tests/unit/fraktionen.test.js</files>
  <read_first>
    - `tests/unit/welt-story.test.js` — vollstaendig; besonders der Dateikopf (Zeilen 1-14) und die fuenf Blockgrenzen bei den Zeilen 15, 109, 192, 301, 500
    - `tests/unit/avatars.test.js` (Zeilen 1-40) — die Kopfkommentar- und Ladekonvention des Projekts fuer Unit-Tests
    - `.planning/phases/14-tests-gates/14-GATE-BASELINE.md` — die protokollierte Ausgangs-Testzahl der Sammeldatei
  </read_first>
  <action>
    Fuenf neue Dateien unter `tests/unit/` anlegen. Feste Zuordnung, nach Quellverzeichnis
    benannt: Block `WELT-01: Session-Prep-Assistent` (ab Zeile 15) nach `session-prep.test.js`;
    `WELT-02: NPC-Generator` (ab Zeile 109) nach `npc-generator.test.js`;
    `WELT-03: Kampagnen-Timeline` (ab Zeile 192) nach `timeline.test.js`;
    `WELT-04: Reise- & Wetter-Simulator` (ab Zeile 301) nach `reise.test.js`;
    `WELT-05: Fraktionen & Ruf-System` (ab Zeile 500 bis Dateiende) nach `fraktionen.test.js`.

    Jeder Block bringt sein eigenes lokales Setup mit: eine eigene `makeMockD()`-Hilfsfunktion
    und eigene `global.*`-Stubs, die innerhalb des Blocks definiert sind. Dieses Setup wandert
    vollstaendig mit in die jeweilige Zieldatei. Es wird ausdruecklich NICHT in eine gemeinsame
    Setup-Datei extrahiert und nicht nach `tests/setup.js` gehoben — das waere bereits eine
    Struktur- und Verhaltensaenderung ueber den Schnitt hinaus und von D-05 ausgeschlossen.
    Tragen zwei Bloecke gleichnamige, aber unterschiedliche Hilfsfunktionen, ist das nach dem
    Schnitt kein Problem mehr, weil sie in getrennten Dateien liegen; sie werden trotzdem nicht
    angeglichen.

    Jede Zieldatei erhaelt einen Dateikopf-Kommentar im Stil der Quelldatei, auf den jeweiligen
    Bereich zugeschnitten, mit dem Hinweis auf die Herkunft aus den Phase-5-Wave-0-Stubs. Danach
    der `// ====`-Sektionsbanner aus der Quelle und der `describe`-Block Zeichen fuer Zeichen
    unveraendert. Deutsche Testnamen bleiben woertlich.

    Die Blockbeschriftungen werden nicht umbenannt. Dass der dritte Bereich hier
    „Kampagnen-Timeline" und auf der E2E-Seite „Kalender-Tab" heisst, ist eine bestehende
    Inkonsistenz; der Dateiname ist in beiden Faellen `timeline`, weil das Quellverzeichnis
    `features/timeline/` heisst.

    Innerhalb der Bloecke wird nichts angefasst: keine Assertion, kein `test.skip`/`it.todo`,
    keine Umbenennung, kein neuer Test. Nach dem Anlegen die Testzahl je Datei einzeln zaehlen;
    die Summe muss die Ausgangszahl aus `14-GATE-BASELINE.md` treffen. Die Quelldatei bleibt in
    diesem Task noch bestehen, damit ein direkter Vergleich moeglich ist.
  </action>
  <verify>
    <automated>test "$(ls tests/unit/session-prep.test.js tests/unit/npc-generator.test.js tests/unit/timeline.test.js tests/unit/reise.test.js tests/unit/fraktionen.test.js | wc -l)" -eq 5</automated>
    <fails_when>non-zero exit — mindestens eine der fuenf Zieldateien fehlt</fails_when>
    <automated>node -e "const fs=require('fs');const n=['session-prep','npc-generator','timeline','reise','fraktionen'];let bad=[];for(const x of n){const s=fs.readFileSync('tests/unit/'+x+'.test.js','utf8');const d=(s.match(/^describe\(/gm)||[]).length;if(d!==1)bad.push(x+':'+d);}if(bad.length){console.log('nicht genau ein describe-Block:',bad.join(', '));process.exit(1)}console.log('je genau ein describe-Block')"</automated>
    <fails_when>non-zero exit, oder die Ausgabe beginnt mit "nicht genau ein describe-Block:"</fails_when>
    <automated>npx jest tests/unit/session-prep.test.js tests/unit/npc-generator.test.js tests/unit/timeline.test.js tests/unit/reise.test.js tests/unit/fraktionen.test.js</automated>
    <fails_when>non-zero exit, oder die Zusammenfassung nennt eine andere Zahl bestandener Tests als die in 14-GATE-BASELINE.md protokollierte Testzahl der Sammeldatei</fails_when>
    <automated>test "$(grep -l 'makeMockD' tests/unit/session-prep.test.js tests/unit/npc-generator.test.js tests/unit/timeline.test.js tests/unit/reise.test.js tests/unit/fraktionen.test.js | wc -l)" -ge 1</automated>
    <fails_when>non-zero exit — das lokale Setup wurde beim Schnitt nicht mituebernommen</fails_when>
  </verify>
  <acceptance_criteria>
    - Es existieren genau fuenf neue Dateien mit den festgelegten Namen; jede enthaelt genau einen `describe`-Block auf oberster Ebene.
    - `npx jest` ueber die fuenf Dateien meldet dieselbe Testgesamtzahl wie die Sammeldatei vor der Aufteilung.
    - Jede Zieldatei enthaelt ihr eigenes lokales Setup; keine importiert eine neu angelegte gemeinsame Setup-Datei.
    - `tests/setup.js` steht unveraendert in `git status`.
    - Die Zahl der `test.skip`/`it.todo`-Vorkommen ueber die fuenf Dateien ist nicht groesser als in der Quelldatei.
  </acceptance_criteria>
  <done>Fuenf dedizierte Unit-Dateien existieren, die Testsumme ist unveraendert, jedes lokale Setup ist bei seinem Block geblieben.</done>
</task>

<task type="auto">
  <name>Task 2: Sammeldatei entfernen und Neutralitaet binaer belegen</name>
  <files>tests/unit/welt-story.test.js</files>
  <read_first>
    - `tests/unit/welt-story.test.js` — die Datei, die entfaellt; letzte Kontrolle, dass jeder Block ein Ziel gefunden hat
    - `.planning/phases/14-tests-gates/14-GATE-BASELINE.md` — der protokollierte Ausgangswert der Unit-Testsumme und der Suitenzahl
    - `jest.config.cjs` (Zeilen 6-16) — `roots` und `testMatch`, um zu bestaetigen, dass die neuen Dateien ohne Konfigurationsaenderung gefunden werden
  </read_first>
  <action>
    Vor dem Loeschen die Summe der `test(`- und `it(`-Vorkommen ueber die fuenf Zieldateien gegen
    die Quelldatei stellen und je Block einzeln vergleichen. Bei Abweichung wird nicht geloescht,
    sondern der Unterschied gesucht.

    Danach `tests/unit/welt-story.test.js` mit `git rm` entfernen — nicht auskommentieren, nicht
    umbenennen, nicht in ein Archivverzeichnis schieben. Eine liegengelassene Kopie wuerde
    dieselben 37 Tests ein zweites Mal fahren und die Zaehlprobe dauerhaft verfaelschen.

    `jest.config.cjs` wird in diesem Plan nicht angefasst; die `roots`-Reparatur gehoert zu Plan
    14-08. Die neuen Dateien liegen im selben Verzeichnis wie die alte und werden vom bestehenden
    `testMatch` erfasst.

    Abschliessend die volle Unit-Suite fahren und Suiten- wie Testzahl mit dem Ausgangswert
    vergleichen: die Testzahl muss identisch sein, die Suitenzahl um vier hoeher (eine Datei
    wurde zu fuenf).
  </action>
  <verify>
    <automated>test ! -f tests/unit/welt-story.test.js</automated>
    <fails_when>non-zero exit — die Sammeldatei liegt noch im Arbeitsbaum</fails_when>
    <automated>test "$(grep -rl 'welt-story' tests/unit/ | wc -l)" -eq 0</automated>
    <fails_when>non-zero exit — unter `tests/unit/` wird die entfallene Datei noch referenziert</fails_when>
    <automated>npx jest</automated>
    <fails_when>non-zero exit, oder die Zusammenfassung nennt eine andere Gesamtzahl bestandener Tests als der in 14-GATE-BASELINE.md protokollierte Ausgangswert</fails_when>
    <automated>git diff --quiet -- jest.config.cjs tests/setup.js</automated>
    <fails_when>non-zero exit — Jest-Konfiguration oder gemeinsames Setup wurden entgegen D-05 veraendert</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/unit/welt-story.test.js` ist per `git rm` entfernt und erscheint in `git status` als geloescht.
    - `npx jest` meldet dieselbe Gesamtzahl bestandener Tests wie vor der Aufteilung und vier Suiten mehr.
    - `jest.config.cjs` und `tests/setup.js` stehen unveraendert in `git status`.
    - Unter `tests/unit/` gibt es keine Referenz mehr auf den alten Dateinamen.
  </acceptance_criteria>
  <done>Die Sammeldatei ist weg, die fuenf dedizierten Dateien tragen dieselbe Testmenge, die volle Unit-Suite ist unveraendert gruen.</done>
  <reversibility rating="reversible">Reiner Dateischnitt; die Ausgangsdatei bleibt in der Git-Historie erhalten.</reversibility>
</task>

</tasks>

## Artifacts this phase produces

Neu entstehende Symbole, Dateien, Skripte und Konfigurationsschluessel dieser Phase (phasenweit):

- Funktionen: `seedCleanSession`, `generateGlobalsFromModules`, `extractModulesFromLoader`,
  `renderGlobalsArtifact`
- Konstanten: `MODULE_TEST_EXCEPTIONS`
- Neue Dateien: `tools/generate-eslint-globals.js`, `eslint.generated-globals.js`,
  `tsconfig.strict.json`, `tests/unit/eslint-globals-freshness.test.js`,
  `tests/unit/module-test-coverage.test.js`, `tests/e2e/features/session-prep.spec.js`,
  `tests/e2e/features/npc-generator.spec.js`, `tests/e2e/features/timeline.spec.js`,
  `tests/e2e/features/reise.spec.js`, `tests/e2e/features/fraktionen.spec.js`,
  `tests/unit/session-prep.test.js`, `tests/unit/npc-generator.test.js`,
  `tests/unit/timeline.test.js`, `tests/unit/reise.test.js`, `tests/unit/fraktionen.test.js`
- Neue npm-Skripte: `globals:generate`, `typecheck:strict`
- Geaenderte npm-Skripte: `lint`, `check`; entfallendes Skript: `lint:all`
- Messprotokolle: `.planning/phases/14-tests-gates/14-GATE-BASELINE.md`,
  `.planning/phases/14-tests-gates/14-TOAST-RACE-MEASUREMENT.md`
- Entfallende Dateien: `tests/e2e/features/welt-story.spec.js`, `tests/unit/welt-story.test.js`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Testdatei-Menge -> Verifikationsumfang | Ein beim Verschieben verlorener Block reduziert die Absicherung still, ohne dass ein Lauf rot wird. |
| Lokales Testsetup -> globales Testsetup | Ein versehentlich nach `tests/setup.js` gehobenes Setup wuerde alle 40+ Unit-Suiten beeinflussen, nicht nur die fuenf neuen. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-13 | Tampering | Testmenge waehrend der Aufteilung | high | mitigate | Binaeres Abnahmekriterium: `npx jest` ueber die fuenf Zieldateien nennt dieselbe Testzahl wie die Quelldatei; die volle Suite ist gegenueber dem protokollierten Ausgangswert unveraendert. Zusaetzlich Zaehlpruefung, dass uebersprungene Tests nicht zugenommen haben. |
| T-14-14 | Elevation of Privilege | Reichweite des Testsetups | medium | mitigate | `tests/setup.js` wird per `git diff --quiet` als unveraendert nachgewiesen; das lokale Setup bleibt in den Zieldateien. |
| T-14-15 | Repudiation | Entfernen der Sammeldatei | low | accept | `git rm` haelt den Vorgang in der Historie fest. |
| T-14-SC | Tampering | npm/pip/cargo-Installationen | high | mitigate | Kein Installationsschritt in diesem Plan. |
</threat_model>

<verification>
- `npx jest` gruen; Testzahl identisch zum Ausgangswert, Suitenzahl plus vier.
- `npx eslint .` weiterhin ohne Fehler.
- `git status` zeigt fuenf neue und eine geloeschte Datei unter `tests/unit/`, sonst nichts.
</verification>

<success_criteria>
- Fuenf Feature-Bereiche haben je eine eigene Unit-Testdatei, benannt nach ihrem
  Quellverzeichnis.
- Die Testsumme ist nachweislich unveraendert.
- Jedes lokale Setup ist bei seinem Block geblieben; `tests/setup.js` ist unberuehrt.
</success_criteria>

## Planer-Annahmen und Hinweise

- **Offene Kanten-Sonde (nicht aufgeloest, ausdruecklich benannt):** Die Sonde zu `TEST-04` blieb
  unklassifiziert und wird hier nicht stillschweigend fallen gelassen. Angenommen wird, dass die
  binaere Zaehlprobe die Abnahmekante dieses Requirements ist. Kommt in der Verifikation eine
  weitere Kante auf, ist sie hier nicht abgedeckt.
- **Wichtiger Nebenbefund fuer Plan 14-09:** Die elf Module unter `features/session-prep/`,
  `features/npc-generator/`, `features/timeline/`, `features/reise/` und `features/fraktionen/`
  werden von den Welt-Tests **nicht per Pfad oder Dateiname referenziert** — die Tests pruefen
  nachgebildete Logik gegen ein Mock-Objekt. Die Aufteilung allein nimmt diese elf Module daher
  NICHT von der Ausnahmeliste des Modul-zu-Test-Gates herunter. CONTEXT.md D-13 erwartet an
  dieser Stelle einen Rueckgang von 75 auf 64; diese Erwartung traegt nicht, und 14-09 misst die
  Liste deshalb selbst neu, statt sie fortzuschreiben. Sie inhaltlich zu schliessen ist der
  bereits zurueckgestellte Posten „Abdeckungsluecken der fuenf Welt-Features schliessen".

<output>
Create `.planning/phases/14-tests-gates/14-05-SUMMARY.md` when done
</output>
