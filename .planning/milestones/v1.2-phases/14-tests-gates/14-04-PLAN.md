---
phase: 14-tests-gates
plan: 04
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - tests/e2e/features/session-prep.spec.js
  - tests/e2e/features/npc-generator.spec.js
  - tests/e2e/features/timeline.spec.js
  - tests/e2e/features/reise.spec.js
  - tests/e2e/features/fraktionen.spec.js
  - tests/e2e/features/welt-story.spec.js
autonomous: true
requirements: [TEST-04]

estimate:
  tokens: 40000
  raw_tokens: 80000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "Session-Prep, NPC-Generator, Timeline, Reise und Fraktionen haben je eine eigene E2E-Spec unter `tests/e2e/features/`, benannt nach dem Quellverzeichnis, nicht nach den WELT-NN-Codes."
    - "Die Summe der E2E-Tests ueber die fuenf neuen Dateien ist exakt gleich der Testzahl der abgeloesten Sammel-Spec — 26, gemessen und in `14-GATE-BASELINE.md` protokolliert."
    - "`tests/e2e/features/welt-story.spec.js` existiert nicht mehr; es gibt keine Datei, die zwei oder mehr der fuenf Welt-Bereiche zugleich abdeckt."
    - "Kein Test wurde beim Verschieben veraendert, uebersprungen oder hinzugefuegt; die `describe`-Beschriftungen und deutschen Testnamen sind woertlich erhalten."
    - "Die selbstgebaute `APP_URL`-Konstante wandert unveraendert mit; sie wird NICHT auf den `loadApp()`-Helfer umgestellt (D-06), weil das eine Timing-Aenderung waere."
  artifacts:
    - tests/e2e/features/session-prep.spec.js
    - tests/e2e/features/npc-generator.spec.js
    - tests/e2e/features/timeline.spec.js
    - tests/e2e/features/reise.spec.js
    - tests/e2e/features/fraktionen.spec.js
  key_links:
    - "`tests/e2e/features/welt-story.spec.js` (5 `test.describe`-Bloecke) -> je eine Zieldatei mit demselben Block und demselben lokalen Setup"
  prohibitions:
    - "Beim Aufteilen darf kein Test verloren gehen, uebersprungen oder abgeschwaecht werden, um die Aufteilung gruen zu bekommen."
    - "Die Aufteilung darf nicht zur Gelegenheit fuer Verbesserungen werden: keine Umstellung auf gemeinsame Helfer, keine neuen Assertionen, keine Timing-Anpassungen."
---

<objective>
Die E2E-Sammel-Spec `tests/e2e/features/welt-story.spec.js` (520 Zeilen, 26 Tests, fuenf
`test.describe`-Bloecke) in fuenf dedizierte Dateien aufteilen — mechanisch und
verhaltensneutral.

Purpose: `TEST-04`/`DEBT-28` verlangt dedizierte Testdateien fuer Timeline, Reise, Fraktionen,
Session-Prep und NPC-Generator. Heute teilen sich fuenf Feature-Bereiche mit zusammen rund 3200
Quellzeilen eine gemeinsame Datei; welcher Bereich wie duenn abgedeckt ist, ist darin nicht
sichtbar. Die Aufteilung macht es sichtbar, ohne die Abdeckung anzufassen — `TEST-04` verlangt
dedizierte Dateien, nicht mehr Tests (D-05). Jeder der fuenf Bloecke traegt sein eigenes lokales
Setup und ist damit in sich geschlossen; das Verschieben braucht keine Zusammenfuehrung.

Output: fuenf neue Spec-Dateien, benannt nach dem Quellverzeichnis
(`features/session-prep/` -> `session-prep.spec.js` und so fort); die Sammeldatei entfaellt.

**Bewusste Abweichung von Erfolgskriterium 2 der Roadmap:** Dort ist von *einer* gemeinsamen
Sammel-Spec die Rede. `DEBT-28` nennt ausdruecklich beide Dateien — die E2E-Haelfte hier, die
Unit-Haelfte in Plan 14-05. Nur eine zu teilen liesse genau das stehen, worueber das Requirement
klagt (D-04).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-tests-gates/14-CONTEXT.md
@.planning/phases/14-tests-gates/14-PATTERNS.md
@.planning/phases/14-tests-gates/14-GATE-BASELINE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Die fuenf `test.describe`-Bloecke woertlich in fuenf Zieldateien uebernehmen</name>
  <files>tests/e2e/features/session-prep.spec.js, tests/e2e/features/npc-generator.spec.js, tests/e2e/features/timeline.spec.js, tests/e2e/features/reise.spec.js, tests/e2e/features/fraktionen.spec.js</files>
  <read_first>
    - `tests/e2e/features/welt-story.spec.js` — vollstaendig; besonders der Dateikopf (Zeilen 1-20, Kommentar, `import { test, expect }`, die selbstgebaute `APP_URL`-Konstante) und die fuenf Blockgrenzen bei den Zeilen 21, 115, 249, 310, 383
    - `tests/e2e/crud/locations.spec.js` (Zeilen 1-20) — die Playwright-Dateikopf-Konvention des Projekts (`// @ts-check`, Importblock, Beschreibungskommentar)
    - `.planning/phases/14-tests-gates/14-GATE-BASELINE.md` — die dort protokollierte Ausgangs-Testzahl der Sammeldatei
  </read_first>
  <action>
    Fuenf neue Dateien unter `tests/e2e/features/` anlegen. Die Zuordnung von Quellblock zu
    Zieldatei ist fest und richtet sich nach dem Quellverzeichnis der jeweiligen Funktion, nicht
    nach den WELT-NN-Codes: Block `WELT-01: Session-Prep-Tab` (ab Zeile 21) nach
    `session-prep.spec.js`; `WELT-02: NPC-Generator` (ab Zeile 115) nach `npc-generator.spec.js`;
    `WELT-03: Kalender-Tab` (ab Zeile 249) nach `timeline.spec.js`; `WELT-04: Reise-Tab` (ab
    Zeile 310) nach `reise.spec.js`; `WELT-05: Fraktionen-Tab` (ab Zeile 383 bis Dateiende) nach
    `fraktionen.spec.js`.

    Die WELT-NN-Codes sind Planungsartefakte aus Phase 5 und sagen niemandem etwas, der den
    Testbaum liest; `features/timeline/` gehoert zu `timeline.spec.js`, auch wenn der Block dort
    „Kalender-Tab" heisst. Die Blockbeschriftung selbst wird NICHT umbenannt — sie ist Testtext
    und faellt unter die Verhaltensneutralitaet aus D-05. Dass E2E den dritten Bereich
    „Kalender-Tab" und die Unit-Seite ihn „Kampagnen-Timeline" nennt, ist eine bestehende
    Inkonsistenz und bleibt bestehen.

    Jede Zieldatei erhaelt: den `import { test, expect } from '@playwright/test';`-Kopf, die
    selbstgebaute `APP_URL`-Konstante woertlich aus Zeile 16 der Quelldatei, den zugehoerigen
    `// ====`-Sektionsbanner-Kommentar aus der Quelle, und danach den `test.describe`-Block
    Zeichen fuer Zeichen unveraendert. Der erklaerende Dateikopf-Kommentar der Quelldatei
    (Zeilen 1-11) wird sinngemaess auf den jeweiligen Bereich zugeschnitten uebernommen, damit
    die Herkunft aus den Phase-5-Wave-0-Stubs dokumentiert bleibt.

    **D-06 ausdruecklich beachten:** die `APP_URL`-Konstante wandert unveraendert mit und wird
    NICHT durch den gemeinsamen `loadApp()`-Helfer ersetzt. `loadApp()` macht mehr als ein
    `goto` — es wartet auf einen Selektor und haengt 500 ms Nachlauf an. Der Wechsel waere eine
    Timing-Aenderung und damit genau die Verhaltensaenderung, die D-05 ausschliesst. Die
    Inkonsistenz ist real, notiert und als eigener Posten zurueckgestellt.

    Innerhalb der Bloecke wird nichts angefasst: keine Assertion, kein `test.skip`, kein
    `waitForTimeout`, keine Umbenennung eines Testnamens, kein neuer Test. Nach dem Anlegen die
    Testzahl je Datei einzeln zaehlen und mit der Blockzahl aus der Quelldatei vergleichen; die
    Summe muss die Ausgangszahl aus `14-GATE-BASELINE.md` treffen.

    Die Quelldatei bleibt in diesem Task noch bestehen, damit ein direkter Vergleich moeglich
    ist. Sie wird in Task 2 entfernt.
  </action>
  <verify>
    <automated>test "$(ls tests/e2e/features/session-prep.spec.js tests/e2e/features/npc-generator.spec.js tests/e2e/features/timeline.spec.js tests/e2e/features/reise.spec.js tests/e2e/features/fraktionen.spec.js | wc -l)" -eq 5</automated>
    <fails_when>non-zero exit — mindestens eine der fuenf Zieldateien fehlt</fails_when>
    <automated>node -e "const fs=require('fs');const n=['session-prep','npc-generator','timeline','reise','fraktionen'];let bad=[];for(const x of n){const s=fs.readFileSync('tests/e2e/features/'+x+'.spec.js','utf8');const d=(s.match(/test\.describe\(/g)||[]).length;if(d!==1)bad.push(x+':'+d);}if(bad.length){console.log('nicht genau ein describe-Block:',bad.join(', '));process.exit(1)}console.log('je genau ein describe-Block')"</automated>
    <fails_when>non-zero exit, oder die Ausgabe beginnt mit "nicht genau ein describe-Block:"</fails_when>
    <automated>npx playwright test tests/e2e/features/session-prep.spec.js tests/e2e/features/npc-generator.spec.js tests/e2e/features/timeline.spec.js tests/e2e/features/reise.spec.js tests/e2e/features/fraktionen.spec.js --list</automated>
    <fails_when>non-zero exit, oder die Auflistung nennt eine andere Gesamtzahl als die in 14-GATE-BASELINE.md protokollierte Testzahl der Sammel-Spec</fails_when>
    <automated>test "$(grep -lc 'test.skip\|test.fixme' tests/e2e/features/session-prep.spec.js tests/e2e/features/npc-generator.spec.js tests/e2e/features/timeline.spec.js tests/e2e/features/reise.spec.js tests/e2e/features/fraktionen.spec.js 2>/dev/null | wc -l)" -le "$(grep -lc 'test.skip\|test.fixme' tests/e2e/features/welt-story.spec.js 2>/dev/null | wc -l)"</automated>
    <fails_when>non-zero exit — in den Zieldateien stehen mehr uebersprungene Tests als in der Quelldatei</fails_when>
  </verify>
  <acceptance_criteria>
    - Es existieren genau fuenf neue Dateien mit den festgelegten Namen; jede enthaelt genau einen `test.describe`-Block.
    - `--list` ueber die fuenf Dateien nennt dieselbe Testgesamtzahl wie die Sammel-Spec vor der Aufteilung.
    - Jede Zieldatei enthaelt die selbstgebaute `APP_URL`-Konstante; keine importiert `loadApp` aus dem Helfermodul.
    - Ein zeilenweiser Vergleich der zusammengesetzten Blockinhalte gegen die Quelldatei zeigt ausser Dateikoepfen keine inhaltliche Abweichung.
    - Die Zahl der `test.skip`/`test.fixme`-Vorkommen ist gegenueber der Quelldatei nicht gestiegen.
  </acceptance_criteria>
  <done>Fuenf dedizierte E2E-Dateien existieren, die Testsumme ist unveraendert, und keine davon nutzt den `loadApp()`-Helfer.</done>
</task>

<task type="auto">
  <name>Task 2: Sammel-Spec entfernen und Neutralitaet der Aufteilung binaer belegen</name>
  <files>tests/e2e/features/welt-story.spec.js</files>
  <read_first>
    - `tests/e2e/features/welt-story.spec.js` — die Datei, die entfaellt; letzte Kontrolle, dass jeder ihrer Bloecke ein Ziel gefunden hat
    - `.planning/phases/14-tests-gates/14-GATE-BASELINE.md` — der protokollierte Ausgangswert der E2E-Testsumme
    - `playwright.config.js` — `testDir`/`testMatch`, um zu bestaetigen, dass die neuen Dateien ohne Konfigurationsaenderung gefunden werden
  </read_first>
  <action>
    Vor dem Loeschen ein letztes Mal gegenpruefen: die Summe der `test(`-Vorkommen ueber die fuenf
    Zieldateien gegen die der Quelldatei, und je Block die Testzahl einzeln. Stimmen sie nicht
    ueberein, wird nicht geloescht, sondern der Unterschied gesucht.

    Danach `tests/e2e/features/welt-story.spec.js` mit `git rm` entfernen — nicht auskommentieren,
    nicht umbenennen, nicht in ein Archivverzeichnis schieben. Die Datei ist ueber die
    Git-Historie erreichbar; eine liegengelassene Kopie waere eine zweite Wahrheit und wuerde die
    Tests doppelt fahren.

    `playwright.config.js` wird nicht angefasst: die neuen Dateien liegen im selben Verzeichnis
    wie die alte und werden vom bestehenden `testMatch` erfasst.

    Abschliessend die volle E2E-Suite nach einem frischen Build fahren und die Gesamtzahl mit dem
    Stand vor dieser Phase vergleichen. Sie muss identisch sein: die Aufteilung hat Dateien
    umgeschichtet, nicht Tests hinzugefuegt oder entfernt.
  </action>
  <verify>
    <automated>test ! -f tests/e2e/features/welt-story.spec.js</automated>
    <fails_when>non-zero exit — die Sammel-Spec liegt noch im Arbeitsbaum</fails_when>
    <automated>test "$(grep -rl 'welt-story' tests/e2e/ | wc -l)" -eq 0</automated>
    <fails_when>non-zero exit — irgendwo unter `tests/e2e/` wird die entfallene Datei noch referenziert</fails_when>
    <automated>PYTHONIOENCODING=utf-8 python build.py && npx playwright test tests/e2e/features/</automated>
    <fails_when>non-zero exit, oder die Zusammenfassung enthaelt "failed"</fails_when>
    <automated>npx playwright test --list</automated>
    <fails_when>non-zero exit, oder die Gesamtzahl weicht von der vor dieser Phase protokollierten E2E-Testzahl ab</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/features/welt-story.spec.js` ist per `git rm` entfernt und taucht in `git status` als geloescht auf.
    - Unter `tests/e2e/` gibt es keine Referenz mehr auf den alten Dateinamen.
    - `npx playwright test --list` nennt insgesamt dieselbe Testzahl wie vor der Aufteilung.
    - `playwright.config.js` steht unveraendert in `git status`.
    - `npx playwright test tests/e2e/features/` ist gruen.
  </acceptance_criteria>
  <done>Die Sammel-Spec ist weg, die fuenf dedizierten Dateien tragen dieselbe Testmenge, die volle E2E-Suite ist unveraendert gruen.</done>
  <reversibility rating="reversible">Die Aufteilung ist ein reiner Dateischnitt; die Ausgangsdatei bleibt in der Git-Historie und liesse sich jederzeit wiederherstellen.</reversibility>
</task>

</tasks>

## Artifacts this phase produces

Neu entstehende Symbole, Dateien, Skripte und Konfigurationsschluessel dieser Phase (phasenweit):

- Funktionen: `seedCleanSession`, `generateGlobalsFromModules`, `extractModulesFromLoader`,
  `renderGlobalsArtifact`
- Konstanten: `MODULE_TEST_EXCEPTIONS`
- Neue Dateien: `tools/generate-eslint-globals.js`, `eslint.generated-globals.js`,
  `tsconfig.strict.json`, `tests/unit/eslint-globals-freshness.test.js`,
  `tests/unit/module-test-coverage.test.js`, `tests/e2e/features/session-prep.spec.js`,
  `tests/e2e/features/npc-generator.spec.js`, `tests/e2e/features/timeline.spec.js`,
  `tests/e2e/features/reise.spec.js`, `tests/e2e/features/fraktionen.spec.js`,
  `tests/unit/session-prep.test.js`, `tests/unit/npc-generator.test.js`,
  `tests/unit/timeline.test.js`, `tests/unit/reise.test.js`, `tests/unit/fraktionen.test.js`
- Neue npm-Skripte: `globals:generate`, `typecheck:strict`
- Geaenderte npm-Skripte: `lint`, `check`; entfallendes Skript: `lint:all`
- Messprotokolle: `.planning/phases/14-tests-gates/14-GATE-BASELINE.md`,
  `.planning/phases/14-tests-gates/14-TOAST-RACE-MEASUREMENT.md`
- Entfallende Dateien: `tests/e2e/features/welt-story.spec.js`, `tests/unit/welt-story.test.js`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Testdatei-Menge -> Verifikationsumfang | Welche Dateien existieren, entscheidet, was die E2E-Suite ueberhaupt prueft; ein beim Verschieben verlorener Block reduziert die Absicherung still. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-11 | Tampering | Testmenge waehrend der Aufteilung | high | mitigate | Binaeres Abnahmekriterium: `--list` ueber die fuenf Zieldateien nennt dieselbe Testzahl wie die Quelldatei, und `--list` ueber die volle Suite ist gegenueber dem Stand vor der Phase unveraendert. Zusaetzlich eine Zaehlpruefung, dass die Zahl uebersprungener Tests nicht gestiegen ist. |
| T-14-12 | Repudiation | Entfernen der Sammel-Spec | low | accept | Die Datei wird per `git rm` entfernt; die Historie bleibt vollstaendig und der Loeschvorgang ist im Commit sichtbar. |
| T-14-SC | Tampering | npm/pip/cargo-Installationen | high | mitigate | Kein Installationsschritt in diesem Plan; es entstehen nur Testdateien aus vorhandenem Inhalt. |
</threat_model>

<verification>
- `npx playwright test tests/e2e/features/` gruen.
- `npx playwright test --list` nennt dieselbe Gesamtzahl wie vor der Phase.
- `npx eslint .` weiterhin ohne Fehler (die neuen Dateien fallen unter den `tests/**`-Block).
- `git status` zeigt fuenf neue und eine geloeschte Datei, sonst nichts unter `tests/e2e/`.
</verification>

<success_criteria>
- Fuenf Feature-Bereiche haben je eine eigene E2E-Datei, benannt nach ihrem Quellverzeichnis.
- Die Testsumme ist nachweislich unveraendert — die Aufteilung ist ein Schnitt, keine Aenderung.
- Die bewusst nicht vereinheitlichte `APP_URL`-Konstante ist erhalten (D-06).
</success_criteria>

## Planer-Annahmen und Hinweise

- **Offene Kanten-Sonde (nicht aufgeloest, ausdruecklich benannt):** Die Sonde zu `TEST-04` blieb
  unklassifiziert. Sie wird hier NICHT stillschweigend fallen gelassen, sondern als Annahme
  gefuehrt: die eigene Abnahmekante dieses Requirements ist die binaere Zaehlprobe (Testzahl
  vorher gleich Testzahl nachher, ueber die je fuenf Zieldateien). Findet die Verifikation eine
  darueber hinausgehende Kante, ist sie hier nicht abgedeckt und gehoert benannt statt
  unterstellt.
- **Zahlenherkunft:** Die Blockgrenzen (Zeilen 21, 115, 249, 310, 383, Dateiende 520) und die
  Testzahl stammen aus `14-PATTERNS.md` und wurden gegen den Live-Baum bestaetigt. Weicht der
  Live-Stand beim Ausfuehren ab, gilt der Live-Stand; die Zuordnung Block zu Zieldatei bleibt
  davon unberuehrt.

<output>
Create `.planning/phases/14-tests-gates/14-04-SUMMARY.md` when done
</output>
