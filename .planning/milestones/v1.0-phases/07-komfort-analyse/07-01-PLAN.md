---
phase: 07-komfort-analyse
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/init.js
  - features/dice/dice-core.js
  - features/soundboard/soundboard-idb.js
  - features/soundboard/soundboard-player.js
  - features/soundboard/soundboard-crud.js
  - features/soundboard/soundboard-render.js
  - features/dice-stats/dice-stats-idb.js
  - features/dice-stats/dice-stats-render.js
  - loader.js
  - build.py
  - assets/templates/header.html
  - assets/templates/view-tools.html
  - assets/styles/tools.css
  - systems/spellslots/navigation.js
  - tests/unit/dice-stats.test.js
  - tests/unit/soundboard.test.js
  - tests/e2e/features/soundboard.spec.js
  - tests/e2e/features/dice-stats.spec.js
autonomous: true
requirements: [UX-01, UX-02]

must_haves:
  truths:
    - "IndexedDB opens at version 4 with audioBlobs and diceStats object stores present"
    - "Every dice roll passing through addToDiceHistory() is written to the diceStats IDB store (D-04, additive — in-memory diceHistory unchanged per D-04a)"
    - "A Soundboard tab and a Würfel-Statistiken tab are reachable from the nav bar and render a defensive placeholder"
    - "All six new feature modules appear in BOTH build.py and loader.js and the bundle builds clean"
    - "Four Wave-0 test stub files exist and run (red/skipped, not erroring)"
  artifacts:
    - path: "core/init.js"
      provides: "IDB_VERSION = 4 + audioBlobs + diceStats stores (sessionId + notation indexes)"
      contains: "audioBlobs"
    - path: "features/dice/dice-core.js"
      provides: "statsIdbPut tee inside addToDiceHistory()"
      contains: "statsIdbPut"
    - path: "features/soundboard/soundboard-idb.js"
      provides: "Module skeleton with window exports (real impl in 07-02)"
    - path: "features/dice-stats/dice-stats-idb.js"
      provides: "statsIdbPut + getAllStats + getStatsForSession skeletons"
      exports: ["statsIdbPut"]
    - path: "tests/unit/dice-stats.test.js"
      provides: "Wave-0 unit stub for UX-02c..h + UX-01f"
    - path: "tests/e2e/features/soundboard.spec.js"
      provides: "Wave-0 E2E stub for UX-01"
  key_links:
    - from: "features/dice/dice-core.js"
      to: "window.statsIdbPut"
      via: "typeof guard call inside addToDiceHistory"
      pattern: "window\\.statsIdbPut"
    - from: "loader.js"
      to: "features/soundboard/soundboard-player.js"
      via: "MODULES array entry"
      pattern: "soundboard-player"
    - from: "systems/tab-registry.js"
      to: "renderSoundboard / renderDiceStats"
      via: "TAB_RENDER_REGISTRY entries"
      pattern: "renderSoundboard|renderDiceStats"
---

<objective>
Lay the shared foundation for both Phase 7 features so the two feature plans (07-02/07-03 Soundboard, 07-04 Dice-Stats) can build in parallel without touching shared infrastructure.

Purpose: One IndexedDB upgrade, one roll-capture hook, and the tab/module/CSS/nav scaffolding are the single bottleneck both features depend on. Doing them once here prevents schema/version conflicts and module-registration drift.

Output: IDB v4 with `audioBlobs` + `diceStats` stores; the `addToDiceHistory()` stats tee (D-04); six registered module skeletons; two new tabs (Soundboard, Würfel-Statistiken); the four Wave-0 test stub files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-komfort-analyse/07-RESEARCH.md
@.planning/phases/07-komfort-analyse/07-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase — no exploration required. -->

core/init.js (the IDB upgrade point):
- `const IDB_VERSION = 3;` at line 271 → bump to 4.
- `request.onupgradeneeded` handler at lines 288–314 creates stores inside `if (!db.objectStoreNames.contains(...))` guards. Add the two new stores there.
- `initIndexedDB()` resolves `window.idb`. Generic put helper: `saveToIndexedDB(storeName, data)` at line 318.

features/dice/dice-core.js (the roll hook):
```
function addToDiceHistory(notation, result, rolls) {
    const dmgType = selectedDamageType ? ` (${selectedDamageType})` : '';
    diceHistory.unshift({ notation: notation + dmgType, result, rolls, time: new Date() });
    if (diceHistory.length > 30) diceHistory.pop();
    renderDiceHistory();
}
```
This is the SINGLE function every roll path flows through (verified: 20+ callsites at lines 56–700). Character-attributed rolls embed the character name as a `"Name: "` prefix in `notation` (e.g. `rollAttrCheck` → `"Thorin: STR"`). Attribution in 07-04 is derived by parsing that prefix — do NOT change callsites here.

systems/tab-registry.js — `TAB_RENDER_REGISTRY` object: add two entries
  `soundboard: { renders: ['renderSoundboard'], init: null, cleanup: null }`
  `dicestats:  { renders: ['renderDiceStats'],  init: null, cleanup: null }`

systems/spellslots/navigation.js — `switchView(name)` already drives `.view`/`.nav-tab` active toggling generically via `$('view-' + name)` and `renderTabContent(name)`. New tabs need NO switchView edit beyond the optional `viewNames` mobile label map (add `soundboard`/`dicestats` labels there).

assets/templates/header.html — nav buttons live at lines 50–72, pattern:
  `<button class="nav-tab" data-view="X" draggable="true" role="tab" aria-selected="false">EMOJI Label</button>`

build.py / loader.js — module lists must stay in sync (build.py `check_module_list_sync` enforces it). New `.css` files go in build.py css list (line ~443); new templates in build.py `html_templates` (line ~475). Reuse existing `tools.css` and `view-tools.html` — do NOT create new css/template files for this phase.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: IDB v4 upgrade + addToDiceHistory stats tee</name>
  <files>core/init.js, features/dice/dice-core.js, features/dice-stats/dice-stats-idb.js</files>
  <read_first>
    - core/init.js lines 268–329 (IDB_VERSION, onupgradeneeded store-creation block, saveToIndexedDB)
    - features/dice/dice-core.js lines 434–439 (addToDiceHistory) and 785 (window export)
    - systems/backups.js around line 88 (saveBackupToIndexedDB) + line 115 (getBackups) — the proven IDB tx open/put/getAll pattern to copy
    - .planning/phases/07-komfort-analyse/07-RESEARCH.md § Pattern 1, Pattern 4, "Writing a Roll to the Stats IDB Store", "Reading Stats for a Session Filter"
  </read_first>
  <action>
    In core/init.js: change `IDB_VERSION` from 3 to 4. Inside the existing `onupgradeneeded` handler, add two guarded `createObjectStore` blocks: `audioBlobs` with `{ keyPath: 'id' }`; `diceStats` with `{ keyPath: 'id', autoIncrement: true }` plus two indexes — `sessionId` on `sessionId` (unique:false) and `notation` on `notation` (unique:false). Leave all existing store blocks untouched.

    Create features/dice-stats/dice-stats-idb.js as the real IDB helper module (not a stub — the hook depends on it): implement `statsIdbPut(record)` (fire-and-forget `add()` into diceStats, guarded by `if (!window.idb) return;`), `getAllStats()` and `getStatsForSession(sessionId)` (Promise-returning getAll, resolve `[]` on error). Set a module-level boot session id `const _sbSessionId = Date.now().toString();` exported as `window._currentSessionId` (NOT in `D`; per RESEARCH Open Question 2 + Assumption A6 the "session" = app-boot id). Export `statsIdbPut`, `getAllStats`, `getStatsForSession` to window.

    In features/dice/dice-core.js: inside `addToDiceHistory()`, AFTER `renderDiceHistory()`, add the D-04 tee — call `window.statsIdbPut({ notation, result, rolls, timestamp: Date.now(), sessionId: window._currentSessionId || 'default', charId: null })` guarded by `if (typeof window.statsIdbPut === 'function')`. The in-memory `diceHistory.unshift(...)` line stays exactly as-is (D-04a). Do NOT use `var X = window.X` aliases (dedup rule).
  </action>
  <verify>
    <automated>cd "D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular" && grep -n "IDB_VERSION = 4" core/init.js && grep -c "audioBlobs\|diceStats" core/init.js && grep -n "window.statsIdbPut" features/dice/dice-core.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep "const IDB_VERSION = 4" core/init.js` matches; v3 line is gone.
    - core/init.js contains a `createObjectStore('audioBlobs'...)` and a `createObjectStore('diceStats'...)` with `createIndex('sessionId'...)` and `createIndex('notation'...)`.
    - features/dice-stats/dice-stats-idb.js exports `window.statsIdbPut`, `window.getAllStats`, `window.getStatsForSession`, and sets `window._currentSessionId`.
    - addToDiceHistory() still unshifts into `diceHistory` (unchanged) AND calls `window.statsIdbPut(...)` behind a `typeof` guard.
    - No `var X = window.X` redeclaration of any const-global introduced (dedup compliance).
  </acceptance_criteria>
  <done>IDB opens at v4 with both new stores; every roll tees into diceStats without altering the live history list.</done>
</task>

<task type="auto">
  <name>Task 2: Module skeletons + build.py/loader.js/header/CSS/registry registration</name>
  <files>features/soundboard/soundboard-idb.js, features/soundboard/soundboard-player.js, features/soundboard/soundboard-crud.js, features/soundboard/soundboard-render.js, features/dice-stats/dice-stats-render.js, loader.js, build.py, assets/templates/header.html, assets/templates/view-tools.html, assets/styles/tools.css, systems/tab-registry.js, systems/spellslots/navigation.js</files>
  <read_first>
    - loader.js lines 95–135 (Fraktionen/Dice module-block placement pattern) and line ~218 (template list)
    - build.py lines 125–155 (module list), 437–451 (css list), 475–478 (html_templates), 204–215 (check_module_list_sync)
    - assets/templates/header.html lines 50–72 (nav-tab buttons) — add buttons after the Fraktionen button
    - assets/templates/view-tools.html lines 1, 246, 307 (section pattern `<section id="view-X" class="view">`)
    - systems/tab-registry.js lines 7–119 (TAB_RENDER_REGISTRY)
    - systems/spellslots/navigation.js (switchView + viewNames mobile label map)
    - .planning/phases/07-komfort-analyse/07-RESEARCH.md § "Recommended Project Structure"
  </read_first>
  <action>
    Create five skeleton modules (real impl arrives in 07-02/03/04). Each defines its named functions with a defensive body and a window export. Required exports so the tab registry + keyboard layer resolve at boot: soundboard-render.js → `function renderSoundboard(){ const c=$('soundboard-container'); if(!c) return; c.innerHTML='<p>Soundboard</p>'; }` exported as `window.renderSoundboard`; dice-stats-render.js → `function renderDiceStats(){ const c=$('dicestats-container'); if(!c) return; c.innerHTML='<p>Statistiken</p>'; }` exported as `window.renderDiceStats`. soundboard-idb.js, soundboard-player.js, soundboard-crud.js may be near-empty skeletons (a section comment + one exported placeholder function each) — they are filled in 07-02/03. Use a UNIQUE module-level audio-context variable name reserved for 07-02: do not declare `const audioContext` (RESEARCH Pitfall 5) — leave player engine to 07-02.

    Register ALL FIVE new module paths in BOTH build.py MODULES and loader.js MODULES, in matching order (dice-stats-idb.js was created in Task 1 — register it too, so SIX soundboard/dice-stats module entries total). Place the soundboard block and dice-stats block near the Fraktionen/Dice area so load order is after their dependencies. Run `check_module_list_sync` implicitly via a clean build.

    In header.html: add two nav buttons after the Fraktionen button — `data-view="soundboard"` (🔊 Soundboard) and `data-view="dicestats"` (📊 Statistiken).

    In view-tools.html: add two `<section>`s — `<section id="view-soundboard" class="view"><div id="soundboard-container"></div></section>` and `<section id="view-dicestats" class="view"><div id="dicestats-container"></div></section>`.

    In tools.css: add stub sections with header comments `/* SOUNDBOARD */` and `/* DICE STATS */` (real rules in feature plans). tools.css is already in build.py css list and view-tools.html already in html_templates — no list change needed for css/template.

    In tab-registry.js: add `soundboard` and `dicestats` entries to TAB_RENDER_REGISTRY. In navigation.js: add `soundboard: '🔊 Soundboard'` and `dicestats: '📊 Statistiken'` to the mobile `viewNames` map.

    Rebuild: `PYTHONIOENCODING=utf-8 python build.py` from repo root — must exit 0 with module-sync OK.
  </action>
  <verify>
    <automated>cd "D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular" && grep -c "features/soundboard/\|features/dice-stats/" loader.js && grep -c "features/soundboard/\|features/dice-stats/" build.py && grep -c "data-view=\"soundboard\"\|data-view=\"dicestats\"" assets/templates/header.html && PYTHONIOENCODING=utf-8 python build.py >/dev/null 2>&1 && echo BUILD_OK</automated>
  </verify>
  <acceptance_criteria>
    - All six module paths (soundboard-idb/player/crud/render + dice-stats-idb/render) appear in BOTH loader.js and build.py (`check_module_list_sync` passes — build does not warn about sync).
    - `PYTHONIOENCODING=utf-8 python build.py` exits 0 and prints BUILD_OK; no `[DEDUP]` SyntaxError, no orphaned-return warning.
    - header.html has exactly one `data-view="soundboard"` and one `data-view="dicestats"` nav button.
    - view-tools.html contains `id="view-soundboard"` with `#soundboard-container` and `id="view-dicestats"` with `#dicestats-container`.
    - TAB_RENDER_REGISTRY has `soundboard` → `renderSoundboard` and `dicestats` → `renderDiceStats`; both functions are exported to window.
  </acceptance_criteria>
  <done>Both tabs are reachable, render a placeholder defensively, and the bundle builds clean with module lists in sync.</done>
</task>

<task type="auto">
  <name>Task 3: Wave-0 test stubs (4 files)</name>
  <files>tests/unit/dice-stats.test.js, tests/unit/soundboard.test.js, tests/e2e/features/soundboard.spec.js, tests/e2e/features/dice-stats.spec.js</files>
  <read_first>
    - .planning/phases/07-komfort-analyse/07-VALIDATION.md § "Per-Requirement Verification Map" + "Wave 0 Requirements" (exact `-t`/`-g` test names)
    - tests/e2e/features/persistence.spec.js lines 1–10 + 52–75 (ESM import + file:// reload pattern via page.reload + page.evaluate)
    - tests/e2e/features/bestiary.spec.js (ESM import header, navigateToTab helper usage)
    - tests/unit/character-advancement.test.js (Jest jsdom unit style for pure-logic functions)
    - CLAUDE.md "E2E specs are ESM" + project-memory quirk #9 (no CommonJS require, no http://localhost)
  </read_first>
  <action>
    Create the four Wave-0 stub files with test names matching the VALIDATION `-t`/`-g` strings EXACTLY so feature plans turn them green:

    tests/unit/dice-stats.test.js — `describe` blocks with `test.todo` or skipped `test()` for: "histogram 20 bars" (UX-02c), "expected overlay" (UX-02d), "crit rate" (UX-02e), "fumble rate" (UX-02f), "session filter" (UX-02g), "character breakdown" (UX-02h). Pure-JS unit tests against the (future) compute/render functions; for now assert against a placeholder so the file runs without erroring (e.g. mark each `test.todo(...)` or `test.skip(...)`).

    tests/unit/soundboard.test.js — skipped/todo test "size warning" (UX-01f) for the >20 MB file-size guard logic (pure JS, no audio playback).

    tests/e2e/features/soundboard.spec.js — ESM `import { test, expect } from '@playwright/test';`, file:// baseURL via existing helpers. Skipped/todo cases named: "soundboard tab renders" (UX-01a), "import audio file" (UX-01b), "audio blob persists after reload" (UX-01c), "scene quickslot keyboard" (UX-01e).

    tests/e2e/features/dice-stats.spec.js — ESM import; skipped/todo cases: "dice stats tab renders" (UX-02a), "rolls captured in IDB" (UX-02b, via page.evaluate over window.idb).

    Use `test.skip`/`test.todo` (NOT empty bodies) so the suites run green/neutral now and are activated by the feature plans. No CommonJS require; no hard-coded http URL.
  </action>
  <verify>
    <automated>cd "D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular" && npx jest tests/unit/dice-stats.test.js tests/unit/soundboard.test.js 2>&1 | grep -E "Tests:|todo|skipped|passed" | head && grep -L "require(" tests/e2e/features/soundboard.spec.js tests/e2e/features/dice-stats.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist at the exact paths in 07-VALIDATION.md § Wave 0 Requirements.
    - `npx jest tests/unit/dice-stats.test.js tests/unit/soundboard.test.js` runs without a module/parse error (todo/skipped is acceptable; erroring is not).
    - Both E2E specs use `import { test, expect } from '@playwright/test';` and contain NO `require(` and NO `http://localhost`.
    - Test/case names match the VALIDATION `-t`/`-g` strings verbatim (so `npx jest ... -t "crit rate"` and `npx playwright test -g "soundboard tab renders"` select the right stub).
  </acceptance_criteria>
  <done>The four Wave-0 stub files exist, run neutrally, and carry the exact test names the feature plans will activate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → IndexedDB | Roll records (notation/result/rolls) written to diceStats from in-app roll paths; no external input at this layer |
| Bundle → Browser | New module skeletons concatenated into the single-file build; dedup-sensitive global scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-NOTATION-XSS | Tampering | diceStats record `notation` written in addToDiceHistory tee | mitigate | Stored as-is here (no HTML render in this plan); 07-04 MUST `esc(notation)` on any HTML label render. Recorded so the consumer plan inherits the obligation. |
| T-07-IDB-VERSION | Denial of Service | core/init.js IDB upgrade | mitigate | Bump to v4 in the single existing `onupgradeneeded` guard; reuse `if (!contains)` guards so re-open is idempotent and existing v3 stores are never dropped (RESEARCH Pitfall 3) |
| T-07-DEDUP | Tampering | new global-scope module skeletons | mitigate | No `const audioContext` global here; no `var X = window.X` of a const-global; build dedup verified by clean `python build.py` exit 0 (CLAUDE.md dedup rule) |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — all native browser APIs (RESEARCH § Package Legitimacy Audit: "No external packages"). No install task exists. |
</threat_model>

<verification>
- `PYTHONIOENCODING=utf-8 python build.py` exits 0, module lists in sync, no dedup SyntaxError.
- `npm run test` green (existing 421+ unit tests unaffected; new stubs neutral).
- Manual DevTools check (recorded for 07-02/04): `indexedDB.open(APP_CONFIG.IDB_NAME).result.objectStoreNames` includes `audioBlobs` and `diceStats` after one app load.
</verification>

<success_criteria>
- IDB opens at v4 with `audioBlobs` + `diceStats` (sessionId + notation indexes).
- `addToDiceHistory()` tees into diceStats; in-memory `diceHistory` behavior unchanged.
- Soundboard + Würfel-Statistiken tabs reachable and render placeholders.
- Six modules registered in build.py AND loader.js; bundle builds clean.
- Four Wave-0 stub files exist and run.
</success_criteria>

<output>
Create `.planning/phases/07-komfort-analyse/07-01-SUMMARY.md` when done
</output>
