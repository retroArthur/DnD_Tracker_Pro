---
phase: 05-welt-story
plan: 04
type: execute
wave: 4
depends_on: ["05-02", "05-03"]
files_modified:
  - features/npc-generator/npc-generator.js
  - assets/templates/view-welt.html
  - assets/styles/welt.css
  - ui/actions/entity-actions.js
  - tests/unit/welt-story.test.js
  - tests/e2e/features/welt-story.spec.js
autonomous: true
requirements: [WELT-02]
must_haves:
  truths:
    - "Klick auf 'NPC generieren' zeigt in <1s eine Vorschau-Karte mit deutschem Namen, Persönlichkeitszug und Marotte"
    - "Vor-Filter Volk + Geschlecht ändert den verwendeten Namens-Pool"
    - "Re-Roll erzeugt einen anderen NPC, ohne einen Eintrag in D.npcs anzulegen"
    - "'Als NPC speichern' legt genau einen D.npcs-Eintrag mit Undo an"
    - "Der gespeicherte NPC erscheint im NPC-Tab"
  artifacts:
    - path: "features/npc-generator/npc-generator.js"
      provides: "generiereNPC, generiereNPCName, showNPCGeneratorModal, saveGeneratedNPC"
      contains: "generiereNPCName"
  key_links:
    - from: "features/npc-generator/npc-generator.js"
      to: "features/npc-generator/npc-default-tables.js"
      via: "NPC_DEFAULT_TABLES + Math.random()-Index"
      pattern: "NPC_DEFAULT_TABLES"
    - from: "features/npc-generator/npc-generator.js"
      to: "features/npcs/npc-crud.js"
      via: "Übernahme erzeugt D.npcs-Eintrag (window.saveNPC oder direkte D.npcs.push mit pushUndo)"
      pattern: "D.npcs"
---

<objective>
WELT-02 NPC-Generator: Auf Knopfdruck einen deutschen NPC (Name + Persönlichkeitszug + Marotte, dazu Volk/Beruf/Aussehen) aus den eingebauten Tabellen generieren, per Vor-Filter Volk+Geschlecht steuern, in einer Vorschau-Karte mit Re-Roll anzeigen und auf Wunsch als D.npcs-Eintrag speichern.

Purpose: Liefert Success Criterion 2 der Phase (NPC in <1s, sofort speicherbar). Nutzt die in Plan 02 generierten NPC_DEFAULT_TABLES und ist optional über D.randomTables erweiterbar (D-06).
Output: Generator-Funktion + Modal/Mini-Bereich mit Vorschau, Re-Roll und Speichern-Fluss.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-welt-story/05-RESEARCH.md
@.planning/phases/05-welt-story/05-CONTEXT.md

<interfaces>
<!-- Verifizierte Signaturen. Direkt nutzen. -->

NPC_DEFAULT_TABLES (window, aus Plan 02):
  { namen: { mensch:{maennlich,weiblich,neutral}, elf, zwerg, halbling, halbork, tiefling, gnom },
    persoenlichkeitszuege:[...], marotten:[...], berufe:[...], aussehen:[...] }

Generator-Logik (aus 05-RESEARCH.md § Konkrete Wiederverwendungs-Signaturen):
  generiereNPCName(volk, geschlecht): NPC_DEFAULT_TABLES.namen[volk]?.[geschlecht] || []; Math.random()-Index; Fallback 'Unbekannt'.
  generiereNPCZug(): Zufallselement aus persoenlichkeitszuege.

Übernahme: KEIN Auto-Speichern beim Re-Roll. Speichern legt D.npcs-Eintrag an.
  Option A (bevorzugt, dedup-sicher): direkt pushUndo('NPC generiert') → D.npcs.push({id:nextId('npcs'), name, role, race, description, ...}) → save() → renderNPCList().
  Option B: window.saveNPC() (füllt Formular vor) — komplexer; Option A vorziehen.
  WICHTIG: saveNPC NICHT redefinieren; rollWeightedEntry NICHT redefinieren.

Globals (kein const X=window.X in Funktionen): pushUndo, nextId, esc, sanitizeHTML, showModal/hideModal, showToast.
  var D = window.D; var save = window.save;

Discretion (D-Context): Einstieg via Button im NPC-Tab UND/ODER Command-Palette-Aktion "NPC generieren". Mindestens ein klarer Einstiegspunkt.
CSS-Präfix: npcg-.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generator-Kernlogik (Pool-Auswahl + Vollgenerierung)</name>
  <read_first>
    - features/npc-generator/npc-generator.js (Plan-01-Skelett)
    - features/npc-generator/npc-default-tables.js (NPC_DEFAULT_TABLES-Struktur aus Plan 02)
    - features/random-tables.js (rollWeightedEntry — falls für gewichtete Felder genutzt; sonst Math.random())
    - .planning/phases/05-welt-story/05-VALIDATION.md (WELT-02-Unit-Zeile: generiereNPCName('zwerg','maennlich') ∈ Pool)
  </read_first>
  <behavior>
    - generiereNPCName('zwerg','maennlich') liefert einen String aus NPC_DEFAULT_TABLES.namen.zwerg.maennlich.
    - generiereNPCName mit unbekanntem Volk/Geschlecht liefert sicheren Fallback (z.B. 'Unbekannt'), kein Crash.
    - generiereNPC(volk, geschlecht) liefert ein Objekt { name, volk, geschlecht, zug, marotte, beruf, aussehen } — alle Pflichtfelder name/zug/marotte gesetzt.
  </behavior>
  <action>
    In npc-generator.js implementieren: generiereNPCName(volk, geschlecht) (Pool aus NPC_DEFAULT_TABLES.namen[volk]?.[geschlecht], Math.random()-Index, Fallback), Hilfsgriffe für Zug/Marotte/Beruf/Aussehen (Zufallselement aus den jeweiligen Arrays), und generiereNPC(volk, geschlecht) das ein vollständiges NPC-Vorschau-Objekt zusammenstellt. Eindeutige Funktionsnamen (generiereNPC, generiereNPCName, generiereNPCFeld o.ä.). Optionale D.randomTables-Erweiterung (D-06): wenn eine gleichnamige Nutzer-Tabelle existiert, deren Einträge mit einbeziehen (additiv) — minimal, kann als TODO-Hook bleiben, darf aber nicht crashen. window-Exporte.
  </action>
  <verify>
    <automated>cd "D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular" && npx jest tests/unit/welt-story.test.js -t "WELT-02"</automated>
  </verify>
  <acceptance_criteria>
    - WELT-02-Unit-Test (generiereNPCName('zwerg','maennlich') im Pool) ist aktiviert und grün.
    - generiereNPC liefert ein Objekt mit gesetzten Pflichtfeldern name, zug, marotte.
    - Unbekanntes Volk/Geschlecht crasht nicht (Fallback).
    - Keine Redefinition von saveNPC/rollWeightedEntry; eindeutige Funktionsnamen.
  </acceptance_criteria>
  <done>Generator-Kernlogik vorhanden; WELT-02-Unit-Test grün.</done>
</task>

<task type="auto">
  <name>Task 2: Generator-Modal (Vorschau + Vor-Filter + Re-Roll + Speichern) + View/CSS/Actions/E2E</name>
  <read_first>
    - features/npc-generator/npc-generator.js (Task-1-Kernlogik)
    - features/random-tables.js (showGeneratorModal/showTableModal — Modal-Erstellungsmuster, data-action-Buttons)
    - features/npcs/npc-crud.js (saveNPC: D.npcs-Feldnamen name/role/race/description, nextId('npcs'), pushUndo)
    - features/command-palette/action-registry.js (Aktions-Registrierung für "NPC generieren" — Discretion-Einstieg)
    - ui/actions/entity-actions.js (data-action-Handler-Muster)
    - .planning/phases/05-welt-story/05-VALIDATION.md (WELT-02 E2E: <1s, Re-Roll ohne Müll, Speichern → D.npcs.length===1, erscheint im NPC-Tab)
  </read_first>
  <action>
    In npc-generator.js: showNPCGeneratorModal() mit Vor-Filter-Selects (Volk + Geschlecht), einer Vorschau-Karte (Name/Zug/Marotte prominent, Volk/Beruf/Aussehen sekundär — alle via esc()), einem "Neu würfeln/Re-Roll"-Button (ruft generiereNPC mit aktuellen Filtern, ersetzt nur die Vorschau, KEIN D.npcs-Schreibzugriff) und einem "Als NPC speichern"-Button. saveGeneratedNPC(): pushUndo('NPC generiert') → D.npcs.push({ id: nextId('npcs'), name, role: beruf, race: volk, description: sanitizeHTML(zug + Marotte + Aussehen als formatierter Text) }) → save() → renderNPCList() → hideModal → showToast. saveNPC NICHT aufrufen/redefinieren.
    In view-welt.html: optional einen Generator-Einstiegsbutton an passender Stelle (z.B. im NPC-Tab-Toolbar-Hook oder als Tab-loser Modal-Trigger). In welt.css: npcg--präfixierte Klassen für Vorschau-Karte/Modal. In ui/actions/entity-actions.js: data-action-Handler (show-npc-generator, reroll-npc, save-generated-npc). Registriere in features/command-palette/action-registry.js eine Aktion "NPC generieren" → showNPCGeneratorModal (Discretion-Einstieg, Command Palette existiert seit Phase 2).
    Aktiviere die WELT-02-E2E-Stubs: Generierung <1s (performance.now-Delta), 3× Re-Roll ohne D.npcs-Wachstum, Speichern → D.npcs.length===1, NPC im npcs-Tab sichtbar.
  </action>
  <verify>
    <automated>cd "D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular" && set PYTHONIOENCODING=utf-8 && python build.py && npx jest tests/unit/welt-story.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `python build.py` grün.
    - showNPCGeneratorModal zeigt Vor-Filter Volk+Geschlecht + Vorschau-Karte mit Name/Zug/Marotte.
    - Re-Roll ändert die Vorschau, schreibt NICHT in D.npcs (kein Auto-Save).
    - saveGeneratedNPC ruft pushUndo() vor D.npcs.push, nutzt nextId('npcs') und ruft renderNPCList(); saveNPC wird nicht redefiniert.
    - Command-Palette-Aktion "NPC generieren" registriert.
    - WELT-02-E2E-Spec aktiviert (Speichern → genau 1 D.npcs-Eintrag; NPC im NPC-Tab).
  </acceptance_criteria>
  <done>NPC-Generator voll funktionsfähig; Build + Tests grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generierte Tabellentexte + Vor-Filter-Auswahl → DOM | Vorschau-Karte rendert Namen/Züge/Marotten |
| Vorschau → D.npcs | Übernahme schreibt persistente Entität |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | Information Disclosure (XSS) | Vorschau-Karte / gespeicherte description | mitigate | Alle Felder via esc() in der Vorschau; description beim Speichern via sanitizeHTML() |
| T-05-14 | Tampering | Re-Roll erzeugt versehentlich Müll-Einträge in D.npcs | mitigate | Re-Roll mutiert NUR den Vorschau-State, ruft niemals D.npcs.push/save; E2E prüft D.npcs.length===0 nach 3× Re-Roll |
| T-05-15 | Denial of Service | erweiterbare D.randomTables-Schleife | accept | Optionaler additiver Hook über bestehende Tabellen; Math.random()-Index ist O(1); kein unbegrenzter Loop über Nutzerinput |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | Keine neuen Pakete |
</threat_model>

<verification>
- Build grün; Generator-Modal mit Vor-Filter + Vorschau + Re-Roll + Speichern.
- Re-Roll ohne D.npcs-Wachstum; Speichern erzeugt genau 1 Eintrag, sichtbar im NPC-Tab; Undo möglich.
</verification>

<success_criteria>
- WELT-02 erfüllt: NPC in <1s generiert, sofort speicherbar (Phase Success Criterion 2).
</success_criteria>

<output>
Create `.planning/phases/05-welt-story/05-04-SUMMARY.md` when done
</output>
