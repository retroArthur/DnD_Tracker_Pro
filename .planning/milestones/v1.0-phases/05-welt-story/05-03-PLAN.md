---
phase: 05-welt-story
plan: 03
type: execute
wave: 3
depends_on: ["05-01"]
files_modified:
  - features/session-prep/session-prep-render.js
  - features/session-prep/session-prep-crud.js
  - assets/templates/view-welt.html
  - assets/styles/welt.css
  - ui/actions/entity-actions.js
  - tests/unit/welt-story.test.js
  - tests/e2e/features/welt-story.spec.js
autonomous: true
requirements: [WELT-01]
must_haves:
  truths:
    - "Tab Session-Prep zeigt eine Liste der Session-Preps und einen Button zum Anlegen"
    - "Neue Prep öffnet eine Lazy-DM-Vorlage mit 5 Abschnitten: Strong Start, geplante Szenen, geheime Hinweise, wichtige NPCs, mögliche Belohnungen"
    - "Offene Fäden werden automatisch aus offenen Quests (!q.completed) und Story-Arcs vorgeschlagen, manuell ergänzbar"
    - "Szenenkarten-Inhalte unterstützen Entity-Links und werden sanitisiert gerendert"
    - "Speichern legt einen D.sessionPreps-Eintrag mit Undo an; Undo entfernt ihn wieder"
  artifacts:
    - path: "features/session-prep/session-prep-crud.js"
      provides: "saveSessionPrep, deleteSessionPrep, Vorschlag offener Fäden"
      contains: "saveSessionPrep"
    - path: "features/session-prep/session-prep-render.js"
      provides: "renderSessionPrepList + Detail/Modal mit 5 Abschnitten"
      contains: "renderSessionPrepList"
    - path: "assets/templates/view-welt.html"
      provides: "Session-Prep-View-Inhalt"
      contains: "view-sessionprep"
  key_links:
    - from: "features/session-prep/session-prep-render.js"
      to: "D.quests + D.storyArcs"
      via: "offene-Fäden-Vorschlag (read-only)"
      pattern: "completed"
    - from: "features/session-prep/session-prep-render.js"
      to: "systems/entity-links.js"
      via: "parseEntityLinks(sanitizeHTML(...))"
      pattern: "parseEntityLinks"
---

<objective>
WELT-01 Session-Prep-Assistent: eigener Tab mit Liste/Detail, Lazy-DM-Vorlage (5 Abschnitte), Auto-Vorschlag offener Fäden aus Quests/Story-Arcs, Szenenkarten mit Entity-Links, CRUD mit Undo.

Purpose: Liefert dem Spielleiter strukturierte Session-Vorbereitung auf einem Blick (Success Criterion 1 der Phase). Nutzt das in Plan 01 angelegte D.sessionPreps-Schema und den sessionprep-Tab.
Output: Voll funktionsfähiger Session-Prep-Tab (CRUD + Vorlage + Fäden + Links).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-welt-story/05-RESEARCH.md
@.planning/phases/05-welt-story/05-CONTEXT.md

<interfaces>
<!-- Verifizierte Wiederverwendungs-Signaturen. Direkt nutzen, keine Exploration nötig. -->

D.sessionPreps[] Schema (aus Plan 01 / 05-RESEARCH.md § D.sessionPreps):
  { id, sessionNr, datum, inGameDatum, strongStart, szenen:[{id,titel,beschreibung,ort}],
    geheimeHinweise, wichtigeNpcs, belohnungen,
    offeneFaeden:[{text, quelleId, quelleTyp:'quest'|'storyArc'|'manual'}], links:[], erstellt }

Quests-Status: q.completed (boolean) — offen = !q.completed (features/quests/quests-render.js verifiziert).
Story-Arcs: D.storyArcs[] (features/sessions/sessions.js verifiziert; arc.status für offen/abgeschlossen prüfen).

Wiederverwendung (global, KEIN const X=window.X in Funktionskörpern):
  pushUndo('Label')                       — Snapshot vor jeder Mutation (systems/undo.js, const-global → direkt aufrufen)
  nextId('sessionPreps')                  — neue ID (utils/utilities.js)
  parseEntityId(id)                       — ID-Vergleiche
  esc(text) / sanitizeHTML(html)          — Plaintext bzw. Rich-Text-Schutz (utils/basic.js)
  parseEntityLinks(content)               — [[type:id:name]] → klickbare Spans (systems/entity-links.js)
  showInsertEntityLinkModal(editorId)     — Link-Einfügen-Modal (systems/entity-links.js)
  deleteWithConfirm({entityType,id,onSuccess,undoLabel}) / afterCrudOperation(renderFn,msg) (utils/crud-helpers.js)
  safeRender(fn, fnName, containerId, opts) (render/helpers.js)
  var D = window.D; var save = window.save;  — module-level var (window-attached)

Tab-Container: #view-sessionprep (aus view-welt.html). renderSessionPrepList in TAB_RENDER_REGISTRY['sessionprep'].
WICHTIG: parseEntityLinks(sanitizeHTML(text)) — erst sanitisieren, dann Links parsen (RESEARCH Risikotabelle).
CSS-Präfix: wp- (Welt-Prep).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Session-Prep CRUD + offene-Fäden-Vorschlag</name>
  <read_first>
    - features/session-prep/session-prep-crud.js (Plan-01-Skelett)
    - features/npcs/npc-crud.js (saveNPC/deleteNPC-Muster: pushUndo VOR Mutation, nextId, deleteWithConfirm, save)
    - features/quests/quests-render.js (q.completed — offene Quests)
    - features/sessions/sessions.js (D.storyArcs Struktur + Status)
    - utils/crud-helpers.js (deleteWithConfirm, afterCrudOperation)
    - .planning/phases/05-welt-story/05-RESEARCH.md (§ D.sessionPreps Schema)
  </read_first>
  <action>
    In session-prep-crud.js implementieren: saveSessionPrep() — liest Formularfelder (Strong Start, Szenen, geheime Hinweise, wichtige NPCs, Belohnungen, inGameDatum, sessionNr), sanitisiert Rich-Text-Felder via sanitizeHTML(), ruft pushUndo('Session-Prep gespeichert'/'bearbeitet') VOR der Mutation, erzeugt bei Neuanlage id via nextId('sessionPreps') + erstellt=Date.now(), pushed/updated in D.sessionPreps, danach save() + Re-Render. deleteSessionPrep(id) via deleteWithConfirm({entityType:'sessionPreps', id, undoLabel:'Session-Prep gelöscht', onSuccess: ...}). Hilfsfunktion sammleOffeneFaeden() — liest D.quests (Filter !q.completed) und D.storyArcs (offene) read-only und gibt Vorschlags-Array {text, quelleId, quelleTyp} zurück; KEINE Schreibzugriffe auf Quests/Arcs. Eindeutige Funktionsnamen (saveSessionPrep, deleteSessionPrep, sammleOffeneFaeden) — keine Kollision mit Bestand. window-Exporte am Dateiende.
  </action>
  <verify>
    <automated>cd "D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular" && node -e "const fs=require('fs'); const s=fs.readFileSync('features/session-prep/session-prep-crud.js','utf8'); ['saveSessionPrep','deleteSessionPrep','sammleOffeneFaeden'].forEach(f=>{if(!new RegExp('function '+f+'\\\\b').test(s))throw new Error('fehlt: '+f)}); if(!/pushUndo\(/.test(s))throw new Error('pushUndo fehlt'); if(!/completed/.test(s))throw new Error('offene-Quests-Filter fehlt'); console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - saveSessionPrep ruft pushUndo() vor jeder D.sessionPreps-Mutation und nextId('sessionPreps') bei Neuanlage.
    - deleteSessionPrep nutzt deleteWithConfirm mit entityType:'sessionPreps' und undoLabel.
    - sammleOffeneFaeden liest D.quests (!q.completed) + D.storyArcs read-only, schreibt nicht.
    - Keine doppelten Top-Level-Funktionsnamen; window-Exporte vorhanden.
  </acceptance_criteria>
  <done>CRUD + Fäden-Vorschlag implementiert; grep-Assertion bestanden.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Session-Prep Render + Lazy-DM-Modal + Entity-Links + View/CSS/Actions</name>
  <read_first>
    - features/session-prep/session-prep-render.js (Plan-01-Skelett mit renderSessionPrepList-Stub)
    - features/random-tables.js (Render-Muster: Container-Check, esc(), data-action-Buttons, Modal-Erstellung showXModal)
    - features/npcs/npc-render.js (renderNPCRelations + showRelationsModal: Detail/Modal-Layout, RELATION_STATUS-Optik)
    - systems/entity-links.js (parseEntityLinks, showInsertEntityLinkModal — Signaturen)
    - assets/templates/view-bestiary.html (View-Section-Layout als Analog)
    - ui/actions/entity-actions.js (data-action-Registrierungsmuster)
    - .planning/phases/05-welt-story/05-VALIDATION.md (WELT-01-Zeilen: 5 Abschnitte, Entity-Link klickbar, Undo)
  </read_first>
  <behavior>
    - renderSessionPrepList() rendert bei vorhandenem #view-sessionprep-Listencontainer eine Liste aller D.sessionPreps mit "Neu"-Button; leerer Zustand zeigt CTA.
    - Das Anlege-/Bearbeiten-Modal enthält genau 5 Abschnitte mit den Feld-IDs prep-strong-start, prep-szenen, prep-hinweise, prep-npcs, prep-belohnungen.
    - Offene-Fäden-Bereich zeigt Vorschläge aus sammleOffeneFaeden() als Badges, manuell ergänzbar.
    - Szenenkarten-Beschreibung wird als parseEntityLinks(sanitizeHTML(text)) gerendert → [[npcs:1:Elara]] ergibt .entity-link[data-type="npcs"].
  </behavior>
  <action>
    In session-prep-render.js: renderSessionPrepList() (Container #view-sessionprep, defensive Check, safeRender-Wrap optional) + renderSessionPrepDetail()/showSessionPrepModal() mit den 5 Lazy-DM-Abschnitten (Feld-IDs prep-strong-start, prep-szenen, prep-hinweise, prep-npcs, prep-belohnungen) und einem Offene-Fäden-Block, der sammleOffeneFaeden()-Vorschläge als anklickbare Badges + ein manuelles Eingabefeld zeigt. Szenen-Beschreibungen rendern via parseEntityLinks(sanitizeHTML(szene.beschreibung)); alle Plaintext-Felder via esc(). Link-Einfügen-Button pro Szene mit data-action, der showInsertEntityLinkModal(editorId) auslöst.
    In view-welt.html: #view-sessionprep mit section-toolbar (Titel + Neu-Button) und Listencontainer befüllen (Analog view-bestiary.html). In welt.css: wp--präfixierte Klassen für Liste/Karten/Modal-Abschnitte ergänzen (CSS-Variablen var(--gold) etc.). In ui/actions/entity-actions.js: data-action-Handler registrieren (z.B. show-session-prep-modal, save-session-prep, delete-session-prep, add-faeden-manual, insert-entity-link für Szenen).
    Aktiviere die zuvor übersprungenen WELT-01-Unit/E2E-Stubs in tests/unit/welt-story.test.js und tests/e2e/features/welt-story.spec.js, soweit unit-/e2e-prüfbar (Tab sichtbar, 5 Abschnitte vorhanden, offener-Quest-Vorschlag, Entity-Link klickbar, Undo entfernt Eintrag).
  </action>
  <verify>
    <automated>cd "D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular" && set PYTHONIOENCODING=utf-8 && python build.py && npx jest tests/unit/welt-story.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `python build.py` grün (keine Duplikate, Listen synchron).
    - Modal enthält Feld-IDs prep-strong-start, prep-szenen, prep-hinweise, prep-npcs, prep-belohnungen (E2E-verifizierbar).
    - Szenen-Render nutzt parseEntityLinks(sanitizeHTML(...)) (sanitize-then-parse), nicht umgekehrt.
    - Offene-Fäden-Vorschläge erscheinen, wenn eine offene Quest existiert.
    - WELT-01-Tests in welt-story.test.js sind nicht mehr skipped und grün; E2E-Spec für sessionprep aktiviert.
  </acceptance_criteria>
  <done>Session-Prep-Tab funktionsfähig; Build + Unit-Tests grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nutzer-Eingaben (Szenen, Hinweise, Belohnungen) → DOM | Rich-Text + Plaintext-Felder werden gerendert |
| Entity-Link-Syntax → klickbare Spans | [[type:id:name]] aus Nutzerinhalt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Information Disclosure (XSS) | Szenen-/Hinweis-Render | mitigate | Rich-Text via sanitizeHTML(); danach parseEntityLinks() (sanitize-then-parse); Plaintext via esc() |
| T-05-11 | Tampering | offeneFaeden quelleId zeigt auf gelöschte Quest/Arc | mitigate | quelleId nur als Anzeige-Referenz; beim Rendern Existenz prüfen (EntityLookup/Filter), fehlende Quelle → als 'manual' behandeln; parseEntityId() für Vergleiche |
| T-05-12 | Denial of Service | sehr viele offene Fäden/Szenen | accept | DM-eigene Daten, kein externer Input; Listenrender ist linear, virtual-scroll greift >50 |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | Keine neuen Pakete in diesem Plan |
</threat_model>

<verification>
- Build grün; sessionprep-Tab rendert Liste + Neu-Button.
- Modal mit 5 Abschnitten; offene Quest erzeugt Fäden-Vorschlag.
- Entity-Link in Szene klickbar; Undo nach Speichern leert D.sessionPreps.
</verification>

<success_criteria>
- WELT-01 erfüllt: strukturierte Session-Vorbereitung auf einen Blick (Phase Success Criterion 1).
</success_criteria>

<output>
Create `.planning/phases/05-welt-story/05-03-SUMMARY.md` when done
</output>
