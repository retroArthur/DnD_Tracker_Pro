---
phase: 05-welt-story
plan: 05
type: execute
wave: 5
depends_on: ["05-01", "05-04"]
files_modified:
  - features/timeline/timeline-render.js
  - features/timeline/timeline-crud.js
  - assets/templates/view-welt.html
  - assets/styles/welt.css
  - ui/actions/entity-actions.js
  - tests/unit/welt-story.test.js
  - tests/e2e/features/welt-story.spec.js
autonomous: true
requirements: [WELT-03]
must_haves:
  truths:
    - "Der Kalender-Tab zeigt das aktuelle In-Game-Datum mit Harptos-Monatsnamen (z.B. 'Hammer 1492 DR')"
    - "Ein Timeline-Eintrag mit In-Game-Datum wird in D.calendar.events gespeichert"
    - "Timeline-Einträge erscheinen chronologisch sortiert"
    - "addCalendarEvent(datum, titel, typ, quelleId) und advanceCalendarDate(tage) stehen als geteilte Helfer bereit"
  artifacts:
    - path: "features/timeline/timeline-crud.js"
      provides: "saveTimelineEvent, deleteTimelineEvent, addCalendarEvent, advanceCalendarDate, sortiereEvents"
      contains: "advanceCalendarDate"
    - path: "features/timeline/timeline-render.js"
      provides: "renderTimeline + renderKalender (Harptos-Anzeige)"
      contains: "renderKalender"
  key_links:
    - from: "features/timeline/timeline-render.js"
      to: "core/constants.js HARPTOS_MONTHS"
      via: "Monatsname-Anzeige"
      pattern: "HARPTOS_MONTHS"
    - from: "features/timeline/timeline-crud.js"
      to: "D.calendar.events"
      via: "addCalendarEvent push + chronologische Sortierung"
      pattern: "D.calendar.events"
---

<objective>
WELT-03 Kampagnen-Timeline & In-Game-Kalender: Kalender-Tab zeigt das aktuelle Harptos-Datum, Timeline-Einträge mit In-Game-Datum werden chronologisch sortiert in D.calendar.events gehalten, Auto-Vorschläge aus Sessions/Reisen. Dieser Plan etabliert zugleich die geteilten Kalender-Helfer addCalendarEvent() und advanceCalendarDate(), die der Reise-Plan (06) konsumiert.

Purpose: Liefert Success Criterion 3 der Phase und das gemeinsame Kalender-Substrat (D-09/D-10/D-11). Reise (Plan 06) MUSS advanceCalendarDate vorfinden — daher landet die Kalender-Logik hier, vor Reise.
Output: Funktionsfähiger Kalender/Timeline-Tab + geteilte, getestete Kalender-Helfer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-welt-story/05-RESEARCH.md
@.planning/phases/05-welt-story/05-CONTEXT.md

<interfaces>
<!-- Verifizierte Strukturen. -->

D.calendar (nach Migration 4.0.0 1-basiert): { day:1..30, month:1..12, year:1492, events:[] }
HARPTOS_MONTHS (core/constants.js, Plan 01): [{nr:1,name:'Hammer',jahreszeit:'winter'}, ... 12 Monate à 30 Tage]
HARPTOS_FESTIVALS (Plan 01): [{name,nachMonat}, ...]

D.calendar.events[] Schema (aus 05-RESEARCH.md § D.calendar.events):
  { id, datum:{tag,monat,jahr}, titel, beschreibung, typ:'manuell'|'reise'|'session', quelleId }

Geteilte Helfer (HIER definieren, Reise konsumiert):
  addCalendarEvent(datum, titel, typ, quelleId): pushUndo → D.calendar.events.push({id:nextId('calendarEvents'), ...}) → sortieren → save.
  advanceCalendarDate(tage): rückt D.calendar.day/month/year um 'tage' vor; 30-Tage-Monate, 12 Monate/Jahr (Festtage optional; DoS-Cap auf 'tage').
  sortiereEvents(events): chronologisch nach jahr→monat→tag.

Globals (kein const X=window.X in Funktionen): pushUndo, nextId, esc, sanitizeHTML, showModal/hideModal, showToast, parseEntityId.
  var D = window.D; var save = window.save;

Tab-Container: #view-kalender. Registry kalender → renders ['renderTimeline','renderKalender'].
CSS-Präfix: tl-.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Kalender-Helfer (advanceCalendarDate, addCalendarEvent, Sortierung)</name>
  <read_first>
    - features/timeline/timeline-crud.js (Plan-01-Skelett)
    - core/data.js (D.calendar-Schema)
    - core/constants.js (HARPTOS_MONTHS — 12 Monate à 30 Tage, 1-basiert)
    - features/random-tables.js (parseRange-DoS-Muster: MAX_RANGE_SIZE als Vorbild für Tage-Cap)
    - .planning/phases/05-welt-story/05-VALIDATION.md (WELT-03 Unit: chronologische Sortierung; WELT-04 E2E: Start Tag 1 + 3 Tage → day 4)
  </read_first>
  <behavior>
    - advanceCalendarDate(3) bei Start {day:1,month:1,year:1492} ergibt {day:4,month:1,year:1492}.
    - advanceCalendarDate über Monatsgrenze: day 29 + 3 Tage → day 2, month +1 (30-Tage-Monate).
    - advanceCalendarDate über Jahresgrenze: month 12 day 29 + 3 → month 1, jahr +1.
    - sortiereEvents([...]) sortiert nach jahr, dann monat, dann tag aufsteigend.
    - advanceCalendarDate(negativ oder >Cap) wird auf sinnvollen Bereich geklemmt (DoS-Schutz).
  </behavior>
  <action>
    In timeline-crud.js implementieren: advanceCalendarDate(tage) — klemmt tage auf 0..MAX (z.B. 3650, DoS-Cap), addiert Tage auf D.calendar mit 30-Tage-Monaten / 12 Monaten pro Jahr, Jahresüberlauf erhöht year; sortiereEvents(events) — stabile Sortierung nach jahr→monat→tag; addCalendarEvent(datum, titel, typ, quelleId) — pushUndo('Timeline-Eintrag') → push {id:nextId('calendarEvents'), datum, titel:..., beschreibung:..., typ, quelleId} in D.calendar.events → D.calendar.events = sortiereEvents(D.calendar.events) → save(). saveTimelineEvent() liest das Formular (datum tag/monat/jahr, titel, beschreibung, typ) und ruft addCalendarEvent. deleteTimelineEvent(id) via pushUndo + Filter + save. Eindeutige Funktionsnamen. window-Exporte (auch advanceCalendarDate + addCalendarEvent für Reise-Plan).
  </action>
  <verify>
    <automated>cd "D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular" && npx jest tests/unit/welt-story.test.js -t "WELT-03"</automated>
  </verify>
  <acceptance_criteria>
    - advanceCalendarDate behandelt Monats- (30) und Jahresgrenzen (12 Monate) korrekt; geklemmter Tage-Cap.
    - sortiereEvents sortiert chronologisch (jahr→monat→tag) — WELT-03-Unit grün.
    - addCalendarEvent ruft pushUndo, nutzt nextId('calendarEvents'), sortiert nach Push, ruft save().
    - advanceCalendarDate und addCalendarEvent auf window exportiert (für Plan 06 konsumierbar).
  </acceptance_criteria>
  <done>Kalender-Helfer implementiert + getestet; WELT-03-Sortier-Unit grün.</done>
</task>

<task type="auto">
  <name>Task 2: Kalender-/Timeline-Render + Auto-Vorschlag + View/CSS/Actions/E2E</name>
  <read_first>
    - features/timeline/timeline-render.js (Plan-01-Skelett mit renderTimeline/renderKalender-Stubs)
    - features/timeline/timeline-crud.js (Task-1-Helfer)
    - core/constants.js (HARPTOS_MONTHS/HARPTOS_FESTIVALS für Anzeige)
    - features/random-tables.js (Render-/Modal-Muster, esc())
    - features/sessions/sessions.js (D.sessionNotes als Auto-Vorschlag-Quelle für typ:'session')
    - assets/templates/view-bestiary.html (View-Layout-Analog)
    - .planning/phases/05-welt-story/05-VALIDATION.md (WELT-03 E2E: Harptos-Monatsname in Anzeige; Eintrag → events.length===1; Auto-Vorschlag-Dialog)
  </read_first>
  <action>
    In timeline-render.js: renderKalender() — Container #view-kalender, zeigt aktuelles Datum als "{HARPTOS_MONTHS[month-1].name} {day}, {year} DR" (defensive Checks, esc()). renderTimeline() — listet D.calendar.events chronologisch (sortiereEvents) mit Datum/Titel/Typ-Badge; Neu-Button öffnet ein Eintrags-Modal (Felder tag/monat-Select aus HARPTOS_MONTHS/jahr/titel/beschreibung/typ). Auto-Vorschlag-Hook: renderAutoVorschlaege() (oder Inline) bietet aus gespeicherten Sessions (D.sessionNotes) und abgeschlossenen Reisen (kommt aus Plan 06, hier defensiv falls vorhanden) je einen Bestätigen/Verwerfen-Vorschlag, der bei Bestätigung addCalendarEvent ruft. Beschreibung via sanitizeHTML beim Speichern; Anzeige via esc()/parseEntityLinks(sanitizeHTML(...)).
    In view-welt.html: #view-kalender mit Kalender-Anzeige + Timeline-Liste + Neu-Button befüllen. In welt.css: tl--präfixierte Klassen. In ui/actions/entity-actions.js: data-action-Handler (show-timeline-modal, save-timeline-event, delete-timeline-event, confirm-auto-event, dismiss-auto-event).
    Aktiviere WELT-03-E2E-Stubs: Kalender-Anzeige enthält Monatsname; Formular → D.calendar.events.length===1; Auto-Vorschlag-Dialog erscheint nach Session-Speicherung (sofern unit-/e2e-prüfbar).
  </action>
  <verify>
    <automated>cd "D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular" && set PYTHONIOENCODING=utf-8 && python build.py && npx jest tests/unit/welt-story.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `python build.py` grün.
    - renderKalender zeigt Harptos-Monatsname + Jahr ("... 1492 DR") aus HARPTOS_MONTHS.
    - renderTimeline listet Events chronologisch (via sortiereEvents).
    - Eintrags-Modal speichert über addCalendarEvent → D.calendar.events.length wächst um 1.
    - Auto-Vorschlag-Mechanik vorhanden (Bestätigen/Verwerfen ruft addCalendarEvent bzw. verwirft).
    - WELT-03-Tests aktiviert und grün.
  </acceptance_criteria>
  <done>Kalender/Timeline-Tab funktionsfähig; Build + Tests grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Timeline-Formular (Datum/Titel/Beschreibung) → D.calendar.events → DOM | Nutzerinhalt wird persistiert und gerendert |
| advanceCalendarDate(tage) | 'tage' kann aus Reise-Berechnung oder Formular stammen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-16 | Information Disclosure (XSS) | Event-Titel/Beschreibung-Render | mitigate | Titel via esc(); Beschreibung via sanitizeHTML() beim Speichern, parseEntityLinks(sanitizeHTML(...)) bei Anzeige |
| T-05-17 | Denial of Service | advanceCalendarDate mit riesigem 'tage' (z.B. Reise-Bug oder Formular) | mitigate | tage auf 0..MAX (Cap ~3650) klemmen, bevor die Datums-Schleife läuft; verhindert Endlos-/Langläufer |
| T-05-18 | Tampering | Event-datum mit ungültigen monat/tag | mitigate | monat per Select aus HARPTOS_MONTHS (1..12); tag clampen 1..30; parseEntityId für IDs; Sortierung toleriert fehlende Felder |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | Keine neuen Pakete |
</threat_model>

<verification>
- Build grün; Kalender zeigt Harptos-Datum; Timeline chronologisch.
- Eintrag → events.length+1; advanceCalendarDate-Unit (Tag/Monat/Jahr-Überlauf) grün.
- Auto-Vorschlag bestätigbar/verwerfbar.
</verification>

<success_criteria>
- WELT-03 erfüllt (Phase Success Criterion 3); geteilte Kalender-Helfer für Reise bereit.
</success_criteria>

<output>
Create `.planning/phases/05-welt-story/05-05-SUMMARY.md` when done
</output>
