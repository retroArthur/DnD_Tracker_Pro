---
phase: 05-welt-story
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - tools/generate_npc_tables.py
  - tools/generate_reise_tables.py
  - features/npc-generator/npc-default-tables.js
  - features/reise/reise-default-tables.js
autonomous: true
requirements: [WELT-02, WELT-04]
must_haves:
  truths:
    - "tools/generate_npc_tables.py erzeugt features/npc-generator/npc-default-tables.js mit NPC_DEFAULT_TABLES"
    - "tools/generate_reise_tables.py erzeugt features/reise/reise-default-tables.js mit REISE_BEGEGNUNGS_TABELLEN und WETTER_TABELLEN"
    - "NPC_DEFAULT_TABLES enthält Namen für 7 Völker × Geschlechter plus Persönlichkeitszüge, Marotten, Berufe, Aussehen"
    - "WETTER_TABELLEN deckt Klima gemässigt × 4 Jahreszeiten ab; Begegnungstabellen decken 6 Geländearten ab"
    - "python build.py läuft mit den generierten Dateien fehlerfrei durch"
  artifacts:
    - path: "tools/generate_npc_tables.py"
      provides: "Build-Time-Generator für NPC-Tabellen"
      contains: "NPC_DEFAULT_TABLES"
    - path: "tools/generate_reise_tables.py"
      provides: "Build-Time-Generator für Reise-/Wettertabellen"
      contains: "WETTER_TABELLEN"
    - path: "features/npc-generator/npc-default-tables.js"
      provides: "Generierte deutsche NPC-Tabellen"
      contains: "NPC_DEFAULT_TABLES"
    - path: "features/reise/reise-default-tables.js"
      provides: "Generierte Begegnungs-/Wettertabellen"
      contains: "REISE_BEGEGNUNGS_TABELLEN"
  key_links:
    - from: "tools/generate_npc_tables.py"
      to: "features/npc-generator/npc-default-tables.js"
      via: "python-Skript schreibt JS-Datei"
      pattern: "window.NPC_DEFAULT_TABLES"
    - from: "tools/generate_reise_tables.py"
      to: "features/reise/reise-default-tables.js"
      via: "python-Skript schreibt JS-Datei"
      pattern: "window.WETTER_TABELLEN"
---

<objective>
Die großen deutschen Default-Tabellen für NPC-Generator (Namen je Volk+Geschlecht, Persönlichkeitszüge, Marotten, Berufe, Aussehen) und Reise-Simulator (gelände-spezifische Begegnungstabellen, Klima×Jahreszeit-Wettertabellen) per Build-Time-Python-Skript erzeugen — NICHT hand-tippen.

Purpose: Die Tabellen umfassen ~800-1000 Einträge und überschreiten das 32k-Output-Limit eines einzelnen Implementierungs-Agenten (Lektion aus Phase 3: SRD-Statblocks). Generator-Skripte halten die Rohdaten als Python-Dictionarys und schreiben reproduzierbare, dedup-sichere JS-Konstanten. Diese füllen die in Plan 01 angelegten leeren Skelett-Tabellen-Dateien.
Output: Zwei reproduzierbare Generator-Skripte + zwei generierte JS-Datendateien, im Build registriert (bereits durch Plan 01 in loader.js/build.py eingetragen — diese Dateien werden hier nur inhaltlich ersetzt).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-welt-story/05-RESEARCH.md

<interfaces>
<!-- Konsumformat: rollWeightedEntry(table) erwartet { entries: [{range:'1-2', text:'...'}], diceType: 8 } für Range-Tabellen. -->
<!-- Für NPC-Namen reichen String-Arrays (Generator nutzt Math.random()-Index, siehe Plan 04). -->

NPC_DEFAULT_TABLES (Zielstruktur, aus 05-RESEARCH.md):
  { namen: { mensch: {maennlich:[...], weiblich:[...], neutral:[...]}, elf:{...}, zwerg:{...}, halbling:{...}, halbork:{...}, tiefling:{...}, gnom:{...} },
    persoenlichkeitszuege: [...], marotten: [...], berufe: [...], aussehen: [...] }

WETTER_TABELLEN (Zielstruktur): { gemässigt: { winter: {id, diceType:8, entries:[{range,text}]}, fruehling:{...}, sommer:{...}, herbst:{...} } }
REISE_BEGEGNUNGS_TABELLEN (Zielstruktur): { wald: {id, diceType:8, entries:[{range,text}]}, gebirge:{...}, kueste:{...}, strasse:{...}, ruinen:{...}, sumpf:{...} }

Bestehender Generator-Präzedenzfall: Phase 3 SRD-Statblocks via Build-Time-Python-Skript (siehe Memory large-dataset-via-build-script).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: tools/generate_npc_tables.py + npc-default-tables.js</name>
  <read_first>
    - features/npc-generator/npc-default-tables.js (Plan-01-Skelett mit leerem NPC_DEFAULT_TABLES + window-Export — wird ersetzt)
    - features/random-tables.js (Konsumformat-Referenz: rollWeightedEntry, esc-Verwendung)
    - tools/purge-css.py (Muster: script-relative Pfade via os.path, UTF-8-Schreiben — Vorlage für sauberen Generator)
    - .planning/phases/05-welt-story/05-RESEARCH.md (§ Strategie für Deutsche Default-Tabellen — Mindest-Einträge-Tabelle, Skript-Ansatz)
  </read_first>
  <action>
    Erstelle tools/generate_npc_tables.py: hält die Rohdaten als Python-Dicts/Lists (deutsche, setting-passende Inhalte) und schreibt features/npc-generator/npc-default-tables.js. Mindestumfang aus 05-RESEARCH.md § Repräsentative Tabelleninhalte: menschliche Vornamen m/w je ≥20, Elf m/w je ≥15, Zwerg m/w je ≥15, Halbling ≥12, Halbork ≥10, Tiefling ≥10, Gnom ≥10; persoenlichkeitszuege ≥20, marotten ≥20, berufe ≥30, aussehen ≥20. Skript nutzt script-relative Pfade (os.path.dirname(__file__)) und schreibt UTF-8.
    Die generierte Datei beginnt mit `// [SECTION:NPC_DEFAULT_TABLES]` + Warnkommentar "Generiert von tools/generate_npc_tables.py — nicht manuell bearbeiten!", deklariert `const NPC_DEFAULT_TABLES = {...}` und endet mit `window.NPC_DEFAULT_TABLES = NPC_DEFAULT_TABLES;`. KEINE Top-Level-Funktionen in der generierten Datei (nur die const + window-Export) → keine Duplikat-Falle. Skript danach ausführen, sodass npc-default-tables.js inhaltlich befüllt ist.
  </action>
  <verify>
    <automated>cd "D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular" && python tools/generate_npc_tables.py && node -e "const fs=require('fs'); const s=fs.readFileSync('features/npc-generator/npc-default-tables.js','utf8'); global.window={}; eval(s); const T=window.NPC_DEFAULT_TABLES; if(!T.namen.zwerg.maennlich.length||!T.namen.mensch.weiblich.length) throw new Error('Namen fehlen'); if(T.persoenlichkeitszuege.length<20||T.marotten.length<20||T.berufe.length<30||T.aussehen.length<20) throw new Error('Mindestmengen unterschritten'); console.log('OK', Object.keys(T.namen).length, 'Völker');"</automated>
  </verify>
  <acceptance_criteria>
    - tools/generate_npc_tables.py existiert, läuft idempotent und nutzt script-relative Pfade.
    - features/npc-generator/npc-default-tables.js exportiert window.NPC_DEFAULT_TABLES mit namen für 7 Völker (mensch, elf, zwerg, halbling, halbork, tiefling, gnom).
    - Mindestmengen: persoenlichkeitszuege ≥20, marotten ≥20, berufe ≥30, aussehen ≥20; Namenslisten erfüllen die Research-Minima.
    - Generierte Datei enthält KEINE Top-Level-Funktionsdeklaration.
  </acceptance_criteria>
  <done>Generator-Skript + befüllte NPC-Tabellendatei vorhanden; node-Assertion bestanden.</done>
</task>

<task type="auto">
  <name>Task 2: tools/generate_reise_tables.py + reise-default-tables.js, Build-Verifikation</name>
  <read_first>
    - features/reise/reise-default-tables.js (Plan-01-Skelett mit leeren REISE_BEGEGNUNGS_TABELLEN/WETTER_TABELLEN — wird ersetzt)
    - features/random-tables.js (rollWeightedEntry + parseRange: Range-Format '1-2', diceType — Begegnungs-/Wettertabellen müssen dieses Format treffen)
    - tools/generate_npc_tables.py (in Task 1 erstelltes Skript als Muster für Pfade/Schreiblogik)
    - .planning/phases/05-welt-story/05-RESEARCH.md (§ Wetter-Tabellen-Schema, § Repräsentative Tabelleninhalte — 6 Gelände-Begegnungstabellen 1W8, 4 Wettertabellen je 1W8)
  </read_first>
  <action>
    Erstelle tools/generate_reise_tables.py analog: schreibt features/reise/reise-default-tables.js mit `const REISE_BEGEGNUNGS_TABELLEN = {...}` (Schlüssel wald, gebirge, kueste, strasse, ruinen, sumpf — je {id, diceType:8, entries:[{range,text}]} mit ≥8 lückenlosen Range-Einträgen 1-8) und `const WETTER_TABELLEN = { 'gemässigt': { winter:{...}, fruehling:{...}, sommer:{...}, herbst:{...} } }` (je {id, diceType:8, entries:[{range,text}]}, ≥8 Einträge, deutsche Wetterbeschreibungen mit Jahreszeit-Bezug). entries-range-Strings müssen 1..8 lückenlos abdecken (kompatibel zu parseRange/rollWeightedEntry). Datei beginnt mit [SECTION:REISE_DEFAULT_TABLES] + Generiert-Warnung, endet mit window-Exporten beider Konstanten; KEINE Top-Level-Funktionen.
    Skript ausführen. Danach den vollen Build verifizieren, damit die generierten Dateien im Bundle dedup-sicher sind.
  </action>
  <verify>
    <automated>cd "D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular" && python tools/generate_reise_tables.py && node -e "const fs=require('fs'); const s=fs.readFileSync('features/reise/reise-default-tables.js','utf8'); global.window={}; eval(s); const B=window.REISE_BEGEGNUNGS_TABELLEN, W=window.WETTER_TABELLEN; ['wald','gebirge','kueste','strasse','ruinen','sumpf'].forEach(k=>{if(!B[k]||B[k].entries.length<8)throw new Error('Gelände fehlt: '+k)}); ['winter','fruehling','sommer','herbst'].forEach(s=>{if(!W['gemässigt'][s]||W['gemässigt'][s].entries.length<8)throw new Error('Wetter fehlt: '+s)}); console.log('OK');" && set PYTHONIOENCODING=utf-8 && python build.py</automated>
  </verify>
  <acceptance_criteria>
    - tools/generate_reise_tables.py existiert und schreibt features/reise/reise-default-tables.js.
    - REISE_BEGEGNUNGS_TABELLEN deckt 6 Gelände (wald, gebirge, kueste, strasse, ruinen, sumpf) mit je ≥8 Range-Einträgen ab.
    - WETTER_TABELLEN['gemässigt'] deckt winter/fruehling/sommer/herbst mit je ≥8 Range-Einträgen ab.
    - entries treffen das Range-Format (1-8 lückenlos, kompatibel zu rollWeightedEntry).
    - `python build.py` läuft fehlerfrei durch (keine Duplikate durch generierte Dateien).
  </acceptance_criteria>
  <done>Generierte Reise-/Wettertabellen vorhanden; Build grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generierte Tabelleninhalte → DOM (in späteren Plänen) | Begegnungs-/Namens-/Wettertexte werden später in Plan 04/06 gerendert |
| keine Netzwerkgrenze | Offline/lokal; Generator läuft zur Build-Zeit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | generierte JS-Datei enthält versehentlich Code statt Daten | mitigate | Generator schreibt ausschließlich `const <NAME> = {literal}` + window-Export, keine Funktionen; node-eval-Assertion prüft reine Datenstruktur |
| T-05-05 | Injection (XSS-Vorstufe) | Tabellentexte werden später ungeschützt gerendert | accept | Inhalte sind statisch/entwicklerdefiniert (kein Nutzerinput); die rendernden Pläne 04/06 escapen mit esc() — Disposition dort (T-05-12/T-05-17) |
| T-05-06 | Denial of Service | überlange Range-Expansion in parseRange | accept | Begegnungstabellen nutzen feste 1-8-Ranges; parseRange hat bereits MAX_RANGE_SIZE=100-Schutz |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | Keine neuen Pakete; Generatoren nutzen nur Python-Standardbibliothek |
</threat_model>

<verification>
- Beide Generator-Skripte laufen idempotent und reproduzierbar.
- node-eval beider generierter Dateien liefert valide Strukturen mit Mindestmengen.
- `python build.py` grün mit eingebundenen generierten Dateien.
</verification>

<success_criteria>
- NPC- und Reise-/Wettertabellen vollständig generiert (nicht hand-getippt), im Build dedup-sicher.
- Manuelle Stichprobe (separate Verifikation): deutsche Inhalte lesen sich setting-plausibel (siehe VALIDATION § Manual-Only).
</success_criteria>

<output>
Create `.planning/phases/05-welt-story/05-02-SUMMARY.md` when done
</output>
