---
phase: 03-bestiary
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-03"]
autonomous: true
requirements: [BEST-02]
files_modified:
  - features/bestiary/bestiary-editor.js
  - features/bestiary/bestiary-crud.js
  - assets/templates/view-bestiary.html
  - assets/styles/bestiary.css
  - tests/e2e/features/bestiary.spec.js
user_setup: []

must_haves:
  truths:
    - "User clicks '+ Neue Kreatur', fills the full statblock form, saves, and the creature appears in the combined list with an 'Eigen' badge (BEST-02, D-12)"
    - "User edits a custom creature's stats and the change persists; deletes it with confirmation and it disappears (BEST-02 SC2, D-13)"
    - "Every create/edit/delete is Undo-able via Ctrl+Z (saveUndoState before each mutation; deleteWithConfirm/afterCrudOperation) (D-13)"
    - "Custom creatures use the exact same full D-04 statblock schema as SRD monsters incl. legendary actions, reactions, senses (D-04, D-12)"
    - "Custom creatures can carry an optional URL portrait via the existing avatar system; SRD monsters cannot (D-10)"
  artifacts:
    - path: "features/bestiary/bestiary-editor.js"
      provides: "openBestiaryEditor(id) opens/pre-fills the bst-* form modal"
      exports: ["openBestiaryEditor"]
      min_lines: 60
    - path: "features/bestiary/bestiary-crud.js"
      provides: "saveBestiary() + deleteBestiaryEntry() with Undo"
      exports: ["saveBestiary", "deleteBestiaryEntry"]
      min_lines: 60
    - path: "assets/templates/view-bestiary.html"
      provides: "#bestiary-editor-modal with bst-* fields"
      contains: "bestiary-editor-modal"
  key_links:
    - from: "view-bestiary.html + Neue Kreatur / Bearbeiten buttons"
      to: "features/bestiary/bestiary-editor.js openBestiaryEditor"
      via: "data-action=call data-value=openBestiaryEditor"
      pattern: "openBestiaryEditor"
    - from: "features/bestiary/bestiary-crud.js saveBestiary/deleteBestiaryEntry"
      to: "D.bestiary + saveUndoState + afterCrudOperation"
      via: "CRUD-with-Undo"
      pattern: "saveUndoState|deleteWithConfirm"
---

<objective>
Add full custom-creature CRUD: a dedicated `bst-*` editor modal that captures the complete D-04 statblock (the encounter form's superset), plus save/delete with Undo. Custom creatures share the SRD schema exactly so the plan-03 detail panel, clickable dice, and plan-05 takeover work identically for them.

Purpose: BEST-02 SC2 — user creates, edits, deletes their own creatures, all Undo-able.
Output: A working creature editor + CRUD service + the CRUD/Undo E2E turned real.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/CLAUDE.md
@D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/.planning/phases/03-bestiary/03-UI-SPEC.md
@D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/.planning/phases/03-bestiary/03-PATTERNS.md

<interfaces>
Editor strategy (RESEARCH § Editor-Strategie, Discretion-resolved): a DEDICATED bestiary editor with `bst-*` field IDs — do NOT reuse saveEncounter()/enc-* (avoids DOM-ID collision since both tabs coexist in the DOM). Mirror the encounters-crud.js field-collection idioms (saves checkbox+value, resistance chips, rich-text innerHTML) but write to D.bestiary.

Full schema to capture (must equal SRD schema from plan 02): name, size, creatureType, alignment, cr (string), xp, ac, acInfo, hp, hpFormula, speed{walk,fly,swim,climb,burrow}, str..cha, savingThrows{}, skills{}, damageResistances[], damageImmunities[], conditionImmunities[], senses[], languages[], traits (sanitized HTML), actions, reactions, legendaryActions, legendaryActionsPerRound, avatar, plus `source:'custom'` and numeric `id`.

CRUD helpers (utils/crud-helpers.js): `deleteWithConfirm({ entityType:'bestiary', id, onSuccess })`; `afterCrudOperation(renderFn, toastMsg)`; `saveUndoState(label)` BEFORE every mutation; `nextId('bestiary')`; `parseEntityId(id)` for all comparisons. (encounters-crud.js saveEncounter shows the exact save/edit shape at lines 27–160.)

Avatar (D-10): `showAvatarModal('bestiary', id)` from systems/avatars.js for the optional portrait (custom only). validateAvatarURL() inside avatars.js already guards the URL.

Modal helpers: showModal('bestiary-editor-modal') / hideModal(...) — same modal system used elsewhere. Editor titles: `Neue Kreatur` (create) / `Kreatur bearbeiten` (edit); save button `Kreatur speichern`; no-name error `⚠️ Name erforderlich — bitte einen Namen eingeben` (frozen strings). Delete confirm string `"%name%" wirklich löschen?`.

renderBestiaryList (plan 03) is the re-render target after every CRUD op. Detail buttons `Bearbeiten`/`Löschen` (custom-only) and `+ Neue Kreatur` already emit `openBestiaryEditor`/delete actions from plan 03/01 wiring.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creature editor modal (#bestiary-editor-modal, bst-* fields) + openBestiaryEditor</name>
  <files>features/bestiary/bestiary-editor.js, assets/templates/view-bestiary.html, assets/styles/bestiary.css</files>
  <read_first>
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/features/encounters/encounters-crud.js (editEnc lines 109–160 — field pre-fill idioms to mirror with bst-* IDs)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/assets/templates/modals-entity.html (existing .modal markup conventions)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/systems/avatars.js (showAvatarModal signature for D-10 portrait)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/.planning/phases/03-bestiary/03-UI-SPEC.md (Component Inventory 5: Kreatur-Editor Modal)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/features/bestiary/bestiary-editor.js (plan-01 stub to fill)
  </read_first>
  <action>
    Add a `#bestiary-editor-modal` (a `.modal`) to `assets/templates/view-bestiary.html` with the full statblock form using `bst-*` field IDs: `bst-edit-id` (hidden), `bst-name`, `bst-size`, `bst-type`, `bst-alignment`, `bst-cr`, `bst-xp`, `bst-ac`, `bst-ac-info`, `bst-hp`, `bst-hp-formula`, speed inputs `bst-speed-walk/fly/swim/climb/burrow`, attribute inputs `bst-str..bst-cha`, saving-throw checkbox+value pairs `bst-save-<attr>`/`bst-save-val-<attr>`, resistance/immunity/condition-immunity chip groups, senses + languages inputs, rich-text contenteditable fields `bst-traits`, `bst-actions`, `bst-reactions`, `bst-legendary`, `bst-legendary-count`, an inline validation error element under the name field, an optional portrait button (`showAvatarModal('bestiary', id)`), a save button labeled `Kreatur speichern` (`data-action="call" data-value="saveBestiary"`) and a cancel button. Title element `#bestiary-editor-title`. Add minimal modal-specific CSS to `assets/styles/bestiary.css`.
    Implement `openBestiaryEditor(id)` in `features/bestiary/bestiary-editor.js` (replace plan-01 stub): create-mode (no id) clears all bst-* fields and sets title `Neue Kreatur`; edit-mode finds the creature in `D.bestiary` via `parseEntityId(id)` and pre-fills every bst-* field incl. innerHTML for the rich-text fields; then `showModal('bestiary-editor-modal')`. Also implement `cleanupBestiaryEditor()` (close modal on tab leave) replacing the plan-01 no-op. Do not use `const X = window.X` inside functions; grep new function names first.
  </action>
  <acceptance_criteria>
    - `assets/templates/view-bestiary.html` contains `id="bestiary-editor-modal"`, `id="bestiary-editor-title"`, `id="bst-name"`, `id="bst-cr"`, `id="bst-legendary"`, `id="bst-legendary-count"`, and a save button with `data-value="saveBestiary"`.
    - `features/bestiary/bestiary-editor.js` exports `openBestiaryEditor`; edit-mode pre-fills bst-* from `D.bestiary` using `parseEntityId`; create-mode sets title `Neue Kreatur`.
    - The modal exposes inputs for legendary actions, reactions, and senses (D-04 completeness for custom creatures).
    - `cleanupBestiaryEditor` is defined (no longer a no-op) and closes the modal.
    - `python build.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python build.py</automated>
  </verify>
  <done>The creature editor modal exists with the full D-04 field set and opens pre-filled for edit / blank for create.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: saveBestiary + deleteBestiaryEntry with Undo</name>
  <files>features/bestiary/bestiary-crud.js</files>
  <read_first>
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/features/encounters/encounters-crud.js (saveEncounter field collection + save/update branch, lines 27–105)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/utils/crud-helpers.js (deleteWithConfirm + afterCrudOperation signatures)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/.planning/phases/03-bestiary/03-PATTERNS.md (bestiary-crud.js block: saveBestiary/deleteBestiaryEntry)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/features/bestiary/bestiary-crud.js (plan-01 stub to fill)
  </read_first>
  <behavior>
    - saveBestiary() reads all bst-* fields into the full schema; rejects empty name with toast `⚠️ Name erforderlich — bitte einen Namen eingeben` and keeps the modal open; on valid input calls `saveUndoState('Kreatur angelegt'|'Kreatur bearbeitet')` BEFORE mutating, then updates D.bestiary[idx] (edit, matched via parseEntityId) or pushes a new entry with `id: nextId('bestiary')` and `source:'custom'`; hides the modal; `afterCrudOperation(renderBestiaryList, 'Kreatur gespeichert')`. trait/action/reaction/legendary read via sanitizeHTML of the contenteditable innerHTML.
    - deleteBestiaryEntry(id) uses `deleteWithConfirm({ entityType:'bestiary', id, onSuccess: () => afterCrudOperation(renderBestiaryList, 'Kreatur gelöscht') })` — which performs saveUndoState + confirm (`"%name%" wirklich löschen?`) + filter.
    - After delete, Ctrl+Z (undo) restores D.bestiary and the list shows the creature again.
  </behavior>
  <action>
    Implement `saveBestiary()` and `deleteBestiaryEntry(id)` in `features/bestiary/bestiary-crud.js` (replace plan-01 stubs) per the behavior + PATTERNS bestiary-crud block, mirroring encounters-crud field collection but writing to `D.bestiary` with the full D-04 schema (incl. reactions/legendaryActions/senses). Use `esc()` is NOT applied at save time (raw value stored); sanitizeHTML only for the rich-text HTML fields. Export both via window. Grep names first; no in-function `const X = window.X`.
  </action>
  <acceptance_criteria>
    - `features/bestiary/bestiary-crud.js` exports `saveBestiary` and `deleteBestiaryEntry`.
    - saveBestiary calls `saveUndoState(` before any D.bestiary mutation and `afterCrudOperation(renderBestiaryList,` after; empty-name path shows the frozen error and does not mutate D.
    - new entries get `source:'custom'` and `id: nextId('bestiary')`; edits match via `parseEntityId`.
    - deleteBestiaryEntry routes through `deleteWithConfirm({ entityType: 'bestiary'`.
    - Saved custom creatures include `reactions`, `legendaryActions`, `legendaryActionsPerRound`, `senses` keys (D-04 parity).
    - `npx playwright test tests/e2e/features/bestiary.spec.js -g "Kreatur anlegen|Kreatur bearbeiten|Kreatur loeschen|Undo loeschen"` passes.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/features/bestiary.spec.js -g "Kreatur anlegen|Kreatur bearbeiten|Kreatur loeschen|Undo loeschen"</automated>
  </verify>
  <done>Create/edit/delete custom creatures with full schema, all Undo-able; CRUD+Undo E2E pass.</done>
</task>

<task type="auto">
  <name>Task 3: Real E2E for create/edit/delete/undo</name>
  <files>tests/e2e/features/bestiary.spec.js</files>
  <read_first>
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/tests/e2e/features/bestiary.spec.js (scaffold from plan 01)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/.planning/phases/03-bestiary/03-VALIDATION.md (BEST-02 SC2 rows incl. Undo)
  </read_first>
  <action>
    Make the `Kreatur anlegen`, `Kreatur bearbeiten`, `Kreatur loeschen`, `Undo loeschen` tests real: open Bestiar, click `+ Neue Kreatur`, fill name + a few stats, save, assert the new row with the `Eigen` badge appears; reopen via `Bearbeiten`, change a stat, save, assert persisted; delete via `Löschen` (accept confirm), assert removed; press Ctrl+Z and assert the creature is back. Keep titles byte-identical.
  </action>
  <acceptance_criteria>
    - The four named tests contain real assertions (no fixme/skip).
    - `npx playwright test tests/e2e/features/bestiary.spec.js -g "Kreatur anlegen|Kreatur bearbeiten|Kreatur loeschen|Undo loeschen"` passes.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/features/bestiary.spec.js -g "Kreatur anlegen|Kreatur bearbeiten|Kreatur loeschen|Undo loeschen"</automated>
  </verify>
  <done>BEST-02 SC2 verified by passing E2E incl. Undo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor form (bst-*) → D.bestiary | User-entered statblock fields persisted to D |
| portrait URL → avatar | User-supplied URL for custom-creature image |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Spoofing | XSS via custom name/type/alignment | mitigate | `esc()` at render (plan 03); rich-text fields stored via `sanitizeHTML()` in saveBestiary |
| T-03-06 | Tampering | XSS via custom traits/actions HTML | mitigate | `sanitizeHTML()` on bst-traits/actions/reactions/legendary innerHTML at save time |
| T-03-08 | Tampering | URL injection in portrait avatar | mitigate | `showAvatarModal`/`validateAvatarURL` (avatars.js) — existing guard, custom-only |
| T-03-09 | Tampering | Lost Undo / data integrity on CRUD | mitigate | `saveUndoState()` before every mutation; deleteWithConfirm performs undo+confirm |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (RESEARCH audit: none) |
</threat_model>

<verification>
- `python build.py` green; editor opens/saves/deletes.
- `npx playwright test tests/e2e/features/bestiary.spec.js -g "Kreatur anlegen|Kreatur bearbeiten|Kreatur loeschen|Undo loeschen"` green.
- `npm run test:unit` green (no regressions).
</verification>

<success_criteria>
BEST-02 SC2 delivered: full-schema custom-creature create/edit/delete with confirmation and working Undo; custom creatures are first-class (same schema, detail panel, dice, takeover) with optional portraits; frozen German copy.
</success_criteria>

<output>
Create `.planning/phases/03-bestiary/03-04-SUMMARY.md` when done.
</output>
