---
phase: 03-bestiary
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
autonomous: true
requirements: [BEST-01]
files_modified:
  - features/bestiary/bestiary-render.js
  - assets/styles/bestiary.css
  - assets/templates/view-bestiary.html
  - tests/e2e/features/bestiary.spec.js
user_setup: []

must_haves:
  truths:
    - "User opens the Bestiar tab, types 'Goblin', and matching monsters appear instantly with no network (BEST-01 SC1, D-07)"
    - "CR (HG) and Typ filters plus 'Nur Eigene' and 'Favoriten' chips narrow the combined SRD+custom list in real time (D-07, D-08, D-11)"
    - "Clicking a list row opens a classic 5e parchment statblock in the detail panel with red headings and the full D-04 layout (D-05, D-06)"
    - "To-hit modifiers and damage formulas in the statblock are clickable and roll via the shared dice roller (D-09)"
    - "Each row shows an SRD/Eigen origin badge and a favorites star; SRD monsters show no portrait, custom ones may (D-08, D-10)"
  artifacts:
    - path: "features/bestiary/bestiary-render.js"
      provides: "renderBestiaryList, renderBestiaryDetail, renderClickableDice, crToSortValue, selectBestiary"
      exports: ["renderBestiaryList", "renderBestiaryDetail", "crToSortValue"]
      min_lines: 120
    - path: "assets/templates/view-bestiary.html"
      provides: "toolbar (search + HG/Typ/Nur-Eigene/Favoriten filters + + Neue Kreatur) and master-detail layout"
      contains: "bestiary-search"
    - path: "assets/styles/bestiary.css"
      provides: "list/detail/statblock/badge/dice styling reusing NPC + parchment tokens"
      contains: "bestiary-statblock"
  key_links:
    - from: "view-bestiary.html #bestiary-search/#bestiary-filter-cr/#bestiary-filter-type"
      to: "features/bestiary/bestiary-render.js renderBestiaryList()"
      via: "data-action=search-input data-render=renderBestiaryList"
      pattern: "renderBestiaryList"
    - from: "bestiary-render.js renderBestiaryList()"
      to: "getSRDMonsters() + D.bestiary + applyFilters + VirtualScroll.create"
      via: "combined list + single-pass filter"
      pattern: "getSRDMonsters|applyFilters"
    - from: "statblock dice spans"
      to: "rollQrefDice()"
      via: "data-action=bestiary-roll-dice (handler in plan 05)"
      pattern: "bestiary-roll-dice"
---

<objective>
Build the visible Bestiary tab: the toolbar + master-detail HTML, the list/detail render functions (combined SRD+custom list, single-pass filter, CR-sorted, virtual-scrolled), the classic 5e parchment statblock detail with clickable dice, and the CSS. Delivers BEST-01's instant offline search/filter/display.

Purpose: BEST-01 SC1 — user searches/filters and reads full statblocks offline.
Output: A populated, styled, interactive Bestiary list + statblock detail, plus the E2E search/filter/dice tests turned real.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/CLAUDE.md
@D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/.planning/phases/03-bestiary/03-UI-SPEC.md
@D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/.planning/phases/03-bestiary/03-PATTERNS.md

<interfaces>
applyFilters(items, { searchText, searchFields, filters:{key:value}, customFilter }) — utils/filter-engine.js; '' / 'all' filter values are ignored; array item fields are matched by .includes. searchFields for bestiary: ['name','creatureType']; filters: { cr, creatureType }.

VirtualScroll.create(container, items, renderItemFn, rowHeight) — ui/virtual-scroll.js; auto-activates above 50 items; rowHeight 52 (UI-SPEC).

rollQrefDice(formula) — systems/spellslots/quick-reference.js:157; rolls any dice formula. (Click handler wiring lives in plan 05's action handler; this plan only emits the data-action spans.)

sanitizeHTML(html) for trait/action text (contains intentional <b>); esc(text) for name/cr/type. NEVER raw-interpolate statblock text.

CR string set + sort: build CR_SORT_ORDER map ('0','1/8','1/4','1/2','1'..'30') and crToSortValue(cr) using `?? parseFloat(cr) ?? 0` (PATTERNS Muster 4).

isBestiaryFavorite(monster) is exported by bestiary-actions.js (plan 05); this plan's stub from plan 01 returns false — render against the exported function (window.isBestiaryFavorite) so it lights up once plan 05 lands. Same for the fav/select/roll data-action handlers (plan 05 owns the handlers).

UI strings + statblock section order are frozen in 03-UI-SPEC.md (Copywriting Contract + section ordering list 1–20). CR label in detail: `Herausforderungsgrad %cr% (%xp% EP)`; in list row: `HG %cr%`. Attribution footer: `Monster: SRD 5.1 DE, CC BY 4.0, Wizards of the Coast`.

Layout tokens: `.bestiary-layout { grid-template-columns: 1fr 825px }`; reuse `.read-aloud.parchment` from assets/styles/party.css for the statblock block; section headings `color: var(--red)` Georgia serif; dice spans `color: var(--cyan)` underline dotted, hover var(--gold).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: view-bestiary.html toolbar + master-detail + bestiary.css</name>
  <files>assets/templates/view-bestiary.html, assets/styles/bestiary.css</files>
  <read_first>
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/assets/templates/view-content.html (NPC view section — exact toolbar+master-detail structure to clone)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/assets/styles/npcs.css (toolbar/list/detail/empty-state tokens to mirror)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/assets/styles/party.css (.read-aloud.parchment rule to reuse)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/.planning/phases/03-bestiary/03-UI-SPEC.md (Component Inventory 1–3, Copywriting Contract, statblock section order)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/assets/templates/view-bestiary.html (plan-01 placeholder section to fill)
  </read_first>
  <action>
    Fill `assets/templates/view-bestiary.html`: a `<section id="view-bestiary" class="view bestiary-view">` with a toolbar (title `🐉 Bestiar` + count `#bestiary-count`; search input `#bestiary-search` placeholder `Monster suchen...` with `data-action="search-input" data-render="renderBestiaryList"`; HG `<select id="bestiary-filter-cr">` and Typ `<select id="bestiary-filter-type">` both with `data-action="search-input" data-render="renderBestiaryList"`; `Nur Eigene` checkbox `#bestiary-filter-custom` and `Favoriten` checkbox `#bestiary-filter-favs` as filter chips; primary `+ Neue Kreatur` button `data-action="call" data-value="openBestiaryEditor"`) and a `.bestiary-layout` master-detail (`.bestiary-master > #bestiary-list`, `.bestiary-detail#bestiary-detail-panel` with the dragon-emoji empty state `Wähle ein Monster aus der Liste`). Use the frozen German strings verbatim.
    Fill `assets/styles/bestiary.css` (replace plan-01 placeholder): list/detail grid `1fr 825px`, list-item card + hover + `.selected` gold border, `.bestiary-badge.srd`/`.custom`, `.bestiary-fav`/`.active`, `.bestiary-statblock` reusing `.read-aloud.parchment`, `.bestiary-statblock-section-heading` red Georgia, `.bestiary-dice` cyan underline-dotted hover-gold, `.bestiary-attribution` footer, and the mobile single-column `[data-layout="mobile"]` rules — all via existing CSS variables, no new palette. Use 4px-grid spacing tokens per UI-SPEC.
  </action>
  <acceptance_criteria>
    - `assets/templates/view-bestiary.html` contains `id="bestiary-search"`, `id="bestiary-filter-cr"`, `id="bestiary-filter-type"`, `id="bestiary-filter-custom"`, `id="bestiary-filter-favs"`, `id="bestiary-list"`, `id="bestiary-detail-panel"`, and the `+ Neue Kreatur` button with `data-value="openBestiaryEditor"`.
    - Every German UI string in the toolbar/empty-state matches the UI-SPEC Copywriting Contract verbatim (e.g. `Monster suchen...`, `Nur Eigene`, `Favoriten`, `Wähle ein Monster aus der Liste`).
    - `assets/styles/bestiary.css` contains `.bestiary-layout` with `grid-template-columns: 1fr 825px`, `.bestiary-statblock`, `.bestiary-badge`, `.bestiary-dice`, and a `[data-layout="mobile"]` single-column rule; no hardcoded hex except the parchment overrides.
    - `python build.py` exits 0 (template + CSS concatenated).
  </acceptance_criteria>
  <verify>
    <automated>python build.py</automated>
  </verify>
  <done>Tab toolbar + master-detail layout + statblock styling exist with frozen German strings; build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: renderBestiaryList + renderBestiaryDetail + clickable dice</name>
  <files>features/bestiary/bestiary-render.js</files>
  <read_first>
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/features/npcs/npc-render.js (list render + applyFilters + VirtualScroll + showNPCDetail analog)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/utils/filter-engine.js (applyFilters signature)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/features/encounter-calculator.js (CR_TO_XP key order for CR_SORT_ORDER)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/systems/spellslots/quick-reference.js (rollQrefDice — for the data-value format the dice spans must emit)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/.planning/phases/03-bestiary/03-PATTERNS.md (bestiary-render.js block: list-item, detail, renderClickableDice)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/features/bestiary/bestiary-render.js (plan-01 stub to fill)
  </read_first>
  <behavior>
    - renderBestiaryList(): merges getSRDMonsters() (source='srd') with D.bestiary (source='custom'), applies applyFilters over searchFields ['name','creatureType'] + filters {cr,creatureType} + a customFilter honoring `#bestiary-filter-custom` (only custom) and `#bestiary-filter-favs` (only favorites via window.isBestiaryFavorite), sorts by crToSortValue, renders rows via VirtualScroll at rowHeight 52, updates #bestiary-count, and shows `Keine Monster gefunden` when empty.
    - Each row shows badge (SRD/Eigen), name, `HG %cr%`, type, and a fav star (`★`/`☆`) with `data-action="bestiary-toggle-fav"`; the row carries `data-action="bestiary-select"` + `data-id` + `data-source`.
    - renderBestiaryDetail(id, source): renders the parchment statblock in the exact UI-SPEC section order (name red, size/type/alignment italic, AC/HP/Speed, attribute 6-col grid with modifiers, saves/skills/resistances/immunities/condition-immunities/senses/languages, `Herausforderungsgrad %cr% (%xp% EP)`, traits/actions/reactions/legendary), action buttons (`Zur Initiative`, `Zu Encounter`, plus `Bearbeiten`/`Löschen` only when source==='custom'), optional portrait for custom, and the attribution footer. trait/action/reaction/legendary text passes through renderClickableDice then sanitizeHTML; name/cr/type via esc.
    - renderClickableDice(text): wraps damage formulas `Nd N(+/-N)` and isolated `+N` to-hit mods in `<span class="bestiary-dice" data-action="bestiary-roll-dice" data-value="...">` (data-value `1d20+N` for to-hit, the formula itself for damage).
    - crToSortValue('1/4') < crToSortValue('1') < crToSortValue('2') (fractions sort correctly).
  </behavior>
  <action>
    Implement these functions in `features/bestiary/bestiary-render.js` (replace the plan-01 stub), following the npc-render analog and PATTERNS bestiary-render block. Use defensive container guards. Maintain module-scope `selectedBestiaryId`/`selectedBestiarySource`. Export `renderBestiaryList`, `renderBestiaryDetail`, `renderClickableDice`, `crToSortValue`, and a `selectBestiary(id, source)` that sets the selection state and calls renderBestiaryDetail + re-renders the list for the selected highlight. Use `window.isBestiaryFavorite` (defined in plan 05) for the fav state and the frozen UI strings. Do NOT use `const X = window.X` inside functions. Grep for each new function name first to avoid duplicates. Populate the HG and Typ filter dropdowns from distinct cr/creatureType values (CR options ordered via crToSortValue).
  </action>
  <acceptance_criteria>
    - `features/bestiary/bestiary-render.js` defines and exports `renderBestiaryList`, `renderBestiaryDetail`, `renderClickableDice`, `crToSortValue`, `selectBestiary`.
    - renderBestiaryList calls `getSRDMonsters()`, `applyFilters(`, and `VirtualScroll.create(` with rowHeight 52; empty results render the literal `Keine Monster gefunden`.
    - renderBestiaryDetail emits `data-action="bestiary-roll-dice"` spans inside trait/action text and renders the section headings (`Eigenschaften`, `Aktionen`, `Reaktionen`, `Legendäre Aktionen`) and `Herausforderungsgrad` label; custom-only buttons are gated on source.
    - All statblock prose goes through `sanitizeHTML(`; names/type via `esc(`.
    - `grep -c "function renderBestiaryList" features/bestiary/bestiary-render.js` returns 1; `python build.py` exits 0.
    - `npx playwright test tests/e2e/features/bestiary.spec.js -g "Goblin suchen|CR-Filter|Typ-Filter|Wuerfelklick"` passes.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/features/bestiary.spec.js -g "Goblin suchen|CR-Filter|Typ-Filter|Wuerfelklick"</automated>
  </verify>
  <done>List filters/sorts/searches instantly; detail renders the full 5e statblock with clickable dice; search/filter/dice E2E pass.</done>
</task>

<task type="auto">
  <name>Task 3: Real E2E for search, CR/Typ filter, and dice click</name>
  <files>tests/e2e/features/bestiary.spec.js</files>
  <read_first>
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/tests/e2e/features/bestiary.spec.js (the plan-01 scaffold to make real)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/tests/e2e/features/wiki.spec.js (assertion/navigation idioms)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/.planning/phases/03-bestiary/03-VALIDATION.md (BEST-01 SC1 + D-09 rows)
  </read_first>
  <action>
    Turn the `Goblin suchen`, `CR-Filter`, `Typ-Filter`, and `Wuerfelklick` placeholders into real assertions: open the 🐉 Bestiar tab; type `Goblin` in `#bestiary-search` and assert a Goblin row appears and non-matching rows do not; set `#bestiary-filter-cr` to `1/4` and assert only 1/4-CR monsters remain; set `#bestiary-filter-type` to `Humanoid` and assert the result set matches; select a monster, click a `.bestiary-dice` damage span and assert a dice result surfaces (roll history/toast). Keep test titles byte-identical to the contract names. Do not enable network; the dist build is offline.
  </action>
  <acceptance_criteria>
    - Tests `Goblin suchen`, `CR-Filter`, `Typ-Filter`, `Wuerfelklick` are no longer skipped/fixme and contain real `expect` assertions against bestiary DOM.
    - `npx playwright test tests/e2e/features/bestiary.spec.js -g "Goblin suchen|CR-Filter|Typ-Filter|Wuerfelklick"` passes.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/features/bestiary.spec.js -g "Goblin suchen|CR-Filter|Typ-Filter|Wuerfelklick"</automated>
  </verify>
  <done>BEST-01 SC1 + D-09 verified by passing E2E.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| statblock text → innerHTML (detail panel) | Trait/action strings (intentional <b>) rendered into DOM |
| custom-creature fields → list/detail | User-entered name/type rendered into DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | XSS via statblock traits/actions | mitigate | `sanitizeHTML()` (allows <b>, blocks <script>) for all trait/action/reaction/legendary prose; renderClickableDice runs before sanitize, output still sanitized |
| T-03-07 | Spoofing | XSS via custom name/type/cr in rows | mitigate | `esc()` for all name/cr/type/alignment interpolations in list + detail |
| T-03-02 | Denial of Service | SRD data into D | mitigate | Render reads getSRDMonsters() directly; never writes SRD into D |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (RESEARCH audit: none) |
</threat_model>

<verification>
- `python build.py` green; tab renders populated list + statblock.
- `npx playwright test tests/e2e/features/bestiary.spec.js -g "Goblin suchen|CR-Filter|Typ-Filter|Wuerfelklick"` green.
- Manual offline spot-check (VALIDATION manual row): DevTools offline → search/filter still instant.
</verification>

<success_criteria>
BEST-01 SC1 delivered: instant offline search + CR/Typ/Eigene/Favoriten filtering of a combined SRD+custom list, classic 5e parchment statblock detail with clickable dice, origin badges, and favorites star — all with frozen German copy and existing design tokens.
</success_criteria>

<output>
Create `.planning/phases/03-bestiary/03-03-SUMMARY.md` when done.
</output>
