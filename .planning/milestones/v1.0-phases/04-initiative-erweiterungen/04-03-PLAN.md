---
phase: 04-initiative-erweiterungen
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - features/initiative.js
  - features/bestiary/bestiary-actions.js
  - ui/actions/combat-actions.js
  - tests/e2e/features/initiative.spec.js
autonomous: true
requirements: [INIT-02]

must_haves:
  truths:
    - "LA pips appear in a combatant row only when legendaryActionsPerRound > 0 (D-09 auto-display)"
    - "LR pips appear only when a Legendaere Resistenz trait is parsed from the statblock (D-06 detection-only)"
    - "Clicking an LA/LR pip decrements remaining by one with Death-Save toggle feel (D-08)"
    - "LA pips auto-refill at round wrap (Initiative 20 / currentTurn back to 0) in nextTurn() (D-10, SC2 for LA)"
    - "LR pips do NOT auto-reset on round wrap; a manual reset button restores them (D-07 deliberate deviation from SC2 wording)"
    - "LA/LR fields are initialised on combatants when added from the bestiary (runtime-only, no migration)"
  artifacts:
    - path: "features/initiative.js"
      provides: "renderLegendaryActionPips, renderLegendaryResistancePips, useLA, useLR, resetLR; nextTurn LA-reset (NOT LR)"
      contains: "renderLegendaryActionPips"
    - path: "features/bestiary/bestiary-actions.js"
      provides: "LA/LR field init on combatants via legendaryActionsPerRound and parseLegendaryResistanceCount"
      contains: "legendaryActions"
    - path: "ui/actions/combat-actions.js"
      provides: "init-use-la-stop, init-use-lr-stop, init-reset-lr-stop handlers"
      contains: "init-use-la-stop"
  key_links:
    - from: "features/initiative.js renderInit()"
      to: "renderLegendaryActionPips / renderLegendaryResistancePips"
      via: "conditional render in the init-info detail area"
      pattern: "renderLegendaryActionPips"
    - from: "features/initiative.js nextTurn()"
      to: "legendaryActions.remaining reset"
      via: "round-wrap if-block, LA only (NOT LR)"
      pattern: "legendaryActions"
    - from: "features/bestiary/bestiary-actions.js"
      to: "parseLegendaryResistanceCount"
      via: "LR field init when adding combatant"
      pattern: "parseLegendaryResistanceCount"
---

<objective>
Implement INIT-02: clickable round-counters for Legendaere Aktionen (LA) and Legendaere Resistenzen (LR) on boss combatants, styled exactly like the existing Death-Save dots (D-08). LA auto-resets at the round wrap in nextTurn() (D-10, regelkonform per-round); LR does NOT auto-reset and instead has a manual reset button (D-07 deliberate deviation from the Roadmap SC2 wording, which would make a boss effectively immune to saves).

LA/LR fields are initialised on combatants when they are added from the bestiary, using the existing legendaryActionsPerRound field and the Wave-0 parseLegendaryResistanceCount() regex.

Purpose: SC2 — the DM counts and resets legendary actions/resistances during a boss fight, with rules-correct reset semantics.
Output: LA/LR pips + handlers + nextTurn LA-reset + field init + green legendary E2E suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-initiative-erweiterungen/04-PATTERNS.md
@.planning/phases/04-initiative-erweiterungen/04-UI-SPEC.md
@.planning/phases/04-initiative-erweiterungen/04-VALIDATION.md
@.planning/phases/04-initiative-erweiterungen/04-01-SUMMARY.md
@.planning/phases/04-initiative-erweiterungen/04-02-SUMMARY.md

<interfaces>
From features/initiative-mob.js (Wave 0, window-exported):
  function parseLegendaryResistanceCount(monster)  // returns int (0 = no LR detected)
From features/initiative.js:
  function getCombatant(id)
  function renderDeathSaves(cb)   // lines 494-545 — exact pip/dot structure analog
  function toggleDeathSave(cbId, type, index)  // lines 547-577 — exact toggle logic analog
  function nextTurn()             // lines 372-390 — round-wrap if-block at 384-387
From core/srd-monsters.js schema:
  monster.legendaryActionsPerRound  // integer (0 or 3 for the 6 boss monsters)
  monster.traits[]  // contains "Legendaere Resistenz (3-mal taeglich)" for LR

Combatant runtime fields (no migration):
  cb.legendaryActions  = { max, remaining }
  cb.legendaryResistance = { max, remaining }

Pip DOM contract (UI-SPEC INIT-02):
  LA: div.la-pips > span.la-label (gold star LA) + div.la-dots > span.la-dot[.active] data-action init-use-la-stop data-id data-index
  LR: div.lr-pips[title] > span.lr-label (shield LW) + div.lr-dots > span.lr-dot[.active] data-action init-use-lr-stop + button.lr-reset-btn data-action init-reset-lr-stop
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render LA/LR pips and implement use/reset handlers + nextTurn LA-reset</name>
  <files>features/initiative.js</files>
  <read_first>
    - features/initiative.js (renderDeathSaves lines 494-545 for the dot structure; toggleDeathSave lines 547-577 for the toggle logic; renderInit lines 174-181 where pips are inserted into init-info; nextTurn lines 372-390, round-wrap if-block at 384-387)
    - .planning/phases/04-initiative-erweiterungen/04-PATTERNS.md (lines 136-264 renderLegendaryActionPips/renderLegendaryResistancePips, useLA/useLR/resetLR, nextTurn LA-reset insertion)
    - .planning/phases/04-initiative-erweiterungen/04-RESEARCH.md (Muster 2 lines 288-339; Falle 4 LA-reset-not-LR lines 511-517; nextTurn example lines 645-674)
    - .planning/phases/04-initiative-erweiterungen/04-UI-SPEC.md (INIT-02 LA lines 224-287; INIT-02 LR lines 290-374; Copywriting lines 116-136)
  </read_first>
  <action>
    In features/initiative.js add four functions modeled on renderDeathSaves/toggleDeathSave:
    - renderLegendaryActionPips(cb): if no cb.legendaryActions or max <= 0 return empty string. Build div.la-pips with title "Setzt sich bei Initiative 20 zurueck", a span.la-label reading the gold star plus LA, and div.la-dots with cb.legendaryActions.max dots. Each dot is span.la-dot with the active class when index < remaining, data-action init-use-la-stop, data-id cb.id, data-index i, title "Legendaere Aktion {i+1} verwenden" (or verbraucht when used).
    - renderLegendaryResistancePips(cb): if no cb.legendaryResistance or max <= 0 return empty string. Build div.lr-pips with title "Pro Tag - kein automatischer Reset", a span.lr-label reading the shield plus LW, div.lr-dots with cb.legendaryResistance.max dots (class lr-dot, data-action init-use-lr-stop, data-id, data-index, titles per UI-SPEC), and a trailing button.lr-reset-btn.btn-icon with data-action init-reset-lr-stop, data-id, title "Legendaeren Widerstand zuruecksetzen (Lange Rast)", label reset-arrow.
    - useLA(cbId, index): get cb; if no legendaryActions return. Toggle exactly like toggleDeathSave: if index < remaining then remaining = index else remaining = index + 1. renderInit(); window.save().
    - useLR(cbId, index): identical but on cb.legendaryResistance.remaining.
    - resetLR(cbId): set cb.legendaryResistance.remaining = cb.legendaryResistance.max; renderInit(); window.save().
    In renderInit() insert the two pip blocks into the standard combatant template inside .init-info after the effects row (around lines 177-181): render renderLegendaryActionPips(cb) when cb.legendaryActions && cb.legendaryActions.max > 0, and renderLegendaryResistancePips(cb) when cb.legendaryResistance && cb.legendaryResistance.max > 0. Do NOT add pips to the lair branch.
    In nextTurn() inside the round-wrap if-block (after init.round++ at line 386), add a forEach over init.combatants that, for each c with c.legendaryActions && c.legendaryActions.max > 0, sets c.legendaryActions.remaining = c.legendaryActions.max. Add the comments: D-10 LA-Reset bei Rundenuebergang, and D-07 LR KEIN Auto-Reset. Do NOT touch any legendaryResistance field here.
    Export renderLegendaryActionPips, renderLegendaryResistancePips, useLA, useLR, resetLR to window only if needed by other modules (handlers live in combat-actions). No const X = window.X inside functions.
  </action>
  <verify>
    <automated>cd "D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular" && python build.py</automated>
    <source_check>grep "renderLegendaryActionPips" features/initiative.js matches; grep "renderLegendaryResistancePips" features/initiative.js matches; grep "init-use-la-stop" features/initiative.js matches; grep "init-reset-lr-stop" features/initiative.js matches; grep -A3 "init.round++" features/initiative.js shows legendaryActions reset but NOT legendaryResistance inside the round-wrap block</source_check>
  </verify>
  <acceptance_criteria>
    - renderInit() conditionally renders both pip blocks; lair rows are unaffected
    - nextTurn() resets legendaryActions.remaining on round wrap and never mutates legendaryResistance (D-07)
    - useLA/useLR mirror the toggleDeathSave increment/decrement logic; resetLR restores to max
    - python build.py completes with no duplicate-declaration or orphaned-body error
  </acceptance_criteria>
  <done>LA pips auto-reset at round wrap, LR pips stay put with a manual reset; pip clicks toggle correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Initialise LA/LR fields on combatants in addBestiaryToInitiative + register handlers</name>
  <files>features/bestiary/bestiary-actions.js, ui/actions/combat-actions.js</files>
  <read_first>
    - features/bestiary/bestiary-actions.js (addBestiaryToInitiative lines 64-129; the per-combatant push at lines 106-119; getBestiaryMonster lines 11-18 for the monster reference)
    - features/initiative-mob.js (parseLegendaryResistanceCount, window-exported from Wave 0)
    - ui/actions/combat-actions.js (CombatActions object; toggle-death-save-stop at line 10 as the -stop + stopPropagation analog)
    - .planning/phases/04-initiative-erweiterungen/04-PATTERNS.md (lines 471-511 LA/LR field init in addBestiaryToInitiative; lines 515-562 combat-actions -stop registration)
    - .planning/phases/04-initiative-erweiterungen/04-RESEARCH.md (Pill-Initialisierung lines 563-578)
  </read_first>
  <action>
    In features/bestiary/bestiary-actions.js addBestiaryToInitiative(), inside the for-loop that pushes each combatant (lines 89-119), compute once per call: var laCount = (monster.legendaryActionsPerRound || 0); var lrCount = window.parseLegendaryResistanceCount ? window.parseLegendaryResistanceCount(monster) : 0. After constructing each combatant object (or by attaching to the pushed object), set cbObj.legendaryActions = { max: laCount, remaining: laCount } when laCount > 0, and cbObj.legendaryResistance = { max: lrCount, remaining: lrCount } when lrCount > 0. These are runtime-only fields (no migration). Keep the existing fields and numbering unchanged.
    In ui/actions/combat-actions.js register three handlers using the -stop + ctx.event.stopPropagation() pattern (mirrors toggle-death-save-stop):
    - init-use-la-stop: ctx => { ctx.event.stopPropagation(); useLA(ctx.id, parseInt(ctx.target.dataset.index)); }
    - init-use-lr-stop: ctx => { ctx.event.stopPropagation(); useLR(ctx.id, parseInt(ctx.target.dataset.index)); }
    - init-reset-lr-stop: ctx => { ctx.event.stopPropagation(); resetLR(ctx.id); }
  </action>
  <verify>
    <automated>cd "D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular" && python build.py</automated>
    <source_check>grep "parseLegendaryResistanceCount" features/bestiary/bestiary-actions.js matches; grep "legendaryActions" features/bestiary/bestiary-actions.js matches; grep "init-use-la-stop" ui/actions/combat-actions.js matches; grep "init-use-lr-stop" ui/actions/combat-actions.js matches; grep "init-reset-lr-stop" ui/actions/combat-actions.js matches</source_check>
  </verify>
  <acceptance_criteria>
    - Combatants added from a boss monster carry legendaryActions and legendaryResistance fields when detected
    - All three pip handlers are registered with stopPropagation via the -stop suffix
    - python build.py completes without error
  </acceptance_criteria>
  <done>Bestiary-added bosses arrive in initiative with their LA/LR counters populated and clickable.</done>
</task>

<task type="auto">
  <name>Task 3: Fill the legendary E2E suite, build, and run it</name>
  <files>tests/e2e/features/initiative.spec.js</files>
  <read_first>
    - tests/e2e/features/initiative.spec.js (the skipped legendary describe stub from Wave 0; beforeEach navigation; how a bestiary boss reaches initiative)
    - features/bestiary/bestiary-actions.js (how to add a boss with legendaryActionsPerRound > 0 to initiative)
    - .planning/phases/04-initiative-erweiterungen/04-VALIDATION.md (INIT-02 rows of Anforderungs-Test-Mapping; LR-resets-NOT-on-round-wrap behavior; the legendary filter command)
    - .planning/phases/04-initiative-erweiterungen/04-UI-SPEC.md (selectors la-pips, la-dot, lr-pips, lr-dot, lr-reset-btn; data-action next-turn for round advance)
  </read_first>
  <action>
    Replace the Wave-0 skip placeholders in the legendary describe block with real Playwright tests against the freshly built dist bundle. Seed a boss monster with legendaryActionsPerRound > 0 and a Legendaere-Resistenz trait into initiative (via the bestiary add flow or by injecting D.initiative.combatants through page.evaluate, then renderInit()). Cover:
    - LA pips appear (la-dot present) for a boss; absent for a plain combatant.
    - Clicking an active la-dot reduces the number of active la-dot elements by one.
    - Advancing a full round via next-turn until the round increments refills the la-dot active count back to max (Init-20 auto-reset).
    - LR pips appear (lr-dot present) when the LR trait is parsed.
    - After advancing a full round, the active lr-dot count is UNCHANGED (D-07 no auto-reset).
    - Clicking the lr-reset-btn restores all lr-dot active states.
    Keep every test title containing the word legendary so the -g filter selects them.
    Run python build.py first, then run the legendary E2E subset.
  </action>
  <verify>
    <automated>cd "D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular" && python build.py && npm run test:e2e -- tests/e2e/features/initiative.spec.js -g legendary</automated>
  </verify>
  <acceptance_criteria>
    - The legendary E2E subset exits 0 (LA appears+decrements+auto-resets; LR appears+does-not-auto-reset+manual-resets)
    - The legendary describe block has no test.skip/test.fixme placeholders remaining
    - The D-07 no-auto-reset behavior is explicitly asserted (LR active count unchanged after a full round)
  </acceptance_criteria>
  <done>The legendary E2E suite is green and locks in the rules-correct LA-resets / LR-does-not-reset distinction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| monster.traits text -> parseLegendaryResistanceCount | Trait names parsed by regex for the LR count |
| cb.id / data-index -> DOM attributes and handlers | Pip clicks carry combatant id and pip index |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering | parseLegendaryResistanceCount over attacker-controlled trait text | mitigate | Regex match + parseInt only; no DOM write, no eval. Returns 0 on no match. |
| T-04-08 | Tampering (XSS) | pip dots write cb.id/index into data attributes | mitigate | cb.id is an internal numeric id; data-index is a loop integer. parseInt(ctx.target.dataset.index) in the handler coerces to number. No raw user text in pip HTML. |
| T-04-09 | Denial of Service | pip dot count loop runs max times | mitigate | max derives from legendaryActionsPerRound (small int, 0-3) or parsed LR count (typically 3); not user-unbounded. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. Slopcheck N/A. |
</threat_model>

<verification>
- python build.py succeeds (new render/handler functions concatenate cleanly)
- legendary E2E subset green, including the D-07 no-auto-reset assertion
- Manual (phase gate): pip click feel matches Death-Save dots (04-VALIDATION.md manual checks)
</verification>

<success_criteria>
- INIT-02 satisfied: LA/LR pips render conditionally, click to decrement, LA auto-resets at Init 20, LR does NOT auto-reset and has a manual reset
- D-07 deviation correctly implemented and test-locked
- LA/LR fields initialised from the bestiary add flow (runtime-only)
</success_criteria>

<output>
Create `.planning/phases/04-initiative-erweiterungen/04-03-SUMMARY.md` when done
</output>
