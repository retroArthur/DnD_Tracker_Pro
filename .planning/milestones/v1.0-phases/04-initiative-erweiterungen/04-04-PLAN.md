---
phase: 04-initiative-erweiterungen
plan: 04
type: execute
wave: 3
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - features/initiative-mob.js
  - features/initiative.js
  - features/bestiary/bestiary-actions.js
  - ui/actions/combat-actions.js
  - tests/e2e/features/initiative.spec.js
autonomous: true
requirements: [INIT-03]

must_haves:
  truths:
    - "Adding N>1 monsters with the Mob toggle creates ONE mob row instead of N individual rows (D-11, SC3)"
    - "The mob row shows pool-HP and a colour-coded X von N am Leben count derived from poolHp / individualMaxHp (D-12)"
    - "At 0 pool-HP the mob shows a Besiegt badge (D-12, SC3)"
    - "The collective attack supports both modes: N-fach roll and DMG-Mob-Regel, switchable per mob (D-13)"
    - "A hit auto-sums damage (hits times weapon damage) via the existing roller, one click = total (D-14)"
    - "Mob rows suppress single-combatant-only features: Concentration badge/check and Quick Actions are hidden (RESEARCH Falle 5, UI-SPEC Feature Hiding)"
    - "Dissolving a mob calls saveUndoState() before mutating (destructive, undoable)"
  artifacts:
    - path: "features/initiative-mob.js"
      provides: "renderMobRow, applyMobDamage, rollMobAttack, setMobAttackMode, dissolveMob (Wave-3 implementations replacing Wave-0 stubs)"
      contains: "function renderMobRow"
    - path: "features/bestiary/bestiary-actions.js"
      provides: "Mob toggle in the Mengen-Dialog creating one mob combatant via createMobCombatant"
      contains: "createMobCombatant"
    - path: "features/initiative.js"
      provides: "renderInit mob-row branch + feature-hiding guards (concentration, quick actions)"
      contains: "cb.mob"
    - path: "ui/actions/combat-actions.js"
      provides: "init-mob-set-mode-stop, init-mob-attack-stop, init-mob-dissolve-stop handlers"
      contains: "init-mob-attack-stop"
  key_links:
    - from: "features/bestiary/bestiary-actions.js"
      to: "createMobCombatant"
      via: "Mob toggle branch in addBestiaryToInitiative"
      pattern: "createMobCombatant"
    - from: "features/initiative.js renderInit()"
      to: "renderMobRow"
      via: "cb.mob branch before the standard template"
      pattern: "renderMobRow"
    - from: "features/initiative-mob.js"
      to: "calcMobHits"
      via: "DMG-Mob-Regel path in rollMobAttack"
      pattern: "calcMobHits"
---

<objective>
Implement INIT-03: a Mob mode that condenses N>1 identical monsters into a single initiative row with pool-HP, a colour-coded X von N am Leben count, two switchable collective-attack modes (N-fach roll and the DMG-Mob-Regel), and auto-summed damage via the existing roller. The mob row hides single-combatant-only features (Concentration, Quick Actions) per the Feature-Hiding contract.

The Wave-0 pure functions (createMobCombatant, getMobAlive, calcMobHits, parseLegendaryResistanceCount) are already implemented and unit-tested; this plan wires the UI-coupled functions (renderMobRow, applyMobDamage, rollMobAttack, setMobAttackMode, dissolveMob) and the Mengen-Dialog toggle.

Purpose: SC3 — the DM runs ten goblins as one row with pool-HP and one-click collective attacks.
Output: mob toggle + mob row + collective attack (both modes) + feature-hiding + green mob E2E suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-initiative-erweiterungen/04-PATTERNS.md
@.planning/phases/04-initiative-erweiterungen/04-UI-SPEC.md
@.planning/phases/04-initiative-erweiterungen/04-VALIDATION.md
@.planning/phases/04-initiative-erweiterungen/04-01-SUMMARY.md
@.planning/phases/04-initiative-erweiterungen/04-03-SUMMARY.md

<interfaces>
From features/initiative-mob.js (Wave 0, window-exported, already unit-tested):
  function createMobCombatant(monster, count, source)  // returns combatant with cb.mob = {count, poolHp, maxPoolHp, individualMaxHp, attackMode}
  function getMobAlive(cb)         // Math.max(0, ceil(poolHp / individualMaxHp))
  function calcMobHits(count, attackBonus, targetAC)  // floor(count*(21-needed)/20), needed=max(2,AC-bonus)
From features/initiative.js:
  function getCombatant(id); function renderInit()
  function modHp(id, amt)          // single-combatant HP mutation pattern analog
  // lair-type early-return at renderInit lines 147-161 is the branching analog
  // concentration render at lines 179-180; quick actions at line 199
From dice / roller:
  window.rollQrefDice(formula) or window.rollDiceFormula(formula)  // existing roller, returns/shows a roll
Globals: window.D, window.save, window.saveUndoState, window.showToast, esc, $

Mob row DOM contract (UI-SPEC INIT-03 lines 378-576): init-info--mob, init-mob-alive[.healthy/.bloodied/.critical/.defeated], init-mob-defeated-badge, init-mob-controls, init-mob-mode-btn[.active][data-mode], init-mob-dmg-inputs[.hidden] (init-mob-ac-input, init-mob-bonus-input), init-mob-attack-btn, init-mob-dissolve-btn
Attack result is shown via showToast / event-log, NOT inline.
Alive colour coding: ratio>0.5 healthy(green), >0.25 bloodied(yellow), >0 critical(red), =0 defeated badge.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Mob toggle to the Mengen-Dialog (createMobCombatant branch)</name>
  <files>features/bestiary/bestiary-actions.js</files>
  <read_first>
    - features/bestiary/bestiary-actions.js (addBestiaryToInitiative lines 64-129; the count clamp at line 77; saveUndoState at line 87; the per-combatant for-loop at lines 89-119; the sort + render tail at 122-128)
    - features/initiative-mob.js (createMobCombatant from Wave 0, window-exported)
    - .planning/phases/04-initiative-erweiterungen/04-PATTERNS.md (lines 471-511 mob-toggle insertion; Code-Beispiel in RESEARCH lines 580-623)
    - .planning/phases/04-initiative-erweiterungen/04-UI-SPEC.md (Copywriting INIT-03 mob toggle confirm text lines 140-158)
    - .planning/phases/04-initiative-erweiterungen/04-RESEARCH.md (Annahme A3 confirm availability lines 800-803)
    - CLAUDE.md (saveUndoState before destructive ops; clamp user input)
  </read_first>
  <action>
    In features/bestiary/bestiary-actions.js addBestiaryToInitiative(), after the count clamp (line 77) and before saveUndoState (line 87), add: var isMob = false; if (count > 1) { isMob = confirm("Als Mob fuehren? (1 Zeile mit Pool-HP)\n\nJA = Mob-Modus (Pool-HP, Sammel-Angriff)\nNEIN = " + count + " Einzelzeilen"); }
    After saveUndoState(), branch: if (isMob) build one mob combatant via var mobCb = window.createMobCombatant(monster, count, source); apply the LA/LR field init from Plan 03 (laCount = monster.legendaryActionsPerRound || 0; lrCount = window.parseLegendaryResistanceCount(monster); attach legendaryActions/legendaryResistance when > 0); then D.initiative.combatants.push(mobCb). else keep the existing N-row for-loop UNCHANGED (including the Plan-03 LA/LR field init already added there). Keep the existing sort + save + switchView + renderInit + showToast tail working for both branches. The count is already clamped to BESTIARY_MAX_QUANTITY = 100 (DoS bound).
  </action>
  <verify>
    <automated>cd "D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular" && python build.py</automated>
    <source_check>grep "createMobCombatant" features/bestiary/bestiary-actions.js matches; grep "isMob" features/bestiary/bestiary-actions.js matches; grep "Als Mob" features/bestiary/bestiary-actions.js matches; grep -c "for (var i = 0; i < count" features/bestiary/bestiary-actions.js still returns 1 (single-row path preserved)</source_check>
  </verify>
  <acceptance_criteria>
    - When count > 1 the user is asked whether to lead the group as a mob; YES creates exactly one combatant with a cb.mob field, NO keeps the N-row behaviour
    - The mob combatant carries LA/LR fields when the monster is a boss
    - count remains clamped to 100; python build.py completes without error
  </acceptance_criteria>
  <done>The existing Mengen-Dialog now offers a Mob toggle that produces a single pooled combatant.</done>
</task>

<task type="auto">
  <name>Task 2: Implement mob row/attack/damage/dissolve + renderInit branch and feature-hiding</name>
  <files>features/initiative-mob.js, features/initiative.js</files>
  <read_first>
    - features/initiative-mob.js (the Wave-0 stubs for renderMobRow/applyMobDamage/rollMobAttack/setMobAttackMode/dissolveMob to be filled; getMobAlive and calcMobHits already done)
    - features/initiative.js (renderInit lines 104-204; lair branch 147-161 as the branching analog; concentration calls 179-180; quick actions 199; modHp pattern)
    - .planning/phases/04-initiative-erweiterungen/04-PATTERNS.md (lines 346-461 renderMobRow/applyMobDamage/dissolveMob/rollMobAttack; lines 144-177 renderInit mob branch + guards)
    - .planning/phases/04-initiative-erweiterungen/04-UI-SPEC.md (INIT-03 HTML structure lines 386-443; alive colour coding lines 446-451; defeated badge; attack result via showToast lines 467-469; Feature Hiding table lines 580-596)
    - .planning/phases/04-initiative-erweiterungen/04-RESEARCH.md (Falle 5 mob feature-hiding lines 519-551; rollMobDamage auto-sum lines 439-450)
    - CLAUDE.md (saveUndoState before dissolve; esc() for names; window.X() not const X = window.X in functions)
  </read_first>
  <action>
    In features/initiative-mob.js replace the Wave-0 stubs with full implementations:
    - renderMobRow(cb, i, init): return the mob-row HTML string per the UI-SPEC structure. Use getMobAlive(cb) for alive; aliveRatio = cb.mob.count > 0 ? alive/cb.mob.count : 0; pick aliveClass (poolHp<=0 defeated, >0.5 healthy, >0.25 bloodied, else critical). Render init-info--mob with name (esc), the Mob sub-label "Mob, {count} Kreaturen, Pool-HP", the init-mob-alive line ("{alive} von {count} am Leben" or the Besiegt badge when poolHp<=0), and the init-mob-controls block: a Sammel-Angriff label, two init-mob-mode-btn toggles (data-mode nfach / dmg-regel, active class matching cb.mob.attackMode), the init-mob-dmg-inputs (hidden unless attackMode === dmg-regel) with init-mob-ac-input and init-mob-bonus-input, and the init-mob-attack-btn (data-action init-mob-attack-stop). The right cluster keeps HP buttons, the book button (Plan 02), and an init-mob-dissolve-btn (data-action init-mob-dissolve-stop). All D-values via esc.
    - setMobAttackMode(cbId, mode): set cb.mob.attackMode = mode (validate mode is nfach or dmg-regel); renderInit(); window.save().
    - applyMobDamage(cbId, damage): analog modHp pool variant: subtract from cb.mob.poolHp (absorb tempHp first if present), clamp at 0; mirror cb.currentHp = cb.mob.poolHp to keep the HP display synced; renderInit(); window.save().
    - rollMobAttack(cbId): get cb; alive = getMobAlive(cb); if alive <= 0 showToast("Mob besiegt", "warning") and return. In nfach mode: roll a single attack formula via the existing roller alive times and auto-sum damage = alive hits times the damage formula; show the result via showToast in the UI-SPEC format "{N} Angriffe ... Schaden: {total}". In dmg-regel mode: read targetAC from init-mob-ac-input and attackBonus from init-mob-bonus-input (parse + clamp), hits = calcMobHits(alive, attackBonus, targetAC), auto-sum damage = hits times the damage formula, showToast "{hits} Treffer (von {alive}) ... Schaden: {total}". Use a sensible default damage formula from the monster statblock when resolvable, otherwise a safe fallback; never loop on unbounded user input (alive is bounded by count<=100).
    - dissolveMob(cbId): confirm with the UI-SPEC text; saveUndoState("Mob aufgehoben") BEFORE mutating; remove the combatant from D.initiative.combatants; fix currentTurn if out of range; renderInit(); window.save(); showToast("Mob aufgehoben").
    In features/initiative.js renderInit(): after the lair early-return (line 161), add: if (cb.mob) return renderMobRow(cb, i, init); BEFORE the standard template. Add the feature-hiding guards in the standard template: change the concentration render to require !cb.mob (line 179: only render renderConcentration when !dead && !cb.mob), and the pending concentration check to require !cb.mob (line 180). For Quick Actions (line 199), only call renderQuickActionsBar when the active combatant is not a mob (check init.combatants[init.currentTurn] && !that.mob).
    Export the five functions to window. No const X = window.X inside any function.
  </action>
  <verify>
    <automated>cd "D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular" && python build.py</automated>
    <source_check>grep "function renderMobRow" features/initiative-mob.js matches; grep "calcMobHits" features/initiative-mob.js matches; grep "saveUndoState" features/initiative-mob.js matches; grep "cb.mob" features/initiative.js matches; grep "renderMobRow" features/initiative.js matches; grep "!cb.mob" features/initiative.js matches</source_check>
  </verify>
  <acceptance_criteria>
    - renderInit branches to renderMobRow for cb.mob and hides Concentration + Quick Actions for mobs (UI-SPEC Feature Hiding)
    - rollMobAttack supports both modes and auto-sums damage; dissolveMob calls saveUndoState before removal
    - applyMobDamage reduces pool-HP and keeps currentHp synced; defeated badge shows at 0 pool-HP
    - python build.py completes without duplicate-declaration or orphaned-body error
  </acceptance_criteria>
  <done>The mob row renders with colour-coded alive count, both attack modes work with auto-summed damage, and single-combatant features are suppressed.</done>
</task>

<task type="auto">
  <name>Task 3: Register mob handlers and fill the mob E2E suite</name>
  <files>ui/actions/combat-actions.js, tests/e2e/features/initiative.spec.js</files>
  <read_first>
    - ui/actions/combat-actions.js (CombatActions object; the -stop + stopPropagation pattern at lines 10-13)
    - tests/e2e/features/initiative.spec.js (the skipped mob describe stub from Wave 0; beforeEach navigation; page.evaluate injection pattern)
    - tests/unit/initiative-mob.test.js (Wave-0 unit suite to re-run after the mob functions are completed)
    - .planning/phases/04-initiative-erweiterungen/04-VALIDATION.md (INIT-03 rows; the mob filter command; the 0-pool-HP defeated behavior)
    - .planning/phases/04-initiative-erweiterungen/04-UI-SPEC.md (Data-Action Registry lines 626-640; selectors init-mob-mode-btn, init-mob-attack-btn, init-mob-alive, init-mob-defeated-badge, init-mob-dissolve-btn)
  </read_first>
  <action>
    In ui/actions/combat-actions.js register three handlers with the -stop + ctx.event.stopPropagation() pattern:
    - init-mob-set-mode-stop: ctx => { ctx.event.stopPropagation(); setMobAttackMode(ctx.id, ctx.target.dataset.mode); }
    - init-mob-attack-stop: ctx => { ctx.event.stopPropagation(); rollMobAttack(ctx.id); }
    - init-mob-dissolve-stop: ctx => { ctx.event.stopPropagation(); dissolveMob(ctx.id); }
    Replace the Wave-0 skip placeholders in the mob describe block with real Playwright tests against the freshly built dist bundle. Inject a mob combatant (via page.evaluate setting D.initiative.combatants to a single cb with a mob field, then renderInit(), or by driving the bestiary add + mob confirm). Cover:
    - The mob renders as exactly one init row (not N rows) and shows init-mob-alive with the X von N am Leben text (D-11, D-12).
    - Switching attack mode via the init-mob-mode-btn toggles the active class and reveals init-mob-dmg-inputs for dmg-regel.
    - Reducing pool-HP to 0 (via HP buttons or page.evaluate) shows the init-mob-defeated-badge with Besiegt (SC3, 0 pool-HP).
    - The init-mob-dissolve-btn removes the mob row.
    Keep every test title containing the word mob so the -g filter selects them.
  </action>
  <verify>
    <automated>cd "D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular" && python build.py && npm run test:unit -- --testPathPattern=initiative-mob</automated>
    <automated>cd "D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular" && npm run test:e2e -- tests/e2e/features/initiative.spec.js -g mob</automated>
  </verify>
  <acceptance_criteria>
    - All three mob handlers registered with stopPropagation via the -stop suffix
    - The unit suite still exits 0 (calcMobHits 10/+4/AC15 = 5 anchor holds after the mob functions are completed)
    - The mob E2E subset exits 0 (one-row mob, mode toggle, 0-pool-HP Besiegt badge, dissolve)
    - The mob describe block has no test.skip/test.fixme placeholders remaining
  </acceptance_criteria>
  <done>Mob handlers are wired and the mob E2E suite is green; SC3 is satisfied end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| confirm() toggle + count -> mob creation | User decides mob vs N rows; count is a clamped integer |
| init-mob-ac-input / init-mob-bonus-input -> calcMobHits | Numeric attack inputs feed the O(1) hit calc |
| cb.name / mob labels -> mob-row DOM | Mob name and counts rendered via innerHTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering (XSS) | renderMobRow writes cb.name and mob labels into innerHTML | mitigate | All D-derived values wrapped in esc(); numeric mob fields cast via String() before esc(); no raw trait text in the row (statblock text stays in the Plan-02 drawer). |
| T-04-11 | Denial of Service | rollMobAttack N-fach roll loops alive times; createMobCombatant sums count HP rolls | mitigate | alive and count are bounded by BESTIARY_MAX_QUANTITY = 100 (clamped in addBestiaryToInitiative). calcMobHits is O(1). |
| T-04-12 | Tampering | init-mob-ac-input / init-mob-bonus-input numeric inputs | mitigate | Inputs have min/max in the UI; handler parses with parseInt and clamps before calcMobHits; non-numeric falls back to a safe default. No string reaches a loop bound or the DOM unescaped. |
| T-04-13 | Repudiation / data loss | dissolveMob removes a combatant | mitigate | saveUndoState("Mob aufgehoben") called BEFORE the filter mutation so Ctrl+Z restores the mob (CLAUDE.md destructive-op rule). |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. Slopcheck N/A. |
</threat_model>

<verification>
- python build.py succeeds (mob functions + renderInit branch concatenate cleanly)
- npm run test:unit --testPathPattern=initiative-mob green (Wave-0 anchors hold after completion)
- mob E2E subset green
- Manual (phase gate): one-click collective damage feel at the table, both attack modes (04-VALIDATION.md manual checks)
</verification>

<success_criteria>
- INIT-03 satisfied: Mob toggle creates one pooled row; colour-coded alive count; Besiegt at 0 pool-HP; both attack modes with auto-summed damage; single-combatant features hidden; dissolve is undoable
- DMG-Mob-Regel anchored by the Wave-0 unit test (10 Goblins +4 vs AC 15 = 5)
</success_criteria>

<output>
Create `.planning/phases/04-initiative-erweiterungen/04-04-SUMMARY.md` when done
</output>
