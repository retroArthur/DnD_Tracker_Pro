---
phase: 06-spieler-verwaltung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\core\constants.js
  - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\utils\game-rules.js
  - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\utils\testable-utils.js
  - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\core\data.js
  - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\systems\spellslots\version-migration.js
  - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\tests\unit\character-advancement.test.js
  - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\tests\e2e\features\inspiration.spec.js
  - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\tests\e2e\features\character-advancement.spec.js
autonomous: true
requirements: [CHAR-01, CHAR-02, CHAR-03]
user_setup: []

must_haves:
  truths:
    - "XP_LEVEL_THRESHOLDS exists as a 20-value PHB advancement constant, distinct from XP_THRESHOLDS (encounter difficulty) (D-11)"
    - "Pure rules helpers calcSkillModifier, canLevelUp, getXPForCR, distributeXP are unit-testable without the browser DOM (D-04/D-09/D-10/D-11)"
    - "New character fields xp, skillProficiencies, skillExpertise, attacks[] and D.settings.levelingMode are backfilled by migration 5.0.0 with safe defaults (D-07/D-08)"
    - "Wave-0 test stubs and a shared character fixture exist so feature waves have failing tests to satisfy"
  artifacts:
    - path: "core/constants.js"
      provides: "XP_LEVEL_THRESHOLDS constant + DND_RULES namespace entry"
      contains: "XP_LEVEL_THRESHOLDS"
    - path: "utils/game-rules.js"
      provides: "calcSkillModifier, canLevelUp, getXPForCR, distributeXP, getXPForCombatant"
      contains: "calcSkillModifier"
    - path: "utils/testable-utils.js"
      provides: "CommonJS-exported mirror of the four pure helpers for Jest"
      contains: "calcSkillModifier"
    - path: "systems/spellslots/version-migration.js"
      provides: "Migration key '5.0.0' backfilling xp/skill/attack fields + levelingMode"
      contains: "'5.0.0'"
    - path: "tests/unit/character-advancement.test.js"
      provides: "Unit tests for all four pure helpers incl. boundary/fraction/remainder cases"
      contains: "calcSkillModifier"
    - path: "tests/e2e/features/inspiration.spec.js"
      provides: "E2E stub (file:// + ESM import) for inspiration toggle + -stop behavior"
      contains: "@playwright/test"
    - path: "tests/e2e/features/character-advancement.spec.js"
      provides: "E2E stub (file:// + ESM import) for clickable rolls, attacks, XP distribution, milestone mode"
      contains: "@playwright/test"
  key_links:
    - from: "core/constants.js"
      to: "DND_RULES.XP_LEVEL_THRESHOLDS"
      via: "Object.freeze namespace entry + legacy window export"
      pattern: "XP_LEVEL_THRESHOLDS"
    - from: "utils/game-rules.js"
      to: "SKILL_INFO + getAbilityModifier + getProficiencyBonus"
      via: "calcSkillModifier reads SKILL_INFO[skillKey].attr, applies prof factor"
      pattern: "calcSkillModifier"
    - from: "systems/spellslots/version-migration.js"
      to: "D.characters[*].xp / skillProficiencies / skillExpertise / attacks"
      via: "MIGRATIONS['5.0.0'] forEach backfill"
      pattern: "ch.skillProficiencies"
---

<objective>
Foundation wave for Phase 6 (Spieler-Verwaltung). Lands the data and rules substrate that all three feature waves build on: the PHB level→XP advancement constant, four pure rules helpers (computable and unit-testable without the DOM), the character-schema field additions with a versioned migration, the per-group leveling-mode setting, and the Nyquist Wave-0 test scaffolding (unit suite + shared fixture + two E2E stub specs).

Purpose: Sequential execution on `main` means every feature wave touches shared party files; this wave establishes shared contracts ONCE so Waves 2–4 implement against them with no scavenger hunt and no duplicated rules math.
Output: Updated constants/helpers/schema/migration + a green unit suite for the helpers + two failing-but-runnable E2E specs that later waves turn green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\.planning\phases\06-spieler-verwaltung\06-CONTEXT.md
@D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\.planning\phases\06-spieler-verwaltung\06-RESEARCH.md
@D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\.planning\phases\06-spieler-verwaltung\06-VALIDATION.md
@D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\.planning\codebase\CONVENTIONS.md
@D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\.planning\codebase\CONCERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from 06-RESEARCH.md and source reads. -->

SKILL_INFO (core/constants.js:217) — 18 entries, German names:
  { acrobatics: { name:'Akrobatik', attr:'dex' }, stealth: { name:'Heimlichkeit', attr:'dex' }, ... }
  (also exposed via DND_RULES.SKILL_INFO at core/constants.js:504)
  TRAP: the SKILLS constant in features/dice/dice-core.js:11 is a DIFFERENT structure ({name,skill} arrays per attribute) — do NOT use it for skills math.

Rules math already present (utils/game-rules.js):
  getAbilityModifier(score) :16  → Math.floor((score-10)/2)   [NOT window-exported; global lexical scope]
  getProficiencyBonus(level) :32 → Math.ceil(level/4)+1        [window-exported :134]
  formatModifier(mod) :43        → mod>=0 ? `+${mod}` : String(mod)   [NOT window-exported]

CR_TO_XP (features/encounter-calculator.js:32) — MIXED key types:
  whole CRs are Number keys: CR_TO_XP[1] === 200
  fractional CRs are String keys: CR_TO_XP['1/8'] === 25, CR_TO_XP['1/4'] === 50, CR_TO_XP['1/2'] === 100

MIGRATIONS object (systems/spellslots/version-migration.js) — highest existing key is '4.0.0' (Phase 5). Next key MUST be '5.0.0'. Each value is `data => { ...; return data; }`.

Character schema mutation point (features/party/party-crud.js:214 saveCharacter ch literal; :235 inspiration; :198 saveProficiencies; :308 editChar save loop).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add XP_LEVEL_THRESHOLDS constant + four pure rules helpers</name>
  <files>D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\core\constants.js, D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\utils\game-rules.js, D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\utils\testable-utils.js</files>
  <read_first>
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\core\constants.js — read SKILL_INFO (~:217) and the `window.DND_RULES = Object.freeze({...})` namespace block (~:498-519) plus the legacy `window.X = X` exports that follow it; replicate exactly that two-step export pattern.
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\utils\game-rules.js — read getAbilityModifier (:16), getProficiencyBonus (:32), formatModifier (:43) and the existing `window.getProficiencyBonus = ...` export block; replicate the export style.
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\utils\testable-utils.js — read the `module.exports = { ... }` block to see how pure functions are mirrored for Jest (CommonJS).
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\.planning\phases\06-spieler-verwaltung\06-RESEARCH.md § "Fehlende Konstante (D-11)" and § "5e-Regelwerk-Mathematik" — exact values and helper signatures.
  </read_first>
  <action>
    In core/constants.js: add a module-level const XP_LEVEL_THRESHOLDS as the 20-element PHB cumulative-XP array from 06-RESEARCH.md (index 0 = Level 1 = 0 XP, index 19 = Level 20 = 355000). Add a JSDoc banner warning it is NOT XP_THRESHOLDS (encounter difficulty). Register it in the DND_RULES Object.freeze block AND add the legacy `window.XP_LEVEL_THRESHOLDS = XP_LEVEL_THRESHOLDS;` export, matching the existing pattern.

    In utils/game-rules.js: add four NEW top-level functions (names must NOT collide with any existing top-level function anywhere — verify via grep before naming):
      - calcSkillModifier(ch, skillKey): returns attrMod + profBonus * profFactor, where attrMod = getAbilityModifier(ch.attributes?.[SKILL_INFO[skillKey].attr] || 10), profBonus = ch.proficiencyBonus || getProficiencyBonus(ch.level || 1), profFactor = ch.skillExpertise?.[skillKey] ? 2 : (ch.skillProficiencies?.[skillKey] ? 1 : 0). Return 0 when SKILL_INFO[skillKey] is missing. Reuse getAbilityModifier/getProficiencyBonus directly (global lexical scope — do NOT write `const X = window.X`).
      - canLevelUp(ch): nextLevel = (ch.level||1)+1; return false if nextLevel > 20; return (ch.xp||0) >= XP_LEVEL_THRESHOLDS[nextLevel-1].
      - getXPForCR(cr): return CR_TO_XP[cr] ?? CR_TO_XP[String(cr)] ?? 0 (handles mixed Number/String keys). Access CR_TO_XP directly (global lexical scope).
      - distributeXP(totalXP, activeChars): share = Math.floor(totalXP / activeChars.length) when activeChars.length>0 else 0; remainder = totalXP - share*activeChars.length; mutate each active char's ch.xp = (ch.xp||0)+share; return { share, remainder }. Keep this function PURE w.r.t. globals — it operates only on the passed activeChars array (the caller filters living party members and handles save()/undo).
    Export the four via `window.X = X` at the bottom export block (the detail-modal and combat handlers call them).

    In utils/testable-utils.js: mirror the four pure helpers as CommonJS so Jest can require them without jsdom. Inline the minimal dependencies they need (getAbilityModifier formula, a local copy of XP_LEVEL_THRESHOLDS, and accept SKILL_INFO + CR_TO_XP as injected params or inline minimal copies) so the test file does not need the browser globals. Add them to the existing `module.exports` object. Per CONCERNS.md the testable-utils copies drift from source — add a `// keep in sync with utils/game-rules.js` note.
  </action>
  <verify>
    <automated>cd /d D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular && npx jest tests/unit/character-advancement.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const t=require('./utils/testable-utils.js'); console.log(typeof t.calcSkillModifier, typeof t.canLevelUp, typeof t.getXPForCR, typeof t.distributeXP)"` prints `function function function function`
    - core/constants.js contains the token `XP_LEVEL_THRESHOLDS` and the array's index 1 value is 300 and index 19 value is 355000
    - `grep -c '^function calcSkillModifier' utils/game-rules.js` returns exactly 1 (no duplicate top-level name)
    - XP_LEVEL_THRESHOLDS and XP_THRESHOLDS remain two distinct constants (XP_LEVEL_THRESHOLDS in core/constants.js, XP_THRESHOLDS untouched in features/encounter-calculator.js)
  </acceptance_criteria>
  <done>Constant and four helpers exist, exported both as window globals (runtime) and CommonJS (tests); no duplicate top-level function names introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Schema fields + migration 5.0.0 + levelingMode setting + Wave-0 unit suite/fixture</name>
  <files>D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\core\data.js, D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\systems\spellslots\version-migration.js, D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\tests\unit\character-advancement.test.js</files>
  <read_first>
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\systems\spellslots\version-migration.js — read the MIGRATIONS object and confirm the highest key is '4.0.0'; replicate the `'X.Y.Z': data => { ...; return data; }` shape and the version-key sort assumptions.
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\core\data.js — read the D.settings default block and the D.characters default-shape comments; add levelingMode alongside existing settings.
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\.planning\phases\06-spieler-verwaltung\06-RESEARCH.md § "Muster 3: Migration für neue Felder" — exact backfill body.
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\tests\setup.js — see how the jsdom fixture/globals are declared so the shared character fixture fits the existing convention.
  </read_first>
  <behavior>
    - calcSkillModifier(rogueL5, 'stealth') === 10 where rogue = { level:5, attributes:{dex:18}, skillExpertise:{stealth:true} } (DEX+4 + 2×profBonus(+3))
    - calcSkillModifier(plainChar, 'stealth') === DEX mod only when not proficient
    - canLevelUp({level:1, xp:300}) === true; canLevelUp({level:1, xp:299}) === false; canLevelUp({level:20, xp:999999}) === false
    - getXPForCR('1/4') === 50; getXPForCR(1) === 200; getXPForCR('1/8') === 25; getXPForCR('unknown') === 0
    - distributeXP(400, [4 active chars]) gives each char +100, returns { share:100, remainder:0 }; distributeXP(401, [4]) → share 100, remainder 1
    - XP_LEVEL_THRESHOLDS[1] === 300 (Level 2 threshold) AND the encounter table XP_THRESHOLDS stays the difficulty table (distinct)
  </behavior>
  <action>
    In version-migration.js: add MIGRATIONS key '5.0.0' (the next strictly-ascending key after '4.0.0') whose body iterates data.characters and backfills: ch.xp = 0 when undefined, ch.skillProficiencies = {} when falsy, ch.skillExpertise = {} when falsy, ch.attacks = [] when falsy; then ensures data.settings exists and sets data.settings.levelingMode = 'xp' when undefined. Return data. Note A1: APP_CONFIG.VERSION is currently 2.6.1 — the migration is keyed by data version, not APP_CONFIG.VERSION; do not change config version here.

    In core/data.js: add levelingMode: 'xp' to the D.settings default object so fresh campaigns (no migration path) also get the default. Add inline-comment documentation that characters carry xp (Number, default 0), skillProficiencies/skillExpertise ({} keyed by SKILL_INFO keys), attacks ([] of {name, attackBonus, damage, damageType?}). Do NOT alter saveCharacter here (that is Wave 3/4 scope) — only the default-shape documentation and settings default.

    In tests/unit/character-advancement.test.js: write the full unit suite implementing the <behavior> cases above against utils/testable-utils.js (require, not import). Include the shared character fixture inline (or in tests/setup.js if that matches the existing pattern): a sample char with level, xp, attributes, skillProficiencies, skillExpertise, attacks[]. Cover ungeübt/geübt/Expertise, boundary 299/300, fraction CRs, remainder split.
  </action>
  <verify>
    <automated>cd /d D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular && npx jest tests/unit/character-advancement.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest tests/unit/character-advancement.test.js` exits 0 with all <behavior> assertions passing
    - `grep -c "'5.0.0'" systems/spellslots/version-migration.js` returns >= 1 and no key '6.0.0' was added
    - core/data.js D.settings default contains `levelingMode`
    - Running the migration on a fixture character lacking xp/skillProficiencies/skillExpertise/attacks yields ch.xp===0, ch.skillProficiencies deep-equals {}, ch.skillExpertise deep-equals {}, ch.attacks deep-equals [] (assert this in the unit test by importing/invoking MIGRATIONS['5.0.0'] if exportable, else replicate its body in a test helper)
  </acceptance_criteria>
  <done>Migration 5.0.0 backfills all new fields with defaults, fresh-campaign settings default is set, and the Wave-0 unit suite is green.</done>
</task>

<task type="auto">
  <name>Task 3: Create runnable E2E stub specs (file:// + ESM) for Waves 2–4</name>
  <files>D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\tests\e2e\features\inspiration.spec.js, D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\tests\e2e\features\character-advancement.spec.js</files>
  <read_first>
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\tests\e2e\crud\party.spec.js — read the file:// navigation/setup pattern and the ESM `import { test, expect } from '@playwright/test'` header; replicate the harness (do NOT use http://localhost, do NOT use CommonJS require).
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\playwright.config.js — confirm the file:// baseURL/use block and which dist file is targeted (dnd-tracker-bundled.html).
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\.planning\phases\06-spieler-verwaltung\06-VALIDATION.md § "Per-Requirement Verification Map" — the exact assertions each spec must eventually make.
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\.planning\codebase\CONCERNS.md — note the broken addCombatant() helper in initiative.spec.js; do NOT import or depend on it.
  </read_first>
  <action>
    Create inspiration.spec.js with ESM import and the file:// harness from party.spec.js. Add test cases (initially skipped via test.skip or test.fixme so the suite is runnable/green but the assertions are reserved for Wave 2): (a) clicking ☆ toggles to ⭐ and `.char-inspiration-toggle.active` is visible; (b) reload persists the active state; (c) clicking the star does NOT open the character form (`#char-form.open` not visible). Each skipped test carries a comment naming the Wave-2 task and the VALIDATION.md assertion it implements.

    Create character-advancement.spec.js with the same harness. Add skipped/fixme test cases reserved for Waves 3–4: (a) clicking a skill/save/attribute box in the detail modal increases dice history length; (b) an attack with +5 / 1d8+3 renders dice spans with data-value="1d20+5" and data-value="1d8+3"; (c) milestone mode hides XP fields and shows a "+1 Level" button. Do NOT rely on the broken addCombatant() helper — for the XP-distribution path use page.evaluate to seed D.initiative.combatants directly or assert via the XP modal pre-sum, mirroring how Phase 4 E2E injected state via page.evaluate.

    Both specs MUST load and run (zero collection/parse errors) even though the meaningful assertions are deferred. This satisfies the Nyquist Wave-0 contract: the spec files exist and execute.
  </action>
  <verify>
    <automated>cd /d D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular && set PYTHONIOENCODING=utf-8&& python build.py && npx playwright test tests/e2e/features/inspiration.spec.js tests/e2e/features/character-advancement.spec.js --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - Both spec files use `import { test, expect } from '@playwright/test'` (ESM) — `grep -L "require(" tests/e2e/features/inspiration.spec.js` confirms no CommonJS require
    - Neither spec references `addCombatant` — `grep -c addCombatant tests/e2e/features/inspiration.spec.js tests/e2e/features/character-advancement.spec.js` returns 0 for both
    - `npx playwright test tests/e2e/features/inspiration.spec.js tests/e2e/features/character-advancement.spec.js` runs with no load/parse error (exit code reflects only skipped/pending tests, not collection failure)
    - Both specs target the file:// dist build, not http://localhost (grep confirms no "localhost")
  </acceptance_criteria>
  <done>Two E2E stub specs exist, load cleanly under the file:// ESM harness, avoid the broken combatant helper, and are ready for Waves 2–4 to fill in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted D ↔ migration | Old campaign data of unknown shape crosses into the new schema; migration must default-fill, never crash on missing fields |
| (none new for user input) | This wave adds no new HTML templates or user-editable inputs — input-XSS surface is introduced in Waves 3–4 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | MIGRATIONS['5.0.0'] backfill over arbitrary old data | mitigate | Guard every field with `if (undefined/falsy)` before assigning; never overwrite existing values; wrap character iteration in optional chaining so a non-array `data.characters` cannot crash the migration |
| T-06-02 | Denial of Service | distributeXP with empty active list | mitigate | Return {share:0,remainder:totalXP} when activeChars.length===0; no division by zero |
| T-06-03 | Information/Integrity | XP_LEVEL_THRESHOLDS vs XP_THRESHOLDS confusion | mitigate | Keep names distinct; JSDoc warning banner on the new constant; unit test asserts both retain correct distinct values |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — all dependencies (jest, playwright, python) already present per 06-RESEARCH.md § Umgebungs-Verfügbarkeit; no Package Legitimacy Gate required |
</threat_model>

<verification>
- `npx jest tests/unit/character-advancement.test.js` exits 0 (all four helpers + migration backfill cases pass)
- `set PYTHONIOENCODING=utf-8&& python build.py` completes without dedup/syntax errors (no duplicate top-level function names introduced)
- Both E2E stub specs run under Playwright with no collection/parse error
- `npm run check` (tsc + lint + format:check) does not regress (new helpers exported via window; add to eslint globals if no-undef warns)
</verification>

<success_criteria>
- XP_LEVEL_THRESHOLDS (20 values) present and namespaced, distinct from encounter XP_THRESHOLDS (D-11)
- calcSkillModifier / canLevelUp / getXPForCR / distributeXP exist, are window-exported and CommonJS-mirrored, and pass unit tests (D-04/D-09/D-10/D-11)
- Migration 5.0.0 backfills xp / skillProficiencies / skillExpertise / attacks[] + D.settings.levelingMode='xp' (D-07/D-08)
- Wave-0 deliverables complete: unit suite green, shared fixture present, two runnable E2E stub specs (Dimension 8 satisfied)
</success_criteria>

<output>
Create D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\.planning\phases\06-spieler-verwaltung\06-01-SUMMARY.md when done. Note for Waves 2–4: helper names actually chosen, the exact migration key, and any eslint globals additions.
</output>
