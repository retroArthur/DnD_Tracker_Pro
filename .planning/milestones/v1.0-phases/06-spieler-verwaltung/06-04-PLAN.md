---
phase: 06-spieler-verwaltung
plan: 04
type: execute
wave: 4
depends_on: [06-01, 06-03]
files_modified:
  - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\features\initiative.js
  - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\ui\actions\combat-actions.js
  - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\assets\templates\modals-entity.html
  - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\features\party\party-details.js
  - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\ui\actions\entity-actions.js
  - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\assets\styles\initiative.css
  - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\assets\styles\party.css
  - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\tests\e2e\features\character-advancement.spec.js
autonomous: true
requirements: [CHAR-01]
user_setup: []

must_haves:
  truths:
    - "A user finishes an encounter and applies earned XP to all active characters with one button; XP is auto-summed from combat monsters via CR_TO_XP, with manual correction always available (CHAR-01 / D-09)"
    - "Total XP is split evenly across active (living) party characters; remainder is documented (D-10)"
    - "When a character reaches the next XP threshold, a level-up HINT appears — the level is never auto-bumped; the DM confirms manually (CHAR-01 / D-11)"
    - "A per-group setting D.settings.levelingMode toggles XP vs Milestone; in Milestone mode XP UI is hidden and a '+1 Level' button is shown (single char and/or whole group) (D-07)"
    - "The character's current XP and level-up hint are shown in the detail modal (D-08)"
  artifacts:
    - path: "features/initiative.js"
      provides: "finishCombatXp trigger + showXpDistributionModal computing the auto-sum"
      contains: "showXpDistributionModal"
    - path: "ui/actions/combat-actions.js"
      provides: "finish-combat-xp + apply-xp-distribution handlers"
      contains: "finish-combat-xp"
    - path: "assets/templates/modals-entity.html"
      provides: "XP distribution modal (auto-sum display + manual correction input + distribute button)"
      contains: "xp-distribution-modal"
    - path: "features/party/party-details.js"
      provides: "XP/level display + level-up hint; milestone-mode +1 Level button"
      contains: "canLevelUp"
    - path: "ui/actions/entity-actions.js"
      provides: "confirm-level-up (manual +1 level) handler"
      contains: "confirm-level-up"
  key_links:
    - from: "initiative.js showXpDistributionModal"
      to: "getXPForCR over enemy/monster combatants"
      via: "sum getXPForCR(cb.cr) for cb.type enemy|monster; fallback manual input (A3)"
      pattern: "getXPForCR"
    - from: "combat-actions.js apply-xp-distribution"
      to: "distributeXP over living characters + canLevelUp"
      via: "filter hpCurrent>0, distributeXP(total, active), then per-char canLevelUp hint, pushUndo('XP verteilt'), save"
      pattern: "distributeXP"
    - from: "party-details.js"
      to: "canLevelUp + D.settings.levelingMode"
      via: "XP mode shows xp + hint when canLevelUp; milestone mode hides xp, shows +1 Level button"
      pattern: "levelingMode"
---

<objective>
Implement CHAR-01 (XP-/Milestone-Tracker). Add an "encounter complete / distribute XP" trigger in the Initiative view that opens a modal pre-summing XP from the combat's monsters (CR_TO_XP via getXPForCR) with an always-available manual correction field; distribute the total evenly across active (living) party characters; show a level-up HINT (never auto-bump) when a character crosses an XP_LEVEL_THRESHOLDS boundary; and honor a per-group D.settings.levelingMode toggle that hides XP UI and shows a "+1 Level" button in Milestone mode. Show current XP + the hint in the detail modal.

Purpose: The natural table moment — a fight ends, XP gets handed out — lives in Initiative; the DM keeps control of leveling (5e-correct: hint, not silent overwrite). Reuses Wave-1 helpers (getXPForCR, distributeXP, canLevelUp, XP_LEVEL_THRESHOLDS) and the existing CR_TO_XP table.
Output: Initiative trigger + XP modal, distribution + level-up-hint logic, milestone-mode UI, manual +1-level confirmation, detail-modal XP display, CSS, and the CHAR-01 E2E/unit assertions green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\.planning\phases\06-spieler-verwaltung\06-CONTEXT.md
@D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\.planning\phases\06-spieler-verwaltung\06-RESEARCH.md
@D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\.planning\phases\06-spieler-verwaltung\06-01-SUMMARY.md
@D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\.planning\phases\06-spieler-verwaltung\06-03-SUMMARY.md

<interfaces>
<!-- Extracted from source reads. Use directly. -->

endCombat() (features/initiative.js:230): confirms, syncs player HP from combatants to D.characters, resets D.initiative, re-renders. DO NOT overload it with XP logic — add a SEPARATE finish-combat-xp trigger (research § "Muster 4", anti-pattern: don't overload endCombat).

Combatant shape (D.initiative.combatants[*]): { id, name, type:'player'|'enemy'|'monster', currentHp, ... , cr?, statblockRef? }. cb.cr may be unset for some bestiary adds (A3) — manual correction MUST always be available; auto-sum falls back to 0.

Wave-1 helpers (window-exported — confirm final names in 06-01-SUMMARY):
  getXPForCR(cr) → number (handles mixed Number/String CR keys)
  distributeXP(totalXP, activeChars) → { share, remainder }; mutates each active char's xp (PURE w.r.t. globals — caller filters living chars and handles undo/save)
  canLevelUp(ch) → boolean (xp >= XP_LEVEL_THRESHOLDS[nextLevel-1], false at level 20)
  XP_LEVEL_THRESHOLDS (DND_RULES + window) — 20 cumulative values

CombatActions registry (ui/actions/combat-actions.js): plain object; handlers receive ctx={id,value,type,event,target}. -stop precedents present. Register new actions via the existing object.

Modal helpers: showModal(id) / hideModal(id) (systems/spellslots/navigation.js). Existing entity modals live in assets/templates/modals-entity.html (e.g. char-detail-modal).

pushUndo(label) before D mutation that the user expects to undo; save() persists. EntityLookup.character(id). showToast for German feedback.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Initiative XP-distribution trigger + modal + distribution/level-up logic</name>
  <files>D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\features\initiative.js, D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\ui\actions\combat-actions.js, D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\assets\templates\modals-entity.html, D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\assets\styles\initiative.css</files>
  <read_first>
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\features\initiative.js — read endCombat (:230-252) and the initiative toolbar render area (search renderInit / battlefield banner) to find where to place a "Kampf abschließen / XP verteilen" button; read how combatants carry type/cr.
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\ui\actions\combat-actions.js — the CombatActions object shape and -stop precedents (:6-40) for registering finish-combat-xp / apply-xp-distribution.
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\assets\templates\modals-entity.html — an existing modal skeleton (e.g. char-detail-modal) to mirror for the XP modal markup + close button data-action="hide-modal".
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\.planning\phases\06-spieler-verwaltung\06-RESEARCH.md § "Muster 4: XP-Verteilung" and § "Fallstrick 5/7" (CR mixed keys; no function-local const X = window.X).
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\.planning\phases\06-spieler-verwaltung\06-01-SUMMARY.md — final helper names/exports.
  </read_first>
  <behavior>
    - With two CR-1/4 wolves in combat, the modal pre-sum equals 100 (getXPForCR('1/4')===50 ×2)
    - With unknown/unset cb.cr, auto-sum contributes 0 for that combatant but the modal still opens and accepts a manual total (A3)
    - Distributing 400 XP with 4 living characters gives each +100; remainder reported when not evenly divisible
    - Dead characters (hpCurrent<=0) are excluded from the split (D-10)
    - After distribution, a character that crossed its threshold is flagged canLevelUp (hint), but its level field is NOT changed
  </behavior>
  <action>
    In initiative.js: add a `finishCombatXp()` opener and a `showXpDistributionModal()` that computes the auto-sum = sum of getXPForCR(cb.cr) over combatants whose type is 'enemy' or 'monster' (skip players), fills the modal's auto-sum display and a pre-filled manual-correction number input with that sum, and lists the count of living party characters that will receive XP. Use getXPForCR directly (global lexical). Add an `applyXpDistribution()` that reads the (possibly corrected) total from the input, filters D.characters to living members (hpCurrent>0), calls distributeXP(total, activeChars), runs canLevelUp per character to build a level-up hint list, calls pushUndo('XP verteilt') BEFORE mutation ordering as appropriate (distributeXP mutates xp — ensure pushUndo precedes it), save(), renderParty(), and shows a German toast summarizing "+{share} XP je Charakter" plus remainder note and any "X kann aufsteigen" hints. Do NOT modify endCombat; keep XP flow separate. Add a "Kampf abschließen / XP verteilen" button to the initiative toolbar with data-action="finish-combat-xp". Export new functions via window.* and avoid duplicate top-level names.

    In combat-actions.js: register `'finish-combat-xp': () => finishCombatXp()` and `'apply-xp-distribution': () => applyXpDistribution()` on the CombatActions object.

    In modals-entity.html: add an `#xp-distribution-modal` mirroring the existing modal skeleton — an auto-sum readout, a manual-correction number input (id e.g. `xp-dist-total`), the living-character count, a "Verteilen" button (data-action="apply-xp-distribution"), and a close button (data-action="hide-modal" data-value="xp-distribution-modal"). German labels.

    In initiative.css: style the XP distribution modal and the toolbar button.
  </action>
  <verify>
    <automated>cd /d D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular && set PYTHONIOENCODING=utf-8&& python build.py && npx jest tests/unit/character-advancement.test.js && npx playwright test tests/e2e/features/character-advancement.spec.js --reporter=line -g "XP|Verteil"</automated>
  </verify>
  <acceptance_criteria>
    - features/initiative.js contains showXpDistributionModal and applyXpDistribution; endCombat is NOT modified to add XP logic (grep endCombat body shows no distributeXP/getXPForCR call)
    - combat-actions.js registers `finish-combat-xp` and `apply-xp-distribution`
    - modals-entity.html contains `xp-distribution-modal` with a manual-correction input and a `apply-xp-distribution` button
    - Unit: distributeXP(400, [4 living]) → each +100; the two-wolf auto-sum equals 100 (getXPForCR('1/4')===50) — asserted in tests/unit/character-advancement.test.js
    - pushUndo('XP verteilt') is called before the xp mutation in applyXpDistribution (so the distribution is undoable)
    - `set PYTHONIOENCODING=utf-8&& python build.py` builds with no duplicate-function/orphan errors
  </acceptance_criteria>
  <done>An initiative trigger opens an XP modal with a CR-based auto-sum and manual override; distributing splits evenly to living chars, flags level-up hints, and is undoable — endCombat untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Detail-modal XP display + level-up hint + milestone-mode UI + manual level-up</name>
  <files>D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\features\party\party-details.js, D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\ui\actions\entity-actions.js, D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\assets\styles\party.css, D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\tests\e2e\features\character-advancement.spec.js</files>
  <read_first>
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\features\party\party-details.js — read showCharacterDetails header/vital area (:59-130) and the skills/attacks sections added in Wave 3 (06-03) to place the XP block consistently; read how D.settings is accessed.
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\ui\actions\entity-actions.js — the character-actions block and -stop precedents for adding confirm-level-up and milestone +1-level handlers.
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\.planning\phases\06-spieler-verwaltung\06-01-SUMMARY.md — canLevelUp / XP_LEVEL_THRESHOLDS final names; D.settings.levelingMode default.
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\.planning\phases\06-spieler-verwaltung\06-RESEARCH.md § "5e-Regelwerk" (canLevelUp) and CONTEXT D-07/D-08/D-11.
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\tests\e2e\features\character-advancement.spec.js — milestone-mode visibility case to implement.
  </read_first>
  <behavior>
    - In XP mode (D.settings.levelingMode === 'xp'), the detail modal shows the character's current xp and the next threshold; when canLevelUp(ch) is true a "kann aufsteigen" hint is shown
    - The hint does NOT change ch.level; only the explicit confirm-level-up action increments level (with pushUndo)
    - In Milestone mode (levelingMode === 'milestone'), XP figures are hidden and a "+1 Level" button is shown
    - Clicking confirm/+1-level increments ch.level by 1 (capped at 20), recomputes proficiencyBonus, pushUndo + save, re-renders
    - canLevelUp({level:1,xp:300}) true / {level:1,xp:299} false (already unit-tested in Wave 1; reused here)
  </behavior>
  <action>
    In party-details.js showCharacterDetails: add an XP/Level block. Read D.settings.levelingMode (default 'xp'). In 'xp' mode: show `ch.xp || 0` and the next threshold XP_LEVEL_THRESHOLDS[(ch.level||1)] (next level index), and when canLevelUp(ch) render a visible hint ("⬆️ {name} kann aufsteigen") plus a "Stufe bestätigen" button with data-action="confirm-level-up" data-id. In 'milestone' mode: hide the xp/threshold figures and render a "+1 Level" button (data-action="confirm-level-up" data-id) — same handler increments level. Use canLevelUp directly (global lexical).

    In entity-actions.js: add a `confirm-level-up` handler: resolve ch via EntityLookup.character(ctx.id); if ch.level>=20 toast and return; pushUndo('Stufe erhöht'); ch.level += 1; recompute ch.proficiencyBonus via getProfBonus(ch.level) (mirror saveCharacter:232 which uses getProfBonus); save(); renderParty(); re-open/refresh the detail modal or showToast confirmation. No function-local const X = window.X; no duplicate top-level names.

    In party.css: style the XP/level block, the level-up hint badge, and the +1-Level/confirm buttons.

    In character-advancement.spec.js: implement the milestone-mode visibility case (seed D.settings.levelingMode='milestone' via page.evaluate, open detail modal, assert XP figures hidden and "+1 Level" button visible) and the level-up-hint case (seed xp at threshold, assert hint visible and that confirming increments level by exactly 1 without auto-bump beforehand). Do not use the broken addCombatant helper.
  </action>
  <verify>
    <automated>cd /d D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular && set PYTHONIOENCODING=utf-8&& python build.py && npx jest tests/unit/character-advancement.test.js && npx playwright test tests/e2e/features/character-advancement.spec.js --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - party-details.js reads D.settings.levelingMode and calls canLevelUp; renders an XP block in xp mode and a "+1 Level" button in milestone mode
    - entity-actions.js defines `confirm-level-up` that increments ch.level by exactly 1 (capped at 20), recomputes proficiencyBonus via getProfBonus, and calls pushUndo before mutation
    - The level-up hint appears when canLevelUp is true and does NOT change ch.level until the user confirms (asserted in E2E)
    - Milestone mode hides XP figures and shows the +1-Level button (asserted in E2E)
    - `npx jest tests/unit/character-advancement.test.js` stays green; `npx playwright test tests/e2e/features/character-advancement.spec.js` passes the milestone + level-up-hint cases
  </acceptance_criteria>
  <done>The detail modal shows XP + a manual-confirm level-up hint in XP mode and a +1-Level button in Milestone mode; leveling is always DM-confirmed, never auto-bumped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| XP modal input → D.characters[*].xp | A user-entered total XP (manual correction) crosses into character data and the distribution math |
| combatant cr → getXPForCR lookup | Combatant CR values (possibly unset/odd-typed) feed the auto-sum |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Tampering/Integrity | manual XP total input | mitigate | Coerce the modal total via parseInt with a non-negative floor; ignore NaN; distribution uses Math.floor so totals stay bounded |
| T-06-12 | Denial of Service | division by zero / empty living party | mitigate | distributeXP returns {share:0} when activeChars is empty (Wave 1); applyXpDistribution guards the empty-party case with a toast |
| T-06-13 | Integrity (silent level overwrite) | level-up | mitigate | Level is NEVER auto-bumped; only confirm-level-up increments, capped at 20, with pushUndo — D-11 |
| T-06-14 | Integrity (data loss) | distribution not undoable | mitigate | pushUndo('XP verteilt') / pushUndo('Stufe erhöht') before mutation so XP changes and level-ups are reversible |
| T-06-15 | Tampering (XSS) | XP modal / hint German labels | mitigate | All labels are static literals; ch.name in the hint uses esc(); no new unescaped user interpolation |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; tooling already present |
</threat_model>

<verification>
- `set PYTHONIOENCODING=utf-8&& python build.py` builds cleanly (no dedup/orphan errors); endCombat unchanged for XP
- `npx jest tests/unit/character-advancement.test.js` green (getXPForCR, distributeXP, canLevelUp)
- `npx playwright test tests/e2e/features/character-advancement.spec.js` green for XP distribution, milestone mode, level-up hint
- `npm run check` does not regress
</verification>

<success_criteria>
- Initiative trigger opens an XP modal with CR-based auto-sum + always-available manual correction (D-09)
- Even split across living characters with documented remainder (D-10)
- Level-up HINT only, DM confirms manually, no auto-bump (D-11)
- D.settings.levelingMode toggles XP vs Milestone (XP hidden + "+1 Level" button in milestone) (D-07)
- XP shown in detail modal (D-08)
- CHAR-01 unit + E2E assertions green
</success_criteria>

<output>
Create D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\.planning\phases\06-spieler-verwaltung\06-04-SUMMARY.md when done.
</output>
