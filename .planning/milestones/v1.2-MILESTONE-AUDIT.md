---
milestone: v1.2
audited: 2026-09-07
status: tech_debt
scores:
  requirements: 19/19
  phases: 3/3
  integration: 3/4
  flows: 3/3
gaps: {}
tech_debt:
  - phase: 13-h-rtung-wartbarkeit
    items:
      - "GESCHLOSSEN 2026-09-07: 13-SECURITY.md retroaktiv erstellt, 67 Bedrohungen (55 + 12x T-13-SC), threats_open 0"
      - "GESCHLOSSEN 2026-09-07: 13-VALIDATION.md abgeglichen, 16/16 gruen, nyquist_compliant true, 0 Luecken"
      - "Zwei subjektive Manual-Only-Bedienabnahmen aus 13-VALIDATION.md sind nie durchgefuehrt worden (Editor-Bediengefuehl, DM-Screen-Masonry) — legitim manual-only, nicht blockierend"
  - phase: 14-tests-gates
    items:
      - "9 datierte Nyquist-Restposten NQ-03..NQ-11 (Ratschen prosa-only, npm run check rot auf 132 Prettier-Dateien, TEST-04-Unit-Haelfte testet Inline-Nachbauten, 14-GATE-BASELINE.md in 5 Zahlen veraltet)"
      - "DEBT-01 (Typecheck) bewusst nur teilgeschaerft: tsconfig.strict.json deckt 8 von 134 Dateien"
  - phase: cross-phase
    items:
      - "7 von 8 Phase-13-Aufteilungsdateien stehen auf MODULE_TEST_EXCEPTIONS des Phase-14-Coverage-Gates — die Aufteilung fuer Testbarkeit (MAINT-01) und das Abdeckungs-Gate (TEST-05) heben sich gegenseitig weitgehend auf"
nyquist:
  compliant_phases: ["12-datensicherheit", "13-h-rtung-wartbarkeit"]
  partial_phases: ["14-tests-gates"]
  not_validated_phases: []
  missing_phases: []
  overall: partial
security:
  audited_phases: ["12-datensicherheit", "13-h-rtung-wartbarkeit", "14-tests-gates"]
  missing_phases: []
  threats_open_total: 0
---

# Milestone v1.2 „Schulden-Abbau" — Audit

**Auditiert:** 2026-09-07
**Status:** `tech_debt` — keine Blocker; die zwei mechanischen Phase-13-Lücken wurden am
2026-09-07 im selben Lauf geschlossen, der Rest ist bewusst geführte Restschuld
**Phasen:** 12 Datensicherheit · 13 Härtung & Wartbarkeit · 14 Tests & Gates

---

## Milestone-Ziel: erreicht

> „Die 26 in der v1.1-Triage erfassten `DEBT`-Posten abarbeiten — allen voran die Datenverlust-Risiken
> in Backup, Export und Migration — damit der Backlog leer ist und v1.3 wieder Features bringen kann."

**Der Backlog ist leer.** Die Buchhaltung wirkt zunächst widersprüchlich und wurde deshalb
nachgerechnet:

| Quelle | Zahl |
|--------|------|
| `milestones/v1.1-REQUIREMENTS.md` — vergebene IDs | 29 (`DEBT-01`..`DEBT-29`) |
| davon noch in v1.1 geschlossen | 2 (`DEBT-17`, `DEBT-29`, beide Commit `71fb6ef`) |
| Übertragstext im Archiv behauptet | „die verbleibenden 27 sind offen" |
| v1.2 `REQUIREMENTS.md` deckt ab | 26 |

Die Lücke ist **`DEBT-02`** (veraltete Build-System-Dokumentation in `CLAUDE.md`/`docs/bugfixes.md`).
Der Posten wurde von Plan 11-07 **innerhalb derselben Phase 11** behoben; `11-06-SUMMARY.md` hält
ausdrücklich fest, dass er trotzdem eine ID bekam, „per this plan's own completeness invariant".
Der Übertragstext hat ihn danach mitgezählt. Live geprüft: `CLAUDE.md` beschreibt das
Zwei-Pass-Verfahren korrekt, benennt die Entfernung des dritten Passes („removed outright in
Phase 11, ARCH-02/D-05") und stellt fest, dass das Handsynchronisieren zweier Modullisten entfällt;
`docs/bugfixes.md` trägt keine veralteten Dedup-/Sync-Stellen mehr. **Kein übersehener Posten,
sondern Buchhaltungsdrift im Archivtext.**

---

## Requirements: 19/19 erfüllt

Drei-Quellen-Abgleich (Traceability-Tabelle · Phasen-`VERIFICATION.md` · SUMMARY-Frontmatter):
alle 19 Requirements in allen drei Quellen vorhanden, alle drei Phasen `status: passed`.
**Keine verwaisten Requirements**, keine `unsatisfied`-Einträge — das FAIL-Gate aus §5e greift nicht.

| Requirement | Phase | Quellen |
|-------------|-------|---------|
| SAFE-01..SAFE-06 | 12 | Tabelle ✓ · VERIFICATION passed (9/9) ✓ · Frontmatter ✓ |
| SEC-03, SEC-04, PERF-01, PERF-02, MAINT-01..MAINT-06 | 13 | Tabelle ✓ · VERIFICATION passed (7/8, 1 Override) ⚠ · Frontmatter ✓ |
| TEST-03, TEST-04, TEST-05 | 14 | Tabelle ✓ · VERIFICATION passed (5/5) ✓ · Frontmatter ✓ |

---

## Phasen-Verifikation: 3/3

| Phase | VERIFICATION | UAT | SECURITY | VALIDATION (Nyquist) |
|-------|--------------|-----|----------|----------------------|
| 12 Datensicherheit | `passed` 9/9 | complete | `audited`, `threats_open: 0` | `validated`, compliant **COMPLIANT** |
| 13 Härtung & Wartbarkeit | `passed` 7/8, 1 Override (freigegeben) | complete | `verified`, `threats_open: 0` | `validated`, compliant **COMPLIANT** |
| 14 Tests & Gates | `passed` 5/5 | complete | `verified`, `threats_open: 0` | `validated`, nicht compliant **PARTIAL** |

---

## Cross-Phase-Integration: 3/4

Geprüft gegen den Live-Baum (Nicht-ESM-Architektur: `loader.js` MODULES ist die einzige Quelle,
`build.py` liest sie, es gibt keine API-Routen und keinen Server).

| Naht | Ergebnis |
|------|----------|
| Phase-13-Modulaufteilungen → `loader.js` → `build.py` | **WIRED** — alle Zieldateien in korrekter Abhängigkeitsposition registriert, keine Waisen; `build.py` Exit 0, `pytest tests/build/` 24 grün. MAINT-01 bestätigt: alle Dateien ≤ 800 Zeilen (grösste 673) |
| Phase-13-Save-Pfad ↔ Phase-12-Datensicherheit | **WIRED, testbelegt** — `utf8ByteLength()` lädt (`utils/basic.js`, loader-Zeile 19) vor beiden Konsumenten; der `>5 MB`-Pfad schreibt weiterhin **erst** nach IndexedDB und räumt **danach** localStorage ab (Quelltext-Assertion in `tests/unit/stability.test.js`); `registerPostSaveHook()` statt `window.save`-Wrapper an beiden Konsumenten belegt |
| Globals-Drift ↔ Aufteilungsdateien | **WIRED, testbelegt** — `eslint-globals-freshness.test.js` 5/5 grün, leitet aus derselben `loader.js`-Liste ab, enthält einen eigenen WR-01-Wächter gegen destrukturierte Top-Level-Bindungen |
| Coverage-Gate (TEST-05) ↔ Aufteilungsdateien (MAINT-01) | **PARTIAL** — siehe unten |

### Befund: Aufteilung und Abdeckungs-Gate heben sich weitgehend auf

Phase 13 hat vier übergrosse Module aufgeteilt, ausdrücklich für Wartbarkeit und **Testbarkeit**
(MAINT-01). Phase 14s Modul-zu-Test-Gate (TEST-05) hat davon **7 von 8** Ergebnisdateien wieder in
`MODULE_TEST_EXCEPTIONS` ausgenommen:

| Datei | Im Gate |
|-------|---------|
| `features/wiki/wiki-crud.js` | **abgedeckt** (`tests/unit/wiki-links.test.js` lädt sie per `vm` in Loader-Reihenfolge) |
| `features/spells/spell-manager.js` | ausgenommen |
| `features/initiative-loot.js` | ausgenommen |
| `features/initiative-combat-widgets.js` | ausgenommen |
| `features/dmscreen/dmscreen-widgets-base.js` | ausgenommen |
| `features/dmscreen/dmscreen-widgets-combat.js` | ausgenommen |
| `features/dmscreen/dmscreen-widgets-reference.js` | ausgenommen |
| `ui/editors/rich-text-toolbars.js` | ausgenommen |

**Bewertung: WARNUNG, kein Blocker.** Die Ausnahmen sind datiert, sichtbar, mit `CR-01`-Marke
versehen und durch zwei Ratschen-Testfälle gegen stilles Wiederverstecken gesichert — das Gate
lügt nicht, es legt die Lücke offen. Aber das Sicherheitsnetz, das die Aufteilung erst nützlich
machen sollte, steht 1 zu 8. Der Integrationsprüfer meldete 4 von 5; die eigene Nachzählung ergab
7 von 8 — die höhere Zahl gilt.

### End-to-End-Flüsse: 3/3

| Fluss | Ergebnis |
|-------|----------|
| Kampagne anlegen → speichern → neu laden → Daten intakt | WIRED, testbelegt (`stability.test.js` „Data Persistence" plus PERF-01-Tracer) |
| Export/Backup → Import/Restore | WIRED, testbelegt (`full-export`, `migration-wizard`, `file-backup`, `audio-export`, `audio-import-resilience`) |
| Undo nach destruktiver Operation | WIRED — Byte-Budget/Dedupe testbelegt; die Kopplung an die einzelnen CRUD-Aufrufstellen ist aus dem Quelltext geschlossen, nicht je Entitätstyp neu nachgefahren |

---

## Angesammelte Schulden (Entscheidungsbedarf)

### 1. Phase 13: SECURITY.md fehlte — GESCHLOSSEN 2026-09-07

`workflow.security_enforcement` stand auf `true`, alle zwölf Pläne trugen einen
`<threat_model>`-Block, und dennoch existierte kein `13-SECURITY.md`: der `verify:post`-Schritt
`secure-phase` war für diese Phase nie gelaufen. **Retroaktiv nachgeholt** —
`13-SECURITY.md` deckt jetzt 67 Bedrohungen ab (55 nummerierte T-13-01..T-13-55 plus die
zwölfmal wiederholte Lieferkettenprüfung T-13-SC), **alle CLOSED, `threats_open: 0`**.
Eine dokumentierte Abweichung: T-13-03 protokolliert unbedingt statt `DEBUG_MODE`-gegatet —
ein bewusster Folgefix (`01da4f1`), severity `low`, nicht blockierend.

### 2. Phase 13 war NOT-VALIDATED — GESCHLOSSEN 2026-09-07

`13-VALIDATION.md` stand auf `status: draft` mit `TBD`-Platzhaltern. **Retroaktiv
abgeglichen:** 16/16 Verifikationszeilen grün, Wave 0 7/7 geliefert, **0 Lücken**. Die Phase
war nie unvollständig validiert — sie war nur nie abgeglichen. `status: validated`,
`nyquist_compliant: true`.

Offen geblieben, bewusst: zwei subjektive Bedienabnahmen (Editor-Bediengefühl nach dem
`rich-text.js`-Split, DM-Screen-Masonry bei 320/768 px/Vollbreite). Legitim manual-only,
funktional durch das 79-Test-Netz bzw. den 50-Snapshot-Charakterisierungstest gedeckt.

### 3. PERF-01: Override ist freigegeben — kein offener Posten

**Korrektur gegenüber der ersten Fassung dieses Audits.** Der Erstbefund lautete „Override
ohne menschliche Freigabe" und stützte sich auf einen Satz im `gaps:`-Eintrag von
`13-VERIFICATION.md`: „No VERIFICATION.md override has been formally accepted by a human for
this exact deviation." Dieser Satz beschreibt den Stand **zum Zeitpunkt der Gap-Erhebung**.
Weiter unten in derselben Frontmatter steht der `overrides:`-Block, der ihn auflöst:

```yaml
overrides:
  - truth: "Weder ein Undo-Snapshot noch ein Save serialisiert bei jeder Operation die
      vollstaendige Kampagne (Erfolgskriterium 2)"
    accepted_by: maintainer
    accepted_at: "2026-09-06"
    decision: "Freigegeben. …"
```

Sachlage unverändert: `pushUndo()` ruft weiterhin bei jeder destruktiven Operation
`JSON.stringify(window.D)`, der Roadmap-Wortlaut ist wörtlich nicht erfüllt. Aber die
Abweichung war in `13-CONTEXT.md` (D-10) **vorab** benannt, mit der Abnahmebedingung
„einstellige Millisekunden gegen eine realistische Kampagne" versehen, die Messung liegt vor
(0,922 ms Median, `13-PERF-MEASUREMENT.md`) — und die Freigabe ist mit Datum und
`accepted_by` protokolliert. **Kein Handlungsbedarf.**

### 4. Phase 14: 9 datierte Nyquist-Restposten

`NQ-03`..`NQ-11` in `14-VALIDATION.md`. Die schwersten: alle drei Ratschen der Phase
(`--max-warnings`, `tsconfig.strict`-Include, `MODULE_TEST_EXCEPTIONS`) sind reine Prosa-Regeln,
von denen sich zwei bereits gegen die eigene Vorschrift bewegt haben; `npm run check` — erklärtes
Akzeptanzkriterium zweier Pläne — ist **rot** (132 Dateien scheitern an `format:check`, und CI
fährt `format:check` überhaupt nicht); die Unit-Hälfte von TEST-04 testet Inline-Nachbauten statt
der Produktionsmodule.

### 5. DEBT-01 bleibt bewusst teiloffen

`tsconfig.strict.json` deckt 8 von 134 Dateien. In `REQUIREMENTS.md` und `14-GATE-BASELINE.md`
mit gemessenen Zahlen benannt, nicht verschwiegen.

---

## Nachbetrachtung: was dieses Audit gefunden hat, das die Phasen selbst nicht meldeten

Zwei Klassen von Befund tauchten erst beim phasenübergreifenden Blick auf:

1. **Ein gebautes Gate lief nicht.** Phase 14 hat eine korrekt kalibrierte Coverage-Ratsche
   gebaut, die in keiner Automatisierung ausgeführt wurde (`collectCoverage: false`, CI fuhr
   blankes `npm test`). Die Phase hielt sich selbst für fertig. Merkregel für v1.3: „Gate
   existiert" und „Gate läuft" sind zwei verschiedene Aussagen — die zweite steht in
   `.github/workflows/ci.yml`, nicht im SUMMARY.
2. **Eine SUMMARY-Behauptung war falsch.** `14-06-SUMMARY.md` behauptete, alle sieben toten
   Aktionsziele „kommen nirgends mehr vor"; `populateImportNodesList` stand weiter in der
   Whitelist und ging in beide dist-Bundles. Beide Befunde wurden am 2026-09-07 geschlossen
   (Commit `47f07c5`).

---

## Prüfstand zum Auditzeitpunkt

`npx jest` 50 Suiten / 1120 Tests grün · `npx eslint . --max-warnings 367` 0 Fehler / 367 Warnungen ·
`npx tsc --noEmit` 0 · `npx tsc -p tsconfig.strict.json` 0 · `npx jest --coverage` 0 ·
`python -m pytest tests/build/` 24 grün · `python build.py` Exit 0 ·
Playwright Lastprobe `crud/locations`+`crud/encounters` `--repeat-each=20 --workers=8 --retries=0`
380/380 grün.

Volle Playwright-Suite in diesem Lauf neu erhoben: **321 bestanden / 2 übersprungen (323 gesamt)**,
Exit 0, 1,9 min.

Testwachstum über den Milestone: Jest 628 → **1120** · Playwright 319 → **323** ·
pytest 24 → 24 (unverändert).
