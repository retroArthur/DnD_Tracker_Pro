---
phase: 02-technik-fundament
plan: 05
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - features/command-palette/action-registry.js
  - features/command-palette/command-palette.js
  - systems/spellslots/keyboard-shortcuts.js
  - assets/styles/command-palette.css
  - tests/unit/action-registry.test.js
  - tests/e2e/features/command-palette.spec.js
autonomous: true
requirements: [TECH-04]
must_haves:
  truths:
    - "Strg+Shift+K oeffnet und schliesst die Command Palette (.cp-overlay), ohne mit Strg+K (Global Search) zu kollidieren"
    - "searchActions('Neuer NPC') liefert die new-npc-Aktion als Top-Treffer; searchActions('8d6' bzw 'Wuerfle') findet die Wuerfel-Formel-Aktion ueber die bestehende fuzzyMatch-Funktion"
    - "Bei leerem Query zeigt die Palette die Top-8-Aktionen aus der Registry; Pfeiltasten navigieren, Enter fuehrt die fokussierte Aktion aus, Esc schliesst"
    - "Die Command Palette findet AKTIONEN (nicht Entitaeten) und existiert unabhaengig neben der Global Search"
  artifacts:
    - path: "features/command-palette/action-registry.js"
      provides: "ACTION_REGISTRY (Array aus {id,label,keywords,action}) + searchActions (fuzzyMatch-basiert)"
      contains: "ACTION_REGISTRY"
    - path: "features/command-palette/command-palette.js"
      provides: "initCommandPalette, toggleCommandPalette, performCommandSearch, Overlay-Rendering, Tastatur-Navigation"
      contains: "toggleCommandPalette"
    - path: "tests/unit/action-registry.test.js"
      provides: "Gruene Tests fuer Fuzzy-Suche ueber die Registry"
      contains: "searchActions"
    - path: "tests/e2e/features/command-palette.spec.js"
      provides: "Gruener E2E-Test fuer Oeffnen + Treffer"
      contains: "cp-overlay"
  key_links:
    - from: "features/command-palette/action-registry.js"
      to: "fuzzyMatch (systems/search/global-search.js)"
      via: "Wiederverwendung der bestehenden Fuzzy-Suche (kein Duplikat)"
      pattern: "fuzzyMatch"
    - from: "systems/spellslots/keyboard-shortcuts.js"
      to: "window.toggleCommandPalette"
      via: "Strg+Shift+K-Bindung nach dem Strg+K-Block"
      pattern: "toggleCommandPalette"
    - from: "core/init.js"
      to: "initCommandPalette"
      via: "defensiver Aufruf (in Plan 02-01 verdrahtet)"
      pattern: "initCommandPalette"
    - from: "features/command-palette/command-palette.js"
      to: "EventDelegation.registerAction (ui/event-delegation.js)"
      via: "Selbst-Registrierung der data-action execute-command im eigenen Modul (Lookup in window.ACTION_REGISTRY) — kein system-actions.js-Edit (Welle-2-Konfliktfreiheit)"
      pattern: "EventDelegation.registerAction"
---

<objective>
Liefert die Command Palette (TECH-04, Claude's Discretion): ein zentriertes Floating-Overlay, das ueber eine Aktions-Registry per Fuzzy-Suche Aktionen findet und ausfuehrt (z.B. "Neuer NPC", "Wuerfle 8d6"). Kollisionsfreier Shortcut Strg+Shift+K (Strg+K ist durch Global Search belegt; Strg+P blockiert Browser-Drucken). Wiederverwendung der bestehenden fuzzyMatch-Funktion — kein neuer Fuzzy-Algorithmus.

Purpose: Erfuellt Erfolgskriterium 4 der Phase — Nutzer drueckt die Command-Palette-Taste, tippt "Neuer NPC", waehlt den Eintrag und die NPC-Erstellen-Ansicht oeffnet sich; Fuzzy-Suche findet auch Aktionen wie "Wuerfle 8d6". Funktional abgegrenzt zur Global Search (die Entitaeten findet).

Output: features/command-palette/action-registry.js (Registry + Suche), features/command-palette/command-palette.js (Overlay + Tastatur), Strg+Shift+K-Bindung in keyboard-shortcuts.js, Command-Palette-CSS, gruene Unit- und E2E-Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-technik-fundament/02-CONTEXT.md
@.planning/phases/02-technik-fundament/02-RESEARCH.md
@.planning/phases/02-technik-fundament/02-PATTERNS.md
@.planning/phases/02-technik-fundament/02-UI-SPEC.md

<interfaces>
fuzzyMatch (systems/search/global-search.js, gelesen): fuzzyMatch(text, query) -> {match: boolean, score: number}. Globale Funktion, direkt aufrufbar (NICHT als const X = window.X in Funktionen redeklarieren). debounce() ist global verfuegbar.
keyboard-shortcuts.js (gelesen): initKeyboardShortcuts() bindet keydown; Strg+K/Strg+F-Block bei Zeilen 59-63. Neue Strg+Shift+K-Bindung DIREKT nach diesem Block einfuegen (vor dem "if (isTyping) return;" bei Zeile 65, damit die Palette auch beim Tippen aufrufbar ist — sie ist ein globaler Shortcut wie Strg+K).
Beispiel-Aktionen (RESEARCH Pattern 5): new-npc -> window.showModal('npc-modal'); new-char -> window.toggleCollapse('char-form')+window.switchView('party'); roll-d20 -> window.quickRoll(20); nav-* -> window.switchView(...); open-settings -> window.showModal('settings-modal'); backup-setup -> window.showFileBackupSetup (Plan 02-04, defensiv). Pruefe die echten Funktionsnamen in ui/actions/ + systems/spellslots/keyboard-shortcuts.js (action dispatch Zeilen 170-179) und nutze die tatsaechlich existierenden.
Stub von Plan 02-01: command-palette.js exportiert bereits window.initCommandPalette (no-op) — Koerper hier ersetzen.
Analoge (02-PATTERNS.md): command-palette.js folgt systems/search/global-search.js (debounced input, data-action-Result-Rendering, click-outside); action-registry.js folgt keyboard-shortcuts.js action-dispatch.
UI-SPEC Komponente 10: .cp-overlay (z-index 1200), .cp-box (top 20vh), .cp-input-row, .cp-results, .cp-result (.focused mit border-left var(--gold)), .cp-empty; exakte Texte aus Copywriting (Fehler: "Keine Aktion gefunden — Tipp anpassen oder Esc zum Schliessen.").
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: action-registry.js — Aktions-Registry + Fuzzy-Suche (fuzzyMatch-Wiederverwendung)</name>
  <files>features/command-palette/action-registry.js, tests/unit/action-registry.test.js</files>
  <read_first>
    - systems/search/global-search.js (fuzzyMatch Zeilen 10-40 — Rueckgabe {match, score})
    - systems/spellslots/keyboard-shortcuts.js (action-dispatch Zeilen 170-179 — die kanonischen kontextsensitiven Aktionen, aus denen die Registry-Eintraege abgeleitet werden)
    - .planning/phases/02-technik-fundament/02-PATTERNS.md (Abschnitt action-registry.js — Dispatch-Muster, Export-Muster, Regel "kein const X = window.X in Aktionsfunktionen")
    - .planning/phases/02-technik-fundament/02-RESEARCH.md (Pattern 5 — ACTION_REGISTRY-Beispiel + searchActions verbatim)
    - tests/unit/action-registry.test.js (von Plan 02-01 erstellter RED-Stub — hier gruen ziehen)
    - ui/actions/ (echte data-action-Handler / window-Funktionsnamen wie showModal, switchView, toggleCollapse, quickRoll — die in den Aktionen aufgerufenen Funktionen muessen existieren)
  </read_first>
  <behavior>
    - Test "searchActions findet Neuer NPC": searchActions('Neuer NPC')[0].id === 'new-npc' (oder enthaelt new-npc als Top-Treffer).
    - Test "searchActions findet Wuerfel-Aktion": searchActions('8d6') bzw searchActions('Wuerfle') liefert die roll-Formel/roll-Aktion mit score > 0.
    - Test "leerer Query liefert Top-N": searchActions('') gibt bis zu 8 Eintraege ohne Score-Filterung zurueck.
    - Test "kein Treffer": searchActions('xyzqkw') gibt ein leeres Array zurueck.
  </behavior>
  <action>
    Implementiere action-registry.js gemaess RESEARCH Pattern 5 + PATTERNS. Definiere const ACTION_REGISTRY als Array von Objekten {id, label, keywords:[...], action: () => {...}} mit mindestens diesen Aktionen: new-npc, new-char, new-quest, roll-d20, roll-formula (Wuerfle 8d6), undo, backup-setup (window.showFileBackupSetup defensiv), nav-initiative, open-settings, sowie weiteren naheliegenden Navigations-/CRUD-Aktionen. Jede action ruft die ECHTEN window-Funktionen direkt auf (z.B. window.showModal('npc-modal')) — NIEMALS const X = window.X innerhalb der action-Funktion (CLAUDE.md Dedup-Regel); verifiziere die Funktionsnamen gegen ui/actions/ und keyboard-shortcuts.js. Implementiere searchActions(query): bei leerem/zu kurzem Query ACTION_REGISTRY.slice(0,8); sonst je Eintrag max(fuzzyMatch(label).score, max keyword-scores), filter score>0, sort desc, slice(0,10) — fuzzyMatch direkt aufrufen (Wiederverwendung, kein Duplikat). Exportiere window.ACTION_REGISTRY und window.searchActions. Ziehe action-registry.test.js gruen.
  </action>
  <verify>
    <automated>cd "D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular" && npx jest tests/unit/action-registry.test.js 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - npx jest tests/unit/action-registry.test.js — alle Tests gruen (exit code 0)
    - action-registry.js enthaelt ACTION_REGISTRY mit einem Eintrag id 'new-npc' und einem Wuerfel-Eintrag (roll-formula oder roll-d20)
    - action-registry.js ruft fuzzyMatch auf (Wiederverwendung) und definiert KEINE eigene Fuzzy-Algorithmus-Funktion
    - searchActions('') liefert hoechstens 8 Eintraege; searchActions('xyzqkw') liefert []
    - action-registry.js enthaelt KEIN 'const showModal = window.showModal' (oder aehnlich) innerhalb einer action-Funktion
    - window.ACTION_REGISTRY und window.searchActions exportiert
    - node --check exit code 0
  </acceptance_criteria>
  <done>Die Aktions-Registry liefert ueber die bestehende fuzzyMatch-Funktion Treffer fuer "Neuer NPC" und "Wuerfle 8d6"; Unit-Tests gruen.</done>
</task>

<task type="auto">
  <name>Task 2: command-palette.js — Overlay, Tastatur-Navigation, Strg+Shift+K-Bindung</name>
  <files>features/command-palette/command-palette.js, systems/spellslots/keyboard-shortcuts.js, assets/styles/command-palette.css, tests/e2e/features/command-palette.spec.js</files>
  <read_first>
    - systems/search/global-search.js (debounced input Zeile 56; Result-Rendering mit data-action Zeilen 171-188; click-outside Zeilen 247-254; initGlobalSearchListener Zeilen 255-260)
    - systems/spellslots/keyboard-shortcuts.js (Strg+K-Block Zeilen 59-63 — Einfuegepunkt fuer Strg+Shift+K DIREKT danach; Escape-Konsolidierung Zeilen 14-36)
    - .planning/phases/02-technik-fundament/02-PATTERNS.md (Abschnitt command-palette.js — debounced search, data-action-Rendering execute-command, click-outside/Escape, Keyboard-Bindung-Snippet verbatim)
    - .planning/phases/02-technik-fundament/02-UI-SPEC.md (Komponente 10 Command Palette: .cp-overlay/.cp-box/.cp-input-row/.cp-results/.cp-result/.cp-empty, Zustaende .focused, Animationen Slide-down, Focus-Management cp-input; Tastatur-Navigation-Tabelle; exakte Fehlertexte)
    - tests/e2e/features/command-palette.spec.js (von Plan 02-01 erstellter RED-Stub — hier gruen ziehen)
    - ui/event-delegation.js (EventDelegation.registerAction(name, handler) — die oeffentliche Selbst-Registrierungs-API, Zeile 51)
    - ui/actions/entity-actions.js (Vorbild: Modul registriert seine eigenen data-actions am Dateiende via EventDelegation.registerAction-Schleife, Zeilen 279-283)
  </read_first>
  <behavior>
    - command-palette.spec.js Test "Strg+Shift+K oeffnet": nach dem Tastendruck ist .cp-overlay sichtbar und .cp-input fokussiert.
    - command-palette.spec.js Test "Tippen zeigt Treffer": Eingabe 'NPC' rendert mindestens eine .cp-result-Zeile.
  </behavior>
  <action>
    Ersetze den no-op-Koerper von initCommandPalette() so, dass es die Overlay-Struktur vorbereitet und den Input-Listener registriert (Muster initGlobalSearchListener). Implementiere toggleCommandPalette(): erzeugt/zeigt das #cp-overlay (.cp-overlay create-or-update), setzt Fokus auf .cp-input, rendert bei leerem Query die Top-8 aus searchActions('') (UI-SPEC Standard-Zustand); erneutes Toggle schliesst. Implementiere performCommandSearch() (via const debouncedCommandSearch = debounce(performCommandSearch, 80)): liest .cp-input, ruft window.searchActions, rendert .cp-result-Zeilen mit data-action="execute-command" data-command-id (Icon, Label via esc(), optionaler Shortcut-Hint); leeres Ergebnis -> .cp-empty mit exaktem Text "Keine Aktion gefunden — Tipp anpassen oder Esc zum Schliessen.". Implementiere Tastatur-Navigation im Overlay: Pfeil hoch/runter bewegt .cp-result.focused, Enter fuehrt die action des fokussierten Eintrags aus + schliesst, Esc schliesst (in die bestehende Escape-Konsolidierung in keyboard-shortcuts.js einreihen ODER lokaler keydown-Listener am Overlay). click-outside schliesst (Muster global-search). Registriere data-action="execute-command" am Ende DIESES Moduls (command-palette.js) selbst via EventDelegation.registerAction('execute-command', ctx => ...) nach dem Vorbild ui/actions/entity-actions.js (Zeilen 279-283); der Handler liest data-command-id und ruft die action des passenden window.ACTION_REGISTRY-Eintrags auf. KEINE Bearbeitung von ui/actions/system-actions.js (Selbst-Registrierung ist das etablierte Muster laut ARCHITECTURE.md und haelt die parallelen Welle-2-Plaene konfliktfrei). Schuetze die Registrierung mit if (typeof EventDelegation !== 'undefined'). Fuege in keyboard-shortcuts.js DIREKT nach dem Strg+K-Block die Strg+Shift+K-Bindung ein: if ((e.ctrlKey||e.metaKey) && e.shiftKey && (e.key==='K'||e.key==='k')) { e.preventDefault(); if (typeof window.toggleCommandPalette==='function') window.toggleCommandPalette(); return; } — defensiver typeof-Guard, KEIN const X=window.X. Ergaenze die .cp-*-CSS-Klassen in dashboard.css gemaess UI-SPEC (z-index 1200, top 20vh, .cp-result.focused border-left var(--gold), Slide-down-Animation, prefers-reduced-motion). Exportiere window.initCommandPalette, window.toggleCommandPalette, window.performCommandSearch. Ziehe command-palette.spec.js gruen.
  </action>
  <verify>
    <automated>cd "D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular" && node --check features/command-palette/command-palette.js && node --check systems/spellslots/keyboard-shortcuts.js && python build.py 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - node --check fuer command-palette.js und keyboard-shortcuts.js exit code 0
    - command-palette.js enthaelt toggleCommandPalette und rendert .cp-result mit data-action="execute-command"
    - command-palette.js registriert die execute-command-data-action selbst via EventDelegation.registerAction; ui/actions/system-actions.js ist UNVERAENDERT (kein Diff)
    - command-palette.js nutzt esc() fuer den Aktions-Label-Output
    - command-palette.js enthaelt den exakten Leer-Text "Keine Aktion gefunden — Tipp anpassen oder Esc zum Schliessen."
    - keyboard-shortcuts.js enthaelt eine Strg+Shift+K-Bindung mit e.shiftKey und window.toggleCommandPalette (defensiver typeof-Guard)
    - die Strg+K/Strg+F-Bindung (Zeilen 59-63) bleibt unveraendert vorhanden (keine Kollision)
    - assets/styles/command-palette.css enthaelt .cp-overlay und .cp-result.focused
    - python build.py exit code 0
    - npx playwright test tests/e2e/features/command-palette.spec.js — gruen (oder dokumentiert lauffaehig gegen dist-Build)
  </acceptance_criteria>
  <done>Strg+Shift+K oeffnet/schliesst die Command Palette ohne Kollision mit Global Search; Tippen filtert Aktionen via Fuzzy-Suche; Pfeil/Enter/Esc funktionieren; E2E gruen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nutzer-Eingabe -> Command-Palette-UI | Query-String und Aktions-Labels werden in innerHTML gerendert (XSS) |
| Shortcut-Layer | Strg+Shift+K darf bestehende Shortcuts (Strg+K Global Search) nicht brechen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-17 | Tampering/XSS | Aktions-Labels/Query in der Ergebnisliste (innerHTML) | mitigate | esc() fuer alle dynamischen Strings im Result-Rendering (Task 2 acceptance_criteria); Registry-Labels sind statisch/projekteigen |
| T-02-18 | Denial of Service | Strg+Shift+K kollidiert/ueberschreibt Strg+K und bricht Global Search | mitigate | Bindung NUR mit e.shiftKey; Strg+K-Block bleibt unveraendert (Task 2 acceptance_criteria prueft beide); Shortcut-Audit im Smoke-Test (RESEARCH Open Question 2) |
| T-02-SC | Tampering | npm/pip/cargo-Installs | accept | Keine Paket-Installs (Wiederverwendung bestehender fuzzyMatch/debounce). Package-Legitimacy-Gate nicht anwendbar |
</threat_model>

<verification>
- npx jest tests/unit/action-registry.test.js gruen (Fuzzy-Suche ueber Registry)
- npx playwright test tests/e2e/features/command-palette.spec.js gruen (Oeffnen + Treffer)
- node --check fuer command-palette.js und keyboard-shortcuts.js besteht
- python build.py laeuft fehlerfrei
- npm run check bleibt gruen
</verification>

<success_criteria>
- Command Palette oeffnet mit Strg+Shift+K (kollisionsfrei), findet Aktionen via fuzzyMatch
- "Neuer NPC" oeffnet die NPC-Erstellen-Ansicht; "Wuerfle 8d6" wird gefunden
- Tastatur-Navigation (Pfeile/Enter/Esc) funktioniert; klar abgegrenzt zur Global Search
</success_criteria>

<output>
Create `.planning/phases/02-technik-fundament/02-05-SUMMARY.md` when done
</output>
