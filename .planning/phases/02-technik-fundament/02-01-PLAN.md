---
phase: 02-technik-fundament
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - loader.js
  - build.py
  - core/init.js
  - assets/styles.css
  - assets/templates/header.html
  - systems/migration/full-export.js
  - systems/migration/migration-wizard.js
  - systems/file-backup/file-backup-permissions.js
  - systems/file-backup/file-backup-manager.js
  - systems/file-backup/file-backup-ui.js
  - features/command-palette/action-registry.js
  - features/command-palette/command-palette.js
  - assets/styles/fonts.css
  - assets/styles/pwa.css
  - assets/styles/migration.css
  - assets/styles/file-backup.css
  - assets/styles/command-palette.css
  - tests/e2e/features/pwa.spec.js
  - tests/e2e/features/command-palette.spec.js
  - tests/unit/full-export.test.js
  - tests/unit/file-backup.test.js
  - tests/unit/action-registry.test.js
autonomous: true
requirements: [TECH-01, TECH-02, TECH-03, TECH-04]
must_haves:
  truths:
    - "Der Production-Build (python build.py --production) laeuft fehlerfrei durch, obwohl alle neuen Phase-2-Module/CSS nur leere Stubs sind"
    - "init() ruft initFileBackup, initMigrationWizardIfNeeded und initCommandPalette defensiv auf (typeof-Guard), ohne dass die Implementierungen schon existieren"
    - "registerServiceWorker() erkennt eine wartende neue SW-Version und ruft window.showSWUpdateHint defensiv auf (D-03)"
    - "header.html enthaelt die versteckten Andock-Platzhalter .pwa-install-btn und .backup-warning-indicator (Standard display none) — Welle 2 verdrahtet nur das Verhalten"
    - "Alle fuenf neuen CSS-Dateien sind in build.py css_files UND assets/styles.css @import registriert; alle sechs Wave-0-Testdateien existieren und laufen"
  artifacts:
    - path: "loader.js"
      provides: "Registrierung aller 7 neuen Phase-2-Module in korrekter Ladereihenfolge"
      contains: "features/command-palette/command-palette.js"
    - path: "build.py"
      provides: "Identische Modulliste wie loader.js + 5 neue CSS-Dateien in css_files"
      contains: "command-palette.css"
    - path: "core/init.js"
      provides: "Defensive Init-Aufrufe + SW-Update-Erkennung"
      contains: "initCommandPalette"
    - path: "assets/templates/header.html"
      provides: "Versteckte Andock-Platzhalter fuer Install-Button + Backup-Warnindikator"
      contains: "backup-warning-indicator"
    - path: "assets/styles.css"
      provides: "@import aller 5 neuen Phase-2-CSS-Dateien"
      contains: "command-palette.css"
    - path: "tests/unit/full-export.test.js"
      provides: "Test-Stub fuer TECH-02 Voll-Export-Format"
      contains: "describe"
    - path: "tests/unit/file-backup.test.js"
      provides: "Test-Stub fuer TECH-03 mit gemockter File System API"
      contains: "describe"
    - path: "tests/unit/action-registry.test.js"
      provides: "Test-Stub fuer TECH-04 Fuzzy-Suche"
      contains: "describe"
    - path: "tests/e2e/features/pwa.spec.js"
      provides: "E2E-Stub fuer TECH-01 Manifest + SW"
      contains: "test"
    - path: "tests/e2e/features/command-palette.spec.js"
      provides: "E2E-Stub fuer TECH-04 Command Palette"
      contains: "test"
  key_links:
    - from: "loader.js"
      to: "build.py"
      via: "Modullisten-Sync-Check (seit Phase 1)"
      pattern: "command-palette"
    - from: "core/init.js"
      to: "window.initFileBackup / window.initCommandPalette / window.showSWUpdateHint"
      via: "defensiver typeof-Guard"
      pattern: "typeof window"
    - from: "build.py css_files + assets/styles.css"
      to: "assets/styles/*.css (5 neue Dateien)"
      via: "Registrierung + @import"
      pattern: "command-palette.css"
---

<objective>
Errichtet das Integrations-Rueckgrat und das Wave-0-Test-Geruest fuer Phase 2. Dieser Plan besitzt EXKLUSIV alle geteilten Dateien (`loader.js`, `build.py`, `core/init.js`, `assets/styles.css`, `assets/templates/header.html`) und legt alle neuen Modul- und CSS-Dateien als leere, build-sichere Stubs an. Dadurch koennen die vier Feature-Plaene (02-05) in Welle 2 vollstaendig parallel laufen, ohne sich an geteilten Dateien zu blockieren — jeder Feature-Plan beruehrt danach nur seine eigenen, isolierten Dateien.

Purpose: Vermeidet den Scavenger-Hunt- und Merge-Konflikt-Antipattern. Modul-Ladereihenfolge, CSS-Registrierung, Header-Andockpunkte, Init-Verdrahtung und SW-Update-Erkennung werden einmal zentral festgelegt. Ausserdem werden alle in RESEARCH.md (Validation Architecture, Wave-0-Gaps) geforderten Testdateien erstellt, damit die Nyquist-Regel (jeder Task hat einen automatisierten Verify) durchgehend erfuellbar ist.

Output: aktualisierte loader.js + build.py (7 Module + 5 CSS-Dateien registriert), aktualisierte core/init.js (defensive Init-Aufrufe + SW-Update-Erkennung), aktualisierte header.html (2 Platzhalter), aktualisierte styles.css (@imports), 7 leere Stub-Modul-Dateien, 5 leere CSS-Stubs, 5 Wave-0-Testdateien.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-technik-fundament/02-CONTEXT.md
@.planning/phases/02-technik-fundament/02-RESEARCH.md
@.planning/phases/02-technik-fundament/02-PATTERNS.md
@.planning/phases/02-technik-fundament/02-UI-SPEC.md

<interfaces>
Stub-Module exportieren je eine no-op-Init-Funktion (Welle 2 ersetzt den Koerper):
- systems/file-backup/file-backup-manager.js   -> window.initFileBackup = function(){}
- systems/migration/migration-wizard.js        -> window.initMigrationWizardIfNeeded = function(){}
- features/command-palette/command-palette.js   -> window.initCommandPalette = function(){}
- systems/spellslots/pwa-install.js (Plan 02)   -> window.showSWUpdateHint = function(newSW){}  (NICHT in diesem Plan; nur defensiv aufgerufen)

Ladereihenfolge in loader.js UND build.py (Quelle: 02-PATTERNS.md "loader.js and build.py Registration"):
  nach 'systems/campaign-manager/campaign-manager.js':
    'systems/migration/full-export.js'
    'systems/migration/migration-wizard.js'
    'systems/file-backup/file-backup-permissions.js'
    'systems/file-backup/file-backup-manager.js'
    'systems/file-backup/file-backup-ui.js'
  nach 'features/dmscreen/dmscreen-render.js':
    'features/command-palette/action-registry.js'
    'features/command-palette/command-palette.js'

CSS-Registrierung (Quelle: build.py css_files Zeilen 409-415; assets/styles.css @import-Hub):
  build.py css_files: 'fonts.css' als ERSTE Datei (vor 'variables.css'); 'pwa.css', 'migration.css', 'file-backup.css', 'command-palette.css' am Ende (nach 'tools.css').
  assets/styles.css: @import fuer alle 5 in derselben Reihenfolge.
  Inhalt jeder CSS-Datei fuellt Welle 2 (fonts.css/pwa.css -> Plan 02; migration.css -> 03; file-backup.css -> 04; command-palette.css -> 05).

header.html-Platzhalter (Quelle: 02-UI-SPEC.md Komponente 2 + 7): in .header-actions ein Button .pwa-install-btn (data-action="install-pwa", display none) und ein Indikator .backup-warning-indicator (nach .save-indicator, display none). Verhalten/Sichtbarkeit liefert Welle 2.

init.js-Anker (Quelle: core/init.js gelesen):
- init()-Aufrufblock: Zeilen 111-121 (startAutoBackup, registerServiceWorker, initOfflineDetection, initPWA)
- registerServiceWorker(): Zeilen 173-210; HEAD-fetch-Huelle bleibt, innerer .register().then(reg => log(...)) wird durch die Update-Erkennungs-Kette ersetzt
- defensives Guard-Muster: Zeile 58 if (typeof window.renderDashboard === 'function') window.renderDashboard();
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stub-Module + CSS-Stubs + Header-Platzhalter anlegen und zentral registrieren</name>
  <files>
    systems/migration/full-export.js,
    systems/migration/migration-wizard.js,
    systems/file-backup/file-backup-permissions.js,
    systems/file-backup/file-backup-manager.js,
    systems/file-backup/file-backup-ui.js,
    features/command-palette/action-registry.js,
    features/command-palette/command-palette.js,
    assets/styles/fonts.css,
    assets/styles/pwa.css,
    assets/styles/migration.css,
    assets/styles/file-backup.css,
    assets/styles/command-palette.css,
    assets/styles.css,
    assets/templates/header.html,
    loader.js,
    build.py
  </files>
  <read_first>
    - loader.js (komplette MODULES-Liste, Einfuegepunkte bei Zeile 49 und Zeile 85)
    - build.py (Modulliste Einfuegepunkte Zeile 75 und Zeile 114; css_files-Liste Zeilen 409-415; Modullisten-Sync-Check aus Phase 1)
    - assets/styles.css (@import-Hub — aktuelle @import-Reihenfolge)
    - assets/templates/header.html (.header-actions; .save-indicator als Anker fuer den Backup-Warnindikator)
    - .planning/phases/02-technik-fundament/02-PATTERNS.md (Abschnitt "loader.js and build.py Registration" — exakte Modul-Reihenfolge)
    - .planning/phases/02-technik-fundament/02-UI-SPEC.md (Komponente 2 Install-Button, Komponente 7 Header-Warnindikator — Klassen/Texte)
    - CLAUDE.md (Non-ESM-Regeln: keine const X = window.X in Funktionen, keine doppelten Top-Level-Funktionsnamen, Modullisten doppelt pflegen; Pitfall 8 @font-face nur in EINER Datei)
  </read_first>
  <action>
    Lege sieben JS-Stub-Dateien an. Jede enthaelt NUR einen Kommentar-Header (// [SECTION:MODULNAME]) und — bei den drei Modulen mit Init-Hook — eine leere no-op-Funktion plus window-Export:
    - systems/file-backup/file-backup-manager.js: function initFileBackup() {} und window.initFileBackup = initFileBackup;
    - systems/migration/migration-wizard.js: function initMigrationWizardIfNeeded() {} und window.initMigrationWizardIfNeeded = initMigrationWizardIfNeeded;
    - features/command-palette/command-palette.js: function initCommandPalette() {} und window.initCommandPalette = initCommandPalette;
    - full-export.js, file-backup-permissions.js, file-backup-ui.js, action-registry.js: nur Kommentar-Header (Welle 2 fuellt Inhalt).
    Lege fuenf leere CSS-Stub-Dateien an (fonts.css, pwa.css, migration.css, file-backup.css, command-palette.css) mit je einem Kommentar-Header.
    Trage alle sieben JS-Pfade in loader.js (MODULES) UND build.py (Modulliste) in der exakten Reihenfolge aus 02-PATTERNS.md ein (5 systems-Module nach campaign-manager, 2 command-palette-Module nach dmscreen-render). Beide Listen MUESSEN identisch sein (Sync-Check).
    Registriere die fuenf CSS-Dateien: in build.py css_files 'fonts.css' als erste Datei (vor 'variables.css'), die vier Feature-CSS am Ende (nach 'tools.css'); in assets/styles.css je ein @import in derselben Reihenfolge.
    Ergaenze in header.html innerhalb .header-actions: einen Button mit class="pwa-install-btn" data-action="install-pwa" style="display:none" (Text "App installieren") und — direkt nach dem bestehenden Save-Indikator — ein Element class="backup-warning-indicator" style="display:none" (Inhalt-Platzhalter). Keine Logik, nur die versteckten Andockpunkte.
  </action>
  <verify>
    <automated>cd "D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular" && python build.py 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - loader.js enthaelt 'features/command-palette/command-palette.js'
    - build.py enthaelt 'features/command-palette/command-palette.js' und 'command-palette.css' und 'fonts.css'
    - assets/styles.css enthaelt @import fuer 'command-palette.css' und 'fonts.css'
    - python build.py (dev) exit code 0 — Build bricht NICHT mit "has already been declared" oder "Modullisten nicht synchron" ab
    - systems/file-backup/file-backup-manager.js enthaelt window.initFileBackup = initFileBackup
    - systems/migration/migration-wizard.js enthaelt window.initMigrationWizardIfNeeded = initMigrationWizardIfNeeded
    - features/command-palette/command-palette.js enthaelt window.initCommandPalette = initCommandPalette
    - assets/templates/header.html enthaelt class="pwa-install-btn" und class="backup-warning-indicator" (beide display none)
    - die fuenf CSS-Dateien existieren unter assets/styles/
  </acceptance_criteria>
  <done>Sieben JS-Stubs + fuenf CSS-Stubs existieren und sind in loader.js/build.py/styles.css registriert; header.html hat beide versteckten Platzhalter; der Build laeuft fehlerfrei durch.</done>
</task>

<task type="auto">
  <name>Task 2: init.js verdrahten — defensive Init-Aufrufe + SW-Update-Erkennung</name>
  <files>core/init.js</files>
  <read_first>
    - core/init.js (init()-Block Zeilen 111-121; registerServiceWorker() Zeilen 173-210; defensives Guard-Muster Zeile 58)
    - .planning/phases/02-technik-fundament/02-RESEARCH.md (Code Example "Vollstaendiger SW-Update-Flow" Zeilen 540-588 — updatefound/statechange-Kette, showSWUpdateHint mit sessionStorage-Guard)
    - .planning/phases/02-technik-fundament/02-PATTERNS.md (Abschnitt core/init.js — exakte Einfuegepunkte)
  </read_first>
  <action>
    Ergaenze im init()-Aufrufblock (nach initPWA(), Zeile 121) drei defensive Init-Aufrufe nach dem Guard-Muster aus Zeile 58:
    if (typeof window.initFileBackup === 'function') window.initFileBackup();
    if (typeof window.initMigrationWizardIfNeeded === 'function') window.initMigrationWizardIfNeeded();
    if (typeof window.initCommandPalette === 'function') window.initCommandPalette();
    Ersetze in registerServiceWorker() den inneren erfolgreichen Registrierungs-Callback (navigator.serviceWorker.register('./sw.js').then(reg => log('[SW] Registriert:', reg.scope))) durch die vollstaendige Update-Erkennungs-Kette aus RESEARCH.md: nach erfolgreicher Registrierung if (reg.waiting) showSWUpdateHintGuarded(reg.waiting); dann reg.addEventListener('updatefound', ...) mit installing-statechange-Listener, der bei state === 'installed' && navigator.serviceWorker.controller ebenfalls showSWUpdateHintGuarded(newSW) aufruft. Die aeussere HEAD-fetch-Huelle (Zeilen 196-209) bleibt unveraendert. Definiere eine lokale Hilfsfunktion showSWUpdateHintGuarded(newSW), die window.showSWUpdateHint defensiv aufruft (if (typeof window.showSWUpdateHint === 'function') window.showSWUpdateHint(newSW);) — die echte UI liefert Plan 02 in pwa-install.js. KEIN self.skipWaiting()-Aequivalent erzwingen, kein automatischer reload (D-03). Nutze log(...) statt console.log.
  </action>
  <verify>
    <automated>cd "D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular" && node --check core/init.js && echo "SYNTAX_OK"</automated>
  </verify>
  <acceptance_criteria>
    - node --check core/init.js exit code 0
    - core/init.js enthaelt if (typeof window.initFileBackup === 'function') window.initFileBackup();
    - core/init.js enthaelt if (typeof window.initMigrationWizardIfNeeded === 'function') window.initMigrationWizardIfNeeded();
    - core/init.js enthaelt if (typeof window.initCommandPalette === 'function') window.initCommandPalette();
    - registerServiceWorker() enthaelt addEventListener('updatefound' und reg.waiting
    - core/init.js enthaelt showSWUpdateHint (defensiver Aufruf)
    - core/init.js enthaelt KEIN self.skipWaiting und kein automatisches window.location.reload() in registerServiceWorker
    - die file:-Fruehausstieg-Logik (Zeilen 177-182, protocol === 'file:') bleibt unveraendert vorhanden
  </acceptance_criteria>
  <done>init() ruft die drei neuen Module defensiv auf; registerServiceWorker erkennt wartende und neu installierte SW-Versionen und delegiert den Hinweis an window.showSWUpdateHint, ohne erzwungenen Reload.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave-0-Test-Geruest erstellen (3 Unit + 2 E2E)</name>
  <files>
    tests/unit/full-export.test.js,
    tests/unit/file-backup.test.js,
    tests/unit/action-registry.test.js,
    tests/e2e/features/pwa.spec.js,
    tests/e2e/features/command-palette.spec.js
  </files>
  <read_first>
    - tests/unit/migration.test.js (vm-basiertes Lademuster: fs.readFileSync + vm.runInContext fuer Non-ESM-Module — Vorlage fuer die neuen Unit-Tests)
    - tests/e2e/smoke.spec.js (Smoke-Test-Struktur, SMOKE_BASE_URL-Muster)
    - tests/e2e/features/persistence.spec.js (Playwright-Feature-Test-Muster: page.goto, Selektoren, expect)
    - jest.config.cjs (Test-Match-Pattern, damit die neuen *.test.js erkannt werden)
    - .planning/phases/02-technik-fundament/02-RESEARCH.md (Abschnitt "Phase Requirements -> Test Map" und "Wave 0 Gaps" — definiert pro Req welcher Test welches Verhalten prueft)
  </read_first>
  <behavior>
    - full-export.test.js: Test "buildFullExport enthaelt alle Kampagnen + Settings + diceFavorites + dmScreenProfiles" und "Voll-Export enthaelt KEINE SRD-Spells (D.spells nicht im Output)". RED-Phase mit echter Erwartung.
    - file-backup.test.js: Test "writeBackupForCampaign schreibt aktuelle Datei nach save()" und "pruneOldSnapshots behaelt max 10 Snapshots pro Kampagne (D-12)" mit gemockter File System Access API (window.showDirectoryPicker / dirHandle.getFileHandle / createWritable als Jest-Mocks).
    - action-registry.test.js: Test "searchActions('Neuer NPC') liefert die new-npc-Aktion als Top-Treffer" und "searchActions('8d6') findet die Wuerfel-Formel-Aktion" gegen die Fuzzy-Suche.
    - pwa.spec.js: E2E "manifest.webmanifest ist erreichbar und hat name + icons (192 + 512)" und "Service Worker registriert sich auf https/localhost".
    - command-palette.spec.js: E2E "Strg+Shift+K oeffnet die Command Palette (.cp-overlay sichtbar)" und "Tippen von 'NPC' zeigt mindestens ein Ergebnis".
  </behavior>
  <action>
    Erstelle die drei Unit-Test-Dateien nach dem vm-basierten Lademuster aus tests/unit/migration.test.js (kein ESM-Import; Modul-Quelltext via fs einlesen und in einem vm-Kontext mit Mock-Globals ausfuehren). Erstelle die zwei E2E-Specs unter tests/e2e/features/ nach dem Muster aus persistence.spec.js. Jede Datei enthaelt mindestens zwei test(...)/it(...)-Bloecke entsprechend dem behavior-Abschnitt. Da die Implementierungen (Plan 03/04/05) noch fehlen, sind die Tests in der RED-Phase: nutze echte Assertions, die nach Implementierung gruen werden (NICHT test.skip — die Tests muessen real existieren und ausfuehrbar sein). Wo eine Funktion noch nicht existiert, pruefe defensiv (typeof buildFullExport === 'function') und lasse den Test bei Fehlen bewusst fehlschlagen mit aussagekraeftiger Message, damit Welle 2 ihn gruen zieht. Verwende fuer File-System-Mocks Jest jest.fn() fuer getFileHandle, createWritable, write, close, removeEntry, entries. Stelle sicher, dass die neuen Dateien vom Test-Match-Pattern in jest.config.cjs erfasst werden.
  </action>
  <verify>
    <automated>cd "D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular" && npx jest tests/unit/full-export.test.js tests/unit/file-backup.test.js tests/unit/action-registry.test.js 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/full-export.test.js existiert und enthaelt describe und mindestens zwei test(/it(-Bloecke
    - tests/unit/file-backup.test.js existiert und enthaelt jest.fn( (gemockte FS-API) und pruneOldSnapshots oder 10 (Snapshot-Limit)
    - tests/unit/action-registry.test.js existiert und referenziert searchActions
    - tests/e2e/features/pwa.spec.js existiert und enthaelt manifest.webmanifest
    - tests/e2e/features/command-palette.spec.js existiert und enthaelt cp-overlay oder Shift+K
    - npx jest (die drei Unit-Dateien) LAEUFT (Jest startet, sammelt die Suiten ein) — rote Tests sind in dieser RED-Phase erwartet und KEIN Blocker; Jest darf NICHT mit "No tests found" abbrechen
  </acceptance_criteria>
  <done>Alle sechs Wave-0-Testdateien existieren, sind real (keine leeren skips), werden von Jest/Playwright eingesammelt und definieren die Erwartungen, die Welle 2 gruen zieht.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-Pipeline -> dist | Stub-Module/CSS werden konkateniert; fehlerhafte Stubs koennten den gesamten Build brechen (Boot-Crash-Klasse aus Phase 1) |
| init.js -> Fremdmodule | init() ruft Funktionen auf, die erst Welle 2 liefert — fehlende Guards wuerden Laufzeit-Crash verursachen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service | Stub-Module brechen Build (Duplikat-Deklaration / verwaister Funktionskoerper) | mitigate | Jeder Stub enthaelt genau einen eindeutigen Top-Level-Funktionsnamen; python build.py als Verify-Gate in Task 1; keine const X = window.X (CLAUDE.md-Dedup-Regeln) |
| T-02-02 | Denial of Service | init() ruft noch nicht existierende Module -> ReferenceError beim Boot | mitigate | Ausschliesslich defensive typeof window.X === 'function'-Guards (Muster init.js:58); node --check als Verify in Task 2 |
| T-02-03 | Tampering | SW-Update-Erkennung erzwingt Reload mitten in Session (D-03-Verstoss, Datenverlust-Risiko) | mitigate | registerServiceWorker delegiert nur an window.showSWUpdateHint; KEIN automatischer reload(); acceptance_criteria prueft Abwesenheit von skipWaiting/reload |
| T-02-SC | Tampering | npm/pip/cargo-Installs in diesem Plan | accept | Keine Paket-Installs in dieser Phase (browser-native APIs + GitHub Actions; RESEARCH.md "keine Drittbibliotheken"). Package-Legitimacy-Gate nicht anwendbar |
</threat_model>

<verification>
- python build.py (dev) und python build.py --production laufen fehlerfrei durch (Modul- + CSS-Listen synchron, keine Duplikat-/Boot-Crashes)
- node --check core/init.js bestaetigt syntaktische Korrektheit
- npx jest (die drei neuen Unit-Dateien) wird von Jest eingesammelt
- npm run check (ESLint + TypeScript + Prettier) bleibt gruen (kein neuer Error eingefuehrt)
</verification>

<success_criteria>
- 7 neue Module + 5 CSS-Dateien existieren als build-sichere Stubs und sind in loader.js/build.py/styles.css registriert
- header.html hat die versteckten Andock-Platzhalter (.pwa-install-btn, .backup-warning-indicator)
- init() ruft die 3 Welle-2-Module defensiv auf; SW-Update-Erkennung ist verdrahtet und D-03-konform
- 6 Wave-0-Testdateien existieren und sind ausfuehrbar
- Welle-2-Plaene (02-05) koennen parallel starten, ohne geteilte Dateien zu beruehren
</success_criteria>

<output>
Create `.planning/phases/02-technik-fundament/02-01-SUMMARY.md` when done
</output>
