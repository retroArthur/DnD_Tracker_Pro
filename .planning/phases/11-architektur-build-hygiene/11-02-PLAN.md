---
phase: 11-architektur-build-hygiene
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - loader.js
  - build.py
  - tests/build/test_build_deduplication.py
autonomous: true
requirements: [ARCH-01]

must_haves:
  truths:
    - "Der Dev-Modus (index.html + loader.js) laedt dasselbe Template-Set wie das Bundle — inklusive view-bestiary.html (Live-Drift-Bugfix zu D-04)"
    - "Die Template-Reihenfolge des Bundles stammt zur Build-Zeit aus dem TEMPLATES-Array in loader.js (D-04)"
    - "Die CSS-Reihenfolge des Bundles stammt zur Build-Zeit aus dem @import-Hub in assets/styles.css (D-04)"
    - "Eine gelistete, aber fehlende CSS-Datei oder HTML-Template-Datei bricht `python build.py` mit Exit-Code != 0 ab (D-02)"
    - "build.py enthaelt keine hartkodierte CSS- oder Template-Liste mehr — alle drei Listen haben genau eine Quelle (D-01/D-04)"
  artifacts:
    - path: "loader.js"
      provides: "TEMPLATES-Array als Quelle der Wahrheit fuer die Template-Reihenfolge, Bestiary-Eintrag ergaenzt"
      contains: "assets/templates/view-bestiary.html"
    - path: "build.py"
      provides: "SSoT-Parser fuer Template-Liste und CSS-@import-Reihenfolge"
      contains: "def load_css_import_order("
    - path: "tests/build/test_build_deduplication.py"
      provides: "pytest-Abdeckung fuer Template-/CSS-SSoT und die zugehoerigen Hard-Fail-Zweige"
      contains: "def test_ssot_template_list_parses_from_loader"
  key_links:
    - from: "build.py"
      to: "assets/styles.css"
      via: "load_css_import_order() liest die @import-Reihenfolge zur Build-Zeit"
      pattern: "load_css_import_order\\("
    - from: "build.py"
      to: "loader.js"
      via: "load_template_list() liest das TEMPLATES-Array zur Build-Zeit"
      pattern: "load_template_list\\("
---

<objective>
Ausweitung der in Plan 11-01 bewiesenen SSoT-Kette auf die beiden verbleibenden Listen (D-04):
Template-Liste aus `loader.js` und CSS-Reihenfolge aus dem `@import`-Hub in `assets/styles.css`.
Zusaetzlich wird der beim Recherche-Lauf entdeckte, seit Phase 3 unbemerkte Live-Drift aktiv
korrigiert: `loader.js` fehlt `assets/templates/view-bestiary.html`.

Purpose: Ein Template, das nur in einer der beiden Listen landet, saehe im Dev-Modus korrekt aus
und fehlte im Bundle (oder umgekehrt) — genau die Fehlerklasse, die D-04 schliesst. Ohne die
aktive Korrektur wuerde die Umstellung auf loader.js als Quelle der Wahrheit das
Bestiary-Template still aus dem Bundle entfernen.

Output: `load_template_list()` und `load_css_import_order()` in `build.py`; die hartkodierten
`css_files`- und `html_templates`-Listen in `build()` sind entfernt; `loader.js` fuehrt 12 statt
11 Templates; vier neue pytest-Tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-architektur-build-hygiene/11-CONTEXT.md
@.planning/phases/11-architektur-build-hygiene/11-PATTERNS.md
@.planning/phases/11-architektur-build-hygiene/11-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live-Drift beheben — view-bestiary.html in loader.js ergaenzen</name>
  <files>loader.js</files>
  <read_first>
    - `loader.js` Zeilen 211-242 (`loadModules()`, das funktionslokale `TEMPLATES`-Array mit 11 Eintraegen, `Promise.all`-Fetch)
    - `build.py` Zeilen 481-496 (`html_templates`-Liste mit 12 Eintraegen — die Referenzreihenfolge)
    - `.planning/phases/11-architektur-build-hygiene/11-RESEARCH.md` §„Live-Bug-Fund" (Nachweis, Position, praktische Konsequenz)
  </read_first>
  <action>
In `loader.js` im `TEMPLATES`-Array (heute Zeilen 219-231) den Eintrag
`'assets/templates/view-bestiary.html',` ergaenzen — Position: unmittelbar nach
`'assets/templates/view-encounters.html',` und vor `'assets/templates/view-resources.html',`,
also exakt an der Stelle, an der `build.py:485` ihn heute fuehrt. Damit hat das Array 12 statt 11
Eintraege und deckt sich in Inhalt UND Reihenfolge mit der Bundle-Liste.

Hintergrund fuer die Korrektheitspruefung: die Datei `assets/templates/view-bestiary.html`
existiert; die E2E-Tests in `tests/e2e/features/bestiary.spec.js` sprechen `#view-bestiary` an,
laufen aber ausschliesslich gegen `dist/dnd-tracker-bundled.html` — deshalb ist der Drift seit
Phase 3 unentdeckt geblieben. Nach dieser Korrektur fuegt auch der Dev-Modus den
`#view-bestiary`-Container ins DOM ein.

Keine weitere Aenderung an `loader.js` in diesem Task — insbesondere bleibt `MODULES` unangetastet
(es ist bereits 123/123 inhalts- und reihenfolgegleich).
  </action>
  <verify>
    <automated>grep -c "assets/templates/view-bestiary.html" loader.js && python -c "import re,sys; s=open('loader.js',encoding='utf-8').read(); m=re.search(r'const TEMPLATES\s*=\s*\[(.*?)\];', s, re.DOTALL); t=re.findall(r\"'([^']+)'\", m.group(1)); print(len(t)); sys.exit(0 if len(t)==12 and t[4]=='assets/templates/view-bestiary.html' else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "assets/templates/view-bestiary.html" loader.js` liefert `1`.
    - Das aus `loader.js` geparste `TEMPLATES`-Array hat 12 Eintraege; Index 4 (0-basiert) ist `assets/templates/view-bestiary.html` — belegt durch das Python-Kommando im `<verify>`-Block, Exit-Code 0.
    - Die aus `loader.js` geparste Template-Liste ist elementweise und reihenfolgegleich zu `html_templates` in `build.py` (vor der Umstellung in Task 2 noch vergleichbar).
    - `PYTHONIOENCODING=utf-8 python build.py` endet weiterhin mit Exit-Code 0 (dieser Task allein aendert das Bundle nicht).
  </acceptance_criteria>
  <done>`loader.js` fuehrt dieselben 12 Templates in derselben Reihenfolge wie `build.py`; der Dev-Modus laedt den Bestiary-Container.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Template-Liste auf SSoT umstellen und Hard-Fail verdrahten</name>
  <files>build.py, tests/build/test_build_deduplication.py</files>
  <read_first>
    - `build.py` in der nach Plan 11-01 geaenderten Fassung: `parse_js_string_array`, `load_module_list`, `require_files_exist` und der neue Beschaffungsblock am Anfang von `build()`
    - `build.py` Zeilen 481-496 (Template-Ladeblock: `html_templates`-Liste, Ladeschleife ohne jede Existenzpruefung — `read_file()` wuerde heute mit rohem `FileNotFoundError`-Traceback abbrechen)
    - `loader.js` Zeilen 211-232 nach Task 1 (12 Eintraege; `TEMPLATES` steht funktionslokal in `loadModules()`, nicht auf Modulebene wie `MODULES`)
    - `tests/build/test_build_deduplication.py` (Import-Block und die in Plan 11-01 ergaenzten SSoT-Tests als Muster)
  </read_first>
  <behavior>
    - `test_ssot_template_list_parses_from_loader`: `load_template_list()` gegen die echte `loader.js` liefert genau 12 Pfade in Datei-Reihenfolge; jeder Pfad existiert relativ zur Projektwurzel.
    - `test_missing_template_file_aborts_build`: fehlt eine gelistete Template-Datei, endet der Build mit `SystemExit` und schreibt keine Ausgabedatei.
  </behavior>
  <action>
`load_template_list(loader_path)` in `build.py` ergaenzen, direkt neben `load_module_list`. Sie
liest `loader.js` und gibt `parse_js_string_array(content, 'TEMPLATES', loader_path)` zurueck. Der
regexbasierte Anker findet das Array trotz seiner funktionslokalen Deklaration, weil der Parser auf
Text und nicht auf JS-Scope arbeitet — diesen Umstand als kurzen Docstring-Satz in
`load_template_list` festhalten, damit ein spaeterer Umbau auf einen strengeren Tokenizer die
Annahme sieht.

Wichtig fuer die Pfad-Semantik: `TEMPLATES` in `loader.js` fuehrt vollstaendige Pfade mit dem
Praefix `assets/templates/`, die heutige `html_templates`-Liste in `build.py` dagegen nur
Dateinamen. `load_template_list` liefert die vollstaendigen Pfade; die Ladeschleife (heute
build.py:490-494) baut den Pfad entsprechend aus `SOURCE_DIR` plus geparstem Pfad, ohne
`assets/templates/` erneut anzuhaengen.

Verdrahtung in `build()`:
- Die hartkodierte `html_templates`-Liste (build.py:483-489) ersatzlos loeschen.
- Im Beschaffungsblock am Anfang von `build()` (dort, wo Plan 11-01 `load_module_list` verdrahtet
  hat) `templates = load_template_list(loader_js_path)` ergaenzen, gefolgt von
  `require_files_exist(SOURCE_DIR, templates, 'HTML-Template')` — damit ist D-02 auch fuer
  Templates erfuellt und der heutige rohe `FileNotFoundError`-Traceback durch eine deutsche
  `[FEHLER]`-Meldung mit anschliessendem `sys.exit(1)` ersetzt.
- Die Template-Ladeschleife und ihre `log.success`-Zeile (build.py:490-495) auf die neue Variable
  umstellen; die Zaehlanzeige bleibt inhaltlich gleich.

Tests wie in `<behavior>` beschrieben ergaenzen. `test_missing_template_file_aborts_build` folgt dem
in Plan 11-01 etablierten `monkeypatch`-Muster: `load_template_list` auf eine Funktion setzen, die
die echte Liste plus einen nicht existierenden Pfad liefert, `build()` in `pytest.raises(SystemExit)`
aufrufen und danach pruefen, dass die Ausgabedatei unveraendert bzw. nicht vorhanden ist.
  </action>
  <verify>
    <automated>python -m pytest tests/build/ -v && PYTHONIOENCODING=utf-8 python build.py && python -m pytest tests/build/ -v</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/build/ -v` endet mit Exit-Code 0.
    - Quellzusicherung: `grep -c "def load_template_list(" build.py` liefert `1`.
    - Quellzusicherung: `grep -v '^\s*#' build.py | grep -c "html_templates = \["` liefert `0`.
    - Quellzusicherung: `grep -c "'view-encounters.html'" build.py` liefert `0` (kein hartkodierter Template-Dateiname mehr in `build.py`).
    - `PYTHONIOENCODING=utf-8 python build.py` endet mit Exit-Code 0; `grep -c 'id="view-bestiary"' dist/dnd-tracker-bundled.html` liefert mindestens `1` (Bestiary-Template weiterhin im Bundle).
    - `npx playwright test tests/e2e/features/bestiary.spec.js` endet mit Exit-Code 0.
  </acceptance_criteria>
  <done>Die Template-Liste hat genau eine Quelle; ein fehlendes Template bricht den Build kontrolliert ab; das Bestiary-Template ist unveraendert im Bundle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CSS-@import-Reihenfolge auf SSoT umstellen und Hard-Fail verdrahten</name>
  <files>build.py, tests/build/test_build_deduplication.py</files>
  <read_first>
    - `assets/styles.css` (vollstaendig, 24 Zeilen: 4 Kommentarzeilen plus 20 `@import url('styles/DATEI.css');`-Zeilen, ein Import pro Zeile, keine Kommentare dazwischen)
    - `build.py` Zeilen 448-479 (CSS-Ladeblock: `css_files`-Liste mit 20 Eintraegen, Ladeschleife mit `log.warning`-Weiterlauf bei fehlender Datei, die CR-08-`url()`-Umschreibung nach dem Zusammenfuegen)
    - `build.py` nach Task 2 (Beschaffungsblock am Anfang von `build()`)
    - `tests/build/test_build_deduplication.py` (Import-Block, bestehende SSoT-Tests)
  </read_first>
  <behavior>
    - `test_ssot_css_order_matches_styles_hub`: `load_css_import_order()` gegen die echte `assets/styles.css` liefert genau 20 Dateinamen in Datei-Reihenfolge, beginnend mit `fonts.css` und endend mit `welt.css`; jede Datei existiert unter `assets/styles/`.
    - `test_missing_css_file_aborts_build`: fehlt eine im Hub gelistete CSS-Datei, endet der Build mit `SystemExit` und schreibt keine Ausgabedatei.
  </behavior>
  <action>
`load_css_import_order(styles_css_path)` in `build.py` ergaenzen, neben den beiden loader-Parsern.
Sie liest `assets/styles.css` und extrahiert die Dateinamen per
`re.findall(r"@import url\('styles/([^']+)'\);", content)`. Wie bei den loader-Parsern gilt D-01:
kein Treffer bzw. leere Ergebnisliste -> `[FEHLER]`-Meldung mit Dateipfad und `sys.exit(1)`, nicht
`log.warning` mit Weiterlauf. Da `assets/styles.css` genau ein `@import` pro Zeile fuehrt und keine
Kommentare zwischen den Imports stehen, ist keine Kommentar-Vorbehandlung noetig — diese Annahme
als Docstring-Satz festhalten.

Verdrahtung in `build()`:
- Die hartkodierte `css_files`-Liste (build.py:450-460) ersatzlos loeschen.
- Im Beschaffungsblock `css_files = load_css_import_order(os.path.join(SOURCE_DIR, 'assets', 'styles.css'))`
  ergaenzen, gefolgt von einem Existenz-Gate. `require_files_exist` erwartet Pfade relativ zu einem
  Basisverzeichnis: entweder die Namen vorher auf `assets/styles/<name>` abbilden und
  `require_files_exist(SOURCE_DIR, …, 'CSS-Datei')` aufrufen, oder `require_files_exist` mit
  `os.path.join(SOURCE_DIR, 'assets', 'styles')` als Basis aufrufen. Eine der beiden Formen waehlen
  und konsistent halten.
- Den `else`-Zweig der CSS-Ladeschleife (build.py:467-468) entfernen — die Existenz ist vorher
  geprueft, die Schleife laedt nur noch.
- Die CR-08-`url()`-Umschreibung (build.py:475) und die Minifizierung bleiben unveraendert an ihrer
  Stelle; die Reihenfolge des Zusammenfuegens darf sich nicht aendern, sonst kippt die
  CSS-Kaskade.

Tests wie in `<behavior>` beschrieben ergaenzen; `test_missing_css_file_aborts_build` folgt
demselben `monkeypatch`-Muster wie die beiden Vorgaenger-Tests.

Verhaltensneutralitaets-Nachweis fuer diesen Task: die Zeichenzahl des CSS-Blocks im erzeugten
Bundle muss vor und nach der Umstellung identisch sein (die Build-Ausgabe nennt sie in der
`CSS geladen: … Zeichen`-Zeile) — vor der Aenderung notieren, danach vergleichen.
  </action>
  <verify>
    <automated>python -m pytest tests/build/ -v && PYTHONIOENCODING=utf-8 python build.py && PYTHONIOENCODING=utf-8 python build.py --production && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/build/ -v` endet mit Exit-Code 0.
    - Quellzusicherung: `grep -c "def load_css_import_order(" build.py` liefert `1`.
    - Quellzusicherung: `grep -v '^\s*#' build.py | grep -c "css_files = \["` liefert `0`.
    - Quellzusicherung: `grep -c "'dmscreen.css'" build.py` liefert `0` (kein hartkodierter CSS-Dateiname mehr in `build.py`).
    - `PYTHONIOENCODING=utf-8 python build.py` gibt in der Zeile `CSS geladen:` dieselbe Zeichenzahl aus wie vor der Umstellung (Wert vor der Aenderung protokollieren, danach vergleichen).
    - `PYTHONIOENCODING=utf-8 python build.py --production` endet mit Exit-Code 0 und `npm test` endet mit Exit-Code 0.
  </acceptance_criteria>
  <done>Alle drei Listen (Module, Templates, CSS-Reihenfolge) haben genau eine Quelle; jede gelistete, aber fehlende Datei bricht den Build ab.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `loader.js` / `assets/styles.css` -> `build.py` | Build-Zeit-Parser leiten aus zwei git-versionierten Projektdateien Dateipfade ab, die in das Bundle eingelesen werden |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-03 | Tampering | `load_template_list()` / `load_css_import_order()` in `build.py` | low | mitigate | Identische Absicherung wie T-11-01: jeder geparste Pfad wird vor dem Einlesen per `require_files_exist()` geprueft (D-02); die bestehende Post-Build-Validierung bleibt Backstop. Beide Quelldateien sind git-versionierte Projektdateien, kein externer Input. |
| T-11-04 | Tampering | Reihenfolge der CSS-Kaskade | low | mitigate | Die Reihenfolge stammt jetzt aus derselben Datei, die der Dev-Modus laedt; eine Umsortierung im Hub wirkt in beiden Modi gleich. Zusaetzlich Verhaltensnachweis ueber die identische CSS-Zeichenzahl im Bundle (Task-3-Akzeptanzkriterium). |
</threat_model>

## Artifacts this phase produces

**Neu in diesem Plan (`build.py`):** `load_template_list(loader_path)`,
`load_css_import_order(styles_css_path)`.
**Neu in diesem Plan (`tests/build/test_build_deduplication.py`):**
`test_ssot_template_list_parses_from_loader`, `test_missing_template_file_aborts_build`,
`test_ssot_css_order_matches_styles_hub`, `test_missing_css_file_aborts_build`.
**Geaendert in diesem Plan (`loader.js`):** `TEMPLATES` fuehrt 12 statt 11 Eintraege.
**Entfernt in diesem Plan:** die hartkodierten Listen `css_files` und `html_templates` in `build()`.

**Phasenweit (Referenz — vom Verifier NICHT als Drift zu werten):**
neu: `parse_js_string_array`, `load_module_list`, `require_files_exist`, `build_favicon_data_uri`
(build.py); `requirements-dev.txt`; `11-CONCERNS-TRIAGE.md`; `KNOWN_DEPRECATION_STRINGS` und der
Test „Keine Favicon-404 und keine Meta-Tag-Deprecation" (`tests/e2e/smoke.spec.js`);
`<link rel="icon">` und `<meta name="mobile-web-app-capable">` in beiden HTML-Koepfen; ein
pytest-Schritt im CI-`test`-Job; Backlog-Requirements `DEBT-01`..`DEBT-07`.
entfernt: `remove_duplicate_functions`, die `MODULES`-Kopie, `check_module_list_sync` (build.py);
`test_no_orphaned_return_statements`, `test_module_lists_are_synchronized`; die `favicon`-
Ausnahmefilter in `tests/e2e/smoke.spec.js`; ggf. `<meta name="apple-mobile-web-app-capable">`.

<verification>
- `python -m pytest tests/build/ -v` gruen
- `PYTHONIOENCODING=utf-8 python build.py` und `python build.py --production` je Exit-Code 0
- `npm test` gruen; `npx playwright test tests/e2e/features/bestiary.spec.js` gruen
- CSS-Zeichenzahl im Build-Log unveraendert gegenueber dem Stand vor Task 3
</verification>

<success_criteria>
- ARCH-01 ist fuer alle drei Listen erfuellt (D-01/D-04); `build.py` fuehrt keine eigene Kopie mehr
- D-02 gilt fuer JS-Module, CSS-Dateien und HTML-Templates gleichermassen
- Der Live-Drift (fehlendes Bestiary-Template in `loader.js`) ist aktiv behoben, nicht nur kuenftig verhindert
</success_criteria>

<output>
Create `.planning/phases/11-architektur-build-hygiene/11-02-SUMMARY.md` when done
</output>
