---
phase: 11-architektur-build-hygiene
plan: 06
type: execute
wave: 6
depends_on: ["11-05"]
files_modified:
  - .planning/phases/11-architektur-build-hygiene/11-CONCERNS-TRIAGE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [ARCH-04]

must_haves:
  truths:
    - "Jeder diskrete Eintrag aus .planning/codebase/CONCERNS.md hat in 11-CONCERNS-TRIAGE.md genau eine Disposition (D-13)"
    - "Jede Disposition traegt einen Beleg gegen den Live-Code — Datei:Zeile, Phase oder Commit — und nicht die Beschreibung aus CONCERNS.md (D-15)"
    - "Die Triage steht als eigenes Phasen-Artefakt fest, bevor der Map-Refresh CONCERNS.md ueberschreibt (D-13)"
    - "Jeder Restposten mit echter Codeaenderung steht als benanntes Requirement in .planning/REQUIREMENTS.md §v2 mit Rueckverweis auf seinen CONCERNS-Ursprung (D-16)"
    - "Phase 11 fixt keinen dieser Restposten aktiv — die Milestone-Leitplanke bleibt verhaltensneutral (D-16)"
  prohibitions:
    - statement: "MUST NOT einen CONCERNS-Eintrag als erledigt, obsolet oder akzeptiert dispositionieren, ohne einen ueberpruefbaren Beleg gegen den Live-Code anzugeben — eine Disposition, die sich allein auf die Beschreibung in CONCERNS.md stuetzt, ist unzulaessig"
      requirement_id: ARCH-04
      category: transparency
  artifacts:
    - path: ".planning/phases/11-architektur-build-hygiene/11-CONCERNS-TRIAGE.md"
      provides: "vollstaendige Triage-Historie aller CONCERNS.md-Eintraege mit Disposition und Live-Code-Beleg"
      contains: "| Disposition |"
    - path: ".planning/REQUIREMENTS.md"
      provides: "benannte Backlog-Requirements fuer die uebernommenen Restposten"
      contains: "DEBT-01"
  key_links:
    - from: ".planning/phases/11-architektur-build-hygiene/11-CONCERNS-TRIAGE.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Disposition 'uebernommen' nennt die vergebene DEBT-ID; das Requirement nennt den CONCERNS-Ursprung zurueck"
      pattern: "DEBT-0[0-9]"
---

<objective>
Erste Haelfte von ARCH-04: jeder Eintrag in `.planning/codebase/CONCERNS.md` wird dispositioniert und
das Ergebnis in einem eigenen Phasen-Artefakt festgehalten (D-13), jede Disposition mit einem Beleg
gegen den Live-Code (D-15), und die Restposten mit echter Codeaenderung werden als benannte
Requirements in den Backlog uebernommen statt gefixt (D-16).

Purpose: `/gsd-map-codebase` ueberschreibt `CONCERNS.md` vollstaendig — ohne vorherige Triage ginge
die Historie verloren, was erledigt, obsolet oder uebernommen wurde. Und CONCERNS.md ist
nachweislich stale: mehrere Eintraege waren beim Scouting bereits erledigt, ohne dass das Dokument
davon weiss. Deshalb erst triagieren, dann in Plan 11-07 regenerieren.

Output: `11-CONCERNS-TRIAGE.md` mit einer Tabellenzeile je Eintrag; die uebernommenen Posten als
`DEBT-`-Requirements in `.planning/REQUIREMENTS.md` §v2.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-architektur-build-hygiene/11-CONTEXT.md
@.planning/phases/11-architektur-build-hygiene/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Triage Teil 1 — Tech Debt, Known Bugs, Security Considerations, Performance Bottlenecks</name>
  <files>.planning/phases/11-architektur-build-hygiene/11-CONCERNS-TRIAGE.md</files>
  <read_first>
    - `.planning/codebase/CONCERNS.md` Zeilen 1-213 (die vier Abschnitte dieses Tasks; Stand 2026-06-11)
    - `.planning/phases/11-architektur-build-hygiene/11-CONTEXT.md` §D-13/D-15/D-16 und §Deferred (welche Posten ausdruecklich uebernommen statt gefixt werden)
    - `.planning/phases/10-security-h-rtung/10-CONTEXT.md` §D-08 (CSP und die class/style-Breite im Sanitizer sind bewusst akzeptierte, in SECURITY.md dokumentierte Risiken — die zugehoerigen CONCERNS-Eintraege sind „akzeptiert", nicht „offen")
    - `.planning/STATE.md` §„Open TODOs" (offene Befunde aus Phase 10: IN-01 markdown-converter, WR-02 doppeltes data-id, IN-02 un-escapte Regex-Capture, latente Toast-Race in locations/encounters)
  </read_first>
  <action>
`.planning/phases/11-architektur-build-hygiene/11-CONCERNS-TRIAGE.md` neu anlegen.

Kopf des Dokuments: Datum, Bezug auf D-13/D-15/D-16, die gezaehlte Gesamtzahl diskreter Eintraege in
`CONCERNS.md` (Recherche-Stand: 46 — vor der Verwendung selbst nachzaehlen und die eigene Zahl
eintragen; weicht sie ab, die Abweichung im Kopf begruenden) und eine Legende der vier zulaessigen
Dispositionswerte:
- `erledigt` — der beschriebene Zustand existiert im Live-Code nicht mehr
- `obsolet` — die Beschreibung traf nie zu oder der Gegenstand existiert nicht mehr
- `akzeptiert` — bewusst getragenes Risiko mit dokumentierter Entscheidung
- `uebernommen` — als benanntes Requirement in den Backlog verschoben (D-16), mit `DEBT-`-ID

Ein fuenfter Wert `offen` ist NICHT zulaessig: die Roadmap fordert fuer jeden Eintrag eine der drei
Endzustaende erledigt/obsolet/uebernommen; `akzeptiert` ist die in Phase 10 (D-08) etablierte
Variante von „obsolet, weil bewusst getragen".

Danach je Abschnitt von `CONCERNS.md` eine Markdown-Tabelle mit den Spalten
`| # | Eintrag (Kurztitel) | CONCERNS-Abschnitt | Disposition | Beleg (Live-Code) |`. In diesem Task
die vier Abschnitte „Tech Debt", „Known Bugs", „Security Considerations" und „Performance
Bottlenecks" abarbeiten.

**Belegregel (D-15, harte Anforderung):** Der Beleg muss gegen den Live-Code gefuehrt werden, nicht
gegen die Beschreibung in CONCERNS.md. Zulaessige Belegformen: `datei.js:ZEILE` mit dem tatsaechlich
dort stehenden Sachverhalt, eine Phasen-/Plan-Referenz (z. B. „Phase 9, 09-09-SUMMARY.md") oder ein
Commit-Hash. Unzulaessig: „laut CONCERNS.md bereits behoben" oder eine Disposition ohne Belegspalte.
Jede Zahl aus CONCERNS.md ist vor Verwendung gegen den Ist-Stand zu pruefen — das Dokument spricht
von 92 Modulen (heute rund 123), 26 E2E-Fails, 504 window-Imports und einer angeblich fehlenden
E2E-Suite in CI; diese Zahlen sind als Fakten unbrauchbar.

**Seed-Wissen aus der Recherche** (jeweils selbst gegenpruefen, nicht ungeprueft uebernehmen — die
Recherche hat diese Punkte stichprobenartig als bereits erledigt gefunden, ohne dass CONCERNS.md es
weiss): die Service-Worker-Cache-Liste arbeitet heute mit `CORE_ASSETS`/`OPTIONAL_ASSETS` in
`sw.js`; `main.js`, `tsconfig.json.backup` und `MIGRATION_REPORT.md` sind geloescht; die
Lizenz-Diskrepanz ist behoben; `validate.py` loest seinen Pfad ueber `Path(__file__).parent` auf;
`tests/unit/sanitizer-parity.test.js` existiert; „kein automatisierter dist-Smoke-Test" trifft nicht
mehr zu (der `smoke-test`-Job faehrt seit Phase 2 das Production-Bundle im Browser); die 26
E2E-Fails sind Phase 8; die execCommand-Stellen im Editor sind Phase 9; CSP und die
class/style-Breite im Sanitizer sind Phase 10 D-08 („akzeptiert").

**Eintraege, die dieser Task nicht selbst fixt:** alles, was eine echte Codeaenderung braucht, wird
`uebernommen` dispositioniert und bekommt in Task 3 eine `DEBT-`-ID; die ID wird hier bereits in der
Dispositionsspalte notiert, damit Triage und Requirements aufeinander zeigen. Die enge Ausnahme aus
D-16 gilt nur fuer Posten, die durch die Plaene 11-01 bis 11-05 im Build-, CI- oder Doku-Bereich
ohnehin angefasst wurden — die werden `erledigt` mit Verweis auf den jeweiligen Plan.
  </action>
  <verify>
    <automated>python -c "import re,sys; t=open('.planning/phases/11-architektur-build-hygiene/11-CONCERNS-TRIAGE.md',encoding='utf-8').read(); rows=[l for l in t.splitlines() if l.startswith('|') and not set(l.replace('|','').strip())<=set('-: ') and 'Disposition' not in l]; bad=[l for l in rows if not re.search(r'\|\s*(erledigt|obsolet|akzeptiert|uebernommen)', l) or len(l.rsplit('|',2)[1].strip())<3]; print('rows=',len(rows),'bad=',len(bad)); [print(b) for b in bad[:5]]; sys.exit(0 if rows and not bad else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - Die Datei `.planning/phases/11-architektur-build-hygiene/11-CONCERNS-TRIAGE.md` existiert und traegt im Kopf die selbst gezaehlte Gesamtzahl diskreter CONCERNS-Eintraege.
    - Jede Tabellenzeile traegt genau einen der vier Dispositionswerte und eine nicht-leere Belegspalte — belegt durch das Kommando im `<verify>`-Block, Exit-Code 0.
    - `grep -c "laut CONCERNS" .planning/phases/11-architektur-build-hygiene/11-CONCERNS-TRIAGE.md` liefert `0` (kein Beleg stuetzt sich auf die Beschreibung im Ursprungsdokument).
    - Die vier Abschnitte „Tech Debt", „Known Bugs", „Security Considerations" und „Performance Bottlenecks" aus `CONCERNS.md` sind je als eigene Tabelle vertreten; die Zeilenzahl je Tabelle entspricht der Zahl diskreter Eintraege im jeweiligen Abschnitt.
    - Mindestens eine Zeile traegt die Disposition `akzeptiert` mit Verweis auf Phase 10 D-08 (CSP bzw. class/style-Breite im Sanitizer).
    - Kein Produktionscode wurde in diesem Task geaendert: `git status --porcelain` listet ausschliesslich Dateien unter `.planning/`.
  </acceptance_criteria>
  <done>Die ersten vier CONCERNS-Abschnitte sind vollstaendig dispositioniert, jede Zeile mit Live-Code-Beleg.</done>
</task>

<task type="auto">
  <name>Task 2: Triage Teil 2 — Fragile Areas, Scaling Limits, Dependencies at Risk, Missing Critical Features, Test Coverage Gaps</name>
  <files>.planning/phases/11-architektur-build-hygiene/11-CONCERNS-TRIAGE.md</files>
  <read_first>
    - `.planning/codebase/CONCERNS.md` Zeilen 214-351 (die fuenf Abschnitte dieses Tasks)
    - `.planning/phases/11-architektur-build-hygiene/11-CONCERNS-TRIAGE.md` in der nach Task 1 vorhandenen Fassung (Kopf, Legende, Tabellenformat)
    - `.planning/STATE.md` §„Open TODOs" (aktuelle Testzahlen: Jest 621/621, Playwright 318 passed/2 skipped nach Phase 10; die latente Toast-Race in `tests/e2e/crud/locations.spec.js` und `encounters.spec.js` als bekannter, noch offener Posten)
    - `package.json` und `.github/workflows/ci.yml` (fuer die Abschnitte „Dependencies at Risk" und „Test Coverage Gaps" — Ist-Stand statt CONCERNS-Beschreibung)
  </read_first>
  <action>
`11-CONCERNS-TRIAGE.md` um die fuenf verbleibenden Abschnitte ergaenzen, im identischen
Tabellenformat und unter derselben Belegregel wie Task 1.

Besondere Aufmerksamkeit fuer diese Abschnitte:
- „Test Coverage Gaps": jede dort genannte Luecke gegen den heutigen Suite-Stand pruefen. Phase 8,
  9 und 10 haben die Suite erheblich veraendert (Editor-Regressionsnetz, Sanitizer-Beweisnetz,
  Paritaetstest); mehrere Luecken duerften `erledigt` sein. Die Zahl der Tests NICHT aus CONCERNS.md
  uebernehmen, sondern durch einen echten Lauf ermitteln.
- „Dependencies at Risk": gegen `package.json` und `.github/workflows/ci.yml` im Stand NACH Plan
  11-04 pruefen — die Action-Majors und die Node-Version wurden dort gerade gehoben.
- „Fragile Areas": hier liegen die typischen D-16-Kandidaten (ungeschuetztes `setInterval`,
  Tab-Registry-String-Referenzen, oversized modules). Sie werden `uebernommen` dispositioniert mit
  der in Task 3 vergebenen `DEBT-`-ID.
- „Missing Critical Features": Posten, die im v1.1-Milestone bewusst nicht adressiert werden, sind
  `uebernommen` (Backlog) oder `obsolet` — nicht `erledigt`.

Zum Abschluss einen Ergebnis-Abschnitt „Zusammenfassung" ans Dokumentende setzen: Anzahl je
Disposition, die Liste der vergebenen `DEBT-`-IDs, und ein Satz zur Vollstaendigkeit
(Gesamtzahl der Tabellenzeilen == im Kopf genannte Gesamtzahl der CONCERNS-Eintraege).
  </action>
  <verify>
    <automated>python -c "import re,sys; t=open('.planning/phases/11-architektur-build-hygiene/11-CONCERNS-TRIAGE.md',encoding='utf-8').read(); rows=[l for l in t.splitlines() if l.startswith('|') and not set(l.replace('|','').strip())<=set('-: ') and 'Disposition' not in l]; bad=[l for l in rows if not re.search(r'\|\s*(erledigt|obsolet|akzeptiert|uebernommen)', l) or len(l.rsplit('|',2)[1].strip())<3]; print('rows=',len(rows),'bad=',len(bad)); sys.exit(0 if len(rows)>=40 and not bad else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - Alle neun Abschnitte aus `CONCERNS.md` sind in `11-CONCERNS-TRIAGE.md` als eigene Tabelle vertreten.
    - Die Gesamtzahl der Tabellenzeilen entspricht der im Dokumentkopf genannten Gesamtzahl diskreter CONCERNS-Eintraege (Recherche-Erwartung: 46) — belegt durch den Abschnitt „Zusammenfassung" und das Kommando im `<verify>`-Block, Exit-Code 0.
    - Jede Zeile traegt genau einen der vier Dispositionswerte und eine nicht-leere Belegspalte.
    - Der Abschnitt „Zusammenfassung" nennt die Anzahl je Disposition und listet alle vergebenen `DEBT-`-IDs.
    - `grep -c "TODO\|TBD\|offen" .planning/phases/11-architektur-build-hygiene/11-CONCERNS-TRIAGE.md` liefert `0` in der Dispositionsspalte (keine unentschiedene Zeile). Falls das Wort in einem Belegtext vorkommt, per Zeilenpruefung nachweisen, dass keine Dispositionsspalte betroffen ist.
    - Kein Produktionscode wurde in diesem Task geaendert: `git status --porcelain` listet ausschliesslich Dateien unter `.planning/`.
  </acceptance_criteria>
  <done>Die Triage ist vollstaendig: jeder CONCERNS-Eintrag hat eine Disposition mit Live-Code-Beleg, die Zusammenfassung beweist die Vollstaendigkeit.</done>
</task>

<task type="auto">
  <name>Task 3: Uebernommene Restposten als benannte Backlog-Requirements (D-16)</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/11-architektur-build-hygiene/11-CONCERNS-TRIAGE.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` vollstaendig (Struktur: §v1.1 Requirements nach Themen, §v2 Requirements mit dem bestehenden Eintrag SOUND-PT-01, §Out of Scope, §Traceability)
    - `.planning/phases/11-architektur-build-hygiene/11-CONCERNS-TRIAGE.md` nach Task 2 (die Zeilen mit Disposition `uebernommen` und ihren `DEBT-`-IDs)
    - `.planning/phases/11-architektur-build-hygiene/11-CONTEXT.md` §D-16 und §Deferred (die namentlich genannten Posten)
    - `.planning/STATE.md` §„Open TODOs" (die noch offenen Phase-10-Zusatzbefunde IN-01/WR-02/IN-02 und die latente Toast-Race)
  </read_first>
  <action>
Die in der Triage mit `uebernommen` dispositionierten Posten als benannte Requirements in
`.planning/REQUIREMENTS.md` §v2 Requirements eintragen — mit `Edit`, nicht mit einem Voll-Neuschreiben
der Datei; ausserhalb des §v2-Abschnitts darf sich nichts aendern.

Format je Eintrag, konsistent mit dem bestehenden `SOUND-PT-01`:
`- **DEBT-NN**: <praezise Beschreibung des Zustands> — Ursprung: CONCERNS.md §<Abschnitt>, triagiert
in 11-CONCERNS-TRIAGE.md` plus, wo bekannt, die konkrete Fundstelle `datei.js:ZEILE`.

Ein neuer Unterabschnitt `### Technische Schulden (aus Phase-11-Triage)` unter §v2 nimmt diese
Eintraege auf, damit sie nicht mit dem Feature-Backlog vermischt werden.

Die aus D-16 und §Deferred namentlich vorgemerkten Posten sind die Startmenge — jeder davon
bekommt eine ID, sofern die Triage ihn nicht als `erledigt` oder `obsolet` ausgewiesen hat:
- Export-Versionsstempel, der eine Version hart kodiert
- ungeschuetztes `setInterval` in `systems/backups.js` (Fundstelle vor dem Eintragen gegen den
  Live-Code pruefen — die Zeilenangabe aus CONCERNS.md ist nicht vertrauenswuerdig)
- Undo-Snapshot-Performance
- oversized modules
- String-basierte Funktionsreferenzen in der Tab-Registry
- weiche Lint-/Typecheck-Gates
- die drei verbleibenden `execCommand`-Aufrufe ausserhalb des Editor-Moduls
  (`systems/entity-links.js`, `features/wiki/wiki.js`, `ui/actions/system-actions.js` — Fundstellen
  gegen den Live-Code verifizieren)
Weitere `uebernommen`-Zeilen aus der Triage kommen hinzu. Die IDs laufen fortlaufend als
`DEBT-01`, `DEBT-02`, … in der Reihenfolge der Triage-Tabelle.

Zusaetzlich pruefen, ob die aus `.planning/STATE.md` bekannten, noch offenen Posten in der Triage
auftauchen und, falls ja, ebenfalls eine ID bekommen: der nie verdrahtete Waechter in
`ui/editors/markdown-converter.js` (Anzeigebug bei Unterstrichen in URLs), das doppelte
`data-id`-Attribut in `features/wiki/wiki.js`, die un-escapte Regex-Capture in `parseWikiLinks()`
und die latente Toast-Race in `tests/e2e/crud/locations.spec.js` sowie
`tests/e2e/crud/encounters.spec.js` (fehlender Seed-Nachzug aus Plan 08-02).

Abschliessend in `11-CONCERNS-TRIAGE.md` gegenpruefen, dass jede `uebernommen`-Zeile genau die
`DEBT-`-ID nennt, die in `REQUIREMENTS.md` vergeben wurde — die beiden Dokumente muessen
aufeinander zeigen.

KEINE dieser Schulden wird in Phase 11 gefixt: die Milestone-Leitplanke ist verhaltensneutral, und
D-16 fordert ausdruecklich Uebernahme statt Fix.
  </action>
  <verify>
    <automated>python -c "import re,sys; r=open('.planning/REQUIREMENTS.md',encoding='utf-8').read(); t=open('.planning/phases/11-architektur-build-hygiene/11-CONCERNS-TRIAGE.md',encoding='utf-8').read(); ids_r=set(re.findall(r'DEBT-\d{2}', r)); ids_t=set(re.findall(r'DEBT-\d{2}', t)); print('req=',sorted(ids_r)); print('triage=',sorted(ids_t)); sys.exit(0 if ids_r and ids_r==ids_t else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` enthaelt unter §v2 Requirements einen Unterabschnitt mit `DEBT-`-Eintraegen; `grep -c "DEBT-01" .planning/REQUIREMENTS.md` liefert mindestens `1`.
    - Die Menge der `DEBT-`-IDs in `.planning/REQUIREMENTS.md` ist identisch mit der Menge in `11-CONCERNS-TRIAGE.md` — belegt durch das Kommando im `<verify>`-Block, Exit-Code 0.
    - Jeder `DEBT-`-Eintrag nennt seinen CONCERNS-Ursprung (Abschnittsname) und verweist auf `11-CONCERNS-TRIAGE.md`.
    - Ausserhalb des neuen §v2-Unterabschnitts ist `.planning/REQUIREMENTS.md` unveraendert: `git diff --stat .planning/REQUIREMENTS.md` zeigt ausschliesslich Hinzufuegungen (keine geloeschten Zeilen).
    - Kein Produktionscode wurde geaendert: `git status --porcelain` listet ausschliesslich Dateien unter `.planning/`.
    - `npm test` endet mit Exit-Code 0 (Gegenprobe, dass dieser reine Dokumentations-Task nichts am Code beruehrt hat).
  </acceptance_criteria>
  <done>Jeder uebernommene Restposten hat ein benanntes Requirement mit Rueckverweis; Triage und REQUIREMENTS.md zeigen aufeinander.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Keine. Dieser Plan aendert ausschliesslich Planungsartefakte unter `.planning/`; es entsteht kein
neuer Datenpfad, kein neuer Nutzereingabe-Pfad und keine Aenderung an ausgeliefertem Code.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-13 | Repudiation | Disposition von Sicherheits-Eintraegen in `CONCERNS.md` | medium | mitigate | Jede Disposition traegt einen ueberpruefbaren Beleg gegen den Live-Code (D-15); eine als „erledigt" markierte, tatsaechlich offene Sicherheitsluecke waere sonst genau der stille Fehler, den die Triage verhindern soll. Die Eintraege aus dem Abschnitt „Security Considerations" werden zusaetzlich gegen die konsolidierte `SECURITY.md` (`threats_open: 0` nach Phase 10) abgeglichen. |
| T-11-14 | Information Disclosure | `11-CONCERNS-TRIAGE.md` als committetes Dokument | low | accept | Das Dokument beschreibt bekannte Schwaechen einer Single-User-Offline-App in einem oeffentlichen Repo. Dieselbe Information steht bereits in `CONCERNS.md` und `SECURITY.md`; es entsteht kein zusaetzliches Risiko. Bewusst akzeptiert. |
</threat_model>

## Flagged Planner Assumptions (Edge Probe — unclassified)

Der deterministische Edge-Probe hat ARCH-04 als `unclassified` zurueckgegeben (`status: unresolved`).
Daraus wird kein Backstop-Marker abgeleitet, sondern eine sichtbare Planer-Annahme:

- **ARCH-04 (unclassified, unresolved):** Angenommen wird, dass „jeder Eintrag" auf der Granularitaet
  der diskreten Listenpunkte in `CONCERNS.md` zu verstehen ist (Recherche-Zaehlung: 46) und nicht auf
  der Granularitaet der neun Abschnitte. Die Zaehlung selbst ist eine Interpretationsentscheidung —
  ein Eintrag mit mehreren Unterpunkten koennte auch als mehrere Eintraege gelten. Der Plan verlangt
  deshalb, die eigene Zaehlung im Dokumentkopf offenzulegen und Abweichungen zur Recherche zu
  begruenden. Weiterhin gilt: ARCH-04 ist ausschliesslich dokument-verifizierbar, es gibt keinen
  automatisierten Test fuer „vollstaendig dispositioniert" ausser der hier gebauten Struktur- und
  Zaehlpruefung. Falls der Reviewer eine andere Granularitaet erwartet, ist das hier der Ort zum
  Widerspruch.

## Artifacts this phase produces

**Neu in diesem Plan:** die Datei
`.planning/phases/11-architektur-build-hygiene/11-CONCERNS-TRIAGE.md`; der Unterabschnitt
`### Technische Schulden (aus Phase-11-Triage)` mit den Requirement-IDs `DEBT-01` … `DEBT-NN` in
`.planning/REQUIREMENTS.md` §v2.
**Entfernt in diesem Plan:** nichts.

**Phasenweit (Referenz — vom Verifier NICHT als Drift zu werten):**
neu: `parse_js_string_array`, `load_module_list`, `load_template_list`, `load_css_import_order`,
`require_files_exist`, `build_favicon_data_uri` (build.py); `requirements-dev.txt`;
`KNOWN_DEPRECATION_STRINGS` und der Test „Keine Favicon-404 und keine Meta-Tag-Deprecation"
(`tests/e2e/smoke.spec.js`); `<link rel="icon">` und `<meta name="mobile-web-app-capable">` in
beiden HTML-Koepfen; ein pytest-Schritt im CI-`test`-Job.
entfernt: `remove_duplicate_functions`, die `MODULES`-Kopie, `check_module_list_sync`, die
hartkodierten `css_files`/`html_templates`-Listen (build.py);
`test_no_orphaned_return_statements`, `test_module_lists_are_synchronized`; die
`favicon`-Ausnahmefilter in `tests/e2e/smoke.spec.js`; ggf.
`<meta name="apple-mobile-web-app-capable">`.

<verification>
- `11-CONCERNS-TRIAGE.md` existiert, deckt alle neun CONCERNS-Abschnitte ab und jede Zeile traegt Disposition plus Live-Code-Beleg
- Die Zeilenzahl entspricht der im Kopf genannten Gesamtzahl diskreter Eintraege
- Die `DEBT-`-ID-Mengen in Triage und `REQUIREMENTS.md` sind identisch
- `git status --porcelain` zeigt ausschliesslich Aenderungen unter `.planning/`; `npm test` gruen
</verification>

<success_criteria>
- ARCH-04 (Triage-Haelfte) erfuellt: jeder CONCERNS-Eintrag ist erledigt, obsolet, akzeptiert oder als Requirement uebernommen (D-13)
- Jede Disposition ist gegen den Live-Code belegt (D-15)
- Kein Restposten wurde aktiv gefixt — die Milestone-Leitplanke haelt (D-16)
</success_criteria>

<output>
Create `.planning/phases/11-architektur-build-hygiene/11-06-SUMMARY.md` when done
</output>
