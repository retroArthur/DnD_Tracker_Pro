---
phase: 12-datensicherheit
plan: 11
type: execute
wave: 8
depends_on: ["12-05", "12-09", "12-10"]
files_modified:
  - systems/undo.js
  - tests/unit/stability.test.js
autonomous: true
requirements: [SAFE-05]
gap_closure: true
gap_ids: [WR-02]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Schlägt die Serialisierung in pushUndo() fehl, ist der Redo-Stack danach leer — ein späteres Redo kann keine Aktion überschreiben, die nach dem gescheiterten Push ausgeführt wurde"
    - "Die destruktive Aktion selbst läuft weiterhin durch: pushUndo() wirft nicht und blockiert den Aufrufer nicht, auch wenn window.D nicht serialisierbar ist (Spieltisch-Regel aus D-06)"
    - "Der Warn-Toast erscheint weiterhin, damit der Nutzer merkt, dass dieser eine Schritt ohne Undo-Schutz lief"
    - "Der Erfolgspfad von pushUndo() ist unverändert: Eintrag auf den Undo-Stack, UNDO_LIMIT eingehalten, Redo-Stack geleert"
    - "Jest, pytest tests/build und Playwright liegen nicht unter der Basislinie; beide dist-Bundles sind aus dem geänderten Quellstand aller drei Pläne dieser Runde gebaut"
  prohibitions:
    - "Ein Redo darf niemals eine Nutzeraktion überschreiben, die nach dem gescheiterten Undo-Push ausgeführt wurde — der Nutzer hat sie weder erwartet noch bestätigt"
    - "Der Undo-Schutz darf am Spieltisch nie die destruktive Aktion selbst blockieren; ein Fehler im Undo-System darf das Spiel nicht anhalten (D-06)"
    - "Ein ausgeliefertes dist-Bundle darf nicht behaupten, den geprüften Quellstand zu tragen, wenn es älter ist als die zuletzt geänderte Quelldatei"
  artifacts:
    - "systems/undo.js — pushUndo() leert den Redo-Stack auch im catch-Zweig"
    - "tests/unit/stability.test.js — Regressionstest im bestehenden vm-Kontext-Block für undo.js"
    - "dist/dnd-tracker-bundled.html und dist/dnd-tracker-optimized.html — beide neu gebaut (Build-Ausgabe, nicht versionierte Quelle)"
  key_links:
    - "pushUndo() catch-Zweig → redoStack (systems/undo.js:19-23) — die Stelle, die heute vor dem Leeren abbricht"
    - "pushUndo() Erfolgspfad → redoStack.length = 0 (systems/undo.js:34) — die Semantik, die der catch-Zweig spiegeln muss"
    - "redo() → redoStack[redoStack.length - 1] → Object.assign(D, parsed) (systems/undo.js:87-125) — der Konsument, der den veralteten Eintrag anwenden würde"
    - "__pushRawRedo()/__undoDebug() in tests/unit/stability.test.js:1322-1324 — die vorhandenen Prüfhaken auf die const-deklarierten Stacks"
    - "geänderte Quellen aus 12-09/12-10/12-11 → build.py → dist/*.html"
---

<objective>
Schließt **WR-02** (Warnung, vom Nutzer für diesen Lauf ausdrücklich mit hineingenommen — kein
Verifikations-Gap, sondern ein per Nutzerentscheidung gezogener Review-Befund) und bringt danach die
vollen Suiten und beide ausgelieferten Bundles auf den Stand aller drei Pläne dieser Runde.

**WR-02 in einem Satz:** Der Erfolgspfad von `pushUndo()` endet mit `redoStack.length = 0`
(`systems/undo.js:34`) — eine neue Aktion macht die alte Redo-Historie ungültig, Standardsemantik.
Wirft `JSON.stringify(window.D)` (`:17`), kehrt die Funktion im `catch`-Zweig zurück (`:19-23`),
BEVOR der Redo-Stack geleert wird. Die destruktive Aktion läuft laut Design trotzdem durch — „am
Spieltisch nie blockieren" (D-06) — und verändert `D`. Ein späteres Redo wendet den alten Eintrag
dann auf einen bereits weiterentwickelten Stand an und überschreibt die Zwischenaktion, ohne dass
der Nutzer das erwartet oder bestätigt hätte.

Das Fenster ist eng (eine zirkuläre oder sonst nicht serialisierbare Referenz in `D` genau während
einer Aktion, die auf einen vorangegangenen Undo folgt). Der Fix ist eine Zeile. Genau deshalb ist
er hier richtig aufgehoben: das Verhältnis von Aufwand zu vermiedenem stillem Datenverlust ist
extrem günstig.

**Der zweite Teil dieses Plans ist der Abschluss der Welle.** `12-VERIFICATION.md` hält unter
`warnings` fest: die 2026-09-04 gebauten Bundles sind für den 12-08-Quellstand aktuell, tragen die
Fixes aus CR-01/CR-02/WR-01/WR-02 aber NICHT — sie standen zu dem Zeitpunkt noch nicht im Code.
Bei der Vorverifikation am 2026-08-26 war das Produktions-Bundle acht Tage alt und trug die
Korrekturen von vier Plänen nicht (W-1). Dieser Plan wiederholt den Fehler nicht.

Purpose: SAFE-05 verlangt vorhersagbare Persistenz bei Fehlern und Sonderfällen. Ein Redo, das
stillschweigend eine Zwischenaktion überschreibt, ist das Gegenteil davon.
Output: eine Zeile im `catch`-Zweig, ihr Regressionstest, und der Nachweis, dass die drei Pläne
dieser Runde zusammen nichts umgeworfen haben.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-datensicherheit/12-CONTEXT.md
@.planning/phases/12-datensicherheit/12-VERIFICATION.md
@.planning/phases/12-datensicherheit/12-REVIEW.md
@.planning/phases/12-datensicherheit/12-05-SUMMARY.md
@CLAUDE.md

@systems/undo.js
@tests/unit/stability.test.js
</context>

<wave_note>
**Abweichung von der vorgeschlagenen Zerlegung, mit Begründung.** Der Auftrag schlug alle drei
Lückenpläne in Welle 7 vor, mit der Neubau-Aufgabe „im letzten Plan der Welle". Beides zusammen geht
nicht: Pläne derselben Welle laufen parallel, und ein Neubau in einem von ihnen sähe den Arbeitsbaum
der Geschwisterpläne nicht — das Bundle trüge dann genau einen oder zwei der drei Fixes. Das ist
buchstäblich der Fehler, den `12-VERIFICATION.md` als W-1 dokumentiert.

Deshalb: **12-09 und 12-10 in Welle 7** (überschneidungsfreie `files_modified`, echte Parallelität),
**dieser Plan in Welle 8** mit `depends_on: ["12-05", "12-09", "12-10"]`. `12-05` ist die
Herkunftsabhängigkeit für `systems/undo.js`; `12-09` und `12-10` sind Reihenfolgeabhängigkeiten,
damit Task 3 einen vollständigen Quellstand vorfindet. Dieser Plan bleibt „der letzte Plan der
Welle", nur eben einer Welle später — der Sinn der Regel ist gewahrt, ihre wörtliche Fassung wäre
hier selbstwidersprüchlich.
</wave_note>

<scope_fence>
Dieser Plan schließt GENAU einen Befund (WR-02) und schließt die Welle ab.

**Nicht Teil dieses Plans** — auch wenn es beim Lesen naheliegt:
- **Kein Umbau von `undo()` oder `redo()`.** Die Peek-Parse-Pop-Reihenfolge ist in Plan 12-05
  gebaut und in Truth 5 als VERIFIED abgenommen. Der Fix betrifft ausschließlich den `catch`-Zweig
  von `pushUndo()`.
- **Keine Änderung an `UNDO_LIMIT`, `clearUndoHistory()` oder den Undo-Hooks** (`registerUndoHook()`,
  Konsument Plan 12-06).
- **Kein Versuch, `JSON.stringify` durch `structuredClone` oder einen zirkularitätsfesten
  Serialisierer zu ersetzen.** Die Push-Validierung ist eine bewusste Entscheidung aus D-06; sie zu
  ersetzen wäre ein eigenes Vorhaben.
- **Kein Eingriff in `migration-wizard.js` oder `file-backup-manager.js`** — die gehören zu den
  Plänen 12-09 und 12-10; die `files_modified` der drei Pläne sind überschneidungsfrei.
- **Die bekannte E2E-Flake wird hier nicht behoben:** die Toast-Race in
  `tests/e2e/crud/locations.spec.js` und `encounters.spec.js` (nie nachgezogener Seed aus Plan
  08-02) ist als `TEST-03` in Phase 14 eingeplant. Sie ist der EINZIGE bekannte, phasenfremde
  E2E-Ausfall; jeder andere E2E-Fehlschlag ist eine echte Regression.
- **Der offene menschliche Prüfpunkt** aus `12-VERIFICATION.md` (`human_verification`: Audio-
  Bibliothek knapp unter 300 MiB im echten Browser, Recherche-Annahme A1) bekommt hier KEINE
  Aufgabe. Er ist unter Node/jsdom nicht messbar; der Nutzer hat sich am 2026-08-19 bewusst gegen
  das Zusammentragen der Dateien entschieden. Er bleibt offen und wird nach dieser Runde erneut
  fällig — das gehört als Erinnerung in die SUMMARY, nicht in einen Task.
</scope_fence>

<tracer_note>
`TRACER_MODE` ist an, dieser Plan enthält aber bewusst **keinen** `type="tracer"`-Plan und keine
Tracer-Aufgabe. Grund: Phase 12 ist ausgeführt und verifiziert; der Undo-Kern ist seit Plan 12-05
durchgestochen und in Truth 5 als VERIFIED abgenommen.

Der Geist der Regel wird eingehalten: **Task 1 ist der rote Test durch den kaputten Pfad** — Aktion,
Undo, unserialisierbares `D`, zweite Aktion, Redo — und er wird ROT nachgewiesen, BEVOR Task 2 den
Fix schreibt.
</tracer_note>

<reversibility_note>
`REVERSIBILITY_GATES` ist an. Alle Entscheidungen dieses Plans sind mit `reversible` bewertet: eine
Zeile in einem `catch`-Zweig, kein veröffentlichter Vertrag, kein Datenformat, keine Migration; der
Neubau der Bundles ist ohnehin reproduzierbar. Keine Bewertung fällt auf `one-way` — deshalb enthält
dieser Plan **keinen** `checkpoint:decision`, und `autonomous` steht auf `true`. Das ist eine
ausdrückliche Feststellung, keine Auslassung.
</reversibility_note>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Roter Test — Redo nach gescheitertem Push würde die Zwischenaktion überschreiben</name>
  <files>tests/unit/stability.test.js</files>
  <read_first>
    - `tests/unit/stability.test.js` — die zu ändernde Datei. Der `describe('Undo/Redo —
      Peek-Parse-Pop, Push-Validierung, Hooks (12-05 / D-06 / SAFE-05)')` (ab Zeile 1273) mit dem
      `beforeEach`, der `systems/undo.js` in einen eigenen `vm.createContext()` lädt (Zeilen
      1281-1335), und den drei angehängten Prüfhaken `__undoDebug()`, `__pushRawUndo()`,
      `__pushRawRedo()` (Zeilen 1322-1324) — sie sind der einzige Weg an die als `const`
      deklarierten Stacks. Der Test „Ein zirkuläres window.D beim Push legt keinen Eintrag an,
      wirft nicht, Aufrufer läuft weiter" (Zeilen 1379-1393) ist die Vorlage für das Herstellen
      eines nicht serialisierbaren `D`.
    - `systems/undo.js` — `pushUndo()` (Zeilen 9-35) vollständig, insbesondere der `catch`-Zweig
      (Zeilen 19-23) mit dem frühen `return` und die Zeile, die den Redo-Stack leert (Zeile 34), am
      Ende des Erfolgspfads; `undo()` (Zeilen 39-85) — dort wird der Redo-Eintrag angelegt;
      `redo()` (Zeilen 87-125) — der Konsument, der den veralteten Eintrag auf ein bereits
      verändertes `D` anwenden würde.
  </read_first>
  <reversibility rating="reversible">Reine Testdatei-Ergänzung ohne Produktionswirkung.</reversibility>
  <behavior>
    Zwei neue Tests im bestehenden `describe`-Block, beide mit der Kennung `WR-02` im Namen.

    - **Test J (der Durchstich):** Aktion A ausführen und mit `saveUndoState()` sichern, `D`
      verändern, `undo()` aufrufen (der Redo-Stack trägt danach genau einen Eintrag — mit
      `__undoDebug()` bestätigen). Dann `D` unserialisierbar machen (zirkuläre Referenz, wie im
      bestehenden Zirkulär-Test) und `pushUndo('Aktion B')` aufrufen. Erwartung nach dem Fix:
      `__undoDebug().redoLength === 0`. Vor dem Fix ist der Wert `1` — der veraltete Eintrag liegt
      noch da.
    - **Test K (die Gegenprobe, nicht verhandelbar):** derselbe Ablauf, aber die Prüfung gilt dem
      Aufrufer und dem Toast: `pushUndo()` wirft nicht, die Anweisung NACH dem Aufruf läuft
      (dieselbe `ranAfterPush`-Technik wie im bestehenden Zirkulär-Test), `undoLength` bleibt `0`
      (kein kaputter Eintrag wurde angelegt), und `showToast` wurde mit einer Zeichenkette
      aufgerufen, die `Undo-Schutz` enthält. Dieser Test ist der Schutz gegen den Gegenfehler „im
      Fehlerfall einfach werfen oder abbrechen" und muss vor UND nach dem Fix grün sein.
  </behavior>
  <action>
    Nur die Testdatei. Kein Produktionscode in diesem Task.

    **Schritt 1 — die Tests schreiben.** Beide gehören in den bestehenden `describe`-Block, damit
    sie den dort aufgebauten vm-Kontext und die drei Prüfhaken mitbenutzen. Nichts neu aufbauen: der
    `beforeEach` lädt `systems/undo.js` bereits frisch je Test und stellt `realPushUndo`,
    `realUndo`, `realRedo`, `realSaveUndoState`, `getDebug`, `pushRawUndo` und `pushRawRedo` bereit.

    **Schritt 2 — die Zwischenaktion muss echt sein.** Test J darf den Redo-Eintrag nicht per
    `__pushRawRedo()` von Hand hinlegen. Der Eintrag muss durch einen echten `realUndo()`-Lauf
    entstehen, sonst prüft der Test eine Laborsituation statt der Bedienfolge, um die es geht:
    Aktion, Undo, weitere Aktion, Redo. `__pushRawRedo()` bleibt den bestehenden Tests vorbehalten,
    die absichtlich einen kaputten Eintrag einschleusen.

    **Schritt 3 — die zirkuläre Referenz wieder abräumen.** `context.window.D.self = context.window.D`
    macht `D` für die Restlaufzeit des Tests unserialisierbar. Der `beforeEach` baut den Kontext je
    Test neu auf, deshalb genügt das — aber wenn Test J nach dem Push noch etwas prüfen soll, das
    Serialisierung braucht, muss die Referenz vorher gelöscht werden. Das gehört ausdrücklich
    bedacht, nicht dem Zufall überlassen.

    **Schritt 4 — den roten Lauf protokollieren.** Das erste Verify-Kommando dieses Tasks ausführen.
    Test J MUSS fehlschlagen, Test K muss bestehen. Testnamen und Fehlerzeilen für die SUMMARY
    festhalten. Erst danach beginnt Task 2.
  </action>
  <verify>
    <automated>npx jest tests/unit/stability.test.js -t "WR-02" || echo RED_CONFIRMED_WR02</automated>
    <fails_when>Die Ausgabe enthält die Zeichenkette `RED_CONFIRMED_WR02` nicht (dann war die gefilterte Suite grün, obwohl der Fix noch fehlt — der Test hängt nicht am Produktionscode); oder die Jest-Zusammenfassung meldet `0 total` bzw. `Tests: 0` (der `-t`-Filter hat keinen Test getroffen).</fails_when>
  </verify>
  <verify>
    <automated>npx jest tests/unit/stability.test.js -t "Push-Validierung"</automated>
    <fails_when>Exit-Code ≠ 0 an einem der VORHANDENEN Tests dieses describe-Blocks (die Ergänzung hat bestehendes Verhalten verändert); oder die Zusammenfassung meldet `0 total`. Der neue Test J darf in diesem Lauf rot sein — er ist Gegenstand des ersten Verify-Kommandos.</fails_when>
  </verify>
  <acceptance_criteria>
    - Zwei neue Tests mit der Kennung `WR-02` im Namen existieren im bestehenden
      `Undo/Redo`-`describe`-Block von `tests/unit/stability.test.js`.
    - Test J erzeugt den Redo-Eintrag über einen echten `realUndo()`-Lauf, nicht über
      `__pushRawRedo()`.
    - Das erste Verify-Kommando meldet die Bestätigungszeichenkette; die Jest-Ausgabe nennt Test J
      als fehlgeschlagen.
    - Test K besteht bereits vor dem Fix.
    - Die fehlgeschlagenen Testnamen und Fehlerzeilen sind für die SUMMARY notiert.
  </acceptance_criteria>
  <done>
    Der veraltete Redo-Eintrag nach einem gescheiterten Push ist zum ersten Mal von einem Test
    sichtbar, und der Test ist nachweislich rot.
  </done>
</task>

<task type="auto">
  <name>Task 2: Der Fix — der catch-Zweig leert den Redo-Stack, weil die Aktion trotzdem durchläuft</name>
  <files>systems/undo.js</files>
  <read_first>
    - `systems/undo.js` — die zu ändernde Datei. `pushUndo()` (Zeilen 9-35) vollständig: der
      Kommentarblock über der Funktion (Zeilen 10-14), der die Spieltisch-Regel begründet, der
      `try`/`catch` (Zeilen 16-24) und die Zeile, die den Redo-Stack leert (Zeile 34), mit ihrem
      Kommentar „Redo-Stack leeren wenn neue Aktion" — das ist die Semantik, die der `catch`-Zweig
      spiegeln muss.
    - `tests/unit/stability.test.js` — die in Task 1 geschriebenen Tests J und K.
    - `.planning/phases/12-datensicherheit/12-REVIEW.md` — der `**Fix:**`-Block unter WR-02 mit dem
      vorgeschlagenen Patch.
  </read_first>
  <reversibility rating="reversible">Eine Zeile in einem catch-Zweig; kein veröffentlichter Vertrag, kein Datenformat, keine Migration. Ein git revert stellt den Vorzustand her.</reversibility>
  <action>
    Im `catch`-Zweig von `pushUndo()` wird der Redo-Stack geleert, BEVOR die Funktion zurückkehrt.
    Reihenfolge innerhalb des Zweigs: die bestehende `ErrorHandler.log`-Zeile unter
    `DEBUG_MODE` bleibt zuerst, dann der bestehende `showToast`-Aufruf, dann das Leeren, dann das
    bestehende `return`. Der Toast steht bewusst vor dem Leeren — er ist die einzige Rückmeldung,
    die der Nutzer bekommt, und darf durch nichts verdrängt werden.

    Der begründende Kommentar daneben muss den Sachverhalt benennen, der die Zeile überhaupt nötig
    macht: die destruktive Aktion läuft nach dem frühen `return` trotzdem durch und verändert `D`,
    also wäre eine erhalten gebliebene Redo-Historie ab diesem Moment inkonsistent.

    Der Erfolgspfad bleibt vollständig unverändert — insbesondere `undoStack.push(...)`, die
    `UNDO_LIMIT`-Kappung und die abschließende Zeile, die den Redo-Stack auf Länge null setzt. Es
    entsteht keine neue
    Top-Level-Deklaration und kein neuer `window.*`-Export; damit ist
    `check_duplicate_functions()` hier kein Thema. Kein `const x = window.x` (CLAUDE.md-Dedup-Regel).

    **Mutationsnachweis.** Die neue Zeile testweise wieder entfernen, das erste Verify-Kommando
    dieses Tasks laufen lassen, die fehlschlagenden Testnamen notieren, Mutation zurücknehmen,
    erneut grün. Ergebnis in die SUMMARY.
  </action>
  <verify>
    <automated>npx jest tests/unit/stability.test.js</automated>
    <fails_when>Exit-Code ≠ 0; oder die Zusammenfassung meldet weniger als `75 passed` (Basislinie 73, gemessen am 2026-09-04, plus die zwei neuen aus Task 1); oder in der Ausgabe erscheint eine Zeile, die mit `✕` oder `×` beginnt.</fails_when>
  </verify>
  <verify>
    <automated>node --check systems/undo.js</automated>
    <fails_when>Exit-Code ≠ 0 oder die Ausgabe enthält `SyntaxError`.</fails_when>
  </verify>
  <verify>
    <automated>grep -v '^\s*//' systems/undo.js | grep -c "redoStack.length = 0"</automated>
    <fails_when>Die ausgegebene Zahl ist kleiner als 3 (Erfolgspfad in `pushUndo()`, neuer `catch`-Zweig, `clearUndoHistory()` — vor dem Fix sind es 2); oder das Kommando liefert Exit-Code ≠ 0 (kein Treffer, die Zuweisung wurde umgeschrieben). Der vorgeschaltete Filter entfernt reine Kommentarzeilen, damit ein erläuternder Kommentar das Ergebnis nicht selbst erfüllt.</fails_when>
  </verify>
  <acceptance_criteria>
    - Der `catch`-Zweig von `pushUndo()` leert den Redo-Stack vor seinem `return`.
    - Die `ErrorHandler.log`-Zeile unter `DEBUG_MODE` und der `showToast`-Aufruf stehen unverändert
      vor dem Leeren.
    - Der Erfolgspfad ist unverändert: `undoStack.push(...)`, `UNDO_LIMIT`-Kappung und das
      abschließende Leeren des Redo-Stacks.
    - `pushUndo()` wirft weiterhin nicht und bricht den Aufrufer nicht ab (Test K grün).
    - `npx jest tests/unit/stability.test.js` ist grün mit mindestens 75 bestandenen Tests.
    - Mutationsnachweis protokolliert: bei entfernter Zeile fällt Test J um.
  </acceptance_criteria>
  <done>
    Ein Redo nach einem gescheiterten Undo-Push kann keine Zwischenaktion mehr überschreiben, und
    die Aktion selbst läuft am Spieltisch weiterhin ungehindert durch.
  </done>
</task>

<task type="auto">
  <name>Task 3: Volle Suiten gegen die Basislinie und beide dist-Bundles aus dem Stand aller drei Pläne</name>
  <files>systems/undo.js, tests/unit/stability.test.js</files>
  <precondition>Die Pläne 12-09 und 12-10 sind abgeschlossen und ihre Änderungen liegen im Arbeitsbaum — sonst bauen die Bundles einen unvollständigen Quellstand. Prüfbar an `git log --oneline -6` (Commits aus 12-09 und 12-10 vorhanden) oder an `git status` (ihre Dateien geändert).</precondition>
  <precondition>Unter Windows ist `PYTHONIOENCODING=utf-8` gesetzt, sonst bricht `build.py` an den Umlauten der Konsolenausgabe ab.</precondition>
  <read_first>
    - `.planning/phases/12-datensicherheit/12-VERIFICATION.md` — der `warnings`-Block, in dem W-1
      (veraltetes Produktions-Bundle) und die Feststellung stehen, dass die Fixes dieser Runde in
      den 2026-09-04-Bundles NICHT enthalten sind.
    - `.planning/phases/12-datensicherheit/12-08-PLAN.md`, Task 3 — dieselbe Aufgabe eine Welle
      zuvor, inklusive der Reihenfolgebegründung (E2E lädt `dist/dnd-tracker-bundled.html` per
      `file://`, darf also erst NACH dem Build laufen).
    - `CLAUDE.md` § Testing und § Build Commands — die kanonischen Kommandos.
  </read_first>
  <reversibility rating="reversible">Kein Quellcode-Eingriff; `dist/` ist Build-Ausgabe und jederzeit reproduzierbar.</reversibility>
  <action>
    Kein neuer Code. Dieser Task beweist, dass die drei Pläne dieser Runde zusammen nichts anderes
    umgeworfen haben, und bringt die ausgelieferten Bundles auf den vollständigen Quellstand.

    **Reihenfolge einhalten:** erst die volle Jest-Suite, dann die Build-Tests, dann die beiden
    Builds, dann Playwright. Die E2E-Suite lädt `dist/dnd-tracker-bundled.html` per `file://` — sie
    vor dem Build laufen zu lassen, prüfte den alten Stand.

    **Beide Bundles.** `python build.py` erzeugt `dist/dnd-tracker-bundled.html` (Entwicklung),
    `python build.py --production` erzeugt `dist/dnd-tracker-optimized.html`. Beide sind nötig. Bei
    der Vorverifikation war das Produktions-Bundle acht Tage alt und trug die Korrekturen von vier
    Plänen nicht (W-1); nur den Entwicklungs-Build zu erneuern, wiederholt genau diesen Fehler.
    `dist/` steht in `.gitignore` — die Dateien werden gebaut, nicht committet.

    Der Build ist zugleich die Kollisionsprüfung: `check_duplicate_functions()` läuft vor dem
    Bündeln und bricht bei einer doppelten Top-Level-Deklaration hart ab. Keiner der drei Pläne
    dieser Runde führt eine neue Top-Level-Deklaration ein, es sollte also nichts anschlagen — wenn
    doch, ist das ein echter Befund und kein Rauschen.

    **Basislinien, die nicht unterschritten werden dürfen** (gemessen 2026-09-04, nach Plan 12-08 —
    nicht geschätzt): Jest **749** bestanden bei 29 Suiten, `pytest tests/build` **24** bestanden,
    Playwright **321 bestanden / 2 übersprungen** bei Exit-Code 0, `build.py` **124/124** Module.
    Nach dieser Runde kommen mindestens 11 Tests hinzu: 5 aus 12-09 (Basislinie der Datei 41 → 46),
    4 aus 12-10 (17 → 21), 2 aus 12-11 (73 → 75). Erwartungswert also mindestens **760** bestandene
    Jest-Tests.

    **Fällt Playwright aus:** vor jeder Analyse den Isolationslauf der betroffenen Datei fahren. Die
    latente Toast-Race in `tests/e2e/crud/locations.spec.js` und `encounters.spec.js` (nie
    nachgezogener Seed aus Plan 08-02) ist der EINZIGE bekannte, phasenfremde Ausfall — sie ist als
    `TEST-03` in Phase 14 eingeplant und in diesem Plan nicht zu beheben. Jeder andere Fehlschlag
    ist eine echte Regression und gehört untersucht, nicht weggeklickt.
  </action>
  <verify>
    <automated>npx jest</automated>
    <fails_when>Exit-Code ≠ 0; oder die Zeile `Tests:` enthält `failed`; oder sie meldet weniger als `760 passed`; oder die Zeile `Test Suites:` meldet weniger als `29 passed`.</fails_when>
  </verify>
  <verify>
    <automated>python -m pytest tests/build -q</automated>
    <fails_when>Exit-Code ≠ 0 oder die Zusammenfassung meldet weniger als `24 passed`.</fails_when>
  </verify>
  <verify>
    <automated>PYTHONIOENCODING=utf-8 python build.py</automated>
    <fails_when>Exit-Code ≠ 0; oder die Ausgabe enthält `[FEHLER] Doppelte Top-Level-Deklaration`, `[ABORTED] Build NICHT geschrieben!` oder `FEHLER: Doppelte Deklaration`; oder es fehlt die Zeile `[SUCCESS] Build abgeschlossen! (Development)`; oder die letzte Modulzeile lautet nicht `[124/124]`.</fails_when>
  </verify>
  <verify>
    <automated>PYTHONIOENCODING=utf-8 python build.py --production</automated>
    <fails_when>Exit-Code ≠ 0; oder es fehlt die Zeile `[SUCCESS] Build abgeschlossen!`; oder die letzte Modulzeile lautet nicht `[124/124]`.</fails_when>
  </verify>
  <verify>
    <automated>ls -la --time-style=full-iso dist/dnd-tracker-bundled.html dist/dnd-tracker-optimized.html systems/migration/migration-wizard.js systems/file-backup/file-backup-manager.js systems/undo.js</automated>
    <fails_when>Der Zeitstempel eines der beiden `dist/`-Bundles liegt VOR dem Zeitstempel einer der drei Quelldateien — dann trägt das ausgelieferte Bundle nicht den geprüften Quellstand; oder eine der fünf Dateien fehlt (Exit-Code ≠ 0).</fails_when>
  </verify>
  <verify>
    <automated>npx playwright test</automated>
    <fails_when>Exit-Code ≠ 0; oder die Zusammenfassung meldet weniger als `321 passed`; oder mehr als `2 skipped`.</fails_when>
  </verify>
  <acceptance_criteria>
    - `npx jest` grün mit mindestens 760 bestandenen Tests bei mindestens 29 Suiten.
    - `python -m pytest tests/build -q` grün mit mindestens 24 bestandenen Tests.
    - Beide `build.py`-Läufe enden mit Exit-Code 0 bei `[124/124]` Modulen und ohne Kollisionsmeldung.
    - Beide `dist/`-Bundles tragen einen Änderungszeitstempel, der jünger ist als der von
      `systems/migration/migration-wizard.js`, `systems/file-backup/file-backup-manager.js` und
      `systems/undo.js`.
    - `npx playwright test` grün mit mindestens 321 bestandenen und höchstens 2 übersprungenen Tests.
    - Die gemessenen Zahlen sind gegen die Basislinien 749 / 24 / 321+2 / 124-124 in der SUMMARY
      festgehalten.
  </acceptance_criteria>
  <done>
    Alle vier Suiten liegen auf oder über der Basislinie, und beide dist-Bundles sind aus dem
    vollständigen Quellstand aller drei Pläne dieser Runde gebaut.
  </done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

**Geänderte Quelldateien**

| Datei | Symbol / Stelle | Art der Änderung |
|---|---|---|
| `systems/undo.js` | `pushUndo()`, `catch`-Zweig (Zeilen 19-23) | neue Zeile: Redo-Stack wird vor dem `return` geleert, mit begründendem Kommentar |

**Keine neuen Top-Level-Deklarationen, keine neuen `window.*`-Exports.** `undoStack`, `redoStack`,
`UNDO_LIMIT`, `pushUndo`, `saveUndoState`, `undo`, `redo` und `clearUndoHistory` bleiben in Zahl und
Namen unverändert — damit kein Kollisionsrisiko für `check_duplicate_functions()`.

**Neue Tests** (`tests/unit/stability.test.js`, beide mit der Kennung `WR-02` im Namen)

| Kennung | Testinhalt |
|---|---|
| Test J | Nach Aktion → Undo → nicht serialisierbares `D` → `pushUndo()` ist der Redo-Stack leer |
| Test K | Derselbe Ablauf: `pushUndo()` wirft nicht, der Aufrufer läuft weiter, kein Eintrag auf dem Undo-Stack, Warn-Toast erschienen |

**Build-Ausgabe** (nicht versionierte Quelle, `dist/` steht in `.gitignore`)

| Datei | Erzeugt durch |
|---|---|
| `dist/dnd-tracker-bundled.html` | `PYTHONIOENCODING=utf-8 python build.py` |
| `dist/dnd-tracker-optimized.html` | `PYTHONIOENCODING=utf-8 python build.py --production` |

**Nicht erzeugt:** keine neue Datei, kein neues Modul, kein `loader.js`-Eintrag, kein
CSS-`@import`. Die Modulzahl bleibt bei 124.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `pushUndo()` Fehlerpfad → `redoStack` | Ein früher `return` lässt eine Historie stehen, die ab diesem Moment nicht mehr zum Zustand von `D` passt |
| `redoStack` → `redo()` → `Object.assign(D, parsed)` | Der Konsument wendet den Eintrag ungeprüft auf das aktuelle `D` an; alles, was seither geschah, ist danach fort |
| `pushUndo()` → Aufrufer (die destruktive Operation) | Der Aufrufer läuft laut D-06 bewusst weiter. Diese Entscheidung ist der Grund, warum der Fehlerpfad aufräumen MUSS statt bloß abzubrechen |
| geänderter Quellstand → `dist/*.html` → Nutzer | Das ausgelieferte Bundle ist das, was der Nutzer tatsächlich per Doppelklick öffnet; ein veraltetes Bundle macht jede geprüfte Korrektur wirkungslos |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-40 | Tampering | `pushUndo()` `catch`-Zweig lässt den Redo-Stack stehen (`systems/undo.js:19-23`) | medium | mitigate | Ein späteres Redo überschreibt stillschweigend eine Aktion, die der Nutzer nach dem gescheiterten Push ausgeführt hat — ohne Rückfrage und ohne Spur. Enges Fenster, aber echter Datenverlust. Task 2 leert den Stack im Fehlerpfad; Task 1 weist es zuvor rot nach |
| T-12-41 | Denial of Service | Ein Fix, der den Fehlerpfad blockierend macht (Wurf oder Abbruch des Aufrufers) | high | mitigate | Am Spieltisch darf das Undo-System das Spiel nicht anhalten (D-06). Task 1 Test K ist ausdrücklich die Gegenprobe und muss vor UND nach dem Fix grün sein; der Erfolgspfad bleibt unangetastet |
| T-12-42 | Repudiation | Der Warn-Toast entfällt oder wird verdrängt | medium | mitigate | Der Toast ist die einzige Rückmeldung, dass dieser eine Schritt ohne Undo-Schutz lief. Er steht im Fix ausdrücklich VOR dem Leeren, und Test K prüft ihn |
| T-12-43 | Spoofing | Veraltetes `dist/`-Bundle gibt sich als der geprüfte Quellstand aus | high | mitigate | Genau so ist W-1 entstanden: acht Tage altes Produktions-Bundle ohne die Korrekturen von vier Plänen, bei grüner Statusanzeige. Task 3 baut beide Bundles NACH allen drei Plänen dieser Runde und prüft die Zeitstempel gegen die drei geänderten Quelldateien |
| T-12-44 | Repudiation | Grüner Test ohne Bindung an den Produktionscode | medium | mitigate | Wiederkehrende Lehre dieser Phase. Die Tests laufen im bestehenden `vm.createContext()`-Block, der die echte `systems/undo.js` lädt; Task 2 verlangt den Mutationsnachweis mit protokollierten Testnamen |
| T-12-SC | Tampering | npm/pip/cargo-Installationen | n/a | accept | Dieser Plan installiert kein Paket; `package.json` und `requirements-dev.txt` bleiben unberührt. `build.py` und die Testläufer sind bereits installiert. Keine Legitimitätsprüfung nötig |

**ASVS-Ebene:** L1, Blockschwelle `high`. Die App hat keinen Server, keine Sitzung und keine
Authentifizierung — CSRF, Sitzungsfixierung und Transportsicherheit sind gegenstandslos und werden
bewusst NICHT aufgeführt. Schützenswert sind die Kampagnendaten des Nutzers im Speicher und in
localStorage/IndexedDB sowie die Integrität des ausgelieferten Bundles.
</threat_model>

<verification>
- `npx jest tests/unit/stability.test.js` grün, mindestens 75 bestandene Tests (Basislinie 73)
- `node --check systems/undo.js` ohne Befund
- `npx jest` grün, mindestens 760 bestandene Tests bei mindestens 29 Suiten (Basislinie 749)
- `python -m pytest tests/build -q` grün, mindestens 24 bestandene Tests
- `npx playwright test` grün, mindestens 321 bestanden / höchstens 2 übersprungen
- Beide `build.py`-Läufe mit Exit-Code 0 bei 124/124 Modulen
- Beide `dist/`-Bundles jünger als alle drei in dieser Runde geänderten Quelldateien
- Der Rot-Nachweis aus Task 1 und der Mutationsnachweis aus Task 2 sind protokolliert
</verification>

<success_criteria>
- WR-02 geschlossen: nach einem gescheiterten Undo-Push ist der Redo-Stack leer
- Die destruktive Aktion läuft weiterhin durch, der Warn-Toast erscheint weiterhin
- Alle vier Suiten liegen auf oder über der Basislinie
- Beide ausgelieferten Bundles tragen den vollständigen Quellstand aller drei Pläne dieser Runde
- Der offene menschliche Prüfpunkt ist als erneut fällig vermerkt, nicht als erledigt reklamiert
</success_criteria>

<output>
Create `.planning/phases/12-datensicherheit/12-11-SUMMARY.md` when done.

Die SUMMARY trägt einen `coverage:`-Block im Format der Pläne 12-01 bis 12-08 (Vorlage:
`12-06-SUMMARY.md`, Zeilen 50 ff.) mit diesen Einträgen:

- **D1** — „Nach Aktion → Undo → nicht serialisierbarem D → pushUndo() ist der Redo-Stack leer" ·
  requirement `SAFE-05` · `kind: unit`
- **D2** — „pushUndo() wirft im Fehlerfall nicht, der Aufrufer läuft weiter, kein Eintrag landet auf
  dem Undo-Stack, der Warn-Toast erscheint" · requirement `SAFE-05` · `kind: unit`
- **D3** — „Mutationsnachweis: bei entferntem Leeren im catch-Zweig fällt der benannte Test um" ·
  requirement `SAFE-05` · `kind: unit` · Testnamen im `ref` aufführen
- **D4** — „Volle Suiten über der Basislinie und beide dist-Bundles aus dem vollständigen
  Quellstand" · requirement `SAFE-05` · `kind: integration` · gemessene Zahlen im `ref`

Zusätzlich in die SUMMARY:
- Der protokollierte rote Lauf aus Task 1, Schritt 4 (Testname plus Fehlerzeile)
- Die gemessenen Suiten-Zahlen nach dem Lauf, gegen die Basislinien 749 / 24 / 321+2 / 124-124, und
  die Zeitstempel beider `dist/`-Bundles gegen die drei geänderten Quelldateien
- **Erinnerung (kein Task, bewusst offen):** der menschliche Prüfpunkt aus `12-VERIFICATION.md`
  `human_verification` — echte Audio-Bibliothek knapp unter 300 MiB, `dist/dnd-tracker-bundled.html`
  per Doppelklick, Divergenz-Banner, „Audio-Datei herunterladen". Unter Node/jsdom nicht messbar
  (Recherche-Annahme A1); der Nutzer hat sich am 2026-08-19 bewusst gegen das Zusammentragen der
  Dateien entschieden. Er wird nach dieser Runde erneut fällig und ist beim nächsten
  `/gsd-verify-work` vorzulegen
- **Hinweis für `REQUIREMENTS.md`:** SAFE-01 und SAFE-02 stehen dort noch mit „✓" aus der Zeit vor
  dem Code-Review vom 2026-09-04. Nach erfolgreicher Re-Verifikation dieser Runde ist die
  Traceability-Tabelle nachzuziehen (12-09 bei SAFE-01, 12-10 bei SAFE-02, 12-11 bei SAFE-05)
</output>
