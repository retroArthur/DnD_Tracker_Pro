---
phase: 12-datensicherheit
plan: 09
type: execute
wave: 7
depends_on: ["12-02"]
files_modified:
  - systems/migration/migration-wizard.js
  - tests/unit/migration-wizard.test.js
autonomous: true
requirements: [SAFE-01]
gap_closure: true
gap_ids: [CR-01, WR-01]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Ein Klick auf „Überspringen\" NACH einem abgeschlossenen Import nimmt denselben window.location.reload()-Pfad wie „App jetzt nutzen\" — das stale window.D erreicht den beforeunload-Autosave in systems/avatars.js nie"
    - "Wird der Wizard abgebrochen, NACHDEM der Import bereits in den Speicher geschrieben hat, ist garantiert, dass der Abbruchpfad den Speicher nicht auf den Vor-Import-Stand zurücksetzt (SAFE-01, Abbruch-/Nebenläufigkeitskante aus dem Edge-Probe-Bericht)"
    - "Vor dem Import (Schritte 1 bis 3) schließt „Überspringen\" den Wizard weiterhin ohne Reload — der Fix erkauft sich die Sicherheit nicht mit einem unmotivierten Neuladen"
    - "Ab Schritt 4 ist der Überspringen-Button gar nicht mehr sichtbar; vor Schritt 4 ist er es"
    - "Eine Audio-Export-Datei in der Haupt-Dropzone erzeugt Text-Rückmeldung und .file-ready am selben Element — der Nutzer sieht die Antwort dort, wo er die Datei abgelegt hat"
    - "Die Nachweise hängen am Produktionscode: bei zurückgenommenem Fix fallen benannte Tests um (Mutationsnachweis)"
  prohibitions:
    - "Der Skip-Pfad darf die frisch importierten Daten unter keinen Umständen stillschweigend verwerfen — der Umzug läuft laut 12-CONTEXT.md genau einmal, der Verlust wäre unwiederbringlich"
    - "Ein Reload darf NICHT ausgelöst werden, wenn kein Import gelaufen ist — der Nutzer verlöre außerhalb des Wizards ungespeicherte Arbeit, ohne dass ein Import das rechtfertigt"
    - "Der Wizard darf sich nach einem Skip nicht dauerhaft unerreichbar machen, ohne dass der Nutzer den Rückweg kennt (reopen-migration-wizard ist der einzige Weg zurück)"
  artifacts:
    - "systems/migration/migration-wizard.js — wizard-skip-Handler mit Reload-Zweig ab Schritt 4; showWizardStep() blendet den Wizard-Footer ab Schritt 4 aus; _processWizardFile() reicht die Audio-Dropzone weiter statt der Haupt-Dropzone"
    - "tests/unit/migration-wizard.test.js — Regressionstests für den Skip-nach-Import-Pfad, die Gegenprobe vor dem Import, die Footer-Sichtbarkeit und die Dropzone-Weiterleitung"
  key_links:
    - "_processWizardFile() → showWizardStep(4) → _wizardStep === 4 (die Zustandsvariable, an der der Skip-Handler den Reload-Zweig entscheidet)"
    - "_setupWizardActions() → Klick 'wizard-skip' → _wizardStep >= 4 → window.location.reload()"
    - "systems/avatars.js:170-176 beforeunload → StorageAPI.setJSON(key, D) — der Mechanismus, der ohne Reload das stale D zurückschreibt"
    - "showWizardStep(n) → #migration-wizard-footer display — dieselbe Technik, mit der der Audio-Bereich bereits ab Schritt 3 geschaltet wird"
    - "_processWizardFile() Audio-Weiche → _processWizardAudioFile(file, dropzone) → #migration-wizard-audio-status"
---

<objective>
Schließt **CR-01** (Blocker, `12-VERIFICATION.md` `gaps[0]`, Truth 7 FAILED) und **WR-01**
(Warnung, vom Nutzer für diesen Lauf ausdrücklich mit hineingenommen — kein Verifikations-Gap,
sondern ein per Nutzerentscheidung gezogener Review-Befund).

**CR-01 in einem Satz:** Der Footer-Button „Überspringen — ich starte neu"
(`data-action="wizard-skip"`, `migration-wizard.js:283-286`) wird AUSSERHALB aller
`.migration-step`-Container gerendert. `showWizardStep()` (`:152-177`) schaltet nur die
Schritt-Container, den Audio-Bereich und die Fortschritts-Punkte — den Footer nie. Der Button
bleibt damit auch auf Schritt 4 sichtbar, der Erfolgsbestätigung NACH einem bereits gelaufenen
`importFullExport()`. Sein Handler (`:629-632`) ruft nur `_closeWizard()` (`:655-658`, setzt
`modal.style.display = 'none'`), während `wizard-close` und `wizard-setup-backup` an derselben
Stelle bewusst `window.location.reload()` aufrufen — mit einem Quelltextkommentar, der genau diesen
Grund benennt („KEIN renderAll()/save() auf dem stale In-Memory-D — save() würde die frisch
importierte Aktiv-Kampagne mit dem leeren D überschreiben (CR-04)"). Nach einem Skip bleibt
`window.D` auf dem Vor-Import-Stand; der UNBEDINGTE `beforeunload`-Handler in
`systems/avatars.js:170-176` schreibt diesen Stand beim nächsten Tab-Schließen in denselben
Storage-Key zurück. Der Import ist dann rückstandslos rückgängig gemacht. Erschwerend: `wizard-skip`
setzt `migration-wizard-shown: {shown:true, skipped:true}`, sodass `initMigrationWizardIfNeeded()`
den Wizard nicht erneut anbietet.

**WR-01 in einem Satz:** `_processWizardFile()` (`:421-428`) erkennt eine versehentlich in die
HAUPT-Dropzone gezogene `audio-export-v1`-Datei korrekt und leitet an `_processWizardAudioFile()`
weiter — übergibt dabei aber die Haupt-Dropzone. `_processWizardAudioFile()` (`:538-547`) schreibt
seine Text-Rückmeldung immer nach `#migration-wizard-audio-status` (per `getElementById`, unabhängig
vom Parameter) und setzt `.file-ready` auf dem übergebenen, falschen Element. Kein Datenverlust —
aber die Haupt-Dropzone meldet „fertig", während die Antwort in einem anderen Bereich steht.

Purpose: Das Phasenziel lautet wörtlich „Kein Pfad in Backup, Export oder **Migration** verliert
oder überschreibt mehr stillschweigend Daten." CR-01 ist genau ein solcher Pfad, und er trifft den
einmaligen, unwiederholbaren Umzug — den gefährlichsten Moment, den diese App kennt.
Output: zwei kleine, lokale Quelltextänderungen plus das Testnetz, das den bisher unerreichbaren
Bedienpfad überhaupt erst erreichbar macht.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-datensicherheit/12-CONTEXT.md
@.planning/phases/12-datensicherheit/12-VERIFICATION.md
@.planning/phases/12-datensicherheit/12-REVIEW.md
@.planning/phases/12-datensicherheit/12-02-SUMMARY.md
@CLAUDE.md

@systems/migration/migration-wizard.js
@tests/unit/migration-wizard.test.js
@systems/avatars.js
</context>

<scope_fence>
Dieser Plan schließt GENAU zwei Befunde: CR-01 und WR-01. Beide liegen in
`systems/migration/migration-wizard.js`; sie teilen sich deshalb einen Plan, damit nicht zwei
Executoren derselben Welle um dieselbe Datei streiten.

**Nicht Teil dieses Plans** — auch wenn es beim Lesen naheliegt:
- **Kein Eingriff in `systems/avatars.js`.** Der unbedingte `beforeunload`-Autosave ist die zweite
  Hälfte des Mechanismus, aber nicht der Fehler: er tut genau das, wofür er gebaut wurde. Ihn zu
  bedingen wäre ein Eingriff in die Persistenz-Garantie der gesamten App und gehört nicht in einen
  Lückenplan. Er steht in `<read_first>`, damit der Executor den Mechanismus versteht — nicht,
  damit er ihn anfasst.
- **Kein Umbau des Wizard-Layouts, der Schrittfolge oder des Divergenz-Banners.**
- **Keine Änderung an `importFullExport()` / `full-export.js`.**
- **Kein Eingriff in `hasCampaignContent()`/`isFreshInstall()`** — G-12-3 ist mit Plan 12-08
  abgenommen und wird hier nicht erneut angefasst.
- **Der Wiedereinstieg über `reopen-migration-wizard` wird nicht neu gebaut.** Dass ein Skip den
  Wizard dauerhaft stumm stellt, ist als Verbot in `must_haves.prohibitions` festgehalten
  (deskriptorlos, damit es sichtbar unverifiziert bleibt) — nicht stillschweigend fallen gelassen,
  aber auch nicht in diesem Plan behoben.
</scope_fence>

<tracer_note>
`TRACER_MODE` ist an, dieser Plan enthält aber bewusst **keinen** `type="tracer"`-Plan und keine
Tracer-Aufgabe. Grund: Phase 12 ist ausgeführt und verifiziert; der durchgehende Schnitt
„Export → Datei → Import → Kampagne spielt" steht seit Plan 12-02 und ist in Truth 1 als VERIFIED
abgenommen. Ein neuer Durchstich würde nichts beweisen, was nicht schon bewiesen ist.

Der Geist der Regel wird trotzdem eingehalten: **Task 1 ist der Durchstich durch den kaputten
Pfad** — echter `_processWizardFile()`-Import bis Schritt 4, dann der Klick auf `wizard-skip`, dann
die Beobachtung am Ende der Kette. Und er wird ROT nachgewiesen, BEVOR Task 2 den Fix schreibt.
</tracer_note>

<reversibility_note>
`REVERSIBILITY_GATES` ist an. Alle Entscheidungen dieses Plans sind mit `reversible` bewertet:
drei lokale Änderungen in einer Datei, kein veröffentlichter Vertrag, kein Datenformat, keine
Migration, kein Schema. Keine Bewertung fällt auf `one-way` — deshalb enthält dieser Plan
**keinen** `checkpoint:decision`, und `autonomous` steht auf `true`. Das ist eine ausdrückliche
Feststellung, keine Auslassung.
</reversibility_note>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Roter Durchstich — Klick auf „Überspringen" nach abgeschlossenem Import</name>
  <files>tests/unit/migration-wizard.test.js</files>
  <read_first>
    - `tests/unit/migration-wizard.test.js` — die zu ändernde Datei. Besonders: der geteilte
      `context`-Aufbau (Zeilen 78-105), der `beforeEach`-Reset (Zeilen 107-135) und der bestehende
      `describe('_processWizardFile() — Bestandsschutz-Dialog bleibt erhalten…')` (Zeilen 307-383).
      Der zweite Test dort („vorhandene Daten + Bestaetigung -> Reihenfolge saveUndoState VOR
      importFullExport bleibt erhalten", Zeilen 355-382) ist die Vorlage für den erfolgreichen
      Import in diesem Task — abschreiben, nicht neu erfinden.
    - `systems/migration/migration-wizard.js` — `_setupWizardActions()` (Zeilen 615-653) mit dem
      `modal.dataset.actionsBound`-Guard und den vier Handler-Zweigen; `_closeWizard()`
      (Zeilen 655-658); `showWizardStep()` (Zeilen 152-177) — dort steht `_wizardStep = n` VOR der
      Modal-Prüfung, deshalb ist `_wizardStep` auch bei fehlendem Modal korrekt gesetzt;
      `_processWizardFile()` (Zeilen 405-530) mit `showWizardStep(4)` am Ende des Erfolgszweigs.
    - `systems/avatars.js` Zeilen 168-177 — der unbedingte `beforeunload`-Handler
      (`StorageAPI.setJSON(key, D)`). Er ist der Grund, warum „nur das Modal ausblenden" hier
      Datenverlust bedeutet; er wird NICHT geändert.
  </read_first>
  <reversibility rating="reversible">Reine Testdatei-Ergänzung ohne Produktionswirkung; durch Löschen des describe-Blocks vollständig zurückzunehmen.</reversibility>
  <behavior>
    Mindestens drei neue Tests in einem eigenen `describe`-Block. Alle drei tragen die Kennung
    `CR-01` im Testnamen, damit der `-t`-Filter der Verify-Kommandos sie trifft.

    - **Test A (der Durchstich):** echter `_processWizardFile()`-Lauf mit einer gültigen
      `full-v1`-Datei bis zum Erfolg (`_wizardStep` steht danach auf 4). Danach der eingefangene
      Klick-Handler mit `{ target: { dataset: { action: 'wizard-skip' } } }`. Erwartung:
      `window.location.reload` wurde genau einmal aufgerufen. Vor dem Fix ist das FALSCH — der
      Handler ruft nur `_closeWizard()`.
    - **Test B (die Gegenprobe vor dem Import):** `showWizardStep(2)`, dann derselbe Skip-Klick.
      Erwartung: `window.location.reload` wurde NICHT aufgerufen, und
      `StorageAPI.setJSON('migration-wizard-shown', …)` wurde mit einem Objekt aufgerufen, das
      `skipped: true` trägt. Dieser Test verhindert den Gegenfehler „immer reloaden" und ist vor dem
      Fix bereits grün — er darf es nach dem Fix bleiben. Sein Name enthält zusätzlich die
      Wortfolge `vor dem Import`, damit das zweite Verify-Kommando ihn gezielt greifen kann.
    - **Test C (Footer-Sichtbarkeit):** `showWizardStep(3)` → das Footer-Element ist sichtbar
      (`style.display` ist der leere String); `showWizardStep(4)` → es ist ausgeblendet
      (`style.display === 'none'`). Vor dem Fix ist die zweite Hälfte FALSCH.
  </behavior>
  <action>
    Nur die Testdatei. Kein Produktionscode in diesem Task — der kommt in Task 2, und die Trennung
    ist hier nicht dekorativ: der Rot-Nachweis am Ende dieses Tasks ist nur belastbar, wenn der Fix
    noch nicht geschrieben ist.

    **Schritt 1 — den Klick-Handler einfangen.** `_setupWizardActions()` registriert den Handler mit
    `modal.addEventListener('click', fn)` und schützt sich mit `if (modal.dataset.actionsBound)
    return;`. Also je Test ein FRISCHES Attrappen-Modal bauen — ein Objekt mit leerem `dataset`,
    einem `addEventListener(typ, fn)`, das `fn` in eine lokale Variable schreibt, sowie
    `querySelectorAll(sel)` und `querySelector(sel)`. Ein wiederverwendetes Attrappen-Modal liefe in
    den Guard und der Handler bliebe leer; das wäre ein grüner Test ohne Gegenstand. Danach
    `context._setupWizardActions(attrappe)` aufrufen und den eingefangenen Handler direkt mit einem
    Ereignis-Attrappen-Objekt aufrufen. Alle drei internen Funktionen sind im vm-Kontext als
    `context._setupWizardActions`, `context.showWizardStep` und `context._processWizardFile`
    erreichbar (Top-Level-Funktionsdeklarationen werden im vm-Kontext zu Eigenschaften des globalen
    Objekts — dasselbe Muster nutzen die bestehenden Tests bereits für `_processWizardFile`).

    **Schritt 2 — `reload` beobachtbar machen.** Der `beforeEach`-Reset setzt
    `context.window.location = { protocol: 'https:' }`. Im neuen Block auf
    `{ protocol: 'https:', reload: jest.fn() }` erweitern, damit der Produktionsaufruf
    `window.location.reload()` nicht wirft und zählbar wird.

    **Schritt 3 — das DOM soweit stellen, wie die Funktionen es brauchen.** `context.document`
    liefert im neuen Block ein `getElementById`, das `migration-wizard-error`,
    `migration-wizard-filename`, `migration-wizard-result` (wie im bestehenden Block, Zeilen
    316-325) UND `migration-wizard-modal` bedient — letzteres mit derselben Attrappe, die auch
    `_setupWizardActions()` bekommen hat, damit `showWizardStep()` nicht an der Modal-Prüfung
    abbricht. Die Attrappe braucht `querySelectorAll` für `.migration-step` und `.wizard-step`
    (leere Listen genügen, solange `forEach` existiert) und `querySelector` für
    `#migration-wizard-audio-section` und `#migration-wizard-footer` — letzteres liefert ein Objekt
    mit `style: { display: '' }`, an dem Test C misst.

    **Schritt 4 — Test A auf einem ECHTEN Import aufsetzen.** Nicht `showWizardStep(4)` aufrufen und
    behaupten, das sei dasselbe. Der Verifier verlangt ausdrücklich den Nachweis „nach einem
    ABGESCHLOSSENEN Import", nicht „der Handler existiert". Also: `mockConfirm` auf `true`,
    `context.window.saveUndoState` und `context.window.importFullExport` als `jest.fn()` mit
    Rückgabe `{ totalBytes: 4096, campaignCount: 1 }`, eine `global.File` mit
    `{ _exportType: 'full-v1', campaigns: { c1: {} } }`, dann `context._processWizardFile(file,
    dropzone)` und mit dem vorhandenen `waitFor()`-Helfer auf den Abschluss warten. Erst danach den
    Skip-Klick auslösen.

    **Schritt 5 — den roten Lauf protokollieren.** Das erste Verify-Kommando dieses Tasks
    ausführen. Test A und Test C MÜSSEN fehlschlagen, Test B muss bestehen. Die Testnamen und die
    Fehlerzeilen für die SUMMARY festhalten. Erst danach beginnt Task 2.
  </action>
  <verify>
    <automated>npx jest tests/unit/migration-wizard.test.js -t "CR-01" || echo RED_CONFIRMED_CR01</automated>
    <fails_when>Die Ausgabe enthält die Zeichenkette `RED_CONFIRMED_CR01` nicht (dann war die gefilterte Suite grün, obwohl der Fix noch fehlt — der Test hängt nicht am Produktionscode); oder die Jest-Zusammenfassung meldet `0 total` bzw. `Tests: 0` (der `-t`-Filter hat keinen einzigen Test getroffen, es wurde also nichts geprüft).</fails_when>
  </verify>
  <verify>
    <automated>npx jest tests/unit/migration-wizard.test.js -t "vor dem Import"</automated>
    <fails_when>Exit-Code ≠ 0 (die Gegenprobe Test B muss schon VOR dem Fix bestehen — schlägt sie fehl, misst sie etwas anderes als beabsichtigt); oder die Zusammenfassung meldet `0 total`.</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/unit/migration-wizard.test.js` enthält einen neuen `describe`-Block mit mindestens drei
      Tests, deren Namen die Kennung `CR-01` tragen.
    - Das erste Verify-Kommando dieses Tasks meldet die Bestätigungszeichenkette, und die
      Jest-Ausgabe nennt Test A (Skip nach Import) und Test C (Footer ab Schritt 4) als
      fehlgeschlagen.
    - Test A ruft `context._processWizardFile()` auf und wartet dessen Abschluss ab, bevor der
      Skip-Klick ausgelöst wird — er setzt `_wizardStep` NICHT per `showWizardStep(4)` von Hand.
    - Test B besteht bereits vor dem Fix (zweites Verify-Kommando, Exit-Code 0).
    - Die fehlgeschlagenen Testnamen und Fehlerzeilen sind für die SUMMARY notiert.
  </acceptance_criteria>
  <done>
    Der kaputte Bedienpfad ist zum ersten Mal von einem Test erreichbar, und er ist nachweislich rot.
  </done>
</task>

<task type="auto">
  <name>Task 2: Der Fix — Skip nimmt ab Schritt 4 den Reload-Pfad, und der Footer verschwindet dort</name>
  <files>systems/migration/migration-wizard.js</files>
  <read_first>
    - `systems/migration/migration-wizard.js` — die zu ändernde Datei. Drei Stellen:
      der Footer im Template (Zeilen 283-288), `showWizardStep()` (Zeilen 152-177) mit dem
      bestehenden Audio-Bereich-Schalter als Vorlage, und `_setupWizardActions()` (Zeilen 626-652)
      mit dem `wizard-close`-Zweig (Zeilen 633-639) als Referenzimplementierung — dessen Kommentar
      begründet den Reload bereits vollständig.
    - `tests/unit/migration-wizard.test.js` — der in Task 1 geschriebene Block, damit die
      Änderung gegen die dort formulierte Erwartung gebaut wird und nicht daneben.
    - `systems/avatars.js` Zeilen 168-177 — warum ein bloßes `_closeWizard()` hier Datenverlust ist.
  </read_first>
  <reversibility rating="reversible">Drei lokale Änderungen in einer Datei, kein veröffentlichter Vertrag, keine Datenmigration, kein Format-Wechsel; ein git revert stellt den Vorzustand her.</reversibility>
  <action>
    **Schritt 1 — den Footer adressierbar machen.** Dem Footer-Container im Template
    (`<div class="migration-wizard-footer">`, Zeile 283) das Attribut `id="migration-wizard-footer"`
    geben. Die Klasse bleibt unverändert stehen — `assets/styles/migration.css:208` hängt daran und
    wird nicht angefasst.

    **Schritt 2 — `showWizardStep()` schaltet den Footer.** In `showWizardStep(n)` direkt hinter dem
    bestehenden Audio-Bereich-Block einen zweiten Block derselben Bauart ergänzen: das Element über
    `modal.querySelector('#migration-wizard-footer')` holen, defensiv auf Existenz prüfen und
    `style.display` auf `'none'` setzen, sobald `n >= 4`, sonst auf den leeren String. Die
    Schwelle ist `>= 4`, nicht `=== 4`: der Erfolgsschritt ist heute der letzte, aber eine künftige
    Erweiterung um Schritt 5 darf den Button nicht versehentlich wieder einblenden.

    **Schritt 3 — der Reload-Zweig im Skip-Handler.** Im `wizard-skip`-Zweig von
    `_setupWizardActions()` bleibt das Setzen von `migration-wizard-shown` mit
    `{ shown: true, skipped: true }` als ERSTE Anweisung unverändert stehen. Danach: wenn
    `_wizardStep >= 4`, `window.location.reload()` aufrufen und den Zweig mit `return` verlassen;
    andernfalls wie bisher `_closeWizard()`. Der begründende Kommentar gehört daneben und muss
    denselben Sachverhalt benennen wie der bestehende Kommentar im `wizard-close`-Zweig: der Import
    ist bereits gelaufen, `window.D` steht auf dem Vor-Import-Stand, und der unbedingte
    `beforeunload`-Autosave in `systems/avatars.js` würde ihn ohne Neuladen in denselben Storage-Key
    zurückschreiben.

    Keine neue Top-Level-Deklaration in diesem Task — es werden nur bestehende Funktionskörper
    erweitert. Damit ist die Kollisionsprüfung `check_duplicate_functions()` hier kein Thema.
    Ebenso: kein `const x = window.x` (CLAUDE.md-Dedup-Regel).

    **Schritt 4 — Mutationsnachweis.** Den Reload-Zweig testweise wirkungslos machen (den
    `_wizardStep`-Vergleich vorübergehend auf einen nie erfüllten Wert setzen), das erste
    Verify-Kommando dieses Tasks laufen lassen, die fehlschlagenden Testnamen notieren, Mutation
    zurücknehmen, erneut grün. Ohne diesen Nachweis ist offen, ob der Test überhaupt am
    Produktionscode hängt — die wiederkehrende Lehre dieser Phase (T-12-31). Ergebnis in die
    SUMMARY.
  </action>
  <verify>
    <automated>npx jest tests/unit/migration-wizard.test.js</automated>
    <fails_when>Exit-Code ≠ 0; oder die Zusammenfassung meldet weniger als `44 passed` (Basislinie 41, gemessen am 2026-09-04, plus mindestens 3 neue aus Task 1); oder in der Ausgabe erscheint eine Zeile, die mit `✕` oder `×` beginnt.</fails_when>
  </verify>
  <verify>
    <automated>node --check systems/migration/migration-wizard.js</automated>
    <fails_when>Exit-Code ≠ 0 oder die Ausgabe enthält `SyntaxError`.</fails_when>
  </verify>
  <verify>
    <automated>grep -n -A6 "action === 'wizard-skip'" systems/migration/migration-wizard.js</automated>
    <fails_when>Exit-Code ≠ 0 (kein Treffer — der Handler-Zweig wurde umbenannt oder entfernt); oder in den sechs Folgezeilen fehlt sowohl `window.location.reload()` als auch `_wizardStep`, dann steht der Reload-Zweig nicht im Skip-Handler, sondern woanders.</fails_when>
  </verify>
  <acceptance_criteria>
    - Der `wizard-skip`-Zweig in `_setupWizardActions()` ruft `window.location.reload()` genau dann
      auf, wenn `_wizardStep >= 4`, und `_closeWizard()` genau dann, wenn nicht.
    - `StorageAPI.setJSON('migration-wizard-shown', { shown: true, skipped: true })` läuft in BEIDEN
      Fällen und steht weiterhin vor der Fallunterscheidung.
    - `showWizardStep(4)` setzt `style.display` des Elements mit der Id `migration-wizard-footer` auf
      `'none'`; `showWizardStep(3)` setzt es auf den leeren String.
    - Der Footer-Container im Template trägt die Id `migration-wizard-footer` und behält die Klasse
      `migration-wizard-footer`.
    - `npx jest tests/unit/migration-wizard.test.js` ist grün mit mindestens 44 bestandenen Tests.
    - Mutationsnachweis protokolliert: bei wirkungslos gemachtem Reload-Zweig fallen benannte Tests
      um.
  </acceptance_criteria>
  <done>
    „Überspringen" nach einem abgeschlossenen Import kann die importierten Daten nicht mehr
    stillschweigend verwerfen, und der Button ist dort gar nicht mehr sichtbar.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WR-01 — die Rückmeldung erscheint dort, wo der Nutzer die Datei abgelegt hat</name>
  <files>systems/migration/migration-wizard.js, tests/unit/migration-wizard.test.js</files>
  <read_first>
    - `systems/migration/migration-wizard.js` — die Audio-Weiche in `_processWizardFile()`
      (Zeilen 421-428) und `_processWizardAudioFile()` (Zeilen 538-560), insbesondere die lokale
      `showStatus()`-Funktion, die `statusEl` über `getElementById('migration-wizard-audio-status')`
      auflöst und `.file-ready` auf dem ÜBERGEBENEN `dropzone`-Parameter setzt bzw. entfernt.
    - `systems/migration/migration-wizard.js` Zeilen 272-280 — die Audio-Dropzone im Template
      (`id="migration-wizard-audio-dropzone"`), also das Element, das eigentlich gemeint ist.
    - `tests/unit/migration-wizard.test.js` — der bestehende `_processWizardFile()`-Block als
      Vorlage für den `document.getElementById`-Attrappen-Aufbau und den `waitFor()`-Helfer.
  </read_first>
  <reversibility rating="reversible">Eine Zeile Auflösungslogik plus Test; ohne Datenwirkung, ohne Formatänderung.</reversibility>
  <behavior>
    - Test D: Eine Datei mit `_exportType: 'audio-export-v1'` wird an
      `_processWizardFile(file, hauptDropzone)` übergeben, während `document.getElementById` für
      `migration-wizard-audio-dropzone` ein eigenes Attrappen-Element liefert. Erwartung: nach dem
      Import trägt die AUDIO-Dropzone `.file-ready`, die HAUPT-Dropzone nicht.
    - Test E (der Rückfall): dieselbe Lage, aber `getElementById` liefert für die Audio-Dropzone
      `null`. Erwartung: kein Wurf, und die Haupt-Dropzone wird wie bisher benutzt — die Funktion
      darf ohne das Element nicht abstürzen.
    - Beide Testnamen tragen die Kennung `WR-01`.
  </behavior>
  <action>
    **Schritt 1 — roter Test zuerst.** Test D und Test E in `tests/unit/migration-wizard.test.js`
    ergänzen. Der Import-Aufruf ist zu attrappieren: `context.window.importAudioExport` als
    `jest.fn()`, das ein Objekt mit `imported` und einer leeren `skipped`-Liste zurückgibt.
    Zusätzlich muss `getElementById` für `migration-wizard-audio-status` ein Element mit
    `textContent`, `style` und `classList.toggle` liefern, sonst läuft `showStatus()` ins Leere.
    Auf den asynchronen `reader.onload`-Zweig mit dem vorhandenen `waitFor()`-Helfer warten. Lauf
    bestätigen: Test D muss vor dem Fix fehlschlagen. Ergebnis für die SUMMARY notieren.

    **Schritt 2 — der Fix.** In der Audio-Weiche von `_processWizardFile()` (direkt nach
    `clearError()`) die tatsächliche Audio-Dropzone über
    `document.getElementById('migration-wizard-audio-dropzone')` auflösen und beim Rückfall auf
    einen fehlenden Wert die übergebene `dropzone` weiterverwenden. Das aufgelöste Element an
    `_processWizardAudioFile()` weiterreichen. `_processWizardAudioFile()` selbst bleibt
    unverändert — es löst `#migration-wizard-audio-status` bereits selbst auf und ist damit
    konsistent, sobald es das passende Dropzone-Element bekommt.

    Der Rückfall ist nicht Beiwerk: `_processWizardAudioFile()` wird auch direkt aus dem
    Audio-Bereich heraus aufgerufen, und ein fehlendes Element darf den optionalen Audio-Import
    unter keinen Umständen zum Absturz bringen — er blockiert laut D-02 den Hauptimport zu keinem
    Zeitpunkt.
  </action>
  <verify>
    <automated>npx jest tests/unit/migration-wizard.test.js</automated>
    <fails_when>Exit-Code ≠ 0; oder die Zusammenfassung meldet weniger als `46 passed` (44 nach Task 2 plus mindestens 2 neue); oder in der Ausgabe erscheint eine Zeile, die mit `✕` oder `×` beginnt.</fails_when>
  </verify>
  <verify>
    <automated>node --check systems/migration/migration-wizard.js</automated>
    <fails_when>Exit-Code ≠ 0 oder die Ausgabe enthält `SyntaxError`.</fails_when>
  </verify>
  <acceptance_criteria>
    - `_processWizardFile()` reicht an `_processWizardAudioFile()` das Element mit der Id
      `migration-wizard-audio-dropzone` weiter, sofern es existiert.
    - Fehlt das Element, wird die übergebene Haupt-Dropzone benutzt und es wird nicht geworfen.
    - Test D schlug vor dem Fix fehl und besteht danach; beide Läufe sind notiert.
    - `npx jest tests/unit/migration-wizard.test.js` ist grün mit mindestens 46 bestandenen Tests.
  </acceptance_criteria>
  <done>
    Eine Audio-Datei in der Haupt-Dropzone markiert die Audio-Dropzone als fertig — dort, wo auch
    die Text-Rückmeldung steht.
  </done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

**Geänderte Quelldateien**

| Datei | Symbol / Stelle | Art der Änderung |
|---|---|---|
| `systems/migration/migration-wizard.js` | Footer-Container im Wizard-Template (Zeile 283) | neues Attribut `id="migration-wizard-footer"` |
| `systems/migration/migration-wizard.js` | `showWizardStep(n)` | neuer Block: Footer wird ab `n >= 4` ausgeblendet |
| `systems/migration/migration-wizard.js` | `_setupWizardActions()`, Zweig `wizard-skip` | neuer Reload-Zweig für `_wizardStep >= 4` |
| `systems/migration/migration-wizard.js` | `_processWizardFile()`, Audio-Weiche | Auflösung von `migration-wizard-audio-dropzone` mit Rückfall auf die übergebene Dropzone |

**Keine neuen Top-Level-Deklarationen.** Es entstehen keine neuen Funktionen, Konstanten oder
Klassen — damit kein Kollisionsrisiko für `check_duplicate_functions()`.

**Keine neuen `window.*`-Exports.** `showWizardStep` ist bereits als `window.showWizardStep`
exportiert (Zeile 950); `_setupWizardActions`, `_closeWizard`, `_processWizardFile` und
`_processWizardAudioFile` bleiben modul-intern und sind im Test über den vm-Kontext erreichbar.

**Neue Tests** (`tests/unit/migration-wizard.test.js`, alle mit der Kennung im Namen)

| Kennung | Testinhalt |
|---|---|
| `CR-01` Test A | Skip nach abgeschlossenem `_processWizardFile()`-Import löst `window.location.reload()` aus |
| `CR-01` Test B | Skip vor dem Import (Schritt 2) löst KEINEN Reload aus und setzt weiterhin `skipped: true` |
| `CR-01` Test C | Footer ist auf Schritt 3 sichtbar und auf Schritt 4 ausgeblendet |
| `WR-01` Test D | Audio-Datei in der Haupt-Dropzone markiert die Audio-Dropzone als `.file-ready` |
| `WR-01` Test E | Fehlende Audio-Dropzone: Rückfall auf die Haupt-Dropzone, kein Wurf |

**Nicht erzeugt:** keine neue Datei, kein neues Modul, kein `loader.js`-Eintrag, kein
CSS-`@import` — dieser Plan behebt Defekte in bestehenden Dateien. `dist/*.html` ist Build-Ausgabe
und wird in diesem Plan weder geschrieben noch angefasst (der Neubau beider Bundles liegt in
Plan 12-11, Task 3).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frisch importierte Kampagnendaten in localStorage ↔ stale `window.D` im Speicher | `importFullExport()` schreibt die Storage-Keys direkt; `window.D` bleibt bis zum Reload der Vor-Import-Stand. Jeder Pfad, der `D` zurückschreibt, ist ein Angriff auf das Import-Ergebnis |
| Wizard-Bedienpfad → `beforeunload`-Autosave (`systems/avatars.js:170-176`) | Der Handler feuert unbedingt bei jedem Tab-Schließen und schreibt `D` in den aktiven Key. Er ist die Waffe; der fehlende Reload ist der Abzug |
| vom Nutzer abgelegte Datei → `_processWizardFile()` / `_processWizardAudioFile()` | Nicht vertrauenswürdiger Dateiinhalt kreuzt hier die Grenze; das Format wird per `_exportType` unterschieden |
| `_wizardStep` (Modulzustand) → Reload-Entscheidung | Ein einziger Zahlenvergleich entscheidet zwischen „Modal ausblenden" und „App neu laden" |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-29 | Tampering | `wizard-skip`-Handler nach abgeschlossenem Import (`migration-wizard.js:629-632`) | critical | mitigate | Der Nutzer verliert den kompletten, einmaligen Umzug ohne jede Rückmeldung — der `beforeunload`-Autosave macht den Verlust beim nächsten Tab-Schließen praktisch sicher. Task 2 führt den Reload-Zweig für `_wizardStep >= 4` ein; Task 1 nagelt ihn mit einem Test fest, der auf einem ECHTEN Import aufsetzt und bei zurückgenommenem Fix umfällt |
| T-12-30 | Denial of Service | Zu breite Reload-Bedingung (Reload auch vor dem Import) | high | mitigate | Ein Reload auf Schritt 1-3 verwürfe ungespeicherte Arbeit außerhalb des Wizards, ohne dass ein Import das rechtfertigt — der Gegenfehler, den ein naiver Test bestünde. Task 1 Test B ist ausdrücklich die Gegenprobe und muss vor UND nach dem Fix grün sein |
| T-12-31 | Repudiation | Grüner Test ohne Bindung an den Produktionscode | medium | mitigate | Wiederkehrende Lehre dieser Phase (`08-LEARNINGS.md`, `11-LEARNINGS.md`). Der Test lädt die echte Quelle über `vm.createContext()` und fängt den echten, von `_setupWizardActions()` registrierten Handler ein; Task 2 verlangt den Mutationsnachweis mit protokollierten Testnamen |
| T-12-32 | Denial of Service | Footer-Ausblendung greift zu früh | medium | mitigate | Würde der Footer schon ab Schritt 1 verschwinden, käme ein Nutzer ohne Umzugsdatei aus dem Wizard nicht mehr heraus. Task 1 Test C misst beide Seiten der Schwelle (Schritt 3 sichtbar, Schritt 4 aus) |
| T-12-33 | Spoofing | Haupt-Dropzone meldet einen Erfolgszustand, den sie nicht erreicht hat (WR-01) | low | mitigate | Kein Datenverlust; das Element gibt aber einen fremden Zustand als seinen eigenen aus, und die echte Rückmeldung steht möglicherweise außerhalb des Blickfelds. Task 3 löst die richtige Dropzone auf, mit Rückfall auf die übergebene |
| T-12-34 | Repudiation | Skip stellt den Wizard dauerhaft stumm (`migration-wizard-shown: {skipped:true}`) | low | accept | Bewusst nicht behoben: der Wiedereinstieg über `reopen-migration-wizard` existiert, ist dem Nutzer aber nicht angezeigt. Nach dem Fix aus Task 2 ist der Fall entschärft (nach einem Import wird ohnehin neu geladen). Als deskriptorloses Verbot in `must_haves.prohibitions` festgehalten, damit es sichtbar unverifiziert bleibt statt still zu verschwinden |
| T-12-SC | Tampering | npm/pip/cargo-Installationen | n/a | accept | Dieser Plan installiert kein Paket; `package.json` und `requirements-dev.txt` bleiben unberührt. Keine Legitimitätsprüfung nötig |

**ASVS-Ebene:** L1, Blockschwelle `high`. Diese App hat keinen Server, keine Sitzung und keine
Authentifizierung — CSRF, Sitzungsfixierung und Transportsicherheit sind hier gegenstandslos und
werden bewusst NICHT aufgeführt. Das schützenswerte Gut sind die Kampagnendaten des Nutzers in
localStorage/IndexedDB; CR-01 ist ein Integritätsangriff genau darauf.
</threat_model>

<verification>
- `npx jest tests/unit/migration-wizard.test.js` grün, mindestens 46 bestandene Tests
  (Basislinie 41, gemessen am 2026-09-04)
- `node --check systems/migration/migration-wizard.js` ohne Befund
- Der Rot-Nachweis aus Task 1 ist protokolliert (Testnamen plus Fehlerzeilen)
- Der Mutationsnachweis aus Task 2 ist protokolliert (Testnamen, die bei wirkungslosem Reload-Zweig
  umfallen)
- Die volle Suite, die Build-Tests und beide `dist`-Bundles laufen NICHT in diesem Plan, sondern
  gesammelt in Plan 12-11, Task 3 — nach den Fixes aller drei Pläne dieser Runde
</verification>

<success_criteria>
- CR-01 geschlossen: „Überspringen" nach einem abgeschlossenen Import lädt neu, statt das stale `D`
  zurückschreiben zu lassen
- Der Button ist ab Schritt 4 gar nicht mehr sichtbar
- Vor dem Import verhält sich „Überspringen" unverändert (kein unmotivierter Reload)
- WR-01 geschlossen: die Rückmeldung erscheint am selben Element, das als fertig markiert wird
- Beide Nachweise hängen belegbar am Produktionscode (roter Lauf vorher, Mutationsnachweis nachher)
</success_criteria>

<output>
Create `.planning/phases/12-datensicherheit/12-09-SUMMARY.md` when done.

Die SUMMARY trägt einen `coverage:`-Block im Format der Pläne 12-01 bis 12-08 (Vorlage:
`12-06-SUMMARY.md`, Zeilen 50 ff.) mit diesen Einträgen:

- **D1** — „Klick auf wizard-skip nach abgeschlossenem _processWizardFile()-Import löst
  window.location.reload() aus" · requirement `SAFE-01` · `kind: unit`
- **D2** — „Klick auf wizard-skip vor dem Import (Schritt 2) löst keinen Reload aus und setzt
  weiterhin skipped: true" · requirement `SAFE-01` · `kind: unit`
- **D3** — „Der Wizard-Footer ist auf Schritt 3 sichtbar und ab Schritt 4 ausgeblendet" ·
  requirement `SAFE-01` · `kind: unit`
- **D4** — „Eine Audio-Export-Datei in der Haupt-Dropzone markiert die Audio-Dropzone als
  file-ready; fehlt sie, greift der Rückfall ohne Wurf" · requirement `SAFE-01` · `kind: unit`
- **D5** — „Mutationsnachweis: bei wirkungslosem Reload-Zweig fallen benannte Tests um" ·
  requirement `SAFE-01` · `kind: unit` · Testnamen im `ref` aufführen

Zusätzlich in die SUMMARY:
- Der protokollierte rote Lauf aus Task 1, Schritt 5 (Testnamen plus Fehlerzeilen) und aus Task 3,
  Schritt 1
- Die gemessene Testzahl der Datei nach dem Lauf, gegen die Basislinie 41
- Der Hinweis, dass T-12-34 (Skip stellt den Wizard stumm) bewusst offen bleibt und als
  deskriptorloses Verbot geführt wird
- Der Hinweis auf den weiterhin offenen menschlichen Prüfpunkt aus `12-VERIFICATION.md`
  (`human_verification`: Audio-Bibliothek knapp unter 300 MiB im echten Browser). Er ist nicht
  Gegenstand dieses Plans, wird durch die Fixes dieser Runde aber erneut fällig
</output>
