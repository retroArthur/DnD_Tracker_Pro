---
phase: 12-datensicherheit
plan: 17
type: execute
wave: 11
depends_on: ["12-12", "12-13", "12-14", "12-15", "12-16"]
files_modified:
  - dist/dnd-tracker-bundled.html
  - dist/dnd-tracker-optimized.html
autonomous: true
requirements: [SAFE-01, SAFE-02, SAFE-04, SAFE-05]
gap_closure: true
gap_ids: [SEC-01, SEC-02, SEC-03, SEC-04, SEC-05, SEC-06, SEC-07]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Die volle Jest-Suite ist grün und liegt nicht unter der Basislinie von 842 bestandenen Tests; kein Test wird als unerwartet bestanden gemeldet"
    - "Die Build-Tests (python -m pytest tests/build) sind grün — insbesondere die Prüfungen auf doppelte Top-Level-Deklarationen, die von den drei neuen Funktionen dieser Runde berührt werden"
    - "Beide dist-Bundles sind aus dem Quellstand ALLER fünf Fix-Pläne dieser Runde gebaut und jünger als jede geänderte Quelldatei"
    - "Die volle Playwright-Suite ist grün und liegt nicht unter 321 bestandenen und höchstens 2 übersprungenen Tests"
    - "Keine der beiden test.failing-Verankerungen dieser Runde steht noch auf test.failing"
  prohibitions:
    - "Keine Quelltextänderung in diesem Plan — er baut und misst. Zeigt eine Suite einen echten Fehler, wird er hier BENANNT und nicht nebenbei repariert; die Korrektur gehört in den Plan, dem die Datei gehört"
    - "Kein Bundle darf geschrieben werden, wenn build.py eine Kollision meldet — die Abbruchmeldung ist das Ergebnis, nicht ein Hindernis"
    - "Playwright läuft gegen dist/ — ein Lauf vor dem Neubau misst den alten Stand und ist wertlos"
  artifacts:
    - "dist/dnd-tracker-bundled.html — Entwicklungs-Bundle aus dem vollständigen Quellstand dieser Runde"
    - "dist/dnd-tracker-optimized.html — Produktions-Bundle aus demselben Stand"
  key_links:
    - "geänderte Quellen aus 12-12 bis 12-16 → loader.js MODULES → build.py → dist/*.html"
    - "build.py check_duplicate_functions() → die drei neuen Top-Level-Deklarationen dieser Runde (_hatKampagnenInhalt, schaetzeAudioRohbytes, die Grenzfunktion aus 12-16)"
    - "dist/dnd-tracker-bundled.html → tests/e2e/** (Playwright läuft gegen das gebaute Bundle)"
    - "tests/unit/audio-import-resilience.test.js:295 und tests/unit/stability.test.js R11-Rest → die beiden umgestellten Verankerungen"
---

<objective>
Die Integrationsstufe dieser Gap-Closure-Runde. Fünf Pläne haben in den Wellen 9 und 10 sieben Befunde
geschlossen und dabei vier Quelldateien geändert:

| Plan | Welle | Datei | Befund |
|---|---|---|---|
| 12-12 | 9 | `systems/file-backup/file-backup-manager.js` | SEC-04 (critical), SEC-03 |
| 12-13 | 9 | `systems/undo.js` | SEC-02 |
| 12-14 | 9 | `systems/migration/migration-wizard.js` | SEC-01 |
| 12-15 | 9 | `systems/migration/audio-export.js` | SEC-07 |
| 12-16 | 10 | `systems/migration/full-export.js`, `systems/migration/migration-wizard.js` | SEC-05, SEC-06, WR-03 |

Keiner dieser Pläne hat die vollen Suiten gefahren oder `dist/` neu gebaut — bewusst: parallele Pläne
derselben Welle sehen die Commits der anderen nicht, ein Neubau innerhalb der Welle bildete also
einen unvollständigen Quellstand ab. Genau dieser Fehler ist in dieser Phase schon einmal
aufgetreten (W-1, veraltetes Bundle) und wurde in Plan 12-11 mit einer eigenen, nachgelagerten Welle
behoben. Dieselbe Aufteilung gilt hier.

Dieser Plan macht deshalb dreierlei und sonst nichts: er misst die vollen Suiten, er baut beide
Bundles aus dem vollständigen Stand, und er misst die E2E-Suite gegen das Gebaute.

**Besonderes Augenmerk** (die Stellen, an denen diese Runde die Suite kippen könnte):
1. **Drei neue Top-Level-Deklarationen** sind entstanden (`_hatKampagnenInhalt` in 12-12,
   `schaetzeAudioRohbytes` in 12-15, die Audio-Grenzfunktion in 12-16). `build.py` bricht seit
   Phase 11 (ARCH-02/D-05) hart ab, wenn zwei Module denselben Top-Level-Namen deklarieren — das ist
   die erste Stelle, an der eine Kollision auffallen würde.
2. **Zwei `test.failing`-Verankerungen** wurden umgestellt
   (`tests/unit/audio-import-resilience.test.js:295` in 12-14, „R11-Rest" in
   `tests/unit/stability.test.js` in 12-13). Steht eine davon noch auf `test.failing`, meldet Jest
   sie als unerwartet bestanden und die gesamte Suite geht rot.
3. **Zwei Testdateien lesen fremde Quelltexte** (`audio-export.test.js` liest
   `migration-wizard.js`; `full-export.test.js` liest `full-export.js`). Sie sind die Stellen, an
   denen sich Änderungen aus verschiedenen Plänen gegenseitig bemerkbar machen.

Purpose: Die Verifikation dieser Phase misst am gebauten Stand. Ein Bundle, das nicht alle Fixes
enthält, macht jede weitere Aussage wertlos — und ein grüner Einzeltest sagt nichts über die
Wechselwirkung von fünf parallel entstandenen Änderungen.
Output: zwei aktuelle Bundles und drei gemessene Suiten, jede mit ihrer Zahl.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-datensicherheit/12-VERIFICATION.md
@.planning/phases/12-datensicherheit/12-11-SUMMARY.md
@.planning/phases/12-datensicherheit/12-12-SUMMARY.md
@.planning/phases/12-datensicherheit/12-13-SUMMARY.md
@.planning/phases/12-datensicherheit/12-14-SUMMARY.md
@.planning/phases/12-datensicherheit/12-15-SUMMARY.md
@.planning/phases/12-datensicherheit/12-16-SUMMARY.md
@CLAUDE.md

@build.py
@loader.js
</context>

<scope_fence>
Dieser Plan ändert **keine einzige Quelldatei**. Er baut und misst.

**Wenn eine Suite rot ist:** Der Fehlschlag wird mit Testnamen, Datei und Fehlermeldung
protokolliert und in der SUMMARY als offener Punkt benannt — mit der Angabe, welchem Plan die
betroffene Datei gehört. Eine Reparatur „nebenbei" in diesem Plan wäre eine Quelltextänderung ohne
eigenen roten Nachweis, ohne Mutationsprüfung und ohne Scope Fence. Einzige Ausnahme: eine
Kollision zweier Top-Level-Namen aus zwei verschiedenen Plänen dieser Runde — sie ist ein reines
Integrationsproblem, das nur hier sichtbar werden kann; auch dann wird sie zuerst benannt und die
Umbenennung ausdrücklich als Integrationsfix protokolliert.

**Nicht Teil dieses Plans:** kein `npm run lint`, kein `tsc --noEmit`, keine Formatierung. Diese
Läufe gehören zur CI-Pipeline und waren in keinem der elf vorherigen Pläne dieser Phase Teil der
Abnahme; sie hier einzuführen, hieße eine neue Schwelle mitten in einer Gap-Closure-Runde zu setzen.
</scope_fence>

<tasks>

<task type="auto">
  <name>Task 1: Volle Jest-Suite und Build-Tests aus dem vollständigen Quellstand</name>
  <files>—</files>
  <read_first>
    - Die SUMMARYs der Pläne 12-12 bis 12-16 — je die notierte Testbasislinie und die Zahl neu
      hinzugekommener Tests. Ihre Summe ist die Erwartung für diesen Lauf.
    - `.planning/phases/12-datensicherheit/12-VERIFICATION.md` Frontmatter `nyquist_audit` — dort
      steht die Basislinie dieser Runde: Jest 842 bestanden bei 30 Suiten.
  </read_first>
  <precondition>Alle fünf Pläne dieser Runde (12-12 bis 12-16) sind ausgeführt und committet; `git status --porcelain` zeigt keine offenen Änderungen an den vier geänderten Quelldateien.</precondition>
  <action>
    **Schritt 1 — den Stand prüfen.** `git log --oneline -12` und `git status --porcelain`: alle fünf
    Pläne müssen committet sein. Ist der Arbeitsbaum an einer der vier Quelldateien schmutzig, wurde
    ein Plan nicht abgeschlossen — das gehört in die SUMMARY, bevor gemessen wird.

    **Schritt 2 — die volle Jest-Suite fahren** und die Zahlen festhalten: bestandene Tests, Zahl der
    Suiten, Exit-Code. Erwartung: mindestens 842 (die Basislinie aus dem Nyquist-Nachzug) plus die
    Summe der neu hinzugekommenen Tests aus den fünf SUMMARYs. Weicht die tatsächliche Zahl von
    dieser Summe ab, gehört die Differenz erklärt — nicht überschrieben.

    **Ausdrücklich auf zwei Meldungsarten achten:**
    - Ein als „unerwartet bestanden" gemeldeter Test bedeutet, dass eine der beiden
      `test.failing`-Verankerungen nicht umgestellt wurde.
    - Ein Ladefehler in einer der vm-Kontext-Testdateien (`audio-export.test.js`,
      `audio-import-resilience.test.js`, `migration-wizard.test.js`, `file-backup.test.js`) deutet
      auf eine kaputte Deklaration in einer der geänderten Quelldateien hin, nicht auf einen
      Testfehler.

    **Schritt 3 — die Build-Tests fahren.** `python -m pytest tests/build -q`. Sie enthalten die
    Prüfungen zum Deduplizierungssystem und zu doppelten Deklarationen — die für diese Runde
    relevante Schwelle, weil drei neue Top-Level-Namen entstanden sind.

    **Schritt 4 — die beiden Verankerungen gegenprüfen.** Der dritte Verify-Befehl zählt die
    verbliebenen `test.failing`-Vorkommen in den beiden betroffenen Dateien. Erwartung: keine. Findet
    er welche, ist entweder eine Umstellung ausgeblieben oder eine NEUE Verankerung entstanden — im
    zweiten Fall gehört sie benannt, denn eine neue Verankerung mitten in einer Gap-Closure-Runde
    wäre ein eigener Befund.
  </action>
  <verify>
    <automated>npx jest</automated>
    <fails_when>Exit-Code ≠ 0; oder die Zusammenfassung meldet weniger als 842 bestandene Tests; oder sie meldet einen Test als unerwartet bestanden; oder die Zahl der Suiten liegt unter 30 (dann lädt eine Testdatei gar nicht mehr).</fails_when>
  </verify>
  <verify>
    <automated>python -m pytest tests/build -q</automated>
    <fails_when>Exit-Code ≠ 0; oder die Zusammenfassung meldet weniger als 24 bestandene Tests.</fails_when>
  </verify>
  <verify>
    <automated>grep -c "test\.failing" tests/unit/audio-import-resilience.test.js tests/unit/stability.test.js</automated>
    <fails_when>Für eine der beiden Dateien wird eine Zahl größer 0 gemeldet — dann steht dort noch eine Verankerung, die entweder aus dieser Runde stammt (nicht umgestellt) oder neu ist (eigener Befund). Ein Exit-Code ≠ 0 bei `0` Treffern in beiden Dateien ist KEIN Fehlschlag: `grep -c` liefert bei null Treffern regulär 1.</fails_when>
  </verify>
  <acceptance_criteria>
    - Alle fünf Pläne sind committet, der Arbeitsbaum ist an den vier Quelldateien sauber.
    - `npx jest` ist grün mit mindestens 842 bestandenen Tests bei mindestens 30 Suiten; die
      tatsächliche Zahl ist notiert und gegen die Summe aus den fünf SUMMARYs abgeglichen.
    - Kein Test wird als unerwartet bestanden gemeldet.
    - `python -m pytest tests/build -q` ist grün mit mindestens 24 bestandenen Tests.
    - In den beiden betroffenen Testdateien steht keine `test.failing`-Verankerung mehr.
  </acceptance_criteria>
  <done>
    Der vollständige Quellstand dieser Runde ist auf Einheitentest-Ebene gemessen, und beide
    Zeitbomben aus dem Nyquist-Nachzug sind nachweislich entschärft.
  </done>
</task>

<task type="auto">
  <name>Task 2: Beide dist-Bundles aus dem vollständigen Stand neu bauen</name>
  <files>dist/dnd-tracker-bundled.html, dist/dnd-tracker-optimized.html</files>
  <read_first>
    - `build.py` — der Ablauf: Einlesen der Modul-, Template- und CSS-Listen aus `loader.js` bzw.
      `assets/styles.css`, `check_duplicate_functions()` VOR dem Zusammenfügen, die
      Deduplizierungsdurchläufe, und die Nachprüfung im `build()`-Schritt, die bei einer
      Doppeldeklaration `[ABORTED] Build NICHT geschrieben!` meldet.
    - `loader.js` — die `MODULES`-Liste; keine der vier geänderten Dateien ist neu, alle stehen
      bereits darin. Dieser Plan trägt nichts ein.
    - `.planning/phases/12-datensicherheit/12-11-PLAN.md` Task 3 — dort steht der Ablauf, mit dem in
      der vorigen Runde beide Bundles gebaut und gemessen wurden; dieselben Befehle gelten hier.
  </read_first>
  <precondition>Unter Windows ist `PYTHONIOENCODING=utf-8` gesetzt, sonst bricht `build.py` an den Umlauten seiner Konsolenausgabe ab.</precondition>
  <action>
    **Beide Bundles.** `python build.py` erzeugt `dist/dnd-tracker-bundled.html` (Entwicklung),
    `python build.py --production` erzeugt `dist/dnd-tracker-optimized.html`. Beide sind nötig: die
    E2E-Suite läuft gegen das Entwicklungs-Bundle, der Nutzer öffnet das Produktions-Bundle.

    Aus jedem Lauf festhalten: Exit-Code, die gemeldete Modulzahl (Erwartung: dieselbe wie in der
    vorigen Runde, 124/124 — eine abweichende Zahl bedeutet, dass jemand `loader.js` angefasst hat,
    was in dieser Runde niemand tun sollte), und ob eine `[DEDUP]`- oder Kollisionsmeldung erschien.

    **Bei einer Abbruchmeldung** (`[FEHLER] Doppelte Top-Level-Deklaration '…': A und B`): das ist
    das Ergebnis dieses Plans, nicht ein Hindernis. Die beiden genannten Dateien und der Name gehören
    wörtlich in die SUMMARY. Kollidieren zwei Namen aus zwei verschiedenen Plänen dieser Runde, ist
    es ein reines Integrationsproblem — dann darf hier umbenannt werden, ausdrücklich als
    Integrationsfix protokolliert, mit erneutem Lauf der betroffenen Einheitentests. Kollidiert ein
    neuer Name mit einer VORBESTEHENDEN Deklaration, gehört der Fix in den Plan, dem die Datei
    gehört.

    **Frischenachweis.** Nach beiden Läufen die Zeitstempel beider Bundles gegen die Zeitstempel der
    vier geänderten Quelldateien halten (`ls -la --time-style=full-iso`). Beide Bundles müssen jünger
    sein als jede von ihnen. Das ist der Nachweis gegen W-1 (veraltetes Bundle), der in dieser Phase
    schon einmal fällig war.
  </action>
  <verify>
    <automated>PYTHONIOENCODING=utf-8 python build.py</automated>
    <fails_when>Exit-Code ≠ 0; oder die Ausgabe enthält `[FEHLER]`, `[ABORTED]` oder `Doppelte`; oder die gemeldete Modulzahl weicht von `124/124` ab.</fails_when>
  </verify>
  <verify>
    <automated>PYTHONIOENCODING=utf-8 python build.py --production</automated>
    <fails_when>Exit-Code ≠ 0; oder die Ausgabe enthält `[FEHLER]`, `[ABORTED]` oder `Doppelte`; oder die gemeldete Modulzahl weicht von `124/124` ab.</fails_when>
  </verify>
  <verify>
    <automated>ls -la --time-style=full-iso dist/dnd-tracker-bundled.html dist/dnd-tracker-optimized.html systems/file-backup/file-backup-manager.js systems/undo.js systems/migration/migration-wizard.js systems/migration/audio-export.js systems/migration/full-export.js</automated>
    <fails_when>Der Zeitstempel eines der beiden Bundles liegt vor dem Zeitstempel einer der fünf gelisteten Quelldateien; oder eine der Dateien fehlt (Exit-Code ≠ 0).</fails_when>
  </verify>
  <acceptance_criteria>
    - Beide `build.py`-Läufe enden mit Exit-Code 0, ohne Kollisions- oder Abbruchmeldung, bei
      unveränderter Modulzahl.
    - Beide Bundles sind jünger als jede der fünf geänderten Quelldateien.
    - Modulzahl, Exit-Codes und etwaige `[DEDUP]`-Meldungen sind notiert.
  </acceptance_criteria>
  <done>
    Beide Bundles enthalten alle sieben Fixes dieser Runde — der Stand, gegen den ab jetzt gemessen
    wird.
  </done>
</task>

<task type="auto">
  <name>Task 3: Volle E2E-Suite gegen das gebaute Bundle</name>
  <files>—</files>
  <read_first>
    - `.planning/phases/12-datensicherheit/12-VERIFICATION.md` — die Basislinie: Playwright 321
      bestanden / 2 übersprungen bei Exit-Code 0.
    - Die SUMMARYs der fünf Pläne — welche Bedienpfade berührt wurden (Datei-Backup, Undo/Redo,
      Umzugs-Wizard). Sie sind die Stellen, an denen ein E2E-Fehlschlag zuerst zu erwarten wäre.
  </read_first>
  <precondition>Task 2 ist abgeschlossen: beide Bundles sind aus dem vollständigen Quellstand neu gebaut. Ein E2E-Lauf davor misst den alten Stand.</precondition>
  <action>
    **Schritt 1 — die volle Playwright-Suite fahren** und die Zahlen festhalten: bestanden,
    übersprungen, fehlgeschlagen, Exit-Code. Erwartung: mindestens 321 bestanden, höchstens 2
    übersprungen, Exit-Code 0.

    **Schritt 2 — bei einem Fehlschlag: erst diagnostizieren, dann melden.** Ein einzelner
    fehlgeschlagener E2E-Test wird zunächst isoliert erneut gefahren (nur diese Datei, nur dieser
    Testname), um ein Zeitverhalten von einer echten Regression zu unterscheiden. Beides gehört mit
    Namen und Ausgabe in die SUMMARY; repariert wird hier nichts (siehe `<scope_fence>`).

    **Schritt 3 — die Bilanz ziehen.** In der SUMMARY eine Tabelle mit je einer Zeile für Jest,
    `pytest tests/build`, beide `build.py`-Läufe und Playwright, jeweils mit der gemessenen Zahl und
    dem Exit-Code — dieselbe Form wie die Testevidenz-Tabelle in `12-VERIFICATION.md`. Sie ist das,
    was die nächste Verifikationsrunde als Ausgangspunkt übernimmt.

    **Schritt 4 — die Abdeckung der sieben Befunde festhalten.** Je Befund eine Zeile: welcher Plan,
    welche Datei, welche benannten Tests, und ob der Mutationsnachweis geführt wurde. Diese Tabelle
    ist die Grundlage für den Abgleich in `/gsd-verify-work` — sie ersetzt keine Verifikation, aber
    sie macht sie überprüfbar.
  </action>
  <verify>
    <automated>npx playwright test</automated>
    <fails_when>Exit-Code ≠ 0; oder die Zusammenfassung meldet weniger als 321 bestandene oder mehr als 2 übersprungene Tests; oder sie nennt einen fehlgeschlagenen Test.</fails_when>
  </verify>
  <verify>
    <automated>git status --porcelain</automated>
    <fails_when>Die Ausgabe nennt eine geänderte Datei außerhalb von `dist/` und `.planning/` — dann hat dieser Plan entgegen seinem Scope Fence Quelltext verändert.</fails_when>
  </verify>
  <acceptance_criteria>
    - `npx playwright test` ist grün mit mindestens 321 bestandenen und höchstens 2 übersprungenen
      Tests.
    - Ein etwaiger Fehlschlag ist isoliert nachgestellt und mit Namen, Datei und Ausgabe
      protokolliert, nicht repariert.
    - Die Bilanztabelle (fünf Läufe mit Zahlen und Exit-Codes) steht in der SUMMARY.
    - Die Abdeckungstabelle über alle sieben Befunde steht in der SUMMARY.
    - Außerhalb von `dist/` und `.planning/` ist keine Datei verändert.
  </acceptance_criteria>
  <done>
    Alle drei Suiten sind gegen den vollständigen, gebauten Stand dieser Runde gemessen, und die
    Abdeckung der sieben Befunde ist nachvollziehbar dokumentiert.
  </done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

**Gebaute Artefakte**

| Datei | Erzeugt durch |
|---|---|
| `dist/dnd-tracker-bundled.html` | `PYTHONIOENCODING=utf-8 python build.py` |
| `dist/dnd-tracker-optimized.html` | `PYTHONIOENCODING=utf-8 python build.py --production` |

**Keine Quelltextänderung.** Kein neues Modul, kein `loader.js`-Eintrag, kein CSS-`@import`, keine
neue Top-Level-Deklaration, kein neuer `window.*`-Export.

**Messergebnisse** (gehören in die SUMMARY, nicht in eine Datei): Jest, `pytest tests/build`, beide
`build.py`-Läufe, Playwright — je mit Zahl und Exit-Code; dazu die Abdeckungstabelle über die sieben
Befunde dieser Runde.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Quellstand aus fünf parallel entstandenen Plänen → ein Bundle | Erst hier treffen die Änderungen aufeinander; Namenskollisionen und Wechselwirkungen können nur an dieser Stelle sichtbar werden |
| gebautes Bundle → E2E-Suite → Aussage über die Phase | Ein veralteter Stand macht jede nachfolgende Verifikationsaussage wertlos |
| gebautes Produktions-Bundle → der Nutzer | Was hier nicht enthalten ist, existiert für ihn nicht — auch wenn es im Quelltext steht |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-70 | Repudiation | veraltetes `dist/`-Bundle nach einer Fix-Runde | high | mitigate | Genau dieser Fehler (W-1) ist in dieser Phase schon einmal aufgetreten. Task 2 baut beide Bundles und weist per Zeitstempel nach, dass sie jünger sind als jede geänderte Quelldatei |
| T-12-71 | Denial of Service | Namenskollision zweier neuer Top-Level-Deklarationen aus verschiedenen Plänen | medium | mitigate | Drei neue Namen sind in dieser Runde entstanden; `build.py` bricht bei einer Kollision vor dem Zusammenfügen ab. Jeder Plan hat den Namen vorab geprüft, Task 2 ist die letzte Instanz |
| T-12-72 | Repudiation | Eine nicht umgestellte `test.failing`-Verankerung kippt die gesamte Suite | medium | mitigate | Zwei Verankerungen wurden in dieser Runde umgestellt. Task 1 zählt die verbliebenen Vorkommen ausdrücklich nach, statt sich auf den Exit-Code zu verlassen |
| T-12-SC | Tampering | npm/pip/cargo-Installationen | n/a | accept | Dieser Plan installiert kein Paket; `package.json` und `requirements-dev.txt` bleiben unberührt. `build.py`, Jest, pytest und Playwright sind bereits installiert. Keine Legitimitätsprüfung nötig |

**ASVS-Ebene:** L1, Blockschwelle `high`. Kein Server, keine Sitzung, keine Authentifizierung. Das
Schutzgut dieses Plans ist die Belastbarkeit der Aussage über die Phase: ein grüner Lauf gegen einen
unvollständigen Stand ist schlimmer als kein Lauf, weil er Vertrauen erzeugt, das er nicht verdient.
</threat_model>

<verification>
- `npx jest` grün, mindestens 842 bestanden, mindestens 30 Suiten, kein unerwartet bestandener Test
- `python -m pytest tests/build -q` grün, mindestens 24 bestanden
- Beide `build.py`-Läufe mit Exit-Code 0, 124/124 Modulen, ohne Kollisionsmeldung
- Beide `dist/`-Bundles jünger als alle fünf geänderten Quelldateien
- `npx playwright test` grün, mindestens 321 bestanden, höchstens 2 übersprungen
- `grep -c "test\.failing"` meldet für beide betroffenen Testdateien 0
- `git status --porcelain` nennt außerhalb von `dist/` und `.planning/` keine Änderung
</verification>

<success_criteria>
- Der vollständige Quellstand aller fünf Fix-Pläne ist auf allen drei Ebenen gemessen (Einheit,
  Build, E2E)
- Beide Bundles enthalten alle sieben Fixes und sind nachweislich frisch
- Die Bilanz- und die Abdeckungstabelle liegen für die nächste Verifikationsrunde vor
- Kein Befund wurde in diesem Plan nebenbei repariert
</success_criteria>

<output>
Create `.planning/phases/12-datensicherheit/12-17-SUMMARY.md` when done.

Die SUMMARY enthält:

1. **Bilanztabelle** — je eine Zeile für `npx jest`, `python -m pytest tests/build -q`,
   `python build.py`, `python build.py --production`, `npx playwright test`; je mit gemessener Zahl
   und Exit-Code. Form wie die Testevidenz-Tabelle in `12-VERIFICATION.md`.
2. **Abdeckungstabelle** über die sieben Befunde: Befund, Plan, geänderte Datei, benannte Tests,
   Mutationsnachweis geführt (ja/nein).
3. **Frischenachweis** für beide Bundles (Zeitstempel gegen die fünf Quelldateien).
4. **Offene Punkte**, falls eine Suite rot war: mit Testnamen, Datei, Ausgabe und dem Plan, dem die
   betroffene Datei gehört. Ausdrücklich ohne Reparatur.
5. Einen `coverage:`-Block ist für diesen Plan NICHT zu führen — er fügt keine Tests hinzu. Statt
   dessen der Verweis auf die `coverage:`-Blöcke der fünf Fix-Pläne, die dieser Lauf gemeinsam
   bestätigt.
</output>
</content>
