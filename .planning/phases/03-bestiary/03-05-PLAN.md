---
phase: 03-bestiary
plan: 05
type: execute
wave: 4
depends_on: ["03-01", "03-03", "03-04"]
autonomous: true
requirements: [BEST-03]
files_modified:
  - features/bestiary/bestiary-actions.js
  - ui/actions/entity-actions.js
  - tests/e2e/features/bestiary.spec.js
user_setup: []

must_haves:
  truths:
    - "User clicks 'Zur Initiative' on a monster, a quantity dialog asks how many, and N combatants are added with auto-rolled initiative, ±10% HP variation, and correct AC (BEST-03 SC3, D-14, D-15)"
    - "Adding multiple identical monsters numbers them 'Goblin 1', 'Goblin 2', ... for table clarity (D-16)"
    - "Each bestiary-spawned combatant carries a runtime statblockRef {source,id} (no migration) so Phase 4 can show its statblock (D-17)"
    - "User clicks 'Zu Encounter' and a D.encounters entry is created with correct HP/AC from the statblock, Undo-able (BEST-03 SC3)"
    - "Favorites star toggles persist in D.bestiaryFavorites (IDs only) and the Favoriten filter finds them; clickable statblock dice roll via rollQrefDice (D-09, D-11)"
  artifacts:
    - path: "features/bestiary/bestiary-actions.js"
      provides: "addBestiaryToInitiative, addBestiaryToEncounter, toggleBestiaryFavorite, isBestiaryFavorite, getBestiaryMonster"
      exports: ["addBestiaryToInitiative", "addBestiaryToEncounter", "toggleBestiaryFavorite", "isBestiaryFavorite", "getBestiaryMonster"]
      min_lines: 80
    - path: "ui/actions/entity-actions.js"
      provides: "data-action handlers: bestiary-select, bestiary-toggle-fav, bestiary-roll-dice, bestiary-add-init, bestiary-add-enc, bestiary-delete"
      contains: "bestiary-roll-dice"
  key_links:
    - from: "statblock buttons + list fav star + dice spans (plan 03 DOM)"
      to: "ui/actions/entity-actions.js delegated handlers"
      via: "data-action dispatch"
      pattern: "bestiary-add-init|bestiary-roll-dice|bestiary-toggle-fav"
    - from: "addBestiaryToInitiative"
      to: "D.initiative.combatants with statblockRef"
      via: "addCalculatorToInitiative mechanic (auto-roll, HP variation, numbering)"
      pattern: "statblockRef"
    - from: "addBestiaryToEncounter"
      to: "D.encounters"
      via: "saveUndoState + field mapping"
      pattern: "D.encounters"
---

<objective>
Wire up all Bestiary actions: favorites toggle (persisted IDs), clickable-dice rolling, and the BEST-03 takeover paths — "Zur Initiative" (quantity dialog → N combatants with auto-roll, HP variation, numbering, statblockRef) and "Zu Encounter" (D.encounters entry with correct HP/AC, Undo-able). Also register the data-action handlers that the plan-03 DOM emits.

Purpose: BEST-03 SC3 — monsters move from the bestiary into Encounter and Initiative with correct values; D-09/D-11 interactions become live.
Output: A complete bestiary-actions module, registered delegated handlers, and the takeover E2E turned real.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/CLAUDE.md
@D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/.planning/phases/03-bestiary/03-PATTERNS.md
@D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/.planning/phases/03-bestiary/03-RESEARCH.md

<interfaces>
Initiative takeover mechanic (features/encounter-calculator.js addCalculatorToInitiative, lines 853–935) — copy the exact math:
- DEX mod = `Math.floor((monster.dex - 10) / 2)`
- init roll = `Math.floor(Math.random()*20)+1 + dexMod`
- HP variation ±10% = `Math.max(1, Math.round(monster.hp * (0.9 + Math.random()*0.2)))`
- combatant schema keys: id (nextId('combatants')), name, initiative, initBonus, maxHp, currentHp, ac, type:'monster', cr, xp, effects:[].
NOTE on numbering: existing calculator uses `${name} #${i+1}`; D-16 + UI-SPEC require `${monster.name} ${i+1}` (space, no '#') → "Goblin 1". Use the D-16 form.
ADD the runtime field `statblockRef: { source, id }` (D-17) — source 'srd'|'custom', id is `_id` for SRD or numeric `id` for custom. Runtime only — NOT persisted via migration, just lives on the combatant.
After push: sort combatants by initiative desc, `window.save()`, `switchView('initiative')`, `renderInit()`, toast `%n%× %name% zur Initiative hinzugefügt`.

Quantity dialog (D-14): `prompt('Wie viele "%name%" zur Initiative hinzufügen?', '1')`; cancel (null) aborts silently; clamp `Math.max(1, parseInt(input) || 1)`; cap the count to a sane max (e.g. 100) and reject non-numeric beyond the clamp to avoid DoS.

Encounter takeover (RESEARCH Muster 6): build a D.encounters entry mapping statblock → encounter schema (encounters-crud.js fields: name, creatureType, cr, ac, init, hp, speed, perception, languages, str..cha, savingThrows, resistances, immunities, conditionImmunities, traits, actions, skills as HTML). `saveUndoState('Monster zu Encounter hinzugefügt')` BEFORE push; reactions/legendaryActions have no encounter field → omit (encounters schema unchanged). toast `%name% zu Encounter hinzugefügt`; re-render encounters.

Favorites (D-11): key = `'custom:'+id` for custom, `String(_id)` for SRD; toggle in D.bestiaryFavorites with `saveUndoState('Bestiary-Favorit geändert')` + `saveImmediate()` + renderBestiaryList. isBestiaryFavorite(monster) returns membership. These two functions REPLACE the plan-01 stubs that plan 03 renders against (window.isBestiaryFavorite).

getBestiaryMonster(id, source): source==='custom' → D.bestiary.find(parseEntityId); else getSRDMonsters().find(_id===id).

Dice (D-09): `rollQrefDice(value)` from systems/spellslots/quick-reference.js:157 — handler reads `data-value` and calls it.

Handler registration: ui/actions/entity-actions.js is the delegated data-action handler module (loaded after render modules). Add cases for: `bestiary-select` (→ selectBestiary(id,source)), `bestiary-toggle-fav` (→ toggleBestiaryFavorite(id,source)), `bestiary-roll-dice` (→ rollQrefDice(value)), `bestiary-add-init` (→ addBestiaryToInitiative(id,source)), `bestiary-add-enc` (→ addBestiaryToEncounter(id,source)), `bestiary-delete` (→ deleteBestiaryEntry(id)). Read id/source/value from the element dataset.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: bestiary-actions.js — takeover, favorites, lookup</name>
  <files>features/bestiary/bestiary-actions.js</files>
  <read_first>
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/features/encounter-calculator.js (addCalculatorToInitiative lines 853–935 — exact math to copy)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/features/initiative.js (combatant schema / addCombatant — confirm field names)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/features/dice/dice-favorites.js (favorites toggle pattern)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/.planning/phases/03-bestiary/03-PATTERNS.md (bestiary-actions.js block: all 5 functions)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/features/bestiary/bestiary-actions.js (plan-01 stub to fill)
  </read_first>
  <behavior>
    - addBestiaryToInitiative(id, source): resolves the monster via getBestiaryMonster; prompts quantity (`Wie viele "%name%" zur Initiative hinzufügen?`, default '1'); cancel→abort, clamp to [1,100]; for each: pushes a combatant with auto-rolled initiative, ±10% HP, correct ac, name `Goblin 1/2/3…` when count>1 else plain name, and `statblockRef:{source,id}`; sorts desc; saves; switches to initiative; toasts `%n%× %name% zur Initiative hinzugefügt`.
    - addBestiaryToEncounter(id, source): saveUndoState first; pushes a D.encounters entry with name/creatureType/cr/ac/init(DEX-mod)/hp/speed/str..cha/savingThrows/resistances/immunities/conditionImmunities/traits/actions/skills mapped from the statblock; saves; re-renders encounters; toasts `%name% zu Encounter hinzugefügt`. The created encounter's hp and ac equal the source statblock's hp/ac.
    - toggleBestiaryFavorite(id, source) / isBestiaryFavorite(monster): persist/read D.bestiaryFavorites with the keying rule; toggle saves immediately and re-renders the list.
    - getBestiaryMonster(id, source): returns the SRD or custom statblock or null.
  </behavior>
  <action>
    Implement all five functions in `features/bestiary/bestiary-actions.js` (replace plan-01 stubs) per the behavior + interfaces, copying the calculator math verbatim and using the D-16 space-numbering form. Enforce the quantity cap (reject/clamp > 100) for DoS safety. Use `parseEntityId` for custom lookups. Export all five via window. No in-function `const X = window.X`; grep each function name across features/systems/ui/core first to avoid the dedup blind spot (note isBestiaryFavorite/getBestiaryMonster already exist as plan-01 stubs in THIS file — replace, don't duplicate).
  </action>
  <acceptance_criteria>
    - `features/bestiary/bestiary-actions.js` exports `addBestiaryToInitiative`, `addBestiaryToEncounter`, `toggleBestiaryFavorite`, `isBestiaryFavorite`, `getBestiaryMonster`.
    - addBestiaryToInitiative sets `statblockRef` on every pushed combatant and uses the `${monster.name} ${i+1}` numbering (space form) when count>1.
    - Quantity is clamped to a max (≤100); `prompt` returning null aborts without mutation.
    - addBestiaryToEncounter calls `saveUndoState(` before pushing to `D.encounters`; the new entry's `hp`/`ac` equal the statblock's.
    - `grep -c "function isBestiaryFavorite" features/bestiary/bestiary-actions.js` returns 1 (stub replaced, not duplicated).
    - `python build.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python build.py</automated>
  </verify>
  <done>Takeover/favorites/lookup logic implemented with correct HP/AC, numbering, statblockRef, and DoS-capped quantity; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Register delegated data-action handlers</name>
  <files>ui/actions/entity-actions.js</files>
  <read_first>
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/ui/actions/entity-actions.js (existing data-action switch/dispatch shape)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/ui/event-delegation.js (how data-action + dataset reach the handler)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/.planning/phases/03-bestiary/03-PATTERNS.md (clickable-dice + action-handler notes)
  </read_first>
  <action>
    Add handler cases in `ui/actions/entity-actions.js` for the bestiary data-actions, reading `dataset.id`, `dataset.source`, `dataset.value` from the clicked element: `bestiary-select` → `window.selectBestiary(id, source)`; `bestiary-toggle-fav` → `window.toggleBestiaryFavorite(id, source)`; `bestiary-roll-dice` → `window.rollQrefDice(value)`; `bestiary-add-init` → `window.addBestiaryToInitiative(id, source)`; `bestiary-add-enc` → `window.addBestiaryToEncounter(id, source)`; `bestiary-delete` → `window.deleteBestiaryEntry(id)`. Follow the existing dispatch style in the file (do not introduce a new mechanism). Ensure the plan-03 detail buttons emit `bestiary-add-init`/`bestiary-add-enc`/`bestiary-delete` with the right data attributes (confirm or adjust the data-action names to match these cases — keep one consistent set).
  </action>
  <acceptance_criteria>
    - `ui/actions/entity-actions.js` contains dispatch cases for `bestiary-select`, `bestiary-toggle-fav`, `bestiary-roll-dice`, `bestiary-add-init`, `bestiary-add-enc`, `bestiary-delete`.
    - Each case reads id/source/value from the element dataset and calls the matching window function.
    - The plan-03 detail-panel buttons and list fav star use exactly these data-action names (one consistent set across render + handler).
    - `python build.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python build.py</automated>
  </verify>
  <done>All bestiary interactions (select, fav, dice, add-init, add-enc, delete) are dispatched through the delegated handler.</done>
</task>

<task type="auto">
  <name>Task 3: Real E2E for 'Zur Initiative' and 'Zu Encounter'</name>
  <files>tests/e2e/features/bestiary.spec.js</files>
  <read_first>
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/tests/e2e/features/bestiary.spec.js (scaffold from plan 01)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/.planning/phases/03-bestiary/03-VALIDATION.md (BEST-03 SC3 rows)
  </read_first>
  <action>
    Make `Zur Initiative` and `Zu Encounter` real: open Bestiar, select a monster (e.g. Goblin), click `Zur Initiative`, handle the quantity prompt (e.g. 3 — use Playwright dialog handling), switch to Initiative and assert 3 numbered Goblin combatants exist with HP within ±10% of base and the statblock's AC; then from the bestiary click `Zu Encounter`, switch to Encounter and assert an entry with the statblock's HP/AC exists. Keep titles byte-identical (`Zur Initiative`, `Zu Encounter`).
  </action>
  <acceptance_criteria>
    - Both named tests contain real assertions (no fixme/skip), including a dialog handler for the quantity prompt.
    - Assertions verify combatant count = entered quantity, HP within ±10% of base, AC equals statblock AC, and numbered names.
    - `npx playwright test tests/e2e/features/bestiary.spec.js -g "Zur Initiative|Zu Encounter"` passes.
    - Full file green: `npx playwright test tests/e2e/features/bestiary.spec.js` passes all 10 scenarios.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/features/bestiary.spec.js</automated>
  </verify>
  <done>BEST-03 SC3 verified; the entire bestiary E2E suite (all 10 scenarios) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| quantity prompt input → loop count | User-entered number drives a combatant-creation loop |
| statblock → D.encounters / D.initiative | Monster data crosses into user collections |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Denial of Service | Unbounded quantity dialog (D-14) | mitigate | `Math.max(1, parseInt||1)` then cap ≤100; non-numeric clamps to 1; null aborts |
| T-03-11 | Tampering | Data integrity of statblockRef / takeover | mitigate | statblockRef is runtime-only (no migration); encounter takeover uses saveUndoState; parseEntityId for lookups |
| T-03-06 | Tampering | XSS via traits/actions copied into encounter | mitigate | Encounter mapping carries already-sanitized HTML; encounters render path already uses sanitizeHTML |
| T-03-02 | Denial of Service | SRD data into D via takeover | mitigate | Only mapped scalar fields + sanitized HTML copied into D.encounters; no full SRD object stored; statblockRef holds an id, not the data |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (RESEARCH audit: none) |
</threat_model>

<verification>
- `python build.py` green.
- `npx playwright test tests/e2e/features/bestiary.spec.js` green (all 10 scenarios).
- `npm run test && npm run test:e2e` green before /gsd:verify-work (phase gate).
</verification>

<success_criteria>
BEST-03 SC3 delivered: quantity-dialog takeover into Initiative with auto-roll, ±10% HP, numbering, correct AC and statblockRef; takeover into Encounter with correct HP/AC and Undo; favorites persist and filter; statblock dice roll — all wired through delegated handlers. Full bestiary E2E suite green.
</success_criteria>

<output>
Create `.planning/phases/03-bestiary/03-05-SUMMARY.md` when done.
</output>
