---
phase: 03-bestiary
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
autonomous: true
requirements: [BEST-01]
files_modified:
  - core/srd-monsters.js
  - features/encounters/monster-templates.js
  - tests/unit/srd-monsters.test.js
user_setup: []

must_haves:
  truths:
    - "getSRDMonsters() returns ~100–150 fully-German SRD 5.1 statblocks, lazy-cached, never written to D (D-01, D-02, D-03, architecture lock)"
    - "Every SRD monster carries the full D-04 statblock: attributes, saves, skills, resistances/immunities/condition-immunities, senses, languages, traits, actions, reactions, legendary actions"
    - "The 12 former monster-templates.js entries are present in the SRD seed; getMonsterTemplates() aliases getSRDMonsters() so no duplicate data and no dead code remain (Discretion)"
    - "CR is stored as a string (e.g. '1/4') and each monster has a stable string _id key for favorites + statblockRef"
  artifacts:
    - path: "core/srd-monsters.js"
      provides: "~150 SRD 5.1 DE statblocks behind getSRDMonsters() lazy cache"
      contains: "getSRDMonsters"
      min_lines: 300
    - path: "features/encounters/monster-templates.js"
      provides: "getMonsterTemplates() aliased to getSRDMonsters() keyed by _id"
      contains: "getSRDMonsters"
  key_links:
    - from: "features/encounters/monster-templates.js getMonsterTemplates()"
      to: "core/srd-monsters.js getSRDMonsters()"
      via: "alias reduce over _id"
      pattern: "getSRDMonsters"
    - from: "core/srd-monsters.js"
      to: "module closure cache _srdMonstersCache"
      via: "lazy init, never D"
      pattern: "_srdMonstersCache"
---

<objective>
Fill `core/srd-monsters.js` with ~100–150 curated, fully German SRD 5.1 statblocks (full D-04 schema), fold the 12 existing monster-templates into the seed, and re-point `getMonsterTemplates()` at the SRD store so no duplicate data or dead code remains. This is the offline data foundation for the whole tab and the Phase-4 statblock/legendary tracker.

Purpose: Deliver BEST-01's data side — instant offline search/filter needs the data resident and complete now (D-04 is a deliberate Phase-4 foundation; no later data backfill).
Output: A populated `core/srd-monsters.js`, a templates-module alias, and a green schema unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/CLAUDE.md
@D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/.planning/phases/03-bestiary/03-RESEARCH.md
@D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/.planning/phases/03-bestiary/03-PATTERNS.md

<interfaces>
SRD entry schema (RESEARCH Muster 1 / PATTERNS core/srd-monsters.js block) — every entry MUST have:
`_id` (string key), `name`, `size`, `creatureType`, `alignment`, `cr` (string), `xp` (number), `ac` (number), `acInfo`, `hp` (number), `hpFormula`, `speed` {walk,fly,swim,climb,burrow}, `str/dex/con/int/wis/cha` (numbers), `savingThrows` (object), `skills` (object), `damageResistances` (array), `damageImmunities` (array), `conditionImmunities` (array), `senses` (array), `languages` (array), `traits` ([{name,desc}]), `actions` ([{name,desc}]), `reactions` ([{name,desc}]), `legendaryActions` ([{name,desc,cost}]), `legendaryActionsPerRound` (number).

Data source (RESEARCH § Deutsche SRD-Quelle): openrpg.de/srd/5e/de/api/monster/{id}/json (391 monsters, CC-BY-4.0). The list endpoint openrpg.de/srd/5e/de/api/monster/ enumerates IDs. Saves/Reactions/LegendaryActions are partially WORK-IN-PROGRESS in the API → fill missing fields from the German SRD 5.1 PDF when curating bosses.

The 12 existing template keys to include in the seed: goblin, skeleton, zombie, orc, wolf, bandit, guard(Wache), kobold, giant_rat(Riesenratte), cultist, ogre(Oger), troll.

monster-templates.js currently: `let _monsterTemplatesCache = null; function getMonsterTemplates() {...}`. `getMonsterTemplates()` has no live callers (grep-confirmed) — safe to alias.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Acquire + curate ~150 SRD statblocks into core/srd-monsters.js</name>
  <files>core/srd-monsters.js</files>
  <read_first>
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/core/srd-spells.js (exact lazy-cache structure to clone)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/core/srd-monsters.js (the stub from plan 01 to fill)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/features/encounters/monster-templates.js (the 12 German statblocks to fold in)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/.planning/phases/03-bestiary/03-RESEARCH.md (§ Datenbeschaffungs-Workflow, § Datengröße-Spike, UI-text CR format)
  </read_first>
  <action>
    Build a one-off acquisition: GET `openrpg.de/srd/5e/de/api/monster/` for the ID list, select ~150 monsters balanced across CR (the table-common foes Goblin→Drache, per D-02), GET each `/monster/{id}/json`, and transform API fields into the App schema named in the interfaces block. Where the API marks saves/reactions/legendary actions WORK-IN-PROGRESS, complete those fields from the German SRD 5.1 PDF so every entry is full D-04 (especially dragons/bosses with legendaryActions + legendaryActionsPerRound). Embed the result inline in `core/srd-monsters.js` behind the `getSRDMonsters()` lazy cache (replace the plan-01 empty stub), keeping the CC-BY-4.0 attribution header. Include the 12 former templates as part of the seed using the IDs in the interfaces block. Store cr as a string (never a number). NEVER assign to `D`. After embedding, run `python build.py` and confirm the produced bundle's added size keeps the inline decision valid: target < 200 KB minified for the monster data (RESEARCH spike); if the curated set exceeds it, drop the least-common high-CR variants until under budget rather than switching strategies (inline is the locked decision at 150 monsters).
    No runtime network calls remain — the app stays fully offline.
  </action>
  <acceptance_criteria>
    - `getSRDMonsters()` returns an Array of length ≥ 100 (target ~150).
    - Every entry has all 24 schema keys from the interfaces block; spot-checkable: each entry's `cr` is a string and `_id` is a non-empty string.
    - At least one entry has a non-empty `legendaryActions` array with `legendaryActionsPerRound > 0` (a dragon/boss), proving D-04 completeness.
    - The 12 seed IDs (goblin, skeleton, zombie, orc, wolf, bandit, guard, kobold, giant_rat, cultist, ogre, troll — or their German `_id` equivalents) are all present.
    - No `D.` assignment of SRD data anywhere in the file; `python build.py` exits 0.
    - `npx jest tests/unit/srd-monsters.test.js` passes with the now-populated array (schema guard becomes active).
  </acceptance_criteria>
  <verify>
    <automated>npx jest tests/unit/srd-monsters.test.js</automated>
  </verify>
  <done>~150 complete German SRD statblocks are resident behind the lazy cache, inline budget holds, and the schema test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Alias getMonsterTemplates() to the SRD store (no dead code)</name>
  <files>features/encounters/monster-templates.js</files>
  <read_first>
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/features/encounters/monster-templates.js (current getMonsterTemplates + cache)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/.planning/phases/03-bestiary/03-PATTERNS.md (monster-templates.js change block — alias-reduce pattern)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/.planning/phases/03-bestiary/03-RESEARCH.md (§ Schicksal der 12 monster-templates.js-Einträge)
  </read_first>
  <action>
    Replace the inline 12-entry data object in `getMonsterTemplates()` with an alias that derives a `{ _id: monster }` keyed map from `getSRDMonsters()` (reduce over entries with an `_id`), keeping the `_monsterTemplatesCache` lazy-cache shape so behavior is unchanged for any historical caller. Do not delete the module (it stays registered); just stop holding duplicate data. Confirm via grep that no code path still depends on the removed template-specific fields; if a former caller existed it now transparently reads SRD data.
  </action>
  <acceptance_criteria>
    - `features/encounters/monster-templates.js` `getMonsterTemplates()` body references `getSRDMonsters()` and builds a key→monster map; the inline 12-entry literal is gone.
    - `_monsterTemplatesCache` is still used (lazy memoization preserved).
    - `grep -c "name: 'Goblin'" features/encounters/monster-templates.js` returns 0 (no duplicate inline template data).
    - `python build.py` exits 0 (no duplicate-function / dedup warnings introduced).
  </acceptance_criteria>
  <verify>
    <automated>python build.py</automated>
  </verify>
  <done>monster-templates.js holds no duplicate data and reads from the SRD store; build is green; no dead code left.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| openrpg.de API (build-time only) → core/srd-monsters.js | External JSON transformed once into inline source; never fetched at runtime |
| static SRD data → D | SRD data must never cross into D / Undo / export |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | Prototype pollution via transformed JSON | mitigate | Build plain literal objects in source; never `Object.assign` API JSON into D; data is static source, not runtime-parsed |
| T-03-02 | Denial of Service | SRD data inflating D/Undo/localStorage | mitigate | Data lives only in `_srdMonstersCache` closure; grep confirms no `D.* = getSRDMonsters()` |
| T-03-05 | Information disclosure | Missing CC-BY attribution | mitigate | Attribution header retained in srd-monsters.js; footer rendered by plan 03 |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (RESEARCH audit: none) |
</threat_model>

<verification>
- `npx jest tests/unit/srd-monsters.test.js` green with ≥100 entries.
- `python build.py` green; bundle growth consistent with < 200 KB minified monster data.
- Manual spot-check (VALIDATION manual row): sample German statblocks vs SRD 5.1 DE for name/action/CR fidelity.
</verification>

<success_criteria>
~150 complete German SRD statblocks resident offline behind the lazy cache, full D-04 schema, CR-as-string, stable `_id` keys; the 12 templates folded in and `getMonsterTemplates()` aliased; no dead code; schema unit test green.
</success_criteria>

<output>
Create `.planning/phases/03-bestiary/03-02-SUMMARY.md` when done.
</output>
