---
phase: 03-bestiary
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [BEST-01, BEST-02, BEST-03]
files_modified:
  - core/data.js
  - systems/spellslots/version-migration.js
  - systems/tab-registry.js
  - assets/templates/header.html
  - assets/styles.css
  - build.py
  - loader.js
  - core/srd-monsters.js
  - features/bestiary/bestiary-render.js
  - features/bestiary/bestiary-crud.js
  - features/bestiary/bestiary-editor.js
  - features/bestiary/bestiary-actions.js
  - assets/styles/bestiary.css
  - assets/templates/view-bestiary.html
  - tests/e2e/features/bestiary.spec.js
  - tests/unit/srd-monsters.test.js
  - tests/unit/migration.test.js
user_setup: []

must_haves:
  truths:
    - "Migration 3.0.0 initializes D.bestiary=[] and D.bestiaryFavorites=[] on existing campaigns (D-12, D-13 architecture)"
    - "A new '🐉 Bestiar' nav tab is present and registered so switching to it triggers renderBestiaryList (D-05, tab-registry rule)"
    - "All 5 new JS modules + bestiary.css + view-bestiary.html are registered in build.py AND loader.js so a production build succeeds (locked module-sync constraint)"
    - "Wave-0 test files exist for SC1/SC2/SC3 and the SRD schema so downstream plans have failing tests to satisfy (Nyquist)"
  artifacts:
    - path: "core/data.js"
      provides: "D.bestiary and D.bestiaryFavorites in initializeData()"
      contains: "bestiary"
    - path: "systems/spellslots/version-migration.js"
      provides: "3.0.0 migration entry creating D.bestiary / D.bestiaryFavorites"
      contains: "'3.0.0'"
    - path: "systems/tab-registry.js"
      provides: "bestiary tab registration in TAB_RENDER_REGISTRY"
      contains: "bestiary:"
    - path: "core/srd-monsters.js"
      provides: "getSRDMonsters() lazy-cache stub (filled by plan 02)"
      exports: ["getSRDMonsters"]
    - path: "features/bestiary/bestiary-render.js"
      provides: "renderBestiaryList stub + window export"
      exports: ["renderBestiaryList"]
    - path: "assets/templates/view-bestiary.html"
      provides: "Bestiary master-detail view section #view-bestiary"
      contains: "view-bestiary"
    - path: "tests/e2e/features/bestiary.spec.js"
      provides: "Wave-0 E2E scenarios for SC1/SC2/SC3"
      min_lines: 40
    - path: "tests/unit/srd-monsters.test.js"
      provides: "Wave-0 schema unit test for getSRDMonsters()"
      min_lines: 15
  key_links:
    - from: "assets/templates/header.html nav button data-view=bestiary"
      to: "systems/tab-registry.js TAB_RENDER_REGISTRY.bestiary"
      via: "switchView -> renderTabContent('bestiary')"
      pattern: "bestiary"
    - from: "build.py MODULES + css_files + html_templates"
      to: "loader.js MODULES + assets/styles.css @import"
      via: "module/css/template sync"
      pattern: "bestiary"
---

<objective>
Lay the complete structural foundation for the Bestiary tab: data schema, version migration, tab registration, module/CSS/template wiring into BOTH build systems, empty-but-exported module stubs, and the Wave-0 test files. After this plan, the tab appears (empty), a production build succeeds, and downstream plans (02–05) have stubs to fill and failing tests to satisfy.

Purpose: Unblock parallel work in later waves and guarantee the locked module-sync + tab-registry + migration architecture is correct before any feature code is written.
Output: 5 registered JS module stubs, bestiary.css, view-bestiary.html, nav tab, migration 3.0.0, D-schema fields, 3 Wave-0 test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/CLAUDE.md
@D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/.planning/STATE.md
@D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/.planning/phases/03-bestiary/03-PATTERNS.md
@D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/.planning/phases/03-bestiary/03-VALIDATION.md

<interfaces>
Confirmed from codebase (do not re-explore):

build.py has THREE lists that must each gain bestiary entries (PATTERNS.md only mentioned MODULES — this is the load-bearing correction):
- MODULES (JS, lines 40–155): add `'core/srd-monsters.js'` after `'core/srd-spells.js'` (line 131) and the four `features/bestiary/*.js` after `'features/encounters/encounters-crud.js'` (line 105).
- css_files (lines 418–426): add `'bestiary.css'` after `'tools.css'`.
- html_templates (lines 449–454): add `'view-bestiary.html'` after `'view-encounters.html'`.

loader.js MODULES (lines 10–133) must mirror build.py MODULES exactly (CI sync-check, STAB-07): srd-monsters.js after `'core/srd-spells.js'` (line 104); four bestiary modules after `'features/encounters/encounters-crud.js'` (line 75).

systems/tab-registry.js TAB_RENDER_REGISTRY ends with the `dmscreen` entry (lines 88–92), closing `};` at line 93. Existing keys match `data-view` attributes (e.g. `encounter`, `notes`). The bestiary key MUST be `bestiary` to match the nav button below.

assets/templates/header.html nav (lines 46–68): the `<button class="nav-tab" data-view="dmscreen" ...>` is line 65; the Debug button is line 66. Insert the bestiary nav button among the content tabs (e.g. after the `dmscreen` button, before Debug).

core/data.js initializeData() returns the object at lines 3–37; `_nextId: {}` is the last field (line 36).

version-migration.js MIGRATIONS object: last entry is `'2.6.1'` (lines 52–65), object closes `}` at line 66. Follow the exact `if (!data.X) data.X = ...; return data;` shape.

assets/styles.css @import hub ends at `@import url('styles/command-palette.css');` (line 22).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-schema + migration 3.0.0</name>
  <files>core/data.js, systems/spellslots/version-migration.js</files>
  <read_first>
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/core/data.js (current initializeData)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/systems/spellslots/version-migration.js (MIGRATIONS shape, '2.6.1' entry)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/.planning/phases/03-bestiary/03-PATTERNS.md (Muster 7 + Muster 8)
  </read_first>
  <action>
    In `core/data.js` initializeData(), after the `_nextId: {}` field add two fields: `bestiary: []` (custom creatures — CRUD + Undo + Export, per D-12/D-13) and `bestiaryFavorites: []` (ID-key strings only, per D-11). SRD data must NOT be added here.
    In `systems/spellslots/version-migration.js`, add a new `'3.0.0'` entry to the MIGRATIONS object after the `'2.6.1'` entry: it sets `data.bestiary = []` if absent and `data.bestiaryFavorites = []` if absent, then `return data` — exact same defensive shape as the `'2.6.1'` migration. Keys are auto-sorted by `migrateData()`, so placement is for readability only.
  </action>
  <acceptance_criteria>
    - `core/data.js` initializeData() return object contains keys `bestiary` and `bestiaryFavorites` both initialized to `[]`.
    - `systems/spellslots/version-migration.js` MIGRATIONS object contains key `'3.0.0'` whose handler sets `data.bestiary` and `data.bestiaryFavorites` to `[]` when missing and returns `data`.
    - No SRD monster data appears in `core/data.js`.
    - `npx jest tests/unit/migration.test.js -t "3.0.0"` passes (after Task 5 adds the test).
  </acceptance_criteria>
  <verify>
    <automated>npx jest tests/unit/migration.test.js -t "3.0.0"</automated>
  </verify>
  <done>D.bestiary/D.bestiaryFavorites exist fresh AND are created on legacy campaigns by migration 3.0.0.</done>
</task>

<task type="auto">
  <name>Task 2: Create 5 module stubs with window-exports</name>
  <files>core/srd-monsters.js, features/bestiary/bestiary-render.js, features/bestiary/bestiary-crud.js, features/bestiary/bestiary-editor.js, features/bestiary/bestiary-actions.js</files>
  <read_first>
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/core/srd-spells.js (lazy-cache header pattern)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/features/npcs/npc-render.js (defensive render-container pattern, lines 1–60)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/.planning/phases/03-bestiary/03-PATTERNS.md (module-head convention + each module's analog)
  </read_first>
  <action>
    Create five new JS files as minimal stubs (content filled by plans 02–05) — each with the `// [SECTION:NAME]` header banner per convention, a defensive container guard where it renders, and its window-exports so loader/build registration and tab-registry calls resolve at runtime:
    - `core/srd-monsters.js`: define `let _srdMonstersCache = null;` and `function getSRDMonsters()` returning `_srdMonstersCache ??= []` (empty array stub); `window.getSRDMonsters = getSRDMonsters;`. Include the CC-BY-4.0 attribution comment header (SRD 5.1 DE, openrpg.de, Wizards of the Coast). NEVER assign to `D`.
    - `features/bestiary/bestiary-render.js`: `function renderBestiaryList()` with the defensive `$('bestiary-list')` container guard + DEBUG warn (returns early; body filled by plan 03); `function cleanupBestiaryEditor() {}` (no-op stub referenced by tab-registry cleanup); export both via `window.`.
    - `features/bestiary/bestiary-crud.js`: empty `function saveBestiary() {}` and `function deleteBestiaryEntry() {}` stubs + window-exports.
    - `features/bestiary/bestiary-editor.js`: empty `function openBestiaryEditor() {}` stub + window-export.
    - `features/bestiary/bestiary-actions.js`: empty `function addBestiaryToInitiative() {}`, `function addBestiaryToEncounter() {}`, `function toggleBestiaryFavorite() {}`, `function isBestiaryFavorite() { return false; }`, `function getBestiaryMonster() { return null; }` stubs + window-exports.
    Before creating each function name, grep `features/`, `systems/`, `ui/`, `core/` to confirm it is not already defined (avoid the build dedup blind spot). Do NOT use `const X = window.X` inside any function.
  </action>
  <acceptance_criteria>
    - `core/srd-monsters.js` defines and exports `getSRDMonsters`; file contains the CC-BY-4.0 attribution header; no `D.` assignment of SRD data anywhere in the file.
    - `features/bestiary/bestiary-render.js` exports `renderBestiaryList` and `cleanupBestiaryEditor`; renderBestiaryList early-returns when `$('bestiary-list')` is null.
    - `features/bestiary/bestiary-crud.js`, `bestiary-editor.js`, `bestiary-actions.js` exist and export the named stubs listed in the action.
    - `grep -rn "function renderBestiaryList" features/ | wc -l` returns 1 (no duplicate definition).
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');['core/srd-monsters.js','features/bestiary/bestiary-render.js','features/bestiary/bestiary-crud.js','features/bestiary/bestiary-editor.js','features/bestiary/bestiary-actions.js'].forEach(f=>{if(!fs.existsSync(f))throw new Error('missing '+f)});console.log('stubs ok')"</automated>
  </verify>
  <done>Five module stubs exist with correct window-exports; no duplicate function names introduced.</done>
</task>

<task type="auto">
  <name>Task 3: Register modules/CSS/template in build.py + loader.js + styles.css + tab-registry + nav</name>
  <files>build.py, loader.js, assets/styles.css, systems/tab-registry.js, assets/templates/header.html, assets/styles/bestiary.css, assets/templates/view-bestiary.html</files>
  <read_first>
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/build.py (MODULES lines 40–155, css_files 418–426, html_templates 449–454, sync-check 462–467)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/loader.js (MODULES lines 10–133)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/systems/tab-registry.js (TAB_RENDER_REGISTRY, dmscreen entry + closing brace)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/assets/templates/header.html (nav-tabs, lines 46–68)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/assets/styles.css (@import hub)
  </read_first>
  <action>
    Wire all new artifacts into BOTH build systems and the registries (this is the locked module-sync constraint — every list must stay in sync or the CI/pre-build check fails):
    - `build.py` MODULES: add `'core/srd-monsters.js'` immediately after `'core/srd-spells.js'`; add a `# Bestiary-Module (Phase 3)` comment then the four `'features/bestiary/bestiary-render.js'`, `'...bestiary-crud.js'`, `'...bestiary-editor.js'`, `'...bestiary-actions.js'` immediately after `'features/encounters/encounters-crud.js'`.
    - `build.py` css_files: add `'bestiary.css'` after `'tools.css'`.
    - `build.py` html_templates: add `'view-bestiary.html'` after `'view-encounters.html'`.
    - `loader.js` MODULES: mirror the exact same JS insertions at the same relative positions (`core/srd-monsters.js` after `core/srd-spells.js`; four bestiary modules after `features/encounters/encounters-crud.js`).
    - `assets/styles.css`: add `@import url('styles/bestiary.css');` after the command-palette import.
    - `systems/tab-registry.js`: add a `bestiary` entry to TAB_RENDER_REGISTRY after the `dmscreen` entry: `renders: ['renderBestiaryList'], init: null, cleanup: 'cleanupBestiaryEditor'`.
    - `assets/templates/header.html`: add a nav button `<button class="nav-tab" data-view="bestiary" draggable="true" role="tab" aria-selected="false">🐉 Bestiar</button>` among the content tabs (after the dmscreen tab, before the Debug button).
    Create placeholder `assets/styles/bestiary.css` (header comment only) and `assets/templates/view-bestiary.html` (minimal `<section id="view-bestiary" class="view bestiary-view"></section>`) so the build's css_files/html_templates reads do not warn — plan 03 fills both.
  </action>
  <acceptance_criteria>
    - `build.py` MODULES list contains `'core/srd-monsters.js'`, `'features/bestiary/bestiary-render.js'`, `'features/bestiary/bestiary-crud.js'`, `'features/bestiary/bestiary-editor.js'`, `'features/bestiary/bestiary-actions.js'`.
    - `build.py` css_files contains `'bestiary.css'`; html_templates contains `'view-bestiary.html'`.
    - `loader.js` MODULES contains the identical 5 JS paths in the same relative order as build.py.
    - `systems/tab-registry.js` TAB_RENDER_REGISTRY contains a `bestiary:` key with `renders: ['renderBestiaryList']` and `cleanup: 'cleanupBestiaryEditor'`.
    - `assets/templates/header.html` contains exactly one `data-view="bestiary"` nav button with text `🐉 Bestiar`.
    - `assets/styles.css` contains `@import url('styles/bestiary.css');`.
    - `python build.py` exits 0 and prints "Pre-Build-Checks bestanden" (module-list sync + duplicate-function checks pass).
  </acceptance_criteria>
  <verify>
    <automated>python build.py</automated>
  </verify>
  <done>Production build succeeds with all bestiary artifacts registered and module lists in sync; the 🐉 Bestiar tab renders (empty) and is wired to renderBestiaryList via the registry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Wave-0 unit tests (SRD schema + migration 3.0.0)</name>
  <files>tests/unit/srd-monsters.test.js, tests/unit/migration.test.js</files>
  <read_first>
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/tests/unit/migration.test.js (existing migration tests — extend, do not recreate)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/jest.config.cjs (test discovery + setup)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/.planning/phases/03-bestiary/03-VALIDATION.md (Per-Task Verification Map: Schema + Migration rows)
  </read_first>
  <behavior>
    - getSRDMonsters() returns an Array (empty in this plan; plan 02 fills it). When non-empty, every entry must expose string `name`, string `cr`, numeric `ac`, numeric `hp` — assert these only over `getSRDMonsters()` length when > 0 so the test is green now and stays meaningful after plan 02.
    - Migration 3.0.0 applied to a legacy data object lacking bestiary fields produces `data.bestiary === []` and `data.bestiaryFavorites === []`; applied to data that already has entries, it does NOT overwrite them.
  </behavior>
  <action>
    Create `tests/unit/srd-monsters.test.js` asserting `getSRDMonsters()` returns an Array and (guarded by `if (length)`) that each entry has `name`/`cr` strings and `ac`/`hp` numbers — load the module the same way existing core unit tests load source files (see migration.test.js for the require/eval harness). Extend `tests/unit/migration.test.js` with a `3.0.0` describe/test block asserting the migration creates empty `bestiary`/`bestiaryFavorites` on legacy data and preserves existing arrays. Use the existing test harness conventions; do not add watch-mode flags.
  </action>
  <acceptance_criteria>
    - `tests/unit/srd-monsters.test.js` exists and asserts `getSRDMonsters()` is an Array with the guarded schema check.
    - `tests/unit/migration.test.js` contains a test matched by `-t "3.0.0"`.
    - `npx jest tests/unit/srd-monsters.test.js` exits 0.
    - `npx jest tests/unit/migration.test.js -t "3.0.0"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx jest tests/unit/srd-monsters.test.js tests/unit/migration.test.js -t "3.0.0|getSRDMonsters"</automated>
  </verify>
  <done>Unit Wave-0 tests exist and pass against the stubs + migration.</done>
</task>

<task type="auto">
  <name>Task 5: Wave-0 E2E scaffold (SC1/SC2/SC3 scenarios)</name>
  <files>tests/e2e/features/bestiary.spec.js</files>
  <read_first>
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/tests/e2e/features/wiki.spec.js (E2E structure analog cited by VALIDATION.md)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/playwright.config.js (base URL / dist target)
    - D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular/.planning/phases/03-bestiary/03-VALIDATION.md (Per-Task Verification Map: exact `-g` test titles)
  </read_first>
  <action>
    Create `tests/e2e/features/bestiary.spec.js` with test titles that EXACTLY match the grep filters downstream plans verify against (these are contract names — do not rename): `Goblin suchen`, `CR-Filter`, `Typ-Filter`, `Wuerfelklick`, `Kreatur anlegen`, `Kreatur bearbeiten`, `Kreatur loeschen`, `Undo loeschen`, `Zur Initiative`, `Zu Encounter`. In this plan they may be `test.fixme(...)` / skipped placeholders that navigate to the 🐉 Bestiar tab and assert the view is visible — they become real assertions in plans 02–05. Mirror the wiki.spec.js setup (load dist build, open tab via `data-view`). Each placeholder must reference the bestiary tab so it fails meaningfully if the tab is missing, not silently pass.
  </action>
  <acceptance_criteria>
    - `tests/e2e/features/bestiary.spec.js` exists and contains test titles matching each of: `Goblin suchen`, `CR-Filter`, `Typ-Filter`, `Wuerfelklick`, `Kreatur anlegen`, `Kreatur bearbeiten`, `Kreatur loeschen`, `Undo loeschen`, `Zur Initiative`, `Zu Encounter`.
    - At least one non-skipped test opens the 🐉 Bestiar tab and asserts `#view-bestiary` is visible.
    - `npx playwright test tests/e2e/features/bestiary.spec.js --list` enumerates all 10 named tests without error.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/features/bestiary.spec.js --list</automated>
  </verify>
  <done>E2E spec file exists with all 10 contract-named scenarios; the tab-visibility test passes against the registered empty tab.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted campaign JSON → migrateData() | Legacy/imported data crosses into runtime D on load |
| module list (build.py) ↔ module list (loader.js) | Sync drift silently breaks dev or prod build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | migration 3.0.0 on imported data | mitigate | Defensive `if (!data.X)` guards; never overwrite existing user arrays; SRD data never written to D |
| T-03-02 | Denial of Service | SRD data leaking into D/Undo snapshots | mitigate | srd-monsters.js stub returns from a module closure cache; no `D.* = getSRDMonsters()` anywhere (grep-verified in Task 2) |
| T-03-03 | Tampering | build module-list desync | mitigate | `python build.py` runs check_module_list_sync (STAB-07) as the Task 3 gate |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit: none added) |
</threat_model>

<verification>
- `python build.py` succeeds (module sync + dedup checks pass, CSS + template included).
- `npx jest tests/unit/srd-monsters.test.js tests/unit/migration.test.js` green.
- `npx playwright test tests/e2e/features/bestiary.spec.js --list` lists 10 scenarios.
</verification>

<success_criteria>
Bestiary tab is present, registered, and renders (empty); production build is green; migration 3.0.0 + D-schema fields exist; 5 module stubs are exported and registered in both module lists; Wave-0 test files exist for every SC1/SC2/SC3 behavior + the SRD schema.
</success_criteria>

<output>
Create `.planning/phases/03-bestiary/03-01-SUMMARY.md` when done.
</output>
