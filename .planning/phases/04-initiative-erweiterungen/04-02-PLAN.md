---
phase: 04-initiative-erweiterungen
plan: 02
type: execute
wave: 1
depends_on: [04-01]
files_modified:
  - features/initiative-statblock.js
  - features/initiative.js
  - features/bestiary/bestiary-render.js
  - ui/actions/combat-actions.js
  - tests/e2e/features/initiative.spec.js
autonomous: true
requirements: [INIT-01]

must_haves:
  truths:
    - "A book button appears in every initiative combatant row (D-02, D-03 universal, never a dead button)"
    - "Clicking book on a Bestiary monster (with statblockRef) opens a right-side drawer showing the full parchment statblock with Aktionen, Traits, Saves, Senses (D-01, SC1)"
    - "Clicking book on a player/manual combatant without statblockRef opens the drawer showing basic info HP/AC/Effekte (D-03)"
    - "Dice formulas in the statblock panel are clickable and roll via the existing roller (D-04)"
    - "Statblock trait text passes through sanitizeHTML BEFORE renderClickableDice (XSS, Phase-3 sanitize-then-dice order)"
  artifacts:
    - path: "features/initiative-statblock.js"
      provides: "show/close drawer + basic-info fallback calling window.renderStatblockHTML (D-01/D-03/D-04)"
      contains: "function showInitStatblockPanel"
      min_lines: 60
    - path: "features/bestiary/bestiary-render.js"
      provides: "renderStatblockHTML(monster, source) extracted as a pure top-level string function, reused by renderBestiaryDetail"
      contains: "function renderStatblockHTML"
    - path: "features/initiative.js"
      provides: "book button injected into renderInit() init-right cluster (D-02)"
      contains: "show-init-statblock"
    - path: "ui/actions/combat-actions.js"
      provides: "show-init-statblock / close-init-statblock handlers"
      contains: "show-init-statblock"
  key_links:
    - from: "features/initiative.js"
      to: "showInitStatblockPanel"
      via: "data-action show-init-statblock button in each init row"
      pattern: "show-init-statblock"
    - from: "features/initiative-statblock.js"
      to: "getBestiaryMonster"
      via: "statblockRef lookup with String id, not parseEntityId"
      pattern: "getBestiaryMonster"
    - from: "features/initiative-statblock.js"
      to: "renderStatblockHTML"
      via: "window.renderStatblockHTML(monster, source)"
      pattern: "renderStatblockHTML"
---

<objective>
Implement INIT-01: a book button on every initiative row that opens a right-side drawer (mobile: bottom-sheet) showing the full parchment statblock for Bestiary monsters, or basic info for players/manual combatants. Reuse the Phase-3 statblock structure and the clickable-dice roller.

To avoid the tab-bound renderBestiaryDetail trap (RESEARCH.md Falle 1), extract the statblock-building logic into a pure renderStatblockHTML(monster, source) in bestiary-render.js that returns a string, and have BOTH renderBestiaryDetail() (existing) and the new drawer call it (DRY, single source of truth).

Purpose: SC1 — the DM sees the full statblock from initiative without leaving the combat view.
Output: working drawer + book button + data-actions + green statblock E2E suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-initiative-erweiterungen/04-PATTERNS.md
@.planning/phases/04-initiative-erweiterungen/04-UI-SPEC.md
@.planning/phases/04-initiative-erweiterungen/04-VALIDATION.md
@.planning/phases/04-initiative-erweiterungen/04-01-SUMMARY.md

<interfaces>
From features/bestiary/bestiary-render.js:
  function renderBestiaryDetail(id, source)  // lines 218-437; statblock build at lines 349-423 (sections 1-20)
  function renderClickableDice(text)          // window-exported
  // renderTraitList(items), attrMod(score), renderInlineList(arr) are NESTED inside renderBestiaryDetail
From features/bestiary/bestiary-actions.js:
  function getBestiaryMonster(id, source)     // String id for SRD, NEVER parseEntityId
From features/initiative.js:
  function getCombatant(id); function renderCombatantEffects(combatant)  // XSS-safe effects HTML
Globals: window.D, esc, sanitizeHTML, $, document.createElement, showToast

Drawer DOM contract (UI-SPEC Component Visual Contracts INIT-01):
  panel id init-statblock-panel ; class modal-overlay init-statblock-drawer ; open via .show
  content container .init-statblock-content ; overlay-dim click closes
  button: class btn-icon init-statblock-btn, data-action show-init-statblock, data-id, title
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract renderStatblockHTML(monster, source) and implement the drawer</name>
  <files>features/bestiary/bestiary-render.js, features/initiative-statblock.js</files>
  <read_first>
    - features/bestiary/bestiary-render.js (renderBestiaryDetail lines 218-437; extract statblock build at 349-423 plus nested helpers renderTraitList 258-281, attrMod 246-249, renderInlineList 284-287, speed/saves/skills/xp setup 289-347)
    - features/initiative-statblock.js (the Wave-0 skeleton with placeholder renderStatblockHTML and showInitStatblockPanel)
    - .planning/phases/04-initiative-erweiterungen/04-PATTERNS.md (lines 25-119 dynamic panel creation, statblockRef lookup, window exports, basic-info fallback)
    - .planning/phases/04-initiative-erweiterungen/04-RESEARCH.md (Muster 1 lines 190-286; Falle 1/2/3 lines 485-509)
    - .planning/phases/04-initiative-erweiterungen/04-UI-SPEC.md (Copywriting INIT-01 lines 102-115; Drawer spec and responsive lines 184-220)
    - CLAUDE.md (no duplicate top-level function names; no const X = window.X inside functions)
  </read_first>
  <action>
    In bestiary-render.js, refactor renderBestiaryDetail() so the statblock-string assembly (current lines ~245-423: nested helpers attrMod/renderTraitList/renderInlineList, the speed/saves/skills/xp setup, and the statblockHtml build of sections 1-20) becomes a new top-level function renderStatblockHTML(monster, source) that RETURNS the statblockHtml string. Update renderBestiaryDetail() to call renderStatblockHTML(monster, source) for that portion, keeping its panel lookup, monster lookup, portrait, action buttons, attribution, and panel.innerHTML write unchanged. Export window.renderStatblockHTML. Keep the sanitize-then-dice order inside renderTraitList exactly as-is (sanitizeHTML first, then renderClickableDice, Falle 3). The Wave-0 placeholder renderStatblockHTML in initiative-statblock.js MUST be removed in this same task to avoid a duplicate top-level name.
    In features/initiative-statblock.js, delete the Wave-0 placeholder renderStatblockHTML and call window.renderStatblockHTML(...) instead. Implement:
    - showInitStatblockPanel(cbId): get cb via getCombatant(cbId); if missing return. Lazily create the panel id init-statblock-panel (class modal-overlay init-statblock-drawer, inner .init-statblock-content, overlay-dim onclick closes when e.target equals panel) via document.createElement, analog showConcentrationModal at initiative.js:666-680. If cb.statblockRef, resolve var monster = getBestiaryMonster(cb.statblockRef.id, cb.statblockRef.source) using the String id directly, never parseEntityId. Monster found: content innerHTML = window.renderStatblockHTML(monster, cb.statblockRef.source). Otherwise and for no statblockRef: content innerHTML = renderBasicCombatantInfo(cb) (D-03). Add the show class.
    - closeInitStatblockPanel(): remove the show class.
    - renderBasicCombatantInfo(cb): a card with header esc(cb.name) plus Basisinfos suffix, HP esc(String(cb.currentHp||0)) slash esc(String(cb.maxHp||0)), RK esc(String(cb.ac||10)), active effects via window.renderCombatantEffects when available. Empty fallback text Keine weiteren Daten verfuegbar.
    Export showInitStatblockPanel and closeInitStatblockPanel to window. No const X = window.X inside any function.
  </action>
  <verify>
    <automated>cd "D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular" && python build.py</automated>
    <source_check>grep -c "function renderStatblockHTML" features/bestiary/bestiary-render.js returns 1; grep "window.renderStatblockHTML" features/bestiary/bestiary-render.js matches; grep -c "function renderStatblockHTML" features/initiative-statblock.js returns 0; grep "getBestiaryMonster" features/initiative-statblock.js matches</source_check>
  </verify>
  <acceptance_criteria>
    - features/bestiary/bestiary-render.js defines function renderStatblockHTML exactly once and exports it to window
    - features/initiative-statblock.js no longer defines function renderStatblockHTML and contains getBestiaryMonster and renderBasicCombatantInfo
    - features/initiative-statblock.js does NOT call parseEntityId on cb.statblockRef.id
    - python build.py completes with no duplicate-declaration SyntaxError
  </acceptance_criteria>
  <done>renderStatblockHTML is a single shared pure function; the drawer opens for both statblockRef and basic-info cases; build is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Inject the book button into renderInit() and register data-actions</name>
  <files>features/initiative.js, ui/actions/combat-actions.js</files>
  <read_first>
    - features/initiative.js (renderInit lines 104-204; the .init-right cluster at lines 183-194 where the book button goes; the lair-type early-return at 147-161 so the button is NOT added to lair rows)
    - ui/actions/combat-actions.js (CombatActions object lines 6-70; toggle-death-save-stop at line 10 as the -stop analog; non-stop action show-aoe-damage-modal at line 26)
    - .planning/phases/04-initiative-erweiterungen/04-PATTERNS.md (lines 123-134 book-button insertion; lines 515-562 combat-actions registration)
    - .planning/phases/04-initiative-erweiterungen/04-UI-SPEC.md (Button spec lines 177-182; Data-Action Registry lines 626-640)
  </read_first>
  <action>
    In features/initiative.js renderInit(), inside the standard combatant template .init-right flex cluster (around lines 192-193, after the effect button and the remove button), append the book button: class btn-icon init-statblock-btn, data-action show-init-statblock, data-id set to cb.id, title "Statblock anzeigen" when cb.statblockRef is present else "Basisinfos anzeigen". The book emoji is the visible label. Do NOT add it to the lair-type early-return branch (lines 147-161). Match the 24px size of existing init icon buttons.
    In ui/actions/combat-actions.js CombatActions object, register two handlers near the other initiative actions:
    - show-init-statblock mapped to ctx => showInitStatblockPanel(ctx.id)
    - close-init-statblock mapped to () => closeInitStatblockPanel()
    These do not need the -stop suffix because the button is the direct click target (no nested row-handler conflict), consistent with the existing show-aoe-damage-modal pattern.
  </action>
  <verify>
    <automated>cd "D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular" && python build.py</automated>
    <source_check>grep "show-init-statblock" features/initiative.js matches; grep "show-init-statblock" ui/actions/combat-actions.js matches; grep "close-init-statblock" ui/actions/combat-actions.js matches; grep "init-statblock-btn" features/initiative.js matches</source_check>
  </verify>
  <acceptance_criteria>
    - features/initiative.js contains a button with data-action show-init-statblock and class init-statblock-btn in the standard (non-lair) row template
    - ui/actions/combat-actions.js registers both show-init-statblock and close-init-statblock handlers
    - python build.py completes without error
  </acceptance_criteria>
  <done>Every non-lair combatant row renders the book button wired to the drawer handler.</done>
</task>

<task type="auto">
  <name>Task 3: Fill the statblock E2E suite, build, and run it</name>
  <files>tests/e2e/features/initiative.spec.js</files>
  <read_first>
    - tests/e2e/features/initiative.spec.js (the skipped statblock describe stub from Wave 0; beforeEach navigation lines 9-21; existing combatant-add test for how to seed a combatant)
    - features/bestiary/bestiary-actions.js (addBestiaryToInitiative creates monster combatants with statblockRef; how a monster reaches initiative)
    - .planning/phases/04-initiative-erweiterungen/04-VALIDATION.md (INIT-01 rows of Anforderungs-Test-Mapping; the exact filter command for the statblock subset)
    - .planning/phases/04-initiative-erweiterungen/04-UI-SPEC.md (drawer selectors: init-statblock-panel, show class, init-statblock-content, bestiary-dice clickable spans)
  </read_first>
  <action>
    Replace the Wave-0 skip placeholders in the statblock describe block with real Playwright tests against the freshly built dist bundle:
    - Test A: after adding a Bestiary monster to initiative, every init row exposes a button with data-action show-init-statblock (INIT-01 button visible).
    - Test B: clicking the book button on a monster row opens the panel id init-statblock-panel with the show class, containing init-statblock-content with at least one bestiary-statblock element (full statblock for a statblockRef monster).
    - Test C: a combatant without statblockRef (manually added combatant or player) shows the init-statblock-basic card in the drawer instead of a parchment statblock (D-03 basic info).
    - Test D: the panel contains at least one span with data-action bestiary-roll-dice (clickable dice present, D-04). Optionally click it and assert the dice/event-log reacts.
    Use the existing file:// dist-bundle navigation pattern. Keep every test title containing the word statblock so the -g filter selects them.
    Run python build.py first because the E2E tests load the dist bundle, then run the statblock subset.
  </action>
  <verify>
    <automated>cd "D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular" && python build.py && npm run test:e2e -- tests/e2e/features/initiative.spec.js -g statblock</automated>
  </verify>
  <acceptance_criteria>
    - The statblock E2E subset exits 0 (all four statblock tests pass)
    - The statblock describe block no longer contains test.skip or test.fixme placeholders
    - python build.py completes before the E2E run (tests load the fresh dist bundle)
  </acceptance_criteria>
  <done>The statblock E2E suite is green: book button present on every row, drawer opens with full statblock for monsters and basic info for non-monsters, clickable dice present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SRD/custom statblock text to drawer DOM | Trait/Action strings (intentionally contain b markup) reach renderStatblockHTML and are written via innerHTML |
| Combatant D-fields to drawer DOM | Name, HP, AC, effects rendered into the basic-info card |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering (XSS) | renderStatblockHTML trait/action text via innerHTML | mitigate | renderTraitList applies sanitizeHTML(item.desc) FIRST, then renderClickableDice on the sanitized string (Falle 3 order preserved during extraction). Reuses the exact existing renderTraitList code. |
| T-04-05 | Tampering (XSS) | renderBasicCombatantInfo writes cb.name/HP/AC into innerHTML | mitigate | All D-values wrapped in esc(); numeric fields cast via String() before esc(); effects rendered through the already-XSS-safe renderCombatantEffects(). |
| T-04-06 | Tampering | cb.statblockRef.id placed in lookup | mitigate | statblockRef.id resolved via getBestiaryMonster (String compare, no eval); not echoed raw into HTML attributes. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. Slopcheck N/A. |
</threat_model>

<verification>
- python build.py succeeds with the renderStatblockHTML extraction (no duplicate function name, no orphaned body)
- statblock E2E subset is green
- Manual (phase gate): drawer parchment look on desktop and bottom-sheet under 600px viewport (04-VALIDATION.md manual checks)
</verification>

<success_criteria>
- INIT-01 satisfied: book button on every row, right-side drawer shows full statblock for monsters and basic info for others, clickable dice work
- renderStatblockHTML is a single shared source of truth (DRY), sanitize-then-dice order preserved
- statblock E2E suite green
</success_criteria>

<output>
Create `.planning/phases/04-initiative-erweiterungen/04-02-SUMMARY.md` when done
</output>
