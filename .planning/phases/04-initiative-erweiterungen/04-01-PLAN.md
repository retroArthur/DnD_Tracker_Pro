---
phase: 04-initiative-erweiterungen
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - features/initiative-statblock.js
  - features/initiative-mob.js
  - loader.js
  - build.py
  - assets/styles/initiative.css
  - tests/unit/initiative-mob.test.js
  - tests/e2e/features/initiative.spec.js
autonomous: true
requirements: [INIT-01, INIT-02, INIT-03]

must_haves:
  truths:
    - "Both new modules load in the bundled build without console errors (registered in build.py + loader.js)"
    - "Unit tests for parseLegendaryResistanceCount(), getMobAlive(), calcMobHits(), and pool-HP creation pass (Nyquist Wave 0 contract, D-05/D-12/D-13)"
    - "E2E suites 'statblock', 'legendary', 'mob' exist as placeholder describes (filled by Waves 1-3)"
    - "CSS classes for pips, drawer, and mob row exist so later waves render correctly (D-08 exact Death-Save optik)"
  artifacts:
    - path: "features/initiative-mob.js"
      provides: "Pure mob/LR helper functions: parseLegendaryResistanceCount, getMobAlive, calcMobHits, createMobCombatant (D-05/D-12/D-13/D-14)"
      contains: "function parseLegendaryResistanceCount"
      min_lines: 80
    - path: "features/initiative-statblock.js"
      provides: "Statblock drawer skeleton: renderStatblockHTML, showInitStatblockPanel, closeInitStatblockPanel, renderBasicCombatantInfo (D-01/D-03)"
      contains: "function showInitStatblockPanel"
      min_lines: 40
    - path: "tests/unit/initiative-mob.test.js"
      provides: "Wave-0 unit tests anchoring the DMG mob formula and LR parsing"
      contains: "DMG-Mob-Regel"
    - path: "assets/styles/initiative.css"
      provides: "Pip, drawer, and mob-row CSS sections (UI-SPEC Component Visual Contracts)"
      contains: ".la-dot"
  key_links:
    - from: "loader.js"
      to: "features/initiative-statblock.js"
      via: "module list entry after bestiary-actions.js"
      pattern: "features/initiative-statblock.js"
    - from: "build.py"
      to: "features/initiative-mob.js"
      via: "module list entry after initiative-extras.js"
      pattern: "features/initiative-mob.js"
---

<objective>
Lay the Wave-0 foundation for Phase 4: create the two new module files (`initiative-statblock.js`, `initiative-mob.js`) with their pure, testable functions implemented, register both modules in `build.py` AND `loader.js` (kept in sync per the locked Architecture Note), append all new CSS sections to `initiative.css`, and create the Nyquist test scaffolding (`initiative-mob.test.js` unit suite + the three E2E describe blocks).

This plan implements the DMG-mob formula and the LR-parse regex up front so the single [ASSUMED] formula (D-13b) is anchored by a unit test from the start. The UI wiring of these functions into `renderInit()` happens in Waves 1-3.

Purpose: Unblock parallel-safe implementation of INIT-01/02/03 and satisfy the binding Nyquist Wave-0 contract (`04-VALIDATION.md`).
Output: 2 new modules (with pure logic + panel skeleton), build/loader registration, CSS, 1 new unit test file, 3 E2E describe stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-initiative-erweiterungen/04-RESEARCH.md
@.planning/phases/04-initiative-erweiterungen/04-PATTERNS.md
@.planning/phases/04-initiative-erweiterungen/04-VALIDATION.md
@.planning/phases/04-initiative-erweiterungen/04-UI-SPEC.md

<interfaces>
<!-- Functions the new modules will reuse (already exported to window). Use directly — no exploration needed. -->
From features/bestiary/bestiary-actions.js:
  function getBestiaryMonster(id, source)   // SRD: m._id === id (String); custom: c.id === parseEntityId(id)
  var BESTIARY_MAX_QUANTITY = 100           // DoS clamp
From features/bestiary/bestiary-render.js (window-exported):
  window.renderBestiaryDetail(id, source)   // tab-bound — do NOT call from initiative
  window.renderClickableDice(text)          // wraps dice formulas in clickable spans
From features/initiative.js:
  function getCombatant(id)                  // D.initiative.combatants.find(c => c.id === id)
  function nextId(type)                      // unique-id generator (global)
Globals available at runtime: window.D, window.save, window.saveUndoState, window.showToast, esc, sanitizeHTML, $

<!-- DMG mob-rule formula — [ASSUMED], anchored by this plan's unit test (D-13b). -->
needed = max(2, targetAC - attackBonus); fraction = (21 - needed) / 20; hits = floor(count * fraction)
Anchor case: 10 Goblins, +4 attack, AC 15 → needed=11, fraction=0.5, hits=5
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create initiative-mob.js with pure logic + statblock module skeleton</name>
  <files>features/initiative-mob.js, features/initiative-statblock.js</files>
  <read_first>
    - features/bestiary/bestiary-actions.js (analog: addBestiaryToInitiative lines 64-129 — combatant creation, HP-variation, dexMod, statblockRef shape; getBestiaryMonster lines 11-18)
    - features/initiative.js (analog: modHp HP-mutation pattern; getCombatant lines 9-12; renderDeathSaves lines 494-545 for pip structure reference)
    - .planning/phases/04-initiative-erweiterungen/04-PATTERNS.md (section "features/initiative-mob.js" lines 268-461 — exact function structures to copy; section "features/initiative-statblock.js" lines 25-119)
    - .planning/phases/04-initiative-erweiterungen/04-RESEARCH.md (Muster 3 LR-parse regex lines 341-373; Muster 4 mob model + calcMobHits lines 381-450)
    - CLAUDE.md (Build dedup rules: no `const X = window.X` inside functions; no duplicate top-level function names)
  </read_first>
  <action>
    Create `features/initiative-mob.js` with header `// [SECTION:INITIATIVE_MOB]`. Implement these PURE functions (no DOM access at module load, must run under Jest vm.runInContext):
    - `parseLegendaryResistanceCount(monster)` — returns integer. Matches `(\d+)-mal\s+t[äa]glich` (German SRD, e.g. "Legendäre Resistenz (3-mal täglich)"), `\((\d+)\/Day\)` (English fallback), and the HTML-string custom-creature case (`typeof monster.traits === 'string'`). Returns 0 when no match (D-05/D-06).
    - `getMobAlive(cb)` — returns `Math.max(0, Math.ceil(cb.mob.poolHp / cb.mob.individualMaxHp))`; returns 1 when no `cb.mob` (D-12).
    - `calcMobHits(count, attackBonus, targetAC)` — O(1), NO loop: `needed = Math.max(2, targetAC - attackBonus)`; if `needed >= 20` return `Math.max(1, Math.floor(count*0.05))`; else `fraction = (21-needed)/20`, return `Math.max(0, Math.floor(count*fraction))` (D-13b, [ASSUMED]).
    - `createMobCombatant(monster, count, source)` — returns a combatant object. Sum `count` HP-variation rolls (`Math.max(1, Math.round((monster.hp||1)*(0.9+Math.random()*0.2)))`) into `totalHp`; set `individualMaxHp = Math.round(monster.hp||1)`; `dexMod = Math.floor(((monster.dex||10)-10)/2)`; name `monster.name + '-Schwarm'`; `maxHp`/`currentHp` = totalHp; `type: 'monster'`; `statblockRef = { source: source||'srd', id: source === 'custom' ? monster.id : monster._id }`; `mob: { count, poolHp: totalHp, maxPoolHp: totalHp, individualMaxHp, attackMode: 'nfach' }`. Use `nextId('combatants')` via window guard (id assignment can use `window.nextId ? window.nextId('combatants') : Date.now()` so tests don't crash).
    - Stub (empty-safe) the UI-coupled functions that Waves 1-3 will flesh out: `renderMobRow(cb, i, init)` returns `''`, `applyMobDamage(cbId, damage)` no-op-safe, `rollMobAttack(cbId)` no-op-safe, `setMobAttackMode(cbId, mode)` no-op-safe, `dissolveMob(cbId)` no-op-safe. Mark each `// Wave 3 fills implementation`.
    - Export ALL functions to window: `window.parseLegendaryResistanceCount = parseLegendaryResistanceCount;` etc.
    Then create `features/initiative-statblock.js` with header `// [SECTION:INITIATIVE_STATBLOCK]` and skeleton functions: `showInitStatblockPanel(cbId)`, `closeInitStatblockPanel()`, `renderStatblockHTML(monster, source)` (return `''` placeholder — Wave 1 fills), `renderBasicCombatantInfo(cb)` (return `''` placeholder — Wave 1 fills). Export `showInitStatblockPanel`, `closeInitStatblockPanel` to window. Add comment `// Kein direkter Aufruf von renderBestiaryDetail() — tab-spezifisch (RESEARCH.md Falle 1)`.
    Do NOT use `const X = window.X` inside any function body. Use module-level `var` or direct `window.X()` calls.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');['features/initiative-mob.js','features/initiative-statblock.js'].forEach(f=>{const s=fs.readFileSync(f,'utf8');if(/function\s+\w+\s*\([^)]*\)\s*\{[^}]*const\s+\w+\s*=\s*window\./.test(s))throw new Error('const X=window.X in function: '+f);});console.log('OK no forbidden imports')"</automated>
  </verify>
  <acceptance_criteria>
    - `features/initiative-mob.js` exists and contains `function parseLegendaryResistanceCount`, `function getMobAlive`, `function calcMobHits`, `function createMobCombatant`
    - `features/initiative-statblock.js` exists and contains `function showInitStatblockPanel` and `function closeInitStatblockPanel`
    - Grep `grep -c "window\.parseLegendaryResistanceCount\|window\.getMobAlive\|window\.calcMobHits"` returns 3 (all exported)
    - No `const X = window.X` appears inside any function body (build-dedup safety) — verify command exits 0
  </acceptance_criteria>
  <done>Both new module files exist; mob-logic functions are fully implemented and window-exported; statblock module has the panel skeleton; no build-dedup-unsafe patterns present.</done>
</task>

<task type="auto">
  <name>Task 2: Register both modules in build.py + loader.js and append CSS sections</name>
  <files>loader.js, build.py, assets/styles/initiative.css</files>
  <read_first>
    - loader.js (lines 76-116 — bestiary block ends at line 81 `features/bestiary/bestiary-actions.js`; `features/initiative-extras.js` at line 113)
    - build.py (lines 104-145 — same module-list ordering; bestiary-actions.js at line 110, initiative-extras.js at line 139; CSS list line 427, templates line 458)
    - assets/styles/initiative.css (analog: `.death-save-dot` block lines 298-329 — copy 16px size, 50% radius, scale(1.15) hover, 0.15s transition; `.init-entry` grid)
    - .planning/phases/04-initiative-erweiterungen/04-UI-SPEC.md (Component Visual Contracts — full CSS specs for `.init-statblock-drawer`, `.la-pips`/`.la-dot`, `.lr-pips`/`.lr-dot`/`.lr-reset-btn`, `.init-info--mob`/`.init-mob-*`; Responsive section for mobile bottom-sheet; CSS File Assignment lines 600-610)
    - CLAUDE.md (Architecture: module lists in build.py + loader.js MUST stay synchronized)
  </read_first>
  <action>
    In BOTH `loader.js` and `build.py`, add two module entries to the module list, keeping the lists byte-identical in ordering:
    - `'features/initiative-statblock.js'` — insert immediately AFTER `'features/bestiary/bestiary-actions.js'` (loader.js line 81 / build.py line 110).
    - `'features/initiative-mob.js'` — insert immediately AFTER `'features/initiative-extras.js'` (loader.js line 113 / build.py line 139).
    Then append four new CSS sections to the END of `assets/styles/initiative.css`, copying the exact specs from 04-UI-SPEC.md Component Visual Contracts (do NOT introduce new tokens — use `var(--gold)`, `var(--purple)`, `var(--red)`, `var(--green)`, `var(--yellow)`, `var(--bg-dark)`, `var(--bg-card)`, `var(--border)`, `var(--text-dim)`):
    1. `/* Statblock-Drawer (INIT-01) */` — `.init-statblock-drawer` (position:fixed; top:0; right:0; width:420px; height:100%; z-index:1000; display:none; justify-content:flex-end), `.init-statblock-drawer.show { display:flex }`, `.init-statblock-content` (background:var(--bg-dark); width:100%; height:100%; overflow-y:auto; padding:16px; box-shadow:-4px 0 24px rgba(0,0,0,0.5)), `.init-statblock-basic`, plus the `@media (max-width: 600px)` bottom-sheet override (width:100vw; height:60vh; top:auto; bottom:0; border-radius:12px 12px 0 0).
    2. `/* Legendäre Aktionen Pips (INIT-02 LA) */` — `.la-pips`, `.la-label` (color:var(--gold); font-weight:600; font-size:0.75em), `.la-dots`, `.la-dot` (16px; border:2px solid var(--gold); border-radius:50%; background:var(--bg-dark)), `.la-dot.active { background:var(--gold) }`, `.la-dot:hover { transform:scale(1.15) }`.
    3. `/* Legendäre Widerstands-Pips (INIT-02 LR) */` — `.lr-pips`, `.lr-label` (color:var(--purple)), `.lr-dots`, `.lr-dot` (border-color:var(--purple)), `.lr-dot.active { background:var(--purple) }`, `.lr-reset-btn`.
    4. `/* Mob-Zeile (INIT-03) */` — `.init-info--mob`, `.init-mob-alive` + `.healthy`/`.bloodied`/`.critical`/`.defeated` color variants, `.init-mob-controls`, `.init-mob-label`, `.init-mob-mode-toggle`, `.init-mob-mode-btn` + `.active`, `.init-mob-dmg-inputs` + `.hidden`, `.init-mob-attack-btn`, `.init-mob-dissolve-btn`, `.init-mob-defeated-badge`.
    Ensure mobile uses `flex-wrap: wrap` on `.init-mob-controls`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const l=fs.readFileSync('loader.js','utf8');const b=fs.readFileSync('build.py','utf8');const css=fs.readFileSync('assets/styles/initiative.css','utf8');['initiative-statblock.js','initiative-mob.js'].forEach(m=>{if(!l.includes(m))throw new Error('loader.js missing '+m);if(!b.includes(m))throw new Error('build.py missing '+m);});['.la-dot','.lr-dot','.init-statblock-drawer','.init-mob-alive','.init-mob-defeated-badge'].forEach(c=>{if(!css.includes(c))throw new Error('CSS missing '+c);});console.log('OK registration + CSS')"</automated>
  </verify>
  <acceptance_criteria>
    - `loader.js` contains both `'features/initiative-statblock.js'` and `'features/initiative-mob.js'`
    - `build.py` contains both `'features/initiative-statblock.js'` and `'features/initiative-mob.js'`
    - `initiative-statblock.js` appears after `bestiary-actions.js` and `initiative-mob.js` after `initiative-extras.js` in both files
    - `assets/styles/initiative.css` contains all of: `.la-dot`, `.lr-dot`, `.init-statblock-drawer`, `.init-mob-alive`, `.init-mob-defeated-badge`
    - verify command exits 0
  </acceptance_criteria>
  <done>Both new modules are registered in build.py and loader.js in sync; all four CSS sections are appended to initiative.css using existing tokens only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create Wave-0 unit tests + E2E describe stubs, run build + unit suite</name>
  <files>tests/unit/initiative-mob.test.js, tests/e2e/features/initiative.spec.js</files>
  <read_first>
    - tests/unit/srd-monsters.test.js (analog: vm.runInContext pattern lines 1-50 — load a single module file in a vm context, extract `context.window.X`, assert)
    - tests/e2e/features/initiative.spec.js (existing structure lines 1-40 — beforeEach navigates to initiative view; describe-block style)
    - features/initiative-mob.js (the functions under test, from Task 1)
    - .planning/phases/04-initiative-erweiterungen/04-VALIDATION.md (Anforderungs-Test-Mapping — exact behaviors + commands; Wave-0-Anforderungen)
    - .planning/phases/04-initiative-erweiterungen/04-RESEARCH.md (the anchor case: 10 Goblins +4 vs AC 15 = 5 hits)
  </read_first>
  <behavior>
    - parseLegendaryResistanceCount: `{ traits: [{ name: 'Legendäre Resistenz (3-mal täglich)' }] }` → 3
    - parseLegendaryResistanceCount: `{ traits: [{ name: 'Legendary Resistance (2/Day)' }] }` → 2
    - parseLegendaryResistanceCount: `{ traits: [{ name: 'Mehrfachangriff' }] }` → 0 (no LR trait)
    - getMobAlive: `{ mob: { poolHp: 35, individualMaxHp: 7 } }` → 5 (ceil 35/7)
    - getMobAlive: `{ mob: { poolHp: 1, individualMaxHp: 7 } }` → 1 (ceil 1/7)
    - getMobAlive: `{ mob: { poolHp: 0, individualMaxHp: 7 } }` → 0 (defeated)
    - calcMobHits (DMG-Mob-Regel anchor): `calcMobHits(10, 4, 15)` → 5
    - calcMobHits high-AC clamp: `calcMobHits(10, 0, 30)` → ≥1 (only nat-20)
    - createMobCombatant: count=10, monster.hp=7 → `mob.count===10`, `mob.individualMaxHp===7`, `currentHp === maxHp === mob.poolHp`, `mob.poolHp > 0`
  </behavior>
  <action>
    Create `tests/unit/initiative-mob.test.js` using the `vm.runInContext` pattern from `srd-monsters.test.js`. Load `features/initiative-mob.js` into a vm context (provide `window:{}`, `console`, `Math`, and a stub `window.nextId = () => 1`). Extract `parseLegendaryResistanceCount`, `getMobAlive`, `calcMobHits`, `createMobCombatant` from `context.window`. Write the test cases listed in `<behavior>`. Title the DMG-formula test exactly `INIT-03 / DMG-Mob-Regel` so it is the single correction point for the [ASSUMED] formula.
    Then extend `tests/e2e/features/initiative.spec.js` by adding THREE new `test.describe` blocks at the end of the top-level `describe('Initiative System', ...)`: `test.describe('statblock', ...)`, `test.describe('legendary', ...)`, `test.describe('mob', ...)`. Each contains a single placeholder test using `test.fixme(...)` or `test.skip(...)` with a German title (e.g. `test.skip('📖-Button öffnet Statblock-Panel', ...)`) so the `-g "statblock"` / `-g "legendary"` / `-g "mob"` filters resolve to a non-empty suite now and Waves 1-3 replace the skips with real assertions. Do NOT leave the suites empty (an empty `-g` match would falsely pass).
    After writing, run the build and the unit suite to prove integrity.
  </action>
  <verify>
    <automated>cd "D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular" && python build.py && npm run test:unit -- --testPathPattern=initiative-mob</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/initiative-mob.test.js` exists and contains a test titled `INIT-03 / DMG-Mob-Regel`
    - `npm run test:unit -- --testPathPattern=initiative-mob` exits 0 (all unit tests green, including the calcMobHits(10,4,15)===5 anchor)
    - `python build.py` completes without error (both new modules concatenate cleanly, no duplicate-declaration SyntaxError)
    - `tests/e2e/features/initiative.spec.js` contains `describe('statblock'`, `describe('legendary'`, and `describe('mob'` (grep finds all three)
  </acceptance_criteria>
  <done>Unit suite is green and anchors the DMG formula; build is clean with both modules registered; the three E2E suites exist as skipped placeholders ready for Waves 1-3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SRD/custom statblock text → DOM | Trait/Action strings (intentionally contain `<b>`) reach `renderStatblockHTML` (Wave 1) — sanitize required |
| Combatant `D`-fields → DOM | Names, HP, mob labels rendered into init rows — escape required |
| User numeric input → loops | Mob count, attack bonus, target AC could feed iteration counts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering (XSS) | `parseLegendaryResistanceCount` reads `monster.traits` text | mitigate | This plan only parses (regex `match`/`parseInt`) — no DOM write. DOM rendering of statblock text is Wave 1's `sanitizeHTML`-first responsibility. |
| T-04-02 | Denial of Service | `createMobCombatant` HP-variation loop runs `count` times | mitigate | `count` is clamped to `BESTIARY_MAX_QUANTITY = 100` upstream in `addBestiaryToInitiative` (bestiary-actions.js:77). `calcMobHits` is O(1), no loop. |
| T-04-03 | Tampering | `nextId`/Date.now id assignment in module loaded by vm | accept | Test-only stub; runtime uses real `nextId`. No untrusted input. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (RESEARCH.md: "Kein externer Package-Install"). Slopcheck N/A. |
</threat_model>

<verification>
- `python build.py` succeeds — both new modules concatenate without duplicate-declaration errors
- `npm run test:unit -- --testPathPattern=initiative-mob` is green
- `loader.js` and `build.py` module lists both contain the two new modules in the correct positions
- `assets/styles/initiative.css` has the four new sections using only existing tokens
</verification>

<success_criteria>
- Wave-0 unit + E2E scaffolding exists and the unit suite passes (Nyquist Wave-0 satisfied)
- DMG mob formula anchored by `INIT-03 / DMG-Mob-Regel` unit test (single correction point for the [ASSUMED] formula)
- Both modules build-registered and load cleanly; CSS ready for Waves 1-3
</success_criteria>

<output>
Create `.planning/phases/04-initiative-erweiterungen/04-01-SUMMARY.md` when done
</output>
