---
phase: 06-spieler-verwaltung
plan: 05
type: execute
wave: 1
gap_closure: true
depends_on: [06-04]
files_modified:
  - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\features\party\party-render.js
  - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\ui\actions\entity-actions.js
  - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\assets\styles\party.css
  - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\tests\e2e\features\character-advancement.spec.js
autonomous: true
requirements: [CHAR-01]
user_setup: []

must_haves:
  truths:
    - "A per-group UI control (in the party-overview header) toggles D.settings.levelingMode between 'xp' and 'milestone'; the DM can switch into Milestone mode without hand-editing localStorage (D-07)"
    - "Toggling the mode persists via plain save() (no undo — a settings change, like inspiration) and re-renders the party so the control reflects the active mode"
    - "If the character detail modal is open when the mode is toggled, it refreshes so the XP branch / Milestone branch updates live (party-details.js already branches on the mode)"
    - "E2E coverage flips the mode via the new UI control (not via page.evaluate setting the flag) and asserts the detail modal hides .char-xp-section and shows the '+1 Level' button"
  artifacts:
    - path: "features/party/party-render.js"
      provides: "Leveling-mode toggle control rendered into #party-overview by renderPartyOverview()"
      contains: "set-leveling-mode"
    - path: "ui/actions/entity-actions.js"
      provides: "set-leveling-mode handler: sets D.settings.levelingMode, save(), renderParty(), refreshes open detail modal"
      contains: "set-leveling-mode"
    - path: "tests/e2e/features/character-advancement.spec.js"
      provides: "E2E case that toggles the mode via the UI control and asserts the detail-modal branch flips"
      contains: "set-leveling-mode"
  key_links:
    - from: "party-render.js renderPartyOverview"
      to: "D.settings.levelingMode"
      via: "reads current mode, renders a toggle control with data-action='set-leveling-mode' data-value (the target mode)"
      pattern: "set-leveling-mode"
    - from: "entity-actions.js set-leveling-mode"
      to: "D.settings.levelingMode + save() + renderParty() + refresh open modal"
      via: "set mode from ctx.value, plain save() (no undo, inspiration precedent), renderParty(), and if char-detail-modal open re-call showCharacterDetails(currentId)"
      pattern: "levelingMode"
---

<objective>
Close the UAT gap on CHAR-01 / D-07: the Milestone render branch exists (party-details.js:236-284) but is UNREACHABLE from the UI — `D.settings.levelingMode` is only ever READ and only ever written to `'xp'`. Add a per-group (campaign-wide) UI control in the party-overview header that toggles `D.settings.levelingMode` between `'xp'` and `'milestone'`, persists with plain `save()` (a settings change, no undo), re-renders the party, and refreshes the open character detail modal so the existing branch updates live. Add E2E coverage that flips the mode via the new control.

Purpose: Without a UI toggle, a DM can only enter Milestone mode by hand-editing localStorage or an import file — D-07's "toggle" must-have is half-satisfied (correct branch, dead control). This wires the existing render branch to a real control.
Output: A leveling-mode toggle in `#party-overview`, a `set-leveling-mode` data-action handler, supporting CSS, and an E2E case that switches the mode via the UI and asserts the detail modal flips its XP/Milestone branch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\.planning\phases\06-spieler-verwaltung\06-HUMAN-UAT.md
@D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\.planning\phases\06-spieler-verwaltung\06-04-SUMMARY.md
@D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\CLAUDE.md

<interfaces>
<!-- Extracted from source reads. Use directly — no codebase exploration needed. -->

renderPartyOverview() (features/party/party-render.js:360): builds the per-group quick-stats header into #party-overview (id from view-party.html:604). Called from renderParty (party-render.js:54). Sets container.innerHTML to a series of .party-stat-card blocks, then container.classList.add('show'). This is the natural home for a campaign-wide leveling-mode toggle. It reads `const D = window.D;` at the top and early-returns when chars.length === 0 (so the toggle only shows when a party exists — acceptable, the toggle is party-scoped).

D.settings.levelingMode: campaign-wide setting; default 'xp' (core/data.js:37) and migration backfill (version-migration.js:103) are ALREADY in place — do NOT re-add them. Read as `(D && D.settings && D.settings.levelingMode) || 'xp'`.

party-details.js render branch (already correct, do NOT change): in 'milestone' mode renders `.char-xp-milestone-section` (NOT `.char-xp-section`) with a `data-action="milestone-level-up"` "+1 Level" button; in 'xp' mode renders `.char-xp-section`. showCharacterDetails(id) (party-details.js:8) re-renders the modal body and calls showModal('char-detail-modal') (party-details.js:382) — so re-calling it after a mode toggle refreshes an open modal. The modal close button uses data-action="hide-modal" data-value="char-detail-modal".

data-action analogs:
  - 'set-view-mode': ctx => setViewMode(ctx.type, ctx.value)  (ui/actions/ui-actions.js:22) — clean precedent for a data-action that sets a mode from ctx params.
  - 'toggle-inspiration-stop' (ui/actions/entity-actions.js:29-38) — the precedent for a settings-style toggle: `ch.inspiration = !ch.inspiration; // KEIN saveUndoState(); window.save(); window.renderParty(); showToast(...)`. A settings change (like inspiration) uses plain save(), NOT saveUndoState()/pushUndo().

EntityActions registry (ui/actions/entity-actions.js): plain object; handlers receive ctx={id,value,type,event,target}. confirm-level-up / milestone-level-up already registered here. Register the new 'set-leveling-mode' on the same object.

esc(text): XSS-safe escaping (use for any interpolated value; labels here are static so surface is low, but follow the rule). showToast(msg, type?) for German feedback.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Leveling-mode toggle in party-overview + set-leveling-mode handler + E2E via UI control</name>
  <files>D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\features\party\party-render.js, D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\ui\actions\entity-actions.js, D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\assets\styles\party.css, D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\tests\e2e\features\character-advancement.spec.js</files>
  <read_first>
    - features/party/party-render.js:355-419 — renderPartyOverview(): how it reads D, the .party-stat-card markup, and the closing container.classList.add('show'). Append the toggle control to container.innerHTML (or render a dedicated leading/trailing control block) BEFORE the classList.add. The toggle must read the CURRENT mode so the active option is marked.
    - ui/actions/entity-actions.js:29-38 — toggle-inspiration-stop (plain save() + renderParty(), NO undo — the exact pattern to mirror for a settings toggle) and :43-70 — confirm-level-up / milestone-level-up (registry shape + how they re-open the modal via window.showCharacterDetails(ch.id)).
    - features/party/party-details.js:8-12, :382, :233-285 — showCharacterDetails(id) signature + showModal('char-detail-modal'); confirm the milestone/xp branch is already correct (do NOT modify it).
    - tests/e2e/features/character-advancement.spec.js:236-269 — the existing D-07 milestone test that SEEDS the mode via page.evaluate. The NEW case must instead flip the mode via the new UI control.
  </read_first>
  <action>
    In party-render.js renderPartyOverview(): read the current mode `const levelingMode = (D.settings && D.settings.levelingMode) || 'xp';`. Append a leveling-mode toggle block to the container's HTML (before `container.classList.add('show')`). Render it as a two-option segmented control inside a `.party-leveling-toggle` wrapper: an "⭐ XP" option and a "🎯 Meilenstein" option, each a button/element with `data-action="set-leveling-mode"` and `data-value="xp"` / `data-value="milestone"` respectively. Mark the option matching `levelingMode` with an `active` class so the current mode is visible. Use static German labels (no user interpolation). ALL interactivity via data-action delegation — NO inline onclick. Do NOT introduce a new module file (edit existing files only); confirm no loader.js/build.py change is needed.

    In entity-actions.js: register a `'set-leveling-mode'` handler on the EntityActions object. It must: read the target mode from `ctx.value` (accept only 'xp' or 'milestone'; ignore anything else); ensure `window.D.settings` exists (`D.settings = D.settings || {}`); set `D.settings.levelingMode = mode`; call `window.save()` (plain save — NO saveUndoState()/pushUndo(), this is a settings change like inspiration); call `window.renderParty()` (re-renders the overview so the active option updates); if the character detail modal is open (`document.getElementById('char-detail-modal')?.classList.contains('active')` or the project's modal-open check — mirror how confirm-level-up re-opens it), re-call `window.showCharacterDetails(currentDetailCharId)` to refresh the open modal so the XP/Milestone branch flips live — derive the open character's id from the modal (e.g. a data attribute on the modal or the project's existing detail-id tracking; if none exists, read it from the rendered modal DOM). Show a German `showToast` confirming the mode (e.g. "🎯 Meilenstein-Modus aktiv" / "⭐ XP-Modus aktiv"). No function-local `const X = window.X`; no duplicate top-level names.

    In party.css: style `.party-leveling-toggle` and its options (`.party-leveling-option`, `.party-leveling-option.active`) consistent with the existing `.party-stat-card` look (use var(--gold), var(--text-dim) etc.; the active option highlighted, the inactive muted). Mobile-friendly at 320px.

    In character-advancement.spec.js: add a NEW test in the `CHAR-01 / D-07: Milestone-Modus` describe block titled e.g. `'Toggle im Party-Overview schaltet auf Milestone — Detail-Modal flippt Branch'`. It must: loadApp; inject a single character via page.evaluate WITHOUT setting D.settings.levelingMode (leave it at the default 'xp'); renderParty; switch to the party view; assert the XP toggle option is active initially; CLICK the new UI control `[data-action="set-leveling-mode"][data-value="milestone"]` (do NOT set the flag via page.evaluate); open the detail modal via `[data-action="show-char-details"][data-id="..."]`; assert `.char-xp-section` is NOT visible and `[data-action="milestone-level-up"]` ("+1 Level") IS visible. Then optionally click the XP option and re-open to assert the branch flips back. Use the file:// base + ESM import already in the spec; do NOT use the broken addCombatant helper.
  </action>
  <verify>
    <automated>cd /d D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular && set PYTHONIOENCODING=utf-8&& python build.py && npx jest && npx playwright test tests/e2e/features/character-advancement.spec.js --reporter=line -g "D-07|Milestone|Meilenstein|leveling"</automated>
  </verify>
  <acceptance_criteria>
    - party-render.js renderPartyOverview() renders a `set-leveling-mode` toggle into #party-overview with two options (data-value="xp" / data-value="milestone"); the option matching D.settings.levelingMode carries an `active` class.
    - entity-actions.js registers `set-leveling-mode` that sets D.settings.levelingMode from ctx.value (validated to 'xp'|'milestone'), calls plain save() (NO saveUndoState/pushUndo), renderParty(), and refreshes the open char-detail-modal via showCharacterDetails when it is open.
    - The new E2E case flips the mode by CLICKING the UI control (no page.evaluate setting levelingMode) and asserts .char-xp-section hidden + milestone-level-up button visible in the detail modal.
    - A full-source grep shows `levelingMode` is now also WRITTEN by the new handler (no longer only ever set to 'xp').
    - `set PYTHONIOENCODING=utf-8&& python build.py` builds with no duplicate-function/orphan errors (exit 0).
    - `npx jest` stays green (421/421); the new + existing D-07 E2E cases pass.
    - No new module file added (no loader.js/build.py registration needed); only the four listed files changed.
  </acceptance_criteria>
  <done>The DM can switch a group between XP and Milestone mode from the party-overview header; the toggle persists with plain save(), re-renders the party, and live-refreshes an open detail modal so the existing Milestone branch is finally reachable from the UI — covered by an E2E case that flips the mode via the control.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| toggle click (ctx.value) → D.settings.levelingMode | A user-driven mode value crosses into campaign-wide settings and selects the render branch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-16 | Tampering/Integrity | set-leveling-mode ctx.value | mitigate | Whitelist ctx.value to exactly 'xp' or 'milestone'; ignore any other value (no arbitrary string written into settings) |
| T-06-17 | Tampering (XSS) | toggle labels in party-overview | mitigate | Toggle labels are static German literals; no user interpolation — and esc() is used for any interpolated value per project rule |
| T-06-18 | Integrity (data loss) | settings change reversibility | accept | A settings toggle uses plain save() with NO undo by design (inspiration precedent, D-02 pattern); trivially reversible by clicking the other option |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; tooling already present |
</threat_model>

<verification>
- `set PYTHONIOENCODING=utf-8&& python build.py` builds cleanly (exit 0; no dedup/orphan errors)
- `npx jest` stays green (421/421 — no unit regression from this UI-only change)
- `npx playwright test tests/e2e/features/character-advancement.spec.js` green, including the NEW case that flips the mode via the UI control
- Full-source grep confirms `D.settings.levelingMode` is now WRITTEN by set-leveling-mode (the dead branch is reachable)
</verification>

<success_criteria>
- A per-group UI control in #party-overview toggles D.settings.levelingMode between 'xp' and 'milestone' (D-07)
- Toggling persists via plain save() (no undo), re-renders the party, and refreshes an open detail modal so the XP/Milestone branch flips live
- Milestone mode is now reachable from the UI (no localStorage/import hand-editing required) — UAT gap closed
- E2E coverage flips the mode via the UI control and asserts the detail-modal branch flips
- Build exit 0; jest 421/421; CHAR-01 E2E (incl. new case) green
</success_criteria>

<output>
Create D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\.planning\phases\06-spieler-verwaltung\06-05-SUMMARY.md when done.
</output>
