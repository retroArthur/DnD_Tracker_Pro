---
phase: 06-spieler-verwaltung
plan: 03
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\assets\templates\view-party.html
  - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\features\party\party-crud.js
  - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\features\party\party-details.js
  - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\ui\actions\entity-actions.js
  - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\assets\styles\party.css
  - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\tests\e2e\features\character-advancement.spec.js
autonomous: true
requirements: [CHAR-03]
user_setup: []

must_haves:
  truths:
    - "A user opens the character detail modal and sees skill proficiencies (grouped by attribute, German names), saving throws, and attacks at a glance (CHAR-03 / D-06)"
    - "All 18 skills can be marked proficient with an optional expertise checkbox (double proficiency bonus) in the character editor (D-03)"
    - "Clicking a skill, a saving throw, or a raw attribute check in the detail modal rolls W20 + ability mod + proficiency bonus (×2 on expertise) into the dice history, with advantage/disadvantage controls (D-04)"
    - "Attacks are a free manual list of {name, attackBonus, damage, optional damageType}; attack bonus and damage are clickable rolls (D-05)"
    - "Skill/save/attribute/attack rolls do not bubble to the modal/card parent (-stop variants) (D-04)"
  artifacts:
    - path: "assets/templates/view-party.html"
      provides: "18 skill proficiency+expertise checkboxes and an attacks editor (add/remove rows) in the character form"
      contains: "char-skill-"
    - path: "features/party/party-crud.js"
      provides: "saveCharacter reads skillProficiencies/skillExpertise/attacks[]; editChar repopulates them"
      contains: "skillProficiencies"
    - path: "features/party/party-details.js"
      provides: "Skills section (grouped by attribute), clickable saves, attacks section, clickable raw attribute checks"
      contains: "roll-char-skill-stop"
    - path: "ui/actions/entity-actions.js"
      provides: "roll-char-skill-stop / roll-char-save-stop / roll-char-attr-stop handlers + add-attack/delete-attack"
      contains: "roll-char-skill-stop"
    - path: "assets/styles/party.css"
      provides: "Skills grid, expertise toggle, attacks list, clickable-roll styling"
      contains: "char-skills"
  key_links:
    - from: "party-details.js showCharacterDetails"
      to: "calcSkillModifier + SKILL_INFO + renderClickableDice"
      via: "skills section reads SKILL_INFO grouped by attr, rolls via calcSkillModifier; attack damage wrapped by renderClickableDice AFTER esc()"
      pattern: "calcSkillModifier"
    - from: "party-crud.js saveCharacter"
      to: "ch.skillProficiencies / ch.skillExpertise / ch.attacks"
      via: "reads checkboxes + attack rows from the form, mirrors saveProficiencies pattern, caps attacks at 20"
      pattern: "skillExpertise"
    - from: "entity-actions.js roll-char-*-stop"
      to: "displayDiceResult + addToDiceHistory"
      via: "handler computes roll locally (or via calcSkillModifier) and pushes to dice history"
      pattern: "addToDiceHistory"
---

<objective>
Implement CHAR-03 (Erweiterte Charakterwerte). Add skill proficiencies (all 18, German names from SKILL_INFO) with an optional expertise checkbox and a free attacks list to the character editor; display skills (grouped by attribute), clickable saving throws, clickable raw attribute checks, and an attacks section in the detail modal; make skills/saves/attribute-checks and attack bonus/damage clickable rolls that land in the existing dice history with advantage/disadvantage controls.

Purpose: DM-relevant values "auf einen Blick" with one-click 5e-correct rolls at the table — explicitly NOT a full digital character sheet (Out of Scope). Maximum reuse of SKILL_INFO, the saveProficiencies structure, renderClickableDice, and the existing dice roller.
Output: Editor inputs + schema read/write, detail-modal display sections, clickable-roll data-action handlers, CSS, and the relevant E2E assertions turned green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\.planning\phases\06-spieler-verwaltung\06-CONTEXT.md
@D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\.planning\phases\06-spieler-verwaltung\06-RESEARCH.md
@D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\.planning\phases\06-spieler-verwaltung\06-01-SUMMARY.md

<interfaces>
<!-- Extracted from source reads. Use directly — no exploration needed. -->

SKILL_INFO (core/constants.js:217 / DND_RULES.SKILL_INFO :504): 18 entries keyed by skill key, each { name: GermanName, attr: 'str'|'dex'|'con'|'int'|'wis'|'cha' }. Use this — NOT SKILLS from dice-core.js.

Pure helpers from Wave 1 (utils/game-rules.js, window-exported — see 06-01-SUMMARY for final names):
  calcSkillModifier(ch, skillKey) → number (already applies expertise ×2)
  getAbilityModifier(score) → Math.floor((score-10)/2)   [global lexical, call directly]
  formatModifier(mod) → '+N' | '-N'                       [global lexical, call directly]
  getProficiencyBonus(level) → window-exported

Existing save-proficiency pattern to mirror:
  party-crud.js saveCharacter :198 — `saveProficiencies = { str:$('char-save-str').checked, ... }`
  party-crud.js editChar :308 — loops ['str',...] and sets `$(\`char-save-${attr}\`).checked = saves[attr]||false`
  party-details.js :43-46 + :104-114 — reads saveProficiencies, renders attribute boxes with `.proficient` class

Dice roller entry points (features/dice/dice-core.js, window-exported):
  displayDiceResult(result, notation, rolls, isCrit, isFail, extraHtml?) :334
  addToDiceHistory(notation, result, rolls) :434
  rollAdvantage() :144 / rollDisadvantage() :153 (use parseDiceNotation('2d20kh1'/'2d20kl1') internally)

renderClickableDice(text) (features/bestiary/bestiary-render.js:41): wraps NdN[+/-N] formulas and isolated +N bonuses into <span data-action="bestiary-roll-dice" data-value="...">. Runs AFTER sanitizeHTML/esc. (A2: bestiary-roll-dice routes via rollQrefDice — if you instead want history landing guaranteed, use dedicated roll-char-attack-stop handlers calling displayDiceResult+addToDiceHistory directly.)

esc(text) for user content; sanitizeHTML for markup. EntityActions object handlers get ctx={id,value,type,event,target} plus any data-* read via ctx.target.dataset.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Editor — skill/expertise checkboxes + attacks list; schema read/write</name>
  <files>D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\assets\templates\view-party.html, D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\features\party\party-crud.js, D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\ui\actions\entity-actions.js, D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\assets\styles\party.css</files>
  <read_first>
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\assets\templates\view-party.html — read the saving-throw checkbox block (ids char-save-str ... char-save-cha) and the surrounding form-section markup; mirror its structure/classes for the skills section and find a sensible insertion point for the attacks editor.
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\features\party\party-crud.js — read saveCharacter (:174-283), specifically the saveProficiencies collection (:198-205), the ch literal (:214-250), and editChar (:287-320) save-proficiency repopulation loop (:308-312). Mirror exactly for skillProficiencies/skillExpertise and add attacks[].
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\ui\actions\entity-actions.js — handler object shape for add-attack/delete-attack (these mutate a transient form list, not D directly).
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\.planning\phases\06-spieler-verwaltung\06-RESEARCH.md § Sicherheitsdomäne (attack cap 20, dice-formula whitelist) and § "Fallstrick 4: SKILLS ≠ SKILL_INFO".
  </read_first>
  <behavior>
    - After editing a character with stealth proficient + expertise and saving, EntityLookup.character(id).skillProficiencies.stealth === true and .skillExpertise.stealth === true
    - Saving a character with an attack row {name:'Langschwert', attackBonus:5, damage:'1d8+3', damageType:'Hieb'} yields ch.attacks length 1 with those fields
    - An attack name containing markup is stored escaped/neutralized (no raw <script> survives into ch.attacks[].name as executable)
    - Attempting to save more than 20 attacks clamps to 20 (DoS cap)
    - A damage formula not matching /^\d+[dD]\d+([+-]\d+)?$/ is rejected or sanitized before storage (no arbitrary string becomes a clickable dice span later)
  </behavior>
  <action>
    In view-party.html: add a "Fertigkeiten" form section listing all 18 skills. For each skill key, render a proficiency checkbox id `char-skill-${key}` and an expertise checkbox id `char-skill-exp-${key}`, labelled with the German SKILL_INFO name, grouped/ordered by attribute. (If the template is static HTML and cannot read SKILL_INFO at author time, hardcode the 18 rows using the exact SKILL_INFO keys and German names — copy them from core/constants.js, do not invent.) Add an "Angriffe" section with a container for attack rows and an "Angriff hinzufügen" button (data-action="add-attack"); each row has inputs for Name, Bonus (number), Schaden (formula), Typ (optional) and a remove button (data-action="delete-attack").

    In party-crud.js saveCharacter: after the saveProficiencies block, collect skillProficiencies and skillExpertise by reading `$(\`char-skill-${key}\`).checked` / `$(\`char-skill-exp-${key}\`).checked` for each SKILL_INFO key (mirror the save loop). Collect attacks[] from the attack rows: trim+esc the name at read time is wrong (esc is for HTML render, not storage) — instead store the raw trimmed name but VALIDATE: reject rows with empty name; clamp the array to the first 20; validate each damage string against /^\d+[dD]\d+([+-]\d+)?$/ and store '' (or skip the clickable treatment later) if it fails; coerce attackBonus to an integer. Add the three fields to the ch literal. In editChar: repopulate the 18 skill+expertise checkboxes (loop over SKILL_INFO keys, default false) and rebuild the attack rows from ch.attacks.

    In entity-actions.js: add `add-attack` (append an empty attack row to the form container) and `delete-attack` (remove the clicked row) handlers. These operate on the transient form DOM only — they do NOT mutate D and do NOT call save (the actual persistence happens on saveCharacter). No function-local `const X = window.X`.

    In party.css: add a "CHARACTER SKILLS / ATTACKS EDITOR" section styling the skills grid, the expertise sub-checkbox, and the attack rows (inputs + remove button), using var(--*) tokens.
  </action>
  <verify>
    <automated>cd /d D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular && set PYTHONIOENCODING=utf-8&& python build.py && npx playwright test tests/e2e/features/character-advancement.spec.js --reporter=line -g "editor|Angriff|Fertigkeit"</automated>
  </verify>
  <acceptance_criteria>
    - view-party.html contains id tokens `char-skill-` (18 proficiency) and `char-skill-exp-` (18 expertise) — `grep -c 'char-skill-exp-' assets/templates/view-party.html` returns 18
    - party-crud.js saveCharacter reads skillProficiencies, skillExpertise and builds attacks[]; `grep -c 'skillExpertise' features/party/party-crud.js` >= 2 (save + edit)
    - The attacks array is clamped to max 20 (grep shows a slice/length guard) — DoS mitigation present
    - A damage string failing /^\d+[dD]\d+([+-]\d+)?$/ is not stored as a clickable-eligible formula (validated before storage)
    - `set PYTHONIOENCODING=utf-8&& python build.py` builds with no duplicate-function/orphan errors
  </acceptance_criteria>
  <done>The editor captures all 18 skill proficiencies + expertise and a capped, validated attacks list; saveCharacter/editChar round-trip the new fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Detail modal — skills/saves/attribute/attack display + clickable rolls (adv/disadv) + E2E</name>
  <files>D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\features\party\party-details.js, D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\ui\actions\entity-actions.js, D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\assets\styles\party.css, D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\tests\e2e\features\character-advancement.spec.js</files>
  <read_first>
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\features\party\party-details.js — read showCharacterDetails (:8-130+), especially the attribute grid (:103-115) and saveProficiencies handling (:43-46). The skills section, clickable saves, clickable attribute boxes, and attacks section attach here.
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\features\bestiary\bestiary-render.js — read renderClickableDice (:41-49) to reuse exactly for attack bonus/damage spans; note it runs AFTER sanitization.
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\features\dice\dice-core.js — read displayDiceResult (:334), addToDiceHistory (:434), rollAdvantage/rollDisadvantage (:144/:153) to wire the clickable handlers into the existing history/event log.
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\ui\actions\entity-actions.js — the EntityActions object + the existing `bestiary-roll-dice` handler (:289) and -stop precedents; add the new roll-char-*-stop handlers here.
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\.planning\phases\06-spieler-verwaltung\06-RESEARCH.md § "Muster 2: Klickbare Würfe im Detail-Modal" (sanitize-then-dice) and § "Fallstrick 1".
  </read_first>
  <behavior>
    - The detail modal for a character renders a Skills section grouped by attribute with all 18 German skill names and each skill's computed modifier via calcSkillModifier
    - Clicking a skill box rolls 1d20 + calcSkillModifier and the dice history length increases by 1
    - Clicking a saving-throw control rolls 1d20 + ability mod (+ prof bonus if save-proficient) into history
    - Clicking a raw attribute box rolls 1d20 + ability mod into history
    - An attack with attackBonus 5 / damage '1d8+3' renders a clickable hit span (data-value="1d20+5") and a clickable damage span (data-value="1d8+3")
    - Advantage/disadvantage controls produce 2d20kh1 / 2d20kl1 rolls; none of these clicks close or re-trigger the parent modal/card (-stop)
  </behavior>
  <action>
    In showCharacterDetails, add three blocks after the existing attribute grid: (1) a Skills section iterating SKILL_INFO grouped by attr, each skill rendered as a clickable element carrying `data-action="roll-char-skill-stop" data-id="${ch.id}"` + a data attribute for the skill key, showing the German name and the calcSkillModifier value (formatModifier); mark proficient/expertise visually. (2) Make the existing saving-throw display clickable per attribute with `roll-char-save-stop`. (3) Make the raw attribute boxes clickable with `roll-char-attr-stop`. (4) An Attacks section listing ch.attacks: render `esc(attack.name)` and `esc(attack.damageType||'')` (user content escaped FIRST), then build the clickable hit/damage spans — for damage use renderClickableDice(attack.damage) (already whitelisted at save time), and for the attack bonus emit a span with data-value `1d20+{attackBonus}` via renderClickableDice or a dedicated roll-char-attack-stop. Per Fallstrick 1, NEVER call renderClickableDice on a string that still contains unescaped user text — escape names/types first, wrap only the validated dice formulas. Add adv/disadv mini-buttons next to each roll (D-04 discretion: two mini-buttons recommended) carrying a data-adv flag the handler reads.

    In entity-actions.js: add `roll-char-skill-stop`, `roll-char-save-stop`, `roll-char-attr-stop`, and `roll-char-attack-stop` handlers. Each calls ctx.event.stopPropagation() first; resolves ch via EntityLookup.character(ctx.id); reads the skill key / attr / attack index and the adv/disadv flag from ctx.target.dataset; computes the modifier (calcSkillModifier for skills; ability mod ± prof for saves; ability mod for attrs; attack uses 1d20+bonus); rolls 1d20 (or 2d20kh1/kl1 for adv/disadv) and calls displayDiceResult(...) + addToDiceHistory(...) so the result lands in the existing history/event log. Reuse rollAdvantage/rollDisadvantage or parseDiceNotation as appropriate. No function-local `const X = window.X`; call globals directly. Do not duplicate any existing top-level function name.

    In party.css: style the skills section (grouped grid), proficient/expertise markers, the attacks section, clickable-roll affordance (hover/cursor), and the adv/disadv mini-buttons.

    In character-advancement.spec.js: un-skip and implement the clickable-roll and attack assertions from VALIDATION.md — assert dice history length increases after clicking a skill/save/attribute box, and assert the modal contains dice spans with data-value="1d20+5" and data-value="1d8+3" for a seeded attack. Use page.evaluate to seed a character with skillProficiencies/skillExpertise/attacks; do not use the broken addCombatant helper.
  </action>
  <verify>
    <automated>cd /d D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular && set PYTHONIOENCODING=utf-8&& python build.py && npx jest tests/unit/character-advancement.test.js && npx playwright test tests/e2e/features/character-advancement.spec.js --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - party-details.js contains `roll-char-skill-stop`, `roll-char-save-stop`, `roll-char-attr-stop`, and renders all 18 SKILL_INFO skills grouped by attribute
    - entity-actions.js defines the four roll-char-*-stop handlers, each calling `ctx.event.stopPropagation()` and `addToDiceHistory`
    - For a seeded attack +5 / 1d8+3 the modal HTML contains `data-value="1d20+5"` and `data-value="1d8+3"` (asserted in E2E)
    - Attack names are esc()'d BEFORE any renderClickableDice pass (sanitize-then-dice) — no renderClickableDice call receives a string still holding unescaped attack.name
    - `npx playwright test tests/e2e/features/character-advancement.spec.js` passes the clickable-roll + attack-span assertions; `npx jest tests/unit/character-advancement.test.js` stays green
  </acceptance_criteria>
  <done>The detail modal shows skills/saves/attributes/attacks at a glance with one-click 5e-correct rolls (incl. adv/disadv) that land in the dice history, all -stop-guarded and XSS-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| character form → stored attack fields | User-entered attack names, damage formulas, types cross into D and later into HTML/dice spans |
| stored attack/skill data → detail-modal innerHTML | Stored values are interpolated into the modal template and into renderClickableDice |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Tampering (XSS) | attack.name / damageType in detail modal | mitigate | esc(attack.name) and esc(attack.damageType) before interpolation; renderClickableDice is applied only to the validated damage formula and the synthesized 1d20+bonus span — never to a string still containing user name/type |
| T-06-08 | Tampering (XSS) | damage formula as clickable dice | mitigate | Whitelist /^\d+[dD]\d+([+-]\d+)?$/ at SAVE time (Task 1); non-matching formulas are not rendered as clickable spans |
| T-06-09 | Denial of Service | unbounded attacks list | mitigate | Cap attacks at 20 per character at save time (Task 1); editor add-attack respects the cap |
| T-06-10 | Elevation/Unexpected action | roll clicks bubbling to modal/card parent | mitigate | All roll handlers are -stop variants calling ctx.event.stopPropagation() first |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; tooling already present |
</threat_model>

<verification>
- `set PYTHONIOENCODING=utf-8&& python build.py` builds cleanly (no dedup/orphan errors)
- `npx jest tests/unit/character-advancement.test.js` green (calcSkillModifier still correct)
- `npx playwright test tests/e2e/features/character-advancement.spec.js` green for clickable rolls + attack spans
- `npm run check` does not regress (new data-action strings need no eslint globals)
</verification>

<success_criteria>
- 18 skills with proficiency + optional expertise in editor; round-tripped via saveCharacter/editChar (D-03)
- Detail modal shows skills grouped by attribute, clickable saves, clickable raw attribute checks, attacks section (D-06)
- Skills/saves/attributes/attacks are one-click rolls into dice history with adv/disadv, 5e-correct (D-04/D-05)
- All rolls -stop-guarded; all user content esc()'d before dice spans (XSS-safe)
- character-advancement.spec.js green for CHAR-03 cases
</success_criteria>

<output>
Create D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\.planning\phases\06-spieler-verwaltung\06-03-SUMMARY.md when done. Record the exact data-action names and the attack-row id scheme for Wave 4.
</output>
