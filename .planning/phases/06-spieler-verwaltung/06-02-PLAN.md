---
phase: 06-spieler-verwaltung
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\features\party\party-render.js
  - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\ui\actions\entity-actions.js
  - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\assets\styles\party.css
  - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\tests\e2e\features\inspiration.spec.js
autonomous: true
requirements: [CHAR-02]
user_setup: []

must_haves:
  truths:
    - "A user gives Inspiration with one click on an always-visible star on the character card; another click removes it (CHAR-02 / D-01)"
    - "The Inspiration star is visible whether inspiration is true (filled ⭐) or false (outline ☆), so it is tappable to grant (D-01)"
    - "Clicking the star toggles inspiration WITHOUT opening the character edit form (-stop handler) (D-01)"
    - "The toggle is a plain save(), never saveUndoState() — no undo spam (D-02)"
    - "Inspiration persists across reload (CHAR-02)"
  artifacts:
    - path: "features/party/party-render.js"
      provides: "Always-visible clickable inspiration star button in renderCharacterCard"
      contains: "toggle-inspiration-stop"
    - path: "ui/actions/entity-actions.js"
      provides: "toggle-inspiration-stop handler that stops propagation, toggles, plain save"
      contains: "toggle-inspiration-stop"
    - path: "assets/styles/party.css"
      provides: "char-inspiration-toggle styling for active/inactive star"
      contains: "char-inspiration-toggle"
  key_links:
    - from: "features/party/party-render.js renderCharacterCard"
      to: "entity-actions.js toggle-inspiration-stop"
      via: "data-action attribute on the star button with data-id"
      pattern: "toggle-inspiration-stop"
    - from: "entity-actions.js toggle-inspiration-stop"
      to: "ch.inspiration + window.save + renderParty"
      via: "stopPropagation → toggle boolean → plain save (no undo)"
      pattern: "ch.inspiration = !ch.inspiration"
---

<objective>
Implement CHAR-02 (Inspiration). Replace the conditional ⭐ prefix in the character card name with an always-visible, clickable star toggle button. Wire a `toggle-inspiration-stop` data-action handler that stops event propagation (so it does not trigger the card header's `edit-char`), flips the existing `inspiration` boolean, and persists with a plain `save()` — no undo.

Purpose: One-click Inspiration at the table; the star must be visible AND clickable even when inspiration is off so the DM can grant it (today ⭐ only renders when inspiration===true). Smallest feature wave; lands first after foundation.
Output: A toggleable star on every character card, a stop-propagation handler, CSS for active/inactive states, and the inspiration E2E spec turned green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\.planning\phases\06-spieler-verwaltung\06-CONTEXT.md
@D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\.planning\phases\06-spieler-verwaltung\06-RESEARCH.md
@D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\.planning\phases\06-spieler-verwaltung\06-01-SUMMARY.md

<interfaces>
<!-- Extracted from source reads. Executor uses these directly — no exploration. -->

renderCharacterCard (features/party/party-render.js:131): builds the card HTML string.
  - Card header at :169 carries `data-action="edit-char" data-id="${ch.id}"` — clicks anywhere in the header open the edit form.
  - Current star at :175: `${ch.inspiration ? '⭐ ' : ''}${esc(ch.name)}` inside `.char-card-name`.
  - The HP box at :183 already demonstrates a -stop action on a clickable sub-element INSIDE the header:
      `data-action="show-hp-calculator-stop" data-type="characters" data-id="${ch.id}"`

Existing -stop handler precedents (ui/actions/entity-actions.js):
  'edit-npc-stop': ctx => { ctx.event.stopPropagation(); editNPC(ctx.id); }   (:41)
  'delete-npc-stop': ctx => { ctx.event.stopPropagation(); deleteNPC(ctx.id); }  (:45)
  EntityActions is a plain object; handlers receive ctx = { id, value, type, event, target }.

EntityLookup.character(id) returns the character or undefined (handles id string/number).
window.save and window.renderParty are window-attached globals (call as window.save() / window.renderParty()).
showToast(msg, type?) renders into the event log (German strings, emoji prefixes).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Always-visible clickable inspiration star in the card header</name>
  <files>D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\features\party\party-render.js, D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\assets\styles\party.css</files>
  <read_first>
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\features\party\party-render.js — read renderCharacterCard (:131-191), especially the `.char-card-name` block (:174-177) and the `.char-card-hp` -stop sub-element (:183) which is the exact precedent for a clickable element nested inside the edit-char header.
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\assets\styles\party.css — read the existing `.char-card-name`, `.char-card-header`, `.char-card-hp` rules to match BEM-lite naming, CSS variables (var(--gold), var(--text-dim)), and where to append the new section.
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\.planning\phases\06-spieler-verwaltung\06-RESEARCH.md § "Muster 1: Inspiration-Toggle mit -stop-Handler" — the exact button markup.
  </read_first>
  <action>
    In renderCharacterCard, replace the conditional `${ch.inspiration ? '⭐ ' : ''}` prefix in `.char-card-name` with a real button element that is ALWAYS rendered: a `<button class="char-inspiration-toggle ${ch.inspiration ? 'active' : ''}" data-action="toggle-inspiration-stop" data-id="${ch.id}" title="...">` whose glyph is ⭐ when active and ☆ when inactive. Use German titles per D-00b: "Inspiration entfernen" when active, "Inspiration vergeben" when inactive. Keep `esc(ch.name)` for the name. The button sits inside the header (which carries edit-char) — the -stop handler is what prevents the header click; do NOT restructure the header.

    In party.css: add a `// ===` banner section "INSPIRATION TOGGLE" with `.char-inspiration-toggle` rules: inline-flex, no default button chrome (transparent background, no border), pointer cursor, sized to sit next to the name. `.char-inspiration-toggle.active` uses var(--gold) (filled gold star feel); inactive uses var(--text-dim) (greyed outline). Add a subtle hover state. Do NOT create a new CSS file — append to party.css.
  </action>
  <verify>
    <automated>cd /d D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular && set PYTHONIOENCODING=utf-8&& python build.py</automated>
  </verify>
  <acceptance_criteria>
    - features/party/party-render.js contains `data-action="toggle-inspiration-stop"` exactly once inside renderCharacterCard
    - The star button is rendered unconditionally (no `${ch.inspiration ? ... : ''}` guard wrapping the button element itself); only its glyph/class vary by state
    - assets/styles/party.css contains `.char-inspiration-toggle` and `.char-inspiration-toggle.active`
    - `set PYTHONIOENCODING=utf-8&& python build.py` completes with no duplicate-declaration or orphaned-body errors
  </acceptance_criteria>
  <done>Every character card shows a tappable star (filled when inspired, outline when not) wired to a -stop action, styled in party.css.</done>
</task>

<task type="auto">
  <name>Task 2: toggle-inspiration-stop handler (no undo) + green E2E</name>
  <files>D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\ui\actions\entity-actions.js, D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\tests\e2e\features\inspiration.spec.js</files>
  <read_first>
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\ui\actions\entity-actions.js — read the character-actions block (:6-28) and the `edit-npc-stop`/`delete-npc-stop` precedents (:41-48) for the exact `ctx.event.stopPropagation()` then-act shape.
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\.planning\phases\06-spieler-verwaltung\06-RESEARCH.md § "Muster 1" handler body and § "Fallstrick 8: Inspiration-Toggle kein Undo".
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\tests\e2e\features\inspiration.spec.js — the Wave-0 stub created in 06-01; un-skip and fill its assertions.
    - D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\tests\e2e\crud\party.spec.js — how a character is created/seeded in the file:// E2E harness so the spec has a card to click.
  </read_first>
  <action>
    In entity-actions.js, add a `'toggle-inspiration-stop'` handler to the EntityActions object (character section). Body: call ctx.event.stopPropagation() FIRST; resolve ch via EntityLookup.character(ctx.id); return if not found; set ch.inspiration = !ch.inspiration; call window.save() (NOT saveUndoState / pushUndo — D-02); call window.renderParty(); showToast with German feedback ("⭐ Inspiration erhalten!" / "☆ Inspiration entfernt"). Do not introduce a function-local `const X = window.X`; call window.* directly.

    In inspiration.spec.js: un-skip the Wave-0 cases and implement the assertions from VALIDATION.md: after seeding a character and clicking `.char-inspiration-toggle`, assert `.char-inspiration-toggle.active` becomes visible; reload the page and assert it is still active (persistence); assert that clicking the star did NOT open the character form (`#char-form.open` not visible). Use the file:// harness and page.evaluate to seed/inspect state where needed; do not depend on broken initiative helpers.
  </action>
  <verify>
    <automated>cd /d D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular && set PYTHONIOENCODING=utf-8&& python build.py && npx playwright test tests/e2e/features/inspiration.spec.js --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - ui/actions/entity-actions.js contains a `'toggle-inspiration-stop'` key whose body calls `ctx.event.stopPropagation()` and `ch.inspiration = !ch.inspiration`
    - The handler body contains neither `saveUndoState` nor `pushUndo` (grep -c returns 0 for both within the handler) — verifies D-02
    - `npx playwright test tests/e2e/features/inspiration.spec.js` exits 0 with the toggle, persistence, and no-form-open assertions passing
    - Clicking the star with inspiration=false sets inspiration=true and does NOT open the edit form (asserted in the spec)
  </acceptance_criteria>
  <done>The inspiration toggle works end to end without undo, and the E2E spec proves toggle + persistence + stop-propagation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| card click → action dispatch | A click on a nested star button must not bubble to the header's edit-char action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering (XSS) | star button title/glyph | mitigate | Button title strings are static German literals, not user data; ch.name continues to use esc() — no new unescaped interpolation introduced |
| T-06-05 | Elevation/Unexpected action | star click bubbling to edit-char | mitigate | -stop handler calls ctx.event.stopPropagation() first; E2E asserts #char-form does not open |
| T-06-06 | Integrity (undo pollution) | inspiration toggle | accept | By design (D-02) the toggle uses plain save() and is intentionally not undoable; unit/E2E confirm no saveUndoState call |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; all tooling already present |
</threat_model>

<verification>
- `set PYTHONIOENCODING=utf-8&& python build.py` builds cleanly
- `npx playwright test tests/e2e/features/inspiration.spec.js` green (toggle, persistence, stop-propagation)
- `npm run check` does not regress (register `toggle-inspiration-stop` only as a data-action string — no new global needs eslint declaration)
</verification>

<success_criteria>
- Always-visible clickable star on every character card; filled when inspired, outline when not (D-01)
- Click toggles inspiration without opening the edit form (-stop) and persists across reload (CHAR-02 / D-01)
- Toggle uses plain save(), no undo (D-02)
- inspiration.spec.js green
</success_criteria>

<output>
Create D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular\.planning\phases\06-spieler-verwaltung\06-02-SUMMARY.md when done.
</output>
