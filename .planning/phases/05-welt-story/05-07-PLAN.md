---
phase: 05-welt-story
plan: 07
type: execute
wave: 7
depends_on: ["05-06"]
files_modified:
  - features/fraktionen/fraktionen-render.js
  - features/fraktionen/fraktionen-crud.js
  - features/npcs/npc-crud.js
  - assets/templates/view-welt.html
  - assets/templates/modals-entity.html
  - assets/styles/welt.css
  - ui/actions/entity-actions.js
  - tests/unit/welt-story.test.js
  - tests/e2e/features/welt-story.spec.js
autonomous: true
requirements: [WELT-05]
must_haves:
  truths:
    - "Eine angelegte Fraktion erscheint in der Fraktionen-Übersichtsliste mit aktuellem Rufstand"
    - "rufStufe(15).label === 'Freundlich' und rufStufe(21).label === 'Verbündet' (5 Stufen, −50…+50)"
    - "Ruf-Anpassung per +/− oder direktem Setzen schreibt einen Eintrag in faction.rufHistorie"
    - "Undo nach Ruf-Änderung stellt den alten Rufwert wieder her"
    - "Ein NPC mit factionId erscheint in der Mitgliederliste seiner Fraktion"
  artifacts:
    - path: "features/fraktionen/fraktionen-render.js"
      provides: "renderFraktionen, renderFraktionDetail, FRAKTIONS_RUF_STUFEN, rufStufe"
      contains: "FRAKTIONS_RUF_STUFEN"
    - path: "features/fraktionen/fraktionen-crud.js"
      provides: "saveFraktion, deleteFraktion, anpassenRuf"
      contains: "anpassenRuf"
    - path: "features/npcs/npc-crud.js"
      provides: "npc.factionId persistiert in saveNPC"
      contains: "factionId"
  key_links:
    - from: "features/fraktionen/fraktionen-render.js"
      to: "D.npcs (factionId)"
      via: "Mitgliederliste filtert NPCs nach factionId"
      pattern: "factionId"
    - from: "features/fraktionen/fraktionen-crud.js"
      to: "faction.rufHistorie"
      via: "anpassenRuf pushUndo + Historie-Eintrag + clamp"
      pattern: "rufHistorie"
---

<objective>
WELT-05 Fraktionen & Ruf-System: eigener Tab mit Übersichtsliste, Fraktionsfelder (Name, Agenda, Beschreibung, Symbol, Mitglieder via npc.factionId, Sitz via Orts-Link, textuelle Rivalen/Verbündete), Ruf als Zahlenwert (−50…+50) gemappt auf 5 benannte Stufen, Anpassung per +/− oder direktem Setzen mit Ruf-Historie. Plus kleines factionId-Feld im NPC-Modal.

Purpose: Liefert Success Criterion 5 der Phase (D-16…D-19). Nutzt das in Plan 01 angelegte D.factions-Schema und repliziert die RELATION_STATUS-Optik aus npc-render.js als 5-Stufen-Ruf-Skala.
Output: Funktionsfähiger Fraktionen-Tab + NPC-factionId-Kopplung.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-welt-story/05-RESEARCH.md
@.planning/phases/05-welt-story/05-CONTEXT.md

<interfaces>
<!-- Verifizierte Strukturen. -->

D.factions[] Schema (Plan 01 / 05-RESEARCH.md § D.factions):
  { id, name, symbol, agenda, beschreibung, ruf:-50..50,
    rufHistorie:[{delta, grund, zeitstempel}],
    mitgliederNpcIds:[], sitzOrtId:null, rivalen:'', verbuendete:'', links:[] }

RELATION_STATUS (features/npcs/npc-render.js, Vorbild): { friendly:{label:'Freundlich',icon:'🟢',color:'var(--green)'}, neutral:{...}, hostile:{...} }.

FRAKTIONS_RUF_STUFEN (HIER definieren, 5 Stufen, aus 05-RESEARCH.md § Ruf-System):
  [ {min:-50,max:-21,label:'Feindlich',icon:'🔴',farbe:'var(--red)'},
    {min:-20,max:-1,label:'Misstrauisch',icon:'🟠',farbe:'var(--yellow)'},
    {min:0,max:0,label:'Neutral',icon:'⚪',farbe:'var(--text)'},
    {min:1,max:20,label:'Freundlich',icon:'🟡',farbe:'var(--gold)'},
    {min:21,max:50,label:'Verbündet',icon:'🟢',farbe:'var(--green)'} ]
  rufStufe(rufwert): FRAKTIONS_RUF_STUFEN.find(s => rufwert>=s.min && rufwert<=s.max) || Neutral-Fallback.

saveNPC (features/npcs/npc-crud.js): baut npc-Objekt aus Formularfeldern, validiert, pushUndo, push/update in D.npcs.
  factionId-Erweiterung: optionales Input #npc-faction (Select über D.factions); parseInt(...)||null; in npc-Objekt aufnehmen.

Globals: pushUndo, nextId, esc, sanitizeHTML, parseEntityId, showModal/hideModal, showToast.
  deleteWithConfirm({entityType:'factions',...}), EntityLookup.location(id). var D = window.D; var save = window.save;
Tab-Container: #view-fraktionen. Registry fraktionen → renders ['renderFraktionen']. CSS-Präfix: fr-.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ruf-Stufen-Logik + Fraktionen-CRUD + Ruf-Anpassung</name>
  <read_first>
    - features/fraktionen/fraktionen-crud.js (Plan-01-Skelett)
    - features/fraktionen/fraktionen-render.js (Plan-01-Skelett — FRAKTIONS_RUF_STUFEN/rufStufe gehören hierhin, von crud importiert/aufgerufen)
    - features/npcs/npc-render.js (RELATION_STATUS — Stufen-Vorbild)
    - features/npcs/npc-crud.js (saveNPC-Muster: pushUndo, nextId, validate, D.npcs)
    - utils/crud-helpers.js (deleteWithConfirm)
    - .planning/phases/05-welt-story/05-VALIDATION.md (WELT-05 Unit: rufStufe-Grenzen; E2E: rufHistorie, Undo)
  </read_first>
  <behavior>
    - rufStufe(15).label === 'Freundlich'.
    - rufStufe(21).label === 'Verbündet'.
    - rufStufe(0).label === 'Neutral'; rufStufe(-50).label === 'Feindlich'.
    - anpassenRuf(factionId, +10, 'Grund') erhöht faction.ruf um 10 (geklemmt −50..50) und pusht einen rufHistorie-Eintrag.
    - Ruf bleibt nach mehreren Anpassungen im Bereich −50..50 (clamp).
  </behavior>
  <action>
    In fraktionen-render.js: FRAKTIONS_RUF_STUFEN-Konstante (5 Stufen, exakte Werte aus interfaces) + rufStufe(rufwert) mit Neutral-Fallback definieren und exportieren (rufStufe wird von crud + render genutzt). In fraktionen-crud.js: saveFraktion() — liest Formular (name, symbol max 2 Zeichen wie random-tables-Icon, agenda/beschreibung via sanitizeHTML, sitzOrtId, rivalen/verbuendete als Freitext, ruf initial 0), pushUndo('Fraktion gespeichert'/'bearbeitet'), nextId('factions') bei Neuanlage, push/update in D.factions, save, render. deleteFraktion(id) via deleteWithConfirm({entityType:'factions',...}). anpassenRuf(factionId, delta, grund) — pushUndo('Ruf angepasst') VOR Mutation, neuer Wert = clamp(faction.ruf + delta, -50, 50), faction.rufHistorie.push({delta, grund: esc-sicher gespeichert, zeitstempel: Date.now()}), save, render. Eindeutige Funktionsnamen (saveFraktion, deleteFraktion, anpassenRuf, rufStufe). window-Exporte.
  </action>
  <verify>
    <automated>cd "D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular" && npx jest tests/unit/welt-story.test.js -t "WELT-05"</automated>
  </verify>
  <acceptance_criteria>
    - rufStufe(15)==='Freundlich', rufStufe(21)==='Verbündet', rufStufe(0)==='Neutral', rufStufe(-50)==='Feindlich' (Unit grün).
    - anpassenRuf ruft pushUndo() vor der Mutation, klemmt ruf auf −50..50 und pusht einen rufHistorie-Eintrag {delta,grund,zeitstempel}.
    - saveFraktion nutzt nextId('factions') + pushUndo; deleteFraktion via deleteWithConfirm entityType:'factions'.
    - Eindeutige Funktionsnamen; FRAKTIONS_RUF_STUFEN nicht mit RELATION_STATUS kollidierend.
  </acceptance_criteria>
  <done>Ruf-Logik + Fraktionen-CRUD implementiert; WELT-05-Unit grün.</done>
</task>

<task type="auto">
  <name>Task 2: Fraktionen-Render + Mitgliederliste + npc.factionId + View/CSS/Actions/E2E</name>
  <read_first>
    - features/fraktionen/fraktionen-render.js (Task-1-Logik)
    - features/npcs/npc-render.js (renderNPCRelations/showRelationsModal — Detail-/Status-Bar-Layout als Vorbild)
    - features/npcs/npc-crud.js (saveNPC — factionId-Einfügepunkt im npc-Objekt; editNPC — Vorbelegung)
    - assets/templates/modals-entity.html (NPC-Modal — Stelle für ein #npc-faction-Select)
    - assets/templates/view-bestiary.html (View-Layout-Analog)
    - render/helpers.js (EntityLookup.location für Sitz-Anzeige)
    - .planning/phases/05-welt-story/05-VALIDATION.md (WELT-05 E2E: Liste, rufHistorie, Undo, factionId in Mitgliederliste)
  </read_first>
  <action>
    In fraktionen-render.js: renderFraktionen() (Container #view-fraktionen) — Übersichtsliste aller D.factions mit Symbol, Name und aktueller Ruf-Stufe (rufStufe(faction.ruf): Label/Icon/Farbe via fr--Klassen, Optik wie RELATION_STATUS-Statusbar). renderFraktionDetail() — Detail mit Agenda/Beschreibung (sanitizeHTML→Anzeige), +/−-Ruf-Buttons + direktem Setzfeld + Grund-Eingabe (→ anpassenRuf), Ruf-Historie-Liste (esc()), Sitz via EntityLookup.location(sitzOrtId), Mitgliederliste = D.npcs.filter(n => n.factionId === faction.id) (Namen via esc()), Rivalen/Verbündete als esc()-Freitext. Alle Nutzerinhalte via esc()/sanitizeHTML(); parseEntityId für ID-Vergleiche.
    In features/npcs/npc-crud.js: saveNPC um `factionId: parseInt($('npc-faction')?.value) || null` erweitern (optionales Feld, rückwärts-kompatibel); editNPC um Vorbelegung des Selects ergänzen (defensiv, nur wenn Element existiert). In assets/templates/modals-entity.html: ein #npc-faction-Select im NPC-Modal ergänzen (Optionen werden dynamisch/leer; "keine" als Default).
    In view-welt.html: #view-fraktionen mit Toolbar (Titel + Neu-Button) + Liste + Detailbereich. In welt.css: fr--Klassen (Liste, Ruf-Stufen-Badge, Historie, Detail). In ui/actions/entity-actions.js: data-action-Handler (show-fraktion-modal, save-fraktion, delete-fraktion, ruf-plus, ruf-minus, ruf-set, select-fraktion).
    Aktiviere WELT-05-E2E-Stubs: Fraktion anlegen → D.factions.length===1 + Karte; +10 mit Grund → rufHistorie.length===1; Undo → faction.ruf zurück auf 0; NPC mit factionId → erscheint in Mitgliederliste.
  </action>
  <verify>
    <automated>cd "D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular" && set PYTHONIOENCODING=utf-8 && python build.py && npx jest tests/unit/welt-story.test.js && npx jest tests/unit/welt-story.test.js -t "WELT-05"</automated>
  </verify>
  <acceptance_criteria>
    - `python build.py` grün (keine Duplikate; saveNPC nicht dupliziert, nur erweitert).
    - renderFraktionen zeigt Liste mit Ruf-Stufen-Badge; Detail zeigt +/−-Buttons, Historie, Mitgliederliste, Sitz.
    - saveNPC persistiert npc.factionId (optional, default null); NPC mit factionId erscheint in der passenden Fraktions-Mitgliederliste.
    - +10-Ruf mit Grund schreibt genau einen rufHistorie-Eintrag; Undo stellt faction.ruf wieder her.
    - Gesamte welt-story.test.js grün; WELT-05-E2E-Spec aktiviert.
  </acceptance_criteria>
  <done>Fraktionen-Tab + factionId-Kopplung funktionsfähig; Build + Tests grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fraktions-Formular (Name/Agenda/Rivalen/Grund) → D.factions → DOM | Nutzerinhalt persistiert und gerendert |
| Ruf-Delta-Eingabe → faction.ruf | Numerischer Nutzerinput |
| npc.factionId → Mitglieder-Filter | Fremdschlüssel auf D.factions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-23 | Information Disclosure (XSS) | Fraktions-/Historie-/Mitglieder-Render | mitigate | Name/Grund/Rivalen via esc(); Agenda/Beschreibung via sanitizeHTML(); Symbol auf max 2 Zeichen kürzen (random-tables-Muster) |
| T-05-24 | Denial of Service | extremer Ruf-Delta oder unbegrenzte Historie | mitigate | clamp(ruf, -50, 50) vor Speichern; delta selbst nicht über Schleifen genutzt; Historie wächst nur 1 pro Klick |
| T-05-25 | Tampering | factionId/sitzOrtId zeigt auf gelöschte Entität | mitigate | parseEntityId() für Vergleiche; Mitglieder-Filter und EntityLookup.location tolerieren fehlende Refs (kein Crash, leere Anzeige) |
| T-05-26 | Tampering | Ruf-Änderung irreversibel | mitigate | pushUndo('Ruf angepasst') VOR Mutation; E2E prüft Undo stellt ruf===0 wieder her |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | Keine neuen Pakete |
</threat_model>

<verification>
- Build grün; Fraktion anlegen → Übersicht + Ruf-Stufe.
- rufStufe-Grenzen korrekt; +10 schreibt Historie; Undo stellt Ruf wieder her.
- NPC mit factionId in Mitgliederliste sichtbar.
</verification>

<success_criteria>
- WELT-05 erfüllt (Phase Success Criterion 5): Fraktionen mit Ruf-Tracking + Historie + NPC-Kopplung.
</success_criteria>

<output>
Create `.planning/phases/05-welt-story/05-07-SUMMARY.md` when done
</output>
