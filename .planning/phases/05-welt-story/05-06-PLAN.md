---
phase: 05-welt-story
plan: 06
type: execute
wave: 6
depends_on: ["05-02", "05-05"]
files_modified:
  - features/reise/reise-render.js
  - features/reise/reise-crud.js
  - assets/templates/view-welt.html
  - assets/styles/welt.css
  - ui/actions/entity-actions.js
  - tests/unit/welt-story.test.js
  - tests/e2e/features/welt-story.spec.js
autonomous: true
requirements: [WELT-04]
must_haves:
  truths:
    - "berechneTagesmarsch('normal','normal') === 24 und berechneTagesmarsch('langsam','schwierig') === 9"
    - "rollWetter(klima, jahreszeit) liefert ein Ergebnis abhängig von der aus dem In-Game-Datum abgeleiteten Jahreszeit"
    - "rollBegegnung(gelaende, diceType, threshold) gibt {begegnung:bool, ergebnis} mit konfigurierbarer Chance zurück"
    - "Eine abgeschlossene Reise rückt D.calendar um die korrekte Tageszahl vor (advanceCalendarDate)"
    - "Bei Reise-Abschluss wird optional ein Timeline-Eintrag vorgeschlagen (addCalendarEvent)"
  artifacts:
    - path: "features/reise/reise-crud.js"
      provides: "berechneTagesmarsch, rollWetter, rollBegegnung, startReise, abschliessenReise"
      contains: "berechneTagesmarsch"
    - path: "features/reise/reise-render.js"
      provides: "renderReise + Reise-Ergebnis-Anzeige"
      contains: "renderReise"
  key_links:
    - from: "features/reise/reise-crud.js"
      to: "features/timeline/timeline-crud.js"
      via: "abschliessenReise ruft advanceCalendarDate + optional addCalendarEvent"
      pattern: "advanceCalendarDate"
    - from: "features/reise/reise-crud.js"
      to: "features/reise/reise-default-tables.js + features/random-tables.js"
      via: "rollWeightedEntry(WETTER_TABELLEN/REISE_BEGEGNUNGS_TABELLEN)"
      pattern: "rollWeightedEntry"
---

<objective>
WELT-04 Reise- & Wetter-Simulator: Reise mit Tempo + Gelände starten, Tagesmärsche nach 5e-Standard berechnen, Wetter aus Klima×Jahreszeit würfeln, gelände-spezifische Zufallsbegegnungen mit konfigurierbarer Chance auslösen, und bei Reise-Abschluss das In-Game-Datum vorrücken (+ optionalen Timeline-Eintrag).

Purpose: Liefert Success Criterion 4 der Phase (D-12…D-15). Konsumiert die geteilten Kalender-Helfer aus Plan 05 (advanceCalendarDate/addCalendarEvent) und die generierten Tabellen aus Plan 02 (WETTER_TABELLEN/REISE_BEGEGNUNGS_TABELLEN).
Output: Funktionsfähiger Reise-Tab mit Berechnung, Wetter, Begegnungen und Kalender-Vorrücken.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-welt-story/05-RESEARCH.md
@.planning/phases/05-welt-story/05-CONTEXT.md

<interfaces>
<!-- Verifizierte Signaturen und Konstanten. -->

5e-Reisetempo (D-12): langsam 18 / normal 24 / schnell 30 Meilen/Tag. Schwieriges Gelände halbiert (×0,5).
  REISE_GELÄNDE-Mapping (eigene Konstante, aus 05-RESEARCH.md): {normal:1.0, schwierig:0.5, gebirge:0.5, sumpf:0.5, meer:1.0}.
  berechneTagesmarsch('normal','normal') === 24; ('langsam','schwierig') === 9 (18×0,5, abgerundet).

WETTER_TABELLEN (window, Plan 02): { 'gemässigt': { winter, fruehling, sommer, herbst } } je {id, diceType:8, entries:[{range,text}]}.
REISE_BEGEGNUNGS_TABELLEN (window, Plan 02): { wald, gebirge, kueste, strasse, ruinen, sumpf } je {id, diceType:8, entries:[{range,text}]}.
HARPTOS_SEASONS (core/constants.js, Plan 01): Map monat(1..12) → 'winter'|'fruehling'|'sommer'|'herbst'.

Wiederverwendung (KEINE Redefinition!):
  rollWeightedEntry(table) → {entry:{text}, roll, diceType} | null (features/random-tables.js).
  advanceCalendarDate(tage), addCalendarEvent(datum, titel, typ, quelleId) (features/timeline/timeline-crud.js, Plan 05).
  TERRAIN_MODIFIERS (features/encounter-calculator.js) — als Vorbild; für Distanz eigenen distanzFaktor nutzen, NICHT multiplier.

Begegnungschance (D-14): konfigurierbar — encounterDiceType (z.B. 20) + threshold (z.B. 1). Begegnung wenn roll ≤ threshold.
rollBegegnung(gelaende, diceType, threshold) → { begegnung:bool, ergebnis: rollWeightedEntry(tabelle) | null }.

Globals: pushUndo, nextId, esc, sanitizeHTML, showToast. var D = window.D; var save = window.save;
Tab-Container: #view-reise. Registry reise → renders ['renderReise']. CSS-Präfix: rs-.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reise-Rechenkern (Tagesmarsch, Wetter, Begegnung)</name>
  <read_first>
    - features/reise/reise-crud.js (Plan-01-Skelett)
    - features/reise/reise-default-tables.js (WETTER_TABELLEN/REISE_BEGEGNUNGS_TABELLEN aus Plan 02)
    - features/random-tables.js (rollWeightedEntry-Signatur + Range-Format)
    - features/encounter-calculator.js (TERRAIN_MODIFIERS — Vorbild, NICHT redefinieren)
    - core/constants.js (HARPTOS_SEASONS — Jahreszeit aus Monat)
    - .planning/phases/05-welt-story/05-VALIDATION.md (WELT-04 Unit: 24 / 9 / rollWetter / rollBegegnung)
  </read_first>
  <behavior>
    - berechneTagesmarsch('normal','normal') === 24.
    - berechneTagesmarsch('langsam','schwierig') === 9 (18 × 0,5).
    - berechneTagesmarsch('schnell','normal') === 30.
    - rollWetter('gemässigt','winter') !== null und result.entry.text ist ein nicht-leerer String.
    - rollBegegnung('wald', 20, 1) liefert { begegnung: boolean, ergebnis: ... }; bei begegnung===true ist ergebnis nicht null.
  </behavior>
  <action>
    In reise-crud.js: REISE_GELÄNDE-Konstante (distanzFaktor-Mapping) definieren. berechneTagesmarsch(tempo, gelaende) — Basismeilen aus Tempo (langsam 18/normal 24/schnell 30) × distanzFaktor des Gelände, Math.floor auf ganze Meilen. rollWetter(klima, jahreszeit) — WETTER_TABELLEN[klima]?.[jahreszeit] holen, via rollWeightedEntry würfeln; Fallback null wenn Tabelle fehlt. rollBegegnung(gelaende, diceType, threshold) — Wurf Math.floor(Math.random()*diceType)+1; begegnung = roll ≤ threshold; bei Begegnung rollWeightedEntry(REISE_BEGEGNUNGS_TABELLEN[gelaende]); diceType/threshold defensiv klemmen (DoS: diceType 2..100, threshold 0..diceType). jahreszeitAusDatum(monat) — HARPTOS_SEASONS[monat]. Eindeutige Funktionsnamen; rollWeightedEntry/TERRAIN_MODIFIERS NICHT redefinieren. window-Exporte.
  </action>
  <verify>
    <automated>cd "D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular" && npx jest tests/unit/welt-story.test.js -t "WELT-04"</automated>
  </verify>
  <acceptance_criteria>
    - berechneTagesmarsch('normal','normal')===24, ('langsam','schwierig')===9, ('schnell','normal')===30 (Unit grün).
    - rollWetter('gemässigt','winter') liefert nicht-null mit Text; rollBegegnung gibt {begegnung,ergebnis}-Form.
    - diceType/threshold werden vor Verwendung geklemmt (DoS-Schutz).
    - Keine Redefinition von rollWeightedEntry/TERRAIN_MODIFIERS; eindeutige Funktionsnamen.
  </acceptance_criteria>
  <done>Rechenkern implementiert; WELT-04-Unit-Tests grün.</done>
</task>

<task type="auto">
  <name>Task 2: Reise-Flow (Start/Abschluss + Kalender-Vorrücken) + View/CSS/Actions/E2E</name>
  <read_first>
    - features/reise/reise-render.js (Plan-01-Skelett renderReise-Stub)
    - features/reise/reise-crud.js (Task-1-Rechenkern)
    - features/timeline/timeline-crud.js (advanceCalendarDate, addCalendarEvent — Signaturen aus Plan 05)
    - features/random-tables.js (Render-/Modal-Muster, esc())
    - assets/templates/view-bestiary.html (View-Layout-Analog)
    - .planning/phases/05-welt-story/05-VALIDATION.md (WELT-04 E2E: Start Tag 1 + 3 Tage → D.calendar.day===4)
  </read_first>
  <action>
    In reise-render.js: renderReise() (Container #view-reise) mit Eingabe für Tempo, Gelände, Distanz/Tage, Klima, Begegnungs-diceType + threshold; Ergebnisbereich für Tagesmärsche, Wetter (rollWetter mit jahreszeitAusDatum(D.calendar.month)) und Begegnungen (rollBegegnung je Tag). Alle Ergebnistexte via esc().
    In reise-crud.js: startReise() (initialisiert laufende Reise-Eingaben), abschliessenReise(tage) — pushUndo('Reise abgeschlossen') → advanceCalendarDate(tage) → save() → optional Dialog "Timeline-Eintrag hinzufügen?" der bei Bestätigung addCalendarEvent({tag,monat,jahr aktuelles Datum}, 'Reise abgeschlossen', 'reise', reiseId) ruft. Tage clampen (DoS, konsistent mit advanceCalendarDate-Cap).
    In view-welt.html: #view-reise mit Formular + Ergebnisbereich. In welt.css: rs--Klassen. In ui/actions/entity-actions.js: data-action-Handler (start-reise, roll-reise-tag, abschliessen-reise, bestaetigen-reise-timeline).
    Aktiviere WELT-04-E2E-Stub: Start bei D.calendar.day=1, 3 Tage Reise abschließen → D.calendar.day===4.
  </action>
  <verify>
    <automated>cd "D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular" && set PYTHONIOENCODING=utf-8 && python build.py && npx jest tests/unit/welt-story.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `python build.py` grün.
    - renderReise zeigt Tempo/Gelände/Klima/Begegnungs-Konfiguration + Ergebnisbereich (Tagesmarsch, Wetter, Begegnung).
    - abschliessenReise ruft pushUndo() vor advanceCalendarDate(tage); E2E: 3 Tage → D.calendar.day===4.
    - Optionaler Timeline-Vorschlag ruft addCalendarEvent mit typ:'reise'.
    - Wetter nutzt aus D.calendar.month abgeleitete Jahreszeit (HARPTOS_SEASONS).
    - WELT-04-Tests aktiviert und grün.
  </acceptance_criteria>
  <done>Reise-Tab funktionsfähig; Kalender rückt korrekt vor; Build + Tests grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reise-Formular (Tage, diceType, threshold) → Berechnungs-/Datums-Schleifen | Numerische Nutzereingaben steuern Schleifen |
| Wetter-/Begegnungstexte → DOM | Tabelleninhalte werden im Ergebnisbereich gerendert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-19 | Denial of Service | unbegrenzte Tage/diceType in Schleifen (Reise-Tage, Begegnungswürfe) | mitigate | tage clampen (Cap konsistent mit advanceCalendarDate ~3650); diceType auf 2..100, threshold auf 0..diceType klemmen vor jeder Schleife |
| T-05-20 | Information Disclosure (XSS) | Wetter-/Begegnungs-/Reise-Ergebnis-Render | mitigate | Alle Ergebnistexte via esc() (auch generierte Tabellentexte) |
| T-05-21 | Tampering | abschliessenReise verändert D.calendar irreversibel | mitigate | pushUndo('Reise abgeschlossen') VOR advanceCalendarDate; Undo stellt Datum wieder her |
| T-05-22 | Tampering | jahreszeitAusDatum bei ungültigem month | mitigate | HARPTOS_SEASONS-Lookup mit Fallback (z.B. 'fruehling') statt undefined; month aus D.calendar (1..12 nach Migration) |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | Keine neuen Pakete |
</threat_model>

<verification>
- Build grün; Reise-Rechenkern-Units (24/9/30, Wetter, Begegnung) grün.
- Reise-Abschluss rückt D.calendar korrekt vor (E2E day===4); Undo stellt Datum wieder her.
- Optionaler Timeline-Eintrag erstellbar.
</verification>

<success_criteria>
- WELT-04 erfüllt (Phase Success Criterion 4): Tagesmärsche, Wetter, Begegnungen, Kalender-Vorrücken.
</success_criteria>

<output>
Create `.planning/phases/05-welt-story/05-06-SUMMARY.md` when done
</output>
