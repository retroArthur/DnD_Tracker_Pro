---
phase: 05-welt-story
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/data.js
  - core/constants.js
  - systems/spellslots/version-migration.js
  - systems/tab-registry.js
  - loader.js
  - build.py
  - assets/styles.css
  - assets/styles/welt.css
  - assets/templates/view-welt.html
  - features/session-prep/session-prep-render.js
  - features/session-prep/session-prep-crud.js
  - features/npc-generator/npc-default-tables.js
  - features/npc-generator/npc-generator.js
  - features/timeline/timeline-render.js
  - features/timeline/timeline-crud.js
  - features/reise/reise-default-tables.js
  - features/reise/reise-render.js
  - features/reise/reise-crud.js
  - features/fraktionen/fraktionen-render.js
  - features/fraktionen/fraktionen-crud.js
  - tests/unit/welt-story.test.js
  - tests/e2e/features/welt-story.spec.js
autonomous: true
requirements: [WELT-01, WELT-02, WELT-03, WELT-04, WELT-05]
must_haves:
  truths:
    - "Migration 4.0.0 legt D.sessionPreps und D.factions an und konvertiert D.calendar.month auf 1-Basis"
    - "Die 4 neuen Tabs (sessionprep, kalender, reise, fraktionen) sind in TAB_RENDER_REGISTRY registriert"
    - "Alle 11 neuen Feature-Module + view-welt.html + welt.css sind in loader.js UND build.py registriert (Listen synchron)"
    - "HARPTOS_MONTHS, HARPTOS_FESTIVALS und HARPTOS_SEASONS existieren in core/constants.js"
    - "python build.py läuft fehlerfrei durch (Modullisten-Sync + Duplikat-Check bestehen)"
    - "tests/unit/welt-story.test.js und tests/e2e/features/welt-story.spec.js existieren als Wave-0-Stubs"
  artifacts:
    - path: "core/data.js"
      provides: "sessionPreps:[] und factions:[] in initializeData()"
      contains: "sessionPreps"
    - path: "core/constants.js"
      provides: "Harptos-Kalenderdefinition"
      contains: "HARPTOS_MONTHS"
    - path: "systems/spellslots/version-migration.js"
      provides: "Migration 4.0.0"
      contains: "4.0.0"
    - path: "systems/tab-registry.js"
      provides: "4 neue Tab-Registrierungen"
      contains: "sessionprep"
    - path: "assets/styles/welt.css"
      provides: "CSS-Grundgerüst mit Sektionsmarkern je Feature (wp-/npcg-/tl-/rs-/fr-)"
      min_lines: 20
    - path: "assets/templates/view-welt.html"
      provides: "4 Views mit data-view-Containern"
      contains: "view-sessionprep"
    - path: "tests/unit/welt-story.test.js"
      provides: "Wave-0 Nyquist-Test-Stubs"
      contains: "berechneTagesmarsch"
  key_links:
    - from: "loader.js MODULES"
      to: "build.py modules"
      via: "check_module_list_sync()"
      pattern: "features/session-prep/session-prep-render.js"
    - from: "systems/tab-registry.js"
      to: "view-welt.html data-view-Container"
      via: "switchView zeigt #view-<tab>"
      pattern: "view-sessionprep"
---

<objective>
Wave-0-Fundament für Phase 5 (Welt & Story): Schema-Erweiterungen, Migration 4.0.0, Harptos-Kalenderkonstanten, die 0→1-Basis-Korrektur des Kalendermonats, alle 11 Feature-Modul-Skelette mit 3-fach-Registrierung (loader.js + build.py + tab-registry.js), das gemeinsame `view-welt.html`-Template, das `welt.css`-Grundgerüst sowie die Wave-0-Test-Stubs.

Purpose: Schafft das geteilte technische Substrat, auf dem alle fünf Features (WELT-01…05) aufbauen. Ohne dieses Fundament können die Feature-Wellen weder rendern (kein Tab/Container) noch persistieren (kein Schema/Migration) noch bauen (Modullisten-Sync bricht ab).
Output: Lauffähiger Build mit 4 leeren neuen Tabs, persistiertem Schema und grünen (skipped) Test-Stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-welt-story/05-RESEARCH.md
@.planning/phases/05-welt-story/05-CONTEXT.md
@.planning/codebase/CONCERNS.md

<interfaces>
<!-- Verifizierte Signaturen/Strukturen, die der Executor direkt verwendet — keine Codebase-Exploration nötig. -->

D.calendar (core/data.js, bestehend): { day: 1, month: 0, year: 1492, events: [] }
  ACHTUNG: month ist aktuell 0-basiert in initializeData(); Migration 2.4.0 setzt bei fehlendem Kalender month: 4.

MIGRATIONS (systems/spellslots/version-migration.js): Objekt-Map { '<version>': data => data }.
  Letzter Eintrag aktuell '3.0.0'. Reihenfolge via Object.keys(...).sort(); jede Migration läuft, wenn compareVersions(dataVersion, version) < 0.

TAB_RENDER_REGISTRY (systems/tab-registry.js): { '<tab>': { renders: ['fnName',...], init: null, cleanup: null } }.
  Bestehende Keys: dashboard, party, npcs, locations, quests, encounter, initiative, loot, shops, spells, notes, wiki, links, dice, timers, data, dmscreen, bestiary.
  switchView(name) (navigation.js) zeigt #view-<name> und Tab [data-view="<name>"].

loader.js: const MODULES = [...]; — build.py: modules = MODULES (eigene identische Liste). check_module_list_sync() bricht ab bei Divergenz.
build.py css_files endet aktuell mit 'bestiary.css'. build.py html_templates enthält 'view-bestiary.html'.
assets/styles.css ist ein @import-Hub.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema, Migration 4.0.0 und Harptos-Konstanten</name>
  <read_first>
    - core/data.js (initializeData() — Schema-Erweiterungspunkt)
    - systems/spellslots/version-migration.js (MIGRATIONS-Map, compareVersions, Migrationsmuster '3.0.0' als Analog)
    - core/constants.js (DND_RULES-Namespace, UPPER_SNAKE-Konventionen)
    - .planning/phases/05-welt-story/05-RESEARCH.md (§ Harptos-Daten, § Schema-Erweiterungen — HARPTOS_MONTHS/FESTIVALS/SEASONS, Migration-4.0.0-Vorlage)
  </read_first>
  <action>
    In core/data.js: in initializeData() nach bestiaryFavorites die Felder `sessionPreps: []` und `factions: []` ergänzen (Kommentar "Phase 5: Welt & Story"). D.calendar bleibt strukturell unverändert.
    In core/constants.js: drei Konstanten ergänzen — HARPTOS_MONTHS (12 Objekte {nr, name, jahreszeit}), HARPTOS_FESTIVALS (5 Objekte {name, nachMonat}) und HARPTOS_SEASONS (Map monat→jahreszeit). Exakte Werte aus 05-RESEARCH.md § Harptos-Daten übernehmen. Wie andere Konstanten am Dateiende per `window.HARPTOS_MONTHS = HARPTOS_MONTHS;` exportieren und in den DND_RULES-Namespace aufnehmen, falls dort registriert (Muster der bestehenden Konstanten replizieren).
    In systems/spellslots/version-migration.js: Eintrag '4.0.0' zur MIGRATIONS-Map hinzufügen (analog '3.0.0'): setzt `data.sessionPreps = []` und `data.factions = []` falls fehlend; konvertiert `data.calendar.month` von 0- auf 1-Basis (D-09/RESEARCH-Empfehlung: wenn `data.calendar && data.calendar.month < 1` dann `data.calendar.month = (data.calendar.month || 0) + 1`). Keine doppelten Top-Level-Funktionsnamen einführen — nur die MIGRATIONS-Map-Property ergänzen.
  </action>
  <verify>
    <automated>cd "D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular" && node -e "const fs=require('fs'); const d=fs.readFileSync('core/data.js','utf8'); const c=fs.readFileSync('core/constants.js','utf8'); const m=fs.readFileSync('systems/spellslots/version-migration.js','utf8'); if(!/sessionPreps:\s*\[\]/.test(d)||!/factions:\s*\[\]/.test(d)) throw new Error('Schema fehlt'); if(!/HARPTOS_MONTHS/.test(c)||!/HARPTOS_FESTIVALS/.test(c)||!/HARPTOS_SEASONS/.test(c)) throw new Error('Harptos-Konstanten fehlen'); if(!/'4\.0\.0'/.test(m)) throw new Error('Migration 4.0.0 fehlt'); console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - core/data.js initializeData() enthält `sessionPreps: []` und `factions: []`.
    - core/constants.js definiert HARPTOS_MONTHS (12 Monate, Hammer=nr:1) + HARPTOS_FESTIVALS (5) + HARPTOS_SEASONS und exportiert sie auf window.
    - version-migration.js MIGRATIONS hat Schlüssel '4.0.0'; die Funktion setzt sessionPreps/factions und hebt month<1 um +1.
    - Kein neuer doppelter Top-Level-Funktionsname (nur MIGRATIONS-Property + Konstanten).
  </acceptance_criteria>
  <done>Schema, Migration und Harptos-Konstanten vorhanden; obige node-Assertion bestanden.</done>
</task>

<task type="auto">
  <name>Task 2: 11 Modul-Skelette, view-welt.html, welt.css und 3-fach-Registrierung</name>
  <read_first>
    - loader.js (MODULES-Array, Reihenfolge nach features/sessions/sessions.js)
    - build.py (Zeilen ~100-163 modules-Liste, ~426-435 css_files, ~458-463 html_templates, check_module_list_sync(), check_duplicate_functions())
    - systems/tab-registry.js (TAB_RENDER_REGISTRY-Format)
    - assets/templates/view-bestiary.html (Analog für eine neue View-Section mit data-view-Container)
    - assets/templates/header.html (nav-tabs — neue nav-tab-Buttons mit data-view)
    - assets/styles.css (@import-Hub)
    - .planning/phases/05-welt-story/05-RESEARCH.md (§ Build/Dedup-Fallstricke — empfohlene Modulreihenfolge, § Empfohlene Dateistruktur, § TAB_RENDER_REGISTRY-Einträge)
    - .planning/codebase/STRUCTURE.md (§ Where to Add New Code — 3-fach-Registrierung)
  </read_first>
  <action>
    Lege 11 Skelett-Module unter features/ an (je mit `// [SECTION:...]`-Marker und window-Export der enthaltenen render-Funktion als no-op-Stub mit defensivem Container-Check, Muster wie renderRandomTables): session-prep/session-prep-render.js (renderSessionPrepList), session-prep/session-prep-crud.js; npc-generator/npc-default-tables.js (leeres `const NPC_DEFAULT_TABLES = {}` + window-Export — wird in Plan 02 generiert ersetzt), npc-generator/npc-generator.js; timeline/timeline-render.js (renderTimeline, renderKalender), timeline/timeline-crud.js; reise/reise-default-tables.js (leere `const REISE_BEGEGNUNGS_TABELLEN={}` + `const WETTER_TABELLEN={}`), reise/reise-render.js (renderReise), reise/reise-crud.js; fraktionen/fraktionen-render.js (renderFraktionen), fraktionen/fraktionen-crud.js. Jede render-Stub-Funktion: Container per $('...') holen, bei fehlendem Container früh return mit DEBUG-Warnung (Muster renderRandomTables). KEINE doppelten Funktionsnamen — renderTimeline/renderKalender/renderFraktionen/renderReise/renderSessionPrepList existieren nirgends sonst (verifiziert). rollWeightedEntry/rollOnTable/saveNPC/TERRAIN_MODIFIERS NICHT redefinieren.
    Registriere alle 11 Module in loader.js MODULES UND build.py modules an identischer Position in EXAKT der Reihenfolge aus 05-RESEARCH.md § Build/Dedup-Checkliste: nach 'features/sessions/sessions.js' und vor 'features/wiki/wiki.js'. npc-default-tables.js + npc-generator.js MÜSSEN nach features/npcs/npc-crud.js geladen werden (sind sie, da npcs-Block früher liegt). reise-default-tables.js vor reise-render/crud.js. npc-default-tables.js vor npc-generator.js.
    Registriere 4 Tabs in TAB_RENDER_REGISTRY (vor schließender `};`): 'sessionprep' → renders ['renderSessionPrepList']; 'kalender' → renders ['renderTimeline','renderKalender']; 'reise' → renders ['renderReise']; 'fraktionen' → renders ['renderFraktionen']; init/cleanup je null.
    Erstelle assets/templates/view-welt.html mit 4 `<section class="view" id="view-sessionprep|view-kalender|view-reise|view-fraktionen">` (data-view-Tab-Namen müssen 1:1 zu den Registry-Keys passen). Trage view-welt.html in loader.js TEMPLATES UND build.py html_templates ein (Position nach view-bestiary.html). Füge in header.html nach dem Bestiar-nav-tab 4 neue `<button class="nav-tab" data-view="sessionprep|kalender|reise|fraktionen" ...>` mit deutschen Labels + Icons ein (z.B. 📋 Session-Prep, 📅 Kalender, 🧭 Reise, ⚔️ Fraktionen — Tab-Reihenfolge nach Claude's Discretion).
    Erstelle assets/styles/welt.css mit Sektions-Kommentarblöcken je Feature-Präfix (wp- Session-Prep, npcg- NPC-Generator, tl- Timeline, rs- Reise, fr- Fraktionen) als Grundgerüst (Layout-Basis kann leer/minimal sein — Feature-Pläne füllen). Registriere in assets/styles.css per `@import 'styles/welt.css';` UND in build.py css_files (nach 'bestiary.css').
  </action>
  <verify>
    <automated>cd "D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular" && set PYTHONIOENCODING=utf-8 && python build.py</automated>
  </verify>
  <acceptance_criteria>
    - `python build.py` läuft ohne Fehler durch (check_module_list_sync() und check_duplicate_functions() bestehen → keine Divergenz, keine Duplikate).
    - 11 Module existieren unter features/{session-prep,npc-generator,timeline,reise,fraktionen}/ und sind in loader.js + build.py registriert (identische Listen).
    - TAB_RENDER_REGISTRY enthält die Keys sessionprep, kalender, reise, fraktionen.
    - view-welt.html enthält Container #view-sessionprep, #view-kalender, #view-reise, #view-fraktionen; view-welt.html in loader.js TEMPLATES + build.py html_templates.
    - header.html hat 4 neue nav-tab-Buttons mit passenden data-view-Werten.
    - assets/styles.css importiert welt.css; build.py css_files enthält 'welt.css'.
  </acceptance_criteria>
  <done>Build grün; 4 leere Tabs öffnen sich ohne Konsolenfehler im dist-Build; Listen synchron.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave-0 Nyquist-Test-Stubs (Unit + E2E)</name>
  <read_first>
    - tests/unit/encounter-calculator.test.js (Jest-Muster, describe/it, require-Strategie)
    - tests/e2e/features/wiki.spec.js (Playwright-Muster, page-Navigation, dist-Target)
    - .planning/phases/05-welt-story/05-VALIDATION.md (§ Per-Requirement Verification Map, § Wave 0 Requirements — die unit-verifizierbaren Wahrheiten)
  </read_first>
  <behavior>
    - Unit-Stub für berechneTagesmarsch('normal','normal') === 24 und ('langsam','schwierig') === 9 (WELT-04) — als `it.todo` oder `test.skip` bis Plan 06 die Funktion liefert.
    - Unit-Stub für rufStufe(15).label === 'Freundlich' und rufStufe(21).label === 'Verbündet' (WELT-05) — skip bis Plan 07.
    - Unit-Stub für generiereNPCName('zwerg','maennlich') ∈ Pool (WELT-02) — skip bis Plan 03/04-Tabellen.
    - Unit-Stub für rollWetter/rollBegegnung-Rückgabeform (WELT-04) — skip bis Plan 06.
    - Unit-Stub für chronologische Timeline-Sortierung (WELT-03) — skip bis Plan 05.
    - E2E-Stub-Spec mit test.skip-Flows je neuem Tab (sessionprep, kalender, reise, fraktionen).
  </behavior>
  <action>
    Erstelle tests/unit/welt-story.test.js mit describe-Blöcken je WELT-Requirement und den o.g. Behauptungen als `test.skip(...)`/`it.todo(...)` (kein Watch-Mode-Flag). Jeder Skip-Test trägt einen Kommentar mit dem Plan, der ihn aktiviert (z.B. "// aktiviert in 05-06"). Erstelle tests/e2e/features/welt-story.spec.js mit `test.describe`-Blöcken je Tab und `test.skip`-Platzhaltern, die später die data-view-Navigation prüfen. Kommandos müssen ohne --watch laufen.
  </action>
  <verify>
    <automated>cd "D:/AI_CLI/Claude/DnD_Tracker_App_Pro/dnd-tracker-modular" && npx jest tests/unit/welt-story.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest tests/unit/welt-story.test.js` läuft grün (Tests sind skipped/todo, kein Fehler, kein Watch-Mode).
    - tests/unit/welt-story.test.js referenziert berechneTagesmarsch, rufStufe, generiereNPCName, rollWetter/rollBegegnung und Timeline-Sortierung.
    - tests/e2e/features/welt-story.spec.js existiert mit skip-Stubs je neuem Tab.
    - Jeder Skip trägt einen Verweis auf den aktivierenden Plan.
  </acceptance_criteria>
  <done>Test-Stubs vorhanden und grün; keine 3 aufeinanderfolgenden späteren Tasks ohne automatisierte Verifikation (Stub-Basis gelegt).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| importierte/alte Kampagnendaten → migrateData() | Migration 4.0.0 verarbeitet potenziell fehlerhafte/alte `D.calendar.month`-Werte |
| keine Netzwerkgrenze | Offline/lokal (D-00a) — kein API/Auth-Surface in diesem Plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Migration 4.0.0 month-Konvertierung | mitigate | Nur konvertieren wenn `data.calendar` existiert UND `month < 1`; `(data.calendar.month || 0) + 1` verhindert NaN/undefined; idempotent (zweiter Lauf trifft month≥1 nicht) |
| T-05-02 | Denial of Service | Migration läuft über fremde Importe | accept | Migration ist O(1)-Feldzuweisung, keine Schleifen über Nutzereingaben; kein DoS-Vektor |
| T-05-03 | Tampering | doppelte Top-Level-Funktion über 11 neue Module bricht Build | mitigate | check_duplicate_functions() in build.py ist der Gate; Skelette führen nur eindeutige render*-Namen ein; rollWeightedEntry/saveNPC/TERRAIN_MODIFIERS werden NICHT redefiniert |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | Keine neuen Pakete in Phase 5 (RESEARCH: Package Legitimacy Audit entfällt); kein Install-Task vorhanden |
</threat_model>

<verification>
- `python build.py` grün (Sync + Duplikat-Check).
- dist-Build öffnet, 4 neue Tabs sichtbar und ohne Konsolenfehler anklickbar (leere Container).
- `npx jest tests/unit/welt-story.test.js` grün.
- Re-Import eines alten Exports (month 0-basiert) ergibt nach Migration `D.calendar.month >= 1`, `D.sessionPreps` und `D.factions` als Arrays.
</verification>

<success_criteria>
- Schema + Migration 4.0.0 + Harptos-Konstanten vorhanden und persistierbar.
- 11 Module + view-welt.html + welt.css 3-fach registriert; Listen synchron.
- 4 leere Tabs lauffähig im dist-Build.
- Wave-0-Test-Stubs grün.
</success_criteria>

<output>
Create `.planning/phases/05-welt-story/05-01-SUMMARY.md` when done
</output>
