---
phase: 10-security-h-rtung
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - tests/unit/security.test.js
  - tests/unit/sanitizer-parity.test.js
  - utils/basic.js
  - utils/testable-utils.js
  - tests/e2e/features/editor-formatting.spec.js
  - .planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md
autonomous: true
requirements: [SEC-01]

must_haves:
  truths:
    - "Der kuratierte Vektor-Katalog läuft gegen den ECHTEN Produktions-Sanitizer aus utils/basic.js, geladen per vm.runInContext — nicht gegen den Test-Zwilling (D-14, D-15)"
    - "Ein Paritätstest vergleicht utils/basic.js und utils/testable-utils.js über ein gemeinsames Vektor-Set und ist grün, BEVOR die Whitelist geändert wird — er belegt die Drift-Freiheit zum Ist-Zeitpunkt und verhindert sie ab dann (D-14, RESEARCH Pitfall 5)"
    - "Der Paritätsvergleich verwendet exakte Zeichenketten-Gleichheit der Sanitizer-Ausgaben (strikter Vergleich auf Ebene der JavaScript-Zeichenketten, ohne Normalisierung, ohne Trimmen, ohne Umsortieren von Attributen) — abweichende Groß-/Kleinschreibung oder Leerraum gilt als Drift"
    - "Durchgestrichener Text überlebt den Speichern-/Neuladen-Zyklus: die Auszeichnung ist in beiden Sanitizer-Zwillingen erlaubt (D-06)"
    - "Die Änderung an der eingefrorenen Netz-Datei ist im Netz-Freeze-Protokoll von 09-BASELINE.md schriftlich begründet, mit altem und neuem Erwartungswert (Phase-9-D-04a)"
  artifacts:
    - "tests/unit/sanitizer-parity.test.js — Paritätstest über ein gemeinsames Vektor-Set"
    - "Neuer vm-basierter Testblock in tests/unit/security.test.js gegen den echten utils/basic.js"
    - "Eintrag im Abschnitt Netz-Freeze von 09-BASELINE.md mit Begründung der Erwartungswert-Änderung"
  key_links:
    - "tests/unit/sanitizer-parity.test.js → utils/basic.js und utils/testable-utils.js über vm.runInContext → gemeinsames Vektor-Set → strikter Ausgabevergleich"
    - "allowedTags in utils/basic.js ↔ allowedTags in utils/testable-utils.js — jede Whitelist-Änderung muss in beiden Dateien im selben Commit landen; der Paritätstest ist der Zaun"
    - "editor-formatting.spec.js Roundtrip-Erwartung für durchgestrichenen Text → 09-BASELINE.md Netz-Freeze-Protokoll → dokumentierte Begründung"
  prohibitions:
    - statement: "Eine Assertion des eingefrorenen Phase-9-Regressionsnetzes darf NICHT abgeschwächt werden, um eine Änderung grün zu bekommen — zulässig ist ausschließlich die hier geplante Anpassung des Erwartungswerts für durchgestrichenen Text, und nur mit schriftlicher Begründung im Netz-Freeze-Protokoll; jede weitere Netzänderung gilt als Beweis-Leck"
      status: flagged-unverified
      verification: judgment
    - statement: "Ein Sicherheitstest dieser Phase darf NICHT als Nachweis gelten, wenn er auch ohne den Produktionscode bestehen würde — Assertions über die eigenen Eingabe-Literale sind kein Nachweis (RESEARCH Pitfall 4: tests/unit/markdown-converter.test.js ist genau so gebaut und darf nicht als Abdeckung herangezogen werden)"
      status: flagged-unverified
      verification: test
---

<objective>
Baue das Beweisnetz um den Sanitizer selbst: der kuratierte Vektor-Katalog läuft gegen den echten Produktionsquelltext, ein Paritätstest nagelt beide Sanitizer-Kopien aneinander, und erst danach wird die Whitelist um die durchgestrichene Auszeichnung erweitert — der Datenintegritäts-Bug, den Phase 9 bewusst eingefroren und für Phase 10 vorgemerkt hat.

Purpose: Der Sanitizer existiert doppelt im Repository. Ohne strukturellen Zaun landet ein Whitelist-Fix in einer Datei und nicht in der anderen — genau die als hochprior dokumentierte Drift. Der Zaun muss stehen, bevor die erste Whitelist-Änderung durchgeht, sonst beweist er nichts.
Output: vm-basierter Vektor-Katalog, Paritätstest, Whitelist-Fix in beiden Dateien, begründete Netz-Anpassung.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-security-h-rtung/10-CONTEXT.md
@.planning/phases/10-security-h-rtung/10-RESEARCH.md
@.planning/phases/10-security-h-rtung/10-PATTERNS.md
@.planning/phases/10-security-h-rtung/10-VALIDATION.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vektor-Katalog gegen den echten Produktions-Sanitizer</name>
  <files>tests/unit/security.test.js</files>
  <read_first>
    - tests/unit/security.test.js (Kopf Zeile 1-12: der Import zieht heute `sanitizeHTML` und `esc` aus `utils/testable-utils` — dieser bestehende Import und alle bestehenden Testblöcke bleiben unangetastet; der neue Block kommt zusätzlich)
    - tests/unit/storage-conflict.test.js (vollständig — das vm-Muster: Kontextobjekt, `vm.createContext`, `fs.readFileSync`, `vm.runInContext`, Symbol aus dem Kontext ziehen)
    - utils/basic.js (Zeilen 58-160 `sanitizeHTML()`: Vorbereinigungs-Regex Zeilen 61-67, `allowedTags` Zeilen 72-101, `allowedAttributes` Zeilen 102-126, gefährliche Protokolle Zeile 128; Zeilen 19-28 `esc()`)
    - .planning/phases/10-security-h-rtung/10-CONTEXT.md (D-15: der Pflicht-Vektorsatz — Review-Exploit mit Bild und Fehler-Ereignis, Adressen mit Skript-Protokoll, Skript-Elemente, SVG-Ereignis-Attribut, Tabellen-Paste-Nutzlast aus T-09-01)
    - .planning/phases/10-security-h-rtung/10-RESEARCH.md (Pitfall 4: `tests/unit/markdown-converter.test.js` ist wirkungslos und darf nicht als Abdeckung gezählt werden; Pitfall 5: Ist-Stand der Drift)
  </read_first>
  <action>
    Ergänze `tests/unit/security.test.js` um einen neuen, klar abgegrenzten Testblock mit deutscher Überschrift, der den ECHTEN Produktionsquelltext lädt. Bestehende Blöcke und der bestehende Import bleiben unverändert — der neue Block steht daneben und macht durch seinen Namen sichtbar, dass er gegen `utils/basic.js` läuft, während die alten Blöcke gegen den Test-Zwilling laufen.

    Aufbau nach dem Muster aus `tests/unit/storage-conflict.test.js`: Kontextobjekt mit `window`, den jsdom-Globalobjekten `document`, `DOMParser`, `Node` und `console`; `vm.createContext`; Quelltext von `utils/basic.js` per `fs.readFileSync` lesen und mit `vm.runInContext` ausführen; danach die Funktion aus dem Kontextobjekt ziehen. Ein Vorab-Test stellt sicher, dass die geladene Funktion tatsächlich definiert ist — sonst würde ein späterer Fehlschlag als bestandener Test durchgehen.

    Vektor-Katalog (jeder Vektor eine eigene Assertion, deutsche Testnamen):
    - Review-Exploit aus `01-REVIEW.md`: Bild-Element mit Fehler-Ereignis-Attribut. Erwartung: das Ereignis-Attribut kommt in der Ausgabe nicht vor, und das Bild-Element ist verschwunden (es steht nicht in der erlaubten Tag-Liste).
    - Ereignis-Attribut ohne Anführungszeichen (die zweite Vorbereinigungs-Regex deckt diese Schreibweise ab).
    - Skript-Element mit Inhalt; zusätzlich in gemischter Groß-/Kleinschreibung.
    - Adresse mit Skript-Protokoll in einem Verweis-Element: Erwartung — die Adresse enthält das Protokoll nicht mehr.
    - SVG-Element mit Lade-Ereignis-Attribut.
    - Tabellen-Nutzlast im Stil des T-09-01-Tests (Tabelle, in deren Zelle ein Bild mit Fehler-Ereignis steht): Erwartung — Tabelle und Zellen bleiben erhalten, das Ereignis-Attribut nicht.
    - Erhaltungs-Gegenprobe: erlaubte Auszeichnung (fett, kursiv, Liste, Tabelle, Hervorhebung) übersteht den Sanitizer unverändert, und der Textinhalt bleibt in allen Vektoren vollständig erhalten.
    - Randfälle: leere Zeichenkette, `null` und `undefined` liefern eine leere Zeichenkette statt eines Fehlers.

    Führe die Datei aus. Alle neuen Fälle müssen gegen den aktuellen Stand bereits grün sein — der Sanitizer ist intakt, dieser Block dokumentiert und verriegelt den Ist-Zustand. Ist ein Fall rot, ist das ein echter Fund: dokumentiere ihn in der SUMMARY und behandle ihn nach D-12 (Critical/High wird noch in dieser Phase behoben).
  </action>
  <verify>
    <automated>npx jest tests/unit/security.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/security.test.js` enthält einen neuen Testblock, der `utils/basic.js` per `vm.runInContext` aus dem Quelltext lädt und die geladene Funktion vor dem ersten Vektor auf Definiertheit prüft
    - Der Block deckt alle sechs Pflicht-Vektoren aus D-15 mit je eigener Assertion ab
    - Der Block enthält mindestens eine Erhaltungs-Gegenprobe (erlaubte Auszeichnung übersteht unverändert) und die drei Randfälle für leere Eingaben
    - Die bestehenden Testblöcke der Datei sind unverändert (der Import aus dem Test-Zwilling bleibt bestehen)
    - `npx jest tests/unit/security.test.js` endet mit Exit-Code 0
  </acceptance_criteria>
  <done>Der Vektor-Katalog prüft ab jetzt das, was die Anwendung wirklich ausführt — nicht eine Kopie davon.</done>
</task>

<task type="auto">
  <name>Task 2: Paritätstest zwischen Produktions-Sanitizer und Test-Zwilling (Zaun vor der Whitelist-Änderung)</name>
  <files>tests/unit/sanitizer-parity.test.js</files>
  <read_first>
    - utils/basic.js (Zeilen 19-28 `esc()`, Zeilen 58-160 `sanitizeHTML()` — die Produktions-Wahrheit)
    - utils/testable-utils.js (die entsprechenden Zwillinge, insbesondere die `allowedTags`-Liste ab etwa Zeile 48 und die abweichende Behandlung der Zahl Null in `esc()`)
    - tests/unit/storage-conflict.test.js (vm-Muster)
    - .planning/codebase/CONCERNS.md (Abschnitt „Test Coverage Gaps": die Drift zwischen den beiden Dateien ist dort als hochprior geführt — dieser Test schließt genau diese Lücke)
    - .planning/phases/10-security-h-rtung/10-RESEARCH.md (Pitfall 5: die Sanitizer-Funktion ist heute NICHT gedriftet, `esc()` schon; der Test muss also die Drift-Freiheit zum Ist-Zeitpunkt belegen und darf sich nicht auf eine erwartete Abweichung stützen)
  </read_first>
  <action>
    Lege `tests/unit/sanitizer-parity.test.js` an. Der Test lädt BEIDE Dateien getrennt per `vm.runInContext` (je ein eigener Kontext mit denselben jsdom-Globalobjekten) und vergleicht die Ausgaben Vektor für Vektor.

    Vektor-Set: dasselbe Set wie in Task 1 plus die Auszeichnungs-Vektoren, die für die Whitelist relevant sind — fett, kursiv, unterstrichen, beide Schreibweisen für durchgestrichenen Text, Liste, Tabelle, Hervorhebung, Verweis, Schriftart-Element. Definiere das Set als eine einzige benannte Konstante am Dateikopf, damit spätere Erweiterungen an genau einer Stelle passieren.

    Assertion je Vektor: die Ausgaben beider Kopien sind exakt gleich. Verwende strikte Zeichenketten-Gleichheit ohne Vorverarbeitung — kein Trimmen, keine Normalisierung von Groß-/Kleinschreibung, kein Umsortieren von Attributen. Abweichender Leerraum ist Drift und soll den Test rot machen.

    Ergänze zwei Strukturprüfungen, die über den reinen Ausgabevergleich hinausgehen: (a) die Listen der erlaubten Tags beider Dateien enthalten dieselben Einträge in derselben Reihenfolge — ziehe die Listen dazu über einen Ausdrucks-Aufruf im jeweiligen Kontext heraus, da sie als Konstante innerhalb der Funktion deklariert sind und gegebenenfalls über eine kleine Hilfsauswertung im Kontext zugänglich gemacht werden müssen; ist ein direkter Zugriff nicht möglich, prüfe stattdessen für jeden Tag-Namen aus einer im Test gepflegten Liste, dass beide Kopien ihn identisch behandeln (erhalten oder entfernen). (b) Für die Zeichen-Maskierungsfunktion beider Dateien wird die bekannte Abweichung bei der Zahl Null explizit als bekannt dokumentiert: schreibe dafür einen eigenen Testfall mit einem erklärenden Kommentar, der den Ist-Unterschied festhält, statt ihn stillschweigend zu übergehen. Dieser Fall darf die Suite nicht rot machen — er dokumentiert die bestehende Abweichung und verhindert, dass sie unbemerkt wächst.

    Führe die Datei aus. Sie MUSS gegen den aktuellen, noch nicht geänderten Stand grün sein — das ist der Beweis der Drift-Freiheit zum Ist-Zeitpunkt und die Voraussetzung dafür, dass sie in Task 3 als Zaun wirkt. Ist sie rot, existiert bereits eine Drift: dann diese zuerst dokumentieren und beheben, bevor Task 3 beginnt.
  </action>
  <verify>
    <automated>npx jest tests/unit/sanitizer-parity.test.js && npx jest</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/sanitizer-parity.test.js` existiert, lädt beide Dateien per `vm.runInContext` aus dem Quelltext und vergleicht ihre Ausgaben
    - Das Vektor-Set steht als eine benannte Konstante am Dateikopf und enthält mindestens die sechs Angriffs-Vektoren aus D-15 sowie die neun Auszeichnungs-Vektoren
    - Jeder Vergleich verwendet strikte Gleichheit ohne Vorverarbeitung der Ausgaben
    - Der Test enthält einen eigenen, kommentierten Fall für die bekannte Abweichung der Zeichen-Maskierungsfunktion bei der Zahl Null
    - Verhalten: `npx jest tests/unit/sanitizer-parity.test.js` ist grün, BEVOR in Task 3 eine Whitelist-Änderung erfolgt; das Ergebnis dieses Laufs ist in der SUMMARY protokolliert
    - `npx jest` (volle Unit-Suite) endet mit Exit-Code 0
  </acceptance_criteria>
  <done>Der Zaun steht und ist nachweislich grün gegen den unveränderten Stand — ab jetzt kann eine Whitelist-Änderung nicht mehr in nur einer der beiden Dateien landen.</done>
</task>

<task type="auto">
  <name>Task 3: Durchgestrichene Auszeichnung in beide Whitelists aufnehmen und die Netz-Anpassung begründen</name>
  <reversibility rating="costly">Sobald die Auszeichnung erlaubt ist, erzeugen Nutzerdaten entsprechendes Markup; eine spätere Verengung der Whitelist würde dieses Markup in bereits gespeicherten Kampagnen zerstören (D-06).</reversibility>
  <files>utils/basic.js, utils/testable-utils.js, tests/e2e/features/editor-formatting.spec.js, .planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md</files>
  <read_first>
    - utils/basic.js (Zeilen 72-101 `allowedTags` — die Liste, in der neben dem kurzen Durchstreich-Tag der lange Name fehlt)
    - utils/testable-utils.js (die strukturgleiche Liste, etwa ab Zeile 48 — dieselbe Änderung muss dort im selben Commit landen)
    - tests/e2e/features/editor-formatting.spec.js (Zeilen 139-170: die Erwartungswert-Tabelle `NETZ` mit dem Eintrag für durchgestrichenen Text und dem Kommentar zur A4-Entscheidung; Zeilen 403-416: der Roundtrip-Testfall, dessen Name den Verlust als vorbestehenden Fehler beschreibt)
    - .planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md (Abschnitt „Netz-Freeze": die Änderungsregel und das vorgeschriebene Format für eine begründete Abweichung — Migrationsgruppe beziehungsweise Anlass, alter Erwartungswert, neuer Erwartungswert, Begründung, warum das kein Beweis-Leck ist; außerdem die A4-Entscheidung mit der Phase-10-Vormerkung)
    - .planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-CONTEXT.md (D-04a: Netzänderungen sind begründungspflichtig)
  </read_first>
  <action>
    (1) Whitelist-Fix, synchron in beiden Dateien: ergänze in der Liste der erlaubten Tags von `utils/basic.js` UND `utils/testable-utils.js` den langen Namen der Durchstreich-Auszeichnung neben dem bereits vorhandenen kurzen. Rein additive Datenänderung, keine Logikänderung, keine weitere Whitelist-Erweiterung — insbesondere wird die Erlaubnis für Klassen- und Stil-Attribute NICHT angefasst (D-08: bewusst akzeptiertes Risiko, wird in Plan 10-05 dokumentiert). Beide Dateien im selben Commit ändern; der Paritätstest aus Task 2 muss danach weiterhin grün sein — er ist der strukturelle Nachweis, dass die Änderung in beiden Kopien gelandet ist.

    (2) Netz-Anpassung: in `tests/e2e/features/editor-formatting.spec.js` ändert sich damit genau ein Erwartungswert — der Roundtrip-Wert für durchgestrichenen Text ist nicht mehr der nackte Text, sondern das erhaltene Markup, also derselbe Wert wie der Erwartungswert direkt nach der Formatierung. Passe den Erwartungswert in der Tabelle an, aktualisiere den zugehörigen Kommentar (er beschreibt heute den Verlust als eingefrorenen Fehler) und benenne den Testfall um: er belegt künftig, dass die Auszeichnung den Zyklus übersteht. Die Zusatz-Assertion, dass der Text selbst erhalten bleibt, bleibt bestehen. KEINE weitere Assertion dieser Datei und keine Assertion der drei anderen Netz-Dateien wird angefasst.

    (3) Begründung ins Protokoll: ergänze im Abschnitt „Netz-Freeze" von `09-BASELINE.md` einen datierten Eintrag im dort vorgeschriebenen Format — Anlass (Phase 10, D-06, Datenintegritäts-Item mit ausdrücklicher Phase-10-Vormerkung aus der A4-Entscheidung), betroffene Datei und Testfall, alter Erwartungswert, neuer Erwartungswert, Begründung, warum das kein Beweis-Leck ist (die Änderung schwächt keine Assertion ab, sondern verschärft sie: gefordert wird jetzt Erhalt statt Verlust; der zugrunde liegende Produktionsfehler wurde behoben, nicht der Test angepasst, um einen Fehlschlag zu verdecken).

    Baue das Bundle und führe die betroffene Netz-Datei, danach das vollständige Editor-Netz, die volle E2E-Suite und die Jest-Suite aus.
  </action>
  <verify>
    <automated>npx jest tests/unit/sanitizer-parity.test.js && python build.py && npx playwright test tests/e2e/features/editor-formatting.spec.js tests/e2e/features/editor-floating.spec.js tests/e2e/features/editor-insert.spec.js tests/e2e/features/editor-smoke.spec.js && npx playwright test && npx jest</automated>
  </verify>
  <acceptance_criteria>
    - Die Liste der erlaubten Tags enthält in `utils/basic.js` und in `utils/testable-utils.js` denselben neuen Eintrag; `npx jest tests/unit/sanitizer-parity.test.js` endet mit Exit-Code 0
    - In `tests/e2e/features/editor-formatting.spec.js` ist genau ein Erwartungswert geändert (der Roundtrip-Wert für durchgestrichenen Text); `git diff` dieser Datei zeigt keine weitere geänderte Assertion
    - `git diff --name-only` listet aus dem eingefrorenen Netz ausschließlich `tests/e2e/features/editor-formatting.spec.js`
    - `09-BASELINE.md` enthält im Abschnitt „Netz-Freeze" einen datierten Eintrag mit Anlass, Datei, Testfall, altem und neuem Erwartungswert und Begründung
    - Verhalten: ein im Editor durchgestrichen formatierter Text steht nach Speichern und Neuladen weiterhin ausgezeichnet im Editor — nachgewiesen durch den umbenannten Roundtrip-Testfall
    - Das vollständige Editor-Netz, `npx playwright test` und `npx jest` enden mit Exit-Code 0
  </acceptance_criteria>
  <done>Formatierungen des Spielleiters gehen beim Speichern nicht mehr verloren, und die einzige Netzänderung dieser Phase ist aktenkundig begründet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Produktions-Sanitizer ↔ Test-Zwilling | Zwei Kopien derselben Sicherheitslogik; eine Abweichung macht jeden Testnachweis wertlos |
| Testergebnis → Sicherheitsaussage | Ein grüner Lauf wird zur Behauptung „der Sanitizer schützt" |
| Whitelist ↔ gespeicherte Nutzerdaten | Eine Erweiterung erzeugt neues Markup in Nutzerdaten; eine spätere Verengung würde es zerstören |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-11 | Repudiation | Sicherheitstests gegen `utils/testable-utils.js` statt gegen den Produktionscode | high | mitigate | Neuer vm-basierter Testblock lädt `utils/basic.js` aus dem Quelltext; Definiertheitsprüfung verhindert stillen Durchlauf |
| T-10-12 | Tampering | Drift zwischen den beiden Sanitizer-Kopien | high | mitigate | Paritätstest über gemeinsames Vektor-Set, grün vor der Whitelist-Änderung, danach als Zaun wirksam |
| T-10-13 | Tampering | Erweiterung der erlaubten Tag-Liste | medium | mitigate | Genau ein additiver Eintrag für eine reine Textauszeichnung ohne Attribute und ohne Skript-Fähigkeit; Attribut-Erlaubnis unverändert; Vektor-Katalog läuft nach der Änderung erneut |
| T-10-14 | Repudiation | Anpassung einer eingefrorenen Netz-Assertion | high | mitigate | Nur ein Erwartungswert, in Richtung strengerer Erwartung; schriftliche Begründung im Netz-Freeze-Protokoll; Umfang der Änderung über `git diff` prüfbar |
| T-10-SC | Tampering | npm/pip/cargo-Installationen | low | accept | Diese Phase installiert kein Paket (RESEARCH §Package Legitimacy Audit: nicht anwendbar) |
</threat_model>

## Artifacts this phase produces

Siehe die vollständige Liste in `10-01-PLAN.md`. Von DIESEM Plan neu erzeugt:

- `tests/unit/sanitizer-parity.test.js` — neue Testdatei
- Neuer vm-basierter Testblock in `tests/unit/security.test.js`
- Neuer Eintrag in der Liste der erlaubten Tags in `utils/basic.js` und `utils/testable-utils.js`
- Datierter Eintrag im Abschnitt „Netz-Freeze" von `09-BASELINE.md`

<verification>
- Vektor-Katalog läuft gegen den echten Produktionsquelltext und ist grün
- Paritätstest grün VOR der Whitelist-Änderung (protokolliert) und grün danach
- Genau eine geänderte Netz-Assertion, schriftlich begründet
- Volles Editor-Netz, `npx playwright test` und `npx jest` grün
</verification>

<success_criteria>
- Sicherheitsnachweise laufen gegen den Code, den die Anwendung ausführt (D-14)
- Whitelist-Änderungen können strukturell nicht mehr in nur einer Kopie landen (D-14)
- Durchgestrichener Text überlebt den Speichern-/Neuladen-Zyklus (D-06)
</success_criteria>

<output>
Create `.planning/phases/10-security-h-rtung/10-03-SUMMARY.md` when done
</output>
