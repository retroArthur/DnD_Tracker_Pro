---
phase: 10-security-h-rtung
plan: 04
type: execute
wave: 4
depends_on: ["10-03"]
files_modified:
  - ui/editors/rich-text.js
  - tests/e2e/features/editor-insert.spec.js
  - .planning/WINDOWS.md
autonomous: true
requirements: [SEC-01]

must_haves:
  truths:
    - "Wird Tabellen-Markup mit einem Ereignis-Attribut in einen Editor eingefügt, landet das Attribut nicht im Editor-DOM und kein Handler feuert (D-05, Broken-Windows #1)"
    - "Der Tabelleninhalt selbst bleibt beim Einfügen erhalten — Zellen, Zeilen und Text kommen unverändert an; entfernt wird ausschließlich das Ereignis-Attribut"
    - "Der Zauber-Speicherpfad behandelt das Beschreibungsfeld genauso wie das direkt benachbarte Notizfeld: beide werden vor dem Speichern bereinigt (RESEARCH Pitfall 3)"
    - "Der Broken-Windows-Eintrag Nummer 1 steht nach dem Fix auf erledigt — der Auslieferungspfad ist damit nicht mehr blockiert (D-05)"
    - "Der bestehende Sicherheits-Regressionstest T-09-01 bleibt in seinen Assertions unverändert; angepasst wird ausschließlich sein erklärender Kommentar, der bisher den ungefixten Zustand beschreibt (D-16)"
  artifacts:
    - "Zusätzliche Regex-Bereinigung für Ereignis-Attribute im Tabellenzweig von handleEditorPaste() (ui/editors/rich-text.js)"
    - "Bereinigung des Beschreibungsfeldes in saveSpell() (ui/editors/rich-text.js)"
    - "Neuer Sicherheits-Testfall für den Tabellen-Einfügepfad in tests/e2e/features/editor-insert.spec.js"
    - "Aktualisierter Broken-Windows-Ledger-Eintrag Nummer 1 in .planning/WINDOWS.md"
  key_links:
    - "Zwischenablage → handleEditorPaste() Tabellenzweig → Regex-Kette → insertHtmlAtSelection() → Editor-DOM — die Bereinigung für Ereignis-Attribute muss VOR der bestehenden Attributlisten-Bereinigung greifen"
    - "saveSpell() Beschreibungsfeld → D.spells[].description → LocalStorage — der Speicherpfad wird damit konsistent zum benachbarten Notizfeld und zum Wiki-Speicherpfad"
    - ".planning/WINDOWS.md Eintrag Nummer 1 → gsd-tools windows fixed 1 → Auslieferungspfad wieder frei"
  prohibitions:
    - statement: "Der Einfüge-Handler darf NICHT auf den DOMParser-basierten Sanitizer umgebaut werden — der Auftrag ist die minimale Ergänzung der Ereignis-Attribut-Bereinigung; ein Umbau des Handlers wäre eine Architekturänderung außerhalb des Phasenauftrags und würde das eingefrorene Editor-Regressionsnetz in großer Breite berühren (D-05, RESEARCH Pitfall 6)"
      status: flagged-unverified
      verification: judgment
    - statement: "Eine Assertion des eingefrorenen Phase-9-Regressionsnetzes darf NICHT abgeschwächt werden, um eine Änderung grün zu bekommen — in dieser Datei sind ausschließlich ein zusätzlicher Testfall und die Aktualisierung eines erklärenden Kommentars zulässig; bestehende Erwartungswerte bleiben unangetastet"
      status: flagged-unverified
      verification: judgment
---

<objective>
Schließe den zweiten empirisch bestätigten Ausführungspfad: eingefügtes Tabellen-Markup mit Ereignis-Attributen überlebt heute bis in den Editor-DOM und feuert. Der Tabellenzweig des Einfüge-Handlers bereinigt künftig auch Ereignis-Attribute, der Zauber-Speicherpfad wird an seinen eigenen Nachbarn angeglichen, und der Broken-Windows-Eintrag wird geschlossen.

Purpose: Phase 9 hat diesen Fund empirisch belegt, aber bewusst nicht behoben, weil kein Produktionscode geändert werden durfte. Er blockiert seither den Auslieferungspfad. Diese Phase ist der vorgesehene Ort für den Fix.
Output: Ereignis-Attribut-Bereinigung im Tabellenzweig, bereinigter Zauber-Speicherpfad, neuer E2E-Sicherheitstest, geschlossener Ledger-Eintrag.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/WINDOWS.md
@.planning/phases/10-security-h-rtung/10-CONTEXT.md
@.planning/phases/10-security-h-rtung/10-RESEARCH.md
@.planning/phases/10-security-h-rtung/10-PATTERNS.md
@.planning/phases/10-security-h-rtung/10-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sicherheits-Testfall für den Tabellen-Einfügepfad anlegen (rot gegen den Ist-Zustand)</name>
  <files>tests/e2e/features/editor-insert.spec.js</files>
  <read_first>
    - tests/e2e/features/editor-insert.spec.js (Zeilen 124-146: der Hilfsaufruf, der ein Einfüge-Ereignis mit Zwischenablage-Daten erzeugt; Zeilen 148-168: der bestehende Tabellen-Einfügetest mit dem empirisch erhobenen, doppelt verschachtelten Erwartungswert — dieses Verschachtelungsverhalten ist ein eingefrorener vorbestehender Befund und bleibt unangetastet; Zeilen 200-285: der bestehende Sicherheits-Regressionstest T-09-01 samt seines erklärenden Kommentarblocks, der den ungefixten Tabellenzweig beschreibt)
    - ui/editors/rich-text.js (Zeilen 952-994 `handleEditorPaste()`: die Bedingung für den Tabellenzweig Zeile 958, der Regex-Ausschnitt Zeile 959, die Bereinigungskette ab Zeile 961 mit der Attributlisten-Regex Zeile 962-965, der Einfügeaufruf Zeile 990)
    - .planning/WINDOWS.md (Eintrag Nummer 1 — der wörtliche Befund mit Datei und Zeilenangabe)
    - .planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md (Netz-Freeze-Regel: was an diesen Dateien geändert werden darf und was begründungspflichtig ist)
  </read_first>
  <action>
    Ergänze in `tests/e2e/features/editor-insert.spec.js` einen NEUEN Testfall direkt neben dem bestehenden Sicherheits-Regressionstest, in derselben Gruppe für Zwischenablage-Vorgänge. Bestehende Erwartungswerte werden nicht angefasst.

    Der neue Fall spiegelt den bestehenden Sicherheitstest, verwendet aber eine Nutzlast MIT Tabellen-Wrapper — genau die Konstellation, die laut Ledger-Eintrag den verwundbaren Zweig trifft: eine Tabelle mit einer Zeile und zwei Zellen, in deren erster Zelle ein Bild mit einem Fehler-Ereignis-Attribut steht, das ein Marker-Flag am Fenster setzt; die zweite Zelle enthält reinen Text. Als Klartext-Anteil der Zwischenablage genügt eine tabulatorgetrennte Zeile.

    Assertionen, alle nach dem Muster des bestehenden Sicherheitstests:
    - Direkt nach dem Einfügen: im Editor existiert kein Element mit einem Attribut, dessen Name mit `on` beginnt; es existiert kein Bild-Element; das Marker-Flag am Fenster ist undefiniert; die gesammelten Seitenfehler sind leer.
    - Erhaltungs-Gegenprobe: die Tabelle ist angekommen — es gibt mindestens ein Tabellenelement und der Text der zweiten Zelle ist im Editor sichtbar. Diese Assertion hält fest, dass der Fix nur das Ereignis-Attribut entfernt und nicht die Tabelle.
    - Nach Speichern, Neuladen und Wiederöffnen des Eintrags gelten dieselben Prüfungen (Hilfsfunktion für Speichern und Wiederöffnen ist in der Datei bereits vorhanden).

    Passe im Kommentarblock des bestehenden Sicherheits-Regressionstests den Hinweis-Absatz an, der den Tabellenzweig heute als offenen, nicht behobenen Befund beschreibt: er verweist künftig auf den neuen Testfall und darauf, dass der Befund in Phase 10 behoben wurde. Die Assertions dieses Tests bleiben Zeichen für Zeichen unverändert — geändert wird ausschließlich erklärender Text. Halte diese Kommentaränderung in der SUMMARY als begründete Netz-Berührung fest (D-16).

    Baue das Bundle und führe die Spec-Datei aus. Der neue Fall MUSS jetzt fehlschlagen. Halte die Ausgabe für die SUMMARY fest.
  </action>
  <verify>
    <automated>python build.py && ! npx playwright test tests/e2e/features/editor-insert.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/features/editor-insert.spec.js` enthält einen neuen Testfall, dessen Nutzlast ein Tabellenelement mit einem Bild samt Fehler-Ereignis-Attribut in einer Zelle einfügt
    - Der neue Fall prüft sowohl die Abwesenheit von Ereignis-Attributen und Bild-Element als auch den Erhalt von Tabelle und Zelltext
    - Der neue Fall wiederholt seine Prüfungen nach Speichern, Neuladen und Wiederöffnen
    - `git diff tests/e2e/features/editor-insert.spec.js` zeigt außer dem neuen Testfall ausschließlich Änderungen an Kommentartext — keine geänderte Assertion und keinen geänderten Erwartungswert
    - Verhalten: der Lauf gegen das ungepatchte Bundle schlägt im neuen Fall fehl (das Prüfkommando dieser Aufgabe kehrt den Rückgabewert bewusst um und ist nur bei einem roten Lauf grün); die Fehlerausgabe ist in der SUMMARY zitiert
  </acceptance_criteria>
  <done>Der seit Phase 9 nur beschriebene Fund ist jetzt ein laufender, roter Test.</done>
</task>

<task type="auto">
  <name>Task 2: Ereignis-Attribute im Tabellenzweig entfernen, Zauber-Speicherpfad angleichen, Ledger schließen</name>
  <files>ui/editors/rich-text.js, .planning/WINDOWS.md</files>
  <read_first>
    - ui/editors/rich-text.js (Zeilen 952-994 `handleEditorPaste()` — die Bereinigungskette, an deren Anfang die neue Bereinigung gehört; Zeilen 1626-1697 `saveSpell()` — Zeile 1663 liest das Beschreibungsfeld aus, Zeile 1678 speichert es unbereinigt, Zeile 1679 zeigt am Notizfeld das korrekte Muster)
    - utils/basic.js (Zeilen 61-67 — die zwei Regex-Muster, mit denen der Sanitizer Ereignis-Attribute vorbereinigt: eines für Werte in Anführungszeichen, eines für Werte ohne; genau dieses Paar wird übernommen, statt eine dritte Variante zu erfinden)
    - .planning/WINDOWS.md (Aufbau des Ledgers: Tabellenzeile und der spiegelbildliche JSON-Block; der Status wird über das Werkzeug gesetzt, nicht von Hand)
    - .planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-01-PLAN.md (die Vormerkung des Zauber-Speicherfundes für Phase 10)
    - CLAUDE.md (Abschnitt zur Ablösung der veralteten Editier-Kommando-Schnittstelle: für Editor-Arbeit die vorhandenen Selection- und Range-Hilfsfunktionen verwenden, die alte Schnittstelle nicht wiederbeleben)
  </read_first>
  <action>
    (1) Fix D-05 — Ereignis-Attribute im Tabellenzweig: Setze in `handleEditorPaste()` an den ANFANG der Bereinigungskette des Tabellenzweigs — also vor die bestehende Bereinigung der harmlosen Attributliste — dasselbe Regex-Paar, das der Sanitizer in `utils/basic.js` zur Vorbereinigung verwendet: eines für Attributwerte in Anführungszeichen, eines für Attributwerte ohne Anführungszeichen. Beide mit den Kennzeichen für globale und Groß-/Kleinschreibung-unabhängige Suche und mit führendem Leerraum im Muster, damit das Attribut samt trennendem Leerzeichen verschwindet. Keine eigene, abweichende Erkennungslogik erfinden — die Muster werden aus dem Sanitizer übernommen, damit im Projekt nur eine Definition davon existiert, was ein Ereignis-Attribut ist.

    Der Umfang endet hier: der Handler wird NICHT auf den DOMParser-Ansatz umgebaut, es werden keine weiteren Attribute ergänzt, und die übrigen Kettenglieder bleiben unverändert. Die verbleibende grundsätzliche Sprödigkeit einer regexbasierten Markup-Bereinigung wird in Plan 10-05 als bewusst akzeptiertes Restrisiko dokumentiert.

    (2) Fix RESEARCH Pitfall 3 — Zauber-Speicherpfad: In `saveSpell()` wird das Beschreibungsfeld beim Zusammenbauen des Zauber-Objekts durch dieselbe Bereinigung geschickt wie das unmittelbar darunter stehende Notizfeld. Nur diese eine Zuweisung ändert sich; das Auslesen des Feldinhalts bleibt, wie es ist.

    (3) Ledger schließen: Setze den Broken-Windows-Eintrag Nummer 1 über das Werkzeug auf erledigt — `node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows fixed 1` aus dem Projektwurzelverzeichnis. Prüfe danach, dass die Kopfdaten der Datei den offenen Zähler auf null führen und der Eintrag ein Auflösungsdatum trägt. Den Ledger nicht von Hand bearbeiten — Tabelle und JSON-Block müssen synchron bleiben.

    Baue das Bundle und führe aus: die betroffene Spec-Datei (der neue Fall ist jetzt grün), danach das vollständige Editor-Netz aus vier Dateien, die volle E2E-Suite und die Jest-Suite. Prüfe im selben Zug manuell nichts, was ein Test abdeckt — der Nachweis läuft vollständig über die Suite.
  </action>
  <verify>
    <automated>python build.py && npx playwright test tests/e2e/features/editor-insert.spec.js && npx playwright test tests/e2e/features/editor-formatting.spec.js tests/e2e/features/editor-floating.spec.js tests/e2e/features/editor-smoke.spec.js && npx playwright test && npx jest && node -e "const s=require('fs').readFileSync('.planning/WINDOWS.md','utf8');process.exit(/open_count:\s*0/.test(s)?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - Die Bereinigungskette des Tabellenzweigs in `handleEditorPaste()` beginnt mit zwei Ersetzungen für Ereignis-Attribute (eine für Werte in Anführungszeichen, eine für Werte ohne), die dem Regex-Paar aus `utils/basic.js` entsprechen
    - Die übrigen Kettenglieder des Tabellenzweigs sind unverändert; der Handler verwendet weiterhin die vorhandenen Einfüge-Hilfsfunktionen und keine wiederbelebte veraltete Editier-Schnittstelle
    - In `saveSpell()` durchläuft das Beschreibungsfeld dieselbe Bereinigung wie das Notizfeld
    - `.planning/WINDOWS.md` führt in den Kopfdaten `open_count: 0`, und Eintrag Nummer 1 hat den Status erledigt samt Auflösungszeitstempel — gesetzt über das Werkzeug, Tabelle und JSON-Block stimmen überein
    - Verhalten: der in Task 1 angelegte Testfall ist grün, inklusive der Erhaltungs-Gegenprobe für Tabelle und Zelltext
    - Das vollständige Editor-Netz aus vier Dateien, `npx playwright test` und `npx jest` enden mit Exit-Code 0
  </acceptance_criteria>
  <done>Der zweite bestätigte Ausführungspfad ist geschlossen, der Zauber-Speicherpfad ist mit seinem Nachbarn konsistent, und der Auslieferungspfad ist nicht mehr blockiert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Zwischenablage → Editor-DOM | Fremdes Markup aus einer beliebigen Quelle wird in den Bearbeitungsbereich eingefügt |
| Editor-DOM → gespeicherte Entität | Der Speicherpfad entscheidet, was dauerhaft im Datenbestand landet |
| Broken-Windows-Ledger → Auslieferungspfad | Ein offener Eintrag blockiert die Auslieferung; sein Status ist eine Freigabeaussage |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-15 | Tampering / Elevation of Privilege | Tabellenzweig von `handleEditorPaste()` (`rich-text.js:961-989`) | high | mitigate | Übernahme des Ereignis-Attribut-Regex-Paars aus `utils/basic.js` an den Anfang der Bereinigungskette; neuer E2E-Testfall mit Tabellen-Nutzlast, vor dem Fix rot |
| T-10-16 | Tampering | `saveSpell()` Beschreibungsfeld (`rich-text.js:1663/1678`) | medium | mitigate | Bereinigung analog zum benachbarten Notizfeld; damit ist der In-App-Speicherpfad der Zauber konsistent zum Wiki-Speicherpfad |
| T-10-17 | Tampering | Regexbasierte Markup-Bereinigung generell (verschachtelte Anführungszeichen, Entitäten in Attributwerten) | medium | accept | Bewusst minimalinvasiver Auftrag (D-05); ein Umbau auf den DOMParser-Ansatz läge außerhalb des Phasenauftrags. Restrisiko wird in Plan 10-05 in SECURITY.md dokumentiert |
| T-10-18 | Repudiation | Statuswechsel des Ledger-Eintrags | medium | mitigate | Statuswechsel ausschließlich über das Werkzeug, damit Tabelle und JSON-Block synchron bleiben; Kopfdaten-Zähler wird automatisiert geprüft |
| T-10-SC | Tampering | npm/pip/cargo-Installationen | low | accept | Diese Phase installiert kein Paket (RESEARCH §Package Legitimacy Audit: nicht anwendbar) |
</threat_model>

## Artifacts this phase produces

Siehe die vollständige Liste in `10-01-PLAN.md`. Von DIESEM Plan neu erzeugt:

- Zwei zusätzliche Ersetzungsschritte im Tabellenzweig von `handleEditorPaste()` (`ui/editors/rich-text.js`)
- Bereinigung des Beschreibungsfeldes in `saveSpell()` (`ui/editors/rich-text.js`)
- Neuer Sicherheits-Testfall in `tests/e2e/features/editor-insert.spec.js`
- Statuswechsel des Broken-Windows-Eintrags Nummer 1 in `.planning/WINDOWS.md`

<verification>
- Neuer Testfall vor dem Fix rot, danach grün — beides protokolliert
- Tabelle und Zelltext überleben das Einfügen (Erhaltungs-Gegenprobe grün)
- `git diff` der Netz-Datei zeigt außer dem neuen Fall nur Kommentaränderungen
- `.planning/WINDOWS.md` führt den offenen Zähler auf null
- Volles Editor-Netz, `npx playwright test` und `npx jest` grün
</verification>

<success_criteria>
- Broken-Windows-Eintrag Nummer 1 ist behoben und geschlossen (D-05)
- Der Zauber-Speicherpfad ist mit seinem unmittelbaren Nachbarn konsistent (RESEARCH Pitfall 3)
- Das eingefrorene Netz wurde nur um einen Fall erweitert, keine Assertion abgeschwächt (D-16)
</success_criteria>

<output>
Create `.planning/phases/10-security-h-rtung/10-04-SUMMARY.md` when done
</output>
