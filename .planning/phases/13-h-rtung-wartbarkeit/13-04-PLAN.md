---
phase: 13-h-rtung-wartbarkeit
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/backups.test.js
  - tests/unit/tab-registry.test.js
  - systems/backups.js
  - systems/tab-registry.js
  - systems/tab-registry.md
  - CLAUDE.md
  - tools/debug.js
  - features/soundboard/soundboard-player.js
autonomous: true
requirements: [MAINT-05, MAINT-02]

estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "`initPerformanceMonitoring()` startet bei mehrfachem Aufruf kein zweites Interval mehr — dieselbe Guard-Form wie `startAutoBackup()` (`systems/backups.js:306-308`): Modulvariable, `clearInterval` vor dem Neustart."
    - "Die Tab-Registry referenziert Renderer, `init`- und `cleanup`-Funktionen nicht mehr allein per Zeichenkette; jeder Eintrag trägt eine verzögerte Funktionsreferenz, deren Bezeichner im Quelltext statisch sichtbar ist."
    - "Ein umbenannter oder entfernter Registry-Eintrag bricht `npx jest tests/unit/tab-registry.test.js` — nicht mehr nur eine `DEBUG_MODE`-Warnung zur Laufzeit."
    - "`features/soundboard/soundboard-player.js` Zeile 145 überschattet das globale Datenobjekt nicht mehr mit einer Zahl; die 169 Vorkommen von `const D = window.D` im Projekt bleiben ausdrücklich unangetastet (etabliertes Zugriffsmuster, kein Defekt)."
    - "Die zwei toten `mindmap`-Schreib-Seeds (`systems/backups.js:232` mit `edges` statt `connections`, `tools/debug.js:917`) sind entfernt; kein Codepfad schreibt den abgeschafften Schlüssel neu."
    - "MAINT-05 · Randfall `empty`: `initPerformanceMonitoring()` läuft ohne Ausnahme, wenn `window.D` keine der gezählten Sammlungen enthält — `updateEntityCounts()` liefert dann Nullen."
    - "MAINT-05 · Randfall `encoding`: Tab-Identität wird als exakte, zeichengenaue ASCII-Zeichenkette gegen das `data-view`-Attribut verglichen; es gibt keine Normalisierung, keine Groß-/Kleinschreibungstoleranz."
    - "MAINT-02 — offene Kanten-Annahme (Probe-Kategorie `unclassified`, bleibt `unresolved`): Die deterministische Kantensonde konnte MAINT-02 keiner Randfallklasse zuordnen. Planer-Annahme: die relevante Kante ist der Wiederherstellungspfad — ein Backup, das den `mindmap`-Schlüssel noch enthält, muss nach dem Entfernen des Seeds weiterhin fehlerfrei einspielbar sein (der Smart-Strip in `systems/spellslots/version-migration.js:53-59` erledigt das bereits). Nicht automatisch aufgelöst; als Annahme markiert."
  artifacts:
    - tests/unit/backups.test.js
    - tests/unit/tab-registry.test.js
    - systems/backups.js
    - systems/tab-registry.js
    - tools/debug.js
    - features/soundboard/soundboard-player.js
  key_links:
    - "`systems/backups.js`: `backupInterval`-Guard in `startAutoBackup()` ist die Vorlage für die neue `perfMonitoringInterval`-Guard in `initPerformanceMonitoring()` — dieselbe Datei, dasselbe Modul"
    - "`systems/navigation`/`switchView()` → `renderTabContent(tabName)` → Registry-Eintrag → aufgelöste Renderfunktion"
    - "`systems/spellslots/version-migration.js:53-59` (Smart-Strip für leere `mindmap`-Seeds) ist der Grund, warum das Entfernen der Seeds den Restore-Pfad nicht bricht"
---

<objective>
Vier fragile bzw. tote Stellen an einem Zug: der ungeschützte `setInterval` in
`initPerformanceMonitoring()`, die reine String-Referenzierung in der Tab-Registry (bricht heute bei
Umbenennung nur mit einer `DEBUG_MODE`-Warnung), die `const D`-Überschattung im Soundboard-Player und
die zwei toten `mindmap`-Schreib-Seeds.

Purpose: Erfolgskriterium 7 der Phase — „Tab-Registry und `initPerformanceMonitoring()` sind gegen
Umbenennung bzw. Mehrfachstart abgesichert; tote `mindmap`-Seeds, `const D`-Überschattung und das
doppelte `data-id` sind weg." Das doppelte `data-id` liegt in `features/wiki/wiki.js` und wird von
Plan 13-02 erledigt; dieser Plan deckt die übrigen vier Punkte ab.
Output: Guard-Parität in `systems/backups.js`, verzögerte Funktionsreferenzen in der Tab-Registry, zwei
neue Unit-Testdateien, drei Löschungen/Umbenennungen.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-h-rtung-wartbarkeit/13-CONTEXT.md
@.planning/phases/13-h-rtung-wartbarkeit/13-RESEARCH.md
@.planning/phases/13-h-rtung-wartbarkeit/13-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Dieser Plan erzeugt:

| Symbol / Datei | Art | Ort |
|---|---|---|
| `perfMonitoringInterval` | neue Modulvariable | `systems/backups.js` |
| `resolveTabFn(entry)` | neue Hilfsfunktion (Auflösung einer verzögerten Registry-Referenz) | `systems/tab-registry.js` |
| `tests/unit/backups.test.js` | neue Jest-Testdatei | `tests/unit/` |
| `tests/unit/tab-registry.test.js` | neue Jest-Testdatei | `tests/unit/` |

Kein neues Quellmodul, kein neuer `loader.js`-`MODULES`-Eintrag.

**Bekannter Nebeneffekt, kein Regressionsbefund:** die verzögerten Funktionsreferenzen machen rund 40
Bezeichner in `systems/tab-registry.js` statisch sichtbar, die dort bisher nur als Zeichenkette
standen. ESLint kennt diese Projekt-Globals nicht und meldet sie als `no-undef` — Regelstufe `warn`,
kein Fehler. Ausgangsstand vor dieser Phase: 1795 Warnungen und **1 vorbestehender Fehler**
(`systems/avatars.js:17`, `no-control-regex` — unabhängig von Phase 13, Zuständigkeit `TEST-05`/Phase 14).
Abnahme deshalb: die Fehlerzahl von `npx eslint .` bleibt bei 1, sie steigt nicht.

<tasks>

<task type="tracer">
  <name>Task 1: Guard-Parität für `initPerformanceMonitoring()` end-to-end</name>
  <files>systems/backups.js, tests/unit/backups.test.js</files>
  <read_first>
    - `systems/backups.js` Zeilen 300–335 (`backupInterval` + `startAutoBackup()` als korrekte Vorlage
      auf 306–308; `initPerformanceMonitoring()` auf 321–330 ohne jede Guard, direkt darunter
      `updateEntityCounts()`)
    - `systems/backups.js` Zeilen 335–380 (`updateEntityCounts()` und `checkPerformance()` — was beim
      Guard-Umbau nicht angefasst werden darf)
    - `tests/unit/file-backup.test.js` (Stilvorlage für einen Service-Test in derselben `systems/`-Familie)
    - `tests/unit/file-backup-idb.test.js` Zeilen 1–75 (Muster: echtes Quellmodul per `vm` in eine
      frische Sandbox laden)
    - `CLAUDE.md` §„Critical Patterns from Bug History" Punkt 5 („setInterval/setTimeout: Always Clean Up")
  </read_first>
  <action>
    Lege `tests/unit/backups.test.js` an und schreibe die Fälle ZUERST, rot gegen den heutigen Stand:
    (a) zweimaliger Aufruf von `initPerformanceMonitoring()` hinterlässt genau ein laufendes Interval,
    (b) beim zweiten Aufruf wird `clearInterval` mit der Handle des ersten aufgerufen,
    (c) `startAutoBackup()` verhält sich unverändert (Kontrollfall, damit die Vorlage nicht mitverändert
    wird), (d) `initPerformanceMonitoring()` wirft nicht, wenn `window.D` ein leeres Objekt ist
    (Randfall `empty`).
    Lade `systems/backups.js` per `vm` in eine Sandbox nach dem Muster aus
    `tests/unit/file-backup-idb.test.js`; ersetze `setInterval`/`clearInterval`/`setTimeout` in der
    Sandbox durch `jest.fn()`-Doubles, die Handles vergeben und mitzählen, und stubbe die weiteren
    Abhängigkeiten des Moduls (`$`, `showToast`, `StorageAPI`, `ErrorHandler`, `D`) minimal.
    Dann in `systems/backups.js` eine Modulvariable `perfMonitoringInterval` (Initialwert `null`)
    direkt vor `initPerformanceMonitoring()` einführen und die Funktion so umbauen, dass sie exakt die
    Form von `startAutoBackup()` bekommt: bei bereits gesetzter Handle zuerst `clearInterval` rufen,
    dann `setInterval` mit demselben 30-Sekunden-Takt und derselben Rückruf-Funktion, Handle in der
    Modulvariablen ablegen. Der einleitende `updateEntityCounts()`-Aufruf und der abschließende
    `checkPerformance()`-Aufruf bleiben unverändert an ihren Positionen — nur die Interval-Zeile
    ändert sich.
  </action>
  <verify>
    <automated>npx jest tests/unit/backups.test.js</automated>
    <fails_when>Exit-Code ungleich 0, oder die Zusammenfassung meldet „0 passed", oder ein Testfall
    meldet nach zwei Aufrufen mehr als ein laufendes Interval</fails_when>
    <automated>node --check systems/backups.js</automated>
    <fails_when>Exit-Code ungleich 0 oder eine SyntaxError-Meldung auf stderr</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/unit/backups.test.js` existiert und enthält mindestens vier `test(`/`it(`-Fälle
    - `systems/backups.js` enthält `let perfMonitoringInterval = null;`
    - `systems/backups.js` enthält innerhalb `initPerformanceMonitoring()` einen `clearInterval(`-Aufruf
      vor dem `setInterval(`-Aufruf
    - `grep -c 'clearInterval' systems/backups.js` liefert mindestens `2` (Vorlage + neue Stelle)
    - Verhalten: zwei aufeinanderfolgende Aufrufe von `initPerformanceMonitoring()` erzeugen genau ein
      aktives Interval-Handle
    - `npx jest tests/unit/backups.test.js` endet mit Exit-Code 0
  </acceptance_criteria>
  <done>Mehrfachstart erzeugt kein zweites Interval mehr, belegt durch einen zuvor roten Testfall.</done>
</task>

<task type="auto">
  <name>Task 2: Tab-Registry auf verzögerte Funktionsreferenzen umstellen</name>
  <files>systems/tab-registry.js, systems/tab-registry.md, tests/unit/tab-registry.test.js, CLAUDE.md</files>
  <read_first>
    - `systems/tab-registry.js` vollständig (213 Zeilen): das `TAB_RENDER_REGISTRY`-Literal mit den
      Einträgen `dashboard`, `party`, `npcs`, `locations`, `quests`, `encounter`, `initiative`, `loot`,
      `shops`, `spells`, `notes`, `wiki`, `links`, `dice`, `timers`, `data`, `dmscreen`, `bestiary`,
      `sessionprep`, `kalender`, `reise`, `fraktionen`, `soundboard`, `dicestats`; danach
      `renderTabContent()` (Auflösung über `window[renderFn]`) und `validateTabRegistry()`
    - `loader.js` `MODULES`-Array (die Ladereihenfolge: `systems/tab-registry.js` steht VOR den
      Feature-Render-Modulen — deshalb darf das Registry-Literal die Bezeichner nicht bei seiner
      eigenen Auswertung auflösen)
    - `systems/tab-registry.md` (die vorhandene Dokumentation des Registry-Musters — sie beschreibt die
      String-Form und muss mitgezogen werden)
    - `CLAUDE.md` §„Tab Navigation Architecture (Tab Registry Pattern)" — dokumentiert die String-Form
      als Projektmuster; der Abschnitt beschreibt danach die neue Form
  </read_first>
  <action>
    Ersetze in `systems/tab-registry.js` jede Zeichenketten-Referenz durch eine **verzögerte**
    Funktionsreferenz: statt des Namens als Zeichenkette steht ein Pfeilausdruck ohne Parameter, dessen
    Rumpf den Bezeichner nennt und ihn zurückgibt (nicht aufruft). Damit wird der Bezeichner im
    Quelltext statisch sichtbar — Grep, ESLint und Suchwerkzeuge finden ihn —, aber er wird erst
    aufgelöst, wenn die Registry benutzt wird, also nachdem alle Module geladen sind. Das ist zwingend:
    im Loader-Modus (`index.html` + `loader.js`) ist jedes Modul ein eigenes `<script>`, und
    `systems/tab-registry.js` läuft vor allen Feature-Modulen. Eine direkte Referenz im Literal würde
    dort beim Laden einen `ReferenceError` werfen.
    Das betrifft alle drei Felder: `renders` (Array von Pfeilausdrücken), `init` und `cleanup` (je ein
    Pfeilausdruck oder weiterhin `null`).
    Führe eine Hilfsfunktion `resolveTabFn(entry)` ein, die einen solchen Eintrag in einem `try`/`catch`
    auswertet und entweder die Funktion oder `null` zurückgibt. `renderTabContent()` und
    `validateTabRegistry()` benutzen ausschließlich diese Hilfsfunktion statt `window[name]`.
    `validateTabRegistry()` löst damit auf, ohne zu rendern.
    Wichtig für die Diagnose: `resolveTabFn()` muss den Fehlerfall unterscheidbar melden — ein Eintrag,
    dessen Bezeichner nicht existiert, wirft beim Auswerten und wird gefangen; der Aufrufer meldet das
    mit dem Tab-Namen. Der bestehende Text der Diagnosemeldungen darf sinngemäß erhalten bleiben; die
    Ausgabe selbst wird in Plan 13-08 (MAINT-06) auf `ErrorHandler.log()` umgestellt — fasse sie hier
    nicht an, damit die Requirement-Grenze sauber bleibt.
    Ziehe `systems/tab-registry.md` und den CLAUDE.md-Abschnitt „Tab Navigation Architecture" nach, so
    dass beide die neue Form zeigen. Ein Dokument, das weiter die String-Form als Muster vorgibt, ist
    genau die irreführende Stelle, die diese Phase beseitigt.
    Lege `tests/unit/tab-registry.test.js` an mit zwei Klassen von Fällen:
    (1) Laufzeit: `systems/tab-registry.js` per `vm` in eine Sandbox laden, in der alle referenzierten
    Funktionen als `jest.fn()` definiert sind; für jeden Tab prüfen, dass `renderTabContent(tab)` genau
    die zu diesem Tab gehörenden Doubles aufruft; zusätzlich ein Fall, in dem eine Funktion FEHLT und
    der Aufruf weder wirft noch die übrigen Renderer überspringt.
    (2) Statisch: die im Registry-Literal genannten Bezeichner aus dem Quelltext extrahieren und für
    jeden prüfen, dass er als Top-Level-Deklaration (`function <name>(`) in mindestens einer der in
    `loader.js` `MODULES` gelisteten Dateien vorkommt. Dieser Fall ist der eigentliche Wächter gegen
    Umbenennung: er wird rot, sobald jemand eine Renderfunktion umbenennt, ohne die Registry
    nachzuziehen.
  </action>
  <verify>
    <automated>npx jest tests/unit/tab-registry.test.js</automated>
    <fails_when>Exit-Code ungleich 0, oder die Zusammenfassung meldet „0 passed", oder der statische
    Fall listet mindestens einen Registry-Bezeichner als nirgends deklariert auf</fails_when>
    <automated>PYTHONIOENCODING=utf-8 python build.py &amp;&amp; npx playwright test tests/e2e/tab-navigation.spec.js</automated>
    <fails_when>Exit-Code ungleich 0, oder die Build-Ausgabe enthält `[FEHLER]`/`[ABORTED]`, oder die
    Playwright-Zusammenfassung meldet mindestens ein `failed`</fails_when>
    <automated>test "$(npx eslint . 2>&amp;1 | grep -oE '[0-9]+ error' | head -1 | grep -oE '^[0-9]+')" -le 1</automated>
    <fails_when>Exit-Code ungleich 0 — dann hat die Änderung neue ESLint-**Fehler** erzeugt (Warnungen
    sind erwartet und zählen nicht)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "renders: \['" systems/tab-registry.js` liefert `0` — kein Eintrag steht mehr als
      Zeichenkette im `renders`-Array
    - `systems/tab-registry.js` enthält `function resolveTabFn(`
    - `grep -c 'window\[' systems/tab-registry.js` liefert `0` — die Auflösung läuft nicht mehr über
      dynamischen Namenszugriff
    - `tests/unit/tab-registry.test.js` existiert und enthält sowohl einen Laufzeit- als auch einen
      statischen Deklarations-Prüffall
    - `systems/tab-registry.md` und der CLAUDE.md-Abschnitt „Tab Navigation Architecture" zeigen die
      neue Form
    - Verhalten: `npx playwright test tests/e2e/tab-navigation.spec.js` läuft grün — alle 19 Tabs
      rendern beim Wechsel weiterhin
    - Die Fehlerzahl von `npx eslint .` bleibt bei höchstens 1 (der vorbestehende
      `systems/avatars.js:17`-Fehler)
  </acceptance_criteria>
  <done>Die Registry referenziert Funktionen statt Namen, und eine Umbenennung bricht einen Test statt still eine Ansicht leer zu lassen.</done>
</task>

<task type="auto">
  <name>Task 3: `const D`-Überschattung auflösen, zwei tote `mindmap`-Seeds entfernen</name>
  <files>features/soundboard/soundboard-player.js, systems/backups.js, tools/debug.js, tests/unit/backups.test.js</files>
  <read_first>
    - `features/soundboard/soundboard-player.js` Zeilen 130–165 (`const D = track.duration;` auf
      Zeile 145 — **RESEARCH-Korrektur: 145, nicht 147 wie in `13-CONTEXT.md`**; direkt darunter
      `const C = computeCrossfade(D);` und die Fade-Berechnungen, die `D` mehrfach lesen)
    - `systems/backups.js` Zeilen 215–245 (das `defaultD`-Literal in `restoreBackup()`/
      `sanitizeBackupData()`-Umgebung, `mindmap: { nodes: [], edges: [] }` auf Zeile 232 — beachte
      `edges` statt des überall sonst verwendeten `connections`)
    - `tools/debug.js` Zeilen 900–930 (`completeReset()`-Seed, `mindmap: { nodes: [], connections: [] }`
      auf Zeile 917)
    - `systems/spellslots/version-migration.js` Zeilen 50–62 (Smart-Strip: leere `mindmap`-Seeds werden
      beim Laden bereits still entfernt — der Grund, warum das Entfernen der Seeds den Restore-Pfad
      nicht bricht)
    - `systems/spellslots/import-export.js` Zeilen 440–535 (der einzige verbleibende echte
      `mindmap`-Konsument: er bietet beim Import ein JSON-Backup an und löscht den Schlüssel danach —
      dieser Pfad bleibt unangetastet)
  </read_first>
  <action>
    (a) `features/soundboard/soundboard-player.js:145`: die lokale Konstante `D`, die dort die Dauer
    eines Tracks hält, überschattet innerhalb dieser Funktion das globale Datenobjekt. Benenne sie in
    einen sprechenden Namen um (`trackDuration`) und ziehe alle Lesestellen innerhalb derselben
    Funktion mit — insbesondere den `computeCrossfade(...)`-Aufruf und die anschließenden
    Fade-in/Fade-out-Berechnungen. Ausschließlich diese eine Deklaration: die 169 Vorkommen von
    `const D = window.D` im Projekt sind das etablierte Zugriffsmuster und bleiben unangetastet
    (`13-CONTEXT.md` Deferred Ideas).
    (b) `systems/backups.js:232`: den `mindmap`-Eintrag aus dem `defaultD`-Literal ersatzlos entfernen.
    Das Mindmap-Feature ist abgeschafft; dieser Seed injizierte den toten Schlüssel bei jedem Restore
    neu, und zwar mit `edges` statt `connections` — also in einer Form, die selbst der frühere
    Konsument nicht gelesen hätte.
    (c) `tools/debug.js:917`: den `mindmap`-Eintrag aus dem `completeReset()`-Seed ersatzlos entfernen.
    Ergänze in `tests/unit/backups.test.js` einen Fall, der belegt, dass ein Backup-Objekt, das den
    `mindmap`-Schlüssel NOCH enthält, weiterhin fehlerfrei durch den Restore-Pfad läuft und danach den
    Schlüssel nicht mehr trägt (Randfall-Annahme aus `must_haves`). Falls der Restore-Pfad in der
    Sandbox zu schwer isolierbar ist, prüfe stattdessen `sanitizeBackupData()` direkt und halte die
    Einschränkung im SUMMARY fest — aber lasse den Fall nicht weg.
  </action>
  <verify>
    <automated>npx jest tests/unit/backups.test.js tests/unit/soundboard.test.js tests/unit/soundboard-loop.test.js</automated>
    <fails_when>Exit-Code ungleich 0, oder die Zusammenfassung meldet mindestens ein `failed`</fails_when>
    <automated>test "$(grep -rn "mindmap: {" --include=*.js core features systems ui utils render tools | wc -l)" -eq 0</automated>
    <fails_when>Exit-Code ungleich 0 — dann steht noch mindestens ein `mindmap`-Schreib-Seed im Quellbaum</fails_when>
    <automated>test "$(grep -c 'const D = track' features/soundboard/soundboard-player.js)" -eq 0</automated>
    <fails_when>Exit-Code ungleich 0 — dann überschattet die lokale Deklaration weiterhin das globale
    Datenobjekt</fails_when>
    <automated>npx jest &amp;&amp; PYTHONIOENCODING=utf-8 python build.py &amp;&amp; npx playwright test tests/e2e/features/soundboard.spec.js tests/e2e/tab-navigation.spec.js</automated>
    <fails_when>Exit-Code ungleich 0, oder eine der beiden Zusammenfassungen meldet mindestens ein
    `failed`, oder die Jest-Suite-Zahl liegt unter 33</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'const D = track' features/soundboard/soundboard-player.js` liefert `0`
    - `grep -c 'trackDuration' features/soundboard/soundboard-player.js` liefert mindestens `2`
    - `grep -rn "mindmap: {" --include=*.js core features systems ui utils render tools | wc -l` liefert `0`
    - `systems/spellslots/import-export.js` ist unverändert (der einzige echte `mindmap`-Konsument
      bleibt bestehen)
    - Verhalten: eine Soundboard-Szene mit Loop spielt nach der Umbenennung unverändert — belegt durch
      `tests/e2e/features/soundboard.spec.js`
    - `npx jest` läuft vollständig grün mit mindestens 33 Suiten (31 Ausgangsstand + `backups.test.js` +
      `tab-registry.test.js`)
  </acceptance_criteria>
  <done>Die Überschattung ist aufgelöst, beide toten Seeds sind weg, und keine Suite wird rot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backup-Datei (JSON, extern) → `sanitizeBackupData()` → `window.D` | Der Restore-Pfad übernimmt fremde Struktur in das laufende Datenobjekt; das `defaultD`-Literal bestimmt, welche Schlüssel dabei entstehen. |
| Tab-Wechsel (Nutzeraktion) → `renderTabContent()` → dynamisch aufgelöste Funktion | Bisher `window[name]` mit einem Namen aus der Registry; nach diesem Plan eine im Quelltext stehende Referenz. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-12 | Denial of Service | `initPerformanceMonitoring()` (`systems/backups.js:321-330`) | medium | mitigate | Modulvariable + `clearInterval` vor `setInterval`, identisch zu `startAutoBackup()`. Mehrfachaufruf (z. B. nach Kampagnenwechsel) kann keine sich stapelnden 30-Sekunden-Timer mehr erzeugen. Abgedeckt durch `tests/unit/backups.test.js`. |
| T-13-13 | Elevation of Privilege | `window[renderFn]()` in `renderTabContent()` | medium | mitigate | Der dynamische Namenszugriff entfällt vollständig; aufgelöst wird nur noch über im Quelltext stehende Referenzen. Damit ist derselbe Angriffsvektor geschlossen, den SEC-03 für die `call`-Aktion schließt — hier zusätzlich als Nebeneffekt. |
| T-13-14 | Tampering | `defaultD`-`mindmap`-Seed in `systems/backups.js` | low | mitigate | Ersatzlos entfernt. Der Seed schrieb einen abgeschafften Schlüssel in einer Form (`edges`) wieder, die kein Konsument liest — reine Datenverschmutzung ohne Nutzen. Der Restore-Pfad bleibt durch den Smart-Strip in `version-migration.js:53-59` abwärtskompatibel. |
| T-13-15 | Tampering | `const D`-Überschattung in `soundboard-player.js` | low | mitigate | Umbenennung; kein Datenfluss betroffen. Die Stelle liest sich nicht mehr, als würde das globale Datenobjekt eine Zahl sein. |
| T-13-SC | Tampering | npm/pip/cargo installs | low | accept | Dieser Plan installiert kein Paket (`13-RESEARCH.md` → „Package Legitimacy Audit: Nicht anwendbar"). |
</threat_model>

<verification>
1. `npx jest tests/unit/backups.test.js tests/unit/tab-registry.test.js` grün
2. `grep -rn "mindmap: {" --include=*.js core features systems ui utils render tools | wc -l` liefert `0`
3. `grep -c 'const D = track' features/soundboard/soundboard-player.js` liefert `0`
4. `grep -c "renders: \['" systems/tab-registry.js` liefert `0`
5. `npx jest` vollständig grün, mindestens 33 Suiten
6. `PYTHONIOENCODING=utf-8 python build.py && npx playwright test tests/e2e/tab-navigation.spec.js tests/e2e/features/soundboard.spec.js` grün
7. `npx eslint .` meldet höchstens 1 Fehler (der vorbestehende in `systems/avatars.js:17`)
</verification>

<success_criteria>
- Erfolgskriterium 7 (ohne den `data-id`-Teil, der in Plan 13-02 liegt): Tab-Registry und
  `initPerformanceMonitoring()` sind abgesichert; tote `mindmap`-Seeds und `const D`-Überschattung sind weg
- Eine Umbenennung einer Renderfunktion bricht ab sofort einen Jest-Test
- Keine neuen ESLint-Fehler; die erwarteten neuen `no-undef`-Warnungen sind im SUMMARY beziffert
</success_criteria>

<output>
Create `.planning/phases/13-h-rtung-wartbarkeit/13-04-SUMMARY.md` when done
</output>
