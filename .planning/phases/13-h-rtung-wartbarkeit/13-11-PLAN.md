---
phase: 13-h-rtung-wartbarkeit
plan: 11
type: execute
wave: 6
depends_on: ["13-10"]
files_modified:
  - ui/editors/rich-text.js
  - ui/editors/rich-text-insert.js
  - ui/editors/rich-text-toolbars.js
  - features/spells/spell-manager.js
  - loader.js
autonomous: false
requirements: [MAINT-01]

estimate:
  tokens: 35000
  raw_tokens: 70000
  tasks: 4
  confidence: high

must_haves:
  truths:
    - "Keine der aus `ui/editors/rich-text.js` entstandenen Dateien überschreitet 800 Zeilen (D-01, hart, gemessen als physische Zeilen mit `wc -l`)."
    - "`rich-text.js` wurde ZUERST entflochten, nicht zerschnitten (D-03): die Zauberverwaltung (STATE-Anteil, RENDER, SPELL FORM, SPELL CRUD — zusammen rund 600 Zeilen) verlässt `ui/editors/` vollständig und zieht nach `features/spells/spell-manager.js`. Eine Datei namens `rich-text.js`, die die Zauberdatenbank-UI enthält, ist selbst eine irreführende Stelle im Sinne dieser Phase."
    - "Die Sektion EDITOR FORMATTING ist mit 799 Zeilen genau eine Zeile unter der Grenze und hat damit null Puffer für Kopfbanner und Exportblock; sie wird deshalb entlang ihrer Unter-Verantwortlichkeiten weiter geteilt (D-02, keine Ausnahme von D-01): Selektions-/Range-Grundlagen und Zeichenformatierung bleiben in `rich-text.js`, Einfügen/Einfügen-aus-Zwischenablage/Tastatur/Tabelle wandern nach `rich-text-insert.js`."
    - "Die Toolbars (FLOATING TOOLBAR + CONTEXT TOOLBARS, zusammen rund 500 Zeilen) liegen in `rich-text-toolbars.js`."
    - "Das eingefrorene 79-Test-Netz aus Phase 9 (`tests/e2e/features/editor-formatting.spec.js`, `editor-floating.spec.js`, `editor-insert.spec.js`, `editor-smoke.spec.js`) läuft vor UND nach der Aufteilung grün — es ist die Schutzschicht für jede Änderung an diesem Modul."
    - "Kein Aufruf der deprecated Editier-Kommando-API kehrt zurück: die Selection/Range-Hilfsfunktionen aus Phase 9 werden verschoben, nicht ersetzt (`wrapRangeWithElement`, `closestEditorAncestor`, `clearInlineFormattingAtSelection`, `applyFontFamilyToSelection`, `applyFontSizeToSelection`, `insertHtmlAtSelection`, `insertTextAtSelection`, `insertLineBreakAtSelection`)."
    - "Der gemeinsam genutzte Editor-Zustand (`editorSelectSavedRange`, `floatingToolbarTarget`, `floatingToolbarRange`, `hideFloatingToolbarTimeout`, `floatingToolbarInteracting`, `floatingToolbarInitialized`, `currentContextTable`, `currentContextLink`, `contextToolbarsInitialized`, `editorHandlersInitialized`) bleibt in `rich-text.js`, das als erstes der drei Editor-Module registriert ist — `ui/actions/system-actions.js` weist `floatingToolbarTarget` als bare Bezeichner zu und braucht die Deklaration vorher."
    - "Alle vier Dateien sind in `loader.js` `MODULES` registriert, in Abhängigkeitsreihenfolge. In `build.py` wird NICHTS eingetragen (ARCH-01)."
    - "Das Verhalten ist unverändert: volle Jest-Suite, Typprüfung, beide `dist`-Bündel, Build-Tests und volle Playwright-Suite laufen grün — der volle Suiten-Lauf ist Hard-Gate VOR dem Commit."
  prohibitions:
    - "Die 800-Zeilen-Grenze wird für keinen Einzelfall aufgeweicht. Passt eine Verantwortlichkeit nicht hinein, wird sie entlang einer Unter-Verantwortlichkeit weiter geteilt (D-02) — die Grenze wird nicht verschoben, und eine Datei mit 801 Zeilen gilt nicht als „praktisch erfüllt\". Erfolgskriterium 4 ist genau deshalb als binäre Zahl formuliert. Das gilt gleichermaßen für die Aufteilungen in den Plänen 13-09, 13-10 und 13-12."
  artifacts:
    - ui/editors/rich-text.js
    - ui/editors/rich-text-insert.js
    - ui/editors/rich-text-toolbars.js
    - features/spells/spell-manager.js
    - loader.js
  key_links:
    - "`ui/actions/system-actions.js` weist `floatingToolbarTarget` direkt zu und ruft `insertTable()` — beide Bezeichner müssen erhalten bleiben und aus früher geladenen Dateien stammen"
    - "`systems/tab-registry.js` referenziert den Renderer des `spells`-Tabs; nach Plan 13-04 ist das eine verzögerte Funktionsreferenz, die zur Aufrufzeit auflöst — der Umzug von `renderSpells` in eine andere Datei ist damit unkritisch, der Bezeichner darf sich aber nicht ändern"
    - "`features/render-spells.js` (Zaubertooltip und Zauber-Zuweisung) hat keine Namensüberschneidung mit den umziehenden Funktionen — geprüft; `check_duplicate_functions()` würde eine Kollision beim Build melden"
    - "`tests/e2e/features/editor-*.spec.js` (79 Tests, Phase 9) — die Schutzschicht, die vor und nach der Aufteilung läuft"
---

<objective>
Dritte der vier Aufteilungen aus `MAINT-01` (Reihenfolge D-05). `ui/editors/rich-text.js` ist mit 1932
Zeilen die größte der vier — aber D-03 stellt klar: sie ist nicht ein zu großes Modul, sondern **zwei
fremde Module in einer Datei**. Die Sektionsbanner belegen es: STATE, RENDER, SPELL FORM und SPELL CRUD
sind Zauberverwaltung (zusammen rund 600 Zeilen); EDITOR FORMATTING, FLOATING TOOLBAR und CONTEXT
TOOLBARS sind der Editor (rund 1300 Zeilen).

Purpose: Erfolgskriterium 4 der Phase, drittes Viertel. Diese Datei trägt das eingefrorene 79-Test-Netz
aus Phase 9 und ist damit — entgegen der Roadmap-Annahme — der **am besten abgesicherte** der vier
Fälle. Jede Änderung ist trotzdem begründungspflichtig, und das Netz läuft vor und nach der
Aufteilung.
Output: `features/spells/spell-manager.js` (neu, Zauberverwaltung), `ui/editors/rich-text.js`
(Selektion und Zeichenformatierung), `ui/editors/rich-text-insert.js` (Einfügen und Tastatur),
`ui/editors/rich-text-toolbars.js` (Toolbars) — alle vier unter 800 Zeilen.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-h-rtung-wartbarkeit/13-CONTEXT.md
@.planning/phases/13-h-rtung-wartbarkeit/13-RESEARCH.md
@.planning/phases/13-h-rtung-wartbarkeit/13-PATTERNS.md
@.planning/phases/13-h-rtung-wartbarkeit/13-10-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Dieser Plan erzeugt:

| Symbol / Datei | Art | Ort |
|---|---|---|
| `features/spells/spell-manager.js` | neues Quellmodul (Zauberverwaltung: Liste, Formular, CRUD) | `features/spells/` (neues Verzeichnis) |
| `ui/editors/rich-text-insert.js` | neues Quellmodul (Einfügen, Zwischenablage, Tastatur, Tabelle) | `ui/editors/` |
| `ui/editors/rich-text-toolbars.js` | neues Quellmodul (Floating- und Kontext-Toolbars) | `ui/editors/` |
| drei neue `MODULES`-Einträge | Registrierung in Abhängigkeitsreihenfolge | `loader.js` |

Verschobene, nicht neu erzeugte Symbole — nach `features/spells/spell-manager.js`: `currentSpellPage`,
`filteredSpellsCache`, `expandedSpells`, `debouncedRenderSpells`, `renderSpells`, `renderSpellCards`,
`renderLoadMoreButton`, `loadMoreSpells`, `toggleSpellCard`, `expandAllSpells`, `collapseAllSpells`,
`setSpellFilter`, `setSpellLevelFilter`, `setSpellSchoolFilter`, `onSpellRangeChange`,
`onSpellTimeChange`, `onSpellDurationChange`, `toggleMaterialField`, `getSpellClassesFromCheckboxes`,
`setSpellClassesCheckboxes`, `saveSpell`, `editSpell`, `deleteSpell`, `clearSpellForm`.
Nach `ui/editors/rich-text-insert.js`: `initEditorPasteHandlers`, `sanitizeInsertedInlineStyle`,
`insertHtmlAtSelection`, `insertTextAtSelection`, `insertLineBreakAtSelection`, `handleEditorKeydown`,
`handleEditorPaste`, `escapeHtml`, `insertTable`, `updateStickyOffsets`.
Nach `ui/editors/rich-text-toolbars.js`: `initFloatingToolbar`, `initContextToolbars` und die übrigen
Funktionen der beiden Toolbar-Sektionen.

**Kein Eintrag in `build.py`** (ARCH-01/D-06).

<tasks>

<task type="tracer">
  <name>Task 1: Zauberverwaltung aus dem Editor-Modul herauslösen</name>
  <precondition>Die vier eingefrorenen Editor-Spezifikationen aus Phase 9 (`tests/e2e/features/editor-formatting.spec.js`, `editor-floating.spec.js`, `editor-insert.spec.js`, `editor-smoke.spec.js`) laufen gegen ein frisch gebautes `dist/dnd-tracker-bundled.html` grün, BEVOR die erste Zeile verschoben wird.</precondition>
  <reversibility rating="costly">Die Zauber-Funktionen wandern in ein anderes Verzeichnis; ein Rückbau müsste `loader.js`, die Exportblöcke und die Testpfade erneut anfassen (D-03).</reversibility>
  <files>features/spells/spell-manager.js, ui/editors/rich-text.js, loader.js</files>
  <read_first>
    - `ui/editors/rich-text.js` vollständig — insbesondere die sieben Sektionsbanner (`STATE`, `RENDER`,
      `SPELL FORM`, `EDITOR FORMATTING`, `FLOATING TOOLBAR`, `CONTEXT TOOLBARS`, `SPELL CRUD`) und den
      `EXPORTS FOR GLOBAL ACCESS`-Block am Dateiende
    - Der `STATE`-Block: die ersten drei Deklarationen (`currentSpellPage`, `filteredSpellsCache`,
      `expandedSpells`) gehören zur Zauberverwaltung, die übrigen zehn zum Editor — dieser Block wird
      als einziger geteilt, alles andere wandert sektionsweise
    - `const debouncedRenderSpells = debounce(renderSpells, 200);` — wird beim Laden ausgewertet,
      braucht `debounce` (aus `utils/`, lädt früh) und `renderSpells` (hoisted in derselben Datei);
      muss also mit `renderSpells` zusammen umziehen
    - `features/render-spells.js` (Zaubertooltip, Zauber-Zuweisung) — bestehendes Geschwistermodul der
      Zauber-Domäne; keine Namensüberschneidung mit den umziehenden Funktionen
    - `features/npcs/npc-crud.js` (Zeilen 1–10 und Exportblock) — die Aufteilungskonvention:
      `// [SECTION:X]`-Kopf je Datei, eigener `EXPORTS FOR GLOBAL ACCESS`-Block, keine ESM-Anweisungen
    - `loader.js` Zeilen 55–160 (`MODULES`: `'features/render-spells.js'` steht in der Render-Gruppe,
      `'ui/editors/rich-text.js'` deutlich später in der UI-Gruppe)
    - `assets/templates/view-resources.html` und `modals-editors.html` — welche `data-action`-Ziele auf
      die umziehenden Zauber-Funktionen zeigen (die Exportnamen müssen erhalten bleiben)
    - `CLAUDE.md` §Conventions → „execCommand-Ablösung (abgeschlossen, Phase 9)" — die Liste der
      Hilfsfunktionen, die verschoben, nicht ersetzt werden
  </read_first>
  <action>
    Lege das Verzeichnis `features/spells/` und darin `spell-manager.js` an, mit eigenem
    `// [SECTION:SPELL_MANAGER]`-Kopf und eigenem `EXPORTS FOR GLOBAL ACCESS`-Block am Dateiende.
    Verschiebe dorthin: die drei Zauber-Zustandsvariablen aus dem `STATE`-Block sowie die vollständigen
    Sektionen `RENDER`, `SPELL FORM` und `SPELL CRUD` — inklusive der beim Laden ausgewerteten Zeile,
    die eine entprellte Fassung von `renderSpells` bildet, und inklusive der beiden `pushUndo()`-Aufrufe
    im Zauber-CRUD.
    **Verschieben, nicht kopieren.** Bleibt eine Funktion zusätzlich in `rich-text.js` stehen, bricht
    `check_duplicate_functions()` den Build vor dem Bündeln ab und nennt beide Dateien — gewünschter
    Ausgang (D-06), nicht umgehen.
    Die zugehörigen Exporte aus dem `rich-text.js`-Exportblock überführen: `debouncedRenderSpells`,
    `renderSpells`, `toggleSpellCard`, `expandAllSpells`, `collapseAllSpells`, `setSpellFilter`,
    `setSpellLevelFilter`, `setSpellSchoolFilter`, `onSpellRangeChange`, `onSpellTimeChange`,
    `onSpellDurationChange`, `toggleMaterialField`, `saveSpell`, `editSpell`, `deleteSpell`.
    Die zehn Editor-Zustandsvariablen bleiben in `rich-text.js` — sie werden von den Toolbar-Funktionen
    und von `ui/actions/system-actions.js` als bare Bezeichner gelesen und geschrieben.
    `'features/spells/spell-manager.js'` in `loader.js` `MODULES` eintragen, unmittelbar HINTER
    `'features/render-spells.js'` — damit steht die Zauber-Domäne beisammen und lädt nach `utils/` und
    `core/constants.js`. Nichts in `build.py`.
    Danach bauen und BEIDE Netze laufen lassen: die vier Editor-Spezifikationen (das Modul, aus dem
    ausgelagert wurde) und die Zauber-Oberfläche über die Tab-Navigations-Spezifikation.
  </action>
  <verify>
    <automated>PYTHONIOENCODING=utf-8 python build.py</automated>
    <fails_when>Exit-Code ungleich 0, oder die Ausgabe enthält `[FEHLER]` (dann steht eine Funktion in
    zwei Quelldateien oder eine gelistete Datei fehlt), oder die Ausgabe enthält `[ABORTED]`</fails_when>
    <automated>node --check ui/editors/rich-text.js &amp;&amp; node --check features/spells/spell-manager.js</automated>
    <fails_when>Exit-Code ungleich 0 oder eine SyntaxError-Meldung auf stderr</fails_when>
    <automated>npx playwright test tests/e2e/features/editor-formatting.spec.js tests/e2e/features/editor-floating.spec.js tests/e2e/features/editor-insert.spec.js tests/e2e/features/editor-smoke.spec.js tests/e2e/tab-navigation.spec.js</automated>
    <fails_when>Exit-Code ungleich 0, oder die Zusammenfassung meldet mindestens ein `failed`, oder die
    Zahl bestandener Tests der vier Editor-Spezifikationen liegt unter 79</fails_when>
  </verify>
  <acceptance_criteria>
    - `features/spells/spell-manager.js` existiert und beginnt mit einem `// [SECTION:` -Kopf
    - `grep -c "features/spells/spell-manager.js" loader.js` liefert `1`
    - `grep -c "spell-manager" build.py` liefert `0`
    - `grep -c "^function renderSpells" ui/editors/rich-text.js` liefert `0` und
      `grep -c "^function renderSpells" features/spells/spell-manager.js` liefert `1`
    - `grep -cE 'saveUndoState\(|pushUndo\(' features/spells/spell-manager.js` liefert `2` — beide
      Zauber-Undo-Aufrufe sind mitgewandert
    - `grep -c "^let floatingToolbarTarget" ui/editors/rich-text.js` liefert `1` — der Editor-Zustand
      ist geblieben
    - `PYTHONIOENCODING=utf-8 python build.py` endet mit Exit-Code 0
    - Verhalten: die vier Editor-Spezifikationen laufen mit mindestens 79 bestandenen Tests grün; die
      Zauberliste rendert beim Tab-Wechsel unverändert
  </acceptance_criteria>
  <done>Die Zauberverwaltung liegt in der Zauber-Domäne; `rich-text.js` enthält nur noch Editor-Code, und beide Netze sind grün.</done>
</task>

<task type="auto">
  <name>Task 2: Editor-Modul in drei Verantwortlichkeiten teilen</name>
  <files>ui/editors/rich-text.js, ui/editors/rich-text-insert.js, ui/editors/rich-text-toolbars.js, loader.js</files>
  <read_first>
    - `ui/editors/rich-text.js` (Stand nach Task 1 — jetzt reiner Editor-Code, rund 1330 Zeilen)
    - Die `EDITOR FORMATTING`-Sektion und ihre zwei Unter-Verantwortlichkeiten:
      **(A) Selektion und Zeichenformatierung** — `wrapRangeWithElement`, `unwrapEditorElement`,
      `closestEditorAncestor`, `applyInlineFormat`, `toggleUnorderedListAtSelection`,
      `clearInlineFormattingAtSelection`, `formatText`, `_lastFontCallKey`,
      `applyFontFamilyToSelection`, `applyFontSizeToSelection`, `setEditorFont`, `setEditorFontSize`,
      `clearEditorFormatting`, `setBorderFormat`, `setReadAloudFormat`, `removeSelectionBorders`;
      **(B) Einfügen, Zwischenablage, Tastatur, Tabelle** — `initEditorPasteHandlers`,
      `sanitizeInsertedInlineStyle`, `insertHtmlAtSelection`, `insertTextAtSelection`,
      `insertLineBreakAtSelection`, `handleEditorKeydown`, `handleEditorPaste`, `escapeHtml`,
      `insertTable`, `updateStickyOffsets`
    - Die Sektionen `FLOATING TOOLBAR` und `CONTEXT TOOLBARS` (zusammen rund 500 Zeilen) — sie bilden
      die dritte Datei
    - `ui/editors/rich-text.js` — der ausführliche Erklärkommentar über `insertLineBreakAtSelection()`
      zur Cursor-Stabilität in Chromium: er wandert vollständig mit der Funktion mit, er ist der
      teuerste Teil des Phase-9-Wissens in dieser Datei
    - `ui/actions/system-actions.js` — die Aktion `insert-table` weist `floatingToolbarTarget` zu und
      ruft `insertTable()`; die Aktion `insert-link` benutzt seit Plan 13-02 `wrapRangeWithElement`
    - `ui/editors/markdown-shortcuts.js` und `systems/markdown-import-export.js` — welche
      Editor-Funktionen sie als Globals rufen
    - `loader.js` (`'ui/editors/rich-text.js'` steht in der UI-Gruppe vor
      `'ui/editors/markdown-shortcuts.js'` und `'ui/editors/markdown-converter.js'`)
  </read_first>
  <action>
    Lege `ui/editors/rich-text-insert.js` und `ui/editors/rich-text-toolbars.js` an, je mit eigenem
    `// [SECTION:…]`-Kopf und eigenem `EXPORTS FOR GLOBAL ACCESS`-Block.
    Verschiebe Unter-Verantwortlichkeit (B) nach `rich-text-insert.js` und die beiden Toolbar-Sektionen
    nach `rich-text-toolbars.js`. In `rich-text.js` bleiben der Editor-Zustand (die zehn Variablen) und
    Unter-Verantwortlichkeit (A).
    Der Grund für diese Zweiteilung der Formatierungs-Sektion ist zwingend, nicht ästhetisch: die
    Sektion misst 799 Zeilen und liegt damit exakt eine Zeile unter der Grenze aus D-01. Kopfbanner und
    Exportblock der neuen Datei kämen obendrauf — sie 1:1 zu übernehmen risse die Grenze sofort. D-02
    schreibt für genau diesen Fall die Weiterteilung entlang einer Unter-Verantwortlichkeit vor. Die
    gewählte Trennlinie deckt sich mit den Namen der Phase-9-Spezifikationen (`editor-formatting` vs.
    `editor-insert`), das Netz bleibt also entlang derselben Grenze lesbar.
    Die Exporte aufteilen: nach `rich-text-insert.js` gehen `initEditorPasteHandlers`, `insertTable`,
    `updateStickyOffsets`; nach `rich-text-toolbars.js` gehen `initFloatingToolbar`,
    `initContextToolbars`; in `rich-text.js` bleiben `formatText`, `setEditorFont`, `setEditorFontSize`,
    `clearEditorFormatting`, `setBorderFormat`, `setReadAloudFormat`.
    Reihenfolge in `loader.js`: `'ui/editors/rich-text.js'` (Zustand + Selektion), danach
    `'ui/editors/rich-text-insert.js'`, danach `'ui/editors/rich-text-toolbars.js'` — beide neuen
    Einträge VOR `'ui/editors/markdown-shortcuts.js'`. Nichts in `build.py`.
    Lauf nach jeder einzelnen Verschiebung `python build.py` — nicht erst am Ende.
    Prüfe danach die Zeilenzahl aller vier Dateien. Erwartung: `spell-manager.js` rund 620,
    `rich-text.js` rund 400, `rich-text-insert.js` rund 450, `rich-text-toolbars.js` rund 530.
    Liegt eine über 800, wird weiter geteilt — die Grenze wird nicht verschoben (Verbot in
    `must_haves.prohibitions`).
    Prüfe zum Schluss die Exportmenge aller vier Dateien gegen die ursprünglichen 30 Exporte aus
    `rich-text.js`: die Vereinigungsmenge muss identisch sein.
  </action>
  <verify>
    <automated>PYTHONIOENCODING=utf-8 python build.py &amp;&amp; node --check ui/editors/rich-text.js &amp;&amp; node --check ui/editors/rich-text-insert.js &amp;&amp; node --check ui/editors/rich-text-toolbars.js</automated>
    <fails_when>Exit-Code ungleich 0, oder die Build-Ausgabe enthält `[FEHLER]`/`[ABORTED]`, oder eine
    SyntaxError-Meldung erscheint auf stderr</fails_when>
    <automated>test "$(wc -l &lt; ui/editors/rich-text.js)" -le 800 &amp;&amp; test "$(wc -l &lt; ui/editors/rich-text-insert.js)" -le 800 &amp;&amp; test "$(wc -l &lt; ui/editors/rich-text-toolbars.js)" -le 800 &amp;&amp; test "$(wc -l &lt; features/spells/spell-manager.js)" -le 800</automated>
    <fails_when>Exit-Code ungleich 0 — dann überschreitet mindestens eine der vier Dateien die
    800-Zeilen-Grenze aus D-01</fails_when>
    <automated>test "$(grep -rn 'document\.execCommand' --include=*.js core features systems ui utils render loader.js | wc -l)" -eq 0</automated>
    <fails_when>Exit-Code ungleich 0 — dann ist beim Verschieben ein Aufruf der abgelösten
    Editier-Kommando-API zurückgekehrt</fails_when>
    <automated>npx playwright test tests/e2e/features/editor-formatting.spec.js tests/e2e/features/editor-floating.spec.js tests/e2e/features/editor-insert.spec.js tests/e2e/features/editor-smoke.spec.js tests/e2e/features/wiki.spec.js</automated>
    <fails_when>Exit-Code ungleich 0, oder die Zusammenfassung meldet mindestens ein `failed`, oder die
    Zahl bestandener Tests der vier Editor-Spezifikationen liegt unter 79</fails_when>
  </verify>
  <acceptance_criteria>
    - `wc -l` liefert für alle vier Dateien einen Wert ≤ 800
    - Die Summe von `grep -c "^window\." ` über die vier Dateien ergibt `30` — dieselbe Exportmenge wie
      vor der Aufteilung
    - `grep -c "^function insertTextAtSelection" ui/editors/rich-text-insert.js` liefert `1`
    - `grep -c "^function wrapRangeWithElement" ui/editors/rich-text.js` liefert `1`
    - `grep -rn 'document\.execCommand' --include=*.js core features systems ui utils render loader.js | wc -l`
      liefert `0`
    - `grep -c "rich-text-insert" loader.js` und `grep -c "rich-text-toolbars" loader.js` liefern je `1`
    - Verhalten: die vier Editor-Spezifikationen laufen mit mindestens 79 bestandenen Tests grün
  </acceptance_criteria>
  <done>Vier Dateien, alle unter der Grenze, das eingefrorene Phase-9-Netz unverändert grün.</done>
</task>

<task type="auto">
  <name>Task 3: Hard-Gate — volle Suiten, beide Bündel, Typprüfung vor dem Commit</name>
  <files>ui/editors/rich-text.js, ui/editors/rich-text-insert.js, ui/editors/rich-text-toolbars.js, features/spells/spell-manager.js, loader.js</files>
  <read_first>
    - Alle vier Ergebnisdateien (Endstand)
    - `loader.js` (`MODULES` mit allen vier Einträgen in Abhängigkeitsreihenfolge)
    - `.planning/ROADMAP.md` §Phase 13 → „Auslegungshinweis zum Zuschnitt" — insbesondere der Satz,
      dass jede Änderung an `rich-text.js` begründungspflichtig ist
    - `.planning/phases/13-h-rtung-wartbarkeit/13-10-SUMMARY.md` — die im vorigen Aufteilungsplan
      tatsächlich gemessenen Suitenzahlen als Vergleichsbasis
  </read_first>
  <action>
    Führe den vollständigen Gate-Lauf aus, BEVOR die Aufteilung committet wird: volle Jest-Suite,
    Typprüfung, beide `dist`-Bündel, Python-Build-Tests und die volle Playwright-Suite gegen das frisch
    gebaute Entwicklungs-Bündel. Playwright läuft gegen `dist/dnd-tracker-bundled.html`; `npm run build`
    schreibt nur das Produktions-Bündel, deshalb muss `python build.py` (ohne Schalter) unmittelbar
    davor laufen.
    Halte im SUMMARY zusätzlich die Begründung fest, die die Roadmap für Änderungen an diesem Modul
    verlangt: welche Sektion in welche Datei ging, warum die Formatierungs-Sektion geteilt werden
    musste (799 Zeilen, kein Puffer), und die gemessene Zahl bestandener Tests der vier
    Editor-Spezifikationen vor und nach der Aufteilung. Die Zahl vorher stammt aus dem
    Vorbedingungs-Lauf von Task 1.
    Erst wenn alle Läufe grün sind, wird committet.
  </action>
  <verify>
    <automated>npx jest</automated>
    <fails_when>Exit-Code ungleich 0, oder die Zusammenfassung meldet mindestens ein `failed`, oder die
    Zahl bestandener Tests liegt unter der in `13-10-SUMMARY.md` festgehaltenen</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>Exit-Code ungleich 0 oder die Ausgabe enthält `error TS`</fails_when>
    <automated>PYTHONIOENCODING=utf-8 python build.py &amp;&amp; PYTHONIOENCODING=utf-8 python build.py --production &amp;&amp; python -m pytest tests/build -q</automated>
    <fails_when>Exit-Code ungleich 0, oder eine Build-Ausgabe enthält `[FEHLER]`/`[ABORTED]`, oder die
    pytest-Zusammenfassung meldet mindestens ein `failed`</fails_when>
    <automated>PYTHONIOENCODING=utf-8 python build.py &amp;&amp; npx playwright test</automated>
    <fails_when>Exit-Code ungleich 0, oder die Zusammenfassung meldet mindestens ein `failed`, oder die
    Zahl bestandener Tests liegt unter 321</fails_when>
  </verify>
  <acceptance_criteria>
    - `npx jest` grün, mindestens so viele bestandene Tests wie in `13-10-SUMMARY.md` festgehalten
    - `npx tsc --noEmit` endet mit Exit-Code 0 ohne `error TS`
    - `npx playwright test` grün mit mindestens 321 bestandenen Tests
    - `python -m pytest tests/build -q` grün mit 24 Tests
    - Das SUMMARY enthält die Begründung für die Aufteilung dieses Moduls und die Testzahlen der vier
      Editor-Spezifikationen vor und nach der Aufteilung
  </acceptance_criteria>
  <done>Das Hard-Gate ist mit vorgelegter Ausgabe bestanden und die begründungspflichtige Änderung dokumentiert.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Bedienprobe — fühlt sich der Editor nach der Aufteilung unverändert an?</name>
  <what-built>`ui/editors/rich-text.js` ist entflochten und aufgeteilt: die Zauberverwaltung liegt jetzt in `features/spells/spell-manager.js`, der Editor in `ui/editors/rich-text.js` (Selektion und Zeichenformatierung), `ui/editors/rich-text-insert.js` (Einfügen, Zwischenablage, Tastatur, Tabelle) und `ui/editors/rich-text-toolbars.js` (Floating- und Kontext-Toolbars). Alle vier Dateien liegen unter 800 Zeilen und sind in `loader.js` registriert. Das eingefrorene 79-Test-Netz aus Phase 9 und die volle Suite sind grün, das Entwicklungs-Bündel `dist/dnd-tracker-bundled.html` ist frisch gebaut.</what-built>
  <how-to-verify>
Das 79-Test-Netz aus Phase 9 deckt die Funktionalität ab, nicht das Bediengefühl. Die
Selektions-Stabilität in Chromium war in Phase 9 der teuerste Befund überhaupt (der ausführliche
Erklärkommentar über `insertLineBreakAtSelection()` dokumentiert eine empirisch ermittelte
Chromium-Eigenheit), und genau solche Effekte fallen in einem automatisierten Test nicht auf.
`13-VALIDATION.md` führt diese Probe deshalb ausdrücklich unter „Manual-Only Verifications".

`dist/dnd-tracker-bundled.html` per `file://` öffnen und im Wiki einen Eintrag anlegen. Dann:

1. Text markieren — die Floating-Toolbar erscheint an der Selektion
2. Fett, Kursiv, Unterstrichen, Durchgestrichen — je einmal an und wieder aus
3. Schriftart wechseln (Arial nach Poppins) und Schriftgröße wechseln
4. Einen Read-Aloud-Stil setzen (Parchment), dann einen anderen (Crimson), dann wieder entfernen
5. Eine Tabelle einfügen (3x3), dann in eine Zelle tippen
6. Zeilenumbruch mitten im Text setzen und unmittelbar weitertippen — der Text muss HINTER dem
   Umbruch landen
7. Formatierten Text aus einer anderen Quelle einfügen — die Formatierung wird bereinigt übernommen
8. „Formatierung entfernen" auf einen gemischt formatierten Absatz anwenden
9. Einen Link einfügen (Kontext-Toolbar) und wieder entfernen
10. Zum Zauber-Tab wechseln: Liste rendert, Filter wirken, ein Zauber lässt sich anlegen, bearbeiten
    und löschen
  </how-to-verify>
  <resume-signal>Antworte mit „freigegeben", wenn sich alle zehn Punkte wie vor der Aufteilung verhalten — sonst die Abweichung beschreiben. Eine Abweichung ist ein Befund, keine Gewöhnungssache.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Zwischenablage-Inhalt (fremdes HTML) → `handleEditorPaste()` → `sanitizeInsertedInlineStyle()` → Editor-DOM | Die schärfste Grenze dieses Moduls: fremdes Markup wird in den Editor übernommen. Die Bereinigungskette darf beim Verschieben nicht zerreißen. |
| Nutzertext im Editor → gespeicherter Eintragsinhalt → Renderpfad | Was der Editor schreibt, landet unverändert in `D` und später im Renderpfad des jeweiligen Features. |
| `loader.js` `MODULES` → `build.py` → Bündel | Reihenfolge ist hier besonders kritisch: der geteilte Editor-Zustand wird aus drei Dateien und aus `ui/actions/system-actions.js` gelesen und geschrieben. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-44 | Tampering | Zerrissene Bereinigungskette beim Einfügen aus der Zwischenablage | high | mitigate | `handleEditorPaste`, `sanitizeInsertedInlineStyle`, `insertHtmlAtSelection` und `escapeHtml` wandern GEMEINSAM in `rich-text-insert.js` — die Kette bleibt in einer Datei und damit sichtbar zusammen. Abgesichert durch `tests/e2e/features/editor-insert.spec.js` im Netz. |
| T-13-45 | Tampering | Rückkehr der deprecated Editier-Kommando-API beim Verschieben | high | mitigate | Eigene Abnahmeprüfung über den gesamten Quellbaum nach jeder Verschiebung. Die Phase-9-Hilfsfunktionen werden verschoben, nicht neu geschrieben. |
| T-13-46 | Denial of Service | Geteilter Editor-Zustand in falscher Ladereihenfolge | high | mitigate | Alle zehn Zustandsvariablen bleiben in `ui/editors/rich-text.js`, das als erstes der drei Editor-Module registriert wird; `ui/actions/system-actions.js` lädt später und schreibt `floatingToolbarTarget` als bare Bezeichner. Ein Fehler hier bricht den Start laut, nicht still. |
| T-13-47 | Denial of Service | Verlorener `window.`-Export beim Verschieben | high | mitigate | Die Vereinigungsmenge der Exporte aller vier Dateien wird gegen die ursprünglichen 30 abgeglichen. |
| T-13-48 | Repudiation | Aufgeweichte 800-Zeilen-Grenze | medium | mitigate | Als Verbot in `must_haves.prohibitions` festgehalten; die Abnahme misst mit `wc -l` und vergleicht gegen 800, nicht gegen „ungefähr 800". |
| T-13-49 | Tampering | Verlorenes Phase-9-Wissen im Erklärkommentar | medium | mitigate | Der ausführliche Kommentar über die Chromium-Cursor-Eigenheit wandert vollständig mit seiner Funktion — er ist der teuerste Teil des Phase-9-Ergebnisses in dieser Datei und darf beim Verschieben nicht gekürzt werden. |
| T-13-SC | Tampering | npm/pip/cargo installs | low | accept | Dieser Plan installiert kein Paket (`13-RESEARCH.md` → „Package Legitimacy Audit: Nicht anwendbar"). |
</threat_model>

<verification>
1. `wc -l` liefert für alle vier Ergebnisdateien einen Wert ≤ 800
2. Exportsumme über die vier Dateien = 30 (unverändert)
3. `grep -rn 'document\.execCommand' --include=*.js core features systems ui utils render loader.js | wc -l` liefert `0`
4. Die vier Editor-Spezifikationen laufen mit ≥ 79 bestandenen Tests grün, vor UND nach der Aufteilung
5. `npx jest` grün, nicht weniger Tests als in `13-10-SUMMARY.md`
6. `npx tsc --noEmit` ohne `error TS`
7. `PYTHONIOENCODING=utf-8 python build.py && PYTHONIOENCODING=utf-8 python build.py --production && python -m pytest tests/build -q` grün
8. `PYTHONIOENCODING=utf-8 python build.py && npx playwright test` grün, ≥ 321 Tests
9. Bedienprobe (Task 4) in allen zehn Punkten unauffällig
</verification>

<success_criteria>
- Erfolgskriterium 4, drittes Viertel: `ui/editors/rich-text.js` ist entflochten und aufgeteilt, keine
  Nachfolgedatei über 800 Zeilen, das Verhalten unverändert
- Die Zauberverwaltung liegt nicht mehr in einer Datei namens `rich-text.js`
- Das eingefrorene 79-Test-Netz aus Phase 9 ist vor und nach der Aufteilung grün
- Volles Suiten-Gate mit vorgelegter Ausgabe vor dem Commit, plus bestandene Bedienprobe
</success_criteria>

<output>
Create `.planning/phases/13-h-rtung-wartbarkeit/13-11-SUMMARY.md` when done
</output>
