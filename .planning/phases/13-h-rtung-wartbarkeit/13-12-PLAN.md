---
phase: 13-h-rtung-wartbarkeit
plan: 12
type: execute
wave: 7
depends_on: ["13-05", "13-11"]
files_modified:
  - features/dmscreen/dmscreen-render.js
  - features/dmscreen/dmscreen-config.js
  - features/dmscreen/dmscreen-widgets-base.js
  - features/dmscreen/dmscreen-widgets-combat.js
  - features/dmscreen/dmscreen-widgets-reference.js
  - loader.js
autonomous: false
requirements: [MAINT-01]

estimate:
  tokens: 37500
  raw_tokens: 75000
  tasks: 4
  confidence: high

must_haves:
  truths:
    - "Keine der aus `features/dmscreen/dmscreen-render.js` entstandenen Dateien überschreitet 800 Zeilen (D-01, hart, gemessen als physische Zeilen mit `wc -l`)."
    - "Der Charakterisierungs-Snapshot aus Plan 13-05 ist VOR der ersten Codeverschiebung grün und bleibt nach der Aufteilung unverändert grün — er wird mit `--ci` geprüft, sodass eine Abweichung ein Fehler ist und kein Anlass zum Neuschreiben (D-04)."
    - "Der Snapshot-Test läuft nach der Aufteilung unverändert weiter, weil er seine Modulliste aus `loader.js` `MODULES` bezieht und nur die öffentliche Oberfläche anspricht (`getDMScreenWidgets()`, `window.renderDMScreen`) — er musste für diesen Plan nicht angefasst werden."
    - "Geschnitten wurde entlang der vorhandenen `// ====`-Sektionsbanner (D-02), mit einer ausdrücklichen Weiterteilung: `dmscreen-render.js` ist mit 1576 Zeilen die strukturell breiteste der vier Dateien (21 unabhängige Widget-Renderer statt zwei Verantwortlichkeiten) und braucht deshalb mehr als die zwei bis drei Zieldateien der übrigen Aufteilungen."
    - "Die Registry `getDMScreenWidgets()` und der Einstieg `renderDMScreen()` bleiben in `features/dmscreen/dmscreen-render.js`; die beiden `window`-Exporte (`renderDMScreen`, `resetDMScreenLayout`) bleiben dort und behalten ihre Namen."
    - "Alle fünf Dateien sind in `loader.js` `MODULES` registriert, `dmscreen-render.js` zuerst. In `build.py` wird NICHTS eingetragen (ARCH-01)."
    - "Das Verhalten ist unverändert: volle Jest-Suite, Typprüfung, beide `dist`-Bündel, Build-Tests und volle Playwright-Suite laufen grün — der volle Suiten-Lauf ist Hard-Gate VOR dem Commit."
  artifacts:
    - features/dmscreen/dmscreen-render.js
    - features/dmscreen/dmscreen-config.js
    - features/dmscreen/dmscreen-widgets-base.js
    - features/dmscreen/dmscreen-widgets-combat.js
    - features/dmscreen/dmscreen-widgets-reference.js
    - loader.js
  key_links:
    - "`tests/unit/dmscreen-characterization.test.js` (Plan 13-05) → `loader.js` `MODULES` (alle Einträge unter `features/dmscreen/`) → gemeinsame Sandbox → `getDMScreenWidgets()`: die Kette, die diese Aufteilung absichert"
    - "`getDMScreenWidgets()` bleibt in `dmscreen-render.js` und referenziert die Renderfunktionen als bare Bezeichner — die Auflösung passiert beim Aufruf der Registry, nicht beim Laden; die Widget-Dateien dürfen deshalb hinter `dmscreen-render.js` stehen"
    - "`systems/tab-registry.js` referenziert den Renderer des `dmscreen`-Tabs; nach Plan 13-04 ist das eine verzögerte Funktionsreferenz auf `renderDMScreen`, die in `dmscreen-render.js` bleiben muss"
    - "`registerPostSaveHook()` (`systems/spellslots/persistence.js`) → die Live-Sync-Registrierung im LIVE-SYNC-Block; sie bleibt im Kernmodul und darf beim Verschieben nicht dupliziert werden"
---

<objective>
Vierte und letzte Aufteilung aus `MAINT-01` (Reihenfolge D-05) — und die riskanteste.
`features/dmscreen/dmscreen-render.js` hat 1576 Zeilen, 58 Funktionen und 21 Widget-Typen, und es kommt
in genau zwei Testdateien mit zusammen fünf beiläufigen Erwähnungen vor. D-04 hat deshalb das
Vorsichtsbudget dieser Phase hierher gelegt: Plan 13-05 hat einen Charakterisierungs-Snapshot gegen das
UNGETEILTE Modul erzeugt, und der muss grün sein, bevor hier die erste Zeile bewegt wird.

Purpose: Erfolgskriterium 4 der Phase, letztes Viertel. Das Aufteilungsverfahren ist zu diesem
Zeitpunkt dreimal geübt (Pläne 13-09, 13-10, 13-11); der schwierigste Fall kommt zuletzt und mit dem
einzigen eigens dafür gebauten Netz.
Output: fünf Dateien unter `features/dmscreen/`, alle unter 800 Zeilen, alle in `loader.js` registriert,
Snapshot unverändert.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-h-rtung-wartbarkeit/13-CONTEXT.md
@.planning/phases/13-h-rtung-wartbarkeit/13-RESEARCH.md
@.planning/phases/13-h-rtung-wartbarkeit/13-PATTERNS.md
@.planning/phases/13-h-rtung-wartbarkeit/13-05-SUMMARY.md
@.planning/phases/13-h-rtung-wartbarkeit/13-11-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Dieser Plan erzeugt:

| Symbol / Datei | Art | Ort |
|---|---|---|
| `features/dmscreen/dmscreen-config.js` | neues Quellmodul (Profile, Widget-Konfiguration, Drag & Drop) | `features/dmscreen/` |
| `features/dmscreen/dmscreen-widgets-base.js` | neues Quellmodul (die 8 Basis-Widgets) | `features/dmscreen/` |
| `features/dmscreen/dmscreen-widgets-combat.js` | neues Quellmodul (kampfbezogene Referenz-Widgets) | `features/dmscreen/` |
| `features/dmscreen/dmscreen-widgets-reference.js` | neues Quellmodul (charakter- und umgebungsbezogene Referenz-Widgets) | `features/dmscreen/` |
| vier neue `MODULES`-Einträge | Registrierung hinter `'features/dmscreen/dmscreen-render.js'` | `loader.js` |

Verschobene, nicht neu erzeugte Symbole — nach `dmscreen-config.js`: `switchDMSProfile`,
`saveDMSProfileAs`, `deleteDMSProfile`, `toggleDMSProfileDropdown`, `renderDMSProfileList`,
`renderDMSConfigList`, `toggleDMSWidget`, `hideDMSWidget`, `saveDMScreenLayout`,
`toggleDMSConfigDropdown`, `dmsDraggedWidget`, `initDMSWidgetDragDrop`, `handleDMSWidgetDragStart`,
`handleDMSWidgetDragEnd`, `handleDMSWidgetDragOver`, `handleDMSWidgetDragEnter`,
`handleDMSWidgetDragLeave`, `handleDMSWidgetDrop`, `reorderDMSWidgets`, `dmsConfigDraggedItem`,
`initDMSConfigDragDrop`, `handleDMSConfigDragStart`, `handleDMSConfigDragEnd`,
`handleDMSConfigDragOver`, `handleDMSConfigDragEnter`, `handleDMSConfigDragLeave`,
`handleDMSConfigDrop`.
Nach `dmscreen-widgets-base.js`: `renderDMSConditionsCompact`, `renderDMSPartyWidget`,
`renderDMSInitiativeWidget`, `renderDMSDiceWidget`, `dmsRollDice`, `renderDMSDCWidget`,
`renderDMSTablesWidget`, `renderDMSRulesWidget`, `renderDMSNotesWidget`, `saveDMSNotes`.
Nach `dmscreen-widgets-combat.js`: `renderDMSActionsWidget`, `renderDMSEconomyWidget`,
`renderDMSDamageWidget`, `renderDMSTerrainWidget`, `renderDMSImprovisedWidget`.
Nach `dmscreen-widgets-reference.js`: `renderDMSAttributesWidget`, `renderDMSSavesWidget`,
`renderDMSSkillsWidget`, `renderDMSSizesWidget`, `renderDMSObjectsWidget`, `renderDMSRitualWidget`,
`renderDMSKnowledgeWidget`, `renderDMSTravelWidget`.

**Kein Eintrag in `build.py`** (ARCH-01/D-06). **`tests/unit/dmscreen-characterization.test.js` und
seine Snapshot-Datei werden NICHT angefasst** — sie beziehen ihre Modulliste aus `loader.js` und
sprechen nur die öffentliche Oberfläche an; genau dafür wurden sie in Plan 13-05 so gebaut.

<tasks>

<task type="tracer">
  <name>Task 1: Referenz-Widgets als erste Gruppe herauslösen, Snapshot beweist Neutralität</name>
  <precondition>`npx jest tests/unit/dmscreen-characterization.test.js --ci` läuft grün gegen das UNGETEILTE `features/dmscreen/dmscreen-render.js`, und `tests/unit/__snapshots__/dmscreen-characterization.test.js.snap` ist eingecheckt und nicht leer (D-04, harte Reihenfolgevorgabe: ohne dieses Netz darf keine Zeile verschoben werden).</precondition>
  <reversibility rating="costly">Die 800-Zeilen-Grenze aus D-01 später zu senken hieße erneut aufteilen; ein Rückbau dieser Aufteilung müsste `loader.js`, fünf Exportblöcke und die Widget-Registry erneut anfassen.</reversibility>
  <files>features/dmscreen/dmscreen-widgets-reference.js, features/dmscreen/dmscreen-render.js, loader.js</files>
  <read_first>
    - `features/dmscreen/dmscreen-render.js` vollständig — insbesondere die Sektionsbanner `CONSTANTS`,
      `LIVE-SYNC SYSTEM`, `LAYOUT PROFILES`, `WIDGET CONFIGURATION`, `DRAG & DROP - WIDGET GRID`,
      `DRAG & DROP - CONFIG LIST`, `WIDGET DEFINITIONS (lazy-loaded to avoid reference issues)`, die
      sieben Basis-Widget-Banner, `NEUE REFERENZ-WIDGETS`, `EVENT HANDLERS`, `KEYBOARD SHORTCUTS`,
      `BACKWARD COMPATIBILITY - Export to window`
    - **Achtung Sektionsgrenze:** der `LAYOUT PROFILES`-Banner umfasst neben den fünf Profilfunktionen
      auch `renderDMScreen()` — der öffentliche Einstieg. Der bleibt im Kernmodul; nur die fünf
      Profilfunktionen wandern. Das ist die einzige Stelle dieser Aufteilung, an der ein Bannerblock
      geteilt wird, und der Grund ist benennbar: der Banner fasst zwei Verantwortlichkeiten zusammen.
    - `getDMScreenWidgets()` — die Registry gibt pro Typ `{ name, icon, render, compact }` zurück und
      referenziert die Renderfunktionen als bare Bezeichner. Die Auflösung passiert beim AUFRUF der
      Registry, nicht beim Laden — deshalb dürfen die Widget-Dateien hinter dem Kernmodul stehen.
    - `tests/unit/dmscreen-characterization.test.js` (Plan 13-05) — wie der Test seine Modulliste aus
      `loader.js` zieht; das erklärt, warum er nach der Aufteilung unverändert läuft
    - `systems/spellslots/` (11 Dateien, z. B. `spell-slots-core.js` plus Feature-Dateien) — die
      nächstliegende Aufteilungskonvention dieses Projekts für „ein Registry-Kern plus viele
      unabhängige Feature-Dateien"
    - `loader.js` Zeile mit `'features/dmscreen/dmscreen-render.js'` (heute der einzige Eintrag der
      DM-Screen-Gruppe)
    - `features/dmscreen/dmscreen-render.d.ts` (die vorhandene Typdeklaration)
    - `CLAUDE.md` §„DM Screen (Widget-Based Dashboard)" (die verbindliche Aufzählung der 21 Typen und
      das Widget-System-Muster), §Gotchas Nr. 1, §„Build System & Deduplication"
  </read_first>
  <action>
    Prüfe zuerst die Vorbedingung: der Charakterisierungs-Snapshot muss grün sein, BEVOR etwas bewegt
    wird. Ist er es nicht, halte an und melde es — das ist die harte Reihenfolgevorgabe aus D-04, nicht
    eine Empfehlung.
    Lege dann `features/dmscreen/dmscreen-widgets-reference.js` an, mit eigenem
    `// [SECTION:DMSCREEN_WIDGETS_REFERENCE]`-Kopf. Verschiebe als Tracer die acht charakter- und
    umgebungsbezogenen Referenz-Widgets aus der `NEUE REFERENZ-WIDGETS`-Sektion:
    `renderDMSAttributesWidget`, `renderDMSSavesWidget`, `renderDMSSkillsWidget`,
    `renderDMSSizesWidget`, `renderDMSObjectsWidget`, `renderDMSRitualWidget`,
    `renderDMSKnowledgeWidget`, `renderDMSTravelWidget`.
    Diese acht sind der ideale Tracer: sie sind reine Referenzdarstellungen ohne Zustand, ohne
    Ereignisbehandlung und ohne Abhängigkeit untereinander — wenn das Verfahren hier etwas kaputtmacht,
    liegt es am Verfahren und nicht am Widget.
    **Verschieben, nicht kopieren.** Bleibt eine Funktion zusätzlich im Kernmodul stehen, bricht
    `check_duplicate_functions()` den Build vor dem Bündeln ab und nennt beide Dateien — gewünschter
    Ausgang (D-06).
    Keine `window.`-Exporte für diese acht: sie werden ausschließlich über die Registry aufgerufen, und
    die CLAUDE.md-Export-Audit-Regel verlangt Exporte nur für das, was andere Module oder
    `data-action`-Handler brauchen. Die Registry in `dmscreen-render.js` referenziert sie als bare
    Bezeichner und löst zur Aufrufzeit auf.
    `'features/dmscreen/dmscreen-widgets-reference.js'` in `loader.js` `MODULES` eintragen, unmittelbar
    HINTER `'features/dmscreen/dmscreen-render.js'`. Nichts in `build.py`.
    Danach bauen und den Charakterisierungs-Snapshot mit `--ci` prüfen: er muss zeichengleich passen.
    Eine Abweichung ist ein Befund über die Aufteilung — der Snapshot wird NICHT nachgezogen
    (Verbot aus Plan 13-05).
  </action>
  <verify>
    <automated>PYTHONIOENCODING=utf-8 python build.py</automated>
    <fails_when>Exit-Code ungleich 0, oder die Ausgabe enthält `[FEHLER]` (dann steht eine Funktion in
    zwei Quelldateien oder eine gelistete Datei fehlt), oder die Ausgabe enthält `[ABORTED]`</fails_when>
    <automated>node --check features/dmscreen/dmscreen-render.js &amp;&amp; node --check features/dmscreen/dmscreen-widgets-reference.js</automated>
    <fails_when>Exit-Code ungleich 0 oder eine SyntaxError-Meldung auf stderr</fails_when>
    <automated>npx jest tests/unit/dmscreen-characterization.test.js --ci</automated>
    <fails_when>Exit-Code ungleich 0, oder die Ausgabe enthält `snapshots written`, oder die
    Zusammenfassung meldet mindestens ein `failed` — jede dieser Meldungen heißt, dass die Aufteilung
    die Widget-Ausgabe verändert hat</fails_when>
  </verify>
  <acceptance_criteria>
    - `features/dmscreen/dmscreen-widgets-reference.js` existiert und beginnt mit einem `// [SECTION:` -Kopf
    - `grep -c "dmscreen-widgets-reference" loader.js` liefert `1`
    - `grep -c "dmscreen" build.py` liefert `0`
    - `grep -c "^function renderDMSSkillsWidget" features/dmscreen/dmscreen-render.js` liefert `0` und
      `grep -c "^function renderDMSSkillsWidget" features/dmscreen/dmscreen-widgets-reference.js` liefert `1`
    - `git diff --name-only -- tests/unit/dmscreen-characterization.test.js tests/unit/__snapshots__/`
      liefert eine leere Ausgabe — weder Test noch Snapshot wurden angefasst
    - `npx jest tests/unit/dmscreen-characterization.test.js --ci` endet mit Exit-Code 0
    - Verhalten: alle 21 Widget-Typen rendern nach der Verschiebung zeichengleich zum eingefrorenen
      Snapshot
  </acceptance_criteria>
  <done>Eine Widget-Gruppe liegt in einer eigenen Datei, und der Snapshot beweist, dass sich an der Ausgabe nichts geändert hat.</done>
</task>

<task type="auto">
  <name>Task 2: Restliche vier Gruppen herauslösen, alle Dateien unter die Grenze bringen</name>
  <files>features/dmscreen/dmscreen-config.js, features/dmscreen/dmscreen-widgets-base.js, features/dmscreen/dmscreen-widgets-combat.js, features/dmscreen/dmscreen-render.js, loader.js</files>
  <read_first>
    - `features/dmscreen/dmscreen-render.js` (Stand nach Task 1)
    - Die verbleibenden Blöcke: die fünf Profilfunktionen (`switchDMSProfile`, `saveDMSProfileAs`,
      `deleteDMSProfile`, `toggleDMSProfileDropdown`, `renderDMSProfileList`), die
      `WIDGET CONFIGURATION`-Sektion (`renderDMSConfigList`, `toggleDMSWidget`, `hideDMSWidget`,
      `saveDMScreenLayout`, `toggleDMSConfigDropdown`), beide `DRAG & DROP`-Sektionen (je eine
      Modulvariable plus sieben bzw. sechs Behandlungsfunktionen), die acht Basis-Widgets
      (`renderDMSConditionsCompact`, `renderDMSPartyWidget`, `renderDMSInitiativeWidget`,
      `renderDMSDiceWidget`, `dmsRollDice`, `renderDMSDCWidget`, `renderDMSTablesWidget`,
      `renderDMSRulesWidget`, `renderDMSNotesWidget`, `saveDMSNotes`) und die fünf kampfbezogenen
      Referenz-Widgets (`renderDMSActionsWidget`, `renderDMSEconomyWidget`, `renderDMSDamageWidget`,
      `renderDMSTerrainWidget`, `renderDMSImprovisedWidget`)
    - `EVENT HANDLERS` und `KEYBOARD SHORTCUTS` — sie enthalten `dmsRollOnTable` und
      `dmsShowConditionDetail` und bleiben im Kernmodul, weil sie die Registry und den Einstieg bedienen
    - `LIVE-SYNC SYSTEM` (`isDMScreenVisible`, `refreshDMScreenIfVisible`, `renderDMScreenWidgetsOnly`,
      `setupDMScreenLiveSync`, `initDMScreenLayout`, `resetDMScreenLayout`) — bleibt im Kernmodul; die
      Registrierung über `registerPostSaveHook()` darf beim Verschieben nicht dupliziert werden
    - `assets/templates/` und `ui/actions/` — welche der Profil-, Konfigurations- und
      Drag-&-Drop-Funktionen über `data-action` oder `window` von außen gerufen werden (diese
      Exportnamen müssen erhalten bleiben)
  </read_first>
  <action>
    Lege die drei übrigen Dateien an, je mit eigenem `// [SECTION:…]`-Kopf und, wo nötig, eigenem
    Exportblock:
    (a) `features/dmscreen/dmscreen-config.js` — die fünf Profilfunktionen, die
    `WIDGET CONFIGURATION`-Sektion und beide `DRAG & DROP`-Sektionen samt ihrer zwei Modulvariablen.
    (b) `features/dmscreen/dmscreen-widgets-base.js` — die acht Basis-Widgets samt `dmsRollDice` und
    `saveDMSNotes`.
    (c) `features/dmscreen/dmscreen-widgets-combat.js` — die fünf kampfbezogenen Referenz-Widgets.
    Im Kernmodul `dmscreen-render.js` verbleiben: `CONSTANTS`, `LIVE-SYNC SYSTEM`, `renderDMScreen()`,
    die Registry `getDMScreenWidgets()`, `EVENT HANDLERS`, `KEYBOARD SHORTCUTS` und der
    `BACKWARD COMPATIBILITY`-Exportblock mit `window.renderDMScreen` und `window.resetDMScreenLayout`.
    Die Gruppierung der Referenz-Widgets folgt dem dargestellten Inhalt, nicht dem Alphabet und nicht
    der Zeilenbalance: kampfbezogen (Aktionen, Kampfökonomie, Schadensarten, Gelände, improvisierte
    Waffen) gegen charakter- und umgebungsbezogen (Attribute, Rettungswürfe, Fertigkeiten,
    Wissensgebiete, Größen, Objekte, Ritual & Konzentration, Reisen & Traglast). Wer später ein
    Widget sucht, sucht nach seinem Inhalt.
    Reihenfolge in `loader.js`: `'features/dmscreen/dmscreen-render.js'` zuerst, danach
    `dmscreen-config.js`, `dmscreen-widgets-base.js`, `dmscreen-widgets-combat.js`,
    `dmscreen-widgets-reference.js`. Nichts in `build.py`.
    Lauf nach JEDER einzelnen Gruppenverschiebung `python build.py` und den Snapshot mit `--ci` — nicht
    erst am Ende. Bei fünf Zieldateien und 58 Funktionen ist das der Unterschied zwischen einem
    lokalisierbaren und einem nicht mehr lokalisierbaren Fehler.
    Prüfe danach die Zeilenzahl aller fünf Dateien. Erwartung: `dmscreen-render.js` rund 560,
    `dmscreen-config.js` rund 330, `dmscreen-widgets-base.js` rund 260,
    `dmscreen-widgets-combat.js` rund 160, `dmscreen-widgets-reference.js` rund 260. Liegt eine über
    800, wird weiter geteilt — die Grenze wird nicht verschoben (Verbot aus Plan 13-11).
    Prüfe zum Schluss, dass die Registry weiterhin exakt 21 Typen liefert und dass die beiden
    `window`-Exporte des Kernmoduls unverändert vorhanden sind.
  </action>
  <verify>
    <automated>PYTHONIOENCODING=utf-8 python build.py &amp;&amp; node --check features/dmscreen/dmscreen-render.js &amp;&amp; node --check features/dmscreen/dmscreen-config.js &amp;&amp; node --check features/dmscreen/dmscreen-widgets-base.js &amp;&amp; node --check features/dmscreen/dmscreen-widgets-combat.js &amp;&amp; node --check features/dmscreen/dmscreen-widgets-reference.js</automated>
    <fails_when>Exit-Code ungleich 0, oder die Build-Ausgabe enthält `[FEHLER]`/`[ABORTED]`, oder eine
    SyntaxError-Meldung erscheint auf stderr</fails_when>
    <automated>for f in features/dmscreen/*.js; do test "$(wc -l &lt; "$f")" -le 800 || exit 1; done</automated>
    <fails_when>Exit-Code ungleich 0 — dann überschreitet mindestens eine Datei unter
    `features/dmscreen/` die 800-Zeilen-Grenze aus D-01</fails_when>
    <automated>npx jest tests/unit/dmscreen-characterization.test.js --ci</automated>
    <fails_when>Exit-Code ungleich 0, oder die Ausgabe enthält `snapshots written`, oder die
    Zusammenfassung meldet mindestens ein `failed`</fails_when>
    <automated>PYTHONIOENCODING=utf-8 python build.py &amp;&amp; npx playwright test tests/e2e/tab-navigation.spec.js tests/e2e/app.spec.js</automated>
    <fails_when>Exit-Code ungleich 0, oder die Zusammenfassung meldet mindestens ein `failed`</fails_when>
  </verify>
  <acceptance_criteria>
    - `wc -l` liefert für alle fünf Dateien unter `features/dmscreen/` einen Wert ≤ 800
    - `grep -c "^window\.renderDMScreen" features/dmscreen/dmscreen-render.js` liefert `1` und
      `grep -c "^window\.resetDMScreenLayout" features/dmscreen/dmscreen-render.js` liefert `1`
    - `grep -c "^function getDMScreenWidgets" features/dmscreen/dmscreen-render.js` liefert `1`
    - `grep -c "registerPostSaveHook" features/dmscreen/dmscreen-render.js` liefert `1` — die
      Live-Sync-Registrierung existiert genau einmal
    - `grep -c "dmscreen" build.py` liefert `0`; alle vier neuen Pfade stehen in `loader.js`
    - `git diff --name-only -- tests/unit/dmscreen-characterization.test.js tests/unit/__snapshots__/`
      liefert eine leere Ausgabe
    - Verhalten: der Snapshot-Test bestätigt für alle 21 Widget-Typen, den Gesamt-Einstieg, die vier
      Profile und den Leerfall zeichengleiche Ausgabe
  </acceptance_criteria>
  <done>Fünf Dateien, alle unter der Grenze, und der Snapshot aus Plan 13-05 sitzt unverändert.</done>
</task>

<task type="auto">
  <name>Task 3: Hard-Gate — volle Suiten, beide Bündel, Typprüfung vor dem Commit</name>
  <files>features/dmscreen/dmscreen-render.js, features/dmscreen/dmscreen-config.js, features/dmscreen/dmscreen-widgets-base.js, features/dmscreen/dmscreen-widgets-combat.js, features/dmscreen/dmscreen-widgets-reference.js, features/dmscreen/dmscreen-render.d.ts, loader.js</files>
  <read_first>
    - Alle fünf Ergebnisdateien (Endstand)
    - `loader.js` (`MODULES` mit allen fünf Einträgen in Abhängigkeitsreihenfolge)
    - `features/dmscreen/dmscreen-render.d.ts` — die vorhandene Typdeklaration; prüfen, ob sie nach der
      Aufteilung noch stimmt
    - `.planning/ROADMAP.md` §Phase 13 → „Auslegungshinweis zum Zuschnitt"
    - `.planning/phases/13-h-rtung-wartbarkeit/13-11-SUMMARY.md` — die im vorigen Aufteilungsplan
      tatsächlich gemessenen Suitenzahlen als Vergleichsbasis
  </read_first>
  <action>
    Führe den vollständigen Gate-Lauf aus, BEVOR die Aufteilung committet wird: volle Jest-Suite,
    Typprüfung, beide `dist`-Bündel, Python-Build-Tests und die volle Playwright-Suite gegen das frisch
    gebaute Entwicklungs-Bündel. Playwright läuft gegen `dist/dnd-tracker-bundled.html`; `npm run build`
    schreibt nur das Produktions-Bündel, deshalb muss `python build.py` (ohne Schalter) unmittelbar
    davor laufen.
    Zur Typprüfung: `features/dmscreen/dmscreen-render.d.ts` beschreibt heute das ungeteilte Modul.
    Meldet `npx tsc --noEmit` nach der Aufteilung einen Fehler, ziehe die Deklaration nach — eine
    stehengebliebene, jetzt falsche Typdeklaration ist selbst eine irreführende Stelle im Sinne dieser
    Phase.
    Lege die Ausgabe jedes Laufs im SUMMARY vor — Zahl der bestandenen Tests je Suite, Größe beider
    Bündel, Zeilenzahl aller fünf DM-Screen-Dateien, und die Bestätigung, dass Snapshot-Datei und
    Charakterisierungstest unverändert sind. Eine Behauptung ohne vorgelegte Ausgabe erfüllt das Gate
    nicht (Verbot aus Plan 13-09).
    Da dies die letzte der vier Aufteilungen ist: halte im SUMMARY zusätzlich die Endzeilenzahl ALLER
    aus `MAINT-01` entstandenen Dateien fest — das ist der Nachweis für Erfolgskriterium 4 der ganzen
    Phase.
    Erst wenn alle Läufe grün sind, wird committet.
  </action>
  <verify>
    <automated>npx jest</automated>
    <fails_when>Exit-Code ungleich 0, oder die Zusammenfassung meldet mindestens ein `failed`, oder die
    Zahl bestandener Tests liegt unter der in `13-11-SUMMARY.md` festgehaltenen</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>Exit-Code ungleich 0 oder die Ausgabe enthält `error TS`</fails_when>
    <automated>PYTHONIOENCODING=utf-8 python build.py &amp;&amp; PYTHONIOENCODING=utf-8 python build.py --production &amp;&amp; python -m pytest tests/build -q</automated>
    <fails_when>Exit-Code ungleich 0, oder eine Build-Ausgabe enthält `[FEHLER]`/`[ABORTED]`, oder die
    pytest-Zusammenfassung meldet mindestens ein `failed`</fails_when>
    <automated>PYTHONIOENCODING=utf-8 python build.py &amp;&amp; npx playwright test</automated>
    <fails_when>Exit-Code ungleich 0, oder die Zusammenfassung meldet mindestens ein `failed`, oder die
    Zahl bestandener Tests liegt unter 321</fails_when>
  </verify>
  <acceptance_criteria>
    - `npx jest` grün, mindestens so viele bestandene Tests wie in `13-11-SUMMARY.md` festgehalten
    - `npx tsc --noEmit` endet mit Exit-Code 0 ohne `error TS`
    - `npx playwright test` grün mit mindestens 321 bestandenen Tests
    - `python -m pytest tests/build -q` grün mit 24 Tests
    - Das SUMMARY listet die Zeilenzahl JEDER aus `MAINT-01` entstandenen Datei (Wiki, Initiative,
      Rich-Text/Zauber, DM-Screen) und belegt damit Erfolgskriterium 4 für die ganze Phase
  </acceptance_criteria>
  <done>Das Hard-Gate ist mit vorgelegter Ausgabe bestanden; Erfolgskriterium 4 ist über alle vier Aufteilungen belegt.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Bedienprobe — Masonry-Layout und Widget-Zuordnung nach der Aufteilung</name>
  <what-built>`features/dmscreen/dmscreen-render.js` ist in fünf Module aufgeteilt: Kern (Konstanten, Live-Sync, `renderDMScreen()`, Widget-Registry, Ereignisbehandlung, Tastaturkürzel), `dmscreen-config.js` (Profile, Widget-Konfiguration, Ziehen-und-Ablegen), `dmscreen-widgets-base.js` (8 Basis-Widgets), `dmscreen-widgets-combat.js` (5 kampfbezogene Referenz-Widgets) und `dmscreen-widgets-reference.js` (8 charakter- und umgebungsbezogene Referenz-Widgets). Alle fünf liegen unter 800 Zeilen und sind in `loader.js` registriert. Der Charakterisierungs-Snapshot aus Plan 13-05 ist unverändert grün, die volle Suite läuft, das Entwicklungs-Bündel `dist/dnd-tracker-bundled.html` ist frisch gebaut.</what-built>
  <how-to-verify>
Der Charakterisierungs-Snapshot friert die HTML-Ausgabe je Widget ein, nicht den Umbruch des
CSS-Masonry-Layouts über verschiedene Bildschirmbreiten und nicht die Zuordnung „kompakt gehört in die
Kopfleiste, nicht-kompakt ins Raster". Beides ist visuell und in einer Assertion nicht greifbar;
`13-VALIDATION.md` führt es deshalb ausdrücklich unter „Manual-Only Verifications". Der DM-Screen ist
zudem das Modul, das vor dieser Phase faktisch keine eigene Testabdeckung hatte — die Bedienprobe ist
hier mehr wert als anderswo.

`dist/dnd-tracker-bundled.html` per `file://` öffnen, zum DM-Screen wechseln. Dann:

1. Alle 21 Widget-Typen über die Konfigurationsliste einblenden
2. Bei Vollbreite prüfen: kompakte Widgets (Zustände) sitzen in der Kopfleiste, alle anderen im Raster
3. Fenster auf 768 px verschmälern — der Masonry-Umbruch greift, kein Widget wird abgeschnitten
4. Fenster auf 320 px verschmälern — einspaltig, alles lesbar, keine horizontale Bildlaufleiste
5. Die vier Standardprofile durchschalten (Standard, Kampf, Minimal, Referenz) — jedes zeigt seine
   erwartete Widget-Auswahl
6. Ein eigenes Profil speichern, wechseln, wieder löschen
7. Ein Widget im Raster per Ziehen umsortieren — die neue Reihenfolge bleibt nach einem Neuladen erhalten
8. Ein Widget über die Konfigurationsliste per Ziehen umsortieren
9. In einer anderen Ansicht Daten ändern (z. B. HP eines Charakters) und zum DM-Screen zurückkehren —
   die Live-Sync aktualisiert das Party-Widget
10. Im Würfel-Widget eine Formel würfeln und im Tabellen-Widget eine Zufallstabelle auslösen
  </how-to-verify>
  <resume-signal>Antworte mit „freigegeben", wenn sich alle zehn Punkte wie vor der Aufteilung verhalten — sonst die Abweichung beschreiben. Eine Abweichung ist ein Befund über die Aufteilung.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kampagnendaten (`D`) → 21 Widget-Renderer → `innerHTML` des DM-Screen-Containers | Charakternamen, Notizen, Tabelleneinträge und Zustandsbeschreibungen fließen in HTML; `esc()` ist die Schutzschicht und muss beim Verschieben Zeile für Zeile mitwandern. |
| Persist-Erfolgspunkt → `registerPostSaveHook()` → Live-Sync des DM-Screens | Die Registrierung darf beim Verschieben weder verloren gehen noch dupliziert werden — eine doppelte Registrierung ließe die Live-Sync doppelt feuern. |
| `loader.js` `MODULES` → sowohl `build.py` als auch der Charakterisierungstest | Beide leiten ihre Dateiliste aus derselben Quelle ab; eine fehlende Registrierung macht den Test still unvollständig statt rot. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-50 | Tampering | Verlorener `esc()`-Aufruf in einem der 21 Widget-Renderer | high | mitigate | Der Charakterisierungs-Snapshot aus Plan 13-05 hält die HTML-Ausgabe jedes Widgets zeichengleich fest; ein verlorenes Escaping ändert die Ausgabe und wird mit `--ci` rot. Das ist der einzige automatisierte Schutz, den dieses Modul hat. |
| T-13-51 | Repudiation | Nachgezogener statt untersuchter Snapshot | high | mitigate | Alle Prüfläufe verwenden `--ci`; eine geschriebene Snapshot-Datei ist ein Fehlschlag. Zusätzlich prüft die Abnahme, dass Test und Snapshot-Datei unverändert sind. Das zugehörige Verbot steht in Plan 13-05. |
| T-13-52 | Denial of Service | Doppelte oder verlorene `registerPostSaveHook()`-Registrierung | high | mitigate | Der LIVE-SYNC-Block bleibt vollständig im Kernmodul; die Abnahme prüft, dass die Registrierung genau einmal vorkommt. Eine doppelte Registrierung ließe die Live-Sync doppelt feuern, eine verlorene machte den DM-Screen still veraltet — beides fiele ohne diese Prüfung erst am Spieltisch auf. |
| T-13-53 | Denial of Service | Unvollständige Widget-Registry nach dem Verschieben | high | mitigate | Der Snapshot-Test friert die sortierte Typenliste als eigenen Eintrag ein; ein verschwundener oder umbenannter Typ fällt sofort auf. |
| T-13-54 | Tampering | Funktion bleibt in zwei Dateien stehen | medium | mitigate | `check_duplicate_functions()` bricht den Build vor dem Bündeln ab und nennt beide Dateien; `python build.py` und der Snapshot laufen nach JEDER Gruppenverschiebung (D-06, RESEARCH Pitfall 4). |
| T-13-55 | Repudiation | Stehengebliebene, jetzt falsche `dmscreen-render.d.ts` | medium | mitigate | `npx tsc --noEmit` ist Teil des Hard-Gates; meldet es einen Fehler, wird die Deklaration nachgezogen statt ignoriert. |
| T-13-SC | Tampering | npm/pip/cargo installs | low | accept | Dieser Plan installiert kein Paket (`13-RESEARCH.md` → „Package Legitimacy Audit: Nicht anwendbar"). |
</threat_model>

<verification>
1. `npx jest tests/unit/dmscreen-characterization.test.js --ci` grün, ohne geschriebene Snapshots — vor UND nach jeder Gruppenverschiebung
2. `wc -l` liefert für jede Datei unter `features/dmscreen/` einen Wert ≤ 800
3. `git diff --name-only -- tests/unit/dmscreen-characterization.test.js tests/unit/__snapshots__/` liefert eine leere Ausgabe
4. `grep -c "registerPostSaveHook" features/dmscreen/dmscreen-render.js` liefert `1`
5. `grep -c "dmscreen" build.py` liefert `0`; alle fünf Pfade stehen in `loader.js`
6. `npx jest` grün, nicht weniger Tests als in `13-11-SUMMARY.md`
7. `npx tsc --noEmit` ohne `error TS`
8. `PYTHONIOENCODING=utf-8 python build.py && PYTHONIOENCODING=utf-8 python build.py --production && python -m pytest tests/build -q` grün
9. `PYTHONIOENCODING=utf-8 python build.py && npx playwright test` grün, ≥ 321 Tests
10. Bedienprobe (Task 4) in allen zehn Punkten unauffällig
</verification>

<success_criteria>
- Erfolgskriterium 4 ist für alle vier Dateien erfüllt: keine der aus `MAINT-01` entstandenen Dateien
  überschreitet 800 Zeilen, bei unverändertem Verhalten, belegt durch die bestehenden Suiten
- Der Charakterisierungs-Snapshot aus Plan 13-05 ist unverändert grün — die Aufteilung ist
  verhaltensneutral bewiesen, nicht behauptet
- Registrierung ausschließlich in `loader.js`, in Abhängigkeitsreihenfolge
- Volles Suiten-Gate mit vorgelegter Ausgabe vor dem Commit, plus bestandene Bedienprobe
</success_criteria>

<output>
Create `.planning/phases/13-h-rtung-wartbarkeit/13-12-SUMMARY.md` when done
</output>
