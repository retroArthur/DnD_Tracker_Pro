---
phase: 13-h-rtung-wartbarkeit
plan: 10
type: execute
wave: 5
depends_on: ["13-09"]
files_modified:
  - features/initiative.js
  - features/initiative-loot.js
  - features/initiative-combat-widgets.js
  - loader.js
autonomous: true
requirements: [MAINT-01]

estimate:
  tokens: 27500
  raw_tokens: 55000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Keine der aus `features/initiative.js` entstandenen Dateien überschreitet 800 Zeilen (D-01, hart, gemessen als physische Zeilen mit `wc -l`)."
    - "Geschnitten wurde entlang der vorhandenen `// ====`-Sektionsbanner (D-02): `initiative.js` behält UTILITY FUNCTIONS, RENDER HELPER FUNCTIONS, XP-VERTEILUNG, den unbenannten HP/AC/Init-Block, BATTLEFIELD CONDITIONS und den globalen Exportblock; `initiative-loot.js` bekommt LOOT SYSTEM; `initiative-combat-widgets.js` bekommt EFFECTS, DEATH SAVES TRACKER, LEGENDÄRE AKTIONEN + WIDERSTÄNDE, CONCENTRATION TRACKER und AOE DAMAGE CALCULATOR."
    - "Das Verhalten ist unverändert: volle Jest-Suite, Typprüfung, beide `dist`-Bündel, Build-Tests und volle Playwright-Suite laufen grün — der volle Suiten-Lauf ist Hard-Gate VOR dem Commit (Roadmap-Auslegungshinweis; Verbot dazu in Plan 13-09 festgehalten)."
    - "Alle drei Dateien sind in `loader.js` `MODULES` registriert, `initiative.js` vor den beiden neuen — die gemeinsam genutzten Modulvariablen und die Renderhelfer stehen in `initiative.js` und müssen im Loader-Modus vorher ausgewertet sein. In `build.py` wird NICHTS eingetragen (ARCH-01)."
    - "Keine Funktion steht nach der Aufteilung in zwei Dateien; `check_duplicate_functions()` bricht den Build sonst vor dem Bündeln ab und nennt beide Dateien — dieser Abbruch ist der gewünschte Ausgang (D-06)."
    - "Die zwei bestehenden `pushUndo()`-Aufrufstellen in `features/initiative.js` (XP-Verteilung und Beute entfernen) bleiben erhalten — die eine wandert mit dem LOOT-Block in die neue Datei, die andere bleibt bei der XP-Sektion. Die Summe über alle drei Dateien bleibt 2."
    - "`features/initiative.d.ts` bleibt gültig oder wird mitgezogen; `npx tsc --noEmit` meldet keinen neuen Fehler."
  artifacts:
    - features/initiative.js
    - features/initiative-loot.js
    - features/initiative-combat-widgets.js
    - loader.js
  key_links:
    - "`systems/tab-registry.js` ruft für den Tab `initiative` die Renderer `renderInit`, `renderBattlefieldBanner` und `renderQuickActionsBar` auf — die ersten beiden bleiben in `initiative.js`, der dritte lebt in `features/quick-actions.js`. Nach Plan 13-04 sind das verzögerte Funktionsreferenzen; die Bezeichner dürfen sich nicht ändern."
    - "`features/initiative-extras.js`, `features/initiative-mob.js` und `features/initiative-statblock.js` sind bestehende Geschwistermodule desselben Features — sie geben das Namensschema `initiative-*` vor und rufen Funktionen aus `initiative.js` als bare Globals auf"
    - "`ui/actions/combat-actions.js` und `ui/actions/entity-actions.js` rufen die exportierten Initiative- und Beute-Funktionen über `window` — die Exportnamen dürfen sich nicht ändern"
---

<objective>
Zweite der vier Aufteilungen aus `MAINT-01` (Reihenfolge D-05). `features/initiative.js` ist mit 1655
Zeilen die zweitgrößte der vier und zerfällt bereits sichtbar in drei Verantwortlichkeiten: den
Initiative-Kern (Render-Helfer, XP-Verteilung, HP/AC-Bearbeitung, Schlachtfeld-Banner), die
Kampf-Widgets (Effekte, Todesrettungswürfe, legendäre Aktionen, Konzentration, Flächenschaden) und das
Beute-System (Master-Detail-Ansicht, 373 Zeilen — die mit Abstand größte Einzelsektion).

Purpose: Erfolgskriterium 4 der Phase, zweites Viertel. `initiative.js` wird in 12 Testdateien erwähnt
und ist damit nach `wiki.js` der zweitbest abgesicherte der vier — das Verfahren aus Plan 13-09 wird
hier ein zweites Mal geübt, bevor es auf `rich-text.js` und `dmscreen-render.js` trifft.
Output: `features/initiative.js` (gekürzt), `features/initiative-loot.js` und
`features/initiative-combat-widgets.js` (neu), alle drei unter 800 Zeilen und in `loader.js` registriert.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-h-rtung-wartbarkeit/13-CONTEXT.md
@.planning/phases/13-h-rtung-wartbarkeit/13-RESEARCH.md
@.planning/phases/13-h-rtung-wartbarkeit/13-PATTERNS.md
@.planning/phases/13-h-rtung-wartbarkeit/13-09-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Dieser Plan erzeugt:

| Symbol / Datei | Art | Ort |
|---|---|---|
| `features/initiative-loot.js` | neues Quellmodul (LOOT SYSTEM) | `features/` |
| `features/initiative-combat-widgets.js` | neues Quellmodul (EFFECTS, DEATH SAVES, LEGENDARY, CONCENTRATION, AOE) | `features/` |
| `'features/initiative-loot.js'`, `'features/initiative-combat-widgets.js'` | zwei neue Einträge im `MODULES`-Array, direkt hinter `'features/initiative.js'` | `loader.js` |

Verschobene, nicht neu erzeugte Symbole — nach `initiative-loot.js`: `selectedLootId`,
`currentLootFilter`, `renderLoot`, `renderLootList`, `renderLootItem`, `selectLoot`, `showLootDetail`,
`clearLootDetail`, `setLootFilter`, `showLootModal`, `saveLoot`, `editLoot`, `removeLoot`.
Nach `initiative-combat-widgets.js`: `showAddEffect`, `renderEffectConditionsGrid`, `addEffectFromGrid`,
`saveCustomEffect`, `removeEffect`, `renderDeathSaves`, `toggleDeathSave`, `resetDeathSaves`,
`renderLegendaryActionPips`, `renderLegendaryResistancePips`, `useLA`, `useLR`, `resetLR`,
`renderConcentration`, `renderConcentrationCheck`, `showConcentrationModal`, `setConcentration`,
`breakConcentration`, `rollConcentrationCheck`, `aoeCurrentDamage`, `showAoEDamageModal`,
`rollAoEDamage`, `updateAoETargetDisplay`, `debouncedUpdateAoE`, `aoeSelectAll`, `aoeSelectNone`,
`aoeSelectEnemies`, `applyAoEDamage`.

**Kein Eintrag in `build.py`** (ARCH-01/D-06).

<tasks>

<task type="tracer">
  <name>Task 1: LOOT SYSTEM in `initiative-loot.js` auslagern, registrieren, bauen</name>
  <reversibility rating="costly">Die Beute-Funktionen wandern in ein eigenes Modul; ein Rückbau müsste `loader.js`, beide Exportblöcke und die Testpfade erneut anfassen. Die 800-Zeilen-Grenze aus D-01 später zu senken hieße erneut aufteilen.</reversibility>
  <files>features/initiative-loot.js, features/initiative.js, loader.js</files>
  <read_first>
    - `features/initiative.js` vollständig — insbesondere die Sektionsbanner `UTILITY FUNCTIONS`,
      `RENDER HELPER FUNCTIONS`, `XP-VERTEILUNG` (mit dem inline stehenden Export
      `window.applyXpDistribution` unmittelbar vor dem `END XP-VERTEILUNG`-Banner), `EFFECTS`,
      `DEATH SAVES TRACKER`, die Legendär-/Widerstands-Sektion, `CONCENTRATION TRACKER`,
      `AOE DAMAGE CALCULATOR`, `LOOT SYSTEM (Master-Detail Layout)`, `BATTLEFIELD CONDITIONS` und
      `GLOBAL EXPORTS`. **Die Zeilennummern aus `13-RESEARCH.md` gelten nach Plan 13-08 nicht mehr
      exakt — orientiere dich an den Bannertexten.**
    - Der `LOOT SYSTEM`-Block: er beginnt mit den beiden Modulvariablen `selectedLootId` und
      `currentLootFilter` und enthält `renderLoot`, `renderLootList`, `renderLootItem`, `selectLoot`,
      `showLootDetail`, `clearLootDetail`, `setLootFilter`, `showLootModal`, `saveLoot`, `editLoot`,
      `removeLoot` — inklusive des `pushUndo()`-Aufrufs in `removeLoot()`
    - `features/loot-distribution.js` — das bestehende Geschwistermodul für die Beuteverteilung; es
      ruft Beute-Funktionen als Globals auf und darf nicht brechen
    - `features/initiative-extras.js` und `features/initiative-mob.js` (je Zeilen 1–10 und der
      Exportblock am Dateiende) — das Namens- und Kopfschema für Geschwistermodule dieses Features
    - `loader.js` Zeilen 80–150 (`MODULES`: `'features/initiative.js'` steht in der Feature-Gruppe;
      `'features/initiative-extras.js'` und `'features/initiative-mob.js'` stehen deutlich später)
    - `features/initiative.d.ts` (die vorhandene Typdeklaration — prüfen, ob die verschobenen Symbole
      dort auftauchen)
    - `CLAUDE.md` §Gotchas Nr. 1, §„Build System & Deduplication", §„const/var Conflict"
  </read_first>
  <action>
    Lege `features/initiative-loot.js` an, mit eigenem `// [SECTION:INITIATIVE_LOOT]`-Kopf im Stil von
    `features/initiative-mob.js` und eigenem `GLOBAL EXPORTS`-Block am Dateiende.
    Verschiebe den vollständigen `LOOT SYSTEM (Master-Detail Layout)`-Block — vom Banner bis
    unmittelbar vor das `BATTLEFIELD CONDITIONS`-Banner — samt der beiden Modulvariablen
    `selectedLootId` und `currentLootFilter`. **Verschieben, nicht kopieren:** bleibt eine Funktion
    zusätzlich in `initiative.js` stehen, bricht `check_duplicate_functions()` den Build vor dem
    Bündeln ab und nennt beide Dateien. Das ist der gewünschte Ausgang (D-06).
    Die zugehörigen Exporte aus dem `GLOBAL EXPORTS`-Block von `initiative.js` in den neuen Exportblock
    überführen: `renderLoot`, `renderLootList`, `selectLoot`, `setLootFilter`, `showLootModal`,
    `saveLoot`, `editLoot`, `removeLoot`.
    Der `pushUndo()`-Aufruf in `removeLoot()` wandert unverändert mit — er wird weder entfernt noch
    umsortiert (PERF-01/D-10 gilt weiterhin: geändert wird nur, was innerhalb von `pushUndo()`
    passiert, nie die Aufrufstellen).
    Keine `import`/`export`-Anweisungen. Kein `var X = window.X` für `const`/`let`-deklarierte Globals —
    die Konstanten aus `core/constants.js` (`CATS`, `RARITY_COLORS`, `RARITY_LABELS`, `ORIGIN_LABELS`,
    `LOOT_TAG_LABELS`) sind als bare Bezeichner lesbar.
    `'features/initiative-loot.js'` in `loader.js` `MODULES` eintragen, unmittelbar HINTER
    `'features/initiative.js'`. Nichts in `build.py`.
    Danach bauen und die Initiative-Spezifikation laufen lassen.
  </action>
  <verify>
    <automated>PYTHONIOENCODING=utf-8 python build.py</automated>
    <fails_when>Exit-Code ungleich 0, oder die Ausgabe enthält `[FEHLER]` (dann steht eine Funktion in
    zwei Quelldateien oder eine gelistete Datei fehlt), oder die Ausgabe enthält `[ABORTED]`</fails_when>
    <automated>node --check features/initiative.js &amp;&amp; node --check features/initiative-loot.js</automated>
    <fails_when>Exit-Code ungleich 0 oder eine SyntaxError-Meldung auf stderr</fails_when>
    <automated>npx playwright test tests/e2e/features/initiative.spec.js</automated>
    <fails_when>Exit-Code ungleich 0, oder die Zusammenfassung meldet mindestens ein `failed`, oder sie
    meldet `0 passed`</fails_when>
    <automated>npx jest tests/unit/initiative-mob.test.js tests/unit/entities.test.js</automated>
    <fails_when>Exit-Code ungleich 0, oder die Zusammenfassung meldet mindestens ein `failed`</fails_when>
  </verify>
  <acceptance_criteria>
    - `features/initiative-loot.js` existiert und beginnt mit einem `// [SECTION:` -Kopf
    - `grep -c "features/initiative-loot.js" loader.js` liefert `1`
    - `grep -c "features/initiative" build.py` liefert `0`
    - `grep -c "^function renderLootList" features/initiative.js` liefert `0` und
      `grep -c "^function renderLootList" features/initiative-loot.js` liefert `1`
    - `grep -c "pushUndo(" features/initiative-loot.js` liefert `1` — der Undo-Aufruf ist mitgewandert
    - `grep -c "^import \|^export " features/initiative-loot.js` liefert `0`
    - `PYTHONIOENCODING=utf-8 python build.py` endet mit Exit-Code 0
    - Verhalten: `npx playwright test tests/e2e/features/initiative.spec.js` läuft grün — Beuteliste,
      Detailansicht, Anlegen/Bearbeiten/Löschen und die Filter funktionieren unverändert
  </acceptance_criteria>
  <done>Der Beute-Block liegt in einem eigenen Modul, ist registriert, gebaut und durch die Initiative-Spezifikation abgesichert.</done>
</task>

<task type="auto">
  <name>Task 2: Kampf-Widgets in `initiative-combat-widgets.js` auslagern</name>
  <files>features/initiative-combat-widgets.js, features/initiative.js, loader.js</files>
  <read_first>
    - `features/initiative.js` (Stand nach Task 1)
    - Die fünf zu verschiebenden Bannerblöcke mit ihren Funktionen: `EFFECTS` (`showAddEffect`,
      `renderEffectConditionsGrid`, `addEffectFromGrid`, `saveCustomEffect`, `removeEffect`),
      `DEATH SAVES TRACKER` (`renderDeathSaves`, `toggleDeathSave`, `resetDeathSaves`), die
      Legendär-/Widerstands-Sektion (`renderLegendaryActionPips`, `renderLegendaryResistancePips`,
      `useLA`, `useLR`, `resetLR`), `CONCENTRATION TRACKER` (`renderConcentration`,
      `renderConcentrationCheck`, `showConcentrationModal`, `setConcentration`, `breakConcentration`,
      `rollConcentrationCheck`) und `AOE DAMAGE CALCULATOR` (`aoeCurrentDamage`, `showAoEDamageModal`,
      `rollAoEDamage`, `updateAoETargetDisplay`, `debouncedUpdateAoE`, `aoeSelectAll`, `aoeSelectNone`,
      `aoeSelectEnemies`, `applyAoEDamage`)
    - `features/initiative.js` `RENDER HELPER FUNCTIONS` — insbesondere die Helfer, die `renderInit()`
      aufruft (`getInitCombatantDetails`, `getCombatantHpStatus`, `renderCombatantEffects`,
      `renderCombatantSpellSlots`); sie bleiben in `initiative.js`, rufen aber `renderDeathSaves()`,
      `renderConcentration()` und die Legendär-Pips auf, die wegwandern — die Auflösung passiert zur
      Laufzeit
    - `const debouncedUpdateAoE = debounce(updateAoETargetDisplay, UI_TIMING.AOE_UPDATE_DEBOUNCE);` —
      diese Zeile wird beim Laden ausgewertet und braucht `debounce` (aus `utils/`, lädt früh) und
      `UI_TIMING` (aus `core/constants.js`, lädt früh) sowie die im selben Block deklarierte Funktion
      (hoisting-sicher). Sie muss zusammen mit `updateAoETargetDisplay` in dieselbe Datei.
    - `ui/actions/combat-actions.js` — welche dieser Funktionen von außen über `window` gerufen werden
  </read_first>
  <action>
    Lege `features/initiative-combat-widgets.js` an, mit eigenem
    `// [SECTION:INITIATIVE_COMBAT_WIDGETS]`-Kopf und eigenem `GLOBAL EXPORTS`-Block am Dateiende.
    Verschiebe die fünf oben genannten Bannerblöcke vollständig und in ihrer bisherigen Reihenfolge.
    Die zugehörigen Exporte aus dem `GLOBAL EXPORTS`-Block von `initiative.js` überführen:
    `showAddEffect`, `addEffectFromGrid`, `saveCustomEffect`, `removeEffect`, `toggleDeathSave`,
    `useLA`, `useLR`, `resetLR`, `showConcentrationModal`, `breakConcentration`,
    `rollConcentrationCheck`, `showAoEDamageModal`.
    Achte auf die beim Laden ausgewertete Zeile, die eine entprellte Fassung von
    `updateAoETargetDisplay` bildet: sie muss in derselben Datei stehen wie die Funktion, die sie
    entprellt, und nach den Modulen laufen, die `debounce` und `UI_TIMING` bereitstellen — beides ist
    erfüllt, wenn die neue Datei hinter `features/initiative.js` registriert wird.
    `'features/initiative-combat-widgets.js'` in `loader.js` `MODULES` eintragen, hinter
    `'features/initiative-loot.js'`. Nichts in `build.py`.
    Lauf nach der Verschiebung sofort `python build.py` — `check_duplicate_functions()` soll eine
    vergessene Dublette nach EINER Verschiebung melden, nicht nach mehreren.
    Prüfe danach die Zeilenzahl aller drei Dateien. Erwartung nach den Sektionsgrößen: `initiative.js`
    bei rund 640, `initiative-combat-widgets.js` bei rund 690, `initiative-loot.js` bei rund 400 Zeilen.
    Liegt eine über 800, teile die betroffene Verantwortlichkeit entlang einer Unter-Sektion weiter auf
    (D-02, keine Ausnahme von D-01) und registriere die vierte Datei ebenfalls in `loader.js`.
    Prüfe zum Schluss den Exportblock aller drei Dateien gegen den ursprünglichen: die
    Vereinigungsmenge muss identisch sein (34 Einträge). Ein verlorener Export macht einen
    `data-action`-Knopf still wirkungslos.
  </action>
  <verify>
    <automated>PYTHONIOENCODING=utf-8 python build.py &amp;&amp; node --check features/initiative.js &amp;&amp; node --check features/initiative-loot.js &amp;&amp; node --check features/initiative-combat-widgets.js</automated>
    <fails_when>Exit-Code ungleich 0, oder die Build-Ausgabe enthält `[FEHLER]`/`[ABORTED]`, oder eine
    SyntaxError-Meldung erscheint auf stderr</fails_when>
    <automated>test "$(wc -l &lt; features/initiative.js)" -le 800 &amp;&amp; test "$(wc -l &lt; features/initiative-loot.js)" -le 800 &amp;&amp; test "$(wc -l &lt; features/initiative-combat-widgets.js)" -le 800</automated>
    <fails_when>Exit-Code ungleich 0 — dann überschreitet mindestens eine der drei Dateien die
    800-Zeilen-Grenze aus D-01</fails_when>
    <automated>npx playwright test tests/e2e/features/initiative.spec.js tests/e2e/features/bestiary.spec.js tests/e2e/tab-navigation.spec.js</automated>
    <fails_when>Exit-Code ungleich 0, oder die Zusammenfassung meldet mindestens ein `failed`</fails_when>
  </verify>
  <acceptance_criteria>
    - `wc -l < features/initiative.js`, `wc -l < features/initiative-loot.js` und
      `wc -l < features/initiative-combat-widgets.js` liefern alle drei einen Wert ≤ 800
    - Die Summe von `grep -c "^window\." ` über die drei Dateien ergibt `34` — dieselbe Exportmenge wie
      vor der Aufteilung
    - Die Summe von `grep -cE 'saveUndoState\(|pushUndo\(' ` über die drei Dateien ergibt `2` — beide
      Undo-Aufrufstellen sind erhalten
    - `grep -c "^function showAoEDamageModal" features/initiative.js` liefert `0`
    - `grep -c "features/initiative-combat-widgets.js" loader.js` liefert `1`
    - `PYTHONIOENCODING=utf-8 python build.py` endet mit Exit-Code 0
    - Verhalten: Effekte hinzufügen/entfernen, Todesrettungswürfe klicken, legendäre Aktionen und
      Widerstände abhaken, Konzentration setzen und prüfen, Flächenschaden auf mehrere Ziele anwenden —
      alles unverändert
  </acceptance_criteria>
  <done>Alle drei Dateien liegen unter 800 Zeilen, die Export- und Undo-Aufrufmenge ist vollständig.</done>
</task>

<task type="auto">
  <name>Task 3: Hard-Gate — volle Suiten, beide Bündel, Typprüfung vor dem Commit</name>
  <files>features/initiative.js, features/initiative-loot.js, features/initiative-combat-widgets.js, features/initiative.d.ts, loader.js</files>
  <read_first>
    - `features/initiative.js`, `features/initiative-loot.js`, `features/initiative-combat-widgets.js`
      (Endstand)
    - `loader.js` (`MODULES` mit allen drei Einträgen in Abhängigkeitsreihenfolge)
    - `features/initiative.d.ts` — die vorhandene Typdeklaration; prüfen, ob sie nach der Aufteilung
      noch stimmt oder ob eine Begleitdatei für die neuen Module gebraucht wird
    - `.planning/ROADMAP.md` §Phase 13 → „Auslegungshinweis zum Zuschnitt"
    - `.planning/phases/13-h-rtung-wartbarkeit/13-09-SUMMARY.md` — die im vorigen Aufteilungsplan
      tatsächlich gemessenen Suitenzahlen als Vergleichsbasis
  </read_first>
  <action>
    Führe den vollständigen Gate-Lauf aus, BEVOR die Aufteilung committet wird: volle Jest-Suite,
    Typprüfung, beide `dist`-Bündel, Python-Build-Tests und die volle Playwright-Suite gegen das frisch
    gebaute Entwicklungs-Bündel. Playwright läuft gegen `dist/dnd-tracker-bundled.html`; `npm run build`
    schreibt nur das Produktions-Bündel, deshalb muss `python build.py` (ohne Schalter) unmittelbar
    davor laufen.
    Zur Typprüfung: `features/initiative.d.ts` beschreibt heute die ambienten Konstanten und die
    `Combatant`-Schnittstelle für `features/initiative.js`. Prüfe nach der Aufteilung, ob
    `npx tsc --noEmit` fehlerfrei bleibt. Meldet es einen Fehler, ziehe die Deklarationen nach — eine
    stehengebliebene, jetzt falsche `.d.ts` ist selbst eine irreführende Stelle im Sinne dieser Phase.
    Lege die Ausgabe jedes Laufs im SUMMARY vor — Zahl der bestandenen Tests je Suite, Größe beider
    Bündel, Zeilenzahl aller drei Initiative-Dateien. Eine Behauptung ohne vorgelegte Ausgabe erfüllt
    das Gate nicht (Verbot aus Plan 13-09).
    Erst wenn alle Läufe grün sind, wird committet.
  </action>
  <verify>
    <automated>npx jest</automated>
    <fails_when>Exit-Code ungleich 0, oder die Zusammenfassung meldet mindestens ein `failed`, oder die
    Zahl bestandener Tests liegt unter der in `13-09-SUMMARY.md` festgehaltenen</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>Exit-Code ungleich 0 oder die Ausgabe enthält `error TS`</fails_when>
    <automated>PYTHONIOENCODING=utf-8 python build.py &amp;&amp; PYTHONIOENCODING=utf-8 python build.py --production &amp;&amp; python -m pytest tests/build -q</automated>
    <fails_when>Exit-Code ungleich 0, oder eine Build-Ausgabe enthält `[FEHLER]`/`[ABORTED]`, oder die
    pytest-Zusammenfassung meldet mindestens ein `failed`</fails_when>
    <automated>PYTHONIOENCODING=utf-8 python build.py &amp;&amp; npx playwright test</automated>
    <fails_when>Exit-Code ungleich 0, oder die Zusammenfassung meldet mindestens ein `failed`, oder die
    Zahl bestandener Tests liegt unter 321</fails_when>
  </verify>
  <acceptance_criteria>
    - `npx jest` grün, mindestens so viele bestandene Tests wie in `13-09-SUMMARY.md` festgehalten
    - `npx tsc --noEmit` endet mit Exit-Code 0 ohne `error TS`
    - `npx playwright test` grün mit mindestens 321 bestandenen Tests
    - `python -m pytest tests/build -q` grün mit 24 Tests
    - Beide `dist`-Dateien werden neu geschrieben, keine Ausgabe enthält `[FEHLER]` oder `[ABORTED]`
    - Das SUMMARY nennt für jede Suite die tatsächlich gemessene Zahl und die Zeilenzahl aller drei
      Initiative-Dateien
  </acceptance_criteria>
  <done>Das Hard-Gate ist mit vorgelegter Ausgabe bestanden; die Aufteilung ist verhaltensneutral belegt und darf committet werden.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kampfdaten (`D.initiative`, `D.loot`) → Render-Funktionen → `innerHTML` | Nutzernamen und Beutebeschreibungen fließen in HTML; `esc()` ist die Schutzschicht und muss beim Verschieben Zeile für Zeile mitwandern. |
| `loader.js` `MODULES` → `build.py` → Bündel | Eine fehlende oder falsch eingeordnete Registrierung erzeugt einen abgebrochenen Build oder ein Bündel, in dem eine beim Laden ausgewertete Zeile ihre Abhängigkeit noch nicht sieht. |
| Destruktive Beute-Operation → `pushUndo()` → Undo-Stack | Der Undo-Aufruf in `removeLoot()` muss beim Verschieben erhalten bleiben; sein Verlust bräche die projektweite Undo-Zusage still. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-39 | Tampering | Verlorener `pushUndo()`-Aufruf beim Verschieben von `removeLoot()` | high | mitigate | Die Summe der Undo-Aufrufstellen über alle drei Dateien wird gegen die ursprünglichen 2 abgeglichen. Ein verlorener Aufruf bräche die Zusage „Strg+Z stellt wieder her" für das Beute-System, ohne dass ein Test es meldet. |
| T-13-40 | Denial of Service | Verlorener `window.`-Export beim Verschieben | high | mitigate | Die Vereinigungsmenge der Exporte aller drei Dateien wird gegen die ursprünglichen 34 abgeglichen. |
| T-13-41 | Denial of Service | Beim Laden ausgewertete Zeile ohne ihre Abhängigkeit | medium | mitigate | Die entprellte Fassung von `updateAoETargetDisplay` wandert zusammen mit der Funktion, die sie entprellt; die neue Datei wird hinter `features/initiative.js` registriert, also nach `utils/` und `core/constants.js`. Ein Fehler hier bricht den Start sofort und laut, nicht still. |
| T-13-42 | Tampering | Funktion bleibt in zwei Dateien stehen | medium | mitigate | `check_duplicate_functions()` bricht den Build vor dem Bündeln ab und nennt beide Dateien; `python build.py` läuft nach jeder Verschiebung (D-06, RESEARCH Pitfall 4). |
| T-13-43 | Repudiation | Stehengebliebene, jetzt falsche `features/initiative.d.ts` | medium | mitigate | `npx tsc --noEmit` ist Teil des Hard-Gates; meldet es einen Fehler, wird die Deklaration nachgezogen statt ignoriert. Eine falsche Typdeklaration ist selbst eine irreführende Stelle im Sinne dieser Phase. |
| T-13-SC | Tampering | npm/pip/cargo installs | low | accept | Dieser Plan installiert kein Paket (`13-RESEARCH.md` → „Package Legitimacy Audit: Nicht anwendbar"). |
</threat_model>

<verification>
1. `wc -l` aller drei Initiative-Dateien liefert je einen Wert ≤ 800
2. Exportsumme über die drei Dateien = 34; Undo-Aufrufsumme = 2
3. `grep -c "features/initiative" build.py` liefert `0`; beide neuen Pfade stehen in `loader.js`
4. `npx jest` grün, nicht weniger Tests als in `13-09-SUMMARY.md`
5. `npx tsc --noEmit` ohne `error TS`
6. `PYTHONIOENCODING=utf-8 python build.py && PYTHONIOENCODING=utf-8 python build.py --production && python -m pytest tests/build -q` grün
7. `PYTHONIOENCODING=utf-8 python build.py && npx playwright test` grün, ≥ 321 Tests
</verification>

<success_criteria>
- Erfolgskriterium 4, zweites Viertel: `features/initiative.js` ist entlang seiner Sektionsbanner in
  drei Module aufgeteilt, keines über 800 Zeilen, das Verhalten unverändert
- Registrierung ausschließlich in `loader.js`, in Abhängigkeitsreihenfolge
- Volles Suiten-Gate mit vorgelegter Ausgabe vor dem Commit
</success_criteria>

<output>
Create `.planning/phases/13-h-rtung-wartbarkeit/13-10-SUMMARY.md` when done
</output>
