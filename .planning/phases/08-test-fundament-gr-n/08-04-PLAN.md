---
phase: 08-test-fundament-gr-n
plan: 04
type: execute
wave: 4
depends_on: [08-03]
files_modified:
  - .github/workflows/ci.yml
  - docs/e2e-failure-triage.md
autonomous: true
requirements: [TEST-02]

must_haves:
  truths:
    - "A new blocking e2e job in ci.yml runs a dev build then npx playwright test (Chromium, file://) with no continue-on-error, and uploads Playwright report + test-results on failure"
    - "A red e2e run transitively blocks deploy — the e2e job is wired into the build/smoke-test/deploy needs chain"
    - "docs/e2e-failure-triage.md records, for every former fail, its classification (Test-Bug vs App-Bug), one-sentence root cause, fix description, and commit hash (D-07)"
    - "The Cluster 3 '9x DOM duplication' theory is marked corrected/superseded (it was Playwright's retry-poll counter; live DOM count is always 1); Cluster 4 (Undo-nach-Delete) is marked resolved-by-prior-work with today's baseline as evidence (no code fix)"
  artifacts:
    - ".github/workflows/ci.yml contains an e2e: job (dev build + playwright install --with-deps chromium + npx playwright test + upload-artifact on failure)"
    - "docs/e2e-failure-triage.md has a dated Phase 8 resolution section covering all former fails plus the two corrected/resolved cluster notes"
  key_links:
    - "e2e job needs [lint-and-typecheck, test] -> runs its own python build.py (dev) -> npx playwright test against file:// per playwright.config.js (CI retries=2, workers=1)"
    - "build.needs includes e2e -> red e2e blocks build -> blocks smoke-test -> blocks deploy (blockierend per D-03)"
  prohibitions:
    - statement: "The e2e CI job must NOT be made non-blocking (no continue-on-error) — D-03 requires a blocking gate"
      status: flagged-unverified
    - statement: "The gate must NOT be flipped on while the suite is red — the full suite must be green twice before this plan finalizes"
      status: flagged-unverified
    - statement: "The e2e job must NOT run the production build or an HTTP server — D-03 requires the dev build and file:// per playwright.config.js"
      status: flagged-unverified
  flagged_assumptions:
    - requirement: TEST-02
      status: unresolved
      note: "Edge-probe left TEST-02 unclassified (unresolved). Surfaced as a flagged assumption: TEST-02's 'CI-tauglicher Gate' criterion is treated as proven by the e2e job running the same full-suite command already green twice locally, wired blocking into the deploy chain — not by a probed edge predicate. Actual GitHub-runner execution and branch-protection are verified on push / in repo settings (outside ci.yml). Reviewer should confirm."
---

<objective>
Make the green suite a trustworthy, blocking CI gate (D-03) and document every former fail's resolution (D-07). This runs last, after 08-03's hardening, so the gate is flipped on only once the full suite is green twice (the phase's whole point is gate trustworthiness) and the triage doc can cite real fix commit hashes from 08-01/08-02/08-03.

Purpose: Prove Success Criterion 5 ("als CI-Gate nutzbar") rather than assert it, and leave a single durable record (per D-07) of what each of the freshly-measured fails was and how it was resolved — including correcting the stale "9x DOM duplication" theory and noting the resolved Undo-nach-Delete cluster.
Output: A blocking `e2e` job in `ci.yml` wired into the deploy chain, and an appended Phase-8 section in `docs/e2e-failure-triage.md`.

FLAGGED ASSUMPTION (edge-probe unresolved): TEST-02 was left `unclassified` by the deterministic edge-probe and is surfaced here as an explicit flagged assumption (see frontmatter) — not silently dropped.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-test-fundament-gr-n/08-RESEARCH.md
@.planning/phases/08-test-fundament-gr-n/08-CONTEXT.md
@.github/workflows/ci.yml
@playwright.config.js
@docs/e2e-failure-triage.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Blockierender e2e-CI-Job (D-03)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the existing `lint-and-typecheck`, `test`, `build`, `smoke-test`, `deploy` jobs; note `smoke-test` runs the PRODUCTION build via a python HTTP server + SMOKE_BASE_URL — the new e2e job must NOT copy that server/env pattern; the current chain is build.needs=[lint-and-typecheck,test], smoke-test.needs=[build], deploy.needs=[smoke-test])
    - playwright.config.js (E2E defaults: file:// baseURL against `dist/dnd-tracker-bundled.html`, CI `retries:2` / `workers:1`, reporter html -> `tests/e2e/reports`, `outputDir: tests/e2e/test-results`)
    - 08-RESEARCH.md — "CI job addition for D-03" (ready-to-paste YAML) and Open Question 1 (whether e2e should transitively block deploy)
  </read_first>
  <action>
    Add a new `e2e:` job to `.github/workflows/ci.yml`, modeled on the existing `smoke-test` job's step sequence (checkout@v4, setup-node@v4 node '20' + cache npm, setup-python@v5 python '3.x', `npm ci`) but with these D-03-required differences: `needs: [lint-and-typecheck, test]`; run `python build.py` (the DEV bundle — NOT `--production`, per D-03); then `npx playwright install --with-deps chromium`; then `npx playwright test` (uses playwright.config.js defaults — file:// baseURL, CI retries=2/workers=1; NO HTTP server, NO SMOKE_BASE_URL env). Add a final `actions/upload-artifact@v4` step guarded by `if: failure()` uploading `tests/e2e/reports/` and `tests/e2e/test-results/` with `retention-days: 7`. Do NOT add continue-on-error. Per RESEARCH Open Question 1, wire the gate to be blocking as far as ci.yml can express: add `e2e` to the `build` job's `needs:` array so a red e2e transitively blocks build -> smoke-test -> deploy. Add a short YAML comment noting that requiring the `e2e` check before merge is a GitHub branch-protection setting outside ci.yml.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.github/workflows/ci.yml','utf8'); const ok = /\n\s*e2e:/.test(s) && /npx playwright install --with-deps chromium/.test(s) && /npx playwright test/.test(s) && /if:\s*failure\(\)/.test(s) && !/continue-on-error/.test(s); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains an `e2e:` job that runs `python build.py` (dev, without `--production`), `npx playwright install --with-deps chromium`, and `npx playwright test`
    - The `e2e` job has no `continue-on-error` and no `SMOKE_BASE_URL`/HTTP-server step
    - The `e2e` job has an `actions/upload-artifact@v4` step with `if: failure()` pointing at `tests/e2e/reports/` and `tests/e2e/test-results/`
    - The `build` job `needs:` array includes `e2e` (red e2e transitively blocks deploy)
    - `ci.yml` parses (the node-based verify command exits 0)
    - Running the job's own commands locally — `npm run build:dev && npx playwright test` — exits 0 (the same full-suite command proven green in 08-03)
  </acceptance_criteria>
  <done>The full E2E suite runs as a blocking CI job on the dev bundle; a red run blocks the deploy chain; failure artifacts are captured.</done>
</task>

<task type="auto">
  <name>Task 2: Triage-Dokumentation je Fail + Cluster-Korrekturen (D-07)</name>
  <files>docs/e2e-failure-triage.md</files>
  <read_first>
    - docs/e2e-failure-triage.md (existing prose "Cluster 1-6" sections + the stale "9x duplicate #random-tables-list" note in Cluster 3 and the "Undo may not restore" suspicion in Cluster 4 — match the existing prose-section format, do not introduce a table if the doc uses prose)
    - 08-RESEARCH.md — all Pitfalls (1-8), the "Cluster 4 (Undo-nach-Delete) is resolved" section, and Pitfall 6's empirical disproof of the "9x DOM duplication" theory (it was Playwright's retry-poll counter; live DOM count is always 1)
    - 08-CONTEXT.md — D-07 format: per fail = Klassifikation (Test-Bug | App-Bug) + one-sentence root cause + fix description + commit hash
    - The 08-01/08-02/08-03 SUMMARY files (for the actual fix commit hashes) — or `git log` on the touched files if a SUMMARY lacks a hash
  </read_first>
  <action>
    Append a new dated "Phase 8 — Resolution (2026-07)" section to `docs/e2e-failure-triage.md`, in the existing prose-section style. For EACH former fail, record its Klassifikation + one-sentence root cause + fix description + commit hash: (App-Bug) character/encounter attribute-modifier badge — duplicate `update-attr-mod`/`update-enc-attr-mod` registration in combat-actions.js overwrote the correct entity-actions.js handler; (App-Bug) global-search unclickable — fixed migration-hint-banner with no layout offset covered the header; (App-Bug) undo/redo left dice/timers panels stale — renderAll() omitted renderRandomTables/renderTimers; (Test-Bug) tab-navigation stale classes — `.init-combatant`/`.party-member` never matched real `.init-entry`/`.char-card`; (Test-Bug) dice panel hidden — ambiguous `.dice-details` selector opened the wrong native <details>; (Test-Bug) timers test — wrote to non-existent `D.timers` and asserted stale `.timer-item`; (Test-Bug) validation toast-race — onboarding setTimeout overwrote the validation error in the shared #toast node. Then add two documentation-only corrections: mark Cluster 3's "9x DOM duplication" theory as CORRECTED/superseded (Playwright retry-poll counter, not DOM nodes; live count always 1), and mark Cluster 4 (Undo-nach-Delete) as RESOLVED by prior work — all 5 "Löschen kann rückgängig gemacht werden" tests pass in the current baseline — with no code fix needed. Use the actual commit hashes from this phase's fix commits.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('docs/e2e-failure-triage.md','utf8'); const ok = /Phase 8/.test(s) && /Test-Bug/.test(s) && /App-Bug/.test(s) && /(retry-poll|retry poll|Retry-Poll)/.test(s); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/e2e-failure-triage.md` contains a new dated Phase 8 resolution section (grep finds `Phase 8`)
    - The section classifies every former fail as `Test-Bug` or `App-Bug` and includes a root cause, fix, and commit hash for each (grep finds `Test-Bug`, `App-Bug`, and commit-hash references)
    - Cluster 3's "9x DOM duplication" theory is explicitly marked corrected/superseded, referencing the retry-poll-counter explanation
    - Cluster 4 (Undo-nach-Delete) is marked resolved-by-prior-work with the current baseline as evidence (no code fix)
  </acceptance_criteria>
  <done>Every former fail has a durable, classified, commit-linked triage entry; the two stale cluster theories are corrected in the same document.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → npm/registry (npm ci, playwright install) | The e2e job pulls already-pinned dependencies and a browser binary |
| CI gate → deploy chain | A non-blocking or misconfigured gate could let a red suite reach deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-08 | Tampering | e2e job supply chain (`npm ci`, `npx playwright install --with-deps`) | medium | mitigate | Reuse existing smoke-test trust model: pinned actions (@v4/@v5), lockfile-pinned `npm ci`, Chromium-only install; no NEW package is added this phase (no `T-08-SC` checkpoint needed) |
| T-08-09 | Elevation of Privilege (gate bypass) | e2e job made non-blocking or absent from the deploy chain | medium | mitigate | No continue-on-error (D-03); add `e2e` to `build.needs` so a red run blocks build -> smoke-test -> deploy; comment flags branch-protection as the out-of-repo complement |

No new external package is introduced (`npm ci` uses the existing lockfile); no new user-input, persistence, or runtime attack surface is added by CI/docs changes.
</threat_model>

<verification>
- The e2e job's own commands run green locally: `npm run build:dev && npx playwright test` exits 0 twice (phase gate: full suite green twice before `/gsd-verify-work`)
- `ci.yml` parses and contains the blocking `e2e` job wired into `build.needs` with no continue-on-error
- `docs/e2e-failure-triage.md` has a complete Phase-8 section (every former fail classified + commit-linked) plus the Cluster 3 correction and Cluster 4 resolved note
- Actual GitHub-runner execution is confirmed on the next push (outside local verification); branch-protection requiring the `e2e` check is a repo-settings follow-up flagged in the job comment
</verification>

<success_criteria>
- D-03 satisfied: blocking `e2e` job (dev build + full Playwright suite, no continue-on-error) wired into the deploy chain — Success Criterion 5 proven, not asserted
- D-07 satisfied: single durable triage record with per-fail classification, root cause, fix, and commit hash; stale Cluster 3/4 theories corrected
- TEST-02's CI-gate requirement met; the suite is green twice before the gate is finalized
</success_criteria>

## Artifacts this plan produces

New symbols/sections created by this plan (for downstream drift-verification exclusion):
- New CI job name `e2e` in `.github/workflows/ci.yml`, and `e2e` added to the `build` job's `needs:` array
- New dated "Phase 8 — Resolution (2026-07)" section in `docs/e2e-failure-triage.md`, plus corrected Cluster 3 / resolved Cluster 4 notes

<output>
Create `.planning/phases/08-test-fundament-gr-n/08-04-SUMMARY.md` when done
</output>
