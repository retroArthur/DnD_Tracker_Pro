---
phase: 14-tests-gates
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - tests/e2e/helpers/test-utils.js
  - tests/e2e/crud/party.spec.js
  - tests/e2e/crud/npcs.spec.js
  - tests/e2e/crud/quests.spec.js
  - tests/e2e/crud/locations.spec.js
  - tests/e2e/crud/encounters.spec.js
  - .planning/phases/14-tests-gates/14-TOAST-RACE-MEASUREMENT.md
autonomous: true
requirements: [TEST-03]

estimate:
  tokens: 50000
  raw_tokens: 100000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "`seedCleanSession(page)` existiert genau einmal, als Export in `tests/e2e/helpers/test-utils.js`; im gesamten Baum `tests/e2e/crud/` steht keine Kopie des 61-zeiligen Seed-Blocks mehr."
    - "Alle fuenf CRUD-Specs (`party`, `npcs`, `quests`, `locations`, `encounters`) rufen `seedCleanSession(page)` in ihrem `beforeEach` auf."
    - "Der 40-zeilige Ursachenkommentar aus Plan 08-02 steht im Helfer — die einzige Stelle im Repo, die erklaert, warum geseedet wird, geht bei der Extraktion nicht verloren."
    - "In allen fuenf Specs steht der Aufruf von `seedCleanSession(page)` VOR dem Aufruf von `loadApp(page)`; die Reihenfolge ist festgelegt und per Quelltextpruefung nachweisbar."
    - "Auf einem frischen Playwright-Kontext ohne vorherigen localStorage-Inhalt schreibt `seedCleanSession(page)` den vollstaendigen Seed; die fuenf CRUD-Specs laufen aus einem leeren Profil gruen."
    - "`seedCleanSession(page)` ist idempotent: ein Wiederholungslauf mit `--repeat-each` und mehreren parallelen Workern erzeugt in jedem Worker denselben Ausgangszustand; wiederholte Aufrufe im selben Kontext ueberschreiben denselben localStorage-Schluessel und erzeugen keinen abweichenden Zustand."
    - "Ein protokollierter Lastlauf mit `--retries=0` gegen den ungefixten Stand ist ROT; derselbe Lauf mit identischen Parametern gegen den gefixten Stand ist GRUEN. Beide Ausgaben liegen als Artefakt vor."
    - "`playwright.config.js` ist unveraendert: `retries: process.env.CI ? 2 : 0` bleibt stehen (D-03)."
  artifacts:
    - tests/e2e/helpers/test-utils.js
    - .planning/phases/14-tests-gates/14-TOAST-RACE-MEASUREMENT.md
  key_links:
    - "`tests/e2e/helpers/test-utils.js` `seedCleanSession` -> `beforeEach` in fuenf CRUD-Specs -> `page.addInitScript` -> localStorage vor dem App-Boot -> `initRandomTables()`/`validateDataIntegrity()` loesen keinen fruehen `save()`-Toast mehr aus -> die Assertionen an `locations.spec.js:80` und `encounters.spec.js:131-132` sehen ihren eigenen Toast"
  prohibitions:
    - "Der Nachweis zur Toast-Race darf nicht ohne roten Vorlauf abgenommen werden; ein gruener Nachlauf allein beweist nichts ueber den Fix."
    - "Retries, hoehere Timeouts, `waitForTimeout`-Verlaengerungen oder `test.skip` duerfen nicht eingesetzt werden, um den Lauf gruen zu bekommen — der fehlende Seed ist der Defekt, nicht die Wiederholungszahl."
---

<objective>
Die latente Toast-Race in `tests/e2e/crud/locations.spec.js` und `encounters.spec.js` schliessen,
indem der bereits dreifach vorhandene, byte-identische Seed-Block als
`seedCleanSession(page)` nach `tests/e2e/helpers/test-utils.js` wandert und von allen fuenf
CRUD-Specs aufgerufen wird — und den Fix mit einem protokollierten Vorher/Nachher-Lastlauf
belegen statt behaupten.

Purpose: Die drei bestehenden Bloecke sind byte-identisch (je 61 Zeilen, gleiche
MD5-Pruefsumme). Genau das macht die Extraktion risikoarm — der teure Teil einer Extraktion ist
das Zusammenfuehren abweichender Varianten, und den gibt es hier nicht. Eine vierte und fuenfte
Kopie ergaeben 305 Zeilen dupliziertes Setup fuer zwei Assertionen (D-01). Der Nachweis folgt der
wiederkehrenden Projekterfahrung aus `08-LEARNINGS.md` und `11-LEARNINGS.md`: ein gruener Test
ist kein Beweis. Deshalb ist der rote Vorlauf gegen den ungefixten Stand Pflicht (D-02), und die
CI-Retries bleiben unangetastet (D-03) — abgeschaltet wird nur im Beweislauf, per Kommandozeile.

Output: `seedCleanSession(page)` in `test-utils.js`, fuenf angepasste Specs, und
`.planning/phases/14-tests-gates/14-TOAST-RACE-MEASUREMENT.md` mit beiden Laeufen.

**Bewusste Abweichung von Erfolgskriterium 1 der Roadmap, benannt statt verschwiegen:** Dort
steht, die beiden Specs „tragen den Seed-Nachzug aus Plan 08-02". Sie tragen kuenftig den
*Aufruf*, nicht die Kopie. Sachlich identisch (D-01) — hier festgehalten, damit die Verifikation
es nicht als Luecke liest.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-tests-gates/14-CONTEXT.md
@.planning/phases/14-tests-gates/14-RESEARCH.md
@.planning/phases/14-tests-gates/14-PATTERNS.md
@.planning/milestones/v1.1-phases/08-test-fundament-gr-n/08-02-SUMMARY.md
@.planning/phases/13-h-rtung-wartbarkeit/13-PERF-MEASUREMENT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Falsifikationsprobe — den ungefixten Stand nachweislich ROT bekommen</name>
  <precondition>Ein aktueller Dev-Bundle liegt unter `dist/dnd-tracker-bundled.html`; er wird von `PYTHONIOENCODING=utf-8 python build.py` erzeugt und ist Voraussetzung fuer jeden Playwright-Lauf gegen `file://`.</precondition>
  <files>.planning/phases/14-tests-gates/14-TOAST-RACE-MEASUREMENT.md</files>
  <read_first>
    - `tests/e2e/crud/locations.spec.js` (Zeilen 1-25 und Zeile 80) — der seedlose `beforeEach` und die Assertion, die unter Last kippt
    - `tests/e2e/crud/encounters.spec.js` (Zeilen 1-25 und Zeilen 125-135) — dasselbe, zweite Fundstelle
    - `playwright.config.js` (Zeilen 25-40) — `retries` und `workers`; beide werden hier ausschliesslich per Kommandozeile ueberschrieben, nicht in der Datei
    - `.planning/phases/13-h-rtung-wartbarkeit/13-PERF-MEASUREMENT.md` — die Form des Messprotokolls, der dieses Artefakt folgt
    - `.planning/milestones/v1.1-phases/08-test-fundament-gr-n/08-RESEARCH.md` § Pitfall 4 — die Ursachenanalyse hinter dem Seed
  </read_first>
  <action>
    In diesem Task wird KEIN Quelltext veraendert. Es wird ausschliesslich gemessen, und zwar
    gegen den heutigen, ungefixten Stand von `locations.spec.js` und `encounters.spec.js` —
    deshalb steht dieser Task vor der Extraktion und nicht danach.

    Zuerst `PYTHONIOENCODING=utf-8 python build.py` ausfuehren, damit der Bundle aktuell ist.

    Dann eine Laststufenleiter fahren, jeweils mit `--retries=0`, weil bei den sonst ueblichen
    zwei Wiederholungen ein Flake den Lauf nicht rot machen kann und der Beweis keiner waere.
    Zielbefehl in allen Stufen: `npx playwright test tests/e2e/crud/locations.spec.js
    tests/e2e/crud/encounters.spec.js --retries=0` plus die Stufenparameter. Stufen in dieser
    Reihenfolge, Abbruch bei der ersten roten Stufe: `--repeat-each=5 --workers=4`, dann
    `--repeat-each=10 --workers=8`, dann `--repeat-each=20 --workers=8`, dann
    `--repeat-each=40 --workers=8`. Fuer den maschinenlesbaren Beleg zusaetzlich
    `--reporter=json,list` mit gesetzter Umgebungsvariablen `PLAYWRIGHT_JSON_OUTPUT_NAME` auf
    einen Dateinamen je Stufe; das ist der dokumentierte Weg, den JSON-Report umzuleiten, ohne
    `playwright.config.js` anzufassen.

    Rot heisst hier konkret: mindestens ein Fehlschlag, dessen Meldung an der
    Toast-Assertion in `locations.spec.js` oder `encounters.spec.js` haengt. Ein Fehlschlag aus
    einer anderen Ursache (Bundle fehlt, Browser nicht installiert) zaehlt nicht und ist zuerst
    zu beseitigen.

    `.planning/phases/14-tests-gates/14-TOAST-RACE-MEASUREMENT.md` anlegen und die erste Haelfte
    fuellen: Datum, Commit-Kurz-Hash des gemessenen Standes, je Stufe der wortgetreue Befehl, die
    Zusammenfassungszeile von Playwright, und bei der roten Stufe der Ausschnitt der
    Fehlermeldung inklusive Datei und Zeilennummer. Die Stufe, die rot wurde, wird als
    „Beweisparameter" markiert — Task 3 fahren exakt diese Parameter erneut.

    Bleibt auch die hoechste Stufe gruen, ist der Task NICHT erfuellt: dann ist das Verfahren zu
    wechseln (etwa gezielte CPU-Last waehrend des Laufs oder eine hoehere Worker-Zahl als
    Kerne), und das Ergebnis jedes Versuchs kommt ins Protokoll. Es wird in keinem Fall zur
    Extraktion uebergegangen, solange kein roter Vorlauf vorliegt.

    Die JSON-Reportdateien sind Zwischenergebnisse und werden nicht eingecheckt; ihre relevanten
    Zahlen stehen im Protokoll.
  </action>
  <verify>
    <automated>PYTHONIOENCODING=utf-8 python build.py</automated>
    <fails_when>non-zero exit, oder die Ausgabe enthaelt "[FEHLER]" oder "[ABORTED]"</fails_when>
    <automated>test -f .planning/phases/14-tests-gates/14-TOAST-RACE-MEASUREMENT.md</automated>
    <fails_when>non-zero exit — das Protokoll wurde nicht angelegt</fails_when>
    <automated>grep -q 'Beweisparameter' .planning/phases/14-tests-gates/14-TOAST-RACE-MEASUREMENT.md</automated>
    <fails_when>non-zero exit — im Protokoll ist keine Stufe als Beweisparameter markiert, es gibt also keinen roten Vorlauf</fails_when>
    <automated>git diff --quiet -- tests/e2e playwright.config.js</automated>
    <fails_when>non-zero exit — in diesem Messtask wurde Testcode oder die Playwright-Konfiguration veraendert</fails_when>
  </verify>
  <acceptance_criteria>
    - Das Protokoll nennt fuer jede gefahrene Laststufe den wortgetreuen Befehl und die Playwright-Zusammenfassungszeile.
    - Mindestens eine Stufe ist rot, und der protokollierte Fehlerauszug nennt entweder `locations.spec.js` oder `encounters.spec.js` mit Zeilennummer.
    - Die rote Stufe ist im Protokoll ausdruecklich als Beweisparameter benannt, mit `--repeat-each`- und `--workers`-Wert.
    - Alle Befehle im Protokoll enthalten `--retries=0`.
    - `git diff` zeigt fuer `tests/e2e/**` und `playwright.config.js` keine Aenderung.
  </acceptance_criteria>
  <done>Der ungefixte Stand ist unter protokollierter Last reproduzierbar rot; die Beweisparameter stehen fest.</done>
</task>

<task type="auto">
  <name>Task 2: `seedCleanSession(page)` extrahieren und alle fuenf CRUD-Specs darauf umstellen (D-01)</name>
  <files>tests/e2e/helpers/test-utils.js, tests/e2e/crud/party.spec.js, tests/e2e/crud/npcs.spec.js, tests/e2e/crud/quests.spec.js, tests/e2e/crud/locations.spec.js, tests/e2e/crud/encounters.spec.js</files>
  <read_first>
    - `tests/e2e/crud/party.spec.js` (Zeilen 1-85) — der vollstaendige Seed-Block inklusive des 40-zeiligen Ursachenkommentars und der `beforeEach`-Umgebung
    - `tests/e2e/crud/npcs.spec.js` und `tests/e2e/crud/quests.spec.js` (je Zeilen 1-85) — die beiden byte-identischen Kopien; abweichend sind nur `navigateToTab`-Ziel und ein etwaiger `waitForTimeout`
    - `tests/e2e/helpers/test-utils.js` (Zeilen 1-30 und 176-205) — die Exportkonvention: `export async function`, JSDoc mit `@param {import('@playwright/test').Page} page`, deutsche Kommentare; `loadApp` und `clearAppData` als Muster
    - `tests/e2e/crud/locations.spec.js` und `tests/e2e/crud/encounters.spec.js` (je Zeilen 1-25) — die beiden Importlisten und die seedlosen `beforeEach`-Bloecke
  </read_first>
  <action>
    In `tests/e2e/helpers/test-utils.js` eine neue exportierte Funktion `seedCleanSession(page)`
    anlegen. Der Funktionskoerper ist der Seed-Block aus `party.spec.js` woertlich uebernommen —
    einschliesslich des vollstaendigen Ursachenkommentars, der erklaert, warum geseedet wird
    (der fruehe `save()` aus `initRandomTables()` und `validateDataIntegrity()`, dessen
    Backup-Toast den geteilten Toast-Knoten ueberschreibt, plus der Verweis auf 08-RESEARCH
    Pitfall 4). Dieser Kommentar ist die einzige Stelle im Repo, die den Grund dokumentiert;
    ihn bei der Extraktion zu verlieren waere der eigentliche Schaden (D-01). Er wandert
    ungekuerzt mit, als Kopfkommentar der Funktion.

    Signatur und Form folgen den Nachbarn in derselben Datei: JSDoc mit dem `page`-Parameter,
    ein Satz Zweckbeschreibung auf Deutsch, `export async function`. Platzierung sinnvollerweise
    unmittelbar vor `loadApp`, weil der Aufruf immer davor steht.

    Danach die fuenf Specs verdrahten:
    - In `party.spec.js`, `npcs.spec.js` und `quests.spec.js` den lokalen Seed-Block ersatzlos
      entfernen und durch einen `await seedCleanSession(page);` unmittelbar vor
      `await loadApp(page);` ersetzen. Alles andere im `beforeEach` bleibt unveraendert, auch ein
      dort vorhandener `waitForTimeout`.
    - In `locations.spec.js` und `encounters.spec.js` denselben Aufruf neu hinzufuegen, ebenfalls
      unmittelbar vor `await loadApp(page);`. Sonst wird in diesen beiden Dateien nichts
      geaendert — insbesondere keine Timeouts, keine Assertion, kein `test.skip`.
    - In allen fuenf Dateien `seedCleanSession` in die bestehende Importliste aus
      `../helpers/test-utils.js` aufnehmen; die vorhandene alphabetische bzw. bestehende
      Reihenfolge der Importnamen beibehalten.

    `playwright.config.js` wird nicht angefasst: `retries: process.env.CI ? 2 : 0` bleibt
    unveraendert (D-03). Die Retries sind nicht der Defekt, der fehlende Seed ist es; sie auf 0
    zu setzen wuerde jede Infrastruktur-Stoerung auf `main` rot machen, ohne eine Race zu
    schliessen.

    Zur Kontrolle der Verhaltensneutralitaet: der extrahierte Koerper muss zeichengleich mit dem
    bisherigen Block sein. Vor dem Entfernen der Kopien die MD5-Summe des Blocks aus einer der
    drei Dateien notieren und nach der Extraktion gegen den entsprechenden Ausschnitt des Helfers
    pruefen; Abweichungen ausser Einrueckung sind zu beseitigen.
  </action>
  <verify>
    <automated>test "$(grep -c 'export async function seedCleanSession' tests/e2e/helpers/test-utils.js)" -eq 1</automated>
    <fails_when>non-zero exit — der Helfer fehlt oder existiert mehrfach</fails_when>
    <automated>test "$(grep -l 'await seedCleanSession(page);' tests/e2e/crud/party.spec.js tests/e2e/crud/npcs.spec.js tests/e2e/crud/quests.spec.js tests/e2e/crud/locations.spec.js tests/e2e/crud/encounters.spec.js | wc -l)" -eq 5</automated>
    <fails_when>non-zero exit — weniger als fuenf der CRUD-Specs rufen den Helfer auf</fails_when>
    <automated>test "$(grep -c 'addInitScript' tests/e2e/crud/party.spec.js tests/e2e/crud/npcs.spec.js tests/e2e/crud/quests.spec.js tests/e2e/crud/locations.spec.js tests/e2e/crud/encounters.spec.js | grep -vc ':0$')" -eq 0</automated>
    <fails_when>non-zero exit — in mindestens einer CRUD-Spec steht noch eine lokale Seed-Kopie</fails_when>
    <automated>node -e "const fs=require('fs');const f=['party','npcs','quests','locations','encounters'];let bad=[];for(const n of f){const s=fs.readFileSync('tests/e2e/crud/'+n+'.spec.js','utf8');const a=s.indexOf('seedCleanSession(page)');const b=s.indexOf('loadApp(page)');if(a<0||b<0||a>b)bad.push(n);}if(bad.length){console.log('Reihenfolge falsch in:',bad.join(', '));process.exit(1)}console.log('Reihenfolge in allen fuenf Specs korrekt')"</automated>
    <fails_when>non-zero exit, oder die Ausgabe beginnt mit "Reihenfolge falsch in:" — der Seed steht nicht vor dem App-Start</fails_when>
    <automated>git diff --quiet -- playwright.config.js</automated>
    <fails_when>non-zero exit — die Playwright-Konfiguration wurde entgegen D-03 veraendert</fails_when>
    <automated>PYTHONIOENCODING=utf-8 python build.py && npx playwright test tests/e2e/crud/</automated>
    <fails_when>non-zero exit, oder die Zusammenfassung enthaelt "failed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/helpers/test-utils.js` exportiert genau eine Funktion `seedCleanSession`, deren Kopfkommentar die Begriffe `initRandomTables`, `validateDataIntegrity` und den Verweis auf 08-RESEARCH Pitfall 4 enthaelt.
    - In keiner der fuenf CRUD-Specs steht noch ein `addInitScript`-Aufruf.
    - In allen fuenf Specs steht der Helferaufruf im Quelltext vor dem `loadApp`-Aufruf.
    - `git diff tests/e2e/crud/locations.spec.js tests/e2e/crud/encounters.spec.js` zeigt ausschliesslich eine ergaenzte Importbezeichnung und eine ergaenzte Aufrufzeile je Datei — keine geaenderte Assertion, kein geaenderter Timeout.
    - `playwright.config.js` steht unveraendert in `git status`.
    - `npx playwright test tests/e2e/crud/` ist gruen.
  </acceptance_criteria>
  <done>Der Seed existiert genau einmal, wird von fuenf Specs aufgerufen, und die CRUD-Suite ist gruen.</done>
  <reversibility rating="reversible">Der Helfer liesse sich jederzeit wieder in die Aufrufer ausrollen; die drei Ausgangskopien sind byte-identisch und in der Git-Historie erhalten (CONTEXT.md D-01).</reversibility>
</task>

<task type="auto">
  <name>Task 3: Nachlauf mit identischen Beweisparametern und Abschluss des Messprotokolls (D-02)</name>
  <files>.planning/phases/14-tests-gates/14-TOAST-RACE-MEASUREMENT.md</files>
  <read_first>
    - `.planning/phases/14-tests-gates/14-TOAST-RACE-MEASUREMENT.md` — die in Task 1 als Beweisparameter markierte Laststufe; sie wird hier unveraendert wiederholt
    - `.planning/phases/13-h-rtung-wartbarkeit/13-PERF-MEASUREMENT.md` — die Abschlussform: Vergleichstabelle plus ein Absatz Interpretation
  </read_first>
  <action>
    Den Bundle mit `PYTHONIOENCODING=utf-8 python build.py` neu bauen und danach exakt den
    Befehl aus dem Abschnitt „Beweisparameter" des Protokolls wiederholen — gleiche Dateien,
    gleicher `--repeat-each`-Wert, gleiche Worker-Zahl, weiterhin `--retries=0`. Es wird nichts
    an den Parametern nachjustiert; jede Abweichung entwertet den Vergleich.

    Zusaetzlich denselben Befehl ein zweites Mal fahren, damit ein einzelner gluecklicher Lauf
    nicht als Beweis durchgeht. Beide Laeufe muessen gruen sein.

    Das Protokoll um die zweite Haelfte ergaenzen: Datum, Commit-Kurz-Hash des gefixten Standes,
    die beiden Zusammenfassungszeilen, und eine Vergleichstabelle mit den Spalten Laststufe,
    Stand (ungefixt/gefixt), Ergebnis, Anzahl Tests, Anzahl Fehlschlaege. Darunter ein Absatz
    Interpretation, der ausdruecklich festhaelt: der Vorlauf war rot, der Nachlauf ist gruen, die
    Parameter waren identisch, und die CI-Retries blieben unveraendert (der Beweis lief ohne sie).

    Abschliessend die volle E2E-Suite einmal regulaer fahren, um auszuschliessen, dass die
    Extraktion anderswo etwas verschoben hat.
  </action>
  <verify>
    <automated>PYTHONIOENCODING=utf-8 python build.py</automated>
    <fails_when>non-zero exit, oder die Ausgabe enthaelt "[FEHLER]" oder "[ABORTED]"</fails_when>
    <automated>grep -q 'Nachlauf' .planning/phases/14-tests-gates/14-TOAST-RACE-MEASUREMENT.md</automated>
    <fails_when>non-zero exit — die zweite Haelfte des Protokolls fehlt</fails_when>
    <automated>npx playwright test</automated>
    <fails_when>non-zero exit, oder die Zusammenfassung enthaelt "failed"</fails_when>
    <automated>npx jest</automated>
    <fails_when>non-zero exit, oder die Zusammenfassung enthaelt "failed"</fails_when>
    <human-check>
      Beweisfuehrung freigeben (laut `14-VALIDATION.md` eine ausdruecklich manuelle Abnahme — ein
      einmaliger, protokollierter Vergleich zweier Codestaende, kein wiederholbarer
      Regressionstest):
      1. `.planning/phases/14-tests-gates/14-TOAST-RACE-MEASUREMENT.md` oeffnen.
      2. Steht dort ein ROTER Vorlauf gegen den ungefixten Stand, mit Fehlerauszug und
         Zeilennummer aus `locations.spec.js` oder `encounters.spec.js`?
      3. Sind die Parameter von Vor- und Nachlauf zeichengleich (gleiche Dateien, gleiches
         `--repeat-each`, gleiche `--workers`, beide `--retries=0`)?
      4. Sind beide Nachlaeufe gruen?
      5. Ist `playwright.config.js` unveraendert (D-03: Retries unangetastet)?
      Freigabe nur, wenn alle vier Pruefpunkte zutreffen. Trifft Punkt 2 nicht zu, ist der
      Nachweis nicht erbracht: dann ist die Falsifikationsprobe zu wiederholen, nicht
      abzunehmen.
    </human-check>
  </verify>
  <acceptance_criteria>
    - Das Protokoll enthaelt eine Vergleichstabelle mit je einer Zeile fuer den ungefixten und den gefixten Stand bei identischer Laststufe.
    - Beide Nachlaeufe sind mit Zusammenfassungszeile protokolliert und beide gruen.
    - Der Interpretationsabsatz nennt ausdruecklich, dass die Parameter zwischen Vor- und Nachlauf identisch waren und `--retries=0` in beiden galt.
    - `npx playwright test` (volle Suite) ist gruen; die Zahl bestandener Tests ist nicht kleiner als der in `14-GATE-BASELINE.md` bzw. STATE.md protokollierte Ausgangswert.
  </acceptance_criteria>
  <done>Der Fix ist durch einen roten Vorlauf und zwei gruene Nachlaeufe bei identischen Parametern belegt; das Protokoll liegt als Artefakt vor.</done>
</task>

</tasks>

## Artifacts this phase produces

Neu entstehende Symbole, Dateien, Skripte und Konfigurationsschluessel dieser Phase (phasenweit):

- Funktionen: `seedCleanSession`, `generateGlobalsFromModules`, `extractModulesFromLoader`,
  `renderGlobalsArtifact`
- Konstanten: `MODULE_TEST_EXCEPTIONS`
- Neue Dateien: `tools/generate-eslint-globals.js`, `eslint.generated-globals.js`,
  `tsconfig.strict.json`, `tests/unit/eslint-globals-freshness.test.js`,
  `tests/unit/module-test-coverage.test.js`, `tests/e2e/features/session-prep.spec.js`,
  `tests/e2e/features/npc-generator.spec.js`, `tests/e2e/features/timeline.spec.js`,
  `tests/e2e/features/reise.spec.js`, `tests/e2e/features/fraktionen.spec.js`,
  `tests/unit/session-prep.test.js`, `tests/unit/npc-generator.test.js`,
  `tests/unit/timeline.test.js`, `tests/unit/reise.test.js`, `tests/unit/fraktionen.test.js`
- Neue npm-Skripte: `globals:generate`, `typecheck:strict`
- Geaenderte npm-Skripte: `lint`, `check`; entfallendes Skript: `lint:all`
- Messprotokolle: `.planning/phases/14-tests-gates/14-GATE-BASELINE.md`,
  `.planning/phases/14-tests-gates/14-TOAST-RACE-MEASUREMENT.md`
- Entfallende Dateien: `tests/e2e/features/welt-story.spec.js`, `tests/unit/welt-story.test.js`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-Helfer -> localStorage des Browserkontexts | `page.addInitScript` schreibt einen festen Datensatz in den Speicher des Testbrowsers, bevor die App bootet. |
| Testcode -> Produktionsbundle | Der Lauf laedt `dist/dnd-tracker-bundled.html` per `file://`; der Seed simuliert einen Boot-Zustand, veraendert den Bundle aber nicht. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-08 | Information Disclosure | Fester Seed-Datensatz in `test-utils.js` | low | accept | Der Datensatz enthaelt ausschliesslich Struktur- und Standardwerte (Theme, Ansichtsname, leere Sammlungen, ID-Zaehler) und keine Zugangsdaten. `tests/**` steht nicht in `loader.js MODULES` und geht damit nie in einen Bundle ein. |
| T-14-09 | Tampering | Beweisfuehrung des Lastlaufs | high | mitigate | Der Nachweis ist nur gueltig mit rotem Vorlauf bei identischen Parametern und `--retries=0`; beide Haelften liegen als eingechecktes Protokoll vor und werden im Checkpoint gegengelesen. Ohne roten Vorlauf wird nicht abgenommen. |
| T-14-10 | Denial of Service | Lastlauf mit hoher Worker-Zahl | low | accept | Der Lauf ist lokal und manuell gestartet, laeuft nicht in CI (dort bleiben `retries: 2` und `workers: 1`) und beendet sich selbst. |
</threat_model>

<verification>
- `npx playwright test tests/e2e/crud/` gruen.
- `npx playwright test` (volle Suite) gruen; keine kleinere Zahl bestandener Tests als zuvor.
- `npx jest` gruen.
- `git diff` gegen `playwright.config.js` leer.
- Beide Haelften des Messprotokolls eingecheckt.
</verification>

<success_criteria>
- Der Seed-Block existiert genau einmal im Repo, als benannter Helfer mit erhaltenem
  Ursachenkommentar.
- Alle fuenf CRUD-Specs seeden ihre Sitzung, `locations` und `encounters` erstmals.
- Der Fix ist durch einen dokumentierten roten Vorlauf und gruene Nachlaeufe bei identischen
  Parametern belegt, nicht durch einen gruenen Lauf allein.
</success_criteria>

## Planer-Annahmen und Hinweise

- **Kanten-Sonde, aufgeloest als Zusicherungen oben (`must_haves.truths`):** Die drei fuer
  TEST-03 aufgeworfenen Kanten sind konkret beantwortet — Nachbarschaft (Doppelaufruf und
  parallele Worker), Leerfall (frischer Kontext ohne vorherigen Speicherinhalt) und Reihenfolge
  (Aufruf immer vor `loadApp`). Alle drei sind ueber die Verify-Kommandos dieses Plans pruefbar.
- **Kanon-Breadcrumb (Prohibition-Probe):** Generische Injection-/Speicher-Kanonpunkte werden von
  `/gsd-secure-phase` und dem `<threat_model>` abgedeckt und hier nicht als eigene Prohibition
  gepraegt.

<output>
Create `.planning/phases/14-tests-gates/14-03-SUMMARY.md` when done
</output>