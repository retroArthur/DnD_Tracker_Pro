---
phase: 14-tests-gates
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - tools/generate-eslint-globals.js
  - eslint.generated-globals.js
  - eslint.config.js
  - tests/unit/eslint-globals-freshness.test.js
  - package.json
autonomous: true
requirements: [TEST-05]

estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "`eslint.generated-globals.js` ist aus `loader.js MODULES` abgeleitet und wird von `eslint.config.js` eingelesen; keine zweite, handgepflegte Namensliste entsteht (ARCH-01)."
    - "`tests/unit/eslint-globals-freshness.test.js` wird rot, sobald das eingecheckte Artefakt gegenueber dem Quellbaum veraltet ist — bewiesen durch einen eigenen Testfall, der eine kuenstlich veraltete Liste erkennt."
    - "`tests/unit/eslint-globals-freshness.test.js` wird rot, wenn aus `loader.js` null Module extrahiert werden — ein leer-gruener Waechter ist ausgeschlossen."
    - "Die `no-undef`-Vorkommen aus `npx eslint .` fallen von der in `14-GATE-BASELINE.md` protokollierten Ausgangszahl auf hoechstens zehn; die verbleibenden sind ausschliesslich die in 14-06 behandelten toten Aktionsziele."
    - "`npm run globals:generate` erzeugt das Artefakt reproduzierbar: ein zweiter Lauf ohne Quelltextaenderung laesst die Datei byte-identisch."
  artifacts:
    - tools/generate-eslint-globals.js
    - eslint.generated-globals.js
    - tests/unit/eslint-globals-freshness.test.js
    - eslint.config.js
  key_links:
    - "`loader.js MODULES` -> `tools/generate-eslint-globals.js` -> `eslint.generated-globals.js` -> `eslint.config.js` `languageOptions.globals` -> `npx eslint .`"
    - "`tools/generate-eslint-globals.js` -> `tests/unit/eslint-globals-freshness.test.js` (derselbe Code erzeugt Artefakt und Erwartung — keine Parallelimplementierung)"
  prohibitions:
    - "Kein Gate dieser Phase darf gruen werden, weil sein Pruefumfang verkleinert wurde: `ignores`-Eintraege, Ausnahmelisten und Schwellenwerte duerfen nur mit gemessener Begruendung veraendert werden."
    - "Namen, die nirgends im Quellbaum deklariert sind, duerfen nicht von Hand in die Globals-Liste eingetragen werden, um `no-undef` ruhig zu stellen — das wuerde genau die Fehlerklasse verdecken, die D-08 sichtbar machen soll."
---

<objective>
Den Globals-Generator aus D-08 vollstaendig verdrahten: `loader.js MODULES` als einzige Quelle,
ein eingechecktes Artefakt, ein Konsument in `eslint.config.js`, ein Drift-Waechter in Jest — und
die Konfigurationsluecken fuer Node- und Browser-Globals schliessen, damit `no-undef` in 14-06
ueberhaupt auf `error` gehoben werden **kann**.

Purpose: Heute meldet `npx eslint .` 1829 `no-undef`-Warnungen ueber 539 Namen. Die Triage zeigt,
dass das keine 1829 Probleme sind: der weit ueberwiegende Teil sind die projekteigenen
Cross-Modul-Globals der Non-ESM-Architektur, dazu fehlende Node-Globals in `tests/**` und einige
fehlende Browser-Globals. Ueber 500 Namen von Hand in `eslint.config.js` zu pflegen wuerde binnen
einer Phase verrotten — allein Phase 13 hat beim Aufteilen von vier Modulen 14 neue Dateien
erzeugt. Deshalb: generieren statt pflegen, mit einem Test, der bei Drift bricht. Das Verfahren
ist im Projekt etabliert (`tests/unit/console-hygiene.test.js` aus 13-08,
`tests/unit/tab-registry.test.js` aus 13-04), es muss nichts erfunden werden.

Output: `tools/generate-eslint-globals.js`, `eslint.generated-globals.js`,
`tests/unit/eslint-globals-freshness.test.js`, erweiterte `eslint.config.js`, npm-Skript
`globals:generate`.

**Dies ist der Tracer dieser Phase.** Task 1 fuehrt EINEN Pfad durch alle Schichten, die diese
Phase anfasst — Quelle (`loader.js`) -> Generator -> Artefakt -> Konsument-Konfiguration ->
Gate-Kommando (`npx eslint .`) -> Drift-Waechter (Jest) — und beweist ihn Ende zu Ende, bevor
irgendeine Schicht in die Breite gebaut wird. Alle weiteren Gates dieser Phase (D-11 Allowlist,
D-13 Modul-Gate, D-14 Schwelle) sind Varianten desselben Musters. Der Tracer liegt hier und nicht
in 14-01, weil D-15 den roten Lint-Fehler als eigene erste Aufgabe festschreibt.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-tests-gates/14-CONTEXT.md
@.planning/phases/14-tests-gates/14-RESEARCH.md
@.planning/phases/14-tests-gates/14-PATTERNS.md
@.planning/phases/14-tests-gates/14-GATE-BASELINE.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Ende-zu-Ende — von `loader.js MODULES` bis Lint-Lauf und Drift-Waechter, ein Pfad</name>
  <precondition>`npx eslint .` endet mit Exit 0 (Plan 14-01 / D-07 ist gelaufen) — sonst laesst sich der Lint-Lauf nicht als Nachweis verwenden.</precondition>
  <files>tools/generate-eslint-globals.js, eslint.generated-globals.js, eslint.config.js, tests/unit/eslint-globals-freshness.test.js</files>
  <read_first>
    - `loader.js` — das `MODULES`-Array, Form und Anfuehrungszeichen-Stil der Eintraege
    - `tests/unit/console-hygiene.test.js` — vollstaendig; strukturelles Vorbild ("aus `loader.js MODULES` ableiten, bei Drift rot, leer-gruen als eigener Fehler"), inklusive `REPO_ROOT`/`LOADER_PATH`-Aufloesung und Verstoss-Sammelmuster
    - `tests/unit/tab-registry.test.js` (Zeilen 1-97) — zweites Vorbild: Bezeichner aus dem Quelltext gewinnen statt aus einer Parallelliste
    - `build.py`, Funktion `check_duplicate_functions` (ca. Zeilen 173-208) — der produktive Klammertiefe-0-Scan, dessen Verfahren hier nach JS portiert wird
    - `eslint.config.js` — vollstaendig; Blockstruktur des `tseslint.config(...)`-Arrays und der bestehende `globals`-Block (Zeilen 20-87)
    - `package.json` — `"type": "module"` (bestimmt das Modulformat des Artefakts) und der `scripts`-Block
    - `.planning/phases/14-tests-gates/14-GATE-BASELINE.md` — die dort protokollierte `no-undef`-Ausgangszahl, gegen die der Abfall gemessen wird
  </read_first>
  <behavior>
    - `extractModulesFromLoader(source)` liefert fuer den echten `loader.js`-Inhalt eine nichtleere Liste relativer Pfade; fuer einen Text ohne `MODULES`-Array eine leere Liste.
    - `generateGlobalsFromModules()` enthaelt Namen, die auf Klammertiefe 0 mit `function`, `async function`, `const`, `let`, `var` oder `class` deklariert sind.
    - `generateGlobalsFromModules()` enthaelt NICHT Namen, die nur innerhalb einer Funktion (Klammertiefe groesser 0) deklariert sind.
    - Der Drift-Vergleich meldet einen Unterschied, wenn aus der frisch erzeugten Namensmenge ein Name entfernt wird.
    - Der Drift-Vergleich meldet einen Unterschied, wenn der frisch erzeugten Namensmenge ein Name hinzugefuegt wird.
    - Der Waechter wirft einen Fehler, wenn `extractModulesFromLoader` null Module liefert.
  </behavior>
  <action>
    Drei Dateien, ein Pfad, keine Verzweigung — die Schichtenkette wird vollstaendig verdrahtet,
    aber nichts daneben.

    (a) `tools/generate-eslint-globals.js` als CommonJS mit `module.exports`, damit
    Regenerierungs-Skript und Jest-Test denselben Code benutzen und keine zweite Implementierung
    entsteht. Drei Exporte: `extractModulesFromLoader(source)`, `generateGlobalsFromModules()`,
    `renderGlobalsArtifact(globalsObject)`. `extractModulesFromLoader` folgt exakt dem Idiom aus
    `console-hygiene.test.js` — `MODULES`-Array per regulaerem Ausdruck greifen, dann einfache
    und doppelte Anfuehrungszeichen einsammeln —, damit ein kuenftiger `loader.js`-Formatwechsel
    nur an einer Stelle repariert werden muss. `generateGlobalsFromModules` portiert den
    Klammertiefe-0-Scan aus `build.py`: Tiefe zeilenweise mitfuehren, nur auf Tiefe 0 auf eine
    Deklaration pruefen, danach die Klammern der Zeile verrechnen. Nicht vorhandene Dateien
    werden uebersprungen, nicht als Fehler behandelt.

    Bewusste Abweichung vom `build.py`-Muster, nicht als Portierungsfehler behandeln: `build.py`
    sucht `function|const|let|class` und laesst `async` und `var` weg, weil ein Duplikatpruefer
    konservativ sein darf. Ein Globals-Generator darf das nicht. Gegen den Live-Baum gemessen
    gibt es 57 Top-Level-Deklarationen der Form `async function` — darunter `init` in
    `core/init.js`, `load` in `systems/spellslots/quick-roll.js`, `saveImmediate` in
    `systems/spellslots/persistence.js`, `restoreBackup` und `createAutoBackup` in
    `systems/backups.js`, `initIndexedDB` in `core/init.js`, `importAudioExport` in
    `systems/migration/audio-export.js` — sowie 22 Top-Level-`var`-Deklarationen mit echtem
    Initialisierer, darunter `DEFAULT_DMSCREEN_PROFILES` in
    `features/dmscreen/dmscreen-render.js`. Mit dem unveraenderten `build.py`-Muster blieben rund
    zehn Produktionsnamen undeklariert und `no-undef: error` waere in 14-06 nicht erreichbar. Das
    Deklarationsmuster hier lautet daher: optionales `async`, dann eines von `function`, `const`,
    `let`, `var`, `class`, dann der Bezeichner. Aliase der Form `var X = window.X` mit
    aufzunehmen ist unschaedlich (der Name ist ohnehin anderswo deklariert) und braucht keinen
    Sonderfall.

    `renderGlobalsArtifact` erzeugt den Dateitext: Kopfkommentar mit dem Hinweis, dass die Datei
    automatisch erzeugt wird und nicht von Hand zu bearbeiten ist, plus dem Regenerierungsbefehl;
    danach `export default` mit alphabetisch sortierten Schluesseln und dem Wert `'readonly'` je
    Name. Schluessel werden als JSON-String-Literale ausgegeben, nicht als nackte Bezeichner —
    der Generator liest fremden Quelltext und darf keinen daraus stammenden Text unquotiert in
    eine ausfuehrbare Datei schreiben (siehe `<threat_model>` T-14-05).

    (b) `eslint.generated-globals.js` im Projekt-Wurzelverzeichnis, ESM mit `export default`, weil
    `package.json` `"type": "module"` setzt und `eslint.config.js` die Datei per `import`
    einbindet. Inhalt vollstaendig durch `renderGlobalsArtifact` erzeugt.

    (c) `eslint.config.js`: ganz oben einen `import` des Artefakts unter dem Namen
    `generatedGlobals` ergaenzen und im `tseslint.config(...)`-Array direkt nach dem bestehenden
    Block mit `ecmaVersion: 2022` einen eigenen Konfigurationsblock einfuegen, dessen einziger
    Inhalt `languageOptions.globals` mit `generatedGlobals` ist. Der bestehende `globals`-Block
    bleibt Zeile fuer Zeile unveraendert; Flat-Config mergt `languageOptions.globals` additiv
    ueber Konfigurationsobjekte hinweg, es wird nichts ersetzt. `no-undef` bleibt in diesem Task
    auf `warn` — die Verschaerfung auf `error` ist Sache von 14-06 und braucht D-09 als
    Vorbedingung.

    (d) `tests/unit/eslint-globals-freshness.test.js` als Drift-Waechter in der Struktur von
    `console-hygiene.test.js`, mit drei Testfaellen. Erstens ein Leer-gruen-Waechter:
    `extractModulesFromLoader` gegen den echten `loader.js` liefert mehr als null Eintraege; bei
    null wird ein Fehler mit erklaerender Meldung geworfen, im Geist der Formulierung aus
    `console-hygiene.test.js`, dass ein leer-gruener Test schlimmer waere als keiner. Zweitens
    ein dauerhaft eingebauter Fail-first-Nachweis: aus der frisch erzeugten Namensmenge einen
    Schluessel entfernen und einen erfundenen hinzufuegen, dann die Vergleichslogik anwenden und
    erwarten, dass sie beide Unterschiede meldet — damit ist der Waechter nachweislich in der
    Lage, rot zu werden, ohne dass ein Verify-Kommando eingecheckte Dateien anfassen muss.
    Drittens die eigentliche Pruefung: die Schluessel aus dem eingecheckten Artefakt sind
    sortiert identisch mit denen aus `generateGlobalsFromModules()`; bei Abweichung eine
    Fehlermeldung, die fehlende und ueberzaehlige Namen einzeln auflistet und den
    Regenerierungsbefehl nennt.

    Der Test liest das ESM-Artefakt ueber den Dateitext und gewinnt die Schluessel per regulaerem
    Ausdruck. Das ist die Projektkonvention (Quelltext-als-Text-Pruefung wie in
    `console-hygiene.test.js` und `tab-registry.test.js`) und vermeidet die ESM/CommonJS-Reibung
    eines dynamischen Imports im vorhandenen Jest-Setup.

    Reihenfolge im Task: zuerst (d) gegen das noch fehlende Artefakt schreiben und rot sehen,
    dann (a) und (b) erzeugen, dann (c) verdrahten, dann gruen.
  </action>
  <verify>
    <automated>node --check tools/generate-eslint-globals.js</automated>
    <fails_when>non-zero exit, oder stderr enthaelt "SyntaxError"</fails_when>
    <automated>npx jest tests/unit/eslint-globals-freshness.test.js</automated>
    <fails_when>non-zero exit, oder die Zusammenfassung meldet weniger als 3 bestandene Tests</fails_when>
    <automated>node -e "const g=require('fs').readFileSync('eslint.generated-globals.js','utf8');const n=(g.match(/readonly/g)||[]).length;console.log('Namen im Artefakt:',n);process.exit(n>1000?0:1)"</automated>
    <fails_when>non-zero exit — das Artefakt enthaelt 1000 oder weniger Namen, der Generator hat den Quellbaum also nicht wirklich gelesen</fails_when>
    <automated>npx eslint . --format json -o .eslint-probe.json; node -e "const r=require('./.eslint-probe.json');const n=r.flatMap(f=>f.messages).filter(m=>m.ruleId==='no-undef').length;console.log('no-undef Vorkommen:',n);require('fs').unlinkSync('./.eslint-probe.json');process.exit(n<1000?0:1)"</automated>
    <fails_when>non-zero exit, oder die Zeile "no-undef Vorkommen:" nennt 1000 oder mehr — das Artefakt wird von `eslint.config.js` dann nicht wirksam eingelesen</fails_when>
  </verify>
  <acceptance_criteria>
    - `tools/generate-eslint-globals.js` exportiert genau die drei Namen `extractModulesFromLoader`, `generateGlobalsFromModules`, `renderGlobalsArtifact`.
    - Das Deklarationsmuster im Generator erfasst `async function`, `var` und `class`; ein Testlauf des Generators enthaelt die Namen `init`, `saveImmediate`, `restoreBackup`, `createAutoBackup`, `importAudioExport` und `DEFAULT_DMSCREEN_PROFILES` in seiner Ausgabe.
    - `eslint.generated-globals.js` beginnt mit einem Kopfkommentar, der den Regenerierungsbefehl nennt, und enthaelt ausschliesslich `export default` mit sortierten, in Anfuehrungszeichen gesetzten Schluesseln.
    - `git diff eslint.config.js` zeigt genau zwei Aenderungen: eine `import`-Zeile am Dateikopf und einen neuen Konfigurationsblock; der bestehende `globals`-Block ist unveraendert und `no-undef` steht weiterhin auf `warn`.
    - `npx jest tests/unit/eslint-globals-freshness.test.js` meldet 3 bestandene Tests; einer davon traegt im Namen den Nachweis, dass Drift erkannt wird.
    - `npx eslint .` endet weiterhin mit 0 Fehlern.
  </acceptance_criteria>
  <done>Der Pfad Quelle -> Generator -> Artefakt -> Konfiguration -> Lint-Lauf -> Drift-Waechter ist vollstaendig verdrahtet und Ende zu Ende belegt; die `no-undef`-Vorkommen sind messbar gefallen.</done>
  <reversibility rating="costly">Der Rueckweg (Artefakt und Generator entfernen, Namen wieder von Hand pflegen) ist technisch trivial, macht aber Generator, Drift-Test und die davon abhaengige Verschaerfung in 14-06 zu totem Beiwerk (CONTEXT.md D-08).</reversibility>
</task>

<task type="auto">
  <name>Task 2: Die drei verbleibenden Globals-Luecken schliessen — Browser, Node, Testumgebung</name>
  <files>eslint.config.js</files>
  <read_first>
    - `eslint.config.js` — nach Task 1; besonders der `globals`-Block (Zeilen 20-87), der `tests/**`-Block (ab Zeile 133) und der `**/*.cjs`-Block (ab Zeile 155)
    - `tests/setup.js` — die Zeilen, die `global.*`-Helfer definieren (u. a. um Zeile 161, 301, 309, 398, 442, 481, 503)
    - `utils/testable-utils.js` (Zeile 483) — die `module.exports`-Zeile, die diese Datei zugleich browser- und Node-tauglich macht
    - `playwright.config.js` und `playwright.smoke.config.js` — beide greifen auf `process` zu
  </read_first>
  <action>
    Vier gezielte Ergaenzungen in `eslint.config.js`. Alle Namen unten sind gegen den Live-Baum
    gemessen; bleiben nach der Aenderung andere Namen uebrig, werden sie im Summary benannt statt
    stillschweigend als Global eingetragen.

    Erstens, in den bestehenden `globals`-Block bei den Browser-Globals acht Namen ergaenzen, in
    alphabetischer Naehe zu den vorhandenen Eintraegen: `AudioContext`, `BroadcastChannel`,
    `ClipboardEvent`, `DataTransfer`, `File`, `HTMLInputElement`, `atob`, `getComputedStyle` —
    alle mit dem Wert `readonly`. Fundstellen zur Kontrolle: `AudioContext` in
    `features/soundboard/soundboard-player.js`, `BroadcastChannel` in `systems/undo.js`, `File`
    und `atob` in `systems/migration/audio-export.js`, `HTMLInputElement` in `utils/basic.js`,
    `getComputedStyle` in `features/dice/dice-core.js` und in drei Editor-E2E-Specs,
    `ClipboardEvent` und `DataTransfer` in `tests/e2e/features/editor-insert.spec.js`.

    Zweitens, den bestehenden `tests/**`-Block um zwei Gruppen erweitern. Node-Globals:
    `require` als `readonly`, `module` als `writable`, `__dirname`, `__filename`, `process` und
    `global` als `readonly` — sie decken die rund 500 `no-undef`-Vorkommen in `tests/**` ab, die
    reine Konfigurationsluecke sind. Und die sieben Helfer, die `tests/setup.js` selbst als
    `global.*` definiert: `createTestCharacter`, `createTestEncounter`, `createTestNPC`,
    `deleteCharacter`, `genId`, `renderInitiative`, `resetTestState` — alle `readonly`. Diese
    sieben sind keine Erfindung: sie stehen in `tests/setup.js` und sind damit zur Testlaufzeit
    real vorhanden.

    Drittens, einen neuen Konfigurationsblock fuer Node-seitige Dateien ausserhalb von `tests/**`
    anlegen, der heute gar nicht existiert. Er gilt fuer `tools/**/*.js`,
    `playwright.config.js` und `playwright.smoke.config.js` und traegt dieselben sechs
    Node-Globals wie oben. `tools/debug.js` braucht darueber hinaus nichts — seine 16
    Projektnamen kommen bereits aus dem generierten Artefakt aus Task 1.

    Viertens, einen eng gefassten Block ausschliesslich fuer `utils/testable-utils.js` mit
    `module` als `writable`. Diese Datei laeuft doppelt: im Browser ueber `loader.js` und in Jest
    ueber `require`; die `module.exports`-Zeile am Dateiende ist beabsichtigt. Ein eigener Block
    dokumentiert das an genau einer Stelle, statt `module` global freizugeben.

    `no-undef` bleibt auf `warn`. Nach der Aenderung erneut messen: es duerfen nur noch die
    Fundstellen uebrig sein, die 14-06 behandelt.
  </action>
  <verify>
    <automated>npx eslint . --format json -o .eslint-probe.json; node -e "const r=require('./.eslint-probe.json');const m=r.flatMap(f=>f.messages).filter(x=>x.ruleId==='no-undef');const names=[...new Set(m.map(x=>(x.message.match(/'([^']+)'/)||[])[1]))].sort();console.log('verbleibend:',m.length,'Namen:',names.join(', '));require('fs').unlinkSync('./.eslint-probe.json');process.exit(m.length<=10?0:1)"</automated>
    <fails_when>non-zero exit, oder die Zeile "verbleibend:" nennt mehr als 10 Vorkommen</fails_when>
    <automated>npx jest</automated>
    <fails_when>non-zero exit, oder die Zusammenfassung nennt eine kleinere Zahl bestandener Tests als der in 14-GATE-BASELINE.md protokollierte Ausgangswert</fails_when>
    <automated>npx eslint .</automated>
    <fails_when>non-zero exit — es gibt wieder Lint-Fehler</fails_when>
  </verify>
  <acceptance_criteria>
    - Die verbleibenden `no-undef`-Namen sind ausschliesslich die sieben Fundstellen, die 14-06 (D-09) behandelt; die Ausgabe des Verify-Kommandos listet sie namentlich auf und enthaelt keinen weiteren Namen.
    - `eslint.config.js` enthaelt einen Konfigurationsblock, dessen `files` sowohl `tools/**/*.js` als auch beide Playwright-Konfigurationsdateien nennt.
    - `eslint.config.js` enthaelt einen Konfigurationsblock, dessen `files` genau `utils/testable-utils.js` nennt.
    - Der `tests/**`-Block enthaelt die sechs Node-Namen und die sieben `tests/setup.js`-Helfer.
    - `no-undef` steht weiterhin auf `warn` (die Verschaerfung gehoert zu 14-06).
    - Die Gesamtzahl der Lint-Warnungen ist gegenueber `14-GATE-BASELINE.md` gefallen, nicht gestiegen.
  </acceptance_criteria>
  <done>Ausser den sieben toten Aktionszielen meldet `no-undef` nichts mehr; die Verschaerfung in 14-06 ist damit erreichbar.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerierungsweg als npm-Skript verankern und Reproduzierbarkeit belegen</name>
  <files>tools/generate-eslint-globals.js, package.json, eslint.generated-globals.js</files>
  <read_first>
    - `package.json` — der `scripts`-Block; die vorhandene Namenskonvention mit Doppelpunkt-Suffixen (`test:unit`, `lint:fix`, `build:dev`)
    - `tools/generate-eslint-globals.js` — nach Task 1; die Exporte, die das Skript aufruft
    - `eslint.generated-globals.js` — nach Task 1; der Kopfkommentar muss auf denselben Befehl verweisen
  </read_first>
  <action>
    `tools/generate-eslint-globals.js` um einen Direktaufruf-Zweig ergaenzen: wird die Datei
    direkt mit Node ausgefuehrt (Erkennung ueber `require.main === module`), schreibt sie
    `eslint.generated-globals.js` im Projekt-Wurzelverzeichnis und gibt auf stdout die Anzahl der
    geschriebenen Namen aus. Kein Schreiben auf stdout des Dateiinhalts und keine
    Shell-Umleitung — die Datei schreibt sich selbst, damit der Befehl unter PowerShell, cmd und
    Git Bash gleich funktioniert.

    In `package.json` ein Skript `globals:generate` ergaenzen, das genau
    `node tools/generate-eslint-globals.js` ausfuehrt. Der Kopfkommentar des Artefakts muss
    denselben Befehl in der `npm run`-Schreibweise nennen, damit es genau eine dokumentierte
    Wahrheit ueber den Regenerierungsweg gibt.

    Reproduzierbarkeit nachweisen: das Artefakt sichern, `npm run globals:generate` erneut
    ausfuehren, die beiden Dateien byte-weise vergleichen. Bei Unterschied liegt eine
    Instabilitaet in der Sortierung oder im Kopfkommentar vor (etwa ein eingebettetes Datum) —
    diese ist zu entfernen, nicht zu tolerieren: ein Artefakt, das sich bei jedem Lauf aendert,
    macht den Drift-Test aus Task 1 wertlos.

    `package.json` sonst nicht anfassen; die Aenderungen an `lint`, `lint:all`, `typecheck:strict`
    und `check` gehoeren zu 14-06 und 14-07.
  </action>
  <verify>
    <automated>node -e "const s=require('./package.json').scripts;if(!s['globals:generate'])process.exit(1);console.log(s['globals:generate'])"</automated>
    <fails_when>non-zero exit — das Skript `globals:generate` fehlt in package.json</fails_when>
    <automated>cp eslint.generated-globals.js eslint.generated-globals.snapshot && npm run globals:generate && diff eslint.generated-globals.snapshot eslint.generated-globals.js && rm eslint.generated-globals.snapshot</automated>
    <fails_when>non-zero exit, oder `diff` gibt Zeilen aus — der Generator ist nicht reproduzierbar</fails_when>
    <automated>npx jest tests/unit/eslint-globals-freshness.test.js</automated>
    <fails_when>non-zero exit, oder die Zusammenfassung meldet weniger als 3 bestandene Tests</fails_when>
    <automated>test ! -f eslint.generated-globals.snapshot</automated>
    <fails_when>non-zero exit — die Vergleichskopie liegt noch im Arbeitsbaum</fails_when>
  </verify>
  <acceptance_criteria>
    - `npm run globals:generate` laeuft ohne Argumente und ohne Shell-Umleitung und schreibt `eslint.generated-globals.js`.
    - Zwei aufeinanderfolgende Laeufe ohne Quelltextaenderung erzeugen eine byte-identische Datei (`diff` ohne Ausgabe).
    - Der Kopfkommentar in `eslint.generated-globals.js` nennt den Befehl in derselben Schreibweise wie das npm-Skript.
    - `git status` zeigt keine `eslint.generated-globals.snapshot`.
    - In `package.json` sind `lint`, `lint:all`, `typecheck` und `check` unveraendert.
  </acceptance_criteria>
  <done>Der Regenerierungsweg ist ein benanntes Skript, sein Ergebnis ist reproduzierbar, und der Drift-Test bleibt gruen.</done>
</task>

</tasks>

## Artifacts this phase produces

Neu entstehende Symbole, Dateien, Skripte und Konfigurationsschluessel dieser Phase (phasenweit):

- Funktionen: `seedCleanSession`, `generateGlobalsFromModules`, `extractModulesFromLoader`,
  `renderGlobalsArtifact`
- Konstanten: `MODULE_TEST_EXCEPTIONS`
- Neue Dateien: `tools/generate-eslint-globals.js`, `eslint.generated-globals.js`,
  `tsconfig.strict.json`, `tests/unit/eslint-globals-freshness.test.js`,
  `tests/unit/module-test-coverage.test.js`,
  `tests/e2e/features/session-prep.spec.js`, `tests/e2e/features/npc-generator.spec.js`,
  `tests/e2e/features/timeline.spec.js`, `tests/e2e/features/reise.spec.js`,
  `tests/e2e/features/fraktionen.spec.js`, `tests/unit/session-prep.test.js`,
  `tests/unit/npc-generator.test.js`, `tests/unit/timeline.test.js`,
  `tests/unit/reise.test.js`, `tests/unit/fraktionen.test.js`
- Neue npm-Skripte: `globals:generate`, `typecheck:strict`
- Geaenderte npm-Skripte: `lint` (Ratsche), `check` (um den strikten Typecheck erweitert);
  entfallendes Skript: `lint:all`
- Messprotokolle: `.planning/phases/14-tests-gates/14-GATE-BASELINE.md`,
  `.planning/phases/14-tests-gates/14-TOAST-RACE-MEASUREMENT.md`
- Entfallende Dateien: `tests/e2e/features/welt-story.spec.js`, `tests/unit/welt-story.test.js`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Quelldateien -> Generator -> ausfuehrbares Artefakt | Der Generator liest 134 Quelldateien und schreibt daraus eine JavaScript-Datei, die `eslint.config.js` per `import` ausfuehrt. Aus Quelltext gewonnener Text wird zu ausgefuehrtem Code. |
| Eingechecktes Artefakt -> Lint-Konfiguration | Wer das Artefakt von Hand editiert, verschiebt still den Pruefumfang von `no-undef` fuer das gesamte Projekt. |
| `loader.js MODULES` -> Generator | Die Modulliste steuert, welche Dateien der Generator oeffnet und liest. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-04 | Tampering | `eslint.generated-globals.js` (Handbearbeitung) | medium | mitigate | Der Drift-Test in `tests/unit/eslint-globals-freshness.test.js` regeneriert und vergleicht die Schluesselmengen bei jedem `npx jest`-Lauf; ein hinzugefuegter oder entfernter Name faellt sofort auf. Der Fail-first-Testfall beweist, dass der Vergleich das wirklich meldet. |
| T-14-05 | Tampering | Generator-Ausgabe (Code-Erzeugung) | medium | mitigate | Es werden ausschliesslich Treffer des Bezeichner-Musters uebernommen; die Schluessel werden als JSON-String-Literale ausgegeben und die Werte sind der feste Text `readonly`. Damit kann kein aus dem Quelltext stammender Text die Objektliteral-Struktur verlassen. |
| T-14-06 | Denial of Service | Generator liest `loader.js MODULES` | low | accept | Nicht vorhandene Dateien werden uebersprungen; der Scan ist linear in der Dateigroesse und laeuft im Rahmen eines `npx jest`-Laufs. Kein Nutzereingabe-Pfad, kein Netzwerkzugriff. |
| T-14-07 | Spoofing | Konfigurationsumfang der neuen `files`-Bloecke | medium | mitigate | Die neuen Bloecke sind eng gefasst (`tools/**/*.js` plus beide Playwright-Konfigurationen; ein eigener Block nur fuer `utils/testable-utils.js`). Node-Globals werden nicht projektweit freigegeben, damit ein versehentliches `process`/`require` in Browsercode weiterhin auffaellt. |
| T-14-SC | Tampering | npm/pip/cargo-Installationen | high | mitigate | Kein Installationsschritt in diesem Plan. Die in RESEARCH.md als optional gefuehrte Abhaengigkeit `globals` wird bewusst NICHT uebernommen; die acht Browser- und sechs Node-Namen werden in Task 2 namentlich eingetragen. Damit entfaellt jede Paket-Legitimitaetspruefung. |
</threat_model>

<verification>
- `npx eslint .` endet mit Exit 0; die `no-undef`-Vorkommen sind auf hoechstens zehn gefallen und
  die verbleibenden Namen sind namentlich die sieben Fundstellen aus D-09.
- `npx jest` (volle Unit-Suite) gruen, mindestens so viele Tests wie in `14-GATE-BASELINE.md`
  protokolliert, plus die drei neuen aus dem Drift-Waechter.
- `npm run globals:generate` zweimal hintereinander erzeugt eine byte-identische Datei.
- `npx tsc --noEmit` weiterhin Exit 0.
</verification>

<success_criteria>
- Die Globals-Liste wird aus `loader.js MODULES` abgeleitet, nicht gepflegt — es gibt keine
  zweite Namensliste im Projekt.
- Ein Test bricht, sobald das Artefakt veraltet, und er ist nachweislich in der Lage, rot zu
  werden.
- `no-undef` ist bis auf die sieben in 14-06 behandelten Fundstellen sauber; die Verschaerfung
  auf `error` ist damit erreichbar statt nur beabsichtigt.
</success_criteria>

## Planer-Annahmen und Hinweise

- **Abweichung gegenueber RESEARCH.md § „D-08: Vollstaendiger Ablauf":** Der dort abgedruckte
  Generator uebernimmt das `build.py`-Deklarationsmuster wortgetreu. Gegen den Live-Baum gemessen
  ist dieses Muster fuer den Zweck des Generators unvollstaendig (fehlendes `async function`,
  fehlendes `var`); Task 1 erweitert es begruendet. RESEARCH.md selbst empfiehlt das
  `build.py`-Muster aus Konsistenzgruenden — die Konsistenz weicht hier der Erreichbarkeit von
  D-08, und die Abweichung ist im Generator zu kommentieren.
- **Zahlen sind gemessen, nicht uebernommen:** 57 `async function`- und 22 `var`-Deklarationen auf
  Top-Level, acht fehlende Browser-Globals, sieben `tests/setup.js`-Helfer, `module.exports` in
  `utils/testable-utils.js`. Alle diese Werte sind vom Ausfuehrenden ueber die Verify-Kommandos
  erneut zu belegen; weicht etwas ab, ist der Live-Stand massgeblich.

<output>
Create `.planning/phases/14-tests-gates/14-02-SUMMARY.md` when done
</output>
