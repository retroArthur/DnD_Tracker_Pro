---
phase: 14-tests-gates
plan: 06
type: execute
wave: 3
depends_on: ["14-02", "14-04", "14-05"]
files_modified:
  - ui/actions/entity-actions.js
  - ui/actions/system-actions.js
  - ui/actions/ui-actions.js
  - core/init.js
  - types/globals.d.ts
  - eslint.config.js
  - package.json
  - .planning/phases/14-tests-gates/14-GATE-BASELINE.md
autonomous: true
requirements: [TEST-05]

estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Der Knopf mit der Beschriftung CSV im Ressourcen-Tab (`assets/templates/view-resources.html`) loest keinen `ReferenceError` mehr aus, sondern startet den vorhandenen CSV-Export aus `systems/spellslots/import-export.js`."
    - "Keine Aktionsregistrierung und kein direkter Aufruf im Produktionscode verweist mehr auf einen Funktionsnamen, den es im Quellbaum nicht gibt."
    - "`types/globals.d.ts` deklariert keine Funktion mehr, die zur Laufzeit nicht existiert — das Typsystem und der Quellbaum sagen dasselbe."
    - "`no-undef` steht auf `error`; `npx eslint .` endet mit 0 Fehlern."
    - "`npm run lint` fuehrt eine `--max-warnings`-Ratsche mit dem frisch gemessenen Reststand; bei genau diesem Wert endet der Lauf mit Exit 0, bei einer Warnung mehr mit Exit 1."
    - "Es gibt genau einen Lint-Befehl mit genau einer Warnungsgrenze; `lint:all` existiert nicht mehr."
  artifacts:
    - eslint.config.js
    - package.json
    - ui/actions/system-actions.js
    - ui/actions/entity-actions.js
    - ui/actions/ui-actions.js
    - core/init.js
    - types/globals.d.ts
  key_links:
    - "`data-action`-Attribut in `assets/templates/**` -> Registrierung in `ui/actions/*.js` -> tatsaechlich existierende Funktion im Quellbaum"
    - "`eslint.config.js` `no-undef: error` -> `npm run lint` -> CI-Job `lint-and-typecheck` -> `needs:`-Kette"
  prohibitions:
    - "Kein Gate dieser Phase darf gruen werden, weil sein Pruefumfang verkleinert wurde: `ignores`-Eintraege, Ausnahmelisten und Schwellenwerte duerfen nur mit gemessener Begruendung veraendert werden."
    - "Eine tote Registrierung darf nicht dadurch beseitigt werden, dass ein leerer Platzhalter-Handler ergaenzt wird — das ersetzt einen sichtbaren Laufzeitfehler durch einen stillen Klick ohne Wirkung."
---

<!-- planner-discipline-allow: exportDataCSV -->
<!-- planner-discipline-allow: scrollToNPC -->
<!-- planner-discipline-allow: removeLootTag -->
<!-- planner-discipline-allow: populateImportNodesList -->
<!-- planner-discipline-allow: setViewMode -->
<!-- planner-discipline-allow: showErrorLogModal -->
<!-- planner-discipline-allow: initLootTagSystem -->
<!-- planner-discipline-allow: lint:all -->

<objective>
Die von der Globals-Triage freigelegten toten Aktionsziele beheben (D-09), `no-undef` auf `error`
heben (D-08, zweite Haelfte) und die Warnungsgrenze als Ratsche auf den gemessenen Reststand
pinnen (D-10).

Purpose: Die Gate-Schaerfung liefert hier ihren ersten harten Ertrag. Sieben Stellen im
Produktionscode verweisen auf Funktionsnamen, die es im gesamten Repo nicht gibt — sie fallen
heute nur zur Laufzeit auf, und eine davon hat einen echten Knopf im Bedienfeld. Das ist exakt
die Fehlerklasse, die Phase 13 in ihrer Tab-Registry-Variante geschlossen hat: eine Referenz
ueber einen Namen, den es nicht mehr gibt. `action-registry-collisions.test.js` faengt sie nicht,
weil es Kollisionen prueft, nicht Existenz. Solange diese Namen offen sind, kann `no-undef` nicht
auf `error` — ohne D-09 ist D-08 nicht erreichbar.

Output: sieben behandelte Fundstellen, `no-undef: error`, eine gepinnte Warnungsgrenze, ein
statt zwei Lint-Befehlen.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-tests-gates/14-CONTEXT.md
@.planning/phases/14-tests-gates/14-RESEARCH.md
@.planning/phases/14-tests-gates/14-PATTERNS.md
@.planning/phases/14-tests-gates/14-GATE-BASELINE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Die sieben toten Aktionsziele je Fundstelle behandeln (D-09)</name>
  <files>ui/actions/entity-actions.js, ui/actions/system-actions.js, ui/actions/ui-actions.js, core/init.js, types/globals.d.ts</files>
  <read_first>
    - `ui/actions/system-actions.js` — Zeile 14 (Registrierung des CSV-Exports) und Zeilen 163-169 (der Block zum Leeren des Fehlerprotokolls)
    - `ui/actions/entity-actions.js` — Zeilen 238, 420, 473; das Registrierungsmuster ist ein einzeiliger Objekteintrag
    - `ui/actions/ui-actions.js` — Zeile 22
    - `core/init.js` — Zeile 106; die `typeof`-Pruefung schuetzt nur die Bedingung, nicht den Aufruf dahinter
    - `systems/spellslots/import-export.js` — Zeilen 233-275; die tatsaechlich vorhandene Exportfunktion, ihre Signatur und ihr `IO_SCHEMA`-Waechter
    - `types/globals.d.ts` — Zeile 437; die ambiente Deklaration, die den Fehler vor dem Typsystem verbirgt
    - `assets/templates/view-resources.html` — Zeile 143; der Knopf, der auf die Aktion zeigt
  </read_first>
  <action>
    Die Entscheidungsregel aus D-09 wird je Fundstelle einzeln angewandt, nicht pauschal:
    verweist kein `data-action` in `assets/templates/**` auf die Aktion, wird die Registrierung
    entfernt; verweist eines darauf, ist es ein echter Bedienfehler und die Funktion fehlt — dann
    wird behoben statt geloescht. Zur Kontrolle je Aktionsname zusaetzlich gegen den JS-Quellbaum
    greppen, weil einige Bedienflaechen erst zur Laufzeit erzeugt werden.

    **Sechs Entfernungen.** Fuer die Aktionen `scroll-to-npc` (`ui/actions/entity-actions.js`,
    Zeile 238), `remove-loot-tag` (dieselbe Datei, Zeile 420), `populate-import-nodes` (dieselbe
    Datei, Zeile 473), `set-view-mode` (`ui/actions/ui-actions.js`, Zeile 22) und
    `clear-error-log` (`ui/actions/system-actions.js`, Zeilen 163-169) gibt es weder in
    `assets/templates/**` noch im JS-Quellbaum ein `data-action`, das sie ausloest; ihre Zielnamen
    `scrollToNPC`, `removeLootTag`, `populateImportNodesList`, `setViewMode` und
    `showErrorLogModal` existieren nirgends im Repo. Die jeweiligen Registrierungszeilen werden
    ersatzlos aus dem Objektliteral gestrichen — bei `clear-error-log` der gesamte mehrzeilige
    Eintrag, weil sein Rumpf ausschliesslich aus dem Aufruf der fehlenden Funktion und dem
    Leeren des Protokolls besteht. Wird dabei ein Gruppenkommentar sinnlos (etwa ein Banner ueber
    einem nun leeren Abschnitt), wandert er mit.
    Die sechste Entfernung betrifft `core/init.js` Zeile 106: dort steht eine `typeof`-Pruefung
    auf `initLootTagSystem` und dahinter der Aufruf. Die Pruefung schuetzt nur sich selbst — der
    Aufruf ist eine unbedingte Referenz und wird von der Regel gemeldet. Da es die Funktion
    nirgends gibt, wird die gesamte Zeile ersatzlos entfernt.

    **Eine Behebung.** Die Aktion `export-csv` in `ui/actions/system-actions.js` Zeile 14 ist
    kein Aufraeumposten: `assets/templates/view-resources.html` Zeile 143 enthaelt einen echten
    Knopf mit `data-action="export-csv"` und `data-value="spells"`. Ein Spielleiter, der im
    Zauber-Tab darauf klickt, loest heute einen `ReferenceError` aus. Die Implementierung
    existiert bereits unter einem anderen Namen: `exportToCSV(dataType)` in
    `systems/spellslots/import-export.js` Zeile 233. Der Registrierungseintrag wird so geaendert,
    dass er diese vorhandene Funktion mit demselben Argument aufruft wie bisher (der Wert aus dem
    `data-value`-Attribut). Es wird KEIN Alias `exportDataCSV` ergaenzt — der Aufruf wird
    korrigiert, damit es genau einen Namen fuer diese Funktion gibt.

    **Im selben Task** die verwaiste ambiente Deklaration in `types/globals.d.ts` Zeile 437
    entfernen. Sie deklariert `exportDataCSV` als globale Funktion; solange sie stehenbleibt,
    haelt TypeScript die Deklaration fuer gueltig und selbst ein spaeterer `checkJs`-Lauf wuerde
    den Fehler nicht finden. Eine zweite, jetzt falsche Wahrheit im Typsystem waere schlimmer als
    gar keine Deklaration. Ist `exportToCSV` dort noch nicht deklariert, wird die Zeile darauf
    umgestellt statt geloescht — mit der Signatur der echten Funktion.

    In keinem Fall wird eine leere Platzhalterfunktion ergaenzt, um eine Registrierung am Leben
    zu halten: ein Klick, der stumm nichts tut, ist schlechter als einer, der einen Fehler wirft.

    `saveUndoState()` ist hier nicht einschlaegig — es werden keine Nutzerdaten veraendert,
    ausschliesslich Registrierungen entfernt und ein Funktionsname korrigiert.
  </action>
  <verify>
    <automated>node --check ui/actions/entity-actions.js && node --check ui/actions/system-actions.js && node --check ui/actions/ui-actions.js && node --check core/init.js</automated>
    <fails_when>non-zero exit, oder stderr enthaelt "SyntaxError"</fails_when>
    <automated>test "$(grep -c 'exportDataCSV' ui/actions/system-actions.js types/globals.d.ts | grep -vc ':0$')" -eq 0</automated>
    <fails_when>non-zero exit — der nicht existierende Funktionsname steht noch in der Registrierung oder in der Typdeklaration</fails_when>
    <automated>test "$(grep -c 'scrollToNPC\|removeLootTag\|populateImportNodesList' ui/actions/entity-actions.js)" -eq 0</automated>
    <fails_when>non-zero exit — mindestens eine der drei toten Registrierungen steht noch in der Datei</fails_when>
    <automated>test "$(grep -c 'setViewMode' ui/actions/ui-actions.js)" -eq 0</automated>
    <fails_when>non-zero exit — die tote Registrierung steht noch in der Datei</fails_when>
    <automated>test "$(grep -c 'showErrorLogModal' ui/actions/system-actions.js)" -eq 0</automated>
    <fails_when>non-zero exit — der Block zum Leeren des Fehlerprotokolls wurde nicht vollstaendig entfernt</fails_when>
    <automated>test "$(grep -c 'initLootTagSystem' core/init.js)" -eq 0</automated>
    <fails_when>non-zero exit — die Zeile mit der toten Referenz steht noch in der Datei</fails_when>
    <automated>test "$(grep -c 'exportToCSV' ui/actions/system-actions.js)" -eq 1</automated>
    <fails_when>non-zero exit — der CSV-Export ist nicht auf die vorhandene Funktion umgestellt oder wurde mehrfach eingetragen</fails_when>
    <automated>grep -q 'data-action="export-csv"' assets/templates/view-resources.html</automated>
    <fails_when>non-zero exit — der Knopf wurde entfernt statt der Aufruf korrigiert</fails_when>
    <automated>PYTHONIOENCODING=utf-8 python build.py</automated>
    <fails_when>non-zero exit, oder die Ausgabe enthaelt "[FEHLER]" oder "[ABORTED]"</fails_when>
    <automated>npx jest</automated>
    <fails_when>non-zero exit, oder die Zusammenfassung enthaelt "failed"</fails_when>
  </verify>
  <acceptance_criteria>
    - In `ui/actions/entity-actions.js`, `ui/actions/ui-actions.js` und `core/init.js` kommt keiner der fuenf entfernten Zielnamen mehr vor.
    - In `ui/actions/system-actions.js` gibt es genau einen Verweis auf die vorhandene CSV-Exportfunktion und keinen auf den nicht existierenden Namen.
    - `types/globals.d.ts` enthaelt keine Deklaration mehr fuer den nicht existierenden Namen.
    - Der Knopf in `assets/templates/view-resources.html` Zeile 143 ist unveraendert vorhanden.
    - `git diff` zeigt in den vier Aktionsdateien ausschliesslich geloeschte Zeilen und eine geaenderte Zeile — keine neue Funktion, kein Platzhalter-Handler.
    - `PYTHONIOENCODING=utf-8 python build.py` laeuft durch; `npx jest` bleibt gruen.
  </acceptance_criteria>
  <done>Sieben Fundstellen sind je nach Befund entfernt oder behoben; ein echter Bedienfehler im Zauber-Tab ist weg.</done>
</task>

<task type="auto">
  <name>Task 2: `no-undef` auf `error` heben und Null-Fehler nachweisen (D-08, Abschluss)</name>
  <precondition>Das generierte Globals-Artefakt wird von `eslint.config.js` eingelesen (Plan 14-02) und die sieben toten Aktionsziele sind behandelt (Task 1) — ohne beides ist die Verschaerfung nicht erreichbar.</precondition>
  <files>eslint.config.js</files>
  <read_first>
    - `eslint.config.js` — der Block mit `files: ['**/*.js']` und die Zeile, die `no-undef` heute auf `warn` setzt (urspruenglich Zeile 102)
    - `.planning/phases/14-tests-gates/14-GATE-BASELINE.md` — die Ausgangszahlen, gegen die der Erfolg gemessen wird
  </read_first>
  <action>
    In `eslint.config.js` im Block mit `files: ['**/*.js']` den Schweregrad der Regel `no-undef`
    von `warn` auf `error` heben. Sonst wird in diesem Task nichts geaendert — keine weiteren
    Globals, keine `ignores`, keine zweite Regel.

    Direkt darueber eine Begruendungszeile im Stil der bereits vorhandenen Ausnahme zu
    `no-misleading-character-class` setzen: dass die Namensliste generiert wird (Verweis auf das
    Artefakt und den Regenerierungsbefehl), und dass ein Treffer damit ein echter Fund ist und
    kein Konfigurationsmangel.

    Danach `npx eslint .` fahren. Bleiben Fehler uebrig, wird **kein** Name von Hand in die
    Globals-Liste eingetragen und **kein** Pfad in `ignores` aufgenommen. Jeder verbleibende Name
    wird stattdessen wie in Task 1 behandelt: gegen `assets/templates/**` und den JS-Quellbaum
    pruefen, dann entfernen oder beheben. Ist ein Name ein echtes, im Quellbaum deklariertes
    Global, das der Generator nicht erfasst hat, ist das ein Defekt im Deklarationsmuster des
    Generators und wird dort behoben — nicht durch einen Handeintrag umgangen.

    Abschliessend `PYTHONIOENCODING=utf-8 python build.py` und die volle Unit-Suite fahren, weil
    Task 1 Produktionscode angefasst hat.
  </action>
  <verify>
    <automated>grep -q "'no-undef': 'error'" eslint.config.js</automated>
    <fails_when>non-zero exit — die Regel steht nicht auf error</fails_when>
    <automated>npx eslint .</automated>
    <fails_when>non-zero exit — es gibt Lint-Fehler; jeder gemeldete Name ist ein echter Fund und muss behandelt, nicht konfiguriert werden</fails_when>
    <automated>git diff --unified=0 eslint.config.js | grep -c '^+.*ignores' | grep -qx 0</automated>
    <fails_when>non-zero exit — der Verschaerfung wurde ein neuer Ignorier-Eintrag zur Seite gestellt</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>npx jest</automated>
    <fails_when>non-zero exit, oder die Zusammenfassung enthaelt "failed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `eslint.config.js` setzt `no-undef` auf `error`, mit einer Begruendungszeile, die auf das generierte Artefakt und den Regenerierungsbefehl verweist.
    - `npx eslint .` meldet 0 Fehler.
    - Der Diff an `eslint.config.js` enthaelt keine neu hinzugefuegten `ignores`-Eintraege und keine neu von Hand eingetragenen Globals.
    - `npx jest` und `npx tsc --noEmit` bleiben gruen.
  </acceptance_criteria>
  <done>Ein Tippfehler in einem Funktionsnamen bricht ab jetzt den Lint-Lauf, statt eine Warnung unter 2000 anderen zu erzeugen.</done>
  <reversibility rating="costly">Der Rueckweg auf `warn` ist eine Zeile, macht aber Generator, Drift-Test und die sieben Funde aus Task 1 zu einer einmaligen Aufraeumaktion ohne Sicherung (CONTEXT.md D-08).</reversibility>
</task>

<task type="auto">
  <name>Task 3: Warnungsgrenze als Ratsche pinnen und den zweiten Lint-Befehl entfernen (D-10)</name>
  <files>package.json, .planning/phases/14-tests-gates/14-GATE-BASELINE.md</files>
  <read_first>
    - `package.json` — der `scripts`-Block; besonders `lint`, der zweite Lint-Befehl mit abweichender Grenze, und `check`
    - `.planning/phases/14-tests-gates/14-GATE-BASELINE.md` — die dort protokollierte Aufschlüsselung der Warnungen nach Regel
    - `.github/workflows/ci.yml` (Zeilen 10-28) — welcher Lint-Befehl in CI tatsaechlich laeuft
  </read_first>
  <action>
    Zuerst messen, dann pinnen — nicht umgekehrt und nicht gerundet. Den Reststand mit
    `npx eslint . --format json` erheben und die Gesamtzahl der Warnungen exakt bestimmen,
    aufgeschluesselt nach Regel. Diese Zahl ist der Ratschenwert. Sie wird weder aufgerundet noch
    mit Puffer versehen: eine lockerere Grenze als der gemessene Stand laesst genau den Zuwachs
    zu, den die Ratsche verhindern soll.

    Das `lint`-Skript in `package.json` um `--max-warnings` mit genau diesem Wert erweitern. Den
    zweiten Lint-Befehl mit der abweichenden Grenze (`lint:all`) ersatzlos aus `scripts`
    entfernen — nach der Verschaerfung waere er eine zweite Wahrheit ueber denselben Sachverhalt,
    dieselbe Begruendung, mit der ARCH-01 zwei Modullisten ausschliesst. Vorher pruefen, ob
    `.github/workflows/ci.yml` oder ein anderes Skript darauf verweist; falls ja, dort auf `lint`
    umstellen.

    Auf 0 Warnungen wird ausdruecklich NICHT gegangen. Der weit ueberwiegende Rest sind
    `no-unused-vars`-Treffer aus dem `const X = window.X`-Zugriffsmuster — dem Muster, das Phase
    13 als Projektstandard geschuetzt hat (allein fuer das globale Datenobjekt gibt es davon 169
    Vorkommen) und dessen Flaechenumbau in `v1.1-REQUIREMENTS.md` ausdruecklich ausserhalb des
    Milestones liegt. Haerte kommt ueber `no-undef: error` mit Toleranz Null dort, wo es zaehlt;
    Trend kommt ueber eine gepinnte Zahl, die nur fallen darf.

    Die Ratschenwirkung an beiden Seiten der Grenze belegen: mit dem gepinnten Wert muss der Lauf
    Exit 0 liefern, mit einem um eins kleineren Wert Exit 1. Beide Ergebnisse gehoeren ins
    Summary. Zusaetzlich im `scripts`-Block eine Kommentarzeile ist nicht moeglich (JSON) — daher
    den Ratschenwert und sein Messdatum im Summary und in `14-GATE-BASELINE.md` festhalten, damit
    eine spaetere Absenkung nachvollziehbar bleibt.
  </action>
  <verify>
    <automated>node -e "const s=require('./package.json').scripts;if(s['lint:all'])process.exit(1);if(!/--max-warnings\s+\d+/.test(s.lint||''))process.exit(1);console.log('lint =',s.lint)"</automated>
    <fails_when>non-zero exit — der zweite Lint-Befehl existiert noch, oder `lint` traegt keine numerische Warnungsgrenze</fails_when>
    <automated>npm run lint</automated>
    <fails_when>non-zero exit — der Reststand liegt ueber der gepinnten Grenze, die Grenze ist also zu eng gesetzt oder es sind Warnungen hinzugekommen</fails_when>
    <automated>node -e "const s=require('./package.json').scripts.lint;const n=Number(s.match(/--max-warnings\s+(\d+)/)[1]);console.log('gepinnt:',n,'| Probe mit',n-1);process.exit(0)" && node -e "const s=require('./package.json').scripts.lint;const n=Number(s.match(/--max-warnings\s+(\d+)/)[1]);const r=require('child_process').spawnSync('npx',['eslint','.','--max-warnings',String(n-1)],{shell:true});process.exit(r.status===0?1:0)"</automated>
    <fails_when>non-zero exit — ein um eins niedrigerer Grenzwert laeuft ebenfalls durch, die Ratsche sitzt dann nicht am gemessenen Reststand</fails_when>
    <automated>! grep -q 'lint:all' .github/workflows/ci.yml package.json</automated>
    <fails_when>non-zero exit — der entfernte Befehl wird noch in der CI-Konfiguration oder in package.json referenziert</fails_when>
  </verify>
  <acceptance_criteria>
    - `npm run lint` enthaelt `--max-warnings` mit einer konkreten Zahl, die exakt dem in diesem Task gemessenen Reststand entspricht.
    - `npm run lint` endet mit Exit 0; derselbe Lauf mit einem um eins kleineren Grenzwert endet mit Exit 1.
    - Der zweite Lint-Befehl ist aus `package.json` entfernt und wird weder von `ci.yml` noch von einem anderen Skript aufgerufen.
    - Der gepinnte Wert und sein Messdatum sind in `14-GATE-BASELINE.md` nachgetragen.
    - `npm run check` laeuft weiterhin durch.
  </acceptance_criteria>
  <done>Es gibt genau eine Lint-Wahrheit mit genau einer Grenze, und die Grenze sitzt nachweislich exakt am gemessenen Reststand.</done>
</task>

</tasks>

## Artifacts this phase produces

Neu entstehende Symbole, Dateien, Skripte und Konfigurationsschluessel dieser Phase (phasenweit):

- Funktionen: `seedCleanSession`, `generateGlobalsFromModules`, `extractModulesFromLoader`,
  `renderGlobalsArtifact`
- Konstanten: `MODULE_TEST_EXCEPTIONS`
- Neue Dateien: `tools/generate-eslint-globals.js`, `eslint.generated-globals.js`,
  `tsconfig.strict.json`, `tests/unit/eslint-globals-freshness.test.js`,
  `tests/unit/module-test-coverage.test.js`, `tests/e2e/features/session-prep.spec.js`,
  `tests/e2e/features/npc-generator.spec.js`, `tests/e2e/features/timeline.spec.js`,
  `tests/e2e/features/reise.spec.js`, `tests/e2e/features/fraktionen.spec.js`,
  `tests/unit/session-prep.test.js`, `tests/unit/npc-generator.test.js`,
  `tests/unit/timeline.test.js`, `tests/unit/reise.test.js`, `tests/unit/fraktionen.test.js`
- Neue npm-Skripte: `globals:generate`, `typecheck:strict`
- Geaenderte npm-Skripte: `lint` (Ratsche), `check`; entfallendes Skript: `lint:all`
- Messprotokolle: `.planning/phases/14-tests-gates/14-GATE-BASELINE.md`,
  `.planning/phases/14-tests-gates/14-TOAST-RACE-MEASUREMENT.md`
- Entfallende Dateien: `tests/e2e/features/welt-story.spec.js`, `tests/unit/welt-story.test.js`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Klick auf ein `data-action`-Element -> Aktionsregistrierung -> Handlerfunktion | Attributwerte aus dem Bedienfeld (`data-value`, `data-id`) kreuzen hier die Grenze in Anwendungscode. |
| Ambiente Typdeklaration -> Typpruefung | Eine Deklaration ohne Implementierung laesst das Typsystem etwas bestaetigen, das zur Laufzeit fehlt. |
| Lint-Konfiguration -> CI-Gate | Der Schweregrad einer Regel entscheidet, ob ein Fund den Merge blockiert oder nur protokolliert wird. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-16 | Tampering | CSV-Export ueber `data-value` | medium | mitigate | Die vorhandene Exportfunktion prueft den uebergebenen Datentyp gegen ihren `IO_SCHEMA`-Waechter und bricht bei unbekanntem Wert mit einer Meldung ab, ohne eine Datei zu erzeugen; zusaetzlich wird das Ergebnis nur bei vorhandenem Array-Inhalt weiterverarbeitet. Die Umstellung aendert nur den Funktionsnamen, nicht den Datenfluss — der Waechter bleibt der bestehende. |
| T-14-17 | Denial of Service | Tote Aktionsregistrierungen | medium | mitigate | Der `ReferenceError` beim Klick auf den CSV-Knopf wird durch die Korrektur beseitigt; die sechs Registrierungen ohne Bedienflaeche werden entfernt, statt mit leeren Platzhaltern am Leben gehalten (ausdrueckliche Prohibition dieses Plans). |
| T-14-18 | Spoofing | Ambiente Deklaration ohne Implementierung | medium | mitigate | Die verwaiste Deklaration wird im selben Task entfernt bzw. auf die echte Funktion umgestellt; eine Zaehlpruefung ueber beide Dateien belegt, dass der nicht existierende Name nirgends mehr steht. |
| T-14-19 | Tampering | Umgehung der Verschaerfung durch Konfiguration | high | mitigate | Der Diff an `eslint.config.js` wird auf neu hinzugefuegte Ignorier-Eintraege geprueft; die Prohibition dieses Plans verbietet Handeintraege in die Globals-Liste. Verbleibende Fehler werden im Code behandelt, nicht wegkonfiguriert. |
| T-14-SC | Tampering | npm/pip/cargo-Installationen | high | mitigate | Kein Installationsschritt in diesem Plan. |
</threat_model>

<verification>
- `npx eslint .` mit `no-undef: error`: 0 Fehler; `npm run lint` mit gepinnter Grenze: Exit 0.
- Derselbe Lauf mit einer um eins kleineren Grenze: Exit 1 (Ratsche sitzt am Reststand).
- `PYTHONIOENCODING=utf-8 python build.py` laeuft durch.
- `npx jest` und `npx tsc --noEmit` gruen.
- `npx playwright test` nach frischem Build gruen (Task 1 fasst Produktionscode an).
</verification>

<success_criteria>
- Kein Verweis im Produktionscode zeigt mehr auf einen Funktionsnamen, den es nicht gibt.
- Ein echter Bedienfehler im Zauber-Tab ist behoben, nicht nur gemeldet.
- `no-undef` ist ein Fehler, kein Rauschen; die Warnungsgrenze ist gemessen gepinnt und es gibt
  nur noch einen Lint-Befehl.
</success_criteria>

## Planer-Annahmen und Hinweise

- **Kanten-Sonde, aufgeloest (Grenzwert):** Die Grenzwert-Kante zu `TEST-05` ist in
  `must_haves.truths` als Zusicherung ausformuliert und in Task 3 beidseitig geprueft — beim
  gepinnten Wert gruen, eine Warnung darunter rot.
- **Kanon-Breadcrumb (Prohibition-Probe):** Prototype-Pollution beim indizierten Zugriff auf das
  globale Datenobjekt ist Kanon und wird von `/gsd-secure-phase` sowie dem `<threat_model>`
  abgedeckt; hier nicht als eigene Prohibition gepraegt.
- **Abweichung gegenueber CONTEXT.md D-09:** Dort sind sechs tote Ziele plus ein als „harmlos"
  eingestuftes gelistet. Gegen den Live-Baum gemeldet ESLint auch das siebte, weil die
  `typeof`-Pruefung nur die Bedingung schuetzt und nicht den Aufruf dahinter. Es sind sieben
  Fundstellen; alle sieben werden hier behandelt.
- **Abweichung gegenueber RESEARCH.md Pitfall 3:** Dort wird 367 als exakter Reststand genannt.
  Diese Phase fuegt zehn Testdateien hinzu und entfernt zwei; der Wert ist deshalb in Task 3
  frisch zu messen und nicht zu uebernehmen.

<output>
Create `.planning/phases/14-tests-gates/14-06-SUMMARY.md` when done
</output>
