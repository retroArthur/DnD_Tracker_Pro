---
phase: 09-editor-regressionsnetz-execcommand-abl-sung
plan: 06
type: execute
wave: 5
depends_on: ["09-05"]
files_modified:
  - ui/editors/rich-text.js
autonomous: true
requirements: [EDIT-01]

must_haves:
  truths:
    - "Fett, Kursiv, Unterstrichen, Durchgestrichen und Liste über die statische Toolbar erzeugen nach der Migration byte-gleiches Markup wie in 09-BASELINE.md protokolliert — das eingefrorene Netz bleibt unverändert grün"
    - "Die Toggle-Semantik bleibt erhalten: eine bereits ausgezeichnete Selektion wird durch erneutes Anwenden wieder entzeichnet, wie im Netz festgenagelt"
    - "Eine Selektion, die eine Elementgrenze nur teilweise überspannt, wird ohne unbehandelten Ausnahmefehler formatiert (Rückfallpfad statt Absturz)"
    - "Die vier UI-losen Zweige (Überschrift, Schriftart, Hervorheben setzen, Hervorheben entfernen) liefern nach der Migration dieselben Ergebnisse wie im Vorher-Beweis aus Plan 09-03"
    - "Nach diesem Plan enthält formatText keinen einzigen Aufruf der deprecated Editier-Kommando-API mehr; die Gesamtzahl im Modul ist von 21 auf 12 gesunken"
  artifacts:
    - "ui/editors/rich-text.js enthält die neuen Hilfsfunktionen wrapRangeWithElement, unwrapEditorElement, applyInlineFormat und toggleUnorderedListAtSelection als eindeutig benannte Top-Level-Funktionen"
    - "Zwei atomare Commits: einer je Migrationsgruppe (D-05)"
  key_links:
    - "Toolbar-Klick -> SystemActions format-text -> formatText -> applyInlineFormat/toggleUnorderedListAtSelection -> Selection/Range-DOM-Umbau -> identisches Markup"
    - "wrapRangeWithElement kapselt den Rückfallpfad (Extrahieren, Anhängen, Einfügen) für Selektionen, die surroundContents ablehnt"
    - "Netz-Assertions aus 09-02/09-03 -> unveränderte Erwartungswerte -> Nachweis der Verhaltensgleichheit je Gruppe"
  prohibitions:
    - statement: "Es darf KEIN Laufzeit-Rückfall auf die deprecated Editier-Kommando-API eingebaut werden — kein Funktionsschalter, kein zweiter Codepfad, kein try/catch, das im Fehlerfall den alten Aufruf nachholt (D-06); Rollback-Einheit ist ausschließlich der Git-Revert einer Gruppe"
      status: flagged-unverified
      verification: test
    - statement: "Bei der Migration darf KEIN vorbestehendes Verhalten stillschweigend verbessert werden — abweichende Tag-Formen, fehlende Attribute oder Eigenheiten der alten Implementierung werden exakt reproduziert; eine als Bug empfundene Eigenheit wird dokumentiert, nicht mitrepariert (Milestone-Leitplanke: verhaltensneutral)"
      status: flagged-unverified
      verification: judgment
    - statement: "Die drei Aufrufe der deprecated Editier-Kommando-API außerhalb von ui/editors/rich-text.js (Link-Erzeugung in der Aktions-Registry, Wiki-Link-Einfügen, Entity-Link-Einfügen) dürfen NICHT mitmigriert werden — sie liegen außerhalb des Phasen-Scopes (D-06 nennt ausdrücklich die 21 Call-Sites in rich-text.js)"
      status: flagged-unverified
      verification: test
---

<!-- planner-discipline-allow: document.execCommand -->

<objective>
Erste Migrationsstufe: Alle neun Aufrufe der deprecated Editier-Kommando-API innerhalb von `formatText()` werden durch Selection/Range-DOM-Operationen ersetzt — fünf über die Toolbar erreichbare (Fett, Kursiv, Unterstrichen, Durchgestrichen, Liste) und vier UI-lose (Überschrift, Schriftart, Hervorheben setzen/entfernen).

Purpose: `formatText()` ist die meistgenutzte Editorfunktion und hat mit `applyFloatingFormat()` bereits ein produktiv laufendes Selection/Range-Vorbild in derselben Datei. Diese Gruppe zuerst zu migrieren bringt den größten Abdeckungsgewinn bei kleinstem Erfindungsrisiko.
Output: migrierte `formatText()`-Zweige plus vier wiederverwendbare Hilfsfunktionen, zwei atomare Commits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-CONTEXT.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-RESEARCH.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-PATTERNS.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md
@.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-05-SUMMARY.md
@ui/editors/rich-text.js
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Gruppe A): Inline-Formate und Liste in formatText auf Selection/Range umstellen — 5 Call-Sites</name>
  <precondition>Der Abschnitt „D-04a Doppel-Grün-Nachweis" in 09-BASELINE.md ist ausgefüllt und weist beide Läufe als grün aus — ohne geführten Vorher-Beweis darf keine Migration beginnen.</precondition>
  <files>ui/editors/rich-text.js</files>
  <behavior>
    Das eingefrorene Netz ist der Test. Erwartetes Verhalten nach der Umstellung, jeweils byte-gleich zum Baseline-Wert:
    - Fett/Kursiv/Unterstrichen/Durchgestrichen auf einer einfachen Wortselektion: exakt die in 09-BASELINE.md protokollierte Tag-Form (für Durchstreichen die Form der STATISCHEN Toolbar, die sich von der floating Variante unterscheidet)
    - Erneutes Anwenden auf bereits ausgezeichneten Text: Auszeichnung wird entfernt (Toggle)
    - Liste auf einer Selektion: die protokollierte Listenstruktur; erneutes Anwenden löst sie wieder auf
    - Selektion, die eine Elementgrenze nur teilweise überspannt: Formatierung greift, kein unbehandelter Ausnahmefehler
    - Aktion ohne Selektion und im leeren Editor: das protokollierte Verhalten, kein Ausnahmefehler
    - Nach jeder Aktion bleibt der Editor-Textinhalt vollständig erhalten
  </behavior>
  <read_first>
    - ui/editors/rich-text.js (Zeilen 324–349: `formatText()` in seiner heutigen Form; Zeilen 817–861: `applyFloatingFormat()` mit Toggle-Erkennung über `closest(tag)` und dem Rückfallpfad um `surroundContents`; Zeilen 880–911: die bereits vorhandene Listen-Implementierung der floating Toolbar inklusive Auflösen einer bestehenden Liste; Zeilen 412–467: `setBorderFormat`/`setReadAloudFormat` als Stilvorbild für Range-Umbau in dieser Datei)
    - .planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md (Inventar-Zeilen 329, 331, 333, 335, 337 — die exakten Erwartungswerte; zusätzlich der Adjazenz- und Ordnungsbefund aus 09-02)
    - tests/e2e/features/editor-formatting.spec.js (die eingefrorenen Assertionen — sie sind die Spezifikation; sie dürfen NICHT geändert werden)
    - 09-RESEARCH.md — Pattern 1 (Toggle mit Rückfallpfad), Pitfall 5 (Tag-Form beim Durchstreichen unterscheidet sich zwischen den Toolbars — vorbestehend), Pitfall 6 (Teilselektionen)
    - CLAUDE.md — Abschnitte „Duplicate Declaration Debugging Pattern" (keine funktions-lokalen `const X = window.X`, keine doppelten Top-Level-Funktionsnamen) und „Build System & Deduplication Pattern"
  </read_first>
  <action>
    Führe zuerst das eingefrorene Netz aus und halte fest, dass es vor dem Eingriff grün ist. Ersetze danach in `ui/editors/rich-text.js` die fünf Aufrufe in den Zweigen `bold`, `italic`, `underline`, `strikethrough` und `list` von `formatText()`.

    Lege dafür drei neue Top-Level-Hilfsfunktionen an (Namen exakt so, sie sind projektweit noch frei und dürfen nicht kollidieren; keine `window`-Exporte, da sie modul-intern bleiben):
    - `wrapRangeWithElement(range, wrapper)`: versucht `range.surroundContents(wrapper)` und fällt bei einer Ausnahme auf Extrahieren des Inhalts, Anhängen an den Wrapper und Einfügen des Wrappers zurück (Muster aus `applyFloatingFormat`, Zeilen 850–856).
    - `unwrapEditorElement(element)`: verschiebt alle Kindknoten vor das Element und entfernt es anschließend (Muster aus `applyFloatingFormat`, Zeilen 841–847).
    - `applyInlineFormat(editor, tagName)`: holt die aktuelle Selektion, bricht ohne Range oder ohne Textinhalt kontrolliert ab, erkennt über `closest(tagName)` innerhalb der Editor-Container, ob die Selektion bereits ausgezeichnet ist, und ruft dann entweder `unwrapEditorElement` oder `wrapRangeWithElement` mit einem frisch erzeugten Element auf.

    In `formatText()` rufen die vier Inline-Zweige `applyInlineFormat` mit dem jeweiligen Tag-Namen auf. Der Tag-Name für das Durchstreichen richtet sich strikt nach dem in 09-BASELINE.md protokollierten Wert der STATISCHEN Toolbar — er weicht bewusst von dem der floating Toolbar ab und darf nicht angeglichen werden.

    Für den `list`-Zweig eine vierte Hilfsfunktion `toggleUnorderedListAtSelection(editor)` anlegen, die die Listen-Logik der floating Toolbar (Zeilen 880–911) übernimmt: eine bestehende Liste im Editor-Container auflösen oder eine neue Listenstruktur um die Selektion bauen, danach die Selektion an das erwartete Ende setzen. Erwartungswert ist der Baseline-Wert der Zeile 337; weicht die Struktur der floating Variante davon ab, gilt der Baseline-Wert, nicht das Vorbild.

    Das bestehende Wächter-Muster am Funktionsanfang (`$(elementId)` prüfen, bei fehlendem Editor früh zurückkehren, danach fokussieren) bleibt unverändert. Kein try/catch um ganze Funktionskörper, nur gezielt um den Umschließen-Aufruf. Kein Rückfall auf die alte API.

    Baue danach das Bundle und führe das komplette Netz aus. Erst wenn ALLE vier Netz-Dateien grün sind, die Gruppe als einen atomaren Commit festschreiben (Commit-Text nennt Gruppe A und die fünf ersetzten Call-Sites).
  </action>
  <verify>
    <automated>npm run build:dev && npx playwright test tests/e2e/features/editor-formatting.spec.js tests/e2e/features/editor-floating.spec.js tests/e2e/features/editor-insert.spec.js tests/e2e/features/editor-smoke.spec.js && npx jest</automated>
  </verify>
  <acceptance_criteria>
    - Das komplette Netz (alle vier Spec-Dateien) ist nach dem Eingriff grün; `npx jest` ebenfalls
    - Verhalten: Die Netz-Assertionen für Fett, Kursiv, Unterstrichen, Durchgestrichen und Liste sind unverändert und grün — `git diff` der vier Spec-Dateien zeigt in diesem Task keine Änderung
    - Im Bereich der Funktion `formatText` verbleiben genau 4 Aufrufe der deprecated API (die noch nicht migrierten UI-losen Zweige): `node -e "const s=require('fs').readFileSync('ui/editors/rich-text.js','utf8');const a=s.indexOf('function formatText');const b=s.indexOf('function setEditorFont');const m=s.slice(a,b);const n=(m.match(/document\.execCommand/g)||[]).length;process.exit(n===4?0:1)"` beendet mit 0
    - `ui/editors/rich-text.js` enthält die Top-Level-Funktionen `wrapRangeWithElement`, `unwrapEditorElement`, `applyInlineFormat` und `toggleUnorderedListAtSelection` (je genau eine Definition)
    - Keine funktions-lokale `const`-Zuweisung eines `window`-Bezeichners wurde neu eingeführt: `node -e "const s=require('fs').readFileSync('ui/editors/rich-text.js','utf8');const bad=s.split('\n').filter(l=>/^\s+const\s+\w+\s*=\s*window\./.test(l));process.exit(bad.length>0?1:0)"` beendet mit 0
    - `npm run build:dev` beendet mit Exit-Code 0 (kein Dedup- oder Syntaxbruch im Bundle)
    - Genau ein Commit für diese Gruppe, dessen Text die Gruppe und die ersetzten Call-Sites benennt
  </acceptance_criteria>
  <done>Die fünf über die Toolbar erreichbaren Inline-/Listen-Call-Sites laufen ohne die deprecated API und erzeugen nachweislich identisches Markup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Gruppe B): UI-lose Zweige von formatText umstellen — 4 Call-Sites (Überschrift, Schriftart, Hervorheben setzen/entfernen)</name>
  <files>ui/editors/rich-text.js</files>
  <behavior>
    Prüfebene sind die Tests aus dem Block „UI-lose Zweige" in `tests/e2e/features/editor-floating.spec.js` (direkter Funktionsaufruf, da kein Toolbar-Pfad existiert). Erwartetes Verhalten nach der Umstellung, jeweils byte-gleich zum Baseline-Wert:
    - Überschriften-Zweig auf einer Selektion: die protokollierte Block-Struktur
    - Schriftart-Zweig mit einem Wert: das protokollierte Element mit der Schriftart-Angabe
    - Hervorheben-Zweig mit einem Farbwert: das protokollierte Element mit der Hintergrundfarbe
    - Hervorheben-Zweig mit dem Wert für „keine": das protokollierte Ergebnis des Entfernens der Auszeichnungen
    - In allen vier Fällen bleibt der Textinhalt vollständig erhalten
  </behavior>
  <read_first>
    - ui/editors/rich-text.js (Zeilen 338–348 nach dem Eingriff aus Task 1: die verbleibenden Zweige `heading`, `font` und `highlight`; Zeilen 400–411 `clearEditorFormatting` als Beispiel für das Entfernen von Auszeichnungen in dieser Datei; Zeilen 912–934 der Zweig „Format entfernen" der floating Toolbar, der Hervorhebungs-Elemente auflöst)
    - .planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-BASELINE.md (Inventar-Zeilen 339, 341, 344, 346 mit dem per direktem Aufruf erhobenen Markup; Abschnitt A2 mit dem Nachweis, dass kein Template diese Zweige auslöst)
    - tests/e2e/features/editor-floating.spec.js (Block „UI-lose Zweige" — die eingefrorene Spezifikation dieses Tasks)
    - ui/actions/system-actions.js (Zeilen 92–140: der reale, getrennte Hervorhebungs-Pfad der Toolbar — er bleibt unangetastet und darf nicht mit dem toten Zweig verschmolzen werden)
    - 09-RESEARCH.md — Pitfall 3 (tote Zweige zählen zu den 21; „ersetzen" heißt Funktion erhalten, nicht entfernen) und Open Question 2
  </read_first>
  <action>
    Ersetze die vier verbleibenden Aufrufe in `formatText()`:

    - Überschriften-Zweig: den umgebenden Blockcontainer der Selektion ermitteln und durch ein Überschriften-Element der protokollierten Ebene ersetzen bzw. die Selektion in ein solches Element einschließen — maßgeblich ist ausschließlich der Baseline-Wert der Zeile 339. Nutze `wrapRangeWithElement` aus Task 1, wo die Struktur das zulässt.
    - Schriftart-Zweig: ein Element mit der protokollierten Schriftart-Angabe erzeugen und über `wrapRangeWithElement` um die Selektion legen (Baseline-Wert der Zeile 341).
    - Hervorheben-Zweig mit Farbwert: ein Element mit der protokollierten Hintergrundfarb-Angabe erzeugen und über `wrapRangeWithElement` anwenden (Baseline-Wert der Zeile 346). Es dürfen ausschließlich Stil-Eigenschaften gesetzt werden, die die bestehende Whitelist bereits erlaubt.
    - Hervorheben-Zweig mit dem Wert für „keine": eine neue Top-Level-Hilfsfunktion `clearInlineFormattingAtSelection(editor)` anlegen, die die in der Selektion liegenden Auszeichnungs-Elemente auflöst (Muster: der Zweig „Format entfernen" der floating Toolbar, Zeilen 917–932, plus `unwrapEditorElement` aus Task 1). Der Erwartungswert ist der Baseline-Wert der Zeile 344.

    Ergänze über den vier Zweigen einen kurzen Kommentar, dass sie derzeit von keinem Template ausgelöst werden (Verweis auf 09-BASELINE.md/A2) und dass sie gemäß D-06 dennoch funktionserhaltend migriert und nicht entfernt wurden.

    Erwäge NICHT, den toten Zweig mit dem realen Hervorhebungs-Pfad der Aktions-Registry zu verschmelzen — das wäre eine Verhaltensänderung.

    Baue danach das Bundle und führe das komplette Netz aus. Erst wenn alle vier Netz-Dateien grün sind, die Gruppe als zweiten atomaren Commit festschreiben.
  </action>
  <verify>
    <automated>npm run build:dev && npx playwright test tests/e2e/features/editor-formatting.spec.js tests/e2e/features/editor-floating.spec.js tests/e2e/features/editor-insert.spec.js tests/e2e/features/editor-smoke.spec.js && npx jest</automated>
  </verify>
  <acceptance_criteria>
    - Das komplette Netz ist grün; `npx jest` ebenfalls
    - Im Bereich der Funktion `formatText` verbleibt kein Aufruf der deprecated API: `node -e "const s=require('fs').readFileSync('ui/editors/rich-text.js','utf8');const a=s.indexOf('function formatText');const b=s.indexOf('function setEditorFont');const m=s.slice(a,b);process.exit(/document\.execCommand/.test(m)?1:0)"` beendet mit 0
    - Die Gesamtzahl im Modul ist auf 12 gesunken: `node -e "const s=require('fs').readFileSync('ui/editors/rich-text.js','utf8');const n=(s.match(/document\.execCommand/g)||[]).length;process.exit(n===12?0:1)"` beendet mit 0
    - `ui/editors/rich-text.js` enthält genau eine Definition von `clearInlineFormattingAtSelection`
    - Verhalten: Die Tests im Block „UI-lose Zweige" sind unverändert und grün — `git diff` von `tests/e2e/features/editor-floating.spec.js` zeigt in diesem Task keine Änderung
    - Die drei Aufrufe der deprecated API außerhalb dieses Moduls sind unangetastet: `node -e "const fs=require('fs');const f=['ui/actions/system-actions.js','features/wiki/wiki.js','systems/entity-links.js'];const n=f.reduce((a,p)=>a+((fs.readFileSync(p,'utf8').match(/document\.execCommand/g)||[]).length),0);process.exit(n===3?0:1)"` beendet mit 0
    - `npm run build:dev` beendet mit Exit-Code 0
    - Genau ein Commit für diese Gruppe
  </acceptance_criteria>
  <done>`formatText()` arbeitet vollständig ohne die deprecated API; alle neun dort liegenden Call-Sites sind funktionserhaltend ersetzt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Selektion → DOM-Umbau (neue Wrapper-Elemente) | Nutzergesteuerter Bereich des Editor-DOM wird programmatisch umgebaut |
| Erzeugte Wrapper-Elemente → Persistenz | Neue Elemente/Stile müssen die bestehende Whitelist passieren |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-02 | Tampering | Neue Wrapper-Elemente in `applyInlineFormat` / Hervorheben-Zweig | medium | mitigate | Es werden ausschließlich Elementtypen und Stil-Eigenschaften erzeugt, die die bestehende Whitelist in `utils/basic.js` bereits kennt (kein neues Tag, keine neue Stil-Eigenschaft). Die Persistenz-Roundtrip-Tests des Netzes belegen das nach jeder Gruppe. |
| T-09-15 | Tampering | Rückfallpfad in `wrapRangeWithElement` | medium | mitigate | Der Rückfall arbeitet ausschließlich mit bereits im Dokument befindlichen Knoten (Extrahieren und Wiedereinfügen); es wird kein HTML aus Zeichenketten geparst, also entsteht kein neuer Einschleusungspfad. |
| T-09-16 | Denial of Service | Toggle-/Auflösungslogik bei tief verschachteltem Markup | low | accept | Die Logik arbeitet ohne Rekursion über die vom Netz abgedeckten Strukturtiefen; der Teilselektions-Testfall deckt den kritischsten Fall ab. |
| T-09-SC | Tampering | npm/pip/cargo-Installationen | low | accept | Keine Paketinstallation in dieser Phase. |
</threat_model>

## Artifacts this phase produces

Siehe die vollständige Liste in `09-01-PLAN.md`. Von DIESEM Plan neu erzeugt (alle in `ui/editors/rich-text.js`, modul-intern, kein `window`-Export):

- `wrapRangeWithElement(range, wrapper)`
- `unwrapEditorElement(element)`
- `applyInlineFormat(editor, tagName)`
- `toggleUnorderedListAtSelection(editor)`
- `clearInlineFormattingAtSelection(editor)`

<verification>
- Komplettes Netz (vier Spec-Dateien) nach JEDER der beiden Gruppen grün
- `npx jest` grün, `npm run build:dev` Exit-Code 0
- Zählstand im Modul: 21 → 12
- Keine Änderung an den Netz-Spec-Dateien in diesem Plan (`git diff --name-only` listet nur `ui/editors/rich-text.js`)
- Keine Änderung an `utils/basic.js`, `utils/testable-utils.js`, `assets/styles/editors.css` (D-02)
- Zwei atomare Commits, einer je Gruppe (D-05)
</verification>

<success_criteria>
- Neun der 21 Call-Sites sind ersetzt, ohne dass eine einzige Netz-Assertion angefasst wurde
- Toggle-, Teilselektions- und Leerfall-Verhalten sind unverändert
- Es existiert kein Rückfallpfad auf die alte API
</success_criteria>

<output>
Create `.planning/phases/09-editor-regressionsnetz-execcommand-abl-sung/09-06-SUMMARY.md` when done
</output>
