---
phase: 01-stabilisierung
plan: 09
type: execute
wave: 5
depends_on: ['08']
files_modified:
  - eslint.config.js
  - package.json
  - utils/game-rules.js
  - features/quick-actions.js
  - features/dmscreen/dmscreen-render.js
  - ui/editors/markdown-converter.js
  - jest.config.cjs
  - features/dice/dice-core.js
  - "<prettier-write: alle .js/.json/.md im Repo außer Ignores>"
autonomous: true
requirements: [STAB-04]
gap_closure: true
nyquist_compliant: true

must_haves:
  truths:
    - "npm run check (tsc:check + lint + format:check) endet mit Exit-Code 0"
    - "ESLint meldet 0 echte Errors (no-case-declarations, no-dupe-keys, no-useless-escape, no-undef im Config-File alle behoben oder regelkonform herabgestuft)"
    - "Die 1196 legitimen Non-ESM-Global-Scope-Warnungen (no-undef/no-unused-vars) lassen den Gate NICHT mehr fehlschlagen"
    - "prettier --check über den npm-Glob findet 0 unformatierte Dateien"
    - "Nach der Massenformatierung bauen dev- UND production-Build weiterhin fehlerfrei (python build.py / --production, Exit 0)"
    - "Nach der Massenformatierung sind Smoke-Test (7/7) und die Jest-Suite weiterhin grün — die Build-Deduplizierung verträgt die umformatierten Deklarationen"
  artifacts:
    - path: "eslint.config.js"
      provides: "CommonJS/Node-Override für .cjs-Config-Dateien (module-Global) + max-warnings-konforme Regel-Strategie"
      contains: "*.cjs"
    - path: "package.json"
      provides: "lint-Script mit tragfähigem --max-warnings (Non-ESM-Warnungen kein Blocker)"
      contains: "max-warnings"
    - path: "utils/game-rules.js"
      provides: "no-dupe-keys behoben — doppelter 'Paladin'-Key entfernt"
      contains: "Paladin"
  key_links:
    - from: "npm run check"
      to: "Exit-Code 0"
      via: "tsc:check (grün) && lint (0 errors, warnings unter Limit) && format:check (0 Dateien)"
      pattern: "check"
    - from: "eslint.config.js Node-Override"
      to: "jest.config.cjs"
      via: "files-Glob '**/*.cjs' mit globals.module + sourceType commonjs"
      pattern: "cjs"
    - from: "prettier --write"
      to: "Build-Deduplizierung (build.py)"
      via: "Re-Build + Smoke + Jest nach Massenformatierung beweisen Verträglichkeit"
      pattern: "build.py"
---

<objective>
Schließt Gap 2 (STAB-04 / SC4 aus 01-VERIFICATION.md): `npm run check` endet mit Exit-Code 1. ESLint meldet 1227 Probleme (31 echte Errors + 1196 Warnings über `--max-warnings 50`), Prettier findet 172 unformatierte Dateien. TypeScript ist grün. Plan 06 hat das bewusst offen gelassen und für einen Gap-Closure-Plan vorgesehen — es ist aber ein Roadmap-Success-Criterion und damit ein echter Gap.

Purpose: `npm run check` und die CI-Pipeline müssen DAUERHAFT grün sein, damit künftige Brüche erkannt werden (Phasenziel: "CI erkennt künftige Brüche"). Ein dauerhaft rotes Gate ist als Schutz wertlos.

WICHTIGE EMPIRISCHE KORREKTUR (vom Planer per `eslint --fix-dry-run` verifiziert): `eslint --fix` behebt KEINEN der 31 Errors — alle (no-case-declarations, no-misleading-character-class, no-dupe-keys, no-useless-escape, no-undef) sind in diesem Kontext NICHT auto-fixbar. Die Verifikations-Empfehlung "ESLint auto-fix für die Errors" greift also nicht; dieser Plan behebt die Errors stattdessen gezielt manuell bzw. per regelkonformer Config-Anpassung.

Genaue Fehler-Verteilung (vom Planer per `eslint . --format json` ermittelt — der Executor muss NICHT erneut suchen, kann es aber zur Kontrolle):
- no-case-declarations (16): features/quick-actions.js (L120,131,132,139,146,147,154), features/dmscreen/dmscreen-render.js (L1454,1455,1460,1485,1491), ui/editors/markdown-converter.js (L81,82,88,96)
- no-misleading-character-class (11): features/dice/dice-core.js (L453 — eine Emoji-Zeichenklasse in einem Regex)
- no-dupe-keys (1): utils/game-rules.js (L80 — 'Paladin' doppelt, beide → 'd10')
- no-useless-escape (2): jest.config.cjs (L17, L26 — `\.` in regex-String-Keys)
- no-undef (1): jest.config.cjs (L2 — `module` in CommonJS-Datei, von ESLint nicht als Global erkannt)

Output: 0 ESLint-Errors, max-warnings-Strategie die die 1196 legitimen Non-ESM-Warnungen toleriert, 0 unformatierte Dateien — und ein bewiesen weiterhin grüner Build + Smoke + Jest nach der Massenformatierung.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stabilisierung/01-CONTEXT.md
@.planning/phases/01-stabilisierung/01-VERIFICATION.md
@.planning/phases/01-stabilisierung/01-06-SUMMARY.md

<interfaces>
<!-- Ist-Konfiguration und exakte Fehlerstellen. Executor implementiert gegen diese — keine Erkundung nötig. -->

package.json scripts (Ist):
  "lint": "eslint . --max-warnings 50",
  "format": "prettier --write \"**/*.{js,json,md}\"",
  "format:check": "prettier --check \"**/*.{js,json,md}\"",
  "check": "npm run tsc:check && npm run lint && npm run format:check",
  "tsc:check": "tsc --noEmit"   // grün, nicht anfassen
  WICHTIG: Der prettier-Glob ist "**/*.{js,json,md}" — er erfasst KEINE .cjs-Dateien.
  D.h. jest.config.cjs wird von format:check NICHT geprüft (muss also nicht für Prettier formatiert werden).

eslint.config.js (Ist — Flat-Config, ESM):
  - js.configs.recommended ist aktiv (liefert no-case-declarations, no-misleading-character-class, no-dupe-keys, no-undef, no-useless-escape als ERROR-Defaults).
  - Globaler Block: languageOptions.sourceType = 'module', globals = { window, document, ... , D:'writable', ... } — KEIN `module`/`require`/`__dirname` (CommonJS) deklariert.
  - JS-Block files:['**/*.js'] setzt no-undef:'warn', no-unused-vars:'warn', no-useless-escape:'warn', no-empty:'warn'.
    → Für **/*.js sind no-undef und no-useless-escape bereits 'warn'. ABER .cjs-Dateien matchen diesen Glob NICHT → dort gelten die recommended-ERROR-Defaults (das erklärt die jest.config.cjs-Errors).
  - Test-Block files:['tests/**/*.js','tests/**/*.ts'] setzt no-unused-vars:'off'.
  - Es gibt KEINEN Override für .cjs/Config-Dateien — der muss ergänzt werden.

Konkrete Fehlerstellen (Ist-Code):
  utils/game-rules.js — hitDice-Objekt:
    Zeile 75: 'Barbar': 'd12', 'Kämpfer': 'd10', 'Paladin': 'd10', 'Waldläufer': 'd10',   // deutscher 'Paladin'
    Zeile 80: 'Barbarian': 'd12', 'Fighter': 'd10', 'Paladin': 'd10', 'Ranger': 'd10',   // englischer 'Paladin' (DUPLIKAT — beide d10)
    → Fix: den englischen 'Paladin' (Zeile 80) entfernen, der deutsche bleibt. Verhalten unverändert (beide d10).

  features/quick-actions.js — switch(actionKey) ab Zeile 118, Cases 'dash'/'hide'/'search' deklarieren let/const direkt im case.
  features/dmscreen/dmscreen-render.js — switch mit lexikalischen Deklarationen (L1454-1491).
  ui/editors/markdown-converter.js — switch(tag) Cases 'a'/'ul'/'ol'/'blockquote' deklarieren const im case (L81-96).
    → Fix-Muster (no-case-declarations): den jeweiligen case-Körper in geschweifte Klammern { } einschließen.
      case 'x': { const y = ...; ...; break; }

  features/dice/dice-core.js Zeile 453:
    const cleanNotation = String(h.notation).split(' ')[0].replace(/[⬆️⬇️📊💥🪙⚔️🛡️👁️🎯]/g, '').trim();
    → Emoji-Zeichenklasse löst no-misleading-character-class (Surrogate-Pairs/kombinierte Zeichen). ENTSCHEIDUNG nötig (siehe Action (d)).

  jest.config.cjs:
    Zeile 2: module.exports = { ... }      // no-undef 'module' (CommonJS-Global fehlt in ESLint-Config)
    Zeile 17: '^.+\.tsx?$': [...]           // no-useless-escape '\.'
    Zeile 26: '^.+\.jsx?$': 'babel-jest'    // no-useless-escape '\.'
    → Fix bevorzugt per Config-Override (Node-Globals für .cjs + no-useless-escape:'warn' dort), NICHT per Regex-Semantik-Änderung in der String-Konstante.

build.py (Ist): python build.py (dev) / python build.py --production (prod). Enthält die zeilen-/regexbasierte Three-Pass-Deduplizierung (entfernt `var X = window.X;`-Duplikate und doppelte Top-Level-Funktionen). Massenformatierung kann Deklarationszeilen umbrechen → nach prettier ZWINGEND neu bauen und Smoke/Jest prüfen.
playwright.smoke.config.js: npx playwright test --config=playwright.smoke.config.js (7 Tests, lokal file://-Fallback).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ESLint grün machen — Config-Override für .cjs + manuelle Fixes der echten Errors + max-warnings-Strategie</name>
  <files>eslint.config.js, package.json, utils/game-rules.js, features/quick-actions.js, features/dmscreen/dmscreen-render.js, ui/editors/markdown-converter.js, jest.config.cjs, features/dice/dice-core.js</files>
  <read_first>
    - eslint.config.js (KOMPLETT — Flat-Config-Struktur: js.configs.recommended, globaler Block mit globals, JS-Block files:['**/*.js'], Test-Block; Stelle, an der ein .cjs-Override eingefügt wird)
    - package.json (Zeile 23 "lint": "eslint . --max-warnings 50"; Zeile 29 "check")
    - utils/game-rules.js (Zeilen 72-87 — hitDice-Objekt mit den zwei 'Paladin'-Keys)
    - features/quick-actions.js (Zeilen 118-160 — switch(actionKey), Cases mit let/const)
    - features/dmscreen/dmscreen-render.js (Zeilen 1448-1495 — switch mit Deklarationen)
    - ui/editors/markdown-converter.js (Zeilen 76-103 — switch(tag), Cases 'a'/'ul'/'ol'/'blockquote')
    - features/dice/dice-core.js (Zeilen 448-460 — Emoji-Regex Zeile 453)
    - jest.config.cjs (KOMPLETT — module.exports, die zwei `\.`-Regex-Strings)
    - 01-06-SUMMARY.md (Quality-Gate-Befund: 31 errors-Kategorien, 1196 warnings sind no-undef/no-unused-vars = legitim in Non-ESM)
    - CLAUDE.md (Non-ESM-Global-Scope-Architektur — warum no-undef/no-unused-vars NICHT als blocking errors sinnvoll sind)
  </read_first>
  <action>
    Ziel: `npm run lint` endet mit Exit-Code 0 (0 Errors UND Warnings unter Limit). Da `eslint --fix` hier NICHTS löst (vom Planer verifiziert), gehe gezielt vor. Feedback-Loop (CLAUDE.md): vor jeder Code-Änderung kurz beschreiben, wie du sie prüfst (hier: `npx eslint <datei>` pro Fundstelle).

    **(a) no-dupe-keys in utils/game-rules.js (echter Lint-Bug).** Im `hitDice`-Objekt (Zeile 80) den englischen Duplikat-Key entfernen: streiche `'Paladin': 'd10', ` aus der Zeile `'Barbarian': 'd12', 'Fighter': 'd10', 'Paladin': 'd10', 'Ranger': 'd10',`, sodass dort `'Barbarian': 'd12', 'Fighter': 'd10', 'Ranger': 'd10',` steht. Der deutsche `'Paladin': 'd10'` (Zeile 75) bleibt — Verhalten unverändert (beide mappten auf 'd10'). KEINEN Key blind löschen, ohne dass der Wert erhalten bleibt (hier identisch, daher sicher).

    **(b) no-case-declarations in 3 Dateien (16 Fälle).** Schließe in jedem betroffenen `case` den Körper in geschweifte Klammern ein, sodass die `let`/`const`-Deklaration einen Block-Scope hat. Muster:
    ```javascript
    // vorher:
    case 'dash':
        let speed = '9m';
        ...
        break;
    // nachher:
    case 'dash': {
        let speed = '9m';
        ...
        break;
    }
    ```
    Wende das an in:
    - features/quick-actions.js: Cases 'dash' (L119-128), 'hide' (L129-141), 'search' (L145-156) — die Cases ohne eigene Deklaration ('help', 'useObject') brauchen keine Klammern.
    - features/dmscreen/dmscreen-render.js: die Cases um L1454-1491, die `let`/`const` deklarieren.
    - ui/editors/markdown-converter.js: Cases 'a' (L80-84), 'ul'/'ol' (L86-93), 'blockquote' (L95-98) — der `const`-deklarierende Block in Klammern fassen. Bei fallthrough-Cases ('ul' fällt in 'ol') die Klammern um den GEMEINSAMEN Körper nach dem letzten case-Label setzen.
    Verhalten bleibt identisch — nur Block-Scope wird ergänzt.

    **(c) jest.config.cjs (no-undef 'module' + 2× no-useless-escape) — per Config-Override lösen, NICHT per Code-Semantik-Änderung.** Füge in `eslint.config.js` einen neuen Override-Block für CommonJS-Config-Dateien hinzu (VOR dem finalen ignores-Block):
    ```javascript
    // CommonJS-Konfigurationsdateien (.cjs) — Node-Globals + entschärfte Stil-Regeln
    {
        files: ['**/*.cjs'],
        languageOptions: {
            sourceType: 'commonjs',
            globals: {
                module: 'writable',
                require: 'readonly',
                __dirname: 'readonly',
                process: 'readonly'
            }
        },
        rules: {
            'no-useless-escape': 'warn'
        }
    }
    ```
    Das behebt `no-undef` für `module` (CommonJS-Global) und stuft `no-useless-escape` in .cjs auf 'warn' herab (konsistent mit dem .js-Block, der das bereits tut). Die `\.`-Escapes in den Regex-String-Keys bleiben damit unverändert (Semantik wird NICHT angetastet). HINWEIS: jest.config.cjs liegt außerhalb des prettier-Globs ({js,json,md}), muss also für Task 2 nicht formatiert werden.

    **(d) no-misleading-character-class in features/dice/dice-core.js:453 (11 Treffer, eine Zeile) — ENTSCHEIDUNG.** Die Emoji-Zeichenklasse `/[⬆️⬇️📊💥🪙⚔️🛡️👁️🎯]/g` strippt Emoji-Präfixe aus Würfel-Notationen (funktional korrekt). Zwei zulässige Wege — wähle (d-1) als Default, (d-2) nur falls (d-1) das Verhalten messbar ändert:
    - (d-1) BEVORZUGT — Regel regelkonform herabstufen: ergänze im JS-Block (`files: ['**/*.js']`) von eslint.config.js die Regel `'no-misleading-character-class': 'warn',`. Begründung: identisch zur bestehenden Linie (no-undef/no-useless-escape sind dort bereits 'warn', weil der Bestand pragmatisch ist); das Verhalten des Emoji-Strippings bleibt unangetastet, kein Risiko einer Regex-Verhaltensänderung am Spieltisch. Dokumentiere als Kommentar: `// Emoji-Zeichenklassen im Bestand (dice-core.js) — bewusst 'warn', kein u-Flag-Umbau in der Stabilisierungsphase`.
    - (d-2) ALTERNATIV (nur wenn ausdrücklich gewünscht, NICHT Default): Regex auf das `u`-Flag + Unicode-Code-Points umstellen. Risiko: Surrogate-/ZWJ-Emoji (⚔️ = Schwert + Variationsselektor) verhalten sich mit `u`-Flag anders → könnte das Stripping verändern. Wenn dieser Weg gewählt wird: vorher/nachher mit einem Mini-Test gegen reale Notationsstrings vergleichen.
    Da CONTEXT.md "reine Stabilisierung, keine Verhaltensänderung" vorgibt, ist (d-1) der korrekte Default.

    **(e) max-warnings-Strategie (Kern von SC4).** Nach (a)-(d) sind 0 Errors übrig, aber ~1196 legitime Warnungen (no-undef/no-unused-vars im Non-ESM-Global-Scope, plus ggf. no-misleading-character-class als 'warn'). `--max-warnings 50` lässt den Gate weiter fehlschlagen. Passe in package.json das `lint`-Script an, sodass die legitimen Warnungen den Gate NICHT blockieren, künftige NEUE Probleme aber sichtbar bleiben. Konkret: ändere
    ```json
    "lint": "eslint . --max-warnings 50",
    ```
    in
    ```json
    "lint": "eslint .",
    ```
    Damit failt `lint` nur noch bei ECHTEN Errors (Severity 2) — Warnungen werden gemeldet, blockieren aber nicht. Das ist die saubere Lösung für eine Non-ESM-Global-Scope-Codebasis (no-undef/no-unused-vars sind hier strukturell unvermeidbar und als 'warn' korrekt klassifiziert). Lasse `lint:all`/`lint:fix` unverändert. Aktualisiere KEINE anderen Scripts. (Begründung dokumentiert die Empfehlung aus 01-06-SUMMARY: "no-undef/no-unused-vars auf warn, max-warnings-Strategie überdenken".)
    Falls stattdessen ein hohes Limit gewünscht ist (z.B. um Warnungs-Wachstum zu deckeln): `eslint . --max-warnings 1300` ist zulässig — aber `eslint .` ohne Limit ist robuster, weil es bei neuen Warnungen nicht plötzlich kippt. Wähle `eslint .` als Default.

    CLAUDE.md-Regeln: keine doppelten Top-Level-Funktionen einführen; keine `const X = window.X` in Funktionen; deutsche UI-Texte sind hier nicht betroffen (kein UI-String geändert). Keine Verhaltensänderung an Spiel-Logik (nur Block-Scope-Klammern + ein entfernter Duplikat-Key + Config).
  </action>
  <verify>
    <automated>cd "D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular" && npx eslint . 2>&1 | tail -15; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `npx eslint .` endet mit Exit-Code 0 (0 Errors; Warnungen blockieren nicht mehr) — der `echo "EXIT:$?"` zeigt `EXIT:0`
    - 0 echte Errors: `npx eslint . --format json` enthält keinen Eintrag mit `"severity":2` (per `node`-Filter prüfbar)
    - no-dupe-keys weg: `grep -c "'Paladin': 'd10'" utils/game-rules.js` gibt `1` zurück (nur noch der deutsche Key)
    - no-case-declarations weg: `npx eslint features/quick-actions.js features/dmscreen/dmscreen-render.js ui/editors/markdown-converter.js --rule '{"no-case-declarations":"error"}' 2>&1 | grep -c "no-case-declarations"` gibt `0` zurück
    - .cjs-Override vorhanden: `grep -c "'\\*\\*/\\*.cjs'" eslint.config.js` gibt mindestens `1` zurück (oder `grep -c "cjs" eslint.config.js` >= 1) und `grep -c "module:" eslint.config.js` gibt mindestens `1` zurück
    - jest.config.cjs sauber: `npx eslint jest.config.cjs 2>&1 | grep -cE "no-undef|no-useless-escape" ` gibt `0` zurück (als Error; ggf. nur als Warnung)
    - lint-Script angepasst: `grep -c '"lint": "eslint ."' package.json` gibt `1` zurück (kein `--max-warnings 50` mehr im lint-Script)
    - `npm run lint` endet mit Exit-Code 0
  </acceptance_criteria>
  <done>ESLint meldet 0 Errors; die echten Bug-Klassen (Dupe-Key, Case-Declarations, .cjs-module/escape) sind behoben bzw. regelkonform konfiguriert; die legitimen Non-ESM-Warnungen blockieren den Gate nicht mehr; `npm run lint` ist grün.</done>
</task>

<task type="auto">
  <name>Task 2: Prettier-Massenformatierung + Build/Smoke/Jest-Re-Verifikation (npm run check grün)</name>
  <files>package.json, "<prettier --write über **/*.{js,json,md} — formatiert ~172 Dateien>"</files>
  <read_first>
    - package.json ("format": "prettier --write \"**/*.{js,json,md}\"", "format:check", "check")
    - CLAUDE.md (Abschnitt "Build System & Deduplication Pattern" + "Duplicate Declaration Debugging Pattern" — WARUM Massenformatierung die zeilen-/regexbasierte Dedup in build.py gefährden kann: `var X = window.X;`-Zeilen, function-Deklarationen, die umgebrochen werden könnten)
    - .planning/phases/01-stabilisierung/01-VERIFICATION.md (Spot-Checks: python build.py / --production Exit 0; Smoke 7/7; jest grün — die Baseline, die nach Formatierung erhalten bleiben MUSS)
  </read_first>
  <action>
    REIHENFOLGE ZWINGEND: Dieser Task läuft NACH Task 1 (damit prettier auch die in Task 1 manuell editierten Dateien mitformatiert) und als LETZTER Schritt der Phase. Feedback-Loop: vor der Massenänderung beschreiben, dass die Verifikation = voller Build + Smoke + Jest ist.

    **(a) Massenformatierung ausführen.** Führe `npm run format` aus (das ist `prettier --write "**/*.{js,json,md}"`). Das formatiert ~172 Dateien, inkl. der in Task 1 geänderten und inkl. systems/spellslots/quick-roll.js (deshalb läuft dieser Plan in Welle 5 NACH Gap-1-Plan 01-08 — sonst Reformatierung einer Datei, die ein paralleler Worktree gerade ändert).

    **(b) ESLint-Re-Check nach Formatierung.** Prettier kann theoretisch keine neuen ESLint-Errors einführen (Stil ≠ Semantik), aber verifiziere: `npx eslint .` weiterhin Exit-Code 0. Falls wider Erwarten ein Error auftaucht (z.B. ein durch Umbruch sichtbar gewordener Fall), behebe ihn nach dem Muster aus Task 1.

    **(c) Build-Re-Verifikation (KRITISCH — Dedup-Verträglichkeit).** Baue beide Bundles frisch und prüfe Exit-Code 0:
    - `python build.py` (dev) → Exit 0, erzeugt dist/dnd-tracker-bundled.html
    - `python build.py --production` → Exit 0, erzeugt dist/dnd-tracker-optimized.html
    Hintergrund: build.py dedupliziert `var X = window.X;`-Zeilen und doppelte Top-Level-Funktionen zeilen-/regexbasiert. Prettier könnte solche Zeilen umbrechen/normalisieren. Falls der Build abbricht oder das Bundle einen SyntaxError enthält (siehe CLAUDE.md "Illegal return statement"/"already been declared"), ist das ein echter Befund — analysiere die betroffene Deklaration und stelle sicher, dass sie nach Formatierung weiterhin von den Dedup-Pässen erkannt wird (ggf. minimale manuelle Korrektur der betroffenen Zeile; NICHT prettier-Regeln global aushebeln).

    **(d) Smoke-Test + Jest-Re-Verifikation.** Beweise, dass das frisch gebaute Bundle bootet und die Suiten grün bleiben:
    - `npx playwright test --config=playwright.smoke.config.js` → 7/7 grün
    - `npx jest --no-coverage` → gesamte Unit-Suite grün (insb. stability.test.js 56/56, migration.test.js 3/3, storage-conflict.test.js aus Plan 01-08)
    - `python -m pytest tests/build/ -v` → 10/10 grün (Build-Dedup-Tests verträglich mit Formatierung)

    **(e) Finaler Gate-Beweis.** `npm run check` (tsc:check && lint && format:check) muss mit Exit-Code 0 enden. `format:check` darf 0 unformatierte Dateien melden (nach (a) erfüllt), `lint` 0 Errors (Task 1), `tsc:check` war und bleibt grün.

    KEINE Änderung an build.py, an der Spiel-Logik oder an den Tests in diesem Task (außer einer minimalen Zeilenkorrektur, falls (c) einen echten Dedup-Konflikt aufdeckt — dann im SUMMARY als Deviation dokumentieren). Prettier-Config NICHT aufweichen, um Dateien zu überspringen.
  </action>
  <verify>
    <automated>cd "D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular" && npm run check 2>&1 | tail -20; echo "CHECK_EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` endet mit Exit-Code 0 — der `echo "CHECK_EXIT:$?"` zeigt `CHECK_EXIT:0`
    - `npm run format:check` endet mit Exit-Code 0 (0 unformatierte Dateien; "All matched files use Prettier code style!" oder keine "Code style issues")
    - `npm run lint` endet mit Exit-Code 0 (0 Errors)
    - dev-Build grün: `python build.py` Exit-Code 0; `test -f dist/dnd-tracker-bundled.html` Exit 0
    - prod-Build grün: `python build.py --production` Exit-Code 0; `test -f dist/dnd-tracker-optimized.html` Exit 0
    - Smoke grün: `npx playwright test --config=playwright.smoke.config.js` Exit-Code 0 (7 passed)
    - Jest grün: `npx jest --no-coverage` Exit-Code 0 (inkl. storage-conflict.test.js, stability.test.js, migration.test.js)
    - Build-Dedup-Tests grün: `python -m pytest tests/build/ -v` Exit-Code 0 (10 passed)
  </acceptance_criteria>
  <done>Alle Quelldateien sind Prettier-konform; npm run check ist mit Exit-Code 0 grün; dev- und prod-Build, Smoke-Test (7/7) und die gesamte Jest- und pytest-Suite bestätigen, dass die Build-Deduplizierung die Massenformatierung verträgt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Massenänderung (~172 Dateien) → Production-Bundle | Eine Formatierung über fast alle Quelldateien kann die zeilen-/regexbasierte Build-Deduplizierung (build.py) brechen und damit ein fehlerhaftes Bundle erzeugen, das am Spieltisch lädt. |
| Lint-/Format-Config → künftige Fehlererkennung | Zu weiche Regeln/Limits würden echte künftige Bugs maskieren (Gate verliert Schutzwirkung). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09-01 | Tampering / Availability | prettier --write bricht Build-Dedup → Bundle mit SyntaxError ("already declared"/"illegal return") | mitigate | Pflicht-Re-Verifikation nach Formatierung: dev+prod-Build Exit 0, Smoke 7/7, jest + pytest grün (acceptance_criteria erzwingen alle); bei Konflikt minimale gezielte Zeilenkorrektur statt Config-Aufweichung |
| T-01-09-02 | Repudiation / Detection-Gap | `eslint .` ohne max-warnings könnte echte NEUE Errors verbergen | accept→mitigate | Errors (Severity 2) failen weiterhin den Gate (kein `--max-warnings` deaktiviert Errors, nur Warnungen); no-dupe-keys/no-case-declarations bleiben Error-Default und fangen reale Bugs |
| T-01-09-03 | Tampering | no-dupe-keys blind auto-fixen könnte den falschen Key gewinnen lassen (potenzieller Bug) | mitigate | Paladin-Duplikat manuell geprüft (beide → 'd10', Wert identisch) statt blind --fix; Verhalten nachweislich unverändert |
| T-01-09-04 | Supply-Chain | prettier/eslint sind Build-Zeit-Devtools (kein Runtime-Dependency) | accept | Beide bereits in devDependencies gepinnt (package.json); keine neuen Abhängigkeiten in diesem Plan |

Hinweis: Keine neuen Runtime-Eingabe-/Netzwerk-/innerHTML-Oberflächen. `esc()`/XSS nicht betroffen. Reiner Lint-/Format-/Config-Plan plus Verifikation.
</threat_model>

<verification>
- `npm run check` Exit-Code 0 (tsc:check + lint + format:check)
- `npx eslint . --format json` enthält keinen `"severity":2`-Eintrag (0 Errors)
- `python build.py` und `python build.py --production` Exit 0
- `npx playwright test --config=playwright.smoke.config.js` 7/7 grün
- `npx jest --no-coverage` grün (inkl. storage-conflict.test.js, stability.test.js)
- `python -m pytest tests/build/ -v` 10/10 grün
</verification>

<success_criteria>
- npm run check dauerhaft grün (STAB-04 / SC4 erfüllt); CI-lint-and-typecheck-Job wird grün
- 0 echte ESLint-Errors; legitime Non-ESM-Warnungen blockieren den Gate nicht
- Massenformatierung bewiesen verträglich mit Build-Deduplizierung (Build + Smoke + Jest + pytest grün)
</success_criteria>

<output>
Nach Abschluss: Erstelle `.planning/phases/01-stabilisierung/01-09-SUMMARY.md`
</output>
</content>
