---
phase: 01-stabilisierung
plan: 06
type: execute
wave: 3
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [STAB-08, STAB-04, STAB-03]
nyquist_compliant: true

must_haves:
  truths:
    - "Die CI baut den dist-Build und fuehrt einen Playwright-Smoke-Test gegen ihn aus"
    - "Der Smoke-Test laeuft in CI gegen einen lokalen HTTP-Server (nicht file://)"
    - "npm run check (ESLint + TypeScript + Prettier) laeuft lokal und in CI gruen"
    - "Frische dev- und production-Builds sind aus dem aktuellen Quellcode erzeugt"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "smoke-test-Job mit HTTP-Server, build-Artefakt-Download, Playwright-Smoke"
      contains: "smoke-test"
  key_links:
    - from: ".github/workflows/ci.yml smoke-test-Job"
      to: "tests/e2e/smoke.spec.js (aus Plan 01)"
      via: "npx playwright test --config=playwright.smoke.config.js gegen SMOKE_BASE_URL"
      pattern: "smoke"
    - from: ".github/workflows/ci.yml smoke-test-Job"
      to: "production-build-Artefakt (build-Job)"
      via: "actions/download-artifact + python http.server"
      pattern: "download-artifact"
---

<objective>
Integriert den Smoke-Test in die CI-Pipeline (STAB-08) und verankert die Quality-Gate-Verifikation der gesamten Phase: npm run check muss gruen sein (STAB-04) und frische Builds aus dem reparierten Code muessen fehlerfrei laden (STAB-03). Dies ist ein Wave-3-Plan, weil er die Code-Aenderungen aller vorherigen Plaene voraussetzt.

Purpose: "CI erkennt Boot-Crashes" (Erfolgskriterium 4). Der Smoke-Test gegen den dist-Build schliesst die dokumentierte Luecke (kein automatisierter dist-Smoke-Test). Der Quality-Gate stellt sicher, dass alle Code-Plaene zusammen lint-/typecheck-/format-sauber sind.
Output: Erweiterte ci.yml mit smoke-test-Job; verifizierte gruene check-Suite und frische Builds.

Claude's-Discretion-Entscheidungen (aus RESEARCH.md): Smoke gegen lokalen HTTP-Server (python -m http.server) statt file:// wegen Playwright-localStorage-/SW-Einschraenkungen unter file://; Umfang Boot + 6-Tab-Sweep; CI baut den dist-Build selbst im build-Job und der smoke-test-Job laedt das Artefakt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stabilisierung/01-CONTEXT.md
@.planning/phases/01-stabilisierung/01-RESEARCH.md
@.planning/phases/01-stabilisierung/01-PATTERNS.md
@.planning/phases/01-stabilisierung/01-01-SUMMARY.md

<interfaces>
<!-- Reale CI-Struktur und die in Plan 01 erstellten Smoke-Artefakte. -->

.github/workflows/ci.yml — bestehende Jobs:
  lint-and-typecheck (npm run typecheck; npm run lint)   // Zeilen 10-20
  test (npm test)                                        // Zeilen 22-31
  build (needs: [lint-and-typecheck, test])              // Zeilen 33-51
    python build.py --production                         // Zeile 46 (CI: python3 verfuegbar, aber python wird durch setup-python bereitgestellt)
    upload-artifact name: production-build, path: dist/dnd-tracker-optimized.html  // Zeilen 47-51

Aus Plan 01 vorhanden:
  tests/e2e/smoke.spec.js                 // Boot + 6-Tab-Sweep, nutzt process.env.SMOKE_BASE_URL
  playwright.smoke.config.js              // testMatch smoke.spec.js, baseURL = SMOKE_BASE_URL || file://-Fallback

Empfohlenes CI-Pattern (RESEARCH.md Zeilen 266-295):
  smoke-test-Job: needs [build]; download-artifact production-build nach dist/;
  python -m http.server 8000 --directory dist & ; SMOKE_BASE_URL=http://localhost:8000/dnd-tracker-optimized.html

Quality-Gate-Befehle:
  npm run check   = tsc --noEmit && eslint . --max-warnings 50 && prettier --check
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: smoke-test-Job in ci.yml ergaenzen (HTTP-Server + Artefakt + Playwright)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (Komplett — bestehende Job-Struktur, build-Job Artefakt-Upload)
    - tests/e2e/smoke.spec.js (aus Plan 01 — SMOKE_BASE_URL-Mechanik)
    - playwright.smoke.config.js (aus Plan 01 — testMatch, baseURL)
    - .planning/phases/01-stabilisierung/01-RESEARCH.md (Abschnitt CI-Smoke-Test — vollstaendiger Job-YAML; Pitfall 5: needs [build] + download-artifact)
    - .planning/phases/01-stabilisierung/01-PATTERNS.md (Abschnitt ci.yml — smoke-test-Job-Pattern)
  </read_first>
  <action>
    Ergaenze in .github/workflows/ci.yml nach dem bestehenden build-Job einen neuen smoke-test-Job:
    ```yaml
      smoke-test:
        runs-on: ubuntu-latest
        needs: [build]
        steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-node@v4
            with:
              node-version: '20'
              cache: 'npm'
          - uses: actions/setup-python@v5
            with:
              python-version: '3.x'
          - run: npm ci
          - run: npx playwright install --with-deps chromium
          - uses: actions/download-artifact@v4
            with:
              name: production-build
              path: dist/
          - name: Starte HTTP-Server
            run: python -m http.server 8000 --directory dist &
            shell: bash
          - name: Warte auf Server
            run: sleep 2
          - name: Smoke-Test
            run: npx playwright test --config=playwright.smoke.config.js
            env:
              SMOKE_BASE_URL: http://localhost:8000/dnd-tracker-optimized.html
    ```
    Wichtige Details (Pitfall 5): needs: [build] sorgt dafuer, dass das production-build-Artefakt existiert; download-artifact laedt es nach dist/. Der Smoke-Test laeuft gegen dnd-tracker-optimized.html (Production-Build) ueber den HTTP-Server. Die smoke.config nutzt SMOKE_BASE_URL — in CI also den HTTP-Server, kein file://. Aendere die bestehenden Jobs (lint-and-typecheck, test, build) NICHT; nur den neuen Job anfuegen. Achte auf korrekte YAML-Einrueckung (Job auf gleicher Ebene wie build).
  </action>
  <verify>
    <automated>cd "D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular" && node -e "var s=require('fs').readFileSync('.github/workflows/ci.yml','utf8'); var ok = s.indexOf('smoke-test:')>=0 && s.indexOf('download-artifact')>=0 && s.indexOf('SMOKE_BASE_URL')>=0 && s.indexOf('playwright.smoke.config.js')>=0; process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "smoke-test:" .github/workflows/ci.yml gibt 1 zurueck
    - grep -c "needs: \[build\]" .github/workflows/ci.yml gibt mindestens 1 zurueck (smoke-test haengt am build-Job)
    - grep -c "SMOKE_BASE_URL" .github/workflows/ci.yml gibt 1 zurueck
    - grep -c "playwright.smoke.config.js" .github/workflows/ci.yml gibt 1 zurueck
    - grep -c "http.server 8000" .github/workflows/ci.yml gibt 1 zurueck
    - node-YAML-Substring-Check oben Exit-Code 0
  </acceptance_criteria>
  <done>ci.yml hat einen smoke-test-Job, der das production-Artefakt ueber einen HTTP-Server smoke-testet; bestehende Jobs unveraendert.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-Quality-Gate VERIFIZIEREN — frische Builds + npm run check + lokaler Smoke (reiner Verifikations-Task)</name>
  <files>(keine — reiner Verifikations-Task; dieser Task aendert KEINE Dateien)</files>
  <read_first>
    - .github/workflows/ci.yml (nach Task 1 — finaler Stand)
    - package.json (scripts: check, build:dev, build:prod — nach Plan 05 mit python)
    - .planning/phases/01-stabilisierung/01-VALIDATION.md (Phase-Gate-Abschnitt — Voll-Suite-Befehle)
  </read_first>
  <action>
    Dies ist ein REINER VERIFIKATIONS-TASK fuer das phasenweite Quality-Gate (STAB-04, STAB-03). Er erzeugt nur frische Build-Artefakte und fuehrt die Pruefbefehle aus. Er DARF KEINE Quell-, Konfig- oder Testdateien aendern — keine Format-/Lint-Reparaturen, keine Logikaenderungen. Wenn eine Pruefung fehlschlaegt, ist das ein ECHTER Phasen-Fehler eines vorherigen Plans: STOPPE und melde den Fehler im SUMMARY (welcher Befehl, welche Datei, welche Meldung) zur Behandlung durch einen gezielten Gap-Closure-Plan. Repariere NICHT spontan fremden oder phaseneigenen Code in diesem Task — Reparaturen gehoeren in den jeweils zustaendigen Plan (01-05) bzw. einen Folge-Plan.

    Schritte (alle nur ausfuehren, nicht editieren):
    1. Frische Builds erzeugen: `python build.py` (dev) und `python build.py --production`. Beide muessen Exit-Code 0 liefern (Plan 04 haertet build.py; Plan 03 entfernt Mindmap-Reste). Das Erzeugen von dist-Artefakten gilt nicht als Quelldatei-Aenderung.
    2. `npm run check` ausfuehren (tsc --noEmit + eslint --max-warnings 50 + prettier --check). Muss Exit-Code 0 liefern. Bei Fehlern: NICHT reparieren — Befund (Datei + Regel + Meldung) ins SUMMARY aufnehmen und Task als blockiert melden.
    3. Lokalen Smoke-Test gegen den frischen Production-Build laufen lassen: setze SMOKE_BASE_URL auf den lokalen file://- oder HTTP-Pfad zu dist/dnd-tracker-optimized.html und fuehre `npx playwright test --config=playwright.smoke.config.js` aus; alternativ ohne SMOKE_BASE_URL gegen den dev-Build (file://-Fallback). Muss gruen sein (beweist STAB-03 Konsolen-Fehlerfreiheit + STAB-08 lokal).

    Da dieser Task keine Dateien aendert, entfaellt die fruehere "minimal-invasive Reparatur"-Option vollstaendig (Checker-Issue 2/7): die Erfolgsbedingung ist abschliessend messbar (drei gruene Befehle), und der Plan bleibt autonom ohne implizite menschliche Entscheidung.
  </action>
  <verify>
    <automated>cd "D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular" && python build.py && python build.py --production && npm run check && npx playwright test --config=playwright.smoke.config.js</automated>
  </verify>
  <acceptance_criteria>
    - python build.py endet mit Exit-Code 0 und erzeugt dist/dnd-tracker-bundled.html
    - python build.py --production endet mit Exit-Code 0 und erzeugt dist/dnd-tracker-optimized.html
    - npm run check endet mit Exit-Code 0 (tsc --noEmit + eslint --max-warnings 50 + prettier --check alle gruen)
    - npx playwright test --config=playwright.smoke.config.js laeuft gruen gegen den frischen Build (alle 7 Smoke-Tests passing, Exit-Code 0)
    - KEINE Quell-/Konfig-/Testdatei wurde von diesem Task geaendert (git status zeigt ausser dist/-Artefakten keine Modifikation durch Task 2)
  </acceptance_criteria>
  <done>Frische dev- und prod-Builds erzeugt; npm run check gruen; Smoke-Test gruen gegen den frischen Build (STAB-03, STAB-04, STAB-08 lokal bestaetigt). Bei rotem Gate: Task stoppt und meldet den Befund — keine spontane Reparatur.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI-Runner -> dist-Artefakt -> Smoke-Test | Der Smoke-Test laedt das gebaute Artefakt ueber einen lokalen HTTP-Server; kein externer Input, Offline-Modell. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Denial of Service | Unentdeckter Boot-Crash erreicht main ohne CI-Gate | mitigate | smoke-test-Job (Task 1) prueft App-Init + 6 Tabs gegen dist-Build bei jedem PR/Push |
| T-06-02 | Tampering | Smoke-Test gegen falsches/fehlendes Artefakt | accept | needs [build] + download-artifact stellen das korrekte production-Artefakt sicher; geringes Risiko |
</threat_model>

<verification>
- python build.py und python build.py --production Exit-Code 0
- npm run check Exit-Code 0
- npx playwright test --config=playwright.smoke.config.js gruen
- grep -c "smoke-test:" .github/workflows/ci.yml == 1
- CI (nach Push, ausserhalb dieses Plans): smoke-test-Job gruen
</verification>

<success_criteria>
- CI smoke-testet den dist-Build gegen HTTP-Server (STAB-08)
- npm run check gruen (STAB-04); frische Builds laden fehlerfrei (STAB-03)
</success_criteria>

<output>
Nach Abschluss: Erstelle .planning/phases/01-stabilisierung/01-06-SUMMARY.md
</output>
</content>
