---
phase: 01-stabilisierung
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - package.json
  - core/config.js
  - validate.py
  - main.js
  - tsconfig.json.backup
  - MIGRATION_REPORT.md
autonomous: true
requirements: [STAB-09, STAB-06]
nyquist_compliant: true

must_haves:
  truths:
    - "package.json license ist MIT (konsistent mit LICENSE und README-Badge)"
    - "APP_CONFIG.VERSION und package.json version sind synchron 2.6.1"
    - "npm-Scripts rufen python statt python3 auf (Windows-kompatibel)"
    - "validate.py nutzt skript-relative Pfade statt Linux-Hardcode; npm run validate laeuft"
    - "Tote Dateien main.js, tsconfig.json.backup, MIGRATION_REPORT.md sind entfernt"
  artifacts:
    - path: "package.json"
      provides: "license MIT, version 2.6.1, python statt python3 in scripts"
      contains: "\"license\": \"MIT\""
    - path: "core/config.js"
      provides: "VERSION 2.6.1"
      contains: "VERSION: '2.6.1'"
    - path: "validate.py"
      provides: "Skript-relativer SOURCE_DIR"
      contains: "Path(__file__)"
  key_links:
    - from: "package.json version"
      to: "core/config.js VERSION"
      via: "beide auf 2.6.1 (D-06 Patch-Bump)"
      pattern: "2\\.6\\.1"
---

<objective>
Repo-Hygiene und Konfigurations-Konsistenz (STAB-09) plus der Versions-Bump-Teil von STAB-06: Lizenz-Metadaten korrigieren (ISC -> MIT, D-17), App-Version auf 2.6.1 anheben (D-06, synchron in config.js und package.json), Windows-kompatible npm-Scripts (python statt python3), validate.py reparieren und tote Dateien entfernen.

Purpose: Entwicklerwerkzeuge muessen auf der primaeren Dev-OS (Windows) funktionieren; Lizenz und Version duerfen nicht widerspruechlich sein. Reines Aufraeumen ohne Logikaenderung — niedrigster Failure-Mode-Anteil der Phase.
Output: Korrigierte package.json, config.js mit 2.6.1, reparierte validate.py, drei geloeschte Altdateien.

Umgesetzte Entscheidungen: D-06 (Version 2.6.1 synchron), D-12 (validate.py reparieren — script-relative Pfade), D-17 (Lizenz MIT). Claude's Discretion: tote Dateien loeschen, python3 -> python.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stabilisierung/01-CONTEXT.md
@.planning/phases/01-stabilisierung/01-RESEARCH.md

<interfaces>
<!-- Reale Stellen aus dem Quellcode. -->

package.json:
  "version": "2.6.0"                 // Zeile 3 -> 2.6.1
  "license": "ISC"                   // Zeile 46 -> MIT
  scripts mit python3 (Zeilen 8-18): analyze:render, analyze:handlers, split:render, migrate:handlers,
    build, build:dev, build:prod, build:minify, dev, serve, validate  // alle python3 -> python
  HINWEIS: analyze:render/analyze:handlers/split:render/migrate:handlers verweisen auf Tools,
    die Plan-uebergreifend (D-12) geloescht werden — diese npm-Scripts ebenfalls entfernen oder
    auf den python-Aufruf umstellen; siehe action.

core/config.js:
  VERSION: '2.6.0'                   // Zeile 8 -> 2.6.1

validate.py:
  SOURCE_DIR = '/mnt/user-data/outputs/dnd-tracker-modular'   // Zeile 11 -> Path(__file__)-relativ
  Pattern aus tools/purge-css.py:12: SOURCE_DIR = str(Path(__file__).parent)

Tote Dateien im Repo-Root: main.js, tsconfig.json.backup, MIGRATION_REPORT.md (alle existieren, von index.html/live-Code nicht referenziert)

ACHTUNG Tool-Loeschungen aus D-12 (analyze-render.py, migrate-event-handlers.py, split-shops.py, purge-css.py)
  sind NICHT Teil dieses Plans (Datei-Ownership) — aber die zugehoerigen npm-Scripts hier bereinigen,
  damit package.json keine kaputten Verweise behaelt.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: package.json — Lizenz MIT, Version 2.6.1, python3 -> python, tote Tool-Scripts bereinigen</name>
  <files>package.json</files>
  <read_first>
    - package.json (Komplett — version Zeile 3, scripts Zeilen 7-38, license Zeile 46)
    - .planning/phases/01-stabilisierung/01-CONTEXT.md (D-06, D-17)
    - .planning/codebase/CONCERNS.md (Abschnitt "Stale/broken developer tooling" und "License metadata mismatch")
  </read_first>
  <action>
    Bearbeite package.json:
    (a) Lizenz (D-17): "license": "ISC" -> "license": "MIT" (Zeile 46).
    (b) Version (D-06): "version": "2.6.0" -> "version": "2.6.1" (Zeile 3).
    (c) python3 -> python in allen scripts: Ersetze in den Skripten "python3" durch "python":
        - "build": "python build.py --production"
        - "build:dev": "python build.py"
        - "build:prod": "python build.py --production"
        - "build:minify": "python build.py --minify"
        - "dev": "python -m http.server 8000"
        - "serve": "python -m http.server 8000"
        - "validate": "python validate.py"
    (d) Tote Tool-Scripts entfernen: Loesche die vier Skript-Eintraege, deren Tools per D-12 entfernt werden:
        "analyze:render", "analyze:handlers", "split:render", "migrate:handlers".
        Begruendung: analyze-render.py/migrate-event-handlers.py werden geloescht (Plan fuer Tool-Dateien separat, aber die package.json-Verweise wuerden sonst ins Leere zeigen). Behalte "validate" (validate.py wird in Task 3 repariert, nicht geloescht).
    Achte auf gueltiges JSON (keine haengenden Kommata). Nach dem Edit: npm-Parsing muss fehlerfrei sein.
  </action>
  <verify>
    <automated>cd "D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular" && node -e "var p=require('./package.json'); if(p.license!=='MIT') throw new Error('license '+p.license); if(p.version!=='2.6.1') throw new Error('version '+p.version); var s=JSON.stringify(p.scripts); if(s.indexOf('python3')>=0) throw new Error('python3 still present'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - node -e Check oben endet mit Exit-Code 0 und gibt "ok" aus
    - grep -c "\"license\": \"MIT\"" package.json gibt 1 zurueck
    - grep -c "\"version\": \"2.6.1\"" package.json gibt 1 zurueck
    - grep -c "python3" package.json gibt 0 zurueck
    - grep -c "analyze:render\|migrate:handlers" package.json gibt 0 zurueck (tote Scripts entfernt)
  </acceptance_criteria>
  <done>package.json: MIT-Lizenz, Version 2.6.1, alle Scripts nutzen python, kaputte Tool-Scripts entfernt, gueltiges JSON.</done>
</task>

<task type="auto">
  <name>Task 2: core/config.js — APP_CONFIG.VERSION auf 2.6.1</name>
  <files>core/config.js</files>
  <read_first>
    - core/config.js (Zeilen 1-30 — APP_CONFIG-Objekt, VERSION Zeile 8)
    - .planning/phases/01-stabilisierung/01-CONTEXT.md (D-06 — Patch-Bump, synchron mit package.json)
  </read_first>
  <action>
    Bearbeite core/config.js: Aendere in APP_CONFIG die Zeile "VERSION: '2.6.0'," zu "VERSION: '2.6.1',". Aendere KEINE anderen Felder (insbesondere DEBUG_MODE bleibt true — der Production-Build flippt es; Plan 04 sichert das ab). Behalte das exakte Format "VERSION: '2.6.1'," bei (Apostrophe, Komma), damit kein Format-bedingter Bruch entsteht.
    Konsequenz: Diese Version aktiviert die Migration 2.6.1 (Plan 03) beim naechsten Laden bestehender Kampagnen (compareVersions(altVersion, '2.6.1') < 0).
  </action>
  <verify>
    <automated>cd "D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular" && grep -c "VERSION: '2.6.1'" core/config.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "VERSION: '2.6.1'" core/config.js gibt 1 zurueck
    - grep -c "VERSION: '2.6.0'" core/config.js gibt 0 zurueck
    - grep -c "DEBUG_MODE: true" core/config.js gibt 1 zurueck (unveraendert; Production-Flip uebernimmt Plan 04)
  </acceptance_criteria>
  <done>APP_CONFIG.VERSION ist 2.6.1, synchron mit package.json; sonst unveraendert.</done>
</task>

<task type="auto">
  <name>Task 3: validate.py reparieren + tote Dateien loeschen</name>
  <files>validate.py, main.js, tsconfig.json.backup, MIGRATION_REPORT.md</files>
  <read_first>
    - validate.py (Zeilen 1-30 — SOURCE_DIR-Hardcode Zeile 11)
    - .planning/phases/01-stabilisierung/01-RESEARCH.md (Abschnitt validate.py-Reparatur — Path(__file__)-Pattern)
    - .planning/codebase/CONCERNS.md (Abschnitt "Abandoned TypeScript migration leftovers" — main.js/tsconfig.json.backup/MIGRATION_REPORT.md sind tot)
  </read_first>
  <action>
    (a) validate.py reparieren (D-12) — ersetze Zeile 11:
        ALT: SOURCE_DIR = '/mnt/user-data/outputs/dnd-tracker-modular'
        NEU: SOURCE_DIR = str(Path(__file__).parent)
        (Path ist bereits importiert, Zeile 9: from pathlib import Path.) Nimm sonst keine Aenderungen an validate.py vor.

    (b) Tote Dateien loeschen — entferne aus dem Repo-Root:
        - main.js (toter Entry-Point-Platzhalter, nicht von index.html referenziert)
        - tsconfig.json.backup (committetes Backup)
        - MIGRATION_REPORT.md (einmaliger Migrationsbericht; D-12/CONTEXT: loeschen)
        Pruefe vor dem Loeschen per grep, dass main.js nirgends aktiv geladen wird (kein script-src/import auf main.js in index.html, loader.js, build.py). tsconfig.json (OHNE .backup) bleibt erhalten.

    Hinweis: validate.py darf nach der Reparatur ueber "npm run validate" (python validate.py) lauffaehig sein. Die Validierung selbst muss keine 0 Fehler liefern (sie prueft Heuristiken), aber das Skript darf nicht mehr am Hardcode-Pfad scheitern.
  </action>
  <verify>
    <automated>cd "D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular" && grep -c "Path(__file__)" validate.py && python validate.py; echo "exit=$?"; test ! -f main.js && test ! -f tsconfig.json.backup && test ! -f MIGRATION_REPORT.md && echo "dead-files-gone"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "Path(__file__)" validate.py gibt mindestens 1 zurueck
    - grep -c "/mnt/user-data" validate.py gibt 0 zurueck (Hardcode entfernt)
    - python validate.py laeuft ohne FileNotFoundError/Pfad-Crash durch (Skript startet und beendet sich; Exit-Code muss nicht 0 sein, aber kein Pfad-Absturz)
    - test ! -f main.js erfolgreich (Datei geloescht)
    - test ! -f tsconfig.json.backup erfolgreich (Datei geloescht)
    - test ! -f MIGRATION_REPORT.md erfolgreich (Datei geloescht)
    - Ausgabe enthaelt "dead-files-gone"
  </acceptance_criteria>
  <done>validate.py nutzt skript-relative Pfade und startet; main.js, tsconfig.json.backup, MIGRATION_REPORT.md geloescht.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo-Metadaten / Build-Scripts | Reine Konfigurations- und Aufraeum-Aenderungen; keine Laufzeit-Datenpfade, kein externer Input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Repudiation | Lizenz-Mismatch (ISC vs MIT) schafft rechtliche Mehrdeutigkeit bei Weitergabe | mitigate | package.json auf MIT angleichen (Task 1), konsistent mit LICENSE/README |
| T-05-02 | Tampering | Ungueltiges JSON nach Edit bricht alle npm-Befehle | accept | node-Parsing-Check in verify (Task 1) faengt JSON-Fehler sofort; geringes Risiko bei sorgfaeltigem Edit |
</threat_model>

<verification>
- node -e Lizenz/Version/python3-Check Exit-Code 0
- grep -c "VERSION: '2.6.1'" core/config.js == 1
- python validate.py startet ohne Pfad-Crash
- tote Dateien geloescht (test ! -f)
- Manuell (laut VALIDATION.md): npm run build:dev auf Windows -> Exit-Code 0 (python-Aufruf funktioniert)
</verification>

<success_criteria>
- Lizenz MIT, Version 2.6.1 synchron, Windows-kompatible Scripts (STAB-09, STAB-06-Version)
- validate.py repariert, tote Dateien entfernt
</success_criteria>

<output>
Nach Abschluss: Erstelle .planning/phases/01-stabilisierung/01-05-SUMMARY.md
</output>
