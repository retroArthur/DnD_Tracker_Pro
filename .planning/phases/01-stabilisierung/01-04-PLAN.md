---
phase: 01-stabilisierung
plan: 04
type: execute
wave: 2
depends_on: ['01']
files_modified:
    - build.py
    - tests/build/test_build_deduplication.py
autonomous: true
requirements: [STAB-07]
nyquist_compliant: true

must_haves:
    truths:
        - 'Der Production-Build bricht hart ab, wenn DEBUG_MODE nach dem Flip noch true ist'
        - 'Der Build bricht hart ab, wenn zwei gebuendelte Quellmodule denselben Top-Level-Funktionsnamen definieren'
        - 'Der Build bricht hart ab, wenn die Modullisten in loader.js und build.py voneinander abweichen'
        - 'Pass-3-Duplikatentfernung hinterlaesst keine verwaisten Funktionskoerper mehr'
    artifacts:
        - path: 'build.py'
          provides: 'Pre-Build-Duplikat-Check, DEBUG_MODE-Assertion, Modullisten-Sync-Check, modulebenes modules-Konstante'
          contains: 'check_duplicate_functions'
        - path: 'tests/build/test_build_deduplication.py'
          provides: 'pytest-Tests fuer DEBUG_MODE-Assertion, Duplikat-Check, Modullisten-Sync'
          contains: 'test_module_lists_are_synchronized'
    key_links:
        - from: 'build.py build()'
          to: 'check_duplicate_functions(SOURCE_DIR, MODULES)'
          via: 'Aufruf vor dem JS-Kombinieren'
          pattern: "check_duplicate_functions\\("
        - from: 'build.py Production-Zweig'
          to: 'DEBUG_MODE-Assertion'
          via: "sys.exit(1) wenn 'DEBUG_MODE: true' nach replace noch vorhanden"
          pattern: 'DEBUG_MODE: true'
---

<objective>
Haertet das Build-System (STAB-07): verhindert die drei dokumentierten Fehlerklassen, die Production-Builds still kaputt machen koennen — verwaiste Funktionskoerper durch Pass-3-Duplikatentfernung, versehentlich aktiviertes DEBUG_MODE in Production, und auseinanderdriftende Modullisten zwischen loader.js und build.py.

Purpose: Builds sollen unbeaufsichtigt sicher sein. Die Three-Pass-Dedup ist laut CLAUDE.md die fragilste Stelle der Architektur; der historische 2026-01-10-Incident (orphaned function body -> Illegal return statement) darf nicht wiederkehren.
Output: Gehaertete build.py mit drei neuen Schutz-Checks, erweiterte pytest-Suite.

Claude's-Discretion-Entscheidungen (aus RESEARCH.md): Pre-Build-Duplikat-Check statt Pass-3-Body-Fix (loest das Problem an der Wurzel, deterministisch); Post-Replace-Assertion fuer DEBUG_MODE; Modullisten-Sync via Parsen des MODULES-Arrays aus loader.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stabilisierung/01-CONTEXT.md
@.planning/phases/01-stabilisierung/01-RESEARCH.md
@.planning/phases/01-stabilisierung/01-PATTERNS.md

<interfaces>
<!-- Reale Struktur aus build.py und der bestehenden pytest-Suite. -->

build.py:
SCRIPT_DIR = os.path.dirname(os.path.abspath(**file**)) // Zeile 35
SOURCE_DIR = SCRIPT_DIR // Zeile 36
def read_file(filepath) // Zeile 38
from tools.logging_util import log // log.info/log.success/log.warning/log.header (Zeile 32)
def remove_duplicate_functions(js_code) // Pass 3, BUG: Body wird nicht uebersprungen (Zeilen 176-228)
def build(minify=False, production=False, verbose=False) // Zeile 230
modules = [ ... 92 Eintraege ... ] // LOKALE Variable, Zeilen 249-355 — muss modul-level werden
JS kombinieren: for i, module in enumerate(modules,1) // Zeilen 400-409
Dedup-Aufruf: deduplicate_window_assignments(js_combined) // Zeile 414
DEBUG-Flip (Zeilen 419-423):
if production:
js_combined = js_combined.replace("DEBUG_MODE: true,", "DEBUG_MODE: false,", 1)
js_combined = js_combined.replace("DEBUG_VALIDATE_ON_SAVE: true,", "DEBUG_VALIDATE_ON_SAVE: false,", 1)

loader.js:
const MODULES = [ '...', '...' ]; // Zeilen 10-124 — Single-Source-of-Truth-Kandidat

tests/build/test_build_deduplication.py:
sys.path.insert(0, projektroot) // Zeile 17
from build import deduplicate_window_assignments, build // Zeile 19 — Importweg fuer neue Funktionen
class TestBuildDeduplication: // bestehende Tests; neue Methoden hier ergaenzen
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: modules zu modul-level MODULES heben + Pre-Build-Duplikat-Check + Modullisten-Sync</name>
  <files>build.py</files>
  <read_first>
    - build.py (Zeilen 1-60 Header/Helpers; Zeilen 230-355 build() + modules-Liste; Zeilen 395-423 JS-Kombinieren + DEBUG-Flip)
    - loader.js (Zeilen 1-130 — MODULES-Array-Struktur, Format der Eintraege)
    - .planning/phases/01-stabilisierung/01-PATTERNS.md (Abschnitt build.py — check_duplicate_functions, check_module_list_sync Snippets)
    - .planning/phases/01-stabilisierung/01-RESEARCH.md (Pitfall 4: testable-utils.js und tests/ aus Pruefung ausschliessen)
  </read_first>
  <action>
    (a) modules zu Modul-Level heben — Verschiebe die `modules = [...]`-Liste (aktuell lokal in build(), Zeilen 249-355) auf Modul-Ebene als Konstante `MODULES` (direkt nach SOURCE_DIR, ~Zeile 37). In build() ersetze die lokale Definition durch die Nutzung von `MODULES` (z.B. `modules = MODULES` am Anfang von build(), oder alle Vorkommen auf MODULES umstellen). Grund: Die neuen Check-Funktionen und der pytest-Test brauchen die Liste importierbar (`from build import MODULES`).

    (b) Pre-Build-Duplikat-Check hinzufuegen — fuege eine Modul-Level-Funktion hinzu:
    ```python
    def check_duplicate_functions(source_dir, modules):
        """Schlaegt fehl, wenn doppelte Top-Level-Funktionsnamen in gebuendelten Quelldateien existieren."""
        func_pattern = re.compile(r'^function\s+(\w+)\s*\(', re.MULTILINE)
        seen = {}
        for module in modules:
            path = os.path.join(source_dir, module)
            if not os.path.exists(path):
                continue
            content = read_file(path)
            for match in func_pattern.finditer(content):
                name = match.group(1)
                if name in seen:
                    print(f"[FEHLER] Doppelte Top-Level-Funktion '{name}': {seen[name]} und {module}")
                    sys.exit(1)
                seen[name] = module
    ```
    Wichtig (Pitfall 4): Die Funktion prueft NUR die `MODULES`-Liste — `utils/testable-utils.js` und das gesamte tests/-Verzeichnis sind nicht Teil von MODULES und damit korrekt ausgeschlossen (dort leben die 11 absichtlichen Kopien).

    (c) Modullisten-Sync-Check hinzufuegen — fuege hinzu:
    ```python
    def check_module_list_sync(loader_path, build_modules):
        """Vergleicht das MODULES-Array aus loader.js mit der build.py-Liste."""
        content = read_file(loader_path)
        match = re.search(r'const MODULES\s*=\s*\[(.*?)\];', content, re.DOTALL)
        if not match:
            log.warning("Konnte MODULES-Array nicht aus loader.js parsen — Sync-Pruefung uebersprungen")
            return
        loader_modules = re.findall(r"'([^']+)'", match.group(1))
        build_set, loader_set = set(build_modules), set(loader_modules)
        only_in_build = build_set - loader_set
        only_in_loader = loader_set - build_set
        if only_in_build or only_in_loader:
            print("[FEHLER] Modullisten-Abweichung zwischen loader.js und build.py!")
            for m in sorted(only_in_build): print(f"  Nur in build.py: {m}")
            for m in sorted(only_in_loader): print(f"  Nur in loader.js: {m}")
            sys.exit(1)
        log.success(f"Modullisten synchron: {len(build_set)} Module")
    ```

    (d) Beide Checks in build() aufrufen — VOR dem JS-Kombinieren (vor Zeile 396 "Lade JavaScript-Module"):
    ```python
    loader_js_path = os.path.join(SCRIPT_DIR, 'loader.js')
    check_module_list_sync(loader_js_path, MODULES)
    check_duplicate_functions(SOURCE_DIR, MODULES)
    ```
    Hinweis: `import sys` ist bereits am Dateianfang vorhanden (Zeile 27). Belasse die bestehende remove_duplicate_functions-Pass-3 vorerst unveraendert — sie wird durch den Pre-Build-Check ueberfluessig, aber ihr Entfernen ist nicht Teil dieser Task (siehe Task 3). Der Pre-Build-Check verhindert, dass Duplikate ueberhaupt bis Pass 3 gelangen.

  </action>
  <verify>
    <automated>cd "D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular" && python build.py --production</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "^MODULES = \[" build.py gibt 1 zurueck (Liste auf Modul-Ebene)
    - grep -c "def check_duplicate_functions" build.py gibt 1 zurueck
    - grep -c "def check_module_list_sync" build.py gibt 1 zurueck
    - grep -c "check_duplicate_functions(SOURCE_DIR" build.py gibt mindestens 1 zurueck (Aufruf in build())
    - grep -c "check_module_list_sync(" build.py gibt mindestens 2 zurueck (Definition + Aufruf)
    - python build.py --production endet mit Exit-Code 0 (aktuelle Listen sind synchron, keine Duplikate) und erzeugt dist/dnd-tracker-optimized.html
  </acceptance_criteria>
  <done>MODULES ist modul-level; Pre-Build-Duplikat-Check und Modullisten-Sync laufen vor dem Bundle und sind beim aktuellen sauberen Stand gruen.</done>
</task>

<task type="auto">
  <name>Task 2: DEBUG_MODE-Post-Replace-Assertion im Production-Build</name>
  <files>build.py</files>
  <read_first>
    - build.py (Zeilen 418-432 — DEBUG-Flip-Block und Folgecode)
    - .planning/phases/01-stabilisierung/01-PATTERNS.md (Abschnitt build.py — DEBUG_MODE-Assertion-Snippet)
    - core/config.js (Zeilen 6-13 — exaktes Format "DEBUG_MODE: true,")
  </read_first>
  <action>
    Bearbeite build.py im Production-Zweig (Zeilen 419-423). Direkt NACH den beiden replace()-Aufrufen die Assertion einbauen:
    ```python
    if production:
        js_combined = js_combined.replace("DEBUG_MODE: true,", "DEBUG_MODE: false,", 1)
        js_combined = js_combined.replace("DEBUG_VALIDATE_ON_SAVE: true,", "DEBUG_VALIDATE_ON_SAVE: false,", 1)
        # STAB-07: Abbruch, falls der Flip fehlschlug (z.B. nach Prettier-Reformatierung von core/config.js)
        if "DEBUG_MODE: true" in js_combined:
            print("[ABORTED] DEBUG_MODE ist noch true im Production-Build! core/config.js Formatierung pruefen.")
            sys.exit(1)
        log.success("DEBUG_MODE deaktiviert und verifiziert.")
    ```
    Begruendung: Der Flip haengt am exakten String "DEBUG_MODE: true," (mit Leerzeichen und Komma). Aendert sich die Formatierung in core/config.js, schlaegt der replace still fehl und Production wuerde mit DEBUG_MODE: true ausgeliefert. Die Assertion macht das zu einem harten Build-Fehler.
  </action>
  <verify>
    <automated>cd "D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular" && python build.py --production && node -e "var s=require('fs').readFileSync('dist/dnd-tracker-optimized.html','utf8'); var bad = s.indexOf('DEBUG_MODE: true')>=0; process.exit(bad?1:0)"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "ABORTED" build.py gibt mindestens 1 zurueck (Assertion-Meldung)
    - grep -c "DEBUG_MODE deaktiviert und verifiziert" build.py gibt 1 zurueck
    - python build.py --production endet mit Exit-Code 0
    - dist/dnd-tracker-optimized.html enthaelt NICHT "DEBUG_MODE: true" (node-Check Exit-Code 0)
    - dist/dnd-tracker-optimized.html enthaelt "DEBUG_MODE: false" (grep -c "DEBUG_MODE: false" dist/dnd-tracker-optimized.html gibt mindestens 1)
  </acceptance_criteria>
  <done>Production-Build verifiziert nach dem Flip, dass DEBUG_MODE: true nicht mehr im Bundle steht, und bricht sonst ab.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: pytest-Suite erweitern (DEBUG_MODE, Duplikat-Check, Modullisten-Sync)</name>
  <files>tests/build/test_build_deduplication.py</files>
  <read_first>
    - tests/build/test_build_deduplication.py (Komplett — Importweg Zeile 19, Klassenstruktur, dist-Pfad-Pattern)
    - build.py (neue Funktionen aus Task 1: check_duplicate_functions, check_module_list_sync, MODULES)
    - .planning/phases/01-stabilisierung/01-PATTERNS.md (Abschnitt test_build_deduplication.py — Test-Patterns DEBUG_MODE/Sync)
  </read_first>
  <behavior>
    Neue pytest-Methoden in TestBuildDeduplication:
    - test_production_build_has_debug_mode_false: dist/dnd-tracker-optimized.html enthaelt NICHT "DEBUG_MODE: true" und enthaelt "DEBUG_MODE: false" (skip, falls Datei fehlt).
    - test_module_lists_are_synchronized: check_module_list_sync(loader.js, MODULES) loest KEINEN SystemExit aus (aktuelle Listen sind synchron).
    - test_duplicate_function_check_detects_duplicate: check_duplicate_functions gegen ein temporaeres Modul-Set mit einem kuenstlich doppelten Funktionsnamen loest SystemExit aus (per pytest.raises(SystemExit)).
    - test_no_orphaned_return_statements: Im dist-Bundle folgt auf keine "[DEDUP] Removed duplicate function"-Zeile innerhalb der naechsten Zeilen ein nackter Top-Level-"return"-Ausdruck (deckt den 2026-01-10-Incident-Typ ab).
  </behavior>
  <action>
    Erweitere tests/build/test_build_deduplication.py. Importiere die neuen Funktionen: aendere die Importzeile zu
    `from build import deduplicate_window_assignments, build, check_duplicate_functions, check_module_list_sync, MODULES`.
    Fuege die vier unter behavior beschriebenen Test-Methoden hinzu. Konkrete Hinweise:
    - DEBUG_MODE-Test: dist-Pfad wie bestehende Tests: `Path(__file__).parent.parent.parent / 'dist' / 'dnd-tracker-optimized.html'`; pytest.skip wenn nicht vorhanden.
    - Sync-Test: `loader_path = Path(__file__).parent.parent.parent / 'loader.js'`; in try/except SystemExit -> bei Exception pytest.fail.
    - Duplikat-Test: Lege eine temporaere Quelldatei (tmp_path-Fixture) mit zwei `function foo()`-Definitionen an, baue eine kuenstliche modules-Liste mit demselben Eintrag zweimal ODER zwei tmp-Dateien, die beide `function foo()` enthalten, und erwarte `with pytest.raises(SystemExit): check_duplicate_functions(str(tmp_path), [...])`.
    - Orphaned-Return-Test: dist-Bundle lesen; alle Zeilenindizes mit "[DEDUP] Removed duplicate function" sammeln; fuer jeden pruefen, dass in den folgenden ~20 Zeilen kein nicht-eingerueckter `return`-Ausdruck auf Top-Level steht (heuristisch: Zeile beginnt mit "return" ohne fuehrende Funktions-Klammerebene). Da der Pre-Build-Check (Task 1) Duplikate verhindert, sollte es im aktuellen Build gar keine DEDUP-Funktion-Kommentare geben -> Test ist trivially gruen, dokumentiert aber die Invariante.
  </action>
  <verify>
    <automated>cd "D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular" && python -m pytest tests/build/ -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "test_production_build_has_debug_mode_false" tests/build/test_build_deduplication.py gibt 1 zurueck
    - grep -c "test_module_lists_are_synchronized" tests/build/test_build_deduplication.py gibt 1 zurueck
    - grep -c "test_duplicate_function_check_detects_duplicate" tests/build/test_build_deduplication.py gibt 1 zurueck
    - grep -c "test_no_orphaned_return_statements" tests/build/test_build_deduplication.py gibt 1 zurueck
    - grep -c "check_module_list_sync\|check_duplicate_functions\|MODULES" tests/build/test_build_deduplication.py gibt mindestens 1 zurueck (neue Imports genutzt)
    - python -m pytest tests/build/ -v endet mit Exit-Code 0 (alle Tests gruen, inkl. der bestehenden)
  </acceptance_criteria>
  <done>pytest-Suite deckt DEBUG_MODE-Assertion, Duplikat-Erkennung, Modullisten-Sync und die Orphaned-Body-Invariante ab; alle Build-Tests gruen.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                        | Description                                                                                                                                                        |
| ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Quellmodule -> gebuendeltes Production-Artefakt | Der Build kombiniert 92 Module in einen globalen Scope; fehlerhafte Dedup oder aktiviertes DEBUG_MODE veraendert das ausgelieferte Verhalten. Kein externer Input. |

## STRIDE Threat Register

| Threat ID | Category          | Component                                                                                           | Disposition | Mitigation Plan                                                                                                 |
| --------- | ----------------- | --------------------------------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------- |
| T-04-01   | Tampering         | DEBUG_MODE versehentlich true in Production (Debug-Validierung + Konsolen-Ausgaben aktiv)           | mitigate    | Post-Replace-Assertion (Task 2) bricht den Build ab; pytest-Test (Task 3) sichert Regression ab                 |
| T-04-02   | Denial of Service | Verwaiste Funktionskoerper -> Runtime SyntaxError (Illegal return) bricht App-Boot                  | mitigate    | Pre-Build-Duplikat-Check (Task 1) verhindert Duplikate vor Pass 3; Orphaned-Return-Invariante getestet (Task 3) |
| T-04-03   | Tampering         | Auseinanderdriftende Modullisten -> Production-Bundle fehlt ein Modul oder enthaelt anderes als dev | mitigate    | check_module_list_sync (Task 1) bricht bei Abweichung ab                                                        |

</threat_model>

<verification>
- python build.py --production Exit-Code 0
- python -m pytest tests/build/ -v Exit-Code 0
- dist/dnd-tracker-optimized.html enthaelt nicht "DEBUG_MODE: true"
- Manuell (laut VALIDATION.md): npm run build:dev auf Windows ausfuehren, Exit-Code 0
</verification>

<success_criteria>

- Build bricht ab bei DEBUG_MODE=true, doppelter Funktion oder Modullisten-Abweichung (STAB-07)
- Keine verwaisten Funktionskoerper mehr moeglich (Pre-Build-Check)
- pytest-Suite deckt alle drei Schutz-Checks ab
  </success_criteria>

<output>
Nach Abschluss: Erstelle .planning/phases/01-stabilisierung/01-04-SUMMARY.md
</output>
</content>
