---
phase: 01-stabilisierung
plan: 03
type: execute
wave: 2
depends_on: ['01']
files_modified:
    - systems/spellslots/version-migration.js
    - systems/spellslots/import-export.js
    - systems/campaign-manager/campaign-manager.js
    - types/globals.d.ts
    - types/entities.d.ts
    - tests/setup.js
    - tests/unit/migration.test.js
    - assets/styles-purged.css
autonomous: true
requirements: [STAB-02]
nyquist_compliant: true

must_haves:
    truths:
        - 'Neu angelegte Kampagnen tragen keinen mindmap-Schluessel mehr'
        - 'Die Migration 2.6.1 entfernt leere mindmap-Keys aus bestehenden Speicherstaenden still'
        - 'Beim Import einer Kampagne mit echten Mindmap-Inhalten erscheint ein Hinweis-Dialog mit JSON-Export-Angebot bevor der Key entfernt wird'
        - 'Keine toten Mindmap-Typdeklarationen oder Test-Reste verbleiben'
        - 'assets/styles-purged.css ist geloescht'
    artifacts:
        - path: 'systems/spellslots/version-migration.js'
          provides: 'Migration 2.6.1 mit Smart-Strip leerer mindmap-Keys'
          contains: '2.6.1'
        - path: 'systems/spellslots/import-export.js'
          provides: 'Import-Pfad mit Mindmap-Inhalts-Check + Export-Dialog (D-09/D-10)'
          contains: 'showMindmapExportDialog'
        - path: 'tests/unit/migration.test.js'
          provides: 'Unit-Tests fuer Smart-Strip-Migration (leer entfernen, echte Inhalte erhalten)'
          min_lines: 25
    key_links:
        - from: 'version-migration.js MIGRATIONS 2.6.1'
          to: 'data.mindmap'
          via: 'delete data.mindmap bei leerem Seed'
          pattern: "delete data\\.mindmap"
        - from: 'import-export.js Import-Pfad'
          to: 'Mindmap-Backup-Download (D-10)'
          via: 'Blob-Download mindmap-backup Datei bei echten Inhalten'
          pattern: 'mindmap-backup'
---

<objective>
Entfernt alle verbleibenden Reste des bereits entfernten Mindmap-Features (STAB-02): den leeren mindmap-Seed in neuen Kampagnen, tote Typdeklarationen, das verwaiste generierte CSS und die Test-Reste. Implementiert die Smart-Strip-Migration, die bestehende Speicherstaende beim Laden selbst heilt — mit Datensicherung bei echten Mindmap-Inhalten.

Purpose: "Kein stiller Verlust von Nutzer-Inhalten" (CONTEXT.md). Leere Seeds werden still entfernt (Normalfall); echte Inhalte loesen einen Hinweis-Dialog mit Export aus.
Output: Migration 2.6.1 (version-migration.js), Import-Dialog (import-export.js), entfernte Seeds und Typen, geloeschtes styles-purged.css, neue migration.test.js.

Umgesetzte Entscheidungen: D-09 (Smart-Strip per Migration + Hinweis-Dialog bei echten Inhalten), D-10 (eigene JSON-Backup-Datei), D-13 (styles-purged.css loeschen). Teil von D-14 (Typ-/Test-Reste) — die CLAUDE.md/README-Mindmap-Abschnitte gehoeren zu Plan 07.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stabilisierung/01-CONTEXT.md
@.planning/phases/01-stabilisierung/01-RESEARCH.md
@.planning/phases/01-stabilisierung/01-PATTERNS.md

<interfaces>
<!-- Reale Stellen aus dem Quellcode. Executor bearbeitet exakt diese. -->

version-migration.js:
const MIGRATIONS = { '2.3.0': fn, '2.4.0': fn }; // Zeilen 9-52 — KEIN 2.5.0/2.6.x vorhanden
function migrateData(data) // iteriert Object.keys(MIGRATIONS).sort() fuer Versionen kleiner CURRENT (Zeilen 53-72)
function compareVersions(v1, v2)
CURRENT_VERSION = window.APP_CONFIG.VERSION (Zeile 8)

campaign-manager.js (Zeile 35 — leerer Seed in neuer Kampagne):
filters: [], mindmap: { nodes: [], connections: [] },

import-export.js — importDataGlobal() (Zeilen 412-520):
Meta-Loeschung (Zeilen 433-435): delete imp.\_campaignName; delete imp.\_exportDate; delete imp.\_version;
Zweiter mindmap-Seed (Zeile 486, neue-Kampagne-Zweig): filters: [], mindmap: { nodes: [], connections: [] },
Download-Pattern (exportData, ~182-189): Blob + URL.createObjectURL + a.click() + revokeObjectURL
onload aktuell SYNCHRON: reader.onload = (e) => { ... } // muss async werden fuer await

types/globals.d.ts, types/entities.d.ts: enthalten mindmap-Typdeklarationen (per grep finden)

tests/setup.js (Zeilen 49 und 395): global.D / resetTestState() seeden mindmap: { nodes: [], connections: [] }
tests/unit/stability.test.js (Zeile 25): seedet ebenfalls mindmap — DIESE Datei NICHT anfassen (gehoert Plan 02); nur tests/setup.js anpassen

utils/utilities.js: showToast(msg, type, duration); esc(text)
Modal-Pattern: import-export.js Zeilen 303-323 (import-modal, infoEl.innerHTML mit esc())
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Smart-Strip-Migration 2.6.1 + Seeds entfernen + Migrations-Tests</name>
  <files>systems/spellslots/version-migration.js, systems/campaign-manager/campaign-manager.js, systems/spellslots/import-export.js, tests/unit/migration.test.js</files>
  <read_first>
    - systems/spellslots/version-migration.js (Zeilen 9-72 — MIGRATIONS-Objekt, migrateData)
    - systems/campaign-manager/campaign-manager.js (Zeilen 30-40 — Seed-Template)
    - systems/spellslots/import-export.js (Zeilen 480-495 — zweiter Seed im neue-Kampagne-Zweig)
    - .planning/phases/01-stabilisierung/01-PATTERNS.md (Abschnitt version-migration.js — 2.6.1-Snippet; Abschnitt stability.test.js — Migrations-Test-Pattern)
  </read_first>
  <behavior>
    Tests in NEUER Datei tests/unit/migration.test.js (RED bis die Migration existiert, dann GREEN):
    - "removes empty mindmap key": migrateData mit data.mindmap = {nodes:[],connections:[]} und _version 2.5.0 ergibt ein Objekt OHNE mindmap.
    - "preserves real mindmap content": migrateData mit data.mindmap.nodes = [{id:1}] behaelt mindmap (Migration strippt echte Inhalte NICHT; das macht der Import-Dialog in Task 2).
    - "new campaign template has no mindmap": Das Seed-Template aus campaign-manager.js traegt keinen mindmap-Key mehr.
  </behavior>
  <action>
    (a) Migration 2.6.1 hinzufuegen — ergaenze in systems/spellslots/version-migration.js im MIGRATIONS-Objekt (nach '2.4.0', vor der schliessenden Klammer) den Eintrag:
    ```javascript
    '2.6.1': (data) => {
        // STAB-02 / D-09: Smart-Strip — leere mindmap-Seeds still entfernen
        if (data.mindmap) {
            const hasRealContent = (
                (data.mindmap.nodes && data.mindmap.nodes.length > 0) ||
                (data.mindmap.connections && data.mindmap.connections.length > 0)
            );
            if (!hasRealContent) {
                delete data.mindmap; // leerer Seed -> still entfernen
            }
            // Echte Inhalte bleiben hier unangetastet — der Import-Pfad (Task 2)
            // loest VOR migrateData() den Hinweis-Dialog + Export aus.
        }
        return data;
    },
    ```
    Hinweis: migrateData iteriert Object.keys(MIGRATIONS).sort() — '2.6.1' sortiert nach '2.4.0', laeuft fuer jeden Stand mit _version kleiner 2.6.1. CURRENT_VERSION wird durch Plan 05 auf 2.6.1 gesetzt; die Migration greift unabhaengig davon fuer aeltere Staende.

    (b) Seed entfernen (campaign-manager.js) — entferne in systems/campaign-manager/campaign-manager.js:35 den Teil-Ausdruck "mindmap: { nodes: [], connections: [] }," aus dem Seed-Template. Lasse umgebende Felder (filters, calendar) intakt.

    (c) Seed entfernen (import-export.js) — entferne in systems/spellslots/import-export.js (neue-Kampagne-Zweig, ~Zeile 486) ebenfalls "mindmap: { nodes: [], connections: [] }," aus dem newData-Objekt.

    (d) Migrations-Tests anlegen — erstelle tests/unit/migration.test.js. Da migrateData/compareVersions global/non-ESM sind, lade sie analog zum bestehenden Test-Harness (siehe wie stability.test.js die Funktionen erreicht; setup.js stellt APP_CONFIG bereit). Schreibe die drei unter behavior beschriebenen Tests. Fuer "new campaign template has no mindmap" genuegt ein Quell-grep-Assert (fs.readFileSync der campaign-manager.js enthaelt KEIN "mindmap:"), falls die Funktion nicht isoliert lauffaehig ist.
    CLAUDE.md Feedback-Loop: kurze Testbeschreibung vor jedem Test ausgeben.

  </action>
  <verify>
    <automated>cd "D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular" && npx jest tests/unit/migration.test.js --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "2.6.1" systems/spellslots/version-migration.js gibt mindestens 1 zurueck (Migrations-Eintrag)
    - grep -c "delete data.mindmap" systems/spellslots/version-migration.js gibt 1 zurueck
    - grep -c "mindmap:" systems/campaign-manager/campaign-manager.js gibt 0 zurueck (Seed entfernt)
    - grep -c "mindmap: { nodes" systems/spellslots/import-export.js gibt 0 zurueck (zweiter Seed entfernt)
    - Datei tests/unit/migration.test.js existiert; npx jest tests/unit/migration.test.js --no-coverage laeuft GRUEN (Exit-Code 0)
  </acceptance_criteria>
  <done>Migration 2.6.1 strippt leere mindmap-Keys; beide Seed-Stellen entfernt; neue Kampagnen sind mindmap-frei; Migrations-Tests gruen.</done>
</task>

<task type="auto">
  <name>Task 2: Import-Pfad — Hinweis-Dialog + JSON-Export bei echten Mindmap-Inhalten (D-09/D-10)</name>
  <files>systems/spellslots/import-export.js</files>
  <read_first>
    - systems/spellslots/import-export.js (Zeilen 412-520 importDataGlobal; Zeilen 182-189 Download-Pattern; Zeilen 303-323 Modal-Pattern mit esc())
    - .planning/phases/01-stabilisierung/01-PATTERNS.md (Abschnitt import-export.js — D-09 Hook-Point, D-10 JSON-Download, XSS-Schutz)
    - .planning/phases/01-stabilisierung/01-RESEARCH.md (Smart-Strip-Migration Pattern — Dialog VOR migrateData)
  </read_first>
  <action>
    Bearbeite systems/spellslots/import-export.js, Funktion importDataGlobal(). Mache zuerst den onload-Handler async: aendere "reader.onload = (e) => {" zu "reader.onload = async (e) => {", damit await zulaessig ist.

    VOR der Meta-Feld-Loeschung (delete imp._campaignName; delete imp._exportDate; delete imp._version; ~Zeile 433) den Mindmap-Inhalts-Check einbauen:
    ```javascript
    // STAB-02 / D-09: Mindmap-Inhalt pruefen, bevor er beim Import verworfen wird
    if (imp.mindmap) {
        const hasRealContent = (imp.mindmap.nodes?.length > 0 || imp.mindmap.connections?.length > 0);
        if (hasRealContent) {
            await showMindmapExportDialog(imp.mindmap, campaignName); // bietet JSON-Backup an (D-10)
        }
        delete imp.mindmap; // danach immer entfernen (Feature ist abgeschafft)
    }
    ```

    Fuege die neue modul-interne Funktion showMindmapExportDialog(mindmap, campaignName) hinzu, die einen Hinweis-Dialog zeigt, einen Button "Mindmap als JSON sichern" anbietet (laedt nodes/connections als eigene Datei mindmap-backup-{name}.json herunter, D-10) und ein Promise aufloest, wenn der Nutzer "fortfahren" waehlt:
    ```javascript
    function showMindmapExportDialog(mindmap, campaignName) {
        return new Promise((resolve) => {
            const safeName = (campaignName || 'kampagne')
                .replace(/[^a-zA-Z0-9aeoeueAEOEUEss\s-]/g, '').replace(/\s+/g, '-');
            const nodeCount = String(mindmap.nodes?.length || 0);
            const connCount = String(mindmap.connections?.length || 0);
            const overlay = document.createElement('div');
            overlay.className = 'modal show';
            overlay.innerHTML =
                '<div class="modal-content" style="max-width:480px">' +
                '<h3>Mindmap-Daten gefunden</h3>' +
                '<p>Diese Kampagne enthaelt Mindmap-Inhalte (' + esc(nodeCount) + ' Knoten, ' +
                esc(connCount) + ' Verbindungen). Das Mindmap-Feature wurde entfernt — die Daten werden ' +
                'beim Import verworfen. Du kannst sie vorher als Datei sichern.</p>' +
                '<div style="display:grid;gap:8px;margin:12px 0">' +
                '<button data-act="save" class="btn btn-primary">Mindmap als JSON sichern</button>' +
                '<button data-act="skip" class="btn">Fortfahren</button>' +
                '</div></div>';
            overlay.addEventListener('click', (ev) => {
                const act = ev.target?.dataset?.act;
                if (act === 'save') {
                    const json = JSON.stringify({ nodes: mindmap.nodes || [], connections: mindmap.connections || [] }, null, 2);
                    const blob = new Blob([json], { type: 'application/json' });
                    const url = URL.createObjectURL(blob);
                    const a = document.createElement('a');
                    a.href = url;
                    a.download = 'mindmap-backup-' + safeName + '.json';
                    a.click();
                    URL.revokeObjectURL(url);
                    showToast('Mindmap-Backup heruntergeladen', 'success');
                    return; // Dialog offen lassen, bis Nutzer "Fortfahren" waehlt
                }
                if (act === 'skip') {
                    document.body.removeChild(overlay);
                    resolve(true);
                }
            });
            document.body.appendChild(overlay);
        });
    }
    ```
    Passe die Modal-Klassen an die echten App-Klassen an (siehe bestehendes import-modal in dieser Datei). Pflicht: esc() fuer alle eingebetteten Strings (auch numerische Zaehler konsequent durch esc()). Deutsche UI-Texte (Umlaute korrekt im echten Code verwenden — im Plan ASCII-ersetzt). Funktion modul-intern halten (kein window-Export — CLAUDE.md Export-Audit).
    CLAUDE.md-Regeln: keine const X = window.X in Funktionen; keine doppelten Top-Level-Funktionsnamen (pruefe, dass showMindmapExportDialog nicht bereits existiert).

  </action>
  <verify>
    <automated>cd "D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular" && npx jest tests/unit/migration.test.js --no-coverage; node -e "var s=require('fs').readFileSync('systems/spellslots/import-export.js','utf8'); var ok = s.indexOf('showMindmapExportDialog')>=0 && s.indexOf('mindmap-backup')>=0; process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "showMindmapExportDialog" systems/spellslots/import-export.js gibt mindestens 2 zurueck (Definition + Aufruf)
    - grep -c "mindmap-backup" systems/spellslots/import-export.js gibt mindestens 1 zurueck (D-10 Download-Dateiname)
    - grep -c "reader.onload = async" systems/spellslots/import-export.js gibt 1 zurueck (await ermoeglicht)
    - grep -c "esc(" systems/spellslots/import-export.js gibt mindestens 1 zurueck (XSS-Schutz; war auch vorher vorhanden — Dialog nutzt esc konsequent)
    - npx jest tests/unit/migration.test.js --no-coverage bleibt GRUEN (keine Regression durch async-Umbau)
  </acceptance_criteria>
  <done>Import erkennt echte Mindmap-Inhalte, bietet JSON-Backup an und entfernt den Key danach; onload ist async; alle Nutzer-Strings escaped.</done>
</task>

<task type="auto">
  <name>Task 3: Tote Reste entfernen — Typdeklarationen, styles-purged.css, Test-Seeds</name>
  <files>types/globals.d.ts, types/entities.d.ts, assets/styles-purged.css, tests/setup.js</files>
  <read_first>
    - types/globals.d.ts (mindmap-Typdeklarationen per grep lokalisieren)
    - types/entities.d.ts (mindmap-Typdeklarationen per grep lokalisieren)
    - tests/setup.js (Zeilen 35-60 global.D-Seed; Zeilen 380-406 resetTestState-Seed)
    - .planning/codebase/CONCERNS.md (Abschnitt "Removed Mindmap/Network feature residue" — vollstaendige Restliste)
  </read_first>
  <action>
    (a) Typdeklarationen bereinigen — entferne in types/globals.d.ts und types/entities.d.ts alle mindmap-bezogenen Typdeklarationen (Interfaces/Properties wie MindmapNode, MindmapConnection, mindmap-Property im D-Typ). Lasse alle anderen Deklarationen unveraendert, damit npm run typecheck (Plan 06) weiterhin gruen bleibt. Falls eine mindmap-Property in einem groesseren Interface steht, nur diese Zeile entfernen.

    (b) styles-purged.css loeschen (D-13) — loesche die Datei assets/styles-purged.css vollstaendig. Sie wird von nichts konsumiert (kein Verweis in build.py, loader.js, index.html; nur das bereits geloeschte purge-css.py erzeugte sie). Pruefe vor dem Loeschen per grep, dass kein aktiver Code sie referenziert.

    (c) Test-Seeds bereinigen (tests/setup.js) — entferne die zwei "mindmap: { nodes: [], connections: [] }," Zeilen aus dem global.D-Objekt (~Zeile 49) und aus resetTestState() (~Zeile 395), damit die Test-Grundstruktur den neuen mindmap-freien Stand widerspiegelt. tests/unit/stability.test.js NICHT anfassen (gehoert Plan 02; dessen beforeEach-Seed wird dort von Plan 02 mitgepflegt).

    Nach allen Aenderungen: npm run typecheck darf nicht durch entfernte Typen brechen (verify unten). Sollte eine entfernte mindmap-Property anderswo referenziert werden, diese Referenz ebenfalls entfernen (sie waere toter Code).

  </action>
  <verify>
    <automated>cd "D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular" && npm run typecheck && npx jest tests/unit/migration.test.js --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - grep -ric "mindmap" types/globals.d.ts gibt 0 zurueck
    - grep -ric "mindmap" types/entities.d.ts gibt 0 zurueck
    - Datei assets/styles-purged.css existiert NICHT mehr (test -f assets/styles-purged.css schlaegt fehl)
    - grep -c "mindmap" tests/setup.js gibt 0 zurueck
    - npm run typecheck endet mit Exit-Code 0 (keine Typfehler durch entfernte Deklarationen)
  </acceptance_criteria>
  <done>Alle Mindmap-Typdeklarationen und Test-Seeds entfernt; styles-purged.css geloescht; typecheck gruen.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                   | Description                                                                                             |
| ------------------------------------------ | ------------------------------------------------------------------------------------------------------- |
| Importierte Kampagnen-JSON -> App-Laufzeit | Importierte Daten (inkl. mindmap-Feld) werden geparst; der neue Dialog rendert Zaehler/Name in den DOM. |
| Mindmap-Backup-Download (D-10)             | Nutzer-eigene Daten werden als JSON-Datei serialisiert und heruntergeladen.                             |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                                      | Disposition | Mitigation Plan                                                                                                                                         |
| --------- | ---------------------- | ---------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-03-01   | Tampering/XSS          | showMindmapExportDialog rendert Knoten-/Verbindungszahlen und ggf. Kampagnenname via innerHTML | mitigate    | Alle eingebetteten Strings mit esc() escapen (acceptance_criteria erzwingt esc(); Zaehler werden ueber esc(String(...)) ausgegeben)                     |
| T-03-02   | Tampering              | Mindmap-Backup-Download (D-10) serialisiert importierte mindmap-Daten                          | accept      | Daten sind Nutzer-eigene Inhalte; JSON.stringify ohne HTML-Kontext; Download-Datei wird nicht von der App wieder ausgefuehrt (RESEARCH Security Domain) |
| T-03-03   | Information Disclosure | Stilles Verwerfen echter Mindmap-Inhalte beim Import                                           | mitigate    | D-09-Dialog erzwingt Sicht + Export-Angebot VOR dem Loeschen (kein stiller Verlust)                                                                     |

</threat_model>

<verification>
- npx jest tests/unit/migration.test.js --no-coverage gruen
- npm run typecheck Exit-Code 0
- grep -c "mindmap:" systems/campaign-manager/campaign-manager.js == 0
- test -f assets/styles-purged.css schlaegt fehl (Datei geloescht)
- Manuell (optional): Kampagne mit echten Mindmap-Knoten importieren -> Dialog erscheint, JSON-Backup ladbar
</verification>

<success_criteria>

- Neue Kampagnen mindmap-frei; Migration heilt Altstaende; echte Inhalte gesichert vor Strip (STAB-02)
- Keine toten Mindmap-Typen/CSS/Test-Reste mehr
  </success_criteria>

<output>
Nach Abschluss: Erstelle .planning/phases/01-stabilisierung/01-03-SUMMARY.md
</output>
</content>
</invoke>
