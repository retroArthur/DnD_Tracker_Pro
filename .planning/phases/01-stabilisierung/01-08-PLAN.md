---
phase: 01-stabilisierung
plan: 08
type: execute
wave: 4
depends_on: ['02']
files_modified:
  - systems/spellslots/quick-roll.js
  - tests/unit/storage-conflict.test.js
autonomous: true
requirements: [STAB-05]
gap_closure: true
nyquist_compliant: true

must_haves:
  truths:
    - "Bei einem echten LS/IDB-Konflikt (LS-Schatten ohne _ts + abweichender IDB-Stand) wird KEIN RangeError (Endlosrekursion) geworfen — der Konfliktpfad läuft sauber durch"
    - "Ohne externen UI-Hook gewinnt im Konfliktfall der neuere IDB-Stand (kein stiller Sieg veralteter LS-Daten)"
    - "Bei identischem Inhalt (lsData === idbData) wird der LS-Stand still genutzt, ohne Dialog und ohne Konfliktverarbeitung"
    - "Die Konflikt-Auflösung wird durch echte Tests abgedeckt, die die PRODUKTIONS-Funktion via vm-Kontext laden und aufrufen (nicht nur Inline-Simulationen)"
    - "Der ungenutzte window-Export showStorageConflictDialogInternal ist entfernt (Export-Audit-Regel)"
  artifacts:
    - path: "systems/spellslots/quick-roll.js"
      provides: "Konflikt-Auflösung ohne Selbstrekursion; delegiert nur an einen ANDERS benannten optionalen UI-Hook, sonst IDB-Fallback"
      contains: "resolveStorageConflict"
    - path: "tests/unit/storage-conflict.test.js"
      provides: "vm-basierte Regressionstests gegen die echte Konflikt-Funktion + Load-Pfad (kein RangeError, IDB-Fallback, Identisch-Fall)"
      contains: "vm.runInContext"
  key_links:
    - from: "systems/spellslots/quick-roll.js load()"
      to: "Konflikt-Auflösung"
      via: "resolveStorageConflict(lsData, idbData, onUseLS, onUseIDB) — keine Selbstreferenz"
      pattern: "resolveStorageConflict"
    - from: "systems/spellslots/quick-roll.js resolveStorageConflict()"
      to: "optionaler externer UI-Dialog"
      via: "window.showStorageConflictDialogUI (anders benannt als die interne Funktion)"
      pattern: "showStorageConflictDialogUI"
    - from: "tests/unit/storage-conflict.test.js"
      to: "systems/spellslots/quick-roll.js (echter Quelltext)"
      via: "fs.readFileSync + vm.runInContext (Muster aus migration.test.js)"
      pattern: "runInContext"
---

<objective>
Schließt Gap 1 (CR-01 aus 01-REVIEW.md / 01-VERIFICATION.md): Die Funktion `showStorageConflictDialog` in `systems/spellslots/quick-roll.js` (Zeile 32-33) delegiert an `window.showStorageConflictDialog` — das ist in einem klassischen Script die Funktion SELBST. Bei jedem echten LS/IDB-Konflikt (`lsData !== idbData`) entsteht dadurch `RangeError: Maximum call stack size exceeded`, der in `load()` verschluckt wird → veraltete localStorage-Daten gewinnen still. Das ist exakt das STAB-05-Datenverlust-Szenario, das die Phase beheben sollte. Es bleibt im Konfliktpfad offen.

Purpose: "Kein stiller Datenverlust, nirgends" (01-CONTEXT.md Leitlinie). Der Konflikt-Dialog ist im Stale-Shadow-Fall die EINZIGE Schutzschicht — solange er per Rekursion abstürzt, ist STAB-05/SC2 nicht erfüllt.
Output: Konflikt-Auflösung ohne Selbstrekursion in quick-roll.js; echte vm-basierte Regressionstests (analog migration.test.js), die die PRODUKTIONS-Funktion aufrufen und beweisen, dass kein RangeError mehr auftritt; Entfernung des ungenutzten `showStorageConflictDialogInternal`-Exports.

Hintergrund WR-06 (01-REVIEW.md): `tests/unit/stability.test.js` (56 Tests) testet ausschließlich Inline-Simulationen, NIE den echten Quellcode — deshalb blieb CR-01 unentdeckt, obwohl die Suite grün war. Dieser Plan schließt diese Test-Lücke gezielt für die Konflikt-Auflösung.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stabilisierung/01-CONTEXT.md
@.planning/phases/01-stabilisierung/01-VERIFICATION.md
@.planning/phases/01-stabilisierung/01-REVIEW.md
@.planning/phases/01-stabilisierung/01-02-SUMMARY.md

<interfaces>
<!-- Reale Signaturen / Ist-Code. Executor implementiert gegen diese — keine Erkundung nötig. -->

systems/spellslots/quick-roll.js — IST-Zustand (BUG):
  // Zeilen 23-39 — die fehlerhafte Funktion:
  function showStorageConflictDialog(lsData, idbData, onUseLS, onUseIDB) {
      if (lsData === idbData) {
          if (typeof onUseLS === 'function') onUseLS();
          return;
      }
      if (typeof window.showStorageConflictDialog === 'function') {   // <-- IMMER true (Selbstreferenz!)
          window.showStorageConflictDialog(lsData, idbData, onUseLS, onUseIDB);  // <-- Endlosrekursion
      } else {
          if (typeof onUseIDB === 'function') onUseIDB();             // <-- Fallback wird NIE erreicht
      }
  }
  window.showStorageConflictDialogInternal = showStorageConflictDialog;  // Zeile 39 — ungenutzter Export (IN-02)

  // Zeilen 40-72 — der Aufrufer in load() (Signatur des Callbacks ist relevant):
  async function load() {
      ...
      if (s && !lsTimestamp) {
          try {
              const idbRecord = await window.loadFromIndexedDBFallbackRaw(key);
              if (idbRecord && idbRecord.data && idbRecord.data !== s) {
                  showStorageConflictDialog(                          // <-- 4-Argument-Aufruf (callback-Stil)
                      s,
                      idbRecord.data,
                      () => { /* LS-Daten nutzen — s bleibt unverändert */ },
                      () => { s = idbRecord.data; }
                  );
              }
          } catch (e) { /* IDB-Konfliktprüfung fehlgeschlagen — verschluckt */ }
      }
      ...
  }

WICHTIG: Der bestehende Aufruf in load() nutzt den CALLBACK-Stil (4 Argumente: lsData, idbData, onUseLS, onUseIDB). Die Fix-Funktion MUSS dieselbe Callback-Signatur behalten — load() darf unverändert bleiben. KEINE Umstellung auf Promise/await in diesem Plan (das wäre eine race-anfällige Änderung; siehe CR-01 Zusatzhinweis im Review).

tests/unit/migration.test.js — KANONISCHES vm-Test-Muster (vollständig lesen, exakt übernehmen):
  const fs = require('fs'); const path = require('path'); const vm = require('vm');
  beforeAll(() => {
    const context = { window: { APP_CONFIG: global.APP_CONFIG, ErrorHandler: { log: jest.fn() }, ... }, APP_CONFIG: global.APP_CONFIG, console };
    vm.createContext(context);
    const code = fs.readFileSync(path.join(__dirname, '../../systems/spellslots/version-migration.js'), 'utf8');
    vm.runInContext(code, context);
    migrateData = context.migrateData; compareVersions = context.compareVersions;
  });
  // Funktionen werden NACH runInContext aus context.<name> extrahiert.

esc(text) (global) — wird hier NICHT benötigt (keine neuen innerHTML-Strings in der Fix-Funktion).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: vm-basierte Regressionstests für die Konflikt-Auflösung schreiben (RED — beweist die Rekursion)</name>
  <files>tests/unit/storage-conflict.test.js</files>
  <read_first>
    - tests/unit/migration.test.js (KOMPLETT — kanonisches vm-Muster: fs.readFileSync + vm.createContext + vm.runInContext + Extraktion aus context.<name>; exakt dieses Muster übernehmen)
    - systems/spellslots/quick-roll.js (KOMPLETT — Ist-Zustand: showStorageConflictDialog Zeilen 23-39 mit der Selbstreferenz, load() Zeilen 40-72, der 4-Argument-Callback-Aufruf)
    - tests/setup.js (prüfen, ob global.APP_CONFIG dort gesetzt wird — migration.test.js verlässt sich darauf; den gleichen Zugriff nutzen)
    - .planning/phases/01-stabilisierung/01-REVIEW.md (Abschnitt CR-01 und WR-06 — die Test-Lücke, die dieser Test schließt)
  </read_first>
  <behavior>
    Neue Test-Datei `tests/unit/storage-conflict.test.js`. Lädt den ECHTEN Quelltext von quick-roll.js via vm (Muster aus migration.test.js) und ruft die echte Konflikt-Funktion auf. Test-Fälle:
    - Test A (Kern-Regression, MUSS initial RED sein): Aufruf der Konflikt-Funktion mit UNTERSCHIEDLICHEN Daten (lsData='ALT', idbData='NEU') wirft KEINEN RangeError. Assertion: `expect(() => resolveStorageConflict('ALT','NEU', onUseLS, onUseIDB)).not.toThrow()`. Gegen den Ist-Code (Selbstreferenz) schlägt das fehl (RangeError: Maximum call stack size exceeded) → beweist den Bug.
    - Test B (IDB-Fallback ohne UI-Hook): Bei unterschiedlichen Daten UND ohne `window.showStorageConflictDialogUI` wird der `onUseIDB`-Callback genau einmal aufgerufen, `onUseLS` nicht. (Spies via jest.fn().)
    - Test C (Identisch-Fall, D-07): Bei `lsData === idbData` wird `onUseLS` aufgerufen, `onUseIDB` NICHT, und kein UI-Hook getriggert.
    - Test D (optionaler externer UI-Hook): Ist `context.window.showStorageConflictDialogUI` als jest.fn() gesetzt, wird BEI unterschiedlichen Daten dieser Hook genau einmal mit (lsData, idbData, onUseLS, onUseIDB) aufgerufen und die internen Fallback-Callbacks NICHT direkt (der Hook übernimmt die Auswahl).
    - Test E (Selbstreferenz-Guard, Quelltext-Audit): `fs.readFileSync` von quick-roll.js enthält INNERHALB der Konflikt-Funktion keinen Aufruf, der ihren eigenen Namen über `window` referenziert (Regex prüft: kein `window.resolveStorageConflict(` und kein `window.showStorageConflictDialog(` im Funktionskörper).
  </behavior>
  <action>
    Erstelle `tests/unit/storage-conflict.test.js`. Vorgehen (Feedback-Loop CLAUDE.md: vor jedem Test kurz beschreiben, wie er das Verhalten prüft):

    1. Übernimm das vm-Setup EXAKT aus `tests/unit/migration.test.js`:
       - `const fs = require('fs'); const path = require('path'); const vm = require('vm');`
       - In `beforeAll`: baue ein `context`-Objekt mit allen Globals, die quick-roll.js erwartet. Mindestens:
         ```javascript
         const dialogUISpy = jest.fn();
         const context = {
             window: {
                 APP_CONFIG: global.APP_CONFIG,
                 ErrorHandler: { log: jest.fn() },
                 // showStorageConflictDialogUI bewusst NICHT setzen für Test A/B/C (Fallback-Pfad)
                 loadFromIndexedDBFallbackRaw: jest.fn(),
                 loadFromIndexedDBFallback: jest.fn(),
                 D: {},
                 STORAGE_KEY: 'test-key'
             },
             APP_CONFIG: global.APP_CONFIG,
             StorageAPI: { get: jest.fn(() => null), set: jest.fn(), remove: jest.fn() },
             showToast: jest.fn(),
             compareVersions: () => 0,
             migrateData: (d) => d,
             console
         };
         vm.createContext(context);
         const code = fs.readFileSync(path.join(__dirname, '../../systems/spellslots/quick-roll.js'), 'utf8');
         vm.runInContext(code, context);
         ```
       - Extrahiere NACH `runInContext` die Funktion: `resolveStorageConflict = context.resolveStorageConflict;` (Name nach dem Fix aus Task 2 — die Tests sind für den Ziel-Zustand geschrieben; bis Task 2 sind A–D rot bzw. `context.resolveStorageConflict` ist undefined → das ist der erwartete RED-Zustand).
    2. Schreibe Test A–E gemäß <behavior>. Für Test E lies die Quelldatei direkt und prüfe per Regex, dass im Körper der Konflikt-Funktion KEINE Selbstreferenz über `window.` mehr steht.
    3. Beachte: Diese Tests prüfen die ECHTE Funktion (kein Inline-Replikat). Das ist der Kernunterschied zu stability.test.js (WR-06). Dokumentiere das als Kommentar oben in der Datei.

    Die Tests müssen JETZT (vor Task 2) FEHLSCHLAGEN: `context.resolveStorageConflict` existiert noch nicht (Ist-Code heißt `showStorageConflictDialog` und rekursiert). Test A gegen den Ist-Namen würde RangeError werfen — beides beweist den Bug. Commit als RED-Schritt.
  </action>
  <verify>
    <automated>cd "D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular" && npx jest tests/unit/storage-conflict.test.js --no-coverage 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Datei existiert: `test -f tests/unit/storage-conflict.test.js` (Exit 0)
    - `grep -c "vm.runInContext" tests/unit/storage-conflict.test.js` gibt mindestens `1` zurück (echter Quelltext wird geladen, kein Inline-Replikat)
    - `grep -c "readFileSync" tests/unit/storage-conflict.test.js` gibt mindestens `1` zurück
    - `grep -ciE "not.toThrow|RangeError|call stack" tests/unit/storage-conflict.test.js` gibt mindestens `1` zurück (Kern-Regression gegen die Rekursion)
    - `grep -c "resolveStorageConflict" tests/unit/storage-conflict.test.js` gibt mindestens `1` zurück (Tests sind auf den Ziel-Namen geschrieben)
    - Die Tests schlagen erwartungsgemäß fehl (RED): `npx jest tests/unit/storage-conflict.test.js --no-coverage` endet mit Exit-Code ungleich 0 (Beweis, dass sie echte, noch nicht gefixte Logik prüfen)
  </acceptance_criteria>
  <done>Eine neue Test-Datei lädt die echte quick-roll.js via vm (Muster migration.test.js) und prüft die Konflikt-Auflösung; sie schlägt gegen den rekursiven Ist-Code fehl und beweist damit CR-01.</done>
</task>

<task type="auto">
  <name>Task 2: CR-01 beheben — Selbstrekursion entfernen, Konflikt-Funktion umbenennen, ungenutzten Export streichen (GREEN)</name>
  <files>systems/spellslots/quick-roll.js</files>
  <read_first>
    - systems/spellslots/quick-roll.js (KOMPLETT — showStorageConflictDialog Zeilen 23-39, der Export Zeile 39, der Aufruf in load() Zeilen 57-62)
    - tests/unit/storage-conflict.test.js (die in Task 1 geschriebenen Tests — sie definieren den Ziel-Namen `resolveStorageConflict` und die erwartete Semantik A–E)
    - .planning/phases/01-stabilisierung/01-REVIEW.md (CR-01 Fix-Vorschlag mit fertigem Code-Snippet `function resolveStorageConflict(...)`; IN-02 zum ungenutzten Export)
    - CLAUDE.md (Abschnitt "Duplicate Declaration Debugging Pattern" + "Export Audit Rule" — keine doppelten Top-Level-Funktionsnamen; nur exportieren, was andere Module nutzen)
  </read_first>
  <action>
    Bearbeite `systems/spellslots/quick-roll.js`. Konkrete Änderungen (Werte exakt, keine Verweise):

    **(a) Konflikt-Funktion umbenennen und Selbstrekursion entfernen.** Ersetze die komplette Funktion `showStorageConflictDialog` (Zeilen 25-37) durch `resolveStorageConflict` mit IDENTISCHER Callback-Signatur (4 Argumente). Die Delegation geht NUR noch an einen ANDERS benannten optionalen UI-Hook `window.showStorageConflictDialogUI` (existiert aktuell nicht → Fallback greift = IDB bevorzugen). Exakter Code:
    ```javascript
    // (D-07) Speicher-Konflikt auflösen. Es gibt (noch) keinen externen UI-Dialog —
    // bei abweichendem Inhalt wird der neuere IDB-Stand bevorzugt (kein stiller Verlust).
    // WICHTIG: KEINE Selbstreferenz über window.<eigener Name> — das war CR-01 (Endlosrekursion).
    function resolveStorageConflict(lsData, idbData, onUseLS, onUseIDB) {
        // Identischer Inhalt: kein Konflikt, LS-Stand still nutzen
        if (lsData === idbData) {
            if (typeof onUseLS === 'function') onUseLS();
            return;
        }
        // Optionaler externer UI-Dialog (anders benannt als diese Funktion!)
        if (typeof window.showStorageConflictDialogUI === 'function') {
            window.showStorageConflictDialogUI(lsData, idbData, onUseLS, onUseIDB);
        } else {
            // Fallback: IDB-Daten bevorzugen (neuere Quelle) — kein stiller Sieg veralteter LS-Daten
            if (typeof onUseIDB === 'function') onUseIDB();
        }
    }
    ```

    **(b) Ungenutzten Export entfernen (IN-02 / Export-Audit-Regel).** Lösche Zeile 39 vollständig: `window.showStorageConflictDialogInternal = showStorageConflictDialog;` — diese Funktion existiert nach (a) nicht mehr und der Export wird nirgends im Repo referenziert. KEINEN Ersatz-Export hinzufügen (resolveStorageConflict wird nur modul-intern von load() genutzt; Export-Audit: nicht exportieren, was kein anderes Modul braucht).

    **(c) Aufruf in load() umbenennen.** In `load()` (aktuell Zeile 57) ändere den Funktionsnamen im Aufruf von `showStorageConflictDialog(` auf `resolveStorageConflict(`. Die 4 Argumente (s, idbRecord.data, onUseLS-Callback, onUseIDB-Callback) bleiben UNVERÄNDERT — nur der Name ändert sich. load() bleibt synchron/callback-basiert (KEIN await, KEINE Promise-Umstellung — das würde mit dem weiterlaufenden Load-Pfad um `s` racen; siehe CR-01 Zusatzhinweis).

    CLAUDE.md-Regeln: KEINE neuen `const X = window.X` innerhalb von Funktionen einführen; `window.showStorageConflictDialogUI` direkt per `typeof window....` prüfen (so wie im Snippet). Sicherstellen, dass `resolveStorageConflict` als Name NICHT bereits an anderer Stelle als Top-Level-Funktion existiert (keine doppelten Deklarationen → Build-Dedup-Pass-3-Falle vermeiden).
  </action>
  <verify>
    <automated>cd "D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular" && npx jest tests/unit/storage-conflict.test.js --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function resolveStorageConflict" systems/spellslots/quick-roll.js` gibt `1` zurück (Funktion umbenannt)
    - `grep -c "resolveStorageConflict(" systems/spellslots/quick-roll.js` gibt mindestens `2` zurück (Definition + Aufruf in load())
    - `grep -c "showStorageConflictDialogInternal" systems/spellslots/quick-roll.js` gibt `0` zurück (ungenutzter Export entfernt)
    - KEINE Selbstreferenz mehr: `grep -c "window.resolveStorageConflict" systems/spellslots/quick-roll.js` gibt `0` zurück; UND die alte rekursive Zeile ist weg: `grep -c "window.showStorageConflictDialog(" systems/spellslots/quick-roll.js` gibt `0` zurück
    - Optionaler UI-Hook anders benannt vorhanden: `grep -c "showStorageConflictDialogUI" systems/spellslots/quick-roll.js` gibt mindestens `1` zurück
    - load() bleibt callback-basiert: `grep -c "await resolveStorageConflict" systems/spellslots/quick-roll.js` gibt `0` zurück (keine race-anfällige await-Umstellung)
    - ALLE Tests aus Task 1 sind GRÜN: `npx jest tests/unit/storage-conflict.test.js --no-coverage` Exit-Code 0 (insbesondere Test A: kein RangeError mehr)
    - Keine Regression: `npx jest tests/unit/stability.test.js --no-coverage` Exit-Code 0 (die 56 bestehenden Tests bleiben grün)
  </acceptance_criteria>
  <done>Der Konfliktpfad läuft ohne Endlosrekursion: bei abweichenden Daten gewinnt ohne UI-Hook der IDB-Stand, bei identischem Inhalt still der LS-Stand; der ungenutzte Export ist weg; alle neuen und bestehenden Tests grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Persistierte Kampagnendaten (LS/IDB) → App-Laufzeit | Bei Erstlade-Konflikt entscheidet die Konflikt-Auflösung, welcher Stand ins D-Objekt gemerged wird. Fehlverhalten hier = stiller Datenverlust. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08-01 | Information Disclosure / Data Loss | resolveStorageConflict — Endlosrekursion verschluckt im Load-Pfad, stale LS gewinnt still (CR-01) | mitigate | Selbstrekursion entfernt; Fallback bevorzugt deterministisch den neueren IDB-Stand; vm-Regressionstest (Test A) erzwingt "kein RangeError" dauerhaft |
| T-01-08-02 | Tampering | Kein Test deckte die reale Funktion ab (WR-06) — Bug konnte unbemerkt einziehen | mitigate | Neue vm-basierte Tests rufen die PRODUKTIONS-Funktion auf (kein Inline-Replikat); Quelltext-Audit (Test E) verhindert Wiedereinführung der Selbstreferenz |
| T-01-08-03 | DoS (lokal) | RangeError (Stack-Überlauf) im kritischsten Code-Pfad der App | mitigate | Rekursion strukturell entfernt; Fix ist synchron und O(1) |

Hinweis: Keine neuen innerHTML-/Netzwerk-/Eingabe-Oberflächen. `esc()` nicht erforderlich (kein neuer DOM-Render in der Fix-Funktion). Eine echte UI-Dialog-Implementierung (mit esc-Pflicht) ist bewusst NICHT Teil dieses Gap-Plans (Out of scope; der Fallback deckt SC2 ab).
</threat_model>

<verification>
- `npx jest tests/unit/storage-conflict.test.js --no-coverage` grün (alle Konflikt-Tests, inkl. "kein RangeError")
- `npx jest tests/unit/stability.test.js --no-coverage` grün (keine Regression der 56 bestehenden Tests)
- `grep -c "showStorageConflictDialogInternal" systems/spellslots/quick-roll.js` == 0
- `grep -c "window.showStorageConflictDialog(" systems/spellslots/quick-roll.js` == 0 (keine Selbstrekursion mehr)
- Manuell (UAT, laut 01-VERIFICATION.md — NICHT Teil dieses Plans): In DevTools LS-Key ohne _ts mit abweichendem Inhalt setzen, App neu laden → kein RangeError in der Konsole; neuerer Stand gewinnt.
</verification>

<success_criteria>
- Der Stale-Shadow-Konfliktpfad wirft keinen RangeError mehr; veraltete LS-Daten gewinnen NICHT mehr still (STAB-05 / SC2 Code-Ebene geschlossen)
- Die Konflikt-Auflösung ist durch echte (vm-basierte) Tests abgesichert (WR-06-Lücke für diesen Pfad geschlossen)
- Kein ungenutzter window-Export mehr (Export-Audit-konform)
</success_criteria>

<output>
Nach Abschluss: Erstelle `.planning/phases/01-stabilisierung/01-08-SUMMARY.md`
</output>
</content>
