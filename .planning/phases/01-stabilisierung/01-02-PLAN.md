---
phase: 01-stabilisierung
plan: 02
type: execute
wave: 2
depends_on: ['01']
files_modified:
  - systems/spellslots/persistence.js
  - systems/spellslots/quick-roll.js
  - tests/unit/stability.test.js
autonomous: true
requirements: [STAB-05, STAB-06]
nyquist_compliant: true

must_haves:
  truths:
    - "Eine Kampagne >5MB verliert beim Neuladen keine Daten mehr (IDB gewinnt gegen veralteten LS-Schatten)"
    - "Ein <5MB-Save schreibt einen Begleit-Zeitstempel, sodass beim Laden der neueste Stand gewinnt"
    - "Beim IDB-only-Save wird der veraltete LS-Schlüssel entfernt und es erscheint genau ein Sitzungs-Hinweis"
    - "Schlägt das IDB-Schreiben fehl, erscheint ein lauter Fehler-Toast mit Export-Aufforderung"
    - "Exporte tragen APP_CONFIG.VERSION statt hartkodiert '2.11'"
    - "Re-Import einer Legacy-'2.11'-Datei führt Migrationen korrekt aus statt sie zu überspringen"
    - "Der Erstlade-Konflikt-Dialog (D-07) erscheint nur bei echtem Altdaten-Konflikt, nicht bei normaler Nutzung"
    - "Der Konflikt-Dialog (D-07) erscheint NICHT, wenn LS- und IDB-Inhalt identisch sind"
  artifacts:
    - path: "systems/spellslots/persistence.js"
      provides: "Save mit Begleit-Timestamp, LS-Schatten-Entfernung, Sitzungs-Hinweise, IDB-Fehler-Toast, IDB-Raw-Read"
      contains: "_ts"
    - path: "systems/spellslots/quick-roll.js"
      provides: "Load mit Timestamp-Vergleich + Konflikt-Dialog, Export-Stempel APP_CONFIG.VERSION, Legacy-2.11-Erkennung"
      contains: "APP_CONFIG.VERSION"
    - path: "tests/unit/stability.test.js"
      provides: "Regressionstests gegen REALE Persistenz-Logik (>5MB, Konflikt, Identisch-Fall, Export-Version, Legacy-Stempel)"
      contains: "5MB"
  key_links:
    - from: "systems/spellslots/quick-roll.js load()"
      to: "systems/spellslots/persistence.js (IDB-Raw-Read)"
      via: "loadFromIndexedDBFallbackRaw liefert {data, timestamp}"
      pattern: "loadFromIndexedDBFallbackRaw"
    - from: "systems/spellslots/persistence.js saveImmediate() IDB-only-Zweig"
      to: "localStorage Schatten-Key"
      via: "StorageAPI.remove(key) nach erfolgreichem IDB-Write"
      pattern: "StorageAPI\\.remove"
    - from: "systems/spellslots/quick-roll.js exportAllDataAsFile()"
      to: "APP_CONFIG.VERSION"
      via: "exp._version = APP_CONFIG.VERSION"
      pattern: "exp\\._version = APP_CONFIG"
---

<objective>
Behebt den Stale-Shadow-Datenverlust-Bug für Kampagnen >5MB (STAB-05) und den hartkodierten Export-Versionsstempel (STAB-06). Dies ist die kritischste Aufgabe der Phase: der Bug verliert stillschweigend Spielerdaten am Spieltisch.

Purpose: "Kein stiller Datenverlust, nirgends" (CONTEXT.md Leitlinie). Der Save-/Load-Pfad ist der heikelste Code der App; subtile async-Reihenfolge und der D-07-Dialog-Trigger müssen exakt stimmen.
Output: Reparierter Save-Pfad (persistence.js), reparierter Load-/Export-Pfad mit Konflikt-Dialog (quick-roll.js), Regressionstests gegen reale Logik (stability.test.js).

Umsetzte Entscheidungen: D-01 (Timestamp-Vergleich beim Laden, LS-Begleit-Key), D-02 (einmaliger IDB-Modus-Hinweis), D-03 (lauter IDB-Fehler-Toast + Export-Aufforderung), D-04 (Jest-Regressionstest), D-05 (Export-Stempel APP_CONFIG.VERSION + Legacy-Erkennung), D-07 (einmaliger Konflikt-Dialog, KEIN Dialog bei identischem Inhalt), D-08 (4-MB-Warnung einmal pro Sitzung).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stabilisierung/01-CONTEXT.md
@.planning/phases/01-stabilisierung/01-RESEARCH.md
@.planning/phases/01-stabilisierung/01-PATTERNS.md

<interfaces>
<!-- Reale Signaturen aus dem Quellcode. Executor implementiert gegen diese — keine Erkundung nötig. -->

systems/spellslots/persistence.js — bestehende Funktionen:
  async function saveImmediate()                         // Save-Pfad mit LS/IDB-Logik (Zeilen 9-69)
  async function saveToIndexedDBFallback(key, dataString)// schreibt store.put({id:key, data:dataString, timestamp:Date.now()}) (Zeilen 71-84)
  async function loadFromIndexedDBFallback(key)          // liefert NUR request.result.data (Zeilen 86-106) — gibt KEINEN timestamp zurück!
  const save = function(showMessage=false)               // debounced Save (Zeilen 109-172) — ebenfalls IDB-only-Zweig anpassen
  Konstanten im Save: LS_LIMIT_MB = 5, LS_WARNING_MB = 4
  Export-Globals am Dateiende: window.save, window.saveImmediate

systems/spellslots/quick-roll.js — bestehende Funktionen:
  async function load()                                  // Load-Pfad mit Stale-Shadow-Bug (Zeilen 23-115)
    Bug-Stelle (Zeilen 31-45): let s = StorageAPI.get(key,null); if(!s){ s = await loadFromIndexedDBFallback(key); }
    Migrations-Aufruf (Zeilen 63-72): if(!p._version || compareVersions(p._version, CURRENT_VERSION)<0){ p = migrateData(p); }
    CURRENT_VERSION = APP_CONFIG.VERSION (Zeile 29)
  function exportAllDataAsFile()                         // Export (Zeilen 116-148)
    Bug-Stelle (Zeile 133): exp._version = '2.11';

systems/spellslots/version-migration.js (NICHT modifizieren in diesem Plan — nur referenziert):
  function migrateData(data)                             // läuft alle MIGRATIONS-Versionen < CURRENT durch
  function compareVersions(v1, v2)                       // -1 / 0 / 1

StorageAPI (utils, global, const-deklariert — direkt nutzbar):
  StorageAPI.get(key, default)  -> string | default
  StorageAPI.set(key, value)    -> {success, error}
  StorageAPI.remove(key)        -> entfernt Key

utils/utilities.js (global):
  showToast(msg, type='success', duration=2000)         // type: 'success'|'error'|'warning'|'info'; schreibt auch ins Event-Log
  esc(text)                                              // XSS-Escape für innerHTML

Modal-Pattern (systems/spellslots/navigation.js): showModal(id) / hideModal(id) via classList 'show'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Regressionstests gegen REALE Persistenz-Logik schreiben (RED)</name>
  <files>tests/unit/stability.test.js</files>
  <read_first>
    - tests/unit/stability.test.js (Komplett — bestehende Struktur, beforeEach, vorhandene save/load-Tests)
    - tests/setup.js (Zeilen 116-343 — WICHTIG: setup.js MOCKT aktuell save/load/saveImmediate komplett; localStorage ist gemockt. Diese Mocks dürfen die neuen Tests NICHT überdecken)
    - systems/spellslots/persistence.js (Save-Logik, die getestet wird)
    - systems/spellslots/quick-roll.js (Load-/Export-Logik, die getestet wird)
    - .planning/phases/01-stabilisierung/01-PATTERNS.md (Abschnitt stability.test.js — Test-Patterns für >5MB, Migration)
  </read_first>
  <behavior>
    Neue describe-Blöcke (alle initial RED, werden in Task 2/3 grün):
    - "Export-Version": exportAllDataAsFile() stempelt exp._version === APP_CONFIG.VERSION (NICHT '2.11').
    - "Legacy-Stempel": Daten mit _version==='2.11' werden beim Laden als Altformat behandelt → migrateData() läuft (Migration wird aufgerufen, nicht übersprungen).
    - "5MB IDB-only Roundtrip": Bei >5MB-Daten schreibt der Save in den IDB-Mock und entfernt den LS-Schatten-Key; ein anschließender Load liest den IDB-Stand (nicht den alten LS-Stand).
    - "Conflict-Dialog-Logik (Trigger)": Bei LS-Stand OHNE _ts und IDB-Stand MIT timestamp und UNTERSCHIEDLICHEM Inhalt wird der Konflikt-Pfad ausgelöst (showStorageConflictDialog wird genau einmal aufgerufen).
    - "Conflict-Dialog-Logik (Kein Dialog bei vorhandenem LS-_ts)": Ist ein LS-_ts gesetzt (>0), erscheint KEIN Dialog — der neueste Stand wird automatisch gewählt.
    - "Conflict-Dialog erscheint NICHT bei identischem Inhalt (D-07)": Bei LS-Stand OHNE _ts und IDB-Stand MIT timestamp, aber IDENTISCHEM Inhalt (lsData === idbData), wird showStorageConflictDialog NICHT aufgerufen; der Load nimmt still den (identischen) Stand. Dies ist die explizite D-07-Bedingung "Sind die Inhalte identisch: kein Dialog".
    - "Begleit-Timestamp": Normaler <5MB-Save setzt zusätzlich den Key `${STORAGE_KEY}_ts`.
  </behavior>
  <action>
    Erweitere `tests/unit/stability.test.js` um die oben genannten describe-Blöcke (inkl. des dedizierten Identisch-Fall-Tests für D-07). ENTSCHEIDENDE Hürde (aus CONCERNS.md Test-Gap): `tests/setup.js` ersetzt `save`/`load`/`saveImmediate` durch Mocks und mockt `localStorage`. Die neuen Tests müssen die REALEN Funktionen aus den Quellmodulen prüfen, nicht die Mocks. Vorgehen:
    1. Lade die realen Funktionen ins Test-Modul: Da der Code non-ESM/global ist, lies die Quelldateien per `fs.readFileSync` und werte sie im Test-Scope aus (Pattern: `new Function('window','D','StorageAPI', ... , src)()` oder `vm`-Modul), ODER definiere die zu testende Logik durch direktes Requiren der Funktionen über einen kleinen Test-Harness. Wähle den Ansatz, der mit der bestehenden jsdom-Umgebung funktioniert; dokumentiere die Wahl als Kommentar im Test.
    2. Mock-Storage statt App-Mock: Behalte ein gemocktes `localStorage` und ein gemocktes IndexedDB (`window.idb` mit transaction/objectStore/put/get, das `{id, data, timestamp}` speichert und zurückgibt). Stelle `window._mockIDBStore = {}` bereit, dessen Inhalt die Tests inspizieren. Mocke `showStorageConflictDialog` (bzw. die Dialog-Funktion) als Jest-Spy, damit die Tests "wurde aufgerufen / nicht aufgerufen" prüfen können — entscheidend für den Identisch-Fall.
    3. Für den >5MB-Test: Erzeuge Daten >5MB (`'X'.repeat(6*1024*1024)` in einem Charakter-Feld) und prüfe, dass nach `saveImmediate()` der IDB-Mock den Stand hat UND `localStorage[STORAGE_KEY]` entfernt wurde.
    4. Für den Export-Test: Rufe die reale `exportAllDataAsFile()` auf (mit gemocktem `document.createElement('a')`/`URL.createObjectURL`) und fange den serialisierten Blob-Inhalt ab; prüfe `_version === APP_CONFIG.VERSION`.
    5. Für den Identisch-Fall-Test (D-07): Setze im LS-Mock einen Stand OHNE `_ts`-Key und im IDB-Mock denselben Daten-String MIT timestamp>0; rufe `load()` auf und assertiere, dass der Dialog-Spy `not.toHaveBeenCalled()`. Ergänze den Trigger-Gegentest (unterschiedlicher Inhalt → Spy `toHaveBeenCalledTimes(1)`), damit beide Pfade abgedeckt sind.
    Diese Tests sollen JETZT FEHLSCHLAGEN (Export stempelt noch '2.11', LS-Schatten wird noch nicht entfernt, kein _ts-Key, keine Legacy-Erkennung, kein Konflikt-/Identisch-Pfad). Commit als RED-Schritt.
    Hinweis (CLAUDE.md Feedback-Loop): Beschreibe vor dem Schreiben jedes Tests kurz, wie er das Verhalten prüft.
  </action>
  <verify>
    <automated>cd "D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular" && npx jest tests/unit/stability.test.js --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "5MB" tests/unit/stability.test.js` gibt mindestens `1` zurück
    - `grep -c "2.11" tests/unit/stability.test.js` gibt mindestens `1` zurück (Legacy-Stempel-Test)
    - `grep -c "_ts" tests/unit/stability.test.js` gibt mindestens `1` zurück (Begleit-Timestamp-Test)
    - `grep -cE "exportAllDataAsFile|_version" tests/unit/stability.test.js` gibt mindestens `1` zurück
    - `grep -ciE "identisch|not.toHaveBeenCalled" tests/unit/stability.test.js` gibt mindestens `1` zurück (expliziter D-07-Identisch-Fall: Dialog wird NICHT aufgerufen)
    - Die NEUEN describe-Blöcke schlagen fehl (RED): `npx jest tests/unit/stability.test.js --no-coverage` meldet mindestens 4 failing tests in den neuen Blöcken (Beweis, dass sie reale, noch nicht implementierte Logik prüfen)
  </acceptance_criteria>
  <done>Neue RED-Test-Blöcke prüfen reale Persistenz-/Export-Logik mit gemocktem Storage — inklusive des dedizierten D-07-Identisch-Falls (kein Dialog bei gleichem Inhalt); sie schlagen erwartungsgemäß fehl.</done>
</task>

<task type="auto">
  <name>Task 2: Save-Pfad härten — Begleit-Timestamp, LS-Schatten-Entfernung, Sitzungs-Hinweise, IDB-Fehler-Toast</name>
  <files>systems/spellslots/persistence.js</files>
  <read_first>
    - systems/spellslots/persistence.js (Komplett — saveImmediate Zeilen 9-69, saveToIndexedDBFallback 71-84, loadFromIndexedDBFallback 86-106, save 109-172)
    - .planning/phases/01-stabilisierung/01-PATTERNS.md (Abschnitt persistence.js — exakte Ergänzungs-Snippets D-01/D-02/D-03/D-08)
    - .planning/phases/01-stabilisierung/01-RESEARCH.md (Pitfall 1: removeItem NACH erfolgreichem IDB-Write; Code-Beispiel Stale-Shadow-Fix)
  </read_first>
  <action>
    Bearbeite `systems/spellslots/persistence.js`. Konkrete Änderungen:

    **(a) Begleit-Timestamp beim normalen LS-Save (D-01)** — in `saveImmediate()` nach erfolgreichem `StorageAPI.set(key, dataString)` (aktuell Zeile 42-46) ergänzen:
    ```javascript
    StorageAPI.set(key + '_ts', String(Date.now()));
    ```
    Gleiches im debounced `save` (Zeile 147) nach erfolgreichem `StorageAPI.set(key, dataString)`.

    **(b) LS-Schatten entfernen beim IDB-only-Save (D-01, Stale-Shadow-Kern-Fix)** — im IDB-only-Zweig von `saveImmediate()` (Zeilen 33-40) NACH `await saveToIndexedDBFallback(key, dataString)` und VOR `return`:
    ```javascript
    StorageAPI.remove(key);
    StorageAPI.remove(key + '_ts');
    ```
    Reihenfolge zwingend: erst `await` IDB-Write, dann `remove` (Pitfall 1 — niemals LS löschen, bevor IDB bestätigt ist). Gleiches im IDB-only-Zweig des debounced `save` (Zeilen 139-146).

    **(c) IDB-Modus-Hinweis einmal pro Sitzung (D-02)** — ersetze im IDB-only-Zweig von `saveImmediate()` den bestehenden Per-Save-Toast `showToast('💾 Große Kampagne in IndexedDB gespeichert', 'success')` durch:
    ```javascript
    if (!window._idbModeSeen) {
        window._idbModeSeen = true;
        showToast('💾 Kampagne im IndexedDB-Modus (>5MB). Daten sicher gespeichert.', 'info', 4000);
    }
    ```
    Im debounced `save` IDB-only-Zweig: den `if (showMessage) showToast(...)` ebenfalls auf das `window._idbModeSeen`-Gate umstellen (kein Toast-Spam).

    **(d) 4-MB-Warnung einmal pro Sitzung (D-08)** — ersetze den bestehenden Warnblock (Zeilen 28-31) durch:
    ```javascript
    if (dataSizeMB > LS_WARNING_MB && dataSizeMB <= LS_LIMIT_MB) {
        if (!window._sizeWarningSeen) {
            window._sizeWarningSeen = true;
            showToast(`⚠️ Kampagne wird groß (${dataSizeMB.toFixed(1)}MB). Backup empfohlen!`, 'warning', 5000);
        }
    }
    ```
    (showToast schreibt automatisch ins Event-Log — die Folge-Saves landen damit weiterhin im Log, ohne erneuten Toast.)

    **(e) Lauter IDB-Fehler-Toast + Export-Aufforderung (D-03)** — im catch-Block `catch (idbError)` (Zeilen 63-67) den Fehler-Toast schärfen und Export anbieten:
    ```javascript
    updateSaveIndicator('error');
    showToast('❌ Speichern fehlgeschlagen! Daten gehen sonst verloren — JETZT exportieren!', 'error', 10000);
    if (typeof window.exportAllDataAsFile === 'function') {
        window.exportAllDataAsFile();
    }
    ```

    **(f) IDB-Raw-Read-Funktion ergänzen (für Load-Konflikt-Erkennung in Task 3, D-01/D-07)** — füge neben `loadFromIndexedDBFallback` eine neue Funktion hinzu, die das VOLLE Record-Objekt mit timestamp zurückgibt (nicht nur `.data`):
    ```javascript
    async function loadFromIndexedDBFallbackRaw(key) {
        const initIndexedDB = window.initIndexedDB;
        let idb = window.idb;
        if (!idb) await initIndexedDB();
        idb = window.idb;
        return new Promise((resolve, reject) => {
            const transaction = idb.transaction(['campaigns'], 'readonly');
            const store = transaction.objectStore('campaigns');
            const request = store.get(key);
            request.onsuccess = () => resolve(request.result || null); // {id, data, timestamp} | null
            request.onerror = () => reject(request.error);
        });
    }
    ```
    Exportiere sie am Dateiende: `window.loadFromIndexedDBFallbackRaw = loadFromIndexedDBFallbackRaw;`

    CLAUDE.md-Regeln beachten: KEINE neuen `const X = window.X` innerhalb von Funktionen einführen (nutze vorhandene Top-Level-Imports/direkten `window.X`-Zugriff); `esc()` ist hier nicht nötig (keine neuen innerHTML-Strings); ErrorHandler/console-Muster wie im Bestand belassen.
  </action>
  <verify>
    <automated>cd "D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular" && npx jest tests/unit/stability.test.js --no-coverage -t "5MB" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_ts" systems/spellslots/persistence.js` gibt mindestens `2` zurück (set im LS-Save + remove im IDB-only-Zweig)
    - `grep -c "StorageAPI.remove" systems/spellslots/persistence.js` gibt mindestens `2` zurück (key + key+'_ts')
    - `grep -c "_idbModeSeen" systems/spellslots/persistence.js` gibt mindestens `1` zurück (D-02 Sitzungs-Gate)
    - `grep -c "_sizeWarningSeen" systems/spellslots/persistence.js` gibt mindestens `1` zurück (D-08 Sitzungs-Gate)
    - `grep -c "loadFromIndexedDBFallbackRaw" systems/spellslots/persistence.js` gibt mindestens `2` zurück (Definition + window-Export)
    - `grep -c "exportAllDataAsFile" systems/spellslots/persistence.js` gibt mindestens `1` zurück (D-03 Export-Aufforderung)
    - Der ">5MB IDB-only Roundtrip"-Test aus Task 1 ist GRÜN: `npx jest tests/unit/stability.test.js --no-coverage -t "5MB"` Exit-Code 0
  </acceptance_criteria>
  <done>Save-Pfad setzt Begleit-Timestamp, entfernt den LS-Schatten beim IDB-only-Save (nach bestätigtem Write), zeigt Sitzungs-Hinweise statt Spam, schreit bei IDB-Fehler mit Export-Angebot; Raw-Read-Funktion bereitgestellt.</done>
</task>

<task type="auto">
  <name>Task 3: Load-Pfad mit Timestamp-Vergleich + Konflikt-Dialog, Export-Stempel-Fix, Legacy-2.11-Erkennung</name>
  <files>systems/spellslots/quick-roll.js</files>
  <read_first>
    - systems/spellslots/quick-roll.js (Komplett — load() Zeilen 23-115, exportAllDataAsFile() 116-148)
    - systems/spellslots/persistence.js (loadFromIndexedDBFallbackRaw aus Task 2 — Signatur {id,data,timestamp}|null)
    - systems/spellslots/version-migration.js (migrateData, compareVersions — Aufruf-Vertrag, NICHT modifizieren)
    - .planning/phases/01-stabilisierung/01-PATTERNS.md (Abschnitt quick-roll.js — D-01/D-05/D-07 Snippets; XSS-Schutz-Pattern)
    - .planning/phases/01-stabilisierung/01-RESEARCH.md (Pitfall 2: Dialog nur bei lsTs===0 && idbTs>0 && Inhalt unterschiedlich; Pitfall 3: Legacy-Stempel)
  </read_first>
  <action>
    Bearbeite `systems/spellslots/quick-roll.js`. Konkrete Änderungen:

    **(a) Stale-Shadow-Fix im Load-Pfad mit Timestamp-Vergleich (D-01)** — ersetze den Block Zeilen 31-45 (`let s = StorageAPI.get(key,null); if(!s){...IDB...}`) durch eine Timestamp-vergleichende Logik:
    ```javascript
    const lsData = StorageAPI.get(key, null);
    const lsTs = lsData ? parseInt(StorageAPI.get(key + '_ts', '0'), 10) : 0;
    let idbData = null, idbTs = 0;
    try {
        const loadFromIndexedDBFallbackRaw = window.loadFromIndexedDBFallbackRaw;
        const idbRecord = await loadFromIndexedDBFallbackRaw(key);
        if (idbRecord && idbRecord.data) { idbData = idbRecord.data; idbTs = idbRecord.timestamp || 0; }
    } catch (e) { /* kein IDB-Eintrag */ }

    let s = null;
    // D-07: Erstlade-Konflikt nur bei Altdaten (LS ohne _ts) UND abweichendem IDB-Stand mit timestamp.
    // Identischer Inhalt (lsData === idbData) -> KEIN Dialog (D-07: "Sind die Inhalte identisch: kein Dialog").
    if (lsData && idbData && lsTs === 0 && idbTs > 0 && lsData !== idbData) {
        const useIdb = await showStorageConflictDialog(lsData, idbData, idbTs);
        s = useIdb ? idbData : lsData;
    } else if (lsData && (!idbData || lsTs >= idbTs)) {
        s = lsData;          // LS ist aktueller oder einzige Quelle (auch: identischer Inhalt -> still LS nehmen)
    } else if (idbData) {
        s = idbData;         // IDB ist aktueller (oder LS leer)
        if (!lsData) showToast('💾 Kampagne aus IndexedDB geladen', 'info', 3000);
    }
    ```
    Wichtig (Pitfall 2 + D-07): Bei vorhandenem `lsTs` (>0) ODER identischem Inhalt (`lsData === idbData`) KEIN Dialog — sonst erscheint er bei jedem Reload normaler <5MB-Kampagnen wegen des 2MB-IDB-Mirrors. Der Identisch-Fall fällt durch die `lsData !== idbData`-Bedingung automatisch in den zweiten Zweig (still LS nehmen); Task 1 testet das explizit.

    **(b) Konflikt-Dialog-Funktion (D-07)** — füge eine neue async-Funktion `showStorageConflictDialog(lsData, idbData, idbTs)` hinzu, die einen einmaligen Auswahl-Dialog zeigt (beide Stände mit Größe in KB und IDB-Speicherdatum) und ein Promise<boolean> auflöst (true = IDB nutzen). Nutze das bestehende Modal-Muster ODER ein dynamisch erzeugtes `<dialog>`/Overlay. Pflicht: alle Nutzer-Strings (Kampagnenname, falls angezeigt) mit `esc()` escapen. Beispiel-Gerüst:
    ```javascript
    function showStorageConflictDialog(lsData, idbData, idbTs) {
        return new Promise((resolve) => {
            const lsKB = (new Blob([lsData]).size / 1024).toFixed(0);
            const idbKB = (new Blob([idbData]).size / 1024).toFixed(0);
            const idbDate = new Date(idbTs).toLocaleString('de-DE');
            const overlay = document.createElement('div');
            overlay.className = 'modal show';
            overlay.innerHTML = `
                <div class="modal-content" style="max-width:480px">
                    <h3>⚠️ Unterschiedliche Speicherstände gefunden</h3>
                    <p>Es wurden zwei Versionen dieser Kampagne gefunden. Welche möchtest du laden?</p>
                    <div style="display:grid;gap:8px;margin:12px 0">
                        <button data-choice="ls" class="btn">Lokaler Stand (${esc(lsKB)} KB, ohne Datum)</button>
                        <button data-choice="idb" class="btn btn-primary">IndexedDB-Stand (${esc(idbKB)} KB, ${esc(idbDate)})</button>
                    </div>
                    <p style="font-size:0.85em;opacity:0.7">Dieser Dialog erscheint nur dieses eine Mal. Danach wird automatisch der neueste Stand geladen.</p>
                </div>`;
            overlay.addEventListener('click', (ev) => {
                const choice = ev.target?.dataset?.choice;
                if (choice) { document.body.removeChild(overlay); resolve(choice === 'idb'); }
            });
            document.body.appendChild(overlay);
        });
    }
    ```
    Passe Klassennamen an die echten Modal-Klassen der App an (siehe navigation.js/import-export.js Modal-Pattern). Exportiere die Funktion nur, falls außerhalb benötigt — sonst modul-intern lassen (CLAUDE.md Export-Audit-Regel). Deutsche UI-Texte (CONTEXT.md specifics).

    **(c) Export-Stempel-Fix (D-05)** — ersetze Zeile 133 `exp._version = '2.11';` durch:
    ```javascript
    exp._version = APP_CONFIG.VERSION;
    ```
    (`APP_CONFIG` ist in dieser Datei bereits über `window.APP_CONFIG` verfügbar; in `exportAllDataAsFile()` ist `const APP_CONFIG = window.APP_CONFIG;` bereits vorhanden — Zeile 118.)

    **(d) Legacy-'2.11'-Erkennung beim Laden (D-05)** — VOR der bestehenden Migrations-Prüfung (Zeile 63 `if (!p._version || compareVersions(...) < 0)`) einfügen:
    ```javascript
    if (p._version === '2.11') {
        p._version = '2.0.0'; // Altformat-Stempel → alle Migrationen laufen lassen
    }
    ```
    Begründung (Pitfall 3): `compareVersions('2.11', '2.6.1')` wertet 11 > 6 → ohne diese Normalisierung würden Legacy-Exporte die Migration überspringen.

    CLAUDE.md-Regeln: KEINE `const X = window.X` in Funktionen neu einführen; `esc()` für ALLE Nutzer-Strings im Dialog; keine doppelten Top-Level-Funktionsnamen (prüfe, dass `showStorageConflictDialog` nicht bereits existiert).
  </action>
  <verify>
    <automated>cd "D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular" && npx jest tests/unit/stability.test.js --no-coverage 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "exp._version = APP_CONFIG.VERSION" systems/spellslots/quick-roll.js` gibt `1` zurück
    - `grep -c "'2.11'" systems/spellslots/quick-roll.js` gibt mindestens `1` zurück (nur noch in der Legacy-Erkennung, NICHT im Export-Stempel)
    - `grep -c "loadFromIndexedDBFallbackRaw" systems/spellslots/quick-roll.js` gibt mindestens `1` zurück (Konflikt-Erkennung nutzt Raw-Read)
    - `grep -c "showStorageConflictDialog" systems/spellslots/quick-roll.js` gibt mindestens `2` zurück (Definition + Aufruf)
    - `grep -c "esc(" systems/spellslots/quick-roll.js` gibt mindestens `1` zurück (XSS-Schutz im Dialog)
    - `grep -c "lsData !== idbData" systems/spellslots/quick-roll.js` gibt mindestens `1` zurück (Identisch-Fall-Ausschluss D-07 + Dialog-Trigger-Bedingung gegen Pitfall 2)
    - ALLE neuen Tests aus Task 1 sind GRÜN: `npx jest tests/unit/stability.test.js --no-coverage` Exit-Code 0 (Export-Version, Legacy-Stempel, 5MB, Conflict-Trigger, Identisch-Fall, Begleit-Timestamp)
  </acceptance_criteria>
  <done>Load-Pfad vergleicht Timestamps und zeigt den Konflikt-Dialog nur beim echten Altdaten-Konflikt (nicht bei identischem Inhalt); Export stempelt APP_CONFIG.VERSION; Legacy-'2.11'-Importe migrieren korrekt; alle Regressionstests grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Importierte/geladene Kampagnendaten → App-Laufzeit | Geladene JSON-Strings (LS/IDB/Re-Import) werden geparst und ins D-Objekt gemerged; bestehende migrateData/validateDataIntegrity sind die Kontrollen. |
| Kampagnenname → Konflikt-Dialog-DOM (D-07) | Der neue Dialog rendert ggf. Nutzer-Strings via innerHTML — XSS-Risiko. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | Re-Import einer manipulierten Kampagnen-JSON (Legacy-Pfad) | mitigate | Legacy-'2.11'-Normalisierung erzwingt vollständigen migrateData-Durchlauf; bestehende validateDataIntegrity/esc bei Render bleiben aktiv |
| T-01-04 | Spoofing/XSS | showStorageConflictDialog (D-07) rendert Speicherstand-Infos | mitigate | Alle eingebetteten Nutzer-Strings mit `esc()` escapen (acceptance_criteria erzwingt `esc(`); nur numerische Größen/Datum sonst |
| T-01-05 | Information Disclosure | Datenverlust durch stillen Stale-Shadow | mitigate | Kern-Fix dieses Plans: LS-Schatten-Entfernung + Timestamp-Vergleich; D-03 macht IDB-Schreibfehler laut sichtbar |
</threat_model>

<verification>
- `npx jest tests/unit/stability.test.js --no-coverage` grün (alle neuen + bestehenden Tests, inkl. D-07-Identisch-Fall)
- `grep -c "exp._version = APP_CONFIG.VERSION" systems/spellslots/quick-roll.js` == 1
- `grep -c "StorageAPI.remove" systems/spellslots/persistence.js` >= 2
- Manuell (laut VALIDATION.md, optional): Kampagne >5MB anlegen, Browser schließen, neu öffnen — Daten vollständig vorhanden; Dialog erscheint NICHT bei normaler <5MB-Nutzung
</verification>

<success_criteria>
- >5MB-Kampagnen verlieren keine Daten mehr (STAB-05)
- Exporte tragen APP_CONFIG.VERSION; Re-Import von Legacy-'2.11' migriert korrekt (STAB-06)
- D-07-Dialog erscheint nur bei echtem Konflikt (nicht bei identischem Inhalt); kein Toast-Spam (D-02/D-08); lauter Fehler bei IDB-Schreibfehler (D-03)
</success_criteria>

<output>
Nach Abschluss: Erstelle `.planning/phases/01-stabilisierung/01-02-SUMMARY.md`
</output>
</content>
