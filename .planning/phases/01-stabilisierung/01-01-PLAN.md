---
phase: 01-stabilisierung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/debug.js
  - tests/e2e/smoke.spec.js
  - playwright.smoke.config.js
autonomous: true
requirements: [STAB-01, STAB-08]
nyquist_compliant: true

must_haves:
  truths:
    - "Die gebaute HTML lädt per file://-Doppelklick ohne pageerror in der Browser-Konsole"
    - "Ein Playwright-Smoke-Test bestätigt erfolgreiche App-Initialisierung gegen den dist-Build"
    - "Der Smoke-Test prüft 6 Haupt-Tabs auf crash-freies Laden"
  artifacts:
    - path: "tools/debug.js"
      provides: "Reparierte Debug-Datei ohne tote clearMindmap-Referenz"
      contains: "function clearAllWiki"
    - path: "tests/e2e/smoke.spec.js"
      provides: "Boot-Check + 6-Tab-Sweep gegen dist-Build"
      min_lines: 30
    - path: "playwright.smoke.config.js"
      provides: "Smoke-Test-Config mit HTTP-Server-baseURL (CI) und file://-Fallback (lokal)"
      min_lines: 15
  key_links:
    - from: "tests/e2e/smoke.spec.js"
      to: "dist/dnd-tracker-bundled.html bzw. SMOKE_BASE_URL"
      via: "page.goto + waitForSelector('.app-title')"
      pattern: "page\\.goto"
    - from: "playwright.smoke.config.js"
      to: "tests/e2e/smoke.spec.js"
      via: "testMatch auf smoke.spec.js beschränkt"
      pattern: "smoke\\.spec"
---

<objective>
Behebt den Boot-Crash (`clearMindmap`-Referenz in `tools/debug.js:99`), der die App beim Start zum Absturz bringt (STAB-01), und legt die Smoke-Test-Infrastruktur an, die diesen Crash-Typ künftig in CI erkennt (STAB-08, Scaffold).

Purpose: Die App muss überhaupt starten, bevor irgendein anderer Fix verifizierbar ist. Dies ist der Wurzel-Blocker der gesamten Phase. Der Smoke-Test ist der Nyquist-Regressionssensor für STAB-01 und STAB-08.
Output: Reparierte `tools/debug.js`, neuer `tests/e2e/smoke.spec.js`, neue `playwright.smoke.config.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stabilisierung/01-CONTEXT.md
@.planning/phases/01-stabilisierung/01-RESEARCH.md
@.planning/phases/01-stabilisierung/01-PATTERNS.md

<interfaces>
<!-- Bestehender Smoke-Test-Aufbau, den smoke.spec.js als Vorlage nutzt (aus tests/e2e/app.spec.js, role-match). -->
<!-- Executor nutzt diese Selektoren direkt — keine weitere Codebase-Exploration nötig. -->

Boot-Selektor (app.spec.js:16): await page.waitForSelector('.app-title', { timeout: 10000 });
Tab-Selektor (app.spec.js:46):  page.locator(`.nav-tab[data-view="${view}"]`)
Fehler-Filter (app.spec.js:32): errors.filter(e => !e.includes('favicon'))
Tab-Liste (app.spec.js:36-43): dashboard, party, npcs, locations, quests, encounter

Bestehende playwright.config.js baseURL (Zeile 41):
  baseURL: `file:///${process.cwd().replace(/\\/g, '/')}/dist/dnd-tracker-bundled.html`

Defekte Zeile in tools/debug.js (Zeile 99, im Kontext der Funktion clearAllWiki davor und runAllTests danach):
  // Alias für Rückwärtskompatibilität
  const clearAllNodes = clearMindmap;   // <- clearMindmap existiert nicht mehr → Boot-Crash
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: clearMindmap-Boot-Crash in tools/debug.js entfernen</name>
  <files>tools/debug.js</files>
  <read_first>
    - tools/debug.js (Zeilen 85-105 — die defekte Zeile 99 und ihr Umfeld; bestätige aktuellen Stand vor Edit)
    - .planning/phases/01-stabilisierung/01-PATTERNS.md (Abschnitt "tools/debug.js" — exakte Änderung)
  </read_first>
  <action>
    Entferne in `tools/debug.js` die zwei Zeilen (Kommentar + Deklaration):
    ```javascript
    // Alias für Rückwärtskompatibilität
    const clearAllNodes = clearMindmap;
    ```
    Diese stehen zwischen der Funktion `clearAllWiki()` (endet vorher) und `function runAllTests()` (beginnt danach). Begründung (D-11): `clearMindmap` existiert seit der Mindmap-Entfernung (Commit 7ef9bf5) nicht mehr; die `const`-Deklaration referenziert eine undefinierte Funktion und wirft beim Auswerten des gebündelten Skripts einen Boot-Crash. `clearAllNodes` wird nirgends mehr konsumiert (Mindmap-Feature ist weg) — ersatzlos streichen, KEIN Alias auf eine andere Funktion setzen.
    Nimm KEINE weiteren Änderungen an `tools/debug.js` vor — die Datei bleibt laut D-11 in beiden Bundles und wird nur repariert, nicht umgebaut. Achte auf die CLAUDE.md-Build-Regel: keine `const X = window.X` in Funktionen einführen.
    Nach dem Edit: Führe `python build.py` aus (Windows: `python`, nicht `python3`), damit `dist/dnd-tracker-bundled.html` neu mit dem Fix gebaut wird — der Smoke-Test in Task 2/3 prüft gegen diesen Build.
  </action>
  <verify>
    <automated>cd "D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular" && grep -c "clearAllNodes" tools/debug.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "clearMindmap" tools/debug.js` gibt `0` zurück (keine Referenz mehr)
    - `grep -c "clearAllNodes" tools/debug.js` gibt `0` zurück (Alias-Zeile entfernt)
    - `grep -c "function clearAllWiki" tools/debug.js` gibt `1` zurück (umgebende Funktion unverändert erhalten)
    - `grep -c "function runAllTests" tools/debug.js` gibt `1` zurück (Folgefunktion unverändert erhalten)
    - `python build.py` endet mit Exit-Code 0 und erzeugt `dist/dnd-tracker-bundled.html`
  </acceptance_criteria>
  <done>tools/debug.js enthält keine clearMindmap-/clearAllNodes-Referenz mehr; umgebende Funktionen intakt; frischer dev-Build erstellt.</done>
</task>

<task type="auto">
  <name>Task 2: playwright.smoke.config.js anlegen (HTTP-Server + file://-Fallback)</name>
  <files>playwright.smoke.config.js</files>
  <read_first>
    - playwright.config.js (Komplett — Vorlage für defineConfig, baseURL-Pattern, chromium-Projekt)
    - .planning/phases/01-stabilisierung/01-RESEARCH.md (Abschnitt "CI-Smoke-Test" — HTTP-Server-Begründung, SMOKE_BASE_URL)
  </read_first>
  <action>
    Erstelle `playwright.smoke.config.js` im Projekt-Root als eigenständige Playwright-Config, die NUR `tests/e2e/smoke.spec.js` ausführt (damit der bestehende `playwright.config.js` und die 26 bekannten E2E-Failures nicht importiert werden). Inhalt:
    ```javascript
    // @ts-check
    import { defineConfig, devices } from '@playwright/test';

    // Smoke-Config: in CI gegen lokalen HTTP-Server (SMOKE_BASE_URL gesetzt),
    // lokal Fallback auf file://-Doppelklick-Pfad (dev-Bundle).
    const BASE_URL = process.env.SMOKE_BASE_URL ||
        `file:///${process.cwd().replace(/\\/g, '/')}/dist/dnd-tracker-bundled.html`;

    export default defineConfig({
        testDir: './tests/e2e',
        testMatch: 'smoke.spec.js',
        timeout: 30 * 1000,
        expect: { timeout: 5000 },
        fullyParallel: true,
        forbidOnly: !!process.env.CI,
        retries: process.env.CI ? 1 : 0,
        workers: process.env.CI ? 1 : undefined,
        reporter: [['list']],
        use: {
            baseURL: BASE_URL,
            screenshot: 'only-on-failure',
            trace: 'retain-on-failure',
        },
        projects: [
            { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
        ],
        outputDir: 'tests/e2e/test-results-smoke',
    });
    ```
    Wichtig: `testMatch: 'smoke.spec.js'` (NICHT der gesamte e2e-Ordner) — der Smoke-Lauf darf NUR die neue Spec ausführen. `BASE_URL` wird in den Tests über `page.goto(BASE_URL)` bzw. relativ über die baseURL genutzt.
  </action>
  <verify>
    <automated>cd "D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular" && node -e "import('./playwright.smoke.config.js').then(m=>{if(!m.default)process.exit(1);console.log('config ok')}).catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - Datei `playwright.smoke.config.js` existiert im Projekt-Root
    - `grep -c "testMatch" playwright.smoke.config.js` gibt mindestens `1` zurück und der Wert ist `'smoke.spec.js'` (`grep "testMatch" playwright.smoke.config.js` enthält `smoke.spec.js`)
    - `grep -c "SMOKE_BASE_URL" playwright.smoke.config.js` gibt mindestens `1` zurück
    - Node kann die Config als ES-Modul importieren (default-Export vorhanden), Exit-Code 0
  </acceptance_criteria>
  <done>Eigenständige Smoke-Config existiert, beschränkt auf smoke.spec.js, mit CI-HTTP-Server- und lokalem file://-Fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: tests/e2e/smoke.spec.js anlegen (Boot-Check + 6-Tab-Sweep)</name>
  <files>tests/e2e/smoke.spec.js</files>
  <read_first>
    - tests/e2e/app.spec.js (Zeilen 1-53 — Boot-Test- und Tab-Sweep-Pattern, Fehler-Filter)
    - playwright.smoke.config.js (aus Task 2 — baseURL/SMOKE_BASE_URL-Mechanik)
    - .planning/phases/01-stabilisierung/01-RESEARCH.md (Abschnitt "Smoke-Test-Umfang" — Boot + 6 Tabs, >10 Tests vermeiden)
  </read_first>
  <behavior>
    - Test "App bootet ohne Konsolen-Fehler": page.goto(BASE_URL); waitForSelector('.app-title'); nach kurzer Wartezeit ist die Liste gesammelter pageerror-Meldungen (ohne 'favicon') leer.
    - Test je Tab (dashboard, party, npcs, locations, quests, encounter): Tab anklicken, kurz warten, keine pageerror-Meldung (ohne 'favicon'); Tab erhält Klasse `active`.
    - Wäre der clearMindmap-Crash noch vorhanden, schlüge der Boot-Test fehl (pageerror beim Init).
  </behavior>
  <action>
    Erstelle `tests/e2e/smoke.spec.js`. Nutze `process.env.SMOKE_BASE_URL` mit file://-Fallback (identisch zur Config, da Tests die baseURL auch direkt referenzieren können). Struktur:
    ```javascript
    // @ts-check
    import { test, expect } from '@playwright/test';

    const BASE_URL = process.env.SMOKE_BASE_URL ||
        `file:///${process.cwd().replace(/\\/g, '/')}/dist/dnd-tracker-bundled.html`;

    test('App bootet ohne Konsolen-Fehler', async ({ page }) => {
        const errors = [];
        page.on('pageerror', err => errors.push(err.message));
        await page.goto(BASE_URL);
        await page.waitForSelector('.app-title', { timeout: 15000 });
        await page.waitForTimeout(1000);
        expect(errors.filter(e => !e.includes('favicon'))).toHaveLength(0);
    });

    test.describe('Tab-Sweep', () => {
        const TABS = ['dashboard', 'party', 'npcs', 'locations', 'quests', 'encounter'];
        for (const tab of TABS) {
            test(`Tab ${tab} lädt ohne Crash`, async ({ page }) => {
                const errors = [];
                page.on('pageerror', err => errors.push(err.message));
                await page.goto(BASE_URL);
                await page.waitForSelector('.app-title', { timeout: 15000 });
                const tabButton = page.locator(`.nav-tab[data-view="${tab}"]`);
                await tabButton.click();
                await page.waitForTimeout(500);
                await expect(tabButton).toHaveClass(/active/);
                expect(errors.filter(e => !e.includes('favicon'))).toHaveLength(0);
            });
        }
    });
    ```
    Halte den Umfang exakt auf Boot + 6 Tabs (7 Tests) begrenzt — laut RESEARCH keine zusätzlichen Interaktionstests, um CI-Zeit und Fragilität der bekannten 26 E2E-Failures zu vermeiden.
  </action>
  <verify>
    <automated>cd "D:\AI_CLI\Claude\DnD_Tracker_App_Pro\dnd-tracker-modular" && npx playwright test --config=playwright.smoke.config.js</automated>
  </verify>
  <acceptance_criteria>
    - Datei `tests/e2e/smoke.spec.js` existiert
    - `grep -c "data-view" tests/e2e/smoke.spec.js` gibt mindestens `1` zurück (Tab-Sweep nutzt data-view-Selektor)
    - `grep -c "pageerror" tests/e2e/smoke.spec.js` gibt mindestens `1` zurück (Konsolen-Fehler-Erfassung)
    - `npx playwright test --config=playwright.smoke.config.js` läuft GRÜN gegen den in Task 1 gebauten dist-Build (alle 7 Tests passing, Exit-Code 0) — das beweist, dass der Boot-Crash behoben ist
  </acceptance_criteria>
  <done>smoke.spec.js existiert und läuft grün gegen den reparierten dist-Build; bestätigt boot-freie Initialisierung und 6 crash-freie Tabs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser-Laufzeit → gebündeltes Skript | Das gebündelte JS wird beim Laden ausgewertet; ein Syntax-/Referenzfehler (wie clearMindmap) bricht die gesamte App. Keine externen Eingaben in diesem Plan. |
| CI-Runner → dist-Artefakt | Smoke-Test lädt das gebaute Artefakt; manipuliertes Artefakt ist kein realistisches Bedrohungsszenario im Single-Dev-Offline-Modell. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Denial of Service | tools/debug.js Boot-Pfad | mitigate | Tote `clearMindmap`-Referenz entfernen (Task 1); Smoke-Test (Task 3) verhindert Wiedereinführung durch CI-Gate |
| T-01-02 | Tampering | Smoke-Test-Konfiguration | accept | Smoke-Config liest nur lokalen dist-Build/HTTP-Server-Output; kein Nutzer-Input, kein PII, Offline-Modell — geringes Risiko |
</threat_model>

<verification>
- `python build.py` Exit-Code 0; `dist/dnd-tracker-bundled.html` neu erzeugt
- `npx playwright test --config=playwright.smoke.config.js` grün (7/7)
- `grep -c "clearMindmap" tools/debug.js` == 0
- Manuell (laut VALIDATION.md, nicht CI-automatisierbar): `dist/dnd-tracker-bundled.html` per Doppelklick öffnen, Browser-Konsole prüfen — keine Fehler
</verification>

<success_criteria>
- App startet ohne pageerror (Boot-Crash behoben, STAB-01)
- Smoke-Test-Harness existiert und ist grün gegen den dist-Build (STAB-08-Fundament)
- Keine toten Mindmap-Referenzen in tools/debug.js
</success_criteria>

<output>
Nach Abschluss: Erstelle `.planning/phases/01-stabilisierung/01-01-SUMMARY.md`
</output>
